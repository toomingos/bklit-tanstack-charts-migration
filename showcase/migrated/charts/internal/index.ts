// Shared utilities barrel — charts import these via `./internal`.

// Margin normalization (shared lifecycle setup)
export { useChartMargin, DEFAULT_CHART_MARGIN, type ChartMargin } from "./use-chart-margin";
