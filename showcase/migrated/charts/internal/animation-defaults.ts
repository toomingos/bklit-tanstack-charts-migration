// Bklit legacy prop defaults — independent of REVEAL_* (legacy vs upstream sources; never alias).

const DEFAULT_ANIMATION_DURATION_MS = 1100;
const DEFAULT_ANIMATION_EASING = "cubic-bezier(0.85, 0, 0.15, 1)";

export { DEFAULT_ANIMATION_DURATION_MS, DEFAULT_ANIMATION_EASING };
