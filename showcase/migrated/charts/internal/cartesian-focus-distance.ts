// Finite whole-plot focus bound (larger than any display diagonal). No Delaunay index is wired:
// A focus strategy takes precedence over a definition index (focus-and-interaction.md:963, D544).
const CARTESIAN_MAX_FOCUS_DISTANCE_PX = 16_384;

export { CARTESIAN_MAX_FOCUS_DISTANCE_PX };
