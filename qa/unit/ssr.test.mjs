// V1.7 home: SSR fixture (definitions render real SVG at initialWidth;
// hydration adoption per 09 invariant 4).
import { test } from 'node:test';

test.todo('ssr/markup: every family renders real SVG at initialWidth 640');
