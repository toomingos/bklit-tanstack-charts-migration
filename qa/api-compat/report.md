# api-compat report (GENERATED by qa/api-compat/generate.mjs — do not edit)

Legacy barrel: 292 value exports, 211 type exports.
Migrated barrel: 275 value exports, 208 type exports.
tsc (pnpm api-compat): 36 red lines in all.ts across 36 exports — the measured V1.6 backlog.

## (a) missing / mismatched legacy exports by 10 §2 family

### Gradients and patterns (14)

- `GradientDarkgreenGreen` (value, `@visx/gradient`) — missing
- `GradientLightgreenGreen` (value, `@visx/gradient`) — missing
- `GradientOrangeRed` (value, `@visx/gradient`) — missing
- `GradientPinkBlue` (value, `@visx/gradient`) — missing
- `GradientPinkRed` (value, `@visx/gradient`) — missing
- `GradientPurpleOrange` (value, `@visx/gradient`) — missing
- `GradientPurpleTeal` (value, `@visx/gradient`) — missing
- `GradientSteelPurple` (value, `@visx/gradient`) — missing
- `GradientTealBlue` (value, `@visx/gradient`) — missing
- `LinearGradient` (value, `@visx/gradient`) — missing
- `RadialGradient` (value, `@visx/gradient`) — missing
- `PatternArea` (value, `./pattern-area`) — mismatch (TS2344)
- `PatternAreaProps` (type, `./pattern-area`) — mismatch (TS2344)
- `PatternLines` (value, `./visx-pattern`) — mismatch (TS2344)

### Loading and skeleton (9)

- `ChartLoadingLabel` (value, `./chart-loading-label`) — missing
- `ChartLoadingLabelProps` (type, `./chart-loading-label`) — missing
- `GenerateChartSkeletonDataOptions` (type, `./generate-chart-skeleton-data`) — missing
- `generateChartSkeletonData` (value, `./generate-chart-skeleton-data`) — missing
- `BarLoadingSkeleton` (value, `./loading-sweep`) — missing
- `BarLoadingSkeletonProps` (type, `./loading-sweep`) — missing
- `getSkeletonHeights` (value, `./loading-sweep`) — missing
- `LineLoadingSweep` (value, `./loading-sweep`) — missing
- `LineLoadingSweepProps` (type, `./loading-sweep`) — missing

### Geo, network, heatmap (7)

- `useChoroplethZoom` (value, `./choropleth`) — mismatch (TS2344)
- `HeatmapContextValue` (type, `./heatmap`) — mismatch (TS2344)
- `HeatmapProvider` (value, `./heatmap`) — mismatch (TS2344)
- `useHeatmap` (value, `./heatmap`) — mismatch (TS2344)
- `SankeyContextValue` (type, `./sankey`) — missing
- `SankeyProvider` (value, `./sankey`) — missing
- `useSankey` (value, `./sankey`) — missing

### Area, line, live-line (4)

- `LineChartLoading` (value, `./line-chart-loading`) — missing
- `LineChartLoadingProps` (type, `./line-chart-loading`) — missing
- `LineLoadingPulseStroke` (value, `./line-loading-pulse`) — missing
- `LineLoadingPulseStrokeProps` (type, `./line-loading-pulse`) — missing

### Brush (2)

- `ChartBrushSelectionOverlay` (value, `./chart-brush-selection-overlay`) — missing
- `ChartBrushTrackOverlay` (value, `./chart-brush-track-overlay`) — missing

## (b) migrated-only exports (V1.6 un-export inventory)

- `ChartSelectionContext` (value) — from `./internal/chart-selection`
- `BrushLayout` (value) — from `./internal/brush-layout`
- `ChartHost` (value) — from `./internal/chart-host`
- `ChartHostProps` (type) — from `./internal/chart-host`
- `ChartMargin` (type) — from `./internal/use-chart-margin`
- `clipRevealTransition` (value) — from `./internal/parity/animation`
- `transitionWithDelay` (value) — from `./internal/parity/animation`
- `ChartEnterTransition` (type) — from `./internal/parity/animation`
- `BrushSelection` (type) — from `./internal/parity/brush`
