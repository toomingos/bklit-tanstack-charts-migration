// A10 gate: prop-less on purpose (defaults are what's gated); only the import source changes.
import { AreaChartLoading } from "@migrated/charts";

export default function MigratedAreaLoading() {
  return <AreaChartLoading />;
}
