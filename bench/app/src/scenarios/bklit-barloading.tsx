// B14 gate: renders the legacy BarChartLoading preset directly (mechanism divergence, not parity).
import { BarChartLoading } from "@bklitui/ui/charts";

export default function BklitBarLoading() {
  return <BarChartLoading />;
}
