# Phase 4.3 — Go-To-Plan (merged synthesis → tasks)

Source of truth for 4.4 (staleness rule 4.4.4: once implementation starts, do NOT re-consult 4.1 reports for touched code).
Inputs merged: `synthesis/parity.md` (235 gaps), `synthesis/centralize.md` (15 opps), `synthesis/redundancies.md` (44 findings), `synthesis/tanstack.md` (44 native rows). D-logs: `docs/phase-3/LOG.md`, `docs/phase-4/LOG.md`.

## Governing doctrine (rulings applied throughout)

| # | Ruling | Rationale |
|---|---|---|
| DOC-1 | **Visual/interaction deltas vs legacy → FIX to legacy** unless already D-ruled. Migrated-only "improvements" that change pixels/interaction (HM16 ring, CP7 exit fade, SB13/14/15) are removed/aligned. | MAIN GOAL: exact same design & interactivity |
| DOC-2 | **Additive non-visual API extras → ACCEPT** (SB3 `onPhaseChange`, XAxis `formatValue`, P1 `style`, ariaLabel additions). | Superset API is seamless for legacy callers |
| DOC-3 | **Public barrel parity is a hard requirement.** Bench/QA/demo consumers through the barrel are real consumers; barrel-live "orphans" stay. Restoration tiering per T-E1. | MAIN GOAL: same components API |
| DOC-4 | **Context/provider architecture removals stay ACCEPTED** (D210-2 precedent): `*Provider`/`use*` internals not re-exported; thin legacy-named wrapper hooks added only where trivial (R3, R4, CP9/CP10 field restoration). Log as D240. | TanStack-native backend; store-based coordination already ruled |
| DOC-5 | **Native swap eligibility**: a TanStack-native swap ships only if pixel/interaction gates (Q1/Q2/G1–G4 + part spot-check) stay green; rows marked "parity break" in tanstack.md are NOT chased (funnel recipe, composed barY, waffle, areaY, interactiveColorLegend, text() halo, tick thinning). | Native backend, but design parity wins |
| DOC-6 | **Event-model modernization ACCEPTED** (mouse→pointer, added reduced-motion branches, pointer-events hardening). Log as D241. | Output-equivalent, a11y-positive |
| DOC-7 | **`enterTransition` structural reshape ACCEPTED family-wide** (framer `Transition` → structural `{tween|spring}` with same fallback timing). Log as D242. Exception: candlestick K4 gains sampled-keyframe tween support (legacy tweens dropped entirely there = behavior gap). | Framer types not reproducible in WAAPI world; timing parity preserved |
| DOC-8 | **Single-axis reality → FIX multi-axis** (L10 family): per-series `yAxisId` resolved in the one scale-resolution layer (custom; TanStack has no axis-id concept — confirmed). Largest single behavior task; own batch. | Silent wrong renders today violate MAIN GOAL |
| DOC-9 | **Bar pilot scope cuts** (B4 orientation, B5 stacked/stackGap/barWidth/squareSnap): ACCEPT for phase 4 with dev-only `console.warn` when set (honest API). Log as D243. `BarYAxis` (B13) ports with orientation later; barrel gets the type surface now. | Documented pilot boundary; warn keeps API honest |
| DOC-10 | **Loading family package ruling**: keep current design (D213/D214 lineage). FIX: wire `loadingStroke*` (L9), export `resolveLineLoadingPulseMode` (FD6), restore `AreaChartLoading`/`BarChartLoading` presets composed from existing parts (A10/B14). ACCEPT + log D244: per-series `loading` controls stay accepted-inert (L5/A7); sweep/skeleton surface (FD7) deleted; heatmap shimmer stays cut with `loadingCell*` props accepted-inert; grid shimmer props stay inert, `resolveGridShimmer` deleted (AX3). | Balance: visible loading design already ruled; dead unreachable surface goes |

## Open-question resolutions (40 OQs across 4 lenses → rulings)

| OQ | Resolution |
|---|---|
| parity 1 / AX2 | FIX — restore resolver default `horizontal: true` (grid.ts:33 → match legacy grid.tsx:97). Add a prop-less `<Grid />` QA scenario so gates stop being blind. |
| parity 2 / C5 | FIX — implement the stacked-sum branch (legacy behavior). Not strip: prop is real legacy API. |
| parity 3 / L10 | FIX per DOC-8 (batch W6). Types stay; per-axis domains resolved in `resolveTimeSeriesYDomain` layer; `createTickColorResolver` per-axis filter (AX7) fixed in same task. |
| parity 4 / SK1+SK2 | FIX both — one-line default restore; controlled hover via existing coordinator controlled mode. |
| parity 5 / FD12 | FIX via tiering (T-E1a/b/c): 276 missing symbols confirmed (item 7), not ~60. Mechanical re-exports (T-E1a), ports/wrappers (T-E1b), ACCEPT ledger D240/D244 (T-E1c). |
| parity 6 / HM14 | FIX — wire `pattern-preset` module into heatmap cell fills (props currently lie). |
| parity 7 | DOC-10. |
| parity 8 / CP4–6, CP9, CP10 | FIX — restore `intFmt` default, indexed fallback name, real index arg; restore hook contract fields (`useChoropleth` full payload, `useChoroplethZoom` isDragging/transformMatrix). |
| parity 9 / RD5, RD6, SB1, SB12, SB14 | FIX RD5 (restore bklit base-delay formula), RD7 (round caps); RD6 attempt transform tween, fallback ACCEPT+D245. FIX SB1 (wire playKey), SB12 (cull, + header), SB13 (interpolate), SB14 (commit immediately, keep midpoint-snapshot nuance), SB15 (350ms fade + svg pointerleave). |
| parity 10 | `animationEasing` policy: FIX — area consumes like line (A2); parts gaining stub props (W5) forward easing into WAAPI/motion timing where the engine supports it; document the mapping in one place (`enter-transition`). |
| parity 11 | DOC-6 (ACCEPT, D241). |
| parity 12 | Dead/inert wire-or-delete executed in W2 (delete) / W5 (wire): B11 wire-or-strip→strip+D243 note, B18–20 delete, C13+C14 delete (yAxisId→L10), F3 delete, HM4 strip, IN2 thread through, LM4 strip containerRef, LM10 un-export. |
| parity 13 / IN12 | RESOLVED (item 1): both constants match legacy exactly — no change needed. |
| parity 14 / D238 | Batch 0 blocker: fix barsquares tooltip absence + bardepth offsets BEFORE baseline capture (baseline must be parity-sane; deliberate deviation from 4.4.1 "before any change", justified: enshrining open regressions in the baseline would corrupt every later compare). |
| centralize 1–2 | DOC-3: barrel-live stays. `Background`: FIX-wire (CH13 — legacy had it wired; add carrier + host slot). `RingCenter`: include in shell merge but wire via real import (R7 symbolic check fix). |
| centralize 3 | RESOLVED (item 4): fallback bit-identical; bar delegates + dead ring-branch (bar-hover-chrome.ts:91–96) deleted. |
| centralize 4 | RESOLVED (item 6): all 7 `*EnterTransition` names survive collapse (4 pure aliases + 2 part-local + 1 heatmap-independent). |
| centralize 5 | YES — converge all inline `bkmRevealed` stamping on `deferred-reveal.checkRevealGuard`; add one svg-root variant helper for ring/radar/sunburst/choropleth. | **✅ CLOSED 2026-08-26 (D356) — executed as P6.3b; 33 sites across 9 files converged on `isRevealed`/`markRevealed`/`clearRevealed`/`findRevealRoot` over a `RevealRoot = HTMLElement | SVGElement` type, and the rewire exposed that the `instanceof HTMLElement` guards had made the stamp permanently dead. Superseded history below. ⚠ WAS OPEN — verified 2026-08-25 (D308). Do NOT read P4.4's `checkRevealGuard` ruling as closure: it answered a different question.** P4.4 was asked whether `checkRevealGuard` was *duplicated* and correctly found one impl (`internal/deferred-reveal.ts:39`) — but this row asks whether the inline stamping *converges on it*, and it does not. `checkRevealGuard` has **ZERO callers outside its own module** (only `deferred-reveal.ts:160` and `:227`), while ~30 inline `dataset.bkmRevealed` sites remain across **9** files: `area-chart.tsx` (:891,:895,:957), `line-chart.tsx` (:641,:646,:651,:715), `bar-chart.tsx` (:873,:888,:893), `candlestick-chart.tsx` (:828,:835,:837,:841), `composed-chart.tsx` (:1282,:1286,:1373), `scatter-chart.tsx` (:703,:707), `sunburst-chart.tsx` (:611,:625,:630,:635,:1021), `choropleth-chart.tsx` (:461,:463,:515), `internal/heatmap-components.tsx` (:638,:645). The svg-root variant helper for ring/radar/sunburst/choropleth is also unwritten. **Note `sunburst-chart.tsx:1021` DELETES the stamp (`delete svg.dataset.bkmRevealed`) — any converged helper must support un-stamping, which `checkRevealGuard` does not currently expose.** **OWNER ASSIGNED 2026-08-25 (D315): P6.2, and it must NOT run earlier.** Two changes land on this exact contract before it: P4.6 added a *third* guard shape to `scatter-chart.tsx` (a `hasRevealedRef` boolean + reveal deadline alongside the dataset stamp, D311), and **P5.5/T-E3 rewires the reveal contract itself** on L2/B3/C4/S3/CP2/SK6 — after which the guard key stops being a boolean and becomes `{signature, duration}` (sankey's `seenRevealKeyRef`, `sankey-chart.tsx:515-534`, is the target shape). Converging ~30 sites on `checkRevealGuard` *before* P5.5 would centralize a contract that is about to change, and would then have to be re-centralized — **the D308 error one level up: doing the work the row names while the question it answers has moved.** Sequence: P5.5 → P6.2.
| centralize 6 | YES — add plain `prefersReducedMotion()` util beside the hook (one-shot semantics preserved). |
| centralize 7 | Promote gauge double-rAF intro into `CenterShell` as opt-in prop (sole copy of legacy PieCenterShell logic; P6 wrapper then reuses it). |
| centralize 8 | Rows 2–3 (chrome skeleton, spring-runtime factory) DROPPED from plan — highest-risk, near-zero LOC gain, partially subsumed by native tooltip cutover (T-D8). Do not revisit. |
| redund 1–2 | DOC-3; `topSquareCenterY` + `computeSeriesBarRevealClipPadding` NOT deleted — re-exported via T-E1 (legacy-public utils). |
| redund 3 | FIX — resurrect wave: port animation loop from dead `bar-pulse-overlay.tsx` into `barPulseMark`, then delete the overlay file (B17). Pixel-gate vs bklit. |
| redund 4 | Keep accepted-inert (DOC-10 / D244). |
| redund 5 | RESOLVED (item 2): byte-identical — consolidate all 5 sites onto `shortDateFmt`. |
| redund 6 | RESOLVED (item 3): second block is verbatim subset — delete 928–984 wholesale; first copy authoritative. |
| tanstack 1 | Ruled: no native tickLabels color field (v0.14.0 verified); `createTickColorResolver` stays custom; line.md note was wrong. No other report relied on it (parity table already routes AX7 through L10). |
| tanstack 2 | YES — ship the one-line CSS alias block (`--ts-chart-N: var(--chart-N)`) as a compensating layer; makes palette row full-native with zero pixel change. |
| tanstack 3 | Pill-knob handle chrome stays permanently (bklit design); `brushX` supplies drag/selection mechanics only (T-D9). |
| tanstack 4 | Native tooltip cutover is per-part gated (T-D8): pill/date-pill/square-ring/fade-gradient stay custom; expected retire ≈ scheduler + box/placement/portal halves. Exact LOC determined at gate, not now. |
| tanstack 5–8, 11 | Implementation-gate criteria (written into T-D tasks as acceptance), not pre-verified: sunburst morph within 0.5% gate; sankey layoutLabels margins; svgAnimation gated-tween equivalence incl. reduced-motion snap; stagger {offset,each} only for linear formulas (non-linear keep delay fns); createChartSpring vs Euler within visual tolerance for {300,30}/{180,28}. Any failure → that swap reverts (DOC-5). |
| tanstack 9 | Keep `use-container-size` family; adopt host sizing only where no HTML overlay needs a rect (audit inside T-D10); add `onRender`-fed rect only if it removes ≥2 hooks. |
| tanstack 10 | Funnel native recipe REJECTED (visual parity break; DOC-5). No waiver. |
| tanstack 12 | Pin-locked; re-check only if the v0.14.0 pin moves. |

## Tasks

Severity/impact ordered within batches. Every task: files → acceptance → batch. Gates (all batches): `pnpm typecheck` + build green, bench compare vs baseline, QA pixel gates Q1/Q2/G1–G4 for touched parts, spot-check demos.

### Batch 0 — D238 blockers (pre-baseline)

| ID | Task | Files | Acceptance |
|---|---|---|---|
| T-01 | ~~Fix v0.14 regression: barsquares tooltip absent~~ **CLOSED (D238a)** — reclassified n=1000 density artifact, no code change; n=100 gates ALL PASS | — | done 2026-08-22 |
| T-02 | ~~Fix v0.14 regression: bardepth offsets~~ **CLOSED (D238a)** — same reclassification; n=100 settled 0.0030%, hovers 0.0005% | — | done 2026-08-22 |
| T-02b | Fix v0.14 regression: **sankey settled 2.4818%** (historic band 0.16–0.59%; diff = node columns + labels, links near-clean — node vertical layout/label drift, possibly interacting with 5a2c444 link reveal). Missed by synthesis; restored from D238 which names THREE regressions | sankey chart/marks (committed showcase code only — never patch clones, D232 rule) | sankey Q1 settled back within historic band; typecheck; zero console errors |

### Batch W0 — Baseline (4.4.1, after Batch 0)

| T-03 | Capture baseline | `docs/phase-4/benchmarks` | bench run + typecheck/build status + visual reference pass per chart recorded |

### Batch W1 — High-severity + surgical parity fixes

| ID | Task | Files | Acceptance |
|---|---|---|---|
| T-W1-1 | Grid `horizontal` default → true (AX2) + prop-less `<Grid/>` QA scenario | `internal/grid.ts:33`, candlestick/scatter/bar inline resolvers, qa scenario | prop-less Grid renders horizontal lines exactly as legacy grid.tsx:97 |
| T-W1-2 | Composed `stacked` sum branch (C5) + `stackGap` interface align (C6) | composed-chart, series-bar-mark | stacked scenario matches legacy stacked render |
| T-W1-3 | Sankey `labelOrientation` default → horizontal (SK1) | sankey-chart.tsx:386 | default labels horizontal (legacy sankey-node.tsx:314 parity) |
| T-W1-4 | Sankey controlled hover (SK2) `hoveredNodeIndex`/`onNodeHoverChange` | sankey chart + coordinator | legend-driven highlight scenario works as legacy |
| T-W1-5 | Choropleth palette → sequential `--chart-scale-01..05` (CP11) | choropleth colors | default map coloring matches legacy tokens |
| T-W1-6 | Choropleth defaults: intFmt, indexed fallback name, real index arg (CP4/5/6) | choropleth-chart | tooltip text identical to legacy for same data |
| T-W1-7 | Heatmap pattern fills wired (HM14) + separator gradient useId (HM7) + separator stroke `var(--border)` (HM6) | heatmap-components, pattern-preset | `pattern*` props render patterns as legacy; two instances no longer collide |
| T-W1-8 | Sankey fidelity trio: reveal keyed signature+duration only (SK6), tooltip dot tokens (SK7), node.value source (SK8), label ±8px slide (SK10) | sankey chart/tooltip | new-array data does NOT replay reveal; dots/labels match legacy |
| T-W1-9 | Remove migrated-only visual deltas (DOC-1): HM16 highlight ring, CP7 exit fade (restore flip+instant unmount via shared TooltipBox) | heatmap, choropleth | hover visuals indistinguishable from legacy |
| T-W1-10 | Reference-area `className` forward (RA1) | ReferenceAreaLayers | className lands on rendered node |
| T-W1-11 | Sunburst semantics: playKey (SB1), cull-on-hover (SB12), center radius interpolate (SB13), commit-immediately (SB14), svg fade+pointerleave (SB15) | sunburst chart/labels | side-by-side hover/zoom behavior matches legacy |
| T-W1-12 | Radar timing: RD5 base-delay formula, RD7 round caps; RD6 axis-label spring (fallback ACCEPT+D245) | radar chart/reveal | area campaign starts at legacy time; caps round |
| T-W1-13 | Candlestick: K7 animate gates reveal, K9 body patterns via pattern-preset, K10 insideStroke inset rect | candlestick chart/marks | props render as legacy |
| T-W1-14 | Gauge: G2 defsChildren mounted on arc overlay svg, G3 fixed-size bypass, G5 debounced measure | gauge | caller gradients work on arc; fixed-size = bare inline-flex |
| T-W1-15 | Scatter: S8 yGradient port, S9/S10 tunables plumbed, S11 drag-select via `useChartSelection`, S12 `_tooltipBoxSpring` honored | scatter chart/chrome | legacy scatter interactivity restored |
| T-W1-16 | LiveLine V1: `LiveYAxis position="right"` implemented | live-line axis | right-side axis renders as legacy |
| T-W1-17 | Markers: LM6 payload restore (+crosshair-hide interplay), LM7 `MarkerTooltipContent`/`useActiveMarkers` port, LM8 isActive wiring verify, LM11 fan scale 1.15 | legend-markers internals | marker hover behavior/pixels match legacy |
| T-W1-18 | Children: CH3 accept both `numTicksRows`/`numTicks`, CH4 `numTicksColumns` plumb, CH5 `tickMode` domain (+`tickerHalfWidth` forward B12), CH13 wire `<Background>` | children.tsx, grid, x-axis, background | legacy child props effective |
| T-W1-19 | Bar: B17 pulse wave resurrected into `barPulseMark` (then delete overlay) | bar-pulse-mark, bar-pulse-overlay | loading wave animates as legacy (pixel gate) |
| T-W1-20 | Area: A2 animationEasing consumed, A4 showLine toggle, A5 gradient stops, A8 fadeEdges directional, A9 touchAction, A3 type declaration | area chart/marks | legacy props effective; D13c respected |

### Batch W2 — Dead code + hygiene (delete-only; no behavior)

| ID | Task | Files |
|---|---|---|
| T-W2-1 | Delete: sweep/skeleton surface (FD7: `LineLoadingSweep`,`BarLoadingSkeleton`,`generateChartSkeletonData`,`getSkeletonHeights`), `resolveGridShimmer`, heatmap loading internals, `bar-pulse-overlay.tsx` (after T-W1-19) | loading-chrome, grid.ts, heatmap-animation |
| T-W2-2 | Delete dead vars/branches: `earlyRenderData` (L12), bar `columns`+voids (B18–20), composed dead import (C13), `input.width`, `hoverInputsRef` (F3), phantom `bandWidth` read, `ScatterHoverChromeState.hoveredIndex`, `groupEl` shim (R8), scatter dup memos, per-chrome `tooltipBoxSpring` voids ×4 (IN2 threads the real one) | per finding list redundancies.md |
| T-W2-3 | Delete orphaned pie-center CSS + duplicate styles.css loading block (after verification item 3) + `:has()` scoping fix (FD16) | styles.css |
| T-W2-4 | Stale comments batch (pie:35, focus-disabled header, gauge.tsx:21, styles.css:548, grid.ts:7-9, sunburst-labels header — after SB12 makes it true) + G6 headers + P11 | comments only |
| T-W2-5 | Un-export MARKER_* dead consts (LM10/series-marker-mark) — behavioral owner stays hover-chrome; values verified legacy-correct (item 1), no alignment needed | series-marker-mark, hover-chrome |
| T-W2-6 | live-line wake-loop collapse; ring fake-Animation sentinel + single cleanup registration | live-line, ring |
| T-W2-7 | **Pie hover-state parity** (lead amendment D249 — NOT delete-only): close the fixed ~3.18% hover-30/hover-70 pixel delta while hover-50 is exactly 0.0000% and settled is clean. Must land before P3.1, whose gate is "zero-drift on pie/scatter/sankey/choropleth/sunburst". | pie-chart, internal/pie-* |

**Amendment note (D249):** T-W2-7 is a lead addition, not from the 4.2 synthesis. Pie's hover failure was one of the three 4.4.1 baseline gate failures but — unlike heatmap (T-W1-9/HM16) and choropleth (T-W1-9/CP7) — no task was ever chartered to close it. D248 flagged it as unowned; this row assigns it. It sits in the W2 *batch slot* for scheduling reasons only; W2's delete-only rule does **not** apply to it.

### Batch W3 — TanStack-native swaps (each gated; failure → revert that swap)

| ID | Task | Native | Acceptance |
|---|---|---|---|
| T-D1 | motion() defaults replace reveal constants (1100ms/cubic-bezier) across 7 parts | `motion()` defaults | zero pixel drift on reveal gates |
| T-D2 | `focusDisabled` native (gauge switch; delete `focus-disabled.ts`) + `defineChart focus:false`/`axis:false` CSS suppression cleanup | `/focus/disabled` | focus behavior unchanged |
| T-D3 | Linear stagger formulas → `stagger({each,offset})` (linear only; non-linear keep delay fns) | `/motion/definition` | reveal timing byte-equal per formula check |
| T-D4 | Grid guides + highlight → `axis.grid`+`ruleY`; funnel bands/lines → `bandX/bandY`+`ruleX/ruleY` | rules/bands | pixel gates green |
| T-D5 | objectBoundingBox gradients → `spec.gradients` (area/composed/segment/gauge/fade-mask candidates; userSpace/PL/sankey-link stay custom) + brush clipPath → `spec.clip` | `/svg/resources` | gradient pixels identical |
| T-D6 | `findNearestPoint` replaces `resolveNearestIndex`/bisect at hover resolution (composed dual-index stays) | scene | hover targeting identical |
| T-D7 | y-domain tween → `svgAnimation` (gated-tween equivalence + reduced-motion snap = acceptance) | ChartAnimationOptions | tween fires only when niced domain moves |
| T-D8 | Native tooltip cutover per-part (placement/offset/motion/portal); pill/date-pill/square-ring/fade-gradient stay custom; retire scheduler + box halves per part only when that part's pixel gate passes | `tooltip` option | per-part QA gates |
| T-D9 | `brushX` mechanics + permanent pill-knob handle chrome; 2-finger gesture stays custom | `/interaction/brush` | brush drag/selection behavior identical |
| T-D10 | `createChartSpring` replaces `spring.ts` integrator. **RE-SCOPED (D268 §1-4) — the old criterion "~~where tolerance passes ({300,30}/{180,28})~~" is REPLACED.** Those two pairs are literally `TOOLTIP_SPRING`/`HIGHLIGHT_SPRING` (`design-tokens.ts:29,31`) — **2 of ~11 distinct configs**, and not the contested ones. Others: `TOOLTIP_BOX_SPRING` {100,20} (remapped ≈[12–500], `chart-config-context.tsx:63-100`), `ENTRANCE_SPRING` {300,25}, `TICK_SPRING` {180,24}, `LABEL_DIM_SPRING` {300,24}, funnel per-ring family {300−i·60, 24−i·3} (`funnel-geometry.ts:97-99`), inline {400,35} (`tooltip-chrome.ts:537-538`), inline {300,25} (`heatmap-components.tsx:848`), radar {80,15,mass:1}, `GAUGE_SPRING_FALLBACK` {300,20,1}, candlestick parametric (`candle-spring.ts:68-135`), `springFromBounce` (`enter-transition.ts:71-79`). **NOT a config: `sankey-animation.ts:31-39`'s spring fields are dead** — `resolveTiming` (`:44-54`) never branches on them. Native `createChartSpring` accepts arbitrary `stiffness/damping/mass/restSpeed/restDelta` (defaults 170/26/1/0.01/0.005), **no bounce/duration shorthand** — a numeric **superset**, so expressiveness is not the blocker. **Two real obstacles:** (a) **architecture inversion** — native is a *stateless analytic sampler* (`sample(elapsedMs,{from,to,velocity})`, `spring.ts:57-121`) where the caller owns the rAF loop; migrated `internal/spring.ts:38` is a *stateful self-driving* object (`set`/`jump`/`stop`) → needs an adapter at **13+ call sites**; (b) **different numerical method** — semi-implicit Euler @120 Hz with two adaptive rest tiers (`:23-27,52-70,82-84`) vs closed-form → curves are visually identical but **not bit-identical**. **SCOPE: only the already-closed-form / WAAPI-sampled springs** — candlestick, gauge fallback, radar label, `enter-transition`'s spring path. **rAF-driven hover springs EXCLUDED** unless an adapter is built and proven separately; **ring EXCLUDED outright until P4.3 lands its oscillation fix** (D258 — swapping the integrator under a loop that never settles makes both changes unattributable). Host sizing adoption audit (tanstack 9 ruling) unchanged | `/spring` | **Per-config**: name each config swapped and assert resolved `{stiffness,damping,mass,restDelta,restSpeed}` match. **Not** membership in two pairs. Ring gates need hover captures (D255/D256/D258) |
| ~~T-D11~~ | ~~Hover dim → mark `states when:{focus:'unmatched'}`~~ — **CLOSED NON-VIABLE (D269). Do not re-open in Phase 4.** Four verified blockers: (1) `ChartMarkStateStyle` has **no `filter`** (`mark-state.ts:271-279`), and marker/scatter dim is an inseparable opacity+`blur(2px)` pair → scatter 100% excluded; (2) the shipped renderer collapses all per-mark durations to one `Math.max` (`mark-state.ts:292-306` → `svg-surface.ts:118-128`), which would retime composed's bar 0.12s up to 0.4s and **break C11**; (3) **decisive** — dim has a second trigger, **legend hover**, a React context (`chart-legend-hover.tsx:3-11`) outside the SVG pointer system, interleaved across 13 sites in `hover-chrome.ts` (`:273,348,371,421,426,501,529,537,550,632,641,695`); native `states` sees only `ChartFocusState` and `ChartProps` has no controlled-`focus` input → **no chart is single-trigger enough for even a partial swap**; (4) native JS `ease-in-out` is quadratic (`reconcile.ts:334-341`), not bklit's `cubic-bezier(0.42,0,0.58,1)`. **Custom hover-chrome STAYS — this is now its documented justification.** Future bridge `interaction.setControlledFocus` (`dom-types.ts:66-88`) exists but gates tooltip pinning → **Phase 5 focus-model scope, not here** | ~~states~~ | **n/a — closed** |
| T-D12 | Sunburst native: `sunburst()` layout + motion sweep + `path:'morph'`; labels overlay stays; hit-test via paint order; d3-arc via `RadialArcOptions.generator` (SB17) | `/hierarchy/sunburst` | 0.5% gate incl. drill-down zoom; else revert |
| T-D13 | Sankey native: `sankeyDiagram()` layout + `text()` labels (layoutLabels margins) + `link()` widths (keep vector-effect override); per-link gradients stay custom | `/network/sankey` | sankey pixel gate |
| T-D14 | Misc natives: heatmap tooltip motion+offset (row 39), date coercion channels (row 41), funnel stage keys (42), `binTimeX` (25), regression (26), `fold` (27), brush `axis.viewport` filter (13), brush-layout → `view` grid (11) | various | per-part gates |
| T-D15 | Palette alias layer: `--ts-chart-N: var(--chart-N)` + `defaultChartTheme.palette` adoption (5 parts) | theme | zero color drift |

### Batch W4 — Centralization (post-native residue)

| ID | Task | Files |
|---|---|---|
| T-C1 | Mapper kit: canonical `toDot/Indicator/BoxConfig` + `nextChromeId()` in `tooltip-chrome.ts`; 5 chromes delegate (bar keeps signature; gap-4 fallback bit-parity per verification item 4). Only for chromes surviving T-D8 | hover chromes |
| T-C2 | Focus-strategy kit: shared `valueKey`/`collectPerGroup`/`navigation`/phase-gate; `resolve` stays per-part | focus strategies |
| T-C3 | Reveal-shim collapse into `enter-transition` (5 shims; barrel aliases keep every public name per verification item 6); gauge keeps `reconcileGaugeReveal`; radar keeps `bklitRadarGrid` (file renamed) — largely subsumed if T-D1 lands first | reveal shims |
| T-C4 | `CenterShell` merge (pie/ring/gauge centers as data-in adapters; gauge intro opt-in; RingCenter wired via real import — R7) | center modules |
| T-C5 | Single-importer folds ×5 (`tooltip-scheduler`→tooltip-chrome [if survives T-D8], `hover-reanchor`→hover-chrome + shared `bisectDateLeft` per verification item 5, `x-ticks`→x-axis-overlay, `visx-pattern-bridge`→pattern-preset, `radar-spring`→enter-transition) | listed |
| T-C6 | Legend hover context unify (`chart-legend-hover` canonical; profit-loss re-export alias) + `intFmt` via formatters | legend modules |
| T-C7 | Broadcast store generalization (`createBroadcastStore<T>`; pie/heatmap instantiate) | coordinators |
| T-C8 | Bar squares math → `bar-squares-layout` (`bandWidthForSquares` ×3 consolidation) | bar modules |
| T-C9 | `XAxisLabelOverlay` shared presentational layer (tick algorithms untouched) | axis overlays |
| T-C10 | Inline dedup batch: `shortDateFmt` ×5 (per verification item 2), `prefersReducedMotion()` util + ~10 sites, `useSanitizedId` + 20 sites, reveal-guard convergence + svg-root helper, `xForDate` helper ×6, reveal-constant tokens (1100/easing/500/margin {40×4}), `SegmentComponent`/`ChartMarker` canonical imports, heatmap levelStyles builder + tooltip JSX single-branch, choropleth zoom-writer merge, brush clamp helper + single blurPx, sunburst `maxRevealDelayMs` consume export, `Symbol.for` → `CHART_ROLE` import | listed sites |

### Batch W5 — API surface restoration

| ID | Task | Files |
|---|---|---|
| T-E1a | **Barrel restore — mechanical tier (FD12)**: re-export the missing TYPE (97) / CONST (18) / HELPER-FN (91) symbols whose backing module survives in migrated code (cssVars families, heatmap helper block, sunburst geometry post-T-D12 survivors, `DEFAULT_HOVER_OFFSET`, `detectMomentum`/`Momentum`, `ArcGeometry`, `computeReferenceAreaRect`, `LoadingStyle`, `resolveLineLoadingPulseMode`, `topSquareCenterY`, `computeSeriesBarRevealClipPadding`, `LineLoadingPulseMode`, bar helper/type blocks, y-domain helpers, …). Worksheet = the 276-symbol categorized list from verification item 7. Symbols with no surviving backing → route to T-E1b or T-E1c | charts/index.ts + shim modules |
| T-E1b | **Barrel restore — port/wrapper tier**: COMPONENT (40) case-by-case — visx `Gradient*` ×11 + `Pattern*` ×4 re-exports (visx still a dep via pattern bridge; else thin ports), `TooltipBox`/`TooltipContent`/`TooltipDot`/`TooltipIndicator`/`DateTicker`/`ChartRevealClip`/`ChartStatFlow`/`SeriesMarkers`/`SeriesPointMarker`/`MarkerGroup`/`Background` (wired by T-W1-18)/`ChartBrush*` overlays (live per D228); HOOK (22) — restore store-backed equivalents via legacy-named wrappers (`useChart*`, `usePie*`, `useRing*` R2–R4, `useRadar*`, `useSankey`, `useSunburst*`, `useChoropleth*` CP9/CP10, `useActiveMarkers` LM7, `useChartInteraction`, `useYScale`/`useAnimatedYDomains` post-T-F1). Cross-refs: `SunburstBreadcrumb` = T-E6, `AreaChartLoading`/`BarChartLoading` = T-E5, `BarYAxis` = DOC-9, `PieCenterShell` = T-E4 | charts/index.ts + parts |
| T-E1c | **Barrel ACCEPT ledger**: CONTEXT-PROVIDER (8) + `*ContextValue` types + hooks with no migrated equivalent → ACCEPT per DOC-4 (D240); `LineLoadingSweep`/`BarLoadingSkeleton`/`generateChartSkeletonData`/`getSkeletonHeights`/`GenerateChartSkeletonDataOptions`/`useStaticChartPreview`+Provider → ACCEPT per DOC-10 (D244). Every ACCEPTed symbol named explicitly in the D-log entry. CH17 export-drift audit folds in here. **⚠ D266 ruling — this row and T-F2 both claim D240 + D244, and both write `docs/phase-4/LOG.md`. Split by CONTENT: this package writes the SYMBOL LEDGER only (the enumeration of record) as its own standalone entry, and must NOT label itself D240/D244 — those numbers are stale per D263 §7 (D240 is an unrelated Wave-0 bench entry; D241–D245 don't exist). Take a next-available number and cite the stale suggestion inline. T-F2 owns the architectural rulings and will cross-reference this ledger instead of re-listing symbols. D263 §3's "earlier package owns the file" doctrine does NOT apply — that settles same-lines file overlaps; this is two genuinely different deliverables the plan happened to file under one number.** | docs/phase-4/LOG.md |
| T-E2 | Default exports restored (~10: line, area, composed, scatter, candlestick, ring ×2, radar ×6, ChartLegend, brush, K3, C2…). **D268 §5 — premise CONFIRMED, method RULED, and my old "one `export default` per file blocker" flag was stated backwards.** `ring-chart.tsx`/`radar-chart.tsx` contain **zero** `export default`, so no uniqueness constraint is hit there — in that framing the flag was a phantom. **The real constraint:** legacy is one-component-per-file each with one default (radar ×6: `radar-chart:262`, `radar-labels:80`, `radar-grid:128`, `radar-context:174`, `radar-area:225`, `radar-axis:58`; ring ×4: `ring-chart:491`, `ring-context:188`, `ring:235`, `ring-center:119`) **and legacy's `package.json` carries a WILDCARD subpath export `"./*": "./src/*.tsx"`** — so those defaults are publicly reachable as `import RadarGrid from "@bklitui/ui/charts/radar-grid"`. Migrated consolidated 6 files into 1, and 6 defaults cannot coexist in one file. **RULING: satisfy with thin default-re-export SHIM MODULES at legacy's paths** (`radar-grid.ts` → `export { RadarGrid as default } from "./radar-chart"`), **not by splitting the consolidated files** — zero pixel risk, and T-E1a already anticipates shim modules. **GATE: P5.4 must FIRST verify whether migrated ships an equivalent wildcard subpath map** — if not, the deep-import surface is unreachable and T-E2 shrinks accordingly. Also **RD2 "radar ×6" is 5 actionable**: the 6th is `radar-context`/`RadarStableContext`, a context provider routed **ACCEPT** by DOC-4 | part entries |
| T-E3 | Stub-prop family: `enterTransition`/`revealSignature`/`animationEasing`/`animate` accepted+wired across line (L1/L2), bar (B1–B3), composed (C3/C4/C8), scatter (S1–S3/S7), choropleth (CP1/CP2 wire epoch + patterns CP3), sunburst (SB2), candlestick K4 sampled-keyframe tween | part hosts + orchestrator |
| T-E4 | Renamed-compat: CH2 `CHART_CLIP_PASSTHROUGH` alias, `PieCenterShell` thin wrapper over CenterShell (P6), legacy-named hooks `useRing` (R4)/`useRingHover`-compat (R3), `useRingStable` payload restore (R2), `useChoropleth`/`useChoroplethZoom` field restore (CP9/CP10), `MarkerTooltipContent` exports, BR3 `BrushSelectedBoxStyle` passthrough extend | listed |
| T-E5 | Loading presets: `AreaChartLoading`/`BarChartLoading` composed from existing parts (A10/B14); `loadingStroke*` wired (L9); L11 `xDomainSlotCount` pass-through. **⚠ D266 ruling — do NOT add FD6 (`resolveLineLoadingPulseMode`) here, even though DOC-10 (`:19`) mentions it alongside this row's items. It is already assigned to T-E1a (`:154`), which names the symbol explicitly; DOC-10 rules the loading family's *substance* while FD6's *mechanism* is a barrel re-export, which is T-E1a's remit. Adding it here would produce a duplicate export in two packages. This row's omission of FD6 is correct.** | loading modules |
| T-E6 | Sunburst surface: SB4 `Hint` render-prop + context, SB8 `SunburstBreadcrumb` family port | sunburst |
| T-E7 | Dev-warn layer for accepted pilot cuts (DOC-9: B4/B5 set → single console.warn in dev) | bar entry |
| T-E8 | Measurement debounce parity: **pie (P9) ONLY** on debounced hook. **⚠ D267 ruling — the gauge (G5) half ALREADY LANDED in Wave 1 via T-W1-14 (`:93`): `gauge.tsx:151-152` imports `useDebouncedContainerSize`/`useDebouncedContainerWidth`, used at `:382`/`:984` with explicit "G5 (bklit ParentSize debounceTime={10})" provenance comments. Do NOT re-implement it. Pie's half IS live — `pie-chart.tsx` has no `useDebounced*` usage at all (lead-verified, not assumed).** | measure hooks | **✅ SATISFIED AND OVER-DELIVERED — verified 2026-08-25 (D308).** The row's "**pie (P9) ONLY**" scope is stale: P4.3 put ring and radar on the hook too (`ring-chart.tsx:275`, `radar-chart.tsx:231`), and pie's half is live. **Five** charts now measure through `useDebouncedContainerSize`: gauge, line, pie, radar, ring. Gauge line refs in the D267 note have drifted — it is `gauge.tsx:157` (import) and `:388` (use), not `:151-152`/`:382`. Nothing left to do here; do not re-open.

### Batch W6 — Multi-axis + closing

| ID | Task | Files |
|---|---|---|
| T-F1 | **Multi-axis `yAxisId` (DOC-8)**: per-axis domain/scale resolution in scale layer; consumers line/area/bar/scatter/composed/reference-area; AX7 tick-color per-axis filter; AX4 `yScaleDomainMax` param re-add | scale resolution layer |
| T-F2 | D-log entries: D240 (provider architecture), D241 (event model), D242 (enterTransition reshape), D243 (bar pilot scope), D244 (loading package), D245 (radar label spring, if fallback), + every ACCEPT row from parity.md not already D-ruled. **⚠ D266 ruling — the suggested numbers D240–D245 are STALE and must not be used (D263 §7): D240 already exists as an unrelated Wave-0 bench entry, D241–D245 are absent. File under next-available numbers, citing the stale suggested number inline for traceability. This row shares D240 + D244 with T-E1c (P5.3): write the ARCHITECTURAL RULINGS only and CROSS-REFERENCE P5.3's symbol ledger rather than re-listing symbols. Also per D263 §8: P1.8's RD6/D245 status is genuinely unresolved — PROGRESS.md implies the radar-label fallback shipped but no confirming entry exists. VERIFY before logging and report the gap rather than log speculatively; an unsourced entry that reads plausibly is worse than a missing one.** | docs/phase-4/LOG.md |
| T-F3 | Final pass (4.4.5): full build + bench compare + parity spot-check ALL charts vs baseline | — |

## Verification results (executor session 57d5b1a8, 2026-08-22 — all 7 VERIFIED)

- Item 1 (IN12 constants): **legacy match confirmed** — `0.12s` = series-bar.tsx:253, `1.35` = series-markers.tsx:196. No value alignment needed; T-W2-5 is un-export only.
- Item 2 (shortDateFmt): **byte-identical** (same locale+options, no timeZone; ECMA-402 equivalence). T-C10 consolidation safe.
- Item 3 (styles.css): second block (928–984) is a **verbatim subset** of the first (846–926, which additionally owns the shimmer keyframes + scoped base rule). Delete second wholesale; first is authoritative. T-W2-3 confirmed.
- Item 4 (bar toDotConfig): fallback **byte-identical** to canonical; ring-only branch at bar-hover-chrome.ts:91–96 is dead (no `bandWidth` field ever set). T-C1 delegation safe; delete dead branch in the same task.
- Item 5 (bisectDateLeft): **equivalent** incl. duplicate-tie case (both leftmost); only accessor shape and `>>` vs `>>>` differ. T-C5 dedupe safe.
- Item 6 (EnterTransition names): **all 7 survive shim collapse** — 4 are pure `= EnterTransition` aliases (one aliases module covers Pie/Ring/Gauge/Funnel), Candlestick/Radar are self-contained part-file interfaces, Heatmap doesn't touch the shims. T-C3 confirmed feasible.
- Item 7 (FD12): **276 missing symbols** (legacy barrel 503 vs migrated 297; the ~60 estimate counted components only), + 70 migrated-only additions, 0 default exports on either barrel (the ~10 missing default exports in parity rows are part-file level — T-E2 unaffected). **D268 §5 confirms this reading is correct and supplies the mechanism that makes T-E2 real: legacy's `package.json` wildcard `"./*": "./src/*.tsx"` exposes those part-file defaults as public deep imports. Not a contradiction — see T-E2 row for the shim-module ruling.** **D268 §7 — NEW real gap, already assigned:** legacy's barrel exports `RadarProvider`/`useRadar`/`useRadarHover`/`useRadarStable`/`radarCssVars`/`defaultRadarColors`/`RadarContextValue` (`repos/bklit-ui/.../charts/index.ts:454-464`) + Ring equivalents (`:481-489`); migrated re-exports **none**. Covered by **T-E1b**'s hook tier → no new task. Cheap half: **`useRingStable`/`useRingHoverCoordinator` already EXIST** (`ring-chart.tsx:119,129`) and are merely un-barrelled — a one-line fix, not a port. Category counts: 40 COMPONENT / 97 TYPE / 91 HELPER-FN / 22 HOOK / 18 CONST / 8 CONTEXT-PROVIDER. Full categorized list preserved at `research/phase-4/fd12-barrel-worksheet.md` (T-E1 worksheet).

## Definition of done

All tasks closed or skipped-with-reason (logged in `docs/phase-4/LOG.md`), typecheck + build + bench pass, final spot-check matches baseline (which itself matches legacy after Batch 0/W1).

## Batch execution order (4.3.2)

Ordering audit + execution waves. Doctrine/OQ rulings are NOT relitigated here. Gate shorthand — **All** packages: `pnpm typecheck` + build green. Extras per package: **Q1/Q2** = pixel gates for named parts · **bench** = compare vs baseline for named scenarios · **CE** = console-errors pass · **RF** = revert-that-swap-on-fail (DOC-5). Baseline (P0.2) is the compare root for everything after it.

### Coverage audit findings

Method: every row of the four synthesis docs cross-checked against the task tables + doctrine table + OQ-resolution table above. Rulings accepted as-is; only holes/contradictions listed.

**Orphans — finding with no task/ruling (lead to disposition in W6/T-F2, do not silently patch):**

| Finding | Doc | Note |
|---|---|---|
| C11 composed per-series `dimOpacity` (area 0.6 / line 0.3 vs chart-wide 0.3) | parity | Suggested FIX, never ruled; no task covers it. Interacts with T-D11 hover-dim swap. |
| C15 composed terminal/end-anchor math ×3 dup | parity | Not covered by any W-batch (T-C10 lists other inline dedups only); redundancies.md xForDate row names composed-chart only. |
| AX5 axis label position tweens (left/top static jump) | parity | Suggested FIX (CSS transitions); absent from all batches. |
| LM1+LM13 QA hook `__qaSetMarkerFan` shipped in prod bundle | parity | FIX-half ("QA-hook gating") has no task; the ACCEPT-half is implicitly covered by barrel rows. |

**Contradictions — internal plan inconsistencies (flagged, not patched):**

| # | Conflict | Note |
|---|---|---|
| K-1 | T-W2-2 says "per-chrome `tooltipBoxSpring` voids ×4 (IN2 threads the real one)" while redundancies.md finding says drop the per-chrome option entirely (keep live `ChartTooltipConfig.boxSpringConfig`) and OQ parity-12 routes IN2 to W2 delete/W5 wire. Wire-vs-delete must be picked before T-W2-2 runs. | |
| K-2 | OQ parity-8 restores CP7 tooltip flip+instant unmount via shared TooltipBox (T-W1-9), but T-D8's custom-retained list includes "fade-gradient" chrome for choropleth's neighborhood; if CP7 lands first the retained-chrome inventory for choropleth shrinks. Not a ruling conflict — a sequencing note: T-W1-9 output feeds T-D8 scoping. | |

Everything else maps cleanly: all 40 OQs resolve into named rulings/tasks; FD12's 276 symbols are fully partitioned across T-E1a/b/c (+T-E2/E4/E5/E6 cross-refs); redundancies.md items 1–44 all route to W2/W4/T-C10 or are ruled (DOC-3/DOC-10/verification items); tanstack.md rows map to T-D1–D15 or explicit DOC-5 rejections (rows 32/33/34/35/38/40).

### Coverage resolutions (decision-maker rulings, 2026-08-22)

| Finding | Ruling |
|---|---|
| C11 per-series `dimOpacity` | **FIX — LANDED + ACCEPTED (D265 §3)** on the existing **custom** mechanism (`composed-chart.tsx:194,220,269,285`; `hover-chrome.ts:62,113,184,373,428,645`). ~~Folded into P3.3 (T-D11) acceptance criteria: native `states` dim must reproduce per-series opacities in composed.~~ **That coupling is void — T-D11 is closed non-viable (D269), and blocker 2 shows the native path would have BROKEN C11**: the shipped renderer merges all per-mark durations to a single `Math.max`, so composed's bar 0.12s would have been retimed to line/area's 0.4s. C11 is correct *because* it stayed custom. |
| C15 terminal/end-anchor math ×3 | **FIX** — centralize into one helper; added to T-C10 list (runs in P4.4). |
| AX5 axis label position tweens | **FIX** — restore CSS position transitions on axis labels; added to T-W1-18 (P1.13, axis/children cluster). |
| LM1+LM13 `__qaSetMarkerFan` in prod | **FIX** — gate the QA hook behind a dev/QA flag (stripped or inert in prod bundle); added to T-W1-17 (P1.12). |
| K-1 `tooltipBoxSpring` wire-vs-delete | **Ruled: single live path.** Per-chrome duplicated option DELETED (the ×4 voids, per redundancies.md); part-level props (e.g. scatter `_tooltipBoxSpring`, S12 in T-W1-15) wire into the one live `ChartTooltipConfig.boxSpringConfig` path. T-W2-2's "(IN2 threads the real one)" is superseded by this ruling: W2 deletes per-chrome options; wiring happens only at the part→config boundary. |
| K-2 CP7 → T-D8 scoping | Sequencing note accepted — no ruling needed; wave order (P1.4 before P3.9) already guarantees it, and P3.9's choropleth sub-run recomputes its retained-chrome inventory at gate time. |
### Dependency graph

Notation `X → Y` = X must land before Y. Hard edges (file-cluster or behavior dependency):

| Edge | Why |
|---|---|
| T-01, T-02 → T-03 | Batch 0 blockers precede baseline capture (parity 14). |
| T-03 → everything after | Baseline is the compare root for all gates (W0 rule). |
| T-W1-18 → T-E1b (`Background` re-export) | Barrel re-export needs the wired module; re-exporting an unwired orphan would ship a dead component. |
| T-W1-19 → T-W2-1 | Overlay file deleted only after pulse loop resurrected into `barPulseMark`. |
| T-D1 → T-C3 | Reveal-shim collapse is "largely subsumed" if motion() defaults land first — order avoids collapsing then rewriting. |
| T-D1 → T-E3 | Stub props forward easing into the engine; shape depends on the landed motion() defaults family-wide. |
| T-D8 → T-C1 | Mapper kit consolidates "only chromes surviving T-D8". |
| T-D8 → T-C5 (tooltip-scheduler fold) | Fold happens only if scheduler survives the cutover. |
| T-D12 → T-E1a (sunburst geometry block) + T-C10 (`maxRevealDelayMs` item) | Native sunburst decides which geometry helpers survive to be re-exported/consumed. |
| T-F1 → T-E1b (`useYScale`/`useAnimatedYDomains` wrappers) | Wrappers wrap the post-multi-axis scale layer. |
| T-F1 → T-C10? no — T-C10's `xForDate`/scale-adjacent items in line/composed touch the same hosts F1 rewires → sequence C10 after F1 | Avoid double-touching scale-resolution consumers. |
| T-W1-9 (CP7 TooltipBox restore) → T-D8 scoping | Choropleth retained-chrome inventory shrinks after CP7 restore (audit K-2); run W1 first regardless per macro order. |
| T-W1-11 (SB semantics) → T-D12 | SB14 commit-immediately/SB13 interpolate define the behaviors native sunburst must reproduce before its gate can be meaningful. |
| **T-W1-11 reveal-replay invariant (F1) → T-D12 (P3.6)** | **Lead cross-ref, added mid-Wave-1.** F1 traced the sunburst hover regression to `handleRender` re-running the **full 1100ms staggered mount reveal on every hover-in** (the `bkmRevealed` dataset guard is deleted at hover time; P1.7's imperative `playKey` replay effect is the prime suspect). Two consequences for the native swap: (1) the invariant **"reveal plays on mount and on `playKey` change, never on hover"** joins SB13/SB14 as a behavior T-D12 must reproduce — state it in P3.6's gate spec explicitly, because a settled-only comparison cannot detect its violation; (2) sunburst's gate must include **hover** captures at **n=27 and n=33**, since `settled` was byte-identical (0.0599%) throughout this entire regression and would have passed a settled-only gate. |
| T-W1-13 (K7 animate-gates-reveal) → T-E3 candlestick K4 sampled-keyframe tween | Both edit candlestick reveal path; K7 defines the gate the tween plugs into. |
| T-W2-x → T-C4/T-C10 (styles.css items) | T-W2-3 deletes pie-center CSS/dup block before centralization passes touch styles.css again. |
| T-D15 (palette alias) → any part whose QA compares colors against baseline with new palette adoption | ~~Alias layer is zero-pixel by design but must exist before parts adopt `defaultChartTheme.palette`.~~ **CORRECTED by the lead 2026-08-24 (see `wave-tasks/P3-01.md`): the alias layer is NOT zero-pixel, and a CSS alias alone cannot make it so.** `defaultChartTheme.palette` has **six** entries (`scene.ts:58-71`, `--ts-chart-1..6`); all five adopting parts (pie `pie-chart.tsx:87`, sunburst `:62/:375/:383`, sankey `sankey-chart.tsx:133`, scatter, choropleth) cycle **five** (`--chart-1..5`). Index-cycling therefore diverges from index 5 on — and aliasing `--ts-chart-6: var(--chart-1)` does not fix it either, since a 6-long cycle `[c1..c5,c1]` falls out of phase with a 5-long cycle at index 6 (`c1` vs `c2`) and never recovers. **Cycle length is the defect, not token naming.** P3.1 must pass an explicit 5-entry `theme.palette` override (resolved at `scene.ts:301-306` / `facet.ts:814` / `runtime.ts:22`) rather than adopt the native default, preserve sankey's literal fallbacks (`var(--chart-1, #7c3aed)`), and add an alias block only if a native surface is found actually consuming `--ts-chart-1..6`. All five gate densities (pie/scatter 1000, sankey 33, sunburst 27) are far past 6 categories, so this would have shipped visible color drift. |
| T-E1c (ACCEPT ledger) last of E-batch | Ledger must name final surviving surface after E1a/b/E2–E7 dispositions. |
| T-E5 → T-E1b cross-ref resolution | `AreaChartLoading`/`BarChartLoading` presets are built (E5) before barrel rows resolve. |
| T-W1-16 (LiveYAxis right) → T-F1? no | Independent: live-line axis position doesn't use the y-axis-id scale layer; parallel-safe. |
| T-D6/T-D7/T-D10 → T-C5 (`hover-reanchor` fold) | Hover-resolution machinery churns in D-batch; fold single-importer modules only after their importer stabilizes. |
| **T-W1-9 (HM16 half / F3 dim-mechanism residual) → T-D11 (hover dim states, P3.3)** | **Lead cross-ref, added mid-Wave-1.** F3's investigation found the heatmap residual is not a leftover highlight ring but a *dim-mechanism* divergence: legacy drops the cell rect's **own opacity to 0.3**, migrated composites a **background-colored overlay rect at 0.7** on top — equivalent over white except at antialiased edges. Whatever F3 lands is therefore a hand-written dim mechanism sitting exactly where T-D11 is chartered to install a **native** states-driven one. ~~P3.3 must (a) treat F3's fix as the parity spec to reproduce, not as code to preserve, and (b) re-gate heatmap at **n=52** after the native swap~~ — **VOID (D269): T-D11 is closed non-viable, so no native swap will ever land here and F3's fix is now permanent code, not an interim parity spec.** Heatmap's own-opacity dim (`heatmap-components.tsx:~497-500`, `HEATMAP_INACTIVE_OPACITY` via `heatmap-hover-chrome.ts:31`) is the final mechanism and stays. Verified untouched by P3.3b, so **no re-gate is owed**. The **n=52 density mandate still stands for every future heatmap gate** — n=1000 is a symmetric harness artifact and must not be "fixed". Note heatmap has the same legend-coupling that killed T-D11 (`heatmap-hover-chrome.ts:2-5`). |

| **Ring moving-hit-area latency (audit, D255) → T-D10 (P3.12) and T-C4 (P4.3)** | **Lead cross-ref, added end of Wave-2.** A repo-wide audit (triggered by pie/sunburst both proving to be the same defect) found exactly one remaining AFFECTED site: ring binds `pointerenter` to the **same** track group its hover spring scales (`ring-chart.tsx:725` ↔ `ring-hover-chrome.ts:104`). Scaling an annulus about its center makes the **inner** edge recede ~`0.03 × innerRadius` (≈3–4px), and unlike pie there is no sibling surface behind the hole to catch an ejected cursor. **Ruled ACCEPT, not FIX (D255): legacy bklit has the identical structure** (`repos/bklit-ui/.../ring.tsx:179-188`), so this is inherited, not migration drift, and "fixing" it would be a deviation under DOC-1. **But it is spring-driven and geometry-coupled, which makes it exactly what P3.12 perturbs**: T-D10 swaps the spring integrator for `createChartSpring`, and any change to overshoot or settle timing changes how long the inner edge sits displaced. Two requirements: (1) **P3.12's ring gate must include hover captures**, not settled-only — an oscillation is invisible to a settled diff, and pie held a stable 3.18% hover delta for four runs without being identified; (2) if ring hover ever fails a gate, **test this first** rather than re-deriving it: Playwright-hover at coordinates just inside the track's inner rim and count `pointerenter`/`pointerleave` churn on the track group over ~2s. The parity-neutral hardening, if it is ever needed, is to bind the listeners to an **unscaled wrapper `<g>`** around track+progress — zero pixel change, kills the ejection. Same watch applies to P4.3 (T-C4 touches ring's center//RingCenter wiring). **UPDATE (D256, same day): requirement (2) has already fired — ring FAILED the Wave 2 gate sweep, and the full 48-run history shows its hover captures are *bimodal* (either ≈0.09 or ≈0.50 at n=1000, either ≈0.07 or ≈0.32 at n=4, nothing between) while `settled` is deterministic in every run ever. Two discrete values = a binary state sampled at capture time, which is what an oscillation predicts. Not a Wave 2 or Wave 1 regression — it is present in the earliest runs on record (2026-07-31), and at `n=100` it has failed *every* run since 2026-08-18 at 1.6–1.7%. The n=1000 bad mode sits astride the 0.5% gate, which is why it reads as flaky. Dispatched as **F4** (`research/phase-4/wave-tasks/F4-ring.md`), scoped diagnosis-first: the ACCEPT ruling stands unless F4's evidence overturns its premise, and F4 must discriminate between both-sides-oscillate (→ gate recommendation, no code), migrated-only (→ the unscaled-wrapper fix above), a non-oscillation cause, or a startup race producing a persistent hover-state disagreement with zero churn. Whatever F4 concludes, P3.12 and P4.3 must still gate ring with hover captures.** **RESOLVED (D258, 2026-08-24): F4 answered — discriminator (b), migrated-only. The ACCEPT ruling is OVERTURNED; ring is a genuine defect.** Measured at the harness's own probe (108,164): at the mandated **n=1000**, bklit shows churn `at=0 tf=11` and a byte-identical settled state across reps, while migrated shows `at=28–32 tf=95–100` and **38 distinct transform signatures still cycling in the final second** — it never settles, and the two reps freeze at different spring positions (1.00247 / 1.00658). n=100 is the same but harder (migrated churn 50–56 vs bklit 0). That fully explains D256's bimodality: the t=700 ms capture samples a random phase of a loop with no steady state. **D255's premise is contradicted by measurement and must not be re-cited:** its structural claim is correct (legacy does bind enter/leave to the springing `motion.g`, `ring.tsx:177-220`) but the inference is false — legacy does **not** oscillate at any density tested (n=4/20/100/1000, churn `at=0` every time). Three constraints for P4.3/P3.12: **(i)** the prescribed unscaled-wrapper `<g>` fix addresses the oscillation but **not** the second finding — at **n=4** (the most realistic density, and where no oscillation occurs on either side) bklit shows the full push-out while **migrated ends completely flat**, because bklit acquires hover on the harness's steps:10 fly-in and retains it resting in the centre hole whereas migrated releases; that gap is open and unruled. **(ii)** migrated's wiring stamps (`cursor`, `transformOrigin`, `pointerEvents:"none"`) all read **empty on the live DOM** while its listeners still fire — TanStack rewrites the style attribute after the wiring effect, so the progress-group `pointer-events` guard the code comment depends on is not holding. **(iii)** Do **not** re-investigate pivot semantics: measured and ruled out — both impls pivot about the ring centre (bklit via Framer's fill-box `50% 50%` → `72/90/108/126px`; migrated satisfies `dx = −dw/2`, `dy = −dh/2` exactly on every row), so hover is pure radial growth with zero translation in both. |

| **Scatter M3a update-path cost (Wave 2 gate, D257) → T-C2 (P4.2)** | **Lead cross-ref, added end of Wave-2.** Migrated scatter n=1000 M3a went **34.3 → 65.5 ms against its own frozen 4.4.1 baseline** (`bench/results/2026-08-23T22-25-41-661Z`; baseline `2026-08-22T15-46-19-823Z`). Read it precisely before acting: bklit's M3a is pinned at 32–33 ms across all 210 samples — that is **exactly two 16.67 ms frames, vsync-quantized**, not 32 ms of work — while migrated scatters 52–104 ms, so the true statement is *migrated overruns to ~4 frames where bklit holds 2*. On the same run migrated is **faster** than bklit at mount (96.9 vs 121.8), settle (1191.9 vs 1270.5) and total script (149.0 vs 564.3 — a 3.8× advantage), so this is one metric, not a broad regression. Probably **not Wave 2's**: the same baseline's migrated M1b of 138.7 vs today's 1191.9 is an 8.6× 'regression' that is really *parity progress* (migrated now runs the ~1.2 s reveal bklit runs at 1264 ms), and the M3a rise is plausibly that same animation work landing on the update path. **Requirement: P4.2 must include a scatter bench cell** — T-C2 rewrites focus strategies, which is exactly the update path in question. If T-C2's native swap collapses M3a back toward the baseline, this closes itself; if it does not, it becomes a Phase-4-final item. Do not chase it before P4.2. |
| **`state=loading` pixel gate is near-blind (Wave 2 gate, D257) → every future loading-state gate** | **Lead cross-ref, added end of Wave-2. Read this before trusting any loading-state PASS.** Step 2 of the Wave 2 gate reported line 0.0531% / area 0.0525% / heatmap 0.3783%, all PASS and all byte-comparable to pre-Wave-2 baselines. I opened the probe captures instead of accepting the numbers, and the two impls are **visibly different in three ways**: bklit's "Loading data" label is **bold and letter-spaced** where migrated's is regular-weight and unspaced; bklit renders a **diagonal sweep glint** upper-right that migrated has no trace of; and the **y-gridlines sit at entirely different positions** (bklit ≈65/175/290/398/505, migrated ≈137/232/327/420/507 — a different placeholder y-domain while loading), even though the x-axis labels align exactly. That scored **510 diff px of 960,000**. The loading surface is all near-invisible low-contrast chrome, so structurally different renders both pass at 0.05%. **Consequence: a loading-state PASS is not evidence of loading-state parity.** Any ruling that depends on one must be backed by looking at the captures. Evidence: `qa/results/line/2026-08-23T21-46-53-518Z/{loading-a,loading-b,loading-diff}.png`. The divergence itself **predates Wave 2** (present in the pre-Wave-2 baselines; P2.1's deletions were verified pre-dead at HEAD) — it needs a parity ruling of its own, but it is not Wave 2's debt. |
| **`GridConfig` inert `shimmer*` fields — ALREADY RULED, do not re-open (Wave 2 gate, D257 → DOC-10/AX3)** | **Lead cross-ref, added end of Wave-2, then corrected the same night. Recorded so nobody re-litigates a settled ruling.** The gate-runner flagged five `shimmer*` fields (`shimmer`, `shimmerStroke`, `shimmerLength`, `shimmerSpeed`, `shimmerSync`) as an orphaned config surface with zero implementation references, and I first elevated it to "needs an API-parity ruling." **Both readings were wrong: `DOC-10` already ruled it** — *"grid shimmer props stay inert, `resolveGridShimmer` deleted (AX3)"*. P2.1 executed that ruling exactly. The fields are **inert by design, kept for API parity**; they are not orphans and not a cleanup target. Two corrections to the runner's report while here: the fields live in **`internal/types.ts:204-213`**, not `types.ts`; and legacy's counterpart `useGridShimmer` (`repos/bklit-ui/packages/ui/src/charts/use-grid-shimmer.ts`) is a **live, fully-implemented motion/react hook** — so the props mirror a real legacy feature that migrated deliberately does not implement. That asymmetry is the ruling, not a defect. Related already-ruled rows, likewise closed: **FD7** sweep/skeleton surface deleted (DOC-10), **HM13** heatmap shimmer cut (DOC-10), **FD5** `ShimmeringText`→CSS label (ruled FIX-className-or-ACCEPT, low). Separately and still genuinely open — the one item no ruling names: **the y-gridline positions differ between impls during `state=loading`** (bklit ≈65/175/290/398/505, migrated ≈137/232/327/420/507 — a different placeholder y-domain), which is chart *geometry*, not loading chrome, and so is not covered by DOC-10. Low severity, invisible to the pixel gate. Rule it at P5.7 (T-E5 loading presets) where the loading path is open anyway. Unrelated hole found by the same gate: **funnel at n=1000 emits ~1000 `viewBox: 0 0 -2.944` errors and bklit emits 1017 of the identical errors** — symmetric bklit-native artifact, faithfully mirrored, not a parity defect, but funnel-at-1000 had never been console-swept. Widen `qa/console-errors.mjs`'s fixed list when convenient. |

Soft edges (same-cluster hygiene, ordered within a wave where noted): T-W2-4 sunburst-labels header fix waits on T-W1-11 (SB12 makes it true); T-W2-5 un-export independent; verification-item-backed tasks (T-C1 gap-4, T-C5 bisect, T-C10 shortDateFmt, T-W2-3 CSS) carry their proof already and need no further gating.
### Waves

Macro order 0 → W0 → W1…W6 respected; individual tasks moved only where dependency/file-cluster conflicts warrant (every move logged below). Package IDs: P<batch>.<n>. "Executor-sized" = one focused run per package.

**Wave 0 — pre-baseline**

| Pkg | Tasks | File clusters | Gate |
|---|---|---|---|
| P0.1 | T-01 + T-02 (both bar-family v0.14 regressions, same cluster) | bar squares mark/chrome wiring; bar depth marks | Q1/Q2 barsquares+bardepth, CE |
| P0.1b | T-02b (sankey v0.14 settled regression — split out of P0.1: different cluster, open investigation `docs/phase-4/notes-p01b-sankey.md`) | sankey chart/marks (committed showcase only, D232) | Q1 sankey settled within historic band ×2 runs, typecheck, CE |
| P0.2 | T-03 baseline capture — per **D246** split into lanes B1 (typecheck+build) / B2 (visual refs, 24 non-sankey parts) / B3 (console sweep), launchable pre-P0.1b-close; B4 (`bench --all` exclusive + sankey refs) after P0.1b closes | `docs/phase-4/benchmarks` | full bench snapshot recorded |

**Wave 1 — W1 parity fixes, split by file cluster (all parallel-safe)**

| Pkg | Tasks | File clusters | Gate |
|---|---|---|---|
| P1.1 | T-W1-1 (grid default) | `internal/grid.ts`, inline resolvers, qa scenario | Q1/Q2 candlestick+scatter+bar prop-less Grid scenario |
| P1.2 | T-W1-3 + T-W1-4 + T-W1-8 (sankey cluster) | sankey chart/coordinator/tooltip | Q1/Q2 sankey incl. controlled-hover scenario |
| P1.3 | T-W1-2 (composed stacked+stackGap) | composed-chart, series-bar-mark | Q1/Q2 composed stacked scenario, bench composed |
| P1.4 | T-W1-5 + T-W1-6 + T-W1-9 choropleth half (CP11 palette, CP4–6 defaults, CP7 exit-fade restore) | choropleth colors/chart/chrome | Q1/Q2 choropleth |
| P1.5 | T-W1-9 heatmap half (HM16 ring removal) + T-W1-7 (HM14 patterns + HM7/HM6 separators) | heatmap components, pattern-preset | Q1/Q2 heatmap |
| P1.6 | T-W1-10 (RA1 className) | ReferenceAreaLayers | spot-check reference-area |
| P1.7 | T-W1-11 (sunburst semantics quintet) | sunburst chart/labels | Q1/Q2 sunburst hover/zoom |
| P1.8 | T-W1-12 (radar RD5/RD7/RD6) | radar chart/reveal | Q1/Q2 radar timing |
| P1.9 | T-W1-13 (candlestick K7/K9/K10) | candlestick chart/marks | Q1/Q2 candlestick |
| P1.10 | T-W1-14 + T-W1-15 (gauge G-cluster; scatter S8–S12) | gauge; scatter chart/chrome | Q1/Q2 gauge+scatter |
| P1.11 | T-W1-16 (LiveYAxis right) | live-line axis | Q1/Q2 liveline |
| P1.12 | T-W1-17 (markers LM cluster) | legend-markers internals | Q1/Q2 markers |
| P1.13 | T-W1-18 (children CH cluster + Background wire) | children.tsx, grid consumer sites, x-axis, background.tsx | Q1/Q2 line/children scenarios, CE |
| P1.14 | T-W1-19 (bar pulse resurrect → delete overlay in W2) | bar-pulse-mark (+overlay read-only until W2) | Q1/Q2 bar pixel gate, bench bar |
| P1.15 | T-W1-20 (area A-cluster) | area chart/marks | Q1/Q2 area |

Note: P1.4/P1.5 keep T-W1-9 split by part so heatmap and choropleth packages don't share files.

**Wave 2 — W2 dead-code hygiene (delete-only; parallel-safe, all after Wave 1)**

| Pkg | Tasks | File clusters | Gate |
|---|---|---|---|
| P2.1 | T-W2-1 (loading surface + overlay deletion, post-T-W1-19) | loading-chrome, grid.ts shimmer, heatmap-animation internals, bar-pulse-overlay.tsx | typecheck/build, bench line+bar, CE |
| P2.2 | T-W2-2 (dead vars/branches sweep) + T-W2-5 (MARKER_* un-export) + T-W2-6 (live-line/ring cleanup) | per finding list redundancies.md; series-marker-mark/hover-chrome; live-line/ring | Q1/Q2 smoke across touched parts, bench scatter+ring+liveline |
| P2.3 | T-W2-3 (styles.css orphans + dup block + `:has()` scoping) + T-W2-4 (stale comments batch, incl. sunburst header now true post-T-W1-11) | styles.css; comments only | typecheck/build, visual spot-check styles.css consumers |
| P2.4 | T-W2-7 (pie hover-state parity — lead amendment D249; not delete-only) | pie-chart, internal/pie-* | solo `pie --n 1000`, all four captures ≤0.5% |

**Wave 3 — W3 native swaps (RF applies to every package; run D-batch mostly serial-ish by cluster)**

| Pkg | Tasks | File clusters | Gate |
|---|---|---|---|
| ~~P3.1~~ **DONE (D260)** | **T-D15 first** (palette layer — see D259: NOT a zero-pixel CSS alias; an explicit 5-entry `theme.palette` override) | `internal/design-tokens.ts` shared constants + pie/sunburst/sankey/scatter adoption sites | **PASSED zero-drift**: pie 0.0141 / scatter 0.4509 / sankey 0.1092 / sunburst 0.0599 / choropleth 0.0000, all byte-identical to history. **choropleth EXCLUDED — it cycles the separate sequential `--chart-scale-01..05` family, not the categorical set (D260)** |
| P3.2 | T-D1 (motion() defaults ×7 parts) | enter-transition/design-tokens consumers | Q1/Q2 reveal gates all parts |
| ~~P3.3a~~ **DONE (D265)** | T-D2 (focusDisabled native) **+ C11 per-series `dimOpacity`** | gauge's 2 call sites → native import; `focus-disabled.ts` **deleted** (lead, D216); `hover-chrome.ts` + `composed-chart.tsx` per-series dim | **PASSED zero-drift**, 6 of 7 charts byte-exact vs baseline; heatmap hover-70 −24 px (0.0025%). Typecheck exit 0 |
| ~~P3.3b~~ | **CLOSED NON-VIABLE (D269) — no code change, none possible. See T-D11 row for the four verified blockers.** Historical framing below retained only to show what was believed before: ~~**T-D11 (hover dim → native `states`) — STILL OPEN.** P3.3 shipped C11 and skipped the mechanism swap; **zero `states:`/`unmatched` in the tree** (D265 §0). Green gate ≠ delivered swap — for mechanism tasks, grep for the mechanism. **Viability confirmed** (native marks + `onFocusGroupChange` focus state); risk is transition-timing desync for markers' co-transitioned opacity+blur~~ — **that lead ruling was OVERTURNED by measurement; "viability confirmed" was wrong.** The **grep-for-the-mechanism rule (D265 §0) still stands** and is unaffected | ~~`internal/hover-chrome.ts`~~ **none — zero files edited** | **Closed with reason (D269)**; "documented non-viability" was one of the three acceptable outcomes in the brief |
| P3.4 | ~~T-D3 (linear stagger)~~ **LANDED + ACCEPTED (D271)** | `internal/native-stagger.ts` (new), `pie-chart.tsx:500,538`, `ring-chart.tsx:536,579,580`, `candlestick-chart.tsx:920`, `gauge.tsx:818,826,1108,1117`, `composed-chart.tsx:1322`, `internal/chart-markers.tsx:367`, `showcase/tsconfig.json` (alias), `internal/index.ts` (barrel) | **MET.** Byte-equality proven by importing the real `stagger()` from clone source vs original formulas, 48 cases incl. both `enterStaggerScale` clamp boundaries — bit-identical or <1e-9. Chosen over pixel capture because D264 §5 holds: no mid-reveal capture mode exists and `--state loading` forces `prefers-reduced-motion: reduce`, suppressing the tween under test. Typecheck 0 + build 0 + settled clean ×6. Non-linear kept custom: sunburst (→P3.6), sankey, heatmap |
| ~~P3.5~~ | ~~T-D4 (grid guides/rules/bands)~~ **LANDED + ACCEPTED (D274)** — scope collapsed by D272: grid guides were **already native** (`resolveGridGuide()` → `axis.grid`/`axis.ticks.count` across 6 cartesian charts), and funnel bands/lines closed **NON-VIABLE under D30** (pixel-arithmetic `<rect>`/`<line>`, no scale to map through, no standalone mark renderer) with **zero funnel edits**. Real deliverable was one file | `internal/grid-highlight-mark.ts` (71→48 lines; `createMark()`+`kind:"polyline"` → native `ruleY` at `:37`, import `:13`), `line-chart.tsx:297` (call site; `innerWidth`/`translateX`/`translateY` dropped as now-derived) | **MET.** tsc 0 / build 0. **First native-rule consumer in the tree.** Gate corrected by the executor: `line` sets no `highlightRowValues` so it cannot exercise the swap — **`profitloss` is the only harness scenario that does** (0.0192% settled, hovers ≤0.1482%, within ±0.0002pp of a byte-stable multi-day baseline); line 0.0000%, funnel 0.0000% ×4 proving the non-viable half unregressed. Style defaults + non-finite-y guard preserved verbatim; y-domain and focus-neutrality verified at clone source. Zero orphans |
| ~~P3.6~~ | ~~T-D12 (sunburst native swap)~~ **SPLIT — ACCEPTED (D276)**. **A: full native `sunburst()` mark swap = NON-VIABLE**, both brief-named overturn conditions met at source: native `path:'morph'` is scene-diff machinery with no external progress input (`motion.ts:2394-2417`, `hierarchy-sunburst.ts:301-305`) so `transitionGeometry` cannot be sampled at arbitrary `zoomT`; and the native resolver hit-tests **live painted (grown)** geometry (`nearest.ts:91` `if (affinity === 'geometry') continue` → `:79,153-168`), so `sunburst-hit.tsx` **kept**. **B: row-44 generator stub-dance = LANDED** | `sunburst-chart.tsx` only (3 hunks): `:546-554` real `d3Arc()` with pinned `.padAngle(() => 0)` replacing `Object.assign(gen, {startAngle: stub, …})` (greps to zero); `:507-509` degenerate cull re-added, thresholds byte-identical to retired `sunburst-geometry.ts:277`. Barrel + clones untouched | **`TSC_EXIT=0`; build + QA deferred to the lead's consolidated sweep (D275).** Reveal-replay ref-identity verified intact (`playCycleRef:401`, `seenRevealedRef:403` — heap state, unstrippable by `syncAttributes`); SB13 `:857-863` / SB14 `:439-441` unchanged. Sweep must add: ≥6-segment hover sweep with no settle pause (the D251/F1 window), and **arcs with `innerR < 1` now render as true annulus not origin-wedge — sub-pixel center delta, lead-found** |
| P3.7 | T-D13 (sankey native) | sankeyDiagram/text/link | Q1/Q2 sankey; RF |
| P3.8 | T-D6 (findNearestPoint) + T-D7 (y-domain svgAnimation) | scene resolution; ChartAnimationOptions | hover targeting + tween-fire gates, bench line/area/composed |
| P3.9 | T-D8 (native tooltip cutover, per-part gated — see split note) | tooltip-chrome halves + per-part chromes | per-part Q1/Q2; RF per part |
| P3.10 | T-D5 (gradients/clipPath natives) | spec.gradients candidates + brush clip | gradient pixel identity |
| P3.11 | T-D9 (brushX mechanics) | brush drag/selection | brush behavior gate |
| P3.12 | T-D10 (createChartSpring where tolerance passes + host sizing audit) | spring.ts consumers; use-container-size audit | spring visual tolerance |
| P3.13 | T-D14 (misc natives bundle: rows 39/41/42/25/26/27/13/11) | heatmap tooltip motion; date coercion; funnel keys; binTimeX; regression; fold; viewport filter; view-grid brush-layout | per-part gates each item |

T-D8 execution shape: one part per sub-run (bar → scatter → candlestick → live-line → heatmap → choropleth), each sub-run gated before the next; retire scheduler/box halves only when a part's pixel gate passes. Kept-chrome list (pill/date-pill/square-ring/fade-gradient) is fixed by ruling; CP7's restored TooltipBox (P1.4) narrows choropleth's cutover scope.

**Wave 4 — W4 centralization (post-native residue; parallel-safe within wave)**

| Pkg | Tasks | File clusters | Gate |
|---|---|---|---|
| P4.1 | T-C1 (mapper kit, surviving chromes only) + T-C5 folds that survived T-D8 (`tooltip-scheduler`, `hover-reanchor`+shared bisect, `x-ticks`, `visx-pattern-bridge`, `radar-spring`) | tooltip-chrome + chromes; listed single-importer modules | Q1/Q2 hover chromes, verification items 4/5 already proven |
| P4.2 | T-C2 (focus-strategy kit) + T-C7 (broadcast store) + T-C8 (bar-squares math) | focus strategies; coordinators; bar modules | Q1/Q2 bar/scatter/candlestick focus + heatmap/pie hover |
| P4.3 | T-C3 (reveal-shim collapse, post-T-D1) + T-C4 (CenterShell merge incl. R7 RingCenter real import + gauge intro opt-in) | reveal shims→enter-transition; center modules | Q1/Q2 pie/ring/gauge/funnel reveals |
| P4.4 | T-C6 (legend hover unify + intFmt) + T-C9 (XAxisLabelOverlay shared layer) + T-C10 minus scale-adjacent items (shortDateFmt ×5, prefersReducedMotion util, useSanitizedId ×20, reveal-guard convergence, margin/reveal tokens, SegmentComponent/ChartMarker canonicals, heatmap builder+JSX, choropleth zoom merge, brush clamp, Symbol.for→CHART_ROLE) | legend modules; axis overlays; listed sites | Q1/Q2 legends/axis overlays, spot-checks per site |
| P4.5 | T-C10 remainder (xForDate helper ×6, reveal-constant tokens) — moved after T-F1? No: F1 runs in Wave 6 per macro order; these two items are deferred into P6.x instead | line/area/composed hosts | Q1/Q2 line/area/composed |

Correction: keep T-F1 in Wave 6 per macro order; therefore the two scale-adjacent C10 items move to P6.2 rather than running here (see Moves).

**Wave 5 — W5 API surface restoration (parallel-safe within wave except noted)**

| Pkg | Tasks | File clusters | Gate |
|---|---|---|---|
| P5.1 | **T-E1a mechanical barrel tier** — split into three sub-runs by worksheet category: E1a-TYPES (97), E1a-CONSTS (18), E1a-HELPER-FNs (91, excluding sunburst-geometry block held for post-T-D12 survivors) | charts/index.ts + shim modules | api-compat QA + typecheck per sub-run |
| P5.2 | T-E1b port/wrapper tier — sub-split: (a) visx Gradient*/Pattern* re-exports + presentational components (TooltipBox/TooltipContent/TooltipDot/TooltipIndicator/DateTicker/ChartRevealClip/ChartStatFlow/SeriesMarkers/SeriesPointMarker/MarkerGroup/Background[post-P1.13]/ChartBrush*); (b) HOOK wrappers (useChart*/usePie*/useRing*/useRadar*/useSankey/useSunburst*/useChoropleth*/useActiveMarkers/useChartInteraction) — EXCLUDING useYScale/useAnimatedYDomains (held for P6.2 post-T-F1) | charts/index.ts + parts | api-compat QA, spot-check demos |
| P5.3 | T-E1c ACCEPT ledger (D240/D244 rows named) + CH17 drift audit fold | docs/phase-4/LOG.md + index diff check | ledger review (no runtime gate beyond typecheck) |
| P5.4 | T-E2 default exports (~10) + T-E7 dev-warn layer (DOC-9) | part entries; bar entry | api-compat QA, CE for dev-warn |
| P5.5 | T-E3 stub-prop family (post-P3.2 easing shape) incl. K4 sampled-keyframe (post-P1.9) + T-E6 sunburst Hint/Breadcrumb (SB4/SB8) | part hosts/orchestrator; sunburst | Q1/Q2 per part touched, sunburst hint/breadcrumb scenario |
| P5.6 | T-E4 renamed-compat batch (CH2 alias, PieCenterShell wrapper [needs P4.3], useRing R2–R4, useChoropleth CP9/CP10 fields, MarkerTooltipContent, BR3 passthrough) | listed | api-compat QA |
| P5.7 | T-E5 loading presets (A10/B14 compose, L9 wire, L11 passthrough) | loading modules | Q1/Q2 line+area loading states |
| P5.8 | T-E8 measurement debounce parity — **pie P9 only; gauge G5 already landed in Wave 1 (D267)** | measure hooks | spot-check resize behavior |

Dependency notes inside wave: P5.6 needs P4.3's CenterShell; P5.3 runs last of E1 trio but may start once P5.1/P5.2 sub-runs settle their dispositions.

**Wave 6 — multi-axis + closing**

| Pkg | Tasks | File clusters | Gate |
|---|---|---|---|
| P6.1 | **T-F1 multi-axis yAxisId** (DOC-8): scale-resolution layer first, then consumers line/area/bar/scatter/composed/reference-area, AX7 per-axis tick-color filter, AX4 param re-add | scale layer + 6 consumer clusters | Q1/Q2 multi-series-per-axis scenario per part; bench line/bar/scatter/composed |
| P6.2 | Deferred scale-adjacent items: T-C10 xForDate helper + reveal-constant tokens recheck; T-E1b `useYScale`/`useAnimatedYDomains` wrappers (post-T-F1 edge) | line/area/composed hosts; barrel hooks | Q1/Q2 line/area/composed |
| P6.3 | T-F2 D-log entries (D240–D245 + every unlogged ACCEPT row incl. Part-A orphans' dispositions) | docs/phase-4/LOG.md | review only |
| P6.4 | T-F3 final pass (4.4.5): full build + bench compare + ALL-chart parity spot-check vs baseline | — | full gate board |

### Moves, splits, merges (log)

Every deviation from the plan's literal batch tables:

- SPLIT T-F1 → P6.1 stays whole (scale layer + consumers are one coherent change set); its downstream dependents split out instead (P6.2). Candidate split not taken.
- SPLIT T-E1a (200+ symbols) → P5.1 three sub-runs by category (TYPES / CONSTS / HELPER-FNs) so no executor run touches 276 lines of barrel at once.
- SPLIT T-E1b → P5.2 two sub-runs (component re-exports vs hook wrappers); useYScale/useAnimatedYDomains peeled to P6.2.
- SPLIT T-D8 → P3.9 per-part sub-runs (six parts, each gated before next) per tanstack-4 ruling.
- MERGE T-01 + T-02 → P0.1 (same v0.14-regression cluster, same files).
- MERGE T-W2-2 + T-W2-5 + T-W2-6 → P2.2 (disjoint files, trivially small tasks).
- MERGE T-W2-3 + T-W2-4 → P2.3 (styles.css + comment-only edits).
- MERGE T-C1 + surviving-half of T-C5 → P4.1 (tooltip-chrome-centric).
- MOVED T-D15 ahead of all other D-swaps inside Wave 3 (alias layer is prerequisite for palette-adoption rows; original order had it last at T-D15).
- MOVED T-W1-9 split across P1.4/P1.5 (heatmap vs choropleth file clusters would otherwise collide).
- DEFERRED 2 items of T-C10 (xForDate, reveal-constant tokens) from P4.4 → P6.2 to avoid double-touching line/area/composed hosts before T-F1 rewires their scale consumption.
- HELD T-E1a sunburst-geometry block + T-C10 maxRevealDelayMs item until after P3.6 (T-D12 decides survivors) — they execute in P5.1's third sub-run.
- NO MOVE for T-W1-16 (independent of T-F1 despite both touching y-axis rendering; confirmed disjoint files).
