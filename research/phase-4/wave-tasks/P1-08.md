# P1.8 — Radar timing RD5/RD7/RD6 (Wave 1)

Repo root: /Users/tomasdomingos/bklit-tanstack-charts-migration

## WRITE INCREMENTALLY
Write each edited file to disk as soon as its edit is complete — do not batch work to the end. Uncommitted work on disk survives interruption; a resumed session picks up from the working tree.

## Rules
- MAIN GOAL: exact same frontend design/interactivity as legacy bklit; TanStack-native backend; identical public component API.
- D232: fixes live in committed showcase/ code ONLY — never patch the clones (`repos/bklit-ui/`, `showcase/repos/tanstack-charts/`; both gitignored, Vercel re-clones fresh). Clones are read-only reference.
- Do NOT touch: `qa/`, `bench/` (running their scripts is allowed), the clones, /tmp. A guard hook denies these writes.
- Do not commit; leave changes in the working tree.
- go-to-plan.md is the source of truth (staleness rule 4.4.4); legacy reference for pixel/behavior parity is the bklit clone source.
- Migrated code root: `showcase/migrated/charts/` + `showcase/migrated/charts/internal/` (consumed by `showcase/` via the `@showcase/migrated-charts` wrapper package).
- Finding IDs (RD5, …) resolve in `research/phase-4/radar.md` — use part reports to LOCATE code only; behavior rulings come from go-to-plan.md.

## Tasks

### T-W1-12 (verbatim from go-to-plan.md, Batch W1)
- **Task:** Radar timing: RD5 base-delay formula, RD7 round caps; RD6 axis-label spring (fallback ACCEPT+D245)
- **Files:** radar chart/reveal
- **Acceptance:** area campaign starts at legacy time; caps round

Coverage resolution (OQ parity 9, radar items, quoted): "FIX RD5 (restore bklit base-delay formula), RD7 (round caps); RD6 attempt transform tween, fallback ACCEPT+D245."

Grounding: `charts/radar-chart.tsx`, `internal/radar-reveal.ts`, `internal/radar-spring.ts`. For RD6: attempt a transform-based label tween first; if the transform approach cannot reproduce legacy motion within pixel gates, STOP on that item, leave current behavior, and record it as ACCEPT+D245 fallback material in your report (the D-log entry itself lands in P6.3/T-F2 — do not write LOG entries from this package).

## Gates (all must pass before reporting done)
- `cd showcase && npx tsc --noEmit` exits 0.
- Timing evidence for RD5 ("area campaign starts at legacy time"): capture reveal start offsets vs legacy (reveal-probe or scripted timing) and report numbers, not impressions. This is a package-level functional check, not swept by the gate-runner.
- **Wave gate-runner (D246 impl/gate split)**: do NOT run QA pixel compares or console checks yourself — swept once for the whole wave. Report for the gate-runner: radar incl. hover captures (`--n 1000`); radar is not in `qa/console-errors.mjs`'s fixed target list — flag that gap rather than covering it yourself.

## Report back
- What changed per item RD5/RD7/RD6 (files + behavior).
- Gate evidence: typecheck exit status + RD5 timing numbers. List the affected chart(s)/scenario(s)/density for the wave gate-runner to sweep (QA pixel compare + console errors).
- RD6 disposition explicitly stated (landed transform tween OR fallback ACCEPT+D245 pending) with reason.
- Anything unresolved or intentionally skipped — report failures honestly; do not omit them.
