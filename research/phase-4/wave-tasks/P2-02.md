# P2.2 — Dead vars/branches sweep + MARKER_* un-export + live-line/ring cleanup (Wave 2)

Repo root: /Users/tomasdomingos/bklit-tanstack-charts-migration

## WRITE INCREMENTALLY
Write each edited file to disk as soon as its edit is complete — do not batch work to the end. Uncommitted work on disk survives interruption; a resumed session picks up from the working tree.

## Rules
- MAIN GOAL: exact same frontend design/interactivity as legacy bklit; TanStack-native backend; identical public component API.
- D232: fixes live in committed showcase/ code ONLY — never patch the clones (`repos/bklit-ui/`, `showcase/repos/tanstack-charts/`; both gitignored, Vercel re-clones fresh). Clones are read-only reference.
- Do NOT touch: `qa/`, `bench/` (running their scripts is allowed), the clones, /tmp. A guard hook denies these writes.
- Do not commit; leave changes in the working tree.
- go-to-plan.md is the source of truth (staleness rule 4.4.4); legacy reference for pixel/behavior parity is the bklit clone source.
- Migrated code root: `showcase/migrated/charts/` + `showcase/migrated/charts/internal/`.

## Wave 2 is DELETE-ONLY — no behavior change
Every edit in this package must be provably behavior-neutral. If removing something would change a single rendered pixel or a single event, it is **out of scope** — stop, leave it, and report it. Wave 2's whole premise is that these symbols are already dead.

Prove deadness before deleting. `grep -rn "<symbol>" showcase/` across the migrated tree and its consumers, not just the file at hand. A symbol exported from a barrel may have an external consumer.

## Parallel-dispatch fences (active right now)
Three fix dispatches are running concurrently on **sunburst**, **choropleth**, and **heatmap**. Do **not** edit any file in those clusters — including `internal/choropleth-hover-chrome.ts`, `internal/heatmap-*`, `internal/sunburst-*`, `choropleth-chart.tsx`, `sunburst-chart.tsx`. Also do **not** edit `internal/tooltip-chrome.ts` or `styles.css` (a parallel dispatch may be in them; `styles.css` belongs to P2.3 regardless). `internal/hover-chrome.ts` IS yours — it is a different file from `tooltip-chrome.ts`.

If a deletion you need genuinely requires touching a fenced file, STOP and report it rather than editing.

## Tasks

### T-W2-2 (verbatim row)
- **Task:** Delete dead vars/branches: `earlyRenderData` (L12), bar `columns`+voids (B18–20), composed dead import (C13), `input.width`, `hoverInputsRef` (F3), phantom `bandWidth` read, `ScatterHoverChromeState.hoveredIndex`, `groupEl` shim (R8), scatter dup memos, per-chrome `tooltipBoxSpring` voids ×4 (IN2 threads the real one)
- **Files:** per finding list `research/phase-4/synthesis/redundancies.md`

Verified anchors from `synthesis/redundancies.md` (rows 36–45) — re-verify each against current source before deleting, as Wave 1 moved code:

| item | location per finding | action |
|---|---|---|
| `earlyRenderData` computed then voided | `line-chart.tsx:144-148` | delete the computation |
| `columns` array built then discarded + duplicate x-rounding | `bar-chart.tsx:845` (`animateSquaresCascade`) | delete build; dedupe the rounding |
| `tooltipBoxSpring` chrome option accepted-then-voided ×4 | `internal/hover-chrome.ts:170`, `internal/scatter-hover-chrome.ts:137`, `internal/candlestick-hover-chrome.ts:98`, `internal/bar-hover-chrome.ts:154` | drop the redundant **per-chrome option** |
| phantom `bandWidth` read | `internal/bar-hover-chrome.ts:382` `(state as {bandWidth?}).bandWidth` | remove the dead branch — the gap-4 fallback IS the observed behavior |
| `ScatterHoverChromeState.hoveredIndex` | `internal/scatter-hover-chrome.ts` | remove field; the pill keeps consuming `datumIndex` |
| `hoverInputsRef` populated, never read | `funnel-chart.tsx:671-698` | delete the ref + its population block |
| `RingHoverConfig.groupEl` `@deprecated` | `internal/ring-hover-chrome.ts:64-65` | remove |
| composed dead import (C13) | `composed-chart.tsx` | delete (C14 `yAxisId` is **NOT** yours — routed to L10) |
| `input.width` | per redundancies.md | delete |
| scatter dup memos | `scatter-chart.tsx` | de-duplicate |

**RULING K-1 — read this, it supersedes the row's parenthetical.** The row says "(IN2 threads the real one)". go-to-plan.md:218 supersedes that:

> **Ruled: single live path.** Per-chrome duplicated option DELETED (the ×4 voids, per redundancies.md); part-level props (e.g. scatter `_tooltipBoxSpring`, S12 in T-W1-15) wire into the one live `ChartTooltipConfig.boxSpringConfig` path. T-W2-2's "(IN2 threads the real one)" is superseded by this ruling: W2 deletes per-chrome options; wiring happens only at the part→config boundary.

So: **delete** the four per-chrome `tooltipBoxSpring` options. **Keep** `ChartTooltipConfig.boxSpringConfig` — it is live parity surface. Do **not** thread anything new in this wave.

### T-W2-5 (verbatim row)
- **Task:** Un-export `MARKER_*` dead consts (LM10/series-marker-mark) — behavioral owner stays hover-chrome; values verified legacy-correct (item 1), no alignment needed
- **Files:** series-marker-mark, hover-chrome

Un-export **only**. go-to-plan.md:172 records that the values are already legacy-correct (`0.12s` = series-bar.tsx:253, `1.35` = series-markers.tsx:196), so **no value alignment is wanted** — changing a number here is a regression, not a fix. P1.10 deferred this to you (LM10).

### T-W2-6 (verbatim row)
- **Task:** live-line wake-loop collapse; ring fake-`Animation` sentinel + single cleanup registration
- **Files:** live-line, ring

Two independent cleanups. The ring half must end with exactly **one** cleanup registration path, not two racing ones. Neither may change observable animation timing.

## Gates (all must pass before reporting done)
- `cd showcase && npx tsc --noEmit` exits 0. Wave 1 left the tree at exit 0, so any error is yours — except errors inside the three fenced clusters above, which belong to the parallel fix dispatches; report those, don't patch them.
- **Wave gate-runner (D246 impl/gate split)**: do NOT run QA pixel compares, console checks, or bench yourself — swept once for the whole wave. Report the affected charts + densities for the gate-runner to sweep. Per the plan row, this package's gate is: Q1/Q2 smoke across touched parts, bench scatter + ring + liveline.
- **Density rules the gate-runner must be told, and must not default away from:** liveline `--n 100`, markers `--n 100`; bar family NEVER `n≥1000` (degenerate at that density); most others `--n 1000`.

## Report back
- What changed per task (files + behavior), and the deadness evidence for each deletion (the grep that proved no consumer).
- Anything you found was NOT actually dead — that is a genuinely useful finding and must be reported, not quietly deleted anyway.
- Gate evidence: typecheck exit status. The affected chart/scenario/density list for the gate-runner, with the density notes above included verbatim.
- Anything unresolved or intentionally skipped, with reason — report failures honestly; do not omit them.
