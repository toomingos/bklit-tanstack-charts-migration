# P5.3 — T-E1c: barrel ACCEPT ledger + CH17 export-drift audit (Wave 5)

Repo root: /Users/tomasdomingos/bklit-tanstack-charts-migration

## WRITE INCREMENTALLY
Write each edit to disk as soon as it is complete — do not batch work to the end. Uncommitted work on disk survives interruption; a resumed session picks up from the working tree.

**This package is an audit + ledger package, not a code package.** Its output is documentation: `docs/phase-4/LOG.md` entries and a written disposition for every symbol in scope. You are not porting anything and not writing new components or hooks.

## Rules
- MAIN GOAL: exact same frontend design/interactivity as legacy bklit; TanStack-native backend; identical public component API. This package's job is to formally close out the symbols that do **not** get restored, so the API-parity ledger is complete and honest, not to restore anything itself.
- D232: never patch the clones (`repos/bklit-ui/`, `showcase/repos/tanstack-charts/`; both gitignored, read-only reference).
- Do NOT touch: `qa/`, `bench/` (running their scripts is allowed), the clones, `/tmp`. A guard hook denies these writes.
- Executors cannot delete files (guard policy D216) — not that this package should need to delete anything.
- Do not commit; leave changes in the working tree.
- go-to-plan.md is the source of truth (staleness rule 4.4.4).
- **You do not "fix" anything you find here.** If auditing turns up a symbol that actually *should* be restored (i.e. you don't believe ACCEPT is the right call), do not go implement it — write that finding down and route it to the lead for re-scoping into a different package. Your deliverable is a correct, evidenced ledger, not code.
- `qa/screenshot.mjs` compares **`bklit` vs `migrated`**. Not relevant to this package's own gate (no runtime changes), but keep it in mind: never characterize `impl=tanstack` behavior as a migrated-code fact anywhere in your ledger entries (D258).

## Scope

### Part 1 — the ACCEPT ledger (T-E1c)

Two ACCEPT groups, per go-to-plan.md's task table (`research/phase-4/go-to-plan.md:156`):

**Group A — DOC-4 (context/provider architecture), the CONTEXT-PROVIDER (8) tier:**
`ChartProvider`, `ChoroplethProvider`, `HeatmapProvider`, `PieProvider`, `RadarProvider`, `RingProvider`, `SankeyProvider`, `StaticChartPreviewProvider` — plus, per the task text, "`*ContextValue` types + hooks with no migrated equivalent." DOC-4's ruling (`go-to-plan.md:13`): *"Context/provider architecture removals stay ACCEPTED (D210-2 precedent): `*Provider`/`use*` internals not re-exported; thin legacy-named wrapper hooks added only where trivial (R3, R4, CP9/CP10 field restoration)."* For each of the 8 Provider symbols, and each `*ContextValue` type worksheet-listed (`ChartContextValue`, `ChartHoverContextValue`, `ChartStableContextValue`, `ChoroplethTooltipData`\*, `PieContextValue`, `RadarContextValue`, `RingContextValue`, `SankeyContextValue`, etc. — cross-check the full TYPE(97) list in `research/phase-4/fd12-barrel-worksheet.md:39` for every `*ContextValue`/`*Context` entry), confirm: (a) the Provider component genuinely still exists internally (it should — TanStack-native store-based coordination replaced the *pattern*, not necessarily the *file*), (b) nothing legitimately needs it exported for the public API to be usable (the store/hook surface is the intended public entry point, not the Provider), (c) it isn't one of the "thin wrapper added" exceptions P5.2/P5.6 are already handling (R3/R4/CP9/CP10 — do not re-litigate those here, they're accounted for elsewhere; your job is everything DOC-4 does **not** except from ACCEPT).

**Group B — DOC-10 (loading family), the specific ACCEPT sub-list:**
`LineLoadingSweep`, `BarLoadingSkeleton`, `generateChartSkeletonData`, `getSkeletonHeights`, `GenerateChartSkeletonDataOptions`, `useStaticChartPreview` + its Provider → ACCEPT per DOC-10 (`go-to-plan.md:19`): *"per-series `loading` controls stay accepted-inert (L5/A7); sweep/skeleton surface (FD7) deleted; heatmap shimmer stays cut with `loadingCell*` props accepted-inert; grid shimmer props stay inert, `resolveGridShimmer` deleted (AX3)."* Note this ruling was already exercised once, correctly, for the grid-shimmer case — see the already-ruled note below. Confirm each Group-B symbol genuinely has no backing implementation in migrated code (the sweep/skeleton rendering surface, FD7, was deliberately deleted) before logging it ACCEPT; if you find a symbol in this list that DOES have a live backing module, that's a contradiction between go-to-plan.md and the actual codebase — do not silently ACCEPT it, flag it.

**Already ruled out — do NOT re-investigate:** the `GridConfig` inert `shimmer*` fields (`shimmer`, `shimmerStroke`, `shimmerLength`, `shimmerSpeed`, `shimmerSync`, `internal/types.ts:204-213`) were already flagged, investigated, and closed by the lead the same night as their discovery — this is exactly the DOC-10/AX3 pattern (inert-by-design props kept for API parity against a live legacy `useGridShimmer` hook migrated deliberately doesn't implement). Don't reopen it; it's precedent for how you should reason about the rest of Group B, not new work.

### Part 2 — the D-number problem (read carefully, this brief's evidence supersedes go-to-plan.md's literal numbers)

go-to-plan.md's task table writes T-E1c's two log entries as literal "(D240)" and "(D244)" (`go-to-plan.md:156`), and DOC-4/DOC-10's own doctrine rows say the same ("Log as D240" at line 13, "ACCEPT + log D244" at line 19). **These are stale placeholder labels, not real available D-numbers.** Checked against `docs/phase-4/LOG.md` immediately before writing this brief:

- **D240 already exists** and is entirely unrelated — a 2026-08-22 bench-harness fix for the `tooltip: true` boolean vs TanStack v0.14.0's `resolveTooltipInput()` contract. It has nothing to do with DOC-4 or context/provider architecture.
- **D244 does not exist at all.** The chronological D-log has moved from D239 straight through D261 with no D241/242/243/244/245 present anywhere.

Do not write your ledger entries as "D240" or "D244" — use the **next real sequential D-number** available in `docs/phase-4/LOG.md` at the time you write (currently past D261; confirm the actual next number yourself, don't trust this brief's count by the time you run). In each entry's body, explicitly cross-reference the stale label so the connection to the doctrine ruling is traceable, e.g.: *"This entry fulfills go-to-plan.md DOC-4's 'Log as D240' instruction; D240 itself was already consumed by an unrelated bench-harness fix (2026-08-22) — see that entry for the coincidence."*

**A second, separate problem: ownership may be double-booked.** `go-to-plan.md:170` (T-F2, Wave 6) lists writing "D240 (provider architecture)... D244 (loading package)..." as part of *its own* scope, alongside D241/D242/D243/D245 and "every ACCEPT row from parity.md not already D-ruled." Read literally, both T-E1c (this package, Wave 5) and T-F2 (Wave 6, runs later) are chartered to produce the DOC-4 and DOC-10 ledger entries. Since you run first: **write both entries now, in full, with every ACCEPTed symbol named explicitly** (per T-E1c's own requirement), using real sequential D-numbers. Do not leave them for T-F2. In your LOG.md entries, state plainly that they fulfill go-to-plan.md's DOC-4/DOC-10 "D240"/"D244" placeholders so that whoever executes T-F2 (P6.3, not yet dispatched as of this writing) can see the work is already done and skip re-logging those two — T-F2 should then only need D241 (event model)/D242 (enterTransition reshape)/D243 (bar pilot scope, likely P5.4/T-E7's job by the same logic — flag that too)/D245 (radar), plus its own catch-all sweep. **Flag this double-booking prominently in your report** — it's a go-to-plan.md scope conflict, not something resolvable purely by you, and the lead needs to adjust either T-E1c's or T-F2's chartered text (and whatever P6-03.md brief eventually gets written for it) once you've landed real entries.

### Part 3 — CH17 export-drift audit

go-to-plan.md's CH17 row is genuinely underspecified: *"export drift block | missing | Aligned re-exports | Drifted set | low | FIX — audit re-exports (barrel-policy fold)"* (`research/phase-4/synthesis/parity.md:193`) doesn't name which symbols drifted or from what. Given P5.3 runs "last of E1 trio" (per go-to-plan.md's Wave 5 dependency note, `go-to-plan.md:347`) — i.e. after P5.1 and P5.2 have landed their additions — treat CH17 concretely as: **re-run the FD12-style barrel diff** (legacy `repos/bklit-ui/packages/ui/src/charts/index.ts` vs migrated `showcase/migrated/charts/index.ts`) now that P5.1/P5.2's additions are in, and confirm the remaining gap between the two barrels consists of *exactly* the symbols this package is about to log ACCEPT, plus anything already-known and intentionally migrated-only (the FD12 audit's own finding: migrated has 70 migrated-only additions the legacy barrel never had — these are not drift, they're superset API per DOC-2, leave them alone). If the remaining diff has symbols that are neither ACCEPTed here nor accounted for by an earlier package, that's real drift — name it and route it, don't ACCEPT it just to make the ledger balance.

### Part 4 — ADDED 2026-08-25 (D312/D314): three items routed here after this brief was written

**Read this part before Parts 1–3; item C changes how you must treat a list you would otherwise take at face value.**

**A. The six retired sunburst symbols go in your ACCEPT ledger.** P3.6's split
retired them and P5.1 confirmed the survivor set by measurement (D301 §2,
D312): `buildRevealDelays`, `buildRevealSchedule`, `buildSunburstEnterTiming`,
`centroidAngle`, `localProgress`, `segmentRevealFromRingSweep`. **`lerpGeometry`
is NOT one of them** — it was grouped with this set in
`research/phase-4/sunburst.md` until 2026-08-25, but P5.1 added the `export`
keyword and a barrel line, so it is public API now; that row is corrected in
place. `buildSunburstEnterTiming`'s successor is `buildRevealTiming`
(`internal/sunburst-reveal.ts:40-46` records the renaming) — so it is a
**rename**, not a deletion, and should be logged as such rather than as a
capability drop.

**B. A real parity FIX, not a ledger row: `sortOthersByY` is wrong for bar and
scatter.** `internal/chart-focus-kit.ts:53,67` takes a `sortOthersByY` flag and,
when true, sorts a hover group's non-primary members by ascending `y`. Current
callers (**re-grep before editing; verified 2026-08-25**):

```
candlestick-focus-strategy.ts:50, :58   -> false
scatter-focus-strategy.ts:42, :50       -> true
bar-focus-strategy.ts:79, :134, :142    -> true
```

The kit's own comment (`:8-9`) states the reasoning outright: *"bar/scatter sort
non-primary members by `a.y - b.y` (TanStack focusX grouped parity);
candlestick does NOT sort — bklit's …"*. **That is TanStack parity chosen over
bklit parity, and this project's main goal is bklit parity** — so the five
`true` sites are the suspect set and `false` is the expected fix. **Do not take
that from me.** Verify against the legacy clone what order bklit actually
emits non-primary tooltip rows in, and report the evidence; if bklit really
does sort by y, the current code is right and this item closes as a no-op
with a corrected comment.

**This defect is invisible to every existing fixture, and that is why it needs
a new one.** While series do not cross, ascending-y order and
series-declaration order are the same list — every gate in this repo has been
passing on data where the bug cannot express itself. **You must add a
crossing-series fixture** (two series that swap vertical order across the
x-range) and gate bar (n=100 — **the bar family is NEVER run at n≥1000**) and
scatter (n=1000) on it. A green run on the existing fixtures proves nothing
here.

**C. The "99 of 206 symbols are fully absent from migrated" list is a LEAD, not
a work queue (D312).** P5.1's notes
(`research/phase-4/wave-tasks/P5-01-executor-notes.md`) report that 99 of the
206 worksheet names do not appear in migrated code. **As a name match that is
accurate. As a statement about functionality it is not safe**, because this
port renamed aggressively: bklit's `y-domain-utils` names are all absent, yet
`internal/y-domain.ts` ships `resolveTimeSeriesYDomain` (`:22`),
`useNicedYDomainChanged` (`:55`) and `createNicedYScale` (`:70`) — the
capability was ported under new names. **Name-absence ≠ functionality-absence.**
Before logging any of those 99 as an ACCEPTed retirement, establish what the
symbol *did* and search migrated for that capability, not for that name. The
notes file carries a `⚠ LEAD CAVEAT (D312)` saying the same. Blocks that
spot-check as genuinely accurate: the `cssVars` family (only
`sunburstCssVars`/`legendCssVars` survive; `chartCssVars` exists solely as a doc
comment at `internal/types.ts:449`) and the loading *type* names — note the
loading **components** `LoadingLabel`/`LineLoadingPulse` do still ship in
`internal/loading-chrome.tsx`, so scope that row to types.

## Gate

**Item B is render code and DOES need a pixel gate** — see Part 4B for the
required crossing-series fixture and densities. The rest of this package is
documentation:
1. `pnpm typecheck` exit 0 (only relevant if you touch any `.ts` doc-adjacent files; LOG.md edits alone need no build gate).
2. Ledger review: every ACCEPT row has a named symbol, a cited doctrine ruling (DOC-4 or DOC-10), and a concrete reason it has no migrated backing (not just "the doctrine says so" — show the absence, e.g. "grep for `LineLoadingSweep` across `showcase/migrated/charts/` returns zero hits").
3. The re-run barrel diff from Part 3 is included as evidence in your LOG.md entry or report, not just asserted.

## Report back

1. Group A (DOC-4 / CONTEXT-PROVIDER): every symbol ACCEPTed, with evidence it has no live public equivalent, and the real D-number you used.
2. Group B (DOC-10 / loading ACCEPT sub-list): same, plus explicit confirmation each symbol in the list genuinely lacks backing (or a flag if one doesn't).
3. The full text (or a close paraphrase) of both LOG.md entries you wrote, and their real D-numbers.
4. The double-booking finding versus T-F2 (Wave 6) — restate it plainly with the two go-to-plan.md line citations, and your resolution (you wrote them now; T-F2/P6.3 should not repeat them).
5. CH17: the re-run barrel diff result — does the remaining gap fully reconcile against ACCEPTed-here + already-known migrated-only additions, or is there real unexplained drift? Name it if so.
6. Anything contradicting this brief, stated plainly with evidence.
