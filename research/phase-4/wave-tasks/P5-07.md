# P5.7 — T-E5: loading presets (AreaChartLoading/BarChartLoading, L9, L11) + the near-blind loading gate + y-gridline geometry (Wave 5)

Repo root: /Users/tomasdomingos/bklit-tanstack-charts-migration

## WRITE INCREMENTALLY
Write each edited file to disk as soon as its edit is complete — do not batch work to the end. Uncommitted work on disk survives interruption; a resumed session picks up from the working tree.

## Rules
- MAIN GOAL: exact same frontend design/interactivity as legacy bklit; TanStack-native backend; identical public component API.
- D232: never patch the clones (`repos/bklit-ui/`, `showcase/repos/tanstack-charts/`; both gitignored, read-only reference).
- Do NOT touch: `qa/`, `bench/` (running their scripts is allowed), the clones, `/tmp`. A guard hook denies these writes.
- Executors cannot delete files (guard policy D216).
- Do not commit; leave changes in the working tree.
- go-to-plan.md is the source of truth (staleness rule 4.4.4).
- Migrated code root: `showcase/migrated/charts/` + `showcase/migrated/charts/internal/`.
- `qa/screenshot.mjs` compares **`bklit` vs `migrated`**. Never probe `impl=tanstack` (D258).

## READ `docs/phase-4/LOG.md` D257 §4 AND §6 BEFORE TOUCHING ANY LOADING CODE

D257 closed Wave 2's gate and, along the way, found the loading-state pixel gate is **methodologically near-blind**. This governs how you gate your own work in this package — do not trust a passing loading-state percentage the way you would trust one anywhere else in this project.

### The finding (§4)

Step 2 of the Wave 2 gate ran `compare-loading` for line/area/heatmap and reported PASS everywhere — line at **0.0531%** (`qa/results/line/2026-08-23T21-46-53-518Z/report.json`: `{"diffPixels":510,"totalPixels":960000,"diffRatio":0.00053125,"diffPercent":0.0531,"pass":true}`), area at 0.0525%, heatmap at 0.3783%. The lead opened the actual diff PNGs (`qa/results/line/2026-08-23T21-46-53-518Z/{loading-a,loading-b,loading-diff}.png`) instead of accepting the percentage, and found the two impls **visibly different in three ways** despite the pass:

1. bklit's "Loading data" label is bold and letter-spaced; migrated's is regular-weight and unspaced.
2. bklit renders a diagonal sweep glint in the upper right; migrated has no trace of it.
3. **The y-gridlines sit at entirely different positions** — bklit ≈65/175/290/398/505, migrated ≈137/232/327/420/507 — a different placeholder y-domain during the loading state. (X-axis labels align exactly between the two.)

**Conclusion, quoted directly because it governs your gate methodology, not just this one finding: "a loading-state PASS is not evidence of loading-state parity, and no ruling that depends on one should be made."** Do not close any loading-related item in this package on a passing percentage alone — open the diff PNGs, every time, for every loading scenario you touch.

### The same-night correction (§6) — two of the three differences are ALREADY RULED, do NOT re-fix them

The lead's own first reaction was to treat this as a new unrecognized parity gap; checking `go-to-plan.md` before acting corrected that. **Only reopen what's actually still open:**

- **Difference #2 (the missing sweep glint) is `FD7`, already ACCEPTED per DOC-10** — "sweep/skeleton surface deleted." This is `LineLoadingSweep`/`BarLoadingSkeleton` and friends, which P5.3 (T-E1c) is separately logging into the ACCEPT ledger. **Do not resurrect the sweep animation in this package** — it stays deleted by design. If you find yourself building a diagonal-sweep effect to close this gap, stop; that's the wrong read of the ruling.
- **Difference #1 (the label styling) is `FD5`**, ruled *"FIX-className-or-ACCEPT, low"* — low-priority, either branch acceptable, and not explicitly assigned to this package's task table row. If it's cheap while you're already touching the loading label rendering path for other reasons, fix it (CSS-only, matching bold/letter-spacing); otherwise leave it and note in your report that it remains open at low severity — do not treat it as blocking.
- **Difference #3 (y-gridline positions) is the ONE genuinely unruled item**, explicitly routed here: *"exactly one substantive item no ruling names — the y-gridlines sit at different positions between impls during loading, which is chart geometry rather than loading chrome and therefore outside DOC-10's scope. Routed to P5.7."* This is real work for this package — see Strand 5 below.

Also note from the same entry, for context only (not this package's job): the five `GridConfig` `shimmer*` fields (`internal/types.ts:204-213`) are inert-by-design per DOC-10/AX3, not orphans — do not "fix" them either.

## Scope

> ## ⚠ LEAD PRE-DISPATCH VERIFICATION 2026-08-25 — read before Strands 2 and 3
>
> I re-checked every locator in this charter against the live tree. **The symbol-level claims
> are all TRUE** — `brush-layout.tsx` exists with `xDomainSlotCount` at `:52`/`:58`;
> `HeatmapChartLoading` is at `heatmap-chart.tsx:471`/`:481`; and `resolveLineLoadingPulseMode`,
> `LineLoadingPulseMode`, `AreaChartLoading`, `BarChartLoading` all have **zero hits**. Trust them.
>
> **Three corrections, two of which change your scope:**
>
> **(a) LINE NUMBERS HAVE DRIFTED BY ONE. Anchor on symbols, not line numbers.** L9's pair is
> `internal/types.ts:54`/`:56`, not `:55`/`:57`. The `GridConfig` shimmer fields are `:203-212`,
> not `:204-213`. Same one-line shift throughout — this charter was written before an insertion
> above them. Every symbol name in it is still correct; only the numbers moved.
>
> **(b) THERE ARE THREE `loadingStroke` DECLARATIONS AND ONLY TWO ARE YOURS.** `:54`/`:56` belong
> to **`LineConfig`** (opens `:35`) — those are L9. **`:180` belongs to `GridConfig`** (opens
> `:164`) and is a *different prop* meaning "grid line stroke while loading chrome is active,
> falls back to `stroke`". It is unrelated to the line pulse and **is not in your scope**. A
> plain `grep loadingStroke` returns all three; wiring the `GridConfig` one into the pulse path
> would be a real bug that typechecks.
>
> **(c) L11 IS TWO CHARTS, NOT ONE — THIS IS A SCOPE EXPANSION.** Strand 3 names only
> `line-chart.tsx:109`/`:129`. **`area-chart.tsx` carries the identical voided prop**:
> `xDomainSlotCount?: number` at `:120`, destructured `xDomainSlotCount: _xDomainSlotCount` at
> `:158`. Same defect, same fix, same underscore-void marker. **Do both.** Fixing line alone
> would leave area silently inconsistent and would look, to the next audit, like a deliberate
> divergence rather than a missed line. If area's threading turns out to differ materially from
> line's, say so and fix what you can rather than dropping it.

### Strand 1 — A10/B14: `AreaChartLoading`/`BarChartLoading` presets

Both are confirmed **absent entirely** from migrated code (zero matches for either symbol anywhere under `showcase/migrated/charts/`). Ruling: *"FIX (compose from existing parts) or de-scope"* (A10) / *"Cross-ref loading OQ"* (B14) — go-to-plan.md's own task text resolves the "or de-scope" branch: *"AreaChartLoading/BarChartLoading composed from existing parts."* A working precedent for exactly this pattern already exists in the codebase: `showcase/migrated/charts/heatmap-chart.tsx:471-489` — `HeatmapChartLoadingProps` interface (471) and `HeatmapChartLoading` function (481-489), which composes a loading-state preset from heatmap's own existing rendering parts rather than introducing new loading machinery. Model `AreaChartLoading`/`BarChartLoading` on that same shape: a thin preset component that renders the area/bar chart's existing skeleton/loading visuals (whatever those currently are — check what `state="loading"` already produces for area/bar today before assuming you need net-new visuals) pre-configured as a drop-in loading placeholder, not a new design.

### Strand 2 — L9: wire `loadingStroke`/`loadingStrokeOpacity`

Confirmed **typed but accepted-inert**: `internal/types.ts:55` (`loadingStroke?: string`) and `:57` (`loadingStrokeOpacity?: number`) exist on the type, consumed by nothing. Ruling: *"FIX — forward to LineLoadingPulse or drop from type."* Trace where line's loading-pulse rendering actually lives (search for the current pulse-stroke drawing code path) and forward these two props into it. If, once you look, wiring them turns out to be genuinely awkward (e.g. the pulse rendering path doesn't have a natural seam for per-instance stroke overrides), the ruling explicitly permits dropping them from the type instead — document which you chose and why, don't default to wiring just because it's listed first.

### Strand 3 — L11: `xDomainSlotCount` pass-through

Confirmed **typed and explicitly voided**: `showcase/migrated/charts/line-chart.tsx:109` types `xDomainSlotCount?: number`, but line 129 destructures it as `xDomainSlotCount: _xDomainSlotCount` — the underscore prefix marks it intentionally unused. Ruling: *"FIX-trivial (pass to brush layout) or ACCEPT."* `internal/brush-layout.tsx` already has `xDomainSlotCount` flowing through internally (lines ~52/58) — the fix here is almost certainly just: stop discarding the prop at line 129, thread it into whatever internal call already accepts an `xDomainSlotCount` at the brush-layout layer. Confirm the value actually reaches `brush-layout.tsx`'s existing parameter, not just that the local variable stops being prefixed with an underscore.

### Strand 4 — FD6: `resolveLineLoadingPulseMode`/`LineLoadingPulseMode` (missing from the task table, present in the doctrine text — do this anyway)

**This item is not in go-to-plan.md's T-E5 task-table row or the Wave 5 package table's description of P5.7** (`go-to-plan.md:160` and the Wave 5 table row for P5.7 both list only A10/B14/L9/L11) — **but DOC-10's own doctrine ruling explicitly assigns it here**: *"FIX: wire `loadingStroke*` (L9), export `resolveLineLoadingPulseMode` (FD6), restore `AreaChartLoading`/`BarChartLoading` presets composed from existing parts (A10/B14)"* (`go-to-plan.md:19`). Treat the doctrine text as authoritative over the abbreviated task-table row (the table is a compressed summary of the doctrine, not an independent ruling) and do this work — flag the table's omission in your report so the lead can tighten the task-table wording.

Confirmed via `synthesis/parity.md:236`: FD6 — `resolveLineLoadingPulseMode`/`LineLoadingPulseMode` — ruling **"Absent... FIX"**, not "unexported." This is a genuine implementation gap, not a barrel-wiring gap: **zero grep hits for either symbol anywhere in `showcase/migrated/charts/`.** P5.1 (T-E1a) separately named these two symbols as candidates for its mechanical re-export tier and found the same thing — no backing module — and was instructed to route them here rather than invent an implementation itself. You are that destination. Find the legacy `resolveLineLoadingPulseMode` implementation under `repos/bklit-ui/packages/ui/src/charts/` (read-only reference), understand what pulse-mode it resolves for the line-loading-pulse rendering path, and port it — a real function, not a stub — plus the `LineLoadingPulseMode` type it returns. This likely intersects directly with Strand 2 (L9, wiring `loadingStroke*` into `LineLoadingPulse`) — do them in the same pass if the two touch the same rendering code, since you'll be in the same file either way.

### Strand 5 — the y-gridline geometry defect (the one genuinely new item from D257)

**This is chart geometry, not loading chrome** — it belongs in this package only because the loading path is already open here and the lead routed it here for convenience, not because it's actually a "loading preset" bug. Investigate why the two impls compute different y-gridline positions specifically during `state="loading"` (bklit ≈65/175/290/398/505 vs migrated ≈137/232/327/420/507 — five gridlines each, both evenly-ish spaced but starting/ending at different y-pixel values, meaning a **different placeholder y-domain** is being used to lay out gridlines while data hasn't loaded yet). Find where each impl derives its loading-state y-domain (likely a default/placeholder domain used before real data arrives) and align migrated's to bklit's. This is a **visual delta vs legacy → FIX to legacy** by DOC-1's general rule (no loading-specific ruling names it, but DOC-1's default applies once something IS named, which D257 just did).

## Strand 6 — LEAD-ADDED 2026-08-25: you must LOG the seven DOC-10 loading ACCEPTs, not just decline to resurrect them

There is a hole between this charter and P5.3's, and you are the only package that can close it.

P5.3's ACCEPT ledger (`research/phase-4/wave-tasks/P5-03-ledger.md`) explicitly **declines to
write rows** for seven loading-family symbols, routing them here and saying so in as many words
("Per P5.1's executor-notes routing these belong to the **P5.7 loading-family package**, not
this ledger — recorded here so the zero-hit evidence is not lost, rows not duplicated"):

```
LineLoadingSweep            BarLoadingSkeleton         generateChartSkeletonData
getSkeletonHeights          GenerateChartSkeletonDataOptions
useStaticChartPreview       StaticChartPreviewProvider
```

All seven are confirmed **zero-hit** across `showcase/migrated/charts/` — verified independently
by P5.3 and by the lead. Meanwhile **this charter only ever tells you what NOT to do with them**
(§6: don't rebuild the sweep, FD7/DOC-10 keeps it deleted). So as things stand, P5.3 has passed
them to you, and you have no instruction to record them anywhere.

**That is the gap: seven symbols that are deliberately gone with no durable record saying so.**
Phase 4's Definition of Done requires every task closed **or skipped-with-reason**, and an
undocumented deliberate deletion reads to the next audit exactly like an oversight — the same
failure mode as an under-scoped strand.

**Write the ACCEPT rows.** One line each, naming the doctrine (DOC-10 for the sweep/skeleton
surface; `StaticChartPreviewProvider` additionally has **no backing implementation at all**,
which P5.2 established, so it cannot be "restored" even in principle). Put them in your own
report and state plainly that P5.3's ledger deliberately does not carry them, so the lead can
fold them into the ledger of record without double-entering them. **Do not write to
`P5-03-ledger.md` yourself** — that file belongs to P5.3.

## Gate

**Every scenario in this package MUST be visually confirmed by opening the diff PNGs, not just checking the pass percentage** — that is the entire point of D257 §4's finding, and it applies with extra force here because you are the package actually working in this exact blind spot.

- Q1/Q2 line + area loading states (this package's named gate scenario, per go-to-plan.md's Wave 5 table). Use `compare-loading` mode. Open `loading-a.png`/`loading-b.png`/`loading-diff.png` for every run, every time.
- Bar loading (for B14): note D257 §7 found the `state="loading"` scenario **ignores `state` for bar entirely, symmetrically with bklit** — the runner had to verify the bar pulse through the real prop path instead (`__qaSetBarPulsePaused(false)` → confirms `internal/bar-pulse-mark.ts` drives the pulse). If `BarChartLoading` as a *preset component* is a separate code path from the `state="loading"` prop, your gate for B14 needs its own scenario that actually renders the new `BarChartLoading` component, not a reliance on the (deliberately inert) `state` prop.
- After the y-gridline fix (Strand 5): confirm the gridline y-positions now match between impls **by reading pixel coordinates off the diff, not by trusting a lower percentage** — a small percentage improvement could still leave the positions measurably different; the original bug was invisible at 0.0531% in the first place.
- Bar family gate density: **never n≥1000, use 100.** Line/area: 1000.
- `qa/screenshot.mjs` compares **`bklit` vs `migrated`**. Never probe `impl=tanstack` (D258).
- Known chronic: markers n=100 hover-30 ~0.5507% (unrelated chart family, but check `qa/results/<chart>/` history before calling any failure new, generally, per project convention).

## Report back

1. A10/B14: the new preset components, `file:line`, and how closely they follow the `HeatmapChartLoading` precedent (or why they diverge).
2. L9: wired to `LineLoadingPulse` or dropped from the type — which, and why, with `file:line`.
3. L11: confirmation the prop now reaches `brush-layout.tsx`'s existing `xDomainSlotCount` parameter, `file:line` of the fix — **for BOTH `line-chart.tsx` AND `area-chart.tsx`** (see lead correction (c)). If you fixed only one, say which and why.
4. The y-gridline fix: root cause (where the placeholder y-domain diverges), the fix, and **pixel-position confirmation** (not just percentage) that the two impls now agree.
5. Explicit confirmation you did NOT resurrect the sweep glint (FD7/DOC-10 stays ACCEPTED) and your decision on the label-styling item (FD5, fixed if cheap or left open, low severity either way).
6. FD6: confirm `resolveLineLoadingPulseMode`/`LineLoadingPulseMode` were ported with a real implementation (not a stub), `file:line`, whether it landed in the same pass/file as Strand 2's L9 wiring, and note the go-to-plan.md task-table/Wave-5-table omission you were told to flag for the lead.
7. Strand 6: the seven DOC-10 ACCEPT rows, one line each with its doctrine, plus explicit
   confirmation you did NOT write to `P5-03-ledger.md`.
8. Anything contradicting this brief, stated plainly with evidence.
