# P4.5 — T-C10 remainder — EMPTIED by Moves, close as skipped (Wave 4)

Repo root: /Users/tomasdomingos/bklit-tanstack-charts-migration

## Status: this package has no code work. Close it, do not invent scope.

Per go-to-plan.md's "Moves, splits, merges (log)" section and `docs/phase-4/PROGRESS.md`'s Wave 4 row for P4.5, every item originally chartered for the T-C10 remainder was reassigned elsewhere during planning:

- **`xForDate` helper** and **reveal-constant tokens** — the two cross-file T-C10 items (duplicated across `line-chart.tsx`, `area-chart.tsx`, `composed-chart.tsx`) — were **deferred to P6.2**. Reason (verbatim from go-to-plan.md's Moves log): *"to avoid double-touching line/area/composed hosts before T-F1 rewires their scale consumption"* — T-F1 is the multi-axis `yAxisId` work in Wave 6/P6.1, and P6.2 runs after it lands.
- **Sunburst `maxRevealDelayMs`** — **held for P5.1's third sub-run**, run after P3.6 (T-D12, sunburst native swap) lands, since native sunburst decides which geometry/reveal helpers survive first. (Verbatim from the Moves log: *"HELD T-E1a sunburst-geometry block + T-C10 maxRevealDelayMs item until after P3.6 (T-D12 decides survivors) — they execute in P5.1's third sub-run."*)

This accounts for **all** T-C10 items not already assigned to P4.4 (see `research/phase-4/wave-tasks/P4-04.md`'s Scope section, which lists the confirmed-real items P4.4 actually executes plus a few items flagged as "not confirmed as duplicated" pending re-verification there). No T-C10 item was found, during drafting of this batch, that lacks a home in P4.4, P5.1, or P6.2.

**I did not find an orphan item during this research pass.** If you find one at dispatch time that go-to-plan.md's Moves log does not account for, name it, scope it minimally, and report it to the lead as a new finding rather than silently absorbing it into this package or another one — do not expand this package's scope on your own judgment.

## Rules (apply even though there is no code to write)

- Do NOT touch: `qa/`, `bench/`, the clones (`repos/bklit-ui/`, `showcase/repos/tanstack-charts/`), `/tmp`.
- Do not commit.
- Do not make any code change under this package. Its job is a closure, not an implementation.

## What to actually do

1. Confirm (by reading go-to-plan.md's Moves log and `docs/phase-4/PROGRESS.md`'s Wave 4 section yourself, at dispatch time — do not just trust this brief, the plan may have changed since drafting) that both leftover items are still assigned to P6.2/P5.1 and that no new T-C10 orphan has appeared.
2. Make **no code change**.
3. Report back to the lead so P4.5 can be logged in `docs/phase-4/LOG.md` as **closed, skipped-with-reason** (not "done," not "n/a" — it was chartered, then emptied by explicit planning decisions, and this closure is itself the deliverable).

## Report back

1. Confirmation, with go-to-plan.md citation, that P4.5 remains empty as of dispatch time (or, if not, what orphan item you found and how you scoped it).
2. Explicit statement that no code was changed.
3. A one-line closure summary for the lead to log in `docs/phase-4/LOG.md`, e.g.: "P4.5 closed skipped-with-reason: both T-C10 remainder items (xForDate/reveal-constant tokens, sunburst maxRevealDelayMs) reassigned to P6.2 and P5.1 respectively per go-to-plan.md Moves log; no orphan found at dispatch."
