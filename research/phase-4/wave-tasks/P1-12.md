# P1.12 — Markers LM cluster (LM6/LM7/LM8/LM11 + QA-hook gating) (Wave 1)

Repo root: /Users/tomasdomingos/bklit-tanstack-charts-migration

## WRITE INCREMENTALLY
Write each edited file to disk as soon as its edit is complete — do not batch work to the end. Uncommitted work on disk survives interruption; a resumed session picks up from the working tree.

## Rules
- MAIN GOAL: exact same frontend design/interactivity as legacy bklit; TanStack-native backend; identical public component API.
- D232: fixes live in committed showcase/ code ONLY — never patch the clones (`repos/bklit-ui/`, `showcase/repos/tanstack-charts/`; both gitignored, Vercel re-clones fresh). Clones are read-only reference.
- Do NOT touch: `qa/`, `bench/` (running their scripts is allowed), the clones, /tmp. A guard hook denies these writes.
- Do not commit; leave changes in the working tree.
- go-to-plan.md is the source of truth (staleness rule 4.4.4); legacy reference for pixel/behavior parity is the bklit clone source.
- Migrated code root: `showcase/migrated/charts/` + `showcase/migrated/charts/internal/` (consumed by `showcase/` via the `@showcase/migrated-charts` wrapper package).
- Finding IDs (LM6, …) resolve in `research/phase-4/internal-legend-markers.md` — use part reports to LOCATE code only; behavior rulings come from go-to-plan.md.

## Tasks

### T-W1-17 (verbatim from go-to-plan.md, Batch W1)
- **Task:** Markers: LM6 payload restore (+crosshair-hide interplay), LM7 `MarkerTooltipContent`/`useActiveMarkers` port, LM8 isActive wiring verify, LM11 fan scale 1.15
- **Files:** legend-markers internals
- **Acceptance:** marker hover behavior/pixels match legacy

Coverage resolution (orphan LM1+LM13, quoted): "LM1+LM13 `__qaSetMarkerFan` in prod | **FIX** — gate the QA hook behind a dev/QA flag (stripped or inert in prod bundle); added to T-W1-17 (P1.12)." Fold this in: gate `__qaSetMarkerFan` behind a dev/QA flag so it is stripped or inert in production bundles. Coordinate carefully with `qa/screenshot.mjs`'s markers branch (`chart === "markers"`) — it drives hover through that hook; keep it functional under the flag and verify the QA run still passes.

Grounding: `charts/internal/chart-markers.tsx`, `series-marker-mark.ts`, `terminal-marker.tsx`, `tooltip-scheduler.ts`, `hover-reanchor.ts` as discovered. Note: LM7's `MarkerTooltipContent` barrel export rows resolve later in P5.2/P5.6; land the port here without pre-building barrel surface. The MARKER_* const un-export (LM10) is P2.2's job — don't do it here.

## Gates (all must pass before reporting done)
- `cd showcase && npx tsc --noEmit` exits 0.
- **Wave gate-runner (D246 impl/gate split)**: do NOT run QA pixel compares or console checks yourself — swept once for the whole wave. Report for the gate-runner: markers incl. hover captures (`--n 100`); console entry `markers`. Explicitly note the `__qaSetMarkerFan` dev/QA-flag gating so the gate-runner knows the markers QA run still needs that hook functional under the flag.

## Report back
- What changed per LM item + the QA-hook gating (files + behavior).
- Gate evidence: typecheck exit status. List the affected chart(s)/scenario(s)/density for the wave gate-runner to sweep (QA pixel compare + console errors).
- The specific density/scenario notes below MUST be included in that list (do not let the gate-runner default to n=1000 where a different density is required).
- Anything unresolved or intentionally skipped, with reason — report failures honestly; do not omit them.
