# P1.7 — Sunburst semantics quintet (SB1/SB12/SB13/SB14/SB15) (Wave 1)

Repo root: /Users/tomasdomingos/bklit-tanstack-charts-migration

## WRITE INCREMENTALLY
Write each edited file to disk as soon as its edit is complete — do not batch work to the end. Uncommitted work on disk survives interruption; a resumed session picks up from the working tree.

## Rules
- MAIN GOAL: exact same frontend design/interactivity as legacy bklit; TanStack-native backend; identical public component API.
- D232: fixes live in committed showcase/ code ONLY — never patch the clones (`repos/bklit-ui/`, `showcase/repos/tanstack-charts/`; both gitignored, Vercel re-clones fresh). Clones are read-only reference.
- Do NOT touch: `qa/`, `bench/` (running their scripts is allowed), the clones, /tmp. A guard hook denies these writes.
- Do not commit; leave changes in the working tree.
- go-to-plan.md is the source of truth (staleness rule 4.4.4); legacy reference for pixel/behavior parity is the bklit clone source.
- Migrated code root: `showcase/migrated/charts/` + `showcase/migrated/charts/internal/` (consumed by `showcase/` via the `@showcase/migrated-charts` wrapper package).
- Finding IDs (SB1, SB12, …) resolve in `research/phase-4/sunburst.md` — use part reports to LOCATE code only; behavior rulings come from go-to-plan.md.

## Tasks

### T-W1-11 (verbatim from go-to-plan.md, Batch W1)
- **Task:** Sunburst semantics: playKey (SB1), cull-on-hover (SB12), center radius interpolate (SB13), commit-immediately (SB14), svg fade+pointerleave (SB15)
- **Files:** sunburst chart/labels
- **Acceptance:** side-by-side hover/zoom behavior matches legacy

Coverage resolution (OQ parity 9, sunburst items, quoted): "FIX … SB1 (wire playKey), SB12 (cull, + header), SB13 (interpolate), SB14 (commit immediately, keep midpoint-snapshot nuance), SB15 (350ms fade + svg pointerleave)." Preserve the midpoint-snapshot nuance on SB14 exactly as ruled.

Downstream dependency (do not break): "T-W1-11 (SB semantics) → T-D12 — SB14 commit-immediately/SB13 interpolate define the behaviors native sunburst must reproduce before its gate can be meaningful." Also note soft edge: the T-W2-4 sunburst-labels header comment fix becomes true only after your SB12 cull lands.

Grounding: `charts/sunburst-chart.tsx`, `internal/sunburst-labels.tsx`, `internal/sunburst-reveal.ts`, `internal/sunburst-center.tsx` (+ `sunburst-geometry.ts`/`sunburst-colors.ts` as discovered).

## Gates (all must pass before reporting done)
- `cd showcase && npx tsc --noEmit` exits 0.
- Hover/zoom behavior verified side-by-side vs legacy (zoom/drill interactions are beyond static captures — verify interactively or via scripted pointer input and describe the evidence). This is a package-level functional check, not swept by the gate-runner.
- **Wave gate-runner (D246 impl/gate split)**: do NOT run QA pixel compares or console checks yourself — swept once for the whole wave. Report for the gate-runner: sunburst incl. hover captures at **n=27** (matches the established sunburst QA density); console entry `sunburst`.

## Report back
- What changed per SB item (files + behavior).
- Gate evidence: typecheck exit status. List the affected chart(s)/scenario(s)/density for the wave gate-runner to sweep (QA pixel compare + console errors).
- The specific density/scenario notes below MUST be included in that list (do not let the gate-runner default to n=1000 where a different density is required).
- Anything unresolved or intentionally skipped, with reason — report failures honestly; do not omit them.
