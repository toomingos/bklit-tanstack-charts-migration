# P1.10 — Gauge G-cluster (G2/G3/G5) + Scatter S-cluster (S8–S12) (Wave 1)

Repo root: /Users/tomasdomingos/bklit-tanstack-charts-migration

## WRITE INCREMENTALLY
Write each edited file to disk as soon as its edit is complete — do not batch work to the end. Uncommitted work on disk survives interruption; a resumed session picks up from the working tree.

## Rules
- MAIN GOAL: exact same frontend design/interactivity as legacy bklit; TanStack-native backend; identical public component API.
- D232: fixes live in committed showcase/ code ONLY — never patch the clones (`repos/bklit-ui/`, `showcase/repos/tanstack-charts/`; both gitignored, Vercel re-clones fresh). Clones are read-only reference.
- Do NOT touch: `qa/`, `bench/` (running their scripts is allowed), the clones, /tmp. A guard hook denies these writes.
- Do not commit; leave changes in the working tree.
- go-to-plan.md is the source of truth (staleness rule 4.4.4); legacy reference for pixel/behavior parity is the bklit clone source.
- Migrated code root: `showcase/migrated/charts/` + `showcase/migrated/charts/internal/` (consumed by `showcase/` via the `@showcase/migrated-charts` wrapper package).
- Finding IDs (G2, S8, …) resolve in `research/phase-4/gauge.md` / `scatter.md` — use part reports to LOCATE code only; behavior rulings come from go-to-plan.md.

## Tasks

### T-W1-14 (verbatim from go-to-plan.md, Batch W1)
- **Task:** Gauge: G2 defsChildren mounted on arc overlay svg, G3 fixed-size bypass, G5 debounced measure
- **Files:** gauge
- **Acceptance:** caller gradients work on arc; fixed-size = bare inline-flex

Grounding: `charts/gauge.tsx`, `internal/gauge-reveal.ts`, `internal/gauge-center.tsx`, `internal/gauge-notch.ts` as discovered. Note for G5: T-E8 (P5.8) later unifies pie+gauge measurement on the debounced hook — land G5 now without pre-building that abstraction.

### T-W1-15 (verbatim)
- **Task:** Scatter: S8 yGradient port, S9/S10 tunables plumbed, S11 drag-select via `useChartSelection`, S12 `_tooltipBoxSpring` honored
- **Files:** scatter chart/chrome
- **Acceptance:** legacy scatter interactivity restored

Coverage resolution (contradiction K-1, quoted verbatim — this governs S12):
"K-1 `tooltipBoxSpring` wire-vs-delete | **Ruled: single live path.** Per-chrome duplicated option DELETED (the ×4 voids, per redundancies.md); part-level props (e.g. scatter `_tooltipBoxSpring`, S12 in T-W1-15) wire into the one live `ChartTooltipConfig.boxSpringConfig` path. T-W2-2's '(IN2 threads the real one)' is superseded by this ruling: W2 deletes per-chrome options; wiring happens only at the part→config boundary."

Concretely for THIS package: wire scatter's `_tooltipBoxSpring` prop into the live `ChartTooltipConfig.boxSpringConfig` path (see `charts/live-line-chart.tsx:451` for the existing wiring pattern; `internal/types.ts:313` defines the field). DO delete the scatter chrome's own dead option stub (`internal/scatter-hover-chrome.ts` `const _tooltipBoxSpring = options.tooltipBoxSpring ?? …; void _tooltipBoxSpring;` around lines 136–137). Do NOT delete the other chromes' voids (bar/candlestick/live) — those belong to W2 (P2.2).

Grounding: `charts/scatter-chart.tsx`, `internal/scatter-hover-chrome.ts`, `internal/scatter-focus-strategy.ts`, `internal/chart-selection.ts` (`useChartSelection` exists — same hook candlestick/composed consume for drag-select).

## Gates (all must pass before reporting done)
- `cd showcase && npx tsc --noEmit` exits 0.
- Drag-select (S11) verified against legacy interaction (scripted pointer drag or interactive check; describe evidence). This is a package-level functional check, not swept by the gate-runner.
- **Wave gate-runner (D246 impl/gate split)**: do NOT run QA pixel compares or console checks yourself — swept once for the whole wave. Report for the gate-runner: gauge (`--n 1000`; not in the fixed console target list, flag for a targeted read-only headless load), scatter incl. hover captures (`--n 1000`; console entry `scatter`).

## Report back
- What changed per item (files + behavior), explicitly separating gauge vs scatter work.
- Gate evidence: typecheck exit status. List the affected chart(s)/scenario(s)/density for the wave gate-runner to sweep (QA pixel compare + console errors).
- The specific density/scenario notes below MUST be included in that list (do not let the gate-runner default to n=1000 where a different density is required).
- Anything unresolved or intentionally skipped, with reason — report failures honestly; do not omit them.
