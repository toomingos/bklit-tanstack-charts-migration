# P1.1 — Grid `horizontal` default → true (AX2) (Wave 1)

Repo root: /Users/tomasdomingos/bklit-tanstack-charts-migration

## WRITE INCREMENTALLY
Write each edited file to disk as soon as its edit is complete — do not batch work to the end. Uncommitted work on disk survives interruption; a resumed session picks up from the working tree.

## Rules
- MAIN GOAL: exact same frontend design/interactivity as legacy bklit; TanStack-native backend; identical public component API.
- D232: fixes live in committed showcase/ code ONLY — never patch the clones (`repos/bklit-ui/`, `showcase/repos/tanstack-charts/`; both gitignored, Vercel re-clones fresh). Clones are read-only reference.
- Do NOT touch: `qa/`, `bench/` (running their scripts is allowed), the clones, /tmp. A guard hook denies these writes.
- Do not commit; leave changes in the working tree.
- go-to-plan.md is the source of truth (staleness rule 4.4.4); legacy reference for pixel/behavior parity is the bklit clone source.
- Migrated code root: `showcase/migrated/charts/` + `showcase/migrated/charts/internal/` (consumed by `showcase/` via the `@showcase/migrated-charts` wrapper package).

## Tasks

### T-W1-1 (verbatim from go-to-plan.md, Batch W1)
- **Task:** Grid `horizontal` default → true (AX2) + prop-less `<Grid/>` QA scenario
- **Files:** `internal/grid.ts:33`, candlestick/scatter/bar inline resolvers, qa scenario
- **Acceptance:** prop-less Grid renders horizontal lines exactly as legacy grid.tsx:97

Coverage resolution (OQ parity 1 / AX2, quoted): "FIX — restore resolver default `horizontal: true` (grid.ts:33 → match legacy grid.tsx:97). Add a prop-less `<Grid />` QA scenario so gates stop being blind."

Grounding (verified in working tree, 2026-08-22):
- Resolver site: `showcase/migrated/charts/internal/grid.ts` — `resolveGridGuide()`, line 33 is currently `horizontal: grid?.horizontal ?? false`.
- Legacy parity reference: `repos/bklit-ui/packages/ui/src/charts/grid.tsx` (default-horizontal behavior around line 97).
- The task row also names "candlestick/scatter/bar inline resolvers": the grid.ts header states the per-chart `grid?.horizontal ?? false` triples were already consolidated into `resolveGridGuide` (initiative 3/D1). Grep `grid?.horizontal` under `showcase/migrated/` and fix any surviving inline site — edit where the default is physically resolved (code state decides *where*, go-to-plan decides *what*).

**AMBIGUITY FLAG — do not resolve yourself:** the "prop-less `<Grid/>` QA scenario" deliverable means adding a scenario under `bench/app/src/scenarios/` (plus possible qa wiring) — inside your no-write zone (`qa/`, `bench/`). Implement the resolver fix; leave the scenario deliverable undone and report it as blocked-by-write-guard for a lead/harness-owner decision.

## Gates (all must pass before reporting done)
- `cd showcase && npx tsc --noEmit` exits 0.
- **Wave gate-runner (D246 impl/gate split)**: do NOT run QA pixel compares or `qa/console-errors.mjs` yourself — a single post-wave gate-runner sweeps QA + console for all Wave 1 packages together. Report the affected charts/densities for it to sweep: candlestick (`--n 1000`), scatter (`--n 1000`), bar (`--n 100` — bar-family density rule: NEVER n≥1000).

## Report back
- What changed per task (file + resolved default; any surviving inline resolver site found).
- Gate evidence: typecheck exit status. List the affected chart(s)/scenario(s)/density for the wave gate-runner to sweep (QA pixel compare + console errors).
- The specific density/scenario notes below MUST be included in that list (do not let the gate-runner default to n=1000 where a different density is required).
- Anything unresolved or intentionally skipped, with reason — report failures honestly; do not omit them.
- The blocked QA-scenario deliverable, explicitly flagged (not silently skipped), plus anything unresolved.
