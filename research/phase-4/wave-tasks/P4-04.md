# P4.4 — T-C6 (legend hover unify + intFmt) + T-C9 (XAxisLabelOverlay) + T-C10 minus deferred (inline dedup batch) (Wave 4)

Repo root: /Users/tomasdomingos/bklit-tanstack-charts-migration

## WRITE INCREMENTALLY
Write each edited file to disk as soon as its edit is complete — do not batch work to the end. Uncommitted work on disk survives interruption; a resumed session picks up from the working tree.

## Rules
- MAIN GOAL: exact same frontend design/interactivity as legacy bklit; TanStack-native backend; identical public component API.
- D232: fixes live in committed showcase/ code ONLY — never patch the clones (`repos/bklit-ui/`, `showcase/repos/tanstack-charts/`; both gitignored, Vercel re-clones fresh). Clones are read-only reference.
- Do NOT touch: `qa/`, `bench/` (running their scripts is allowed), the clones, /tmp. A guard hook denies these writes.
- You cannot delete files (guard policy D216). If this package requires deleting a module, do the un-wiring, leave the orphaned file in place, and report it to the lead for deletion.
- Do not commit; leave changes in the working tree.
- go-to-plan.md is the source of truth (staleness rule 4.4.4); synthesis docs (`research/phase-4/synthesis/centralize.md`, `research/phase-4/synthesis/redundancies.md`) may be stale relative to the current tree — **several items in this package's own source docs are stale, see below.**
- Migrated code root: `showcase/migrated/charts/` + `showcase/migrated/charts/internal/`.

## DO NOT TOUCH — explicitly deferred out of this package

Per go-to-plan.md's Moves log, two T-C10 items are **deferred to P6.2** and must **not** be folded in here even though they live in files this package otherwise touches:

- **`xForDate` helper** — hand-rolled extended-x interpolation duplicated across `line-chart.tsx` (2 sites: lines 777, 825), `area-chart.tsx` (2 sites: lines 438, 466), and `composed-chart.tsx` (see C15 below — these are different call sites in the same file). The cross-file consolidation into one shared helper is **P6.2's job**, done after T-F1 (multi-axis `yAxisId`, Wave 6/P6.1) rewires line/area/composed's scale consumption — folding this now means double-touching those hosts before T-F1 lands.
- **Reveal-constant tokens** — same reason, same deferral target (P6.2).

One more T-C10 item is **held for P5.1's third sub-run**, not this package:

- **Sunburst `maxRevealDelayMs`** — currently exists as both a local `useMemo` in `sunburst-chart.tsx:922` (with `labelsRevealDelayMs = maxRevealDelayMs + enterDurationMs * 0.85` at 927) and an exported function `maxRevealDelayMs(arcs, staggerScale)` in `internal/sunburst-reveal.ts:75`. Held until after T-D12 (sunburst native swap, P3.6) lands, since native sunburst decides which geometry/reveal helpers survive — touching it now risks being immediately obsoleted.

**An executor that "helpfully" folds any of these three items in breaks a later package's gate spec (P6.2's or P5.1's).** If you find yourself editing `line-chart.tsx`/`area-chart.tsx`'s `xForDate` sites or `sunburst-chart.tsx`/`internal/sunburst-reveal.ts`'s `maxRevealDelayMs` for any reason, stop and reconsider — that work belongs to a different package.

## Scope

### Strand 1 — T-C6: legend hover unify + intFmt

**Legend hover.** `internal/chart-legend-hover.tsx` (44 lines) is the canonical provider: `ChartLegendHoverProvider({hoveredIndex, onHoverChange, children})`, `useChartLegendHover()` returns `{hoveredIndex, setHoveredIndex}` (no-op fallback outside provider). Importers: `area-chart.tsx`, `bar-chart.tsx`, `candlestick-chart.tsx`, `composed-chart.tsx`, `line-chart.tsx`, plus barrel files.

`internal/profit-loss-legend-hover.tsx` (31 lines) is an **orphan** with real structural divergence, not a trivial duplicate: `ProfitLossLegendHoverProvider({hoveredIndex, children})` has **no `onHoverChange` prop** (read-only), and carries a `CHART_CHILD_PASSTHROUGH` symbol marker (line 26, `Symbol.for("migrated.chartChildPassthrough")`, canonical export now lives at `children.tsx:36`) that the canonical provider lacks. `useProfitLossLegendHover()` returns `{hoveredIndex}` only. Importers confirmed via grep: only barrel files (`index.ts`, `internal/index.ts`) — **zero real chart consumers**. **A trivial alias will not work** — the signature and the passthrough marker differ. Resolve by either (a) confirming it's dead and can be un-wired from the barrels (leave the file for lead deletion, D216), or (b) if something outside `showcase/migrated/` actually consumes it via the barrel re-export, fold it into the canonical provider as a read-only mode (`onHoverChange` optional) and thread the passthrough marker through. Check barrel re-export consumers outside this directory before deciding it's dead.

**intFmt.** Canonical `intFmt` lives at `internal/formatters.ts:24` (`export const intFmt = new Intl.NumberFormat("en-US").format;`). Two sites locally redeclare it instead of importing the canonical one:
- `internal/legend.tsx:163`
- `internal/chart-legend.tsx:7`

Fix: import from `formatters.ts`, drop the local redeclarations.

### Strand 2 — T-C9: XAxisLabelOverlay layer

Two modules render pixel-identical x-axis label chrome with different tick-selection algorithms underneath:

- `internal/x-axis-overlay.tsx` (227 lines) — importers: `area-chart.tsx`, `bar-chart.tsx`, `scatter-chart.tsx`, `candlestick-chart.tsx`, `line-chart.tsx`, `composed-chart.tsx`. Chrome: `bottom: 12` (195), `data-bkm-xlabel=""` (211), `data-bkm-x={tick.x}` (212), `fontSize: 12` (215), `color: "var(--color-chart-label, var(--chart-label))"` (217), `transition: "opacity 0.4s ease-in-out"` (218).
- `internal/bar-x-axis-overlay.tsx` (90 lines) — importer: `bar-chart.tsx` only. **Byte-identical presentational chrome** at corresponding lines: `bottom: 12` (56), `data-bkm-xlabel=""` (65), `data-bkm-x={item.x}` (66), `fontSize: 12` (69), `color: ...` (71), `transition: ...` (72).

**This is not a naive merge.** The header comment in `bar-x-axis-overlay.tsx` explicitly notes it deliberately does **not** use `x-ticks.ts`'s tick-thinning algorithm — bar keeps its own per-part tick selection on purpose. Extract only the shared *presentational* chrome (the DOM/style properties listed above — a `XAxisLabelOverlay` leaf component or shared style object) into one place both modules render through; do **not** unify the tick-selection/thinning logic itself, which is intentionally different between bar and the rest.

### Strand 3 — T-C10 (minus deferred): inline dedup batch

Confirmed-real items to fix:

1. **`useSanitizedId`** — canonical hook: `internal/use-sanitized-id.ts` (grep `useSanitizedId`/`use-sanitized-id`). Inline `useId().replace(/[^a-zA-Z0-9_-]/g, "")` duplicated across 14 files (confirmed via `grep -rln "useId().replace"`): `area-chart.tsx` (5 sites: 213, 273, 310, 374, 983), `bar-chart.tsx` (2: 376, 403), `candlestick-chart.tsx:200`, `composed-chart.tsx`, `gauge.tsx`, `line-chart.tsx:190`, `scatter-chart.tsx:282`, `internal/background.tsx`, `internal/brush-chrome.tsx`, `internal/dash-tail.ts`, `internal/heatmap-components.tsx`, `internal/heatmap-legend.tsx`, `internal/loading-chrome.tsx`, `internal/reference-area-layer.tsx`. Replace each with the canonical hook. **Stale citation to drop**: centralize.md's original list also cited `bar-pulse-overlay.tsx` as a site — that file **no longer exists** (deleted in Wave 2, D252); do not look for it.

2. **`prefersReducedMotion`** — canonical hook: `internal/use-prefers-reduced-motion.ts` (lines 4/10). Inline `matchMedia("(prefers-reduced-motion: reduce)")` call sites confirmed at: `area-chart.tsx:866,933`, `line-chart.tsx:638,713`, `composed-chart.tsx:1220,1328`, `internal/segment-visuals.tsx:31`, `internal/reference-area-layer.tsx:219`, `internal/chart-markers.tsx:183`. Replace with the canonical hook. (centralize.md also claims funnel-chart.tsx sites — re-grep `funnel-chart.tsx` at dispatch; not independently confirmed during drafting, don't assume it's there without checking.)

3. **`Symbol.for("migrated.chartRole")` → `CHART_ROLE` import** — canonical constant exported at `children.tsx:35` (`export const CHART_ROLE = Symbol.for("migrated.chartRole")`). Two sites still construct the raw symbol inline instead of importing the constant: `internal/chart-selection.ts:193` and `internal/reference-area-config.ts:23,68` (two call sites in that one file). Replace all three with `CHART_ROLE` imports. This matters beyond style — `Symbol.for` is a *global* registry lookup; two independently-constructed calls with the same string are referentially equal so it isn't currently broken, but importing the constant is the intended pattern per `children.tsx` and removes the redundant registry lookups.

4. **`SegmentComponent` — duplicate-named interface, not just duplicate logic.** `internal/segment-visuals.tsx:8` declares `export interface SegmentComponent { ... }` and `internal/chart-selection.ts:178` **separately** declares its own `export interface SegmentComponent { ... }` (plus `extractSegmentComponents` at 184, returning `SegmentComponent[]`). All four consumer charts (`area-chart.tsx:49`, `candlestick-chart.tsx:45`, `line-chart.tsx:31`, `composed-chart.tsx:91`) import `extractSegmentComponents` from `chart-selection.ts` — so `chart-selection.ts`'s version is the live/consumed one. `segment-visuals.tsx`'s same-named interface is a second, independent declaration — check whether it's structurally identical (in which case re-export one from the other) or actually different (in which case the same name for two different shapes is a latent footgun — rename one). Confirm which by reading both interface bodies in full before touching either.

5. **Choropleth zoom-writer merge.** `choropleth-chart.tsx` has two `useCallback` hooks with near-duplicate DOM-transform-writing logic: `syncZoomTransform` (394-415) and `applyZoomToGroups` (417-435). Both compute the same `t`/`tr` transform-string/transition-string pair from a `ZoomWithDrag`, write `transform`+`style.transition` to `marksGRef.current`/`graticuleGRef.current`, and write identical `svg.style.touchAction`/`cursor`/`contain` properties. The only real difference: `applyZoomToGroups` additionally takes an explicit `svg`/`marksG` param and assigns `marksGRef.current = marksG` (used at first-render wiring, `handleRender`, line 444), while `syncZoomTransform` reads refs directly (used at 595-596, rAF-guarded). Merge into one function parameterized by the optional explicit refs, or have `applyZoomToGroups` call `syncZoomTransform` after establishing the refs — don't just delete one, the ref-establishment side effect in `applyZoomToGroups` (line 422) is load-bearing for first render.

6. **C15 — composed-chart.tsx terminal-anchor helper.** This is the item the coverage-audit resolutions table calls out by name ("C15 terminal/end-anchor math x3 — FIX — centralize into one helper; added to T-C10 list"). **The actual count in the current tree is 4 sites, not 3** — `research/phase-4/synthesis/redundancies.md:67` says "x3" and centralize.md's cross-file count implies 2 within this file; direct verification during drafting found **four**, all doing the identical `((t - teRaw.minTime) / (te.maxTime - teRaw.minTime)) * innerW` extended-x interpolation:
   - `composedTerminalAnchors` useMemo (starts ~676), inline `xForDate` at 686-690.
   - `composedEndAnchors` useMemo (starts ~706), inline `xForDate` at 714-718 (byte-identical to the above).
   - `projectionGradientDefsComposed` useMemo (starts ~737), inline `xScaleWithProjection` at 746-750 (same math, different local name — missed by centralize.md's literal-text `xForDate` search).
   - A projection-line-marks-building block (starts ~863), inline `xScaleWithProjection` at 870-874 (same math again — a fourth site missed by both prior counts).

   **Scope boundary — this is the tricky part.** C15's charter is "extract one local helper" **within composed-chart.tsx only**. The deferred cross-file `xForDate` item (see DO NOT TOUCH above) covers the *same lines of code* from a different angle (consolidating across line/area/composed). Resolve this tension exactly as follows: extract all 4 sites in `composed-chart.tsx` into **one local helper function inside `composed-chart.tsx`** (not exported, not shared with line-chart.tsx/area-chart.tsx). Do **not** create a cross-file shared module — that's P6.2's job once T-F1 lands. Do **not** touch `line-chart.tsx`'s or `area-chart.tsx`'s own `xForDate` sites. When P6.2 runs later, it will supersede your local helper with a shared one — that's expected and fine, don't try to preempt it.

### Not confirmed as duplicated — verify at dispatch, do not invent work if absent

The following T-C10 items are named in centralize.md but grep against the current tree during drafting did **not** find the claimed duplication. Re-check at dispatch; if still absent, drop from scope and report as a stale citation rather than fabricating a fold:

- **`DEFAULT_MARGIN`** — exists in 11 files but the values are **not** the same duplicate-of-one-value; `internal/use-chart-margin.ts:9` itself documents "values differ per chart family." 6 charts (area/bar/scatter/line/composed/candlestick) do share the identical `{top:40,right:40,bottom:40,left:40}`, but radar (`60`, scalar not object), sankey (`{40,180,40,180}`), choropleth (`{0,0,0,0}`), live-line (`{24,16,32,16}`), and heatmap (`{28,16,0,40}`) are legitimately different. If you fold anything here, fold only the 6 identical ones into one shared constant — do not touch the others, and note `use-chart-margin.ts`'s own comment suggests the per-family split may be intentional, not an oversight.
- **`checkRevealGuard`** — already appears centralized in `internal/deferred-reveal.ts` (exported at line 39, consumed internally at 160 and 227). Grep found no second implementation elsewhere (no separate "svg-root variant"). If you find one at dispatch, fold it; if not, this item may already be satisfied — report it as such rather than inventing a merge.
- **`blurPx` / brush clamp helper** — `internal/chart-brush.tsx:41` defaults `blurPx = 1.5` and passes it down to `internal/brush-chrome.tsx`, which clamps it (`Math.min(5, Math.max(0, blurPx))`, line 61). Grep found no second default/clamp site elsewhere. Likely already a single flow — verify, don't invent a second site to merge.

## Gate

Legend gate density is **n=4** (legend is a chart-less HTML scenario; `n` is ignored by the component itself but the harness still needs it as an argument).

This package touches: area, bar, candlestick, composed, line (legend hover, XAxisLabelOverlay, useSanitizedId/prefersReducedMotion sites), scatter (useSanitizedId), choropleth (zoom-writer merge), heatmap (useSanitizedId), gauge (useSanitizedId).

Mandated densities: bar family NEVER n≥1000 (use **100**); sankey **33**; heatmap **52**; legend **4**; most others **1000**.

`qa/screenshot.mjs` compares **`bklit` vs `migrated`**. Never probe `impl=tanstack`.

Known chronics — check `qa/results/<chart>/` history before reporting any failure as new: pie hover-30/hover-70 ~3.18% is **RETIRED** (D254); markers n=100 hover-30 ~0.5507% is **chronic and still open**.

## Report back

1. T-C6: legend-hover resolution for `profit-loss-legend-hover.tsx` (dead-and-unwired vs. folded-with-read-only-mode), with evidence for which it was. The two `intFmt` fixes.
2. T-C9: confirmation the shared presentational chrome is extracted without touching bar's distinct tick-selection algorithm.
3. T-C10 items 1-3 (useSanitizedId, prefersReducedMotion, CHART_ROLE): full site list actually fixed, plus confirmation on the funnel-chart.tsx prefersReducedMotion claim (present or absent).
4. T-C10 item 4 (`SegmentComponent`): whether the two interfaces were structurally identical or genuinely different, and what you did.
5. T-C10 item 5 (choropleth zoom-writer merge): confirmation the `marksGRef.current = marksG` side effect is preserved.
6. C15 / T-C10 item 6: confirmation exactly 4 sites in `composed-chart.tsx` were consolidated into one **local, non-exported** helper, and confirmation `line-chart.tsx`/`area-chart.tsx` were not touched.
7. The three "not confirmed as duplicated" items: what you found at dispatch (still absent / found and fixed), for each.
8. Any orphaned files left for lead deletion (D216).
9. Anything contradicting this brief, stated plainly with evidence.
