# P1.14 — Bar pulse wave resurrect (B17 → `barPulseMark`) (Wave 1)

Repo root: /Users/tomasdomingos/bklit-tanstack-charts-migration

## WRITE INCREMENTALLY
Write each edited file to disk as soon as its edit is complete — do not batch work to the end. Uncommitted work on disk survives interruption; a resumed session picks up from the working tree.

## Rules
- MAIN GOAL: exact same frontend design/interactivity as legacy bklit; TanStack-native backend; identical public component API.
- D232: fixes live in committed showcase/ code ONLY — never patch the clones (`repos/bklit-ui/`, `showcase/repos/tanstack-charts/`; both gitignored, Vercel re-clones fresh). Clones are read-only reference.
- Do NOT touch: `qa/`, `bench/` (running their scripts is allowed), the clones, /tmp. A guard hook denies these writes.
- Do not commit; leave changes in the working tree.
- go-to-plan.md is the source of truth (staleness rule 4.4.4); legacy reference for pixel/behavior parity is the bklit clone source.
- Migrated code root: `showcase/migrated/charts/` + `showcase/migrated/charts/internal/` (consumed by `showcase/` via the `@showcase/migrated-charts` wrapper package).

## Tasks

### T-W1-19 (verbatim from go-to-plan.md, Batch W1)
- **Task:** Bar: B17 pulse wave resurrected into `barPulseMark` (then delete overlay)
- **Files:** bar-pulse-mark, bar-pulse-overlay
- **Acceptance:** loading wave animates as legacy (pixel gate)

Coverage resolution (OQ redund 3, quoted): "FIX — resurrect wave: port animation loop from dead `bar-pulse-overlay.tsx` into `barPulseMark`, then delete the overlay file (B17). Pixel-gate vs bklit."

Scope split (plan Moves + dependency graph): port the animation loop INTO `internal/bar-pulse-mark.ts` in this package. The overlay FILE DELETION is explicitly W2's job — "T-W1-19 → T-W2-1: Overlay file deleted only after pulse loop resurrected into `barPulseMark`." Do NOT delete `bar-pulse-overlay.tsx` here; leave it untouched for P2.1.

Grounding: `charts/internal/bar-pulse-mark.ts` (already imported and consumed by `charts/bar-chart.tsx:588`) and `charts/internal/bar-pulse-overlay.tsx` (dead code — READ it as the source of the loop to port). Legacy reference: the bklit clone's bar loading-pulse rendering.

## Gates (all must pass before reporting done)
- `cd showcase && npx tsc --noEmit` exits 0.
- Loading-state check of the pulse wave per the established loading-capture protocol (reduced-motion static frame; dynamic animation verified interactively or via scripted frames). This is a package-level functional check, not swept by the gate-runner.
- **Bar-family density rule: NEVER run gates at n≥1000 for bar charts (AGENTS.md).**
- **Wave gate-runner (D246 impl/gate split)**: do NOT run QA pixel compares, bench, or console checks yourself — swept/compared once for the whole wave (bench in an exclusive slot). Report for the gate-runner: barsquares (`--n 100`), bardepth (`--n 100`), console entries barsquares/bardepth (n=100), and that this package (bar family) should be included in the wave's bench-vs-baseline compare against the P0.2 baseline in `docs/phase-4/BENCHMARKS.md`.

## Report back
- What changed (files + behavior); confirm the overlay was left in place for P2.1.
- Gate evidence: typecheck exit status. List the affected chart(s)/scenario(s)/density for the wave gate-runner to sweep (QA pixel compare + console errors).
- The specific density/scenario notes below MUST be included in that list (do not let the gate-runner default to n=1000 where a different density is required).
- Anything unresolved or intentionally skipped, with reason — report failures honestly; do not omit them.
