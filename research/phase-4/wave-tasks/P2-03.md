# P2.3 — styles.css orphans + dup block + `:has()` scoping + stale comments batch (Wave 2)

Repo root: /Users/tomasdomingos/bklit-tanstack-charts-migration

## WRITE INCREMENTALLY
Write each edited file to disk as soon as its edit is complete — do not batch work to the end. Uncommitted work on disk survives interruption; a resumed session picks up from the working tree.

## Rules
- MAIN GOAL: exact same frontend design/interactivity as legacy bklit; TanStack-native backend; identical public component API.
- D232: fixes live in committed showcase/ code ONLY — never patch the clones (`repos/bklit-ui/`, `showcase/repos/tanstack-charts/`; both gitignored, Vercel re-clones fresh). Clones are read-only reference.
- Do NOT touch: `qa/`, `bench/` (running their scripts is allowed), the clones, /tmp. A guard hook denies these writes.
- Do not commit; leave changes in the working tree.
- go-to-plan.md is the source of truth (staleness rule 4.4.4); legacy reference for pixel/behavior parity is the bklit clone source.
- Migrated code root: `showcase/migrated/charts/` + `showcase/migrated/charts/internal/`.

## Wave 2 is DELETE-ONLY — no behavior change
Prove deadness before deleting. For CSS that means grepping the class name across the migrated tree **and** the showcase app, including template-literal and `clsx`-style construction sites where the class may be assembled from fragments. If removing something would change a rendered pixel, it is out of scope — stop, leave it, report it.

## Tasks

### T-W2-3 (verbatim row)
- **Task:** Delete orphaned pie-center CSS + duplicate styles.css loading block (after verification item 3) + `:has()` scoping fix (FD16)
- **Files:** styles.css

Three parts:

1. **Orphaned pie-center CSS** — `.ts-bkm-pie-center` rules (P12). Recorded in `synthesis/parity.md:113` as *"orphaned `.ts-bkm-pie-center` CSS … dead rules"*. Confirm no consumer, then delete.

2. **Duplicate loading block.** go-to-plan.md:174 already did this verification for you and it is conclusive:
   > Item 3 (styles.css): second block (928–984) is a **verbatim subset** of the first (846–926, which additionally owns the shimmer keyframes + scoped base rule). Delete second wholesale; first is authoritative.

   Line numbers are from the research snapshot and Wave 1 shifted `styles.css` (+20/−? lines). **Re-locate both blocks by content, not by line number**, and re-confirm the subset relationship before deleting. If the second block is no longer a verbatim subset, STOP and report — the ruling's premise would no longer hold.

3. **`:has()` scoping fix (FD16).** `synthesis/parity.md:244`: legacy has a scoped selector; migrated ported an **unscoped** `:has()`. Ruling is *"ACCEPT ruled precedents; FIX scoping"* — scope the selector to match legacy. This is the one item in this package that is not a pure deletion; it is a correctness fix, and it is the item most likely to move pixels. Treat it with care.

### T-W2-4 (verbatim row)
- **Task:** Stale comments batch (pie:35, focus-disabled header, gauge.tsx:21, styles.css:548, grid.ts:7-9, sunburst-labels header — after SB12 makes it true) + G6 headers + P11
- **Files:** comments only

Comment-only edits. Make each comment **true of the current code** — do not delete a comment that is merely terse, and do not invent new documentation.

Named sites (line numbers are pre-Wave-1; locate by content):
- `pie` line ~35 — stale D49 header comment (P11, `synthesis/parity.md:113`).
- focus-disabled header — the claim is wrong (G6, `synthesis/parity.md:128`).
- `gauge.tsx` ~21 — plus `gauge-arc-mark` CSS refs that are now dead (G6).
- `styles.css` ~548.
- `grid.ts` lines 7–9.
- `sunburst-labels` header — **precondition met**: T-W1-11 (SB12) landed in Wave 1, so the header's claim is now true and the stale wording can be corrected. Verify against the current `internal/sunburst-labels.tsx` before rewording.

## Sunburst / choropleth — fences now CLEARED (settled facts, D250/D251)
Those fix dispatches have finished and their outcomes are known, so you are not racing anyone:

- **`styles.css` and `internal/tooltip-chrome.ts` were NOT touched** by the choropleth fix — it stayed inside `internal/choropleth-hover-chrome.ts`. So the `styles.css` you are editing is unchanged from the state the T-W2-3 rulings were written against.
- **Sunburst did change**: `sunburst-chart.tsx` was rewritten further and a **new file `internal/sunburst-hit.tsx`** was added (a ported bklit hit-test layer). `internal/sunburst-labels.tsx` — the file whose header comment you must correct — was **not** modified by the fix. Still read the current file before rewording, per the task.

One live fence remains: **P2.2 is running** and owns dead-var deletions in `line-chart.tsx`, `bar-chart.tsx`, `composed-chart.tsx`, `funnel-chart.tsx`, `scatter-chart.tsx`, `internal/hover-chrome.ts`, `internal/scatter-hover-chrome.ts`, `internal/candlestick-hover-chrome.ts`, `internal/bar-hover-chrome.ts`, `internal/ring-hover-chrome.ts`, live-line and ring. Do not edit those. Comment-only sites in `gauge.tsx`, `pie`, `grid.ts`, `styles.css` are yours.

## Gates (all must pass before reporting done)
- `cd showcase && npx tsc --noEmit` exits 0.
- Production build must succeed.
- **Wave gate-runner (D246 impl/gate split)**: do NOT run QA pixel compares yourself — swept once for the whole wave. Per the plan row this package's gate is typecheck/build plus a **visual spot-check of styles.css consumers** — and `styles.css` is shared, so the consumer list is broad. Name it explicitly for the gate-runner rather than leaving it to default.
- **Density rules the gate-runner must be told, and must not default away from:** bar family NEVER `n≥1000`; sankey `--n 33`; sunburst `--n 27`; heatmap `--n 52`; markers/liveline/barsquares/bardepth `--n 100`; legend `--n 4`; most others `--n 1000`.
- The `:has()` scoping fix is the highest pixel risk in Wave 2. Flag it to the gate-runner as needing hover captures on every chart whose selector it governs, not just settled renders.

## Report back
- What was deleted/reworded, and the deadness evidence for each CSS deletion (the grep that proved no consumer).
- For the duplicate-block deletion: confirm you re-verified the subset relationship post-Wave-1, and say so.
- For `:has()`: the before/after selector, and exactly which charts it governs.
- Anything you found was NOT actually dead — report it, do not delete it anyway.
- Gate evidence: typecheck exit status, build exit status. The affected chart/scenario/density list for the gate-runner, with the density notes above verbatim.
- Anything unresolved or intentionally skipped, with reason — report failures honestly; do not omit them.
