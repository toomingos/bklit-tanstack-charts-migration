# P1.4 — Choropleth palette, formatter defaults, CP7 exit-fade restore (Wave 1)

Repo root: /Users/tomasdomingos/bklit-tanstack-charts-migration

## WRITE INCREMENTALLY
Write each edited file to disk as soon as its edit is complete — do not batch work to the end. Uncommitted work on disk survives interruption; a resumed session picks up from the working tree.

## Rules
- MAIN GOAL: exact same frontend design/interactivity as legacy bklit; TanStack-native backend; identical public component API.
- D232: fixes live in committed showcase/ code ONLY — never patch the clones (`repos/bklit-ui/`, `showcase/repos/tanstack-charts/`; both gitignored, Vercel re-clones fresh). Clones are read-only reference.
- Do NOT touch: `qa/`, `bench/` (running their scripts is allowed), the clones, /tmp. A guard hook denies these writes.
- Do not commit; leave changes in the working tree.
- go-to-plan.md is the source of truth (staleness rule 4.4.4); legacy reference for pixel/behavior parity is the bklit clone source.
- Migrated code root: `showcase/migrated/charts/` + `showcase/migrated/charts/internal/` (consumed by `showcase/` via the `@showcase/migrated-charts` wrapper package).

## Tasks

### T-W1-5 (verbatim from go-to-plan.md, Batch W1)
- **Task:** Choropleth palette → sequential `--chart-scale-01..05` (CP11)
- **Files:** choropleth colors
- **Acceptance:** default map coloring matches legacy tokens

### T-W1-6 (verbatim)
- **Task:** Choropleth defaults: intFmt, indexed fallback name, real index arg (CP4/5/6)
- **Files:** choropleth-chart
- **Acceptance:** tooltip text identical to legacy for same data

Coverage resolution (OQ parity 8, quoted): "FIX — restore `intFmt` default, indexed fallback name, real index arg; restore hook contract fields (`useChoropleth` full payload, `useChoroplethZoom` isDragging/transformMatrix)."
Scope split note: the hook-contract half (CP9/CP10) executes in **P5.6** (T-E4 renamed-compat batch) per go-to-plan's own cross-reference — NOT in this package.

### T-W1-9 — choropleth half only (verbatim row; plan Moves split this task across P1.4/P1.5)
- **Task:** Remove migrated-only visual deltas (DOC-1): HM16 highlight ring, CP7 exit fade (restore flip+instant unmount via shared TooltipBox)
- **Files:** heatmap, choropleth
- **Acceptance:** hover visuals indistinguishable from legacy

Your scope: the **CP7** half only (restore tooltip flip + instant unmount via the shared TooltipBox). The HM16 heatmap half belongs to P1.5 — do not touch heatmap files.

Sequencing fold-in (contradiction note K-2, quoted): "OQ parity-8 restores CP7 tooltip flip+instant unmount via shared TooltipBox (T-W1-9), but T-D8's custom-retained list includes 'fade-gradient' chrome for choropleth's neighborhood; if CP7 lands first the retained-chrome inventory for choropleth shrinks. Not a ruling conflict — a sequencing note: T-W1-9 output feeds T-D8 scoping." (Resolution accepted; P3.9 recomputes choropleth's inventory at gate time.)

Grounding: `charts/choropleth-chart.tsx`, `internal/choropleth-hover-chrome.ts`; the shared TooltipBox box-building lives in `internal/tooltip-chrome.ts` (TooltipBox panel styles in `charts/styles.css`). Locate the legacy flip/instant-unmount behavior in the bklit clone for parity.

## Gates (all must pass before reporting done)
- `cd showcase && npx tsc --noEmit` exits 0.
- **Wave gate-runner (D246 impl/gate split)**: do NOT run QA pixel compares or console checks yourself — swept once for the whole wave. Report for the gate-runner: choropleth incl. hover captures (`--n 1000`); choropleth is not in `qa/console-errors.mjs`'s fixed target list — flag that gap rather than covering it yourself.

## Report back
- What changed per task (files + behavior).
- Gate evidence: typecheck exit status. List the affected chart(s)/scenario(s)/density for the wave gate-runner to sweep (QA pixel compare + console errors).
- The specific density/scenario notes below MUST be included in that list (do not let the gate-runner default to n=1000 where a different density is required).
- Anything unresolved or intentionally skipped, with reason — report failures honestly; do not omit them.
