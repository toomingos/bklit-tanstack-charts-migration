# P5.6 — T-E4: renamed-compat batch (CH2, PieCenterShell, ring R2–R4, choropleth CP9/CP10, MarkerTooltipContent, BR3) (Wave 5)

Repo root: /Users/tomasdomingos/bklit-tanstack-charts-migration

## WRITE INCREMENTALLY
Write each edited file to disk as soon as its edit is complete — do not batch work to the end. Uncommitted work on disk survives interruption; a resumed session picks up from the working tree.

## Rules
- MAIN GOAL: exact same frontend design/interactivity as legacy bklit; TanStack-native backend; identical public component API.
- D232: never patch the clones (`repos/bklit-ui/`, `showcase/repos/tanstack-charts/`; both gitignored, read-only reference).
- Do NOT touch: `qa/`, `bench/` (running their scripts is allowed), the clones, `/tmp`. A guard hook denies these writes.
- Executors cannot delete files (guard policy D216).
- Do not commit; leave changes in the working tree.
- go-to-plan.md is the source of truth (staleness rule 4.4.4).
- Migrated code root: `showcase/migrated/charts/` + `showcase/migrated/charts/internal/`.
- `qa/screenshot.mjs` compares **`bklit` vs `migrated`**. Never probe `impl=tanstack` (D258).
- **Scope boundary vs P5.2**: P5.2 (T-E1b) barrel-exports `useRingStable`/`useRingHoverCoordinator` **as they exist today** (current names, current reduced payload). Do not redo that plain wrapping here — this package's job is the *correctness* fixes (payload restoration, renaming, the missing combiner) layered on top. If P5.2 hasn't landed those exports yet when you start, coordinate rather than duplicate.

## ~~Hard blocker — this package cannot start until P4.3 lands CenterShell~~ — **RESOLVED, THE BLOCKER IS STALE (verified 2026-08-25, D322)**

> **⚠ DO NOT ACT ON THE BLOCKER BELOW. IT IS FALSE AND WOULD COST YOU STRAND 4.**
>
> **`CenterShell` EXISTS: `showcase/migrated/charts/internal/center-stat.tsx:310`**
> (`export function CenterShell<T>({`). It is consumed in production — `internal/gauge-center.tsx:30-36`
> imports `CenterShell`/`CenterStat` from `./center-stat`.
>
> **Wave 4 is CLOSED.** P4.1–P4.6 have all executed and are ticked in `docs/phase-4/PROGRESS.md`.
> The paragraph below was written when they were not, and its grep found "only comment
> references" because at that time the module genuinely did not exist.
>
> **Strand 4 (`PieCenterShell`) is therefore UNBLOCKED and IN SCOPE.** Read `CenterShell`'s
> real signature at `center-stat.tsx:310` before wrapping — do not guess it from
> go-to-plan.md's prose, and do not go looking for P4.3's report to learn the API when the
> source is right there.
>
> This is the same premise-rot that produced D317/D320/D321, in its D310 (negative-existence)
> form: *"no actual module exists"* was true when written and is false now. Had it been
> dispatched unedited, the most likely outcome was Strand 4 skipped as "still blocked" —
> a silent scope loss that the charter's own report-back item 4 tries to guard against but
> cannot, because the charter is the thing that is wrong.

**[HISTORICAL, SUPERSEDED]** `PieCenterShell` is a thin wrapper over `CenterShell`, and `CenterShell` does not exist yet. It is chartered to `research/phase-4/wave-tasks/P4-03.md` (T-C4, "CenterShell merge, including R7 `RingCenter` real import and the gauge intro opt-in"). Checked immediately before writing this brief: `docs/phase-4/PROGRESS.md` shows **Wave 4 (P4.1–P4.5) entirely unchecked** — P4.3 has not executed. Grepping `showcase/migrated/charts/gauge.tsx` and `internal/gauge-center.tsx` for `PieCenterShell`/`CenterShell` finds only comment references (`gauge.tsx:77`; `gauge-center.tsx:9,12,40,45`) — no actual module exists. **Check `docs/phase-4/PROGRESS.md` for P4.3's status before starting the `PieCenterShell` strand specifically** — if it still hasn't landed, do the other four strands (CH2, ring R2–R4, choropleth CP9/CP10, MarkerTooltipContent, BR3) and leave `PieCenterShell` for a resumed session, rather than blocking the whole package on one strand.

## Scope

### Strand 1 — CH2: `CHART_CLIP_PASSTHROUGH` alias

Legacy names this symbol `CHART_CLIP_PASSTHROUGH`; migrated renamed it `CHART_CHILD_PASSTHROUGH` (`showcase/migrated/charts/children.tsx:36`, `Symbol.for("migrated.chartChildPassthrough")`, alongside `CHART_ROLE` at line 35). Ruling is low-severity, either branch acceptable: *"FIX alias or ACCEPT-document (internal contract)"* (`synthesis/parity.md:185`). Given P5.1's CONST sub-run will already export the current-named `CHART_CLIP_PASSTHROUGH` symbol from the barrel if it exists under that name (check whether P5.1 landed before or after you — see the ordering note below), your job specifically is: add a `CHART_CLIP_PASSTHROUGH` alias (`export const CHART_CLIP_PASSTHROUGH = CHART_CHILD_PASSTHROUGH;` or equivalent barrel re-export under the old name) so legacy-named consumers resolve, and document in a comment that the two names are the same `Symbol.for(...)` value under old/new naming. ~~**Since P4.3 may block this whole package, do this strand first regardless**~~ — the P4.3 block is stale (D322); order strands as you see fit. This one is still the cheapest and a fine place to start.

### Strand 2 — ring R2–R4 (do NOT re-cite R6/R7/R8 — out of scope here)

Full evidence (`synthesis/parity.md:114-122`):
- **R2** — `useRingStable` payload reduced. Legacy's `RingStableContextValue`/`RingHoverContextValue` (`repos/bklit-ui/packages/ui/src/charts/ring-context.tsx`) carries `hoveredIndex: number | null`, `animationKey: number`, `isLoaded: boolean`, `containerRef: RefObject<HTMLDivElement | null>` among other fields. Migrated's `RingStableValue` (`showcase/migrated/charts/ring-chart.tsx`, interface with `data, size, center, strokeWidth, ringGap, baseInnerRadius, enterTransition?, enterStaggerScale, totalValue, getColor, getRingRadii, startAngle, endAngle, geometryScrubbing, scrubRingLayers`) has none of those four. Ruling: *"restore fields or version hook"* — restore the missing fields onto the existing hook/context value rather than introducing a second hook version, unless restoring them turns out to conflict with the TanStack-native store architecture, in which case document why you version instead.
- **R3** — `useRingHover` → `useRingHoverCoordinator` rename, and it's not just a rename: legacy is a React-context hover pair; migrated is *"documented store contract; rename breaks consumers"* — an imperative store (`get`/`request`/`set`/`subscribe`). Add a legacy-named `useRingHover` compat wrapper that adapts the imperative store shape back to whatever consumers actually need (check real call sites, not just the type, before deciding what "compat" needs to expose).
- **R4** — `useRing` combiner missing entirely. Legacy: `useRing() { return {...useRingStable(), ...useRingHover()}; }` (`repos/bklit-ui/packages/ui/src/charts/ring-context.tsx:184-185`). Add the same combiner in migrated, built on top of your R2/R3 work (the combiner should read the *restored* payload, not the pre-fix reduced one — sequence R2/R3 before R4 within this strand).

Do not touch R5 (barrel-policy tiering OQ — not chartered to this package), R6 (default exports — P5.4's job), R7 (RingCenter orphan displayName coupling — P4.3's job, already in P4-03.md's scope), or R8 (deprecated `groupEl` shim — not chartered here) even if you notice them while in this file.

### Strand 3 — choropleth CP9/CP10 (field restoration on already-exported hooks)

Both `useChoropleth`/`useChoroplethZoom` **already exist and are already barrel-exported** (`showcase/migrated/charts/index.ts:186-187`) — this strand is not about adding exports, it's about the *contracts being narrower than legacy's*:
- **CP9** — `useChoropleth()`'s value is collapsed to `ChoroplethContextValue { width: number; height: number }` (`showcase/migrated/charts/choropleth-chart.tsx`, ~line 119-131) versus legacy's much richer `ChoroplethStableContextValue` (`repos/bklit-ui/packages/ui/src/charts/choropleth/choropleth-context.tsx`, starting ~line 68). Ruling: *"restore fields consumers need or version hook"* — read the legacy context's full field list, cross-check which fields real legacy callers actually read (not every field legacy exposes necessarily needs restoring — DOC-2's "additive non-visual extras" reasoning cuts the other way here: only fields real consumers depend on are worth restoring), and extend `ChoroplethContextValue` accordingly.
- **CP10** — `useChoroplethZoom`'s `ChoroplethZoomContextValue { zoom: ProvidedZoom<SVGSVGElement> | null }` is missing legacy's `ZoomState` extension (`initialTransformMatrix`, `transformMatrix`, `isDragging` — legacy's `ZoomInstance<E> = ProvidedZoom<E> & ZoomState`, `repos/bklit-ui/packages/ui/src/charts/choropleth/choropleth-context.tsx:17-28`). Note: the underlying visx zoom object at runtime may already carry these fields even though migrated's TS type doesn't expose them (`ProvidedZoom` itself may already have overlapping members) — check the runtime object before assuming you need new plumbing; this may be purely a type-widening fix, not new state tracking.

### Strand 4 — `PieCenterShell` (blocked on P4.3 — see above)

Once `CenterShell` exists (post-P4.3), build `PieCenterShell` as the thin wrapper go-to-plan.md describes and barrel-export it plus `PieCenterShellProps` (already in the worksheet's TYPE(97) list, but only meaningful once the component exists). Check `research/phase-4/wave-tasks/P4-03.md`'s own report-back (once P4.3 has actually executed and reported) for the exact CenterShell API surface before wrapping — do not guess its shape from the go-to-plan.md description alone if the executed package's report gives you the real signature.

### Strand 5 — `MarkerTooltipContent` barrel export

Fully implemented already, purely unexported: `showcase/migrated/charts/internal/marker-tooltip.tsx` — `MarkerTooltipContentProps` (line 19), `MarkerTooltipContent` function (line 23, `.displayName = "MarkerTooltipContent"` at line 87), plus its supporting infrastructure `ActiveMarkersStore` (89), `createActiveMarkersStore` (96), `MarkerActiveTooltipProvider` (125), `useActiveMarkerDate` (139), `useActiveMarkers` (148). **`useActiveMarkers` is P5.2's to export (worksheet HOOK tier, cross-ref LM7) — do not re-export it here, only `MarkerTooltipContent`/`MarkerTooltipContentProps`.** This strand is a pure barrel-wiring addition — no new code, just an `export { MarkerTooltipContent, type MarkerTooltipContentProps } from "./internal/marker-tooltip";` line (or equivalent) in `charts/index.ts`.

The worksheet's parity.md source (LM7, `synthesis/parity.md:257`) actually bundles `MarkerTooltipContent`+`useActiveMarkers` together with ruling *"Port or waive — open Q with CH5/FD1"* — the "port" question is already resolved (it's fully implemented, not a port decision at all), so treat LM7 as closed by inspection; if you find CH5/FD1 raise something that changes this, report it rather than reopening the port-vs-waive question yourself.

### Strand 6 — BR3: `BrushSelectedBoxStyle` passthrough extend

Migrated's `BrushSelectedBoxStyle` (`showcase/migrated/charts/internal/brush-chrome.tsx:25-40`) is a 4-field subset: `{ fill?, fillOpacity?, stroke?, strokeWidth? }`. Legacy's `selectedBoxStyle` prop (`repos/bklit-ui/packages/ui/src/charts/chart-brush.tsx`, multiple sites: lines 30/61/78/132/247/260/332) is typed `React.SVGProps<SVGRectElement>` — the full SVG rect prop surface. Ruling: *"FIX extend passthrough or ACCEPT"* (low severity, either acceptable). If you extend: widen `BrushSelectedBoxStyle` toward `SVGProps<SVGRectElement>` (or a documented larger subset covering what legacy callers plausibly set — check for real usages of unusual SVG rect props on `selectedBoxStyle` in bklit call sites before committing to the full type, since some SVG props may not make sense on a selection overlay rect). If you ACCEPT-document instead, write the reasoning inline near the type definition, not just in your report.

## Gate

- `pnpm typecheck` exit 0 + production build exit 0 for every strand.
- **CH2, MarkerTooltipContent barrel export**: pixel-inert (alias / bare re-export) — no pixel gate needed, resolution spot-check only (P5.1's method).
- **Ring R2–R4**: this changes `useRingStable`/`useRingHoverCoordinator`/`useRing`'s actual payload and hover-coordination behavior — needs a real interaction gate, not just typecheck. Gate ring at **n=1000** (mandated) and reuse P4.3's hover-capture-with-determinism method (3+ runs, confirm stable diff) if the ring hover-oscillation fix from P4.3 is already in place by the time you run this — do not let your R2–R4 changes reopen that defect. If P4.3 hasn't landed the oscillation fix yet, note that your gate results may be confounded by the pre-existing defect and say so.
- **Choropleth CP9/CP10**: Q1/Q2 choropleth at its mandated density, plus a targeted interaction check that consumers reading the newly-restored fields (zoom drag state, stable-context fields) actually observe correct live values, not just that the type compiles.
- **BR3**: brush selection-box visual spot-check with a non-default `selectedBoxStyle` set (to actually exercise whatever new fields you exposed), at brush's mandated density.
- **PieCenterShell**: Q1/Q2 pie/ring/gauge reveals (same scenario P4.3's own gate uses) once this strand executes.
- `qa/screenshot.mjs` compares **`bklit` vs `migrated`**. Never probe `impl=tanstack` (D258). Pie carries a known chronic hover-30/hover-70 delta (~3.18%, D249/P2.4) — **this one is RETIRED as of P2.4/D254, do not re-flag it as new** if you see numbers in that neighborhood; confirm against `qa/results/pie/` history rather than assuming it's still open.

## Report back

1. CH2: the alias mechanism you used and where.
2. Ring R2/R3/R4: exact fields restored on `useRingStable`, the `useRingHover` compat wrapper's shape, the `useRing` combiner — plus the 3-run determinism gate result at n=1000, and whether P4.3's oscillation fix was already present.
3. Choropleth CP9/CP10: exact fields added to each context value, and whether the underlying visx zoom object already carried the CP10 fields at runtime (type-only fix) or needed real new plumbing.
4. `PieCenterShell`: whether P4.3 had landed when you got here — if not, confirm you completed strands 1/2/3/5/6 and left this one explicitly open, not silently skipped.
5. `MarkerTooltipContent`: confirm this was purely a barrel-line addition with no `useActiveMarkers` re-export duplicated from P5.2.
6. BR3: which you did (extend vs ACCEPT-document) and why.
7. Anything contradicting this brief, stated plainly with evidence.
