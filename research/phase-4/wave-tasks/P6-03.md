# P6.3 — T-F2: close the decision-log gap — doctrine rulings D240–D245 + every unlogged ACCEPT row incl. Part-A orphans (Wave 6)

Repo root: /Users/tomasdomingos/bklit-tanstack-charts-migration

## WRITE INCREMENTALLY
Write each log entry to disk as soon as it's drafted and verified — do not batch all entries into one edit at the end. Uncommitted work on disk survives interruption; a resumed session picks up from the working tree.

## Rules
- MAIN GOAL: exact same frontend design/interactivity as legacy bklit; TanStack-native backend; identical public component API.
- D232: fixes live in committed showcase/ code ONLY — never patch the clones (`repos/bklit-ui/`, `showcase/repos/tanstack-charts/`; both gitignored, Vercel re-clones fresh). Clones are read-only reference. (Not directly relevant to this package — you are writing docs, not code — but the doctrine rulings you're logging reference this rule, so know it.)
- Do NOT touch: `qa/`, `bench/` (running their scripts is allowed), the clones, /tmp. A guard hook denies these writes.
- Do not commit; leave changes in the working tree.
- go-to-plan.md is the source of truth (staleness rule 4.4.4).
- **This is a documentation-only package.** You touch exactly one file: `docs/phase-4/LOG.md`. Do not edit `showcase/`, `bench/`, `qa/`, `repos/`, or `research/phase-4/go-to-plan.md`. If a ruling below turns out to require a code change nobody has made, that is a finding to report — not something to go implement yourself.

## The one rule that matters most for this package

**Do not invent rationale.** Every log entry you write must be traceable to something already recorded in `research/phase-4/go-to-plan.md`, `docs/phase-4/PROGRESS.md`, or an existing `docs/phase-4/LOG.md` entry. If an ACCEPT was made without recorded evidence — a ruling with no cited gate run, no cited measurement, nothing you can point to — **report the gap to the lead instead of writing a plausible-sounding justification for it.** An unsourced-but-plausible log entry is worse than a missing one: it looks authoritative and will be trusted by everyone who reads the log afterward, including future executors deciding whether something is "already settled."

## Current state of the log — read before writing anything

`docs/phase-4/LOG.md` is a single markdown table, **newest-first**, header at lines 5-6:

```
| D# | Date | Decision | Rationale |
|---|---|---|---|
```

It is currently written through **D261** (top row). The convention note at the top of the file: "D-number convention continues from `docs/phase-3/LOG.md`. Accepted parity deviations (4.3.1) and skipped-with-reason tasks (Definition of Done) get logged here."

## Part 1 — the doctrine rulings go-to-plan.md suggested logging as D240–D245

go-to-plan.md's doctrine table proposed specific D-numbers for five rulings at the time it was written. **All five numbers are now stale — do not use them.** The log has since advanced past them:

- **D240 is already consumed by an unrelated entry.** The existing `docs/phase-4/LOG.md` D240 (dated 2026-08-22) is titled "Wave-0 B4 (exclusive `bench --all` + sankey visual refs) CLOSED" — a bench-harness regression fix, unrelated to any doctrine ruling. Do not overwrite it, append to it, or renumber it.
- **D241, D242, D243, D244, D245 do not exist under any label anywhere in the file** — confirmed absent, genuinely unwritten.
- The log is now at D261. **Use the next available sequential numbers (D262 onward)** for the substance of each doctrine ruling below. To avoid losing the cross-reference go-to-plan.md's authors intended, **name both numbers in each new entry**: the new sequential D-number you're actually filing under, and the stale suggested number from go-to-plan.md in parentheses, e.g. "logged as D262 (go-to-plan.md's doctrine table suggested D240; that number was since consumed by an unrelated Wave-0 entry)."

Write one entry per ruling below, each sourced from go-to-plan.md's doctrine table (quote or closely paraphrase — these are already-decided rulings, your job is to get them into the permanent record, not to re-litigate them):

1. **DOC-4** (go-to-plan.md suggested D240): "Context/provider architecture removals stay ACCEPTED (D210-2 precedent): `*Provider`/`use*` internals not re-exported; thin legacy-named wrapper hooks added only where trivial (R3, R4, CP9/CP10 field restoration)."
2. **DOC-6** (go-to-plan.md suggested D241): "Event-model modernization ACCEPTED (mouse→pointer, added reduced-motion branches, pointer-events hardening)."
3. **DOC-7** (go-to-plan.md suggested D242): "`enterTransition` structural reshape ACCEPTED family-wide (framer `Transition` → structural `{tween|spring}` with same fallback timing). Exception: candlestick K4 gains sampled-keyframe tween support."
4. **DOC-9** (go-to-plan.md suggested D243): "Bar pilot scope cuts (B4 orientation, B5 stacked/stackGap/barWidth/squareSnap): ACCEPT for phase 4 with dev-only `console.warn` when set." Cross-check against `docs/phase-4/PROGRESS.md` P1.1/P1.3 (D247 records the harness fixtures the lead built for these exact acceptance criteria — `bklit-/migrated-griddefault` and `bklit-/migrated-composedstacked` scenarios) — cite that D247 corroboration in your new entry if it applies, since it's existing recorded evidence, not invented.
5. **DOC-10** (go-to-plan.md suggested D244): "Loading family package ruling: keep current design... ACCEPT + log D244: per-series `loading` controls stay accepted-inert (L5/A7); sweep/skeleton surface (FD7) deleted; heatmap shimmer stays cut with `loadingCell*` props accepted-inert; grid shimmer props stay inert, `resolveGridShimmer` deleted (AX3)." **Note this ruling is already substantively confirmed executed** — `docs/phase-4/LOG.md` D257 §6 verifies DOC-10 was implemented correctly by P2.1 and corrects an in-flight misreading of it (worth citing as corroboration, since it's an existing entry, not new evidence you're inventing).

6. **Parity-9 open question, RD6 fallback** (go-to-plan.md suggested D245, **conditional** — only log if the fallback path was actually taken): "RD6 attempt transform tween, fallback ACCEPT+D245." Check `docs/phase-4/PROGRESS.md`'s P1.8 row, which reads "radar RD5 base-delay formula, RD7 round caps, RD6 label spring (fallback ACCEPT+D245) — gate PASS (radar n=1000 ≤0.297%)" — the parenthetical phrasing implies the fallback was used, but confirm this against P1.8's actual implementation/report before writing the entry (check for a more detailed P1.8 dispatch report or gate note beyond the PROGRESS.md one-liner). **If you cannot find concrete confirmation that the fallback (rather than the primary transform-tween attempt) is what actually shipped, do not write this entry — report the ambiguity instead.** This is exactly the kind of case the no-fabrication rule exists for: the PROGRESS.md phrasing is suggestive, not conclusive.

## Part 2 — every ACCEPT row ruled but never logged, including Part-A orphan dispositions

Search `research/phase-4/go-to-plan.md` for ACCEPT rulings (doctrine table, open-question resolutions, the "Orphans"/"Coverage resolutions" tables, and any inline ACCEPT you find in the moves/splits/merges log) and cross-check each against `docs/phase-4/LOG.md` — if a ruling's substance isn't findable anywhere in the log (not just under its suggested number — search by content, the way you did for D240 above), it needs an entry. You already have four confirmed examples to start with — the **Part-A orphan dispositions**:

- **C11** (composed per-series `dimOpacity`) — go-to-plan.md ruling: "**FIX** — composed dims per-series (area 0.6 / line 0.3), not chart-wide. Folded into P3.3 (T-D11) acceptance criteria: native `states` dim must reproduce per-series opacities in composed." **⚠️ SUPERSEDED 2026-08-25 — the "P3.3 is still `[ ]` pending" line below was true when written and is now wrong in BOTH directions; log the corrected story, not the caveat.** Two separate things happened. (1) The *native `states` swap* the ruling names as C11's vehicle — P3.3b / T-D11 — closed **NON-VIABLE, zero files edited** (LOG.md D269, `PROGRESS.md:71`; four lead-verified blockers, chief among them `ChartMarkStateStyle` having no `filter` field). So C11's chartered routing is dead, not pending. (2) C11 nevertheless **landed on its own**, by giving `HoverChromeSeries` a per-series `dimOpacity` instead of relying on `attachHoverChrome`'s chart-wide option. Verified at source 2026-08-25 — `composed-chart.tsx:61` records the fix, `:197` declares the field, `:223` merges it, and the three role branches set it correctly: bar `undefined` (`:260` — right, legacy never wraps a bar in `SeriesHoverDim`), area `"0.6"` (`:272`), line `"0.3"` (`:288`), each citing its legacy call site. Those match legacy exactly: `repos/bklit-ui/packages/ui/src/charts/area.tsx:317` `dimOpacity={0.6}`, `line.tsx:337` `dimOpacity={0.3}`. **Log C11 as RESOLVED**, and log the routing failure as part of the entry — the ruling's stated mechanism was abandoned and the outcome was reached another way, which is exactly the kind of divergence a ledger exists to record. Do not write it as a clean success.
- **C15** (composed terminal/end-anchor math ×3 duplication) — go-to-plan.md ruling: "**FIX** — centralize into one helper; added to T-C10 list (runs in P4.4)." Check `docs/phase-4/PROGRESS.md` — P4.4 was `[ ]` pending when this was written; as of 2026-08-25 it is `[~]` **in flight** and has already landed several T-C10 items, so by Wave 6 it will almost certainly have closed. Read its row and its executor notes (`research/phase-4/wave-tasks/P4-04-executor-notes.md`) and log the actual outcome. Same caveat as C11 in spirit: log the ruling, its routing, and what really happened — not a false completion, and not a stale "pending" either.
- **AX5** (axis label position tweens) — go-to-plan.md ruling: "**FIX** — restore CSS position transitions on axis labels; added to T-W1-18 (P1.13, axis/children cluster)." Unlike C11/C15, **this one already landed**: `docs/phase-4/PROGRESS.md` P1.13 is `[x]` and its row text explicitly names "AX5 axis-label tweens fold-in" with "gate PASS (line n=1000, legend n=4 0.0000%)". Log it as a completed disposition, citing P1.13's gate numbers.
- **LM1+LM13** (`__qaSetMarkerFan` in prod) — go-to-plan.md ruling: "**FIX** — gate the QA hook behind a dev/QA flag (stripped or inert in prod bundle); added to T-W1-17 (P1.12)." Also already landed: `docs/phase-4/PROGRESS.md` P1.12 is `[x]`, row text: "`__qaSetMarkerFan` dev/QA gating (LM1+LM13 fold-in) — implementation done, `__qaSetMarkerFan` gating verified inert-by-default AND functionally exercised." Log it as completed, citing P1.12's verification language.

Beyond these four, do a genuine sweep of go-to-plan.md's doctrine/OQ/orphan/coverage sections for any other ACCEPT (or FIX-ruling) that never made it into the log under any number — this is not limited to the four named above, those are the ones already confirmed missing by prior research; there may be others. For each one you find:
- If it's a plain ACCEPT with cited evidence somewhere (a gate run, a measurement, a prior LOG.md entry) — log it, citing that evidence.
- If it's a FIX ruling routed to a package that hasn't landed yet — log the ruling and routing only, explicitly marked as implementation-pending, the way C11/C15 are handled above.
- If it's a FIX ruling routed to a package that HAS landed — log it as resolved, citing that package's PROGRESS.md line and/or its own LOG.md entry.
- If you cannot find the evidentiary basis for an ACCEPT at all — **do not log it. Report it as a gap.**

## Format

Match the existing table's newest-first convention and row style — one `| D# | Date | Decision | Rationale |` row per entry, inserted immediately below the header (above D261, since these are being logged now even though the rulings themselves are dated earlier — use today's date for the log entry, not the ruling's original date, consistent with how D246a/D238a etc. handle amendments after the fact). Look at a few recent entries (D257, D258, D261) for the level of detail and citation style expected — these are long, evidence-dense entries, not one-liners. Yours can be shorter since you're recording an already-made decision rather than a new investigation, but every claim in them still needs a citation (go-to-plan.md line/section, or a PROGRESS.md row, or an existing LOG.md D-number).

## Gate

This package has no code gate — `review only`, per go-to-plan.md's own Wave 6 table. The "gate" here is: every entry you write must survive a re-read against its cited source. Before finishing, re-open every source you cited (go-to-plan.md sections, PROGRESS.md rows, LOG.md entries) and confirm your entry accurately represents them — do not paraphrase loosely enough that the citation stops supporting the claim.

## Report back

1. Which of D240–D245's doctrine rulings you logged, under what new D-numbers, with the go-to-plan.md-suggested-number cross-reference preserved in each.
2. The RD6/D245 conditional entry specifically: whether you found confirmation the fallback path was taken, and what you did (logged it, or reported the ambiguity without logging).
3. Every unlogged ACCEPT/FIX-ruling row you found and logged, beyond the four named Part-A orphans — list them with their go-to-plan.md source location.
4. C11 and C15 specifically: confirm you logged the ruling+routing without overstating completion, given P3.3 and P4.4 are still pending.
5. Any ACCEPT ruling you could NOT find evidentiary support for — named plainly, not smoothed over, with exactly where you looked and what was missing.
6. Final D-number range now occupied in `docs/phase-4/LOG.md` (e.g. "log now runs through D26X") so P6.4 knows where the log stands for its Definition-of-Done check.
