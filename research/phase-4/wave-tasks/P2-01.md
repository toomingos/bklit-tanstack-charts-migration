# P2.1 — Loading surface + pulse-overlay deletion (Wave 2)

Repo root: /Users/tomasdomingos/bklit-tanstack-charts-migration

## WRITE INCREMENTALLY
Write each edited file to disk as soon as its edit is complete — do not batch work to the end. Uncommitted work on disk survives interruption; a resumed session picks up from the working tree.

## Rules
- MAIN GOAL: exact same frontend design/interactivity as legacy bklit; TanStack-native backend; identical public component API.
- D232: fixes live in committed showcase/ code ONLY — never patch the clones (`repos/bklit-ui/`, `showcase/repos/tanstack-charts/`; both gitignored, Vercel re-clones fresh). Clones are read-only reference.
- Do NOT touch: `qa/`, `bench/` (running their scripts is allowed), the clones, /tmp. A guard hook denies these writes.
- Do not commit; leave changes in the working tree.
- go-to-plan.md is the source of truth (staleness rule 4.4.4); legacy reference for pixel/behavior parity is the bklit clone source.
- Migrated code root: `showcase/migrated/charts/` + `showcase/migrated/charts/internal/`.

## Wave 2 is DELETE-ONLY — no behavior change
Every deletion must be provably behavior-neutral. Prove deadness before deleting: `grep -rn "<symbol>" showcase/` across the migrated tree **and** its consumers (`showcase/` app code imports through the `@showcase/migrated-charts` wrapper package), not just the file at hand. A symbol exported from a barrel may have an external consumer. If removing something would change a rendered pixel or an event, it is out of scope — stop, leave it, report it.

## Ordering precondition — already satisfied
go-to-plan.md:229: *"T-W1-19 → T-W2-1: Overlay file deleted only after pulse loop resurrected into `barPulseMark`."*

**This is met.** P1.14 completed T-W1-19 in Wave 1: the bar pulse wave now lives in `internal/bar-pulse-mark.ts` (+226 lines) and passed its bar pixel gate. `internal/bar-pulse-overlay.tsx` was held read-only until this wave and is now yours to delete.

Before deleting it, confirm for yourself that nothing still imports it and that the pulse behavior genuinely lives in `bar-pulse-mark.ts` — if the overlay turns out to still be on a live path, STOP and report rather than deleting.

## Task

### T-W2-1 (verbatim row)
- **Task:** Delete: sweep/skeleton surface (FD7: `LineLoadingSweep`, `BarLoadingSkeleton`, `generateChartSkeletonData`, `getSkeletonHeights`), `resolveGridShimmer`, heatmap loading internals, `bar-pulse-overlay.tsx` (after T-W1-19)
- **Files:** loading-chrome, grid.ts, heatmap-animation

Four deletion groups:

1. **Sweep/skeleton surface (FD7)** — `LineLoadingSweep`, `BarLoadingSkeleton`, `generateChartSkeletonData`, `getSkeletonHeights`. Check the barrels (`internal/index.ts`, `charts/index.ts`) for re-exports and remove those too; a dangling barrel export is a typecheck failure.
2. **`resolveGridShimmer`** in `internal/grid.ts`.
3. **Heatmap loading internals** — the loading/animation path only.
4. **`bar-pulse-overlay.tsx`** — delete the file (see precondition above).
5. **`paintHeatmapCellHover`** (`internal/heatmap-hover-chrome.ts:144`) — **lead addition, D250.** Found and reported by the F3 fix dispatch, which correctly left it alone as out of its scope. F3 describes it as *"dead code from an earlier approach"*. It falls to you as the package with heatmap write access. Re-verify deadness yourself (`grep -rn "paintHeatmapCellHover" showcase/`) before deleting — F3 did not delete it, so its deadness is reported, not proven. Note F3 rewrote heatmap dimming to tween the cell rect's own opacity, so an older hover-paint helper being orphaned is consistent, but confirm rather than assume.

## Heatmap — fence CLEARED, but read what landed (D250)
The heatmap fix dispatch has finished. It rewrote dimming in `internal/heatmap-components.tsx` so the **cell rect's own opacity** is tweened (matching legacy) instead of compositing a background-colored overlay on top; the overlay dim rects and the `hoverDimOpacity` memo are gone. **The overlay `<svg>` itself was deliberately kept because it hosts the pattern defs** — do not delete it as "now unused". Run `git diff -- '*heatmap*'` before editing.

Your scope is heatmap **loading** internals plus the item-5 dead function. If a loading deletion cannot be made without editing hover/dim code, STOP and report.

One live fence: **P2.2 is running** and owns `line-chart.tsx`, `bar-chart.tsx`, `composed-chart.tsx`, `funnel-chart.tsx`, `scatter-chart.tsx`, `internal/hover-chrome.ts`, `internal/{scatter,candlestick,bar,ring}-hover-chrome.ts`, live-line and ring. `bar-pulse-overlay.tsx` and `grid.ts` are yours, not P2.2's — but if you hit a conflict in `bar-chart.tsx`, it is P2.2's, so report rather than edit.

## Gates (all must pass before reporting done)
- `cd showcase && npx tsc --noEmit` exits 0. Deletions commonly break barrels and consumers — chase every resulting error to a real fix (removing the dead re-export), never by re-adding the dead symbol.
- Production build must succeed — deletions are exactly the change class that typechecks and then fails to build.
- **Wave gate-runner (D246 impl/gate split)**: do NOT run QA pixel compares, console checks, or bench yourself — swept once for the whole wave. Report the affected charts + densities. Per the plan row, this package's gate is: typecheck/build, bench line + bar, console-errors.
- **Density rules the gate-runner must be told, and must not default away from:** bar family NEVER `n≥1000` (degenerate at that density); heatmap `--n 52` (at `n=1000` the hover probe lands in empty space and the run fails for a harness reason unrelated to your code — a known symmetric artifact, must not be "fixed"); most others `--n 1000`.
- **Loading states specifically:** these deletions are in the loading surface, so the gate-runner must exercise `state=loading` loads, not just settled renders. Say so explicitly in your list.

## Report back
- What was deleted, and the deadness evidence for each (the grep that proved no consumer).
- Anything you found was NOT actually dead — a genuinely useful finding; report it, do not delete it anyway.
- Gate evidence: typecheck exit status, build exit status. The affected chart/scenario/density list for the gate-runner, including the `state=loading` requirement and the density notes above verbatim.
- Anything unresolved or intentionally skipped, with reason — report failures honestly; do not omit them.
