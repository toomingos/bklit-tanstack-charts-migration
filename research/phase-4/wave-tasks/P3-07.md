# P3.7 — T-D13 sankey native swap (Wave 3)

Repo root: /Users/tomasdomingos/bklit-tanstack-charts-migration

## WRITE INCREMENTALLY
Write each edited file to disk as soon as its edit is complete — do not batch work to the end. Uncommitted work on disk survives interruption; a resumed session picks up from the working tree.

## Rules
- MAIN GOAL: exact same frontend design/interactivity as legacy bklit; TanStack-native backend; identical public component API.
- D232: fixes live in committed showcase/ code ONLY — never patch the clones (`repos/bklit-ui/`, `showcase/repos/tanstack-charts/`; both gitignored, Vercel re-clones fresh). Clones are read-only reference.
- Do NOT touch: `qa/`, `bench/` (running their scripts is allowed), the clones, /tmp. A guard hook denies these writes.
- Do not commit; leave changes in the working tree.
- go-to-plan.md is the source of truth (staleness rule 4.4.4); legacy reference is the bklit clone source.
- Migrated code root: `showcase/migrated/charts/` + `showcase/migrated/charts/internal/`.

## What the swap actually is

T-D13: `sankeyDiagram()` (`@tanstack/charts/network/sankey`) replaces the hand-rolled d3-sankey layout wrapper; native `link()` marks replace the current area-path link rendering; `layoutLabels` margin auto-reservation replaces the fixed margin constant.

- `showcase/migrated/charts/internal/sankey-layout.ts:67-103` `computeSankeyLayout` — a d3-sankey wrapper (`.nodeWidth().nodePadding().nodeAlign(sankeyCenter).extent(...)`) with a defensive clone at lines 74-80 (the file's own header comment at lines 16-18 explains why: d3-sankey mutates node/link objects in place). **If the native `sankeyDiagram()` layout does not itself defensively clone, callers must** — verify this before dropping the clone.
- `showcase/migrated/charts/internal/sankey-mark.ts:86-112` — links rendered as `kind:"area"` scene nodes, `path` built from `SANKEY_LINK_PATH` (`sankey-layout.ts:110-113`, `sankeyLinkHorizontal`), `strokeWidth: Math.max(1, link.width ?? 1)` (line 109). Native `link()` mark (row 30) is the replacement target.
- **Correction to tanstack.md row 29's framing**: it implies sankey label rendering is still a pending native swap. It is not, fully — labels already render through TanStack's scene-graph `kind:"label"` node shape (`sankey-mark.ts:145-242`, `pushLabel` at 165-193 computing `d = fontSize*0.35` baseline-emulation at line 173, branching `rotate === -90`/`90`/else at 174-183), not legacy DOM injection. **What genuinely remains open from row 29 is narrower**: margin auto-reservation via `layoutLabels`. Confirmed absent — `sankey-chart.tsx:88` declares `margin?: Partial<Margin>`, and `:413` resolves it from a static `DEFAULT_MARGIN` constant merged with the prop, not from computed label bounding boxes. Scope this package's label-related work to the margin-reservation gap specifically — do not attempt a full label-rendering swap that portions of the row already landed elsewhere (likely as part of the D239 fix or earlier baseline-emulation work; see `sankey-mark.ts:149-157`'s header comment).

## What explicitly STAYS custom

- **Per-link gradients — no native path, confirmed by tanstack.md row 31 itself.** `internal/sankey-animation.ts:89-113` `injectGradientDefs` uses `userSpaceOnUse` `x1`/`x2` coordinates frozen at gradient-definition time (lines 102-105). `SankeyGradientDatum` (imported `sankey-mark.ts:29`) and `gradientDataRef` are threaded chart→mark→animation (`sankey-chart.tsx:343,472,492,494`). Keep this exactly as-is.
- `sankey-mark.ts:101-110` — the `vector-effect` CSS override. Header comment at 105-108: *"Stroke width IS the data (flow value)... but the renderer hardcodes `vector-effect="non-scaling-stroke"` on area paths. `styles.css` overrides it (`.ts-sankey__link { vector-effect: none }`) — CSS beats presentation attributes."* This override must survive the swap to native `link()` unchanged — the native mark's renderer almost certainly hardcodes the same `non-scaling-stroke` attribute on its output path, so the same CSS override requirement applies regardless of which mark type produces the path.

## Cross-reference constraints — three Wave-1 fixes this swap can regress

`research/phase-4/sankey.md` reports these three as missing/deviated. **All three are confirmed present and current on disk** — `sankey.md` is stale (predates Wave 1's P1.2). Do not read its Deviations section as a to-do list; these are live behaviors this swap could *break*, not gaps to add:

1. **T-W1-4 controlled hover.** `hoveredNodeIndex`/`onNodeHoverChange` props are wired: declarations `sankey-chart.tsx:98,100`; destructure `:337-338`; `isNodeHoverControlledRef`/`controlledNodeIndexRef`/`onNodeHoverChangeRef` machinery `:391-406`; "Controlled-mode sync" effect `:552-560` (exists so `ChartLegend`-driven external hover works). **If T-D13 replaces the link/node mark layer with native `link()`/`rect()` marks that own their own internal hover state, verify this dual controlled/uncontrolled architecture survives** — a native mark with self-contained hover state could silently short-circuit the controlled path.
2. **T-W1-8 label-slide (SK10).** Confirmed live: `internal/sankey-animation.ts:241-250` — `motion.g x` animates from an inset `initialX` (`x±8`, node-edge offset) to final `labelX` (`x∓12`), described in-line as "a 20px outward slide" (line 242), sign flipped by anchor (`anchor === "end"` → `+20` at 248, `anchor === "start"` → `-20` at 249). **`sankey.md`'s claim "Label entrance simplified... no positional slide" is FALSE against current code.** If the native label swap adopts a generic native label-entrance transition instead of this specific inset→final slide, the entrance animation changes even though the settled end-state does not — undetectable by a settled-only gate, the same risk class as sunburst's reveal-replay invariant (P3.6). Verify the 20px slide survives.
3. **D239 settled-regression fix (label baseline/dy).** Still present: `sankey-mark.ts:161` `const orientation = labelOrientation ?? "horizontal"` (comment: "bklit's SankeyNode defaults labelOrientation='horizontal' — keep parity"), reasserted at `sankey-chart.tsx:425-426`. The `pushLabel` baseline-emulation math (`sankey-mark.ts:165-193`, `d = fontSize*0.35` at line 173) is the direct descendant of D239's original fix. Any native label-baseline handling introduced by this swap must reproduce this offset exactly, or D239's original settled-regression bug (labels vertically misaligned against node rects) comes back.

Item 4 flagged by research but **not independently verified this round — check directly, do not assume either way**: SK7/SK8 (tooltip dot tokens / `node.value` source). `sankey.md` has already proven unreliable on three adjacent claims; treat any of its remaining claims about sankey with the same skepticism until checked against current source.

## Gate

`Q1/Q2 sankey; RF (revert-that-swap-on-fail, DOC-5).`

Gate at **n=33** (sankey's mandated density — not n=1000). Include the controlled-hover scenario (item 1 above) explicitly, not just a generic hover probe, since that is the behavior most likely to be silently dropped by a native mark's self-contained hover state.

`qa/screenshot.mjs` compares **`bklit` vs `migrated`**. Do not probe `impl=tanstack` — that is the performance-ceiling reference, which deliberately ports none of bklit's styling (D258).

Check sankey's QA history before reporting any pre-existing failure as new. Note P0.1b's sankey v0.14 settled-regression investigation and D239's fix are both already-closed history for this chart — a new settled failure here is not that old issue recurring by default; confirm which it is.

## Report back

- Confirmation the margin-reservation gap (the only genuinely-open piece of row 29) was closed via `layoutLabels`, or an explanation of why not.
- Whether the native layout needed a defensive clone of its own, or whether removing `sankey-layout.ts`'s clone was safe.
- Confirmation all three Wave-1 behaviors (controlled hover, 20px label slide, D239 baseline offset) survived, with before/after evidence for each.
- Whether SK7/SK8 were checked, and what was found.
- Confirmation the `vector-effect` CSS override and per-link gradients were left untouched.
- Gate table at n=33, controlled-hover scenario called out.
- Anything found that contradicts this brief, including any further staleness in `sankey.md`.
