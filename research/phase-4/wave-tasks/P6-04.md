# P6.4 — T-F3: final pass (4.4.5) — Definition of Done verification, full gate board (Wave 6, closes Phase 4)

Repo root: /Users/tomasdomingos/bklit-tanstack-charts-migration

## WRITE INCREMENTALLY
Write your findings to `docs/phase-4/BENCHMARKS.md`'s `## 4.4.5 Final pass` section (see below) as soon as each of its three bullets is confirmed — do not hold everything until the very end. Uncommitted work on disk survives interruption; a resumed session picks up from the working tree.

## Rules
- MAIN GOAL: exact same frontend design/interactivity as legacy bklit; TanStack-native backend; identical public component API.
- D232: fixes live in committed showcase/ code ONLY — never patch the clones (`repos/bklit-ui/`, `showcase/repos/tanstack-charts/`; both gitignored, Vercel re-clones fresh). Clones are read-only reference.
- Do NOT touch: `qa/`, `bench/` (running their scripts is allowed), the clones, /tmp. A guard hook denies these writes.
- Do not commit; leave changes in the working tree.
- go-to-plan.md is the source of truth (staleness rule 4.4.4).
- Migrated code root: `showcase/migrated/charts/` + `showcase/migrated/charts/internal/`.
- `qa/screenshot.mjs` compares **`bklit` vs `migrated`**. Never probe `impl=tanstack` — it is the performance-ceiling reference and deliberately ports none of bklit's chrome (this caused a full wrong-impl investigation once already, D258).

## What this package is, and is not

**You run the final gate board and report results. You do not silently fix failures.** If something fails, it is a finding — write it up with full evidence and route it to the lead (name the owning package if one is obvious from the plan; otherwise flag it as unowned, the way pie's hover defect was flagged in D249 before it got a package). Do not patch code to make a number go green. This package is the last thing that runs in Phase 4 — everything after it is the lead deciding whether Phase 4 is actually done, and that decision needs your unfiltered results, not a pre-cleaned version of them.

Only `showcase/` files you may touch: none for fixing defects. You may edit `docs/phase-4/BENCHMARKS.md` (to fill in the stub tables — see below) and, if instructed elsewhere to also update `docs/phase-4/PROGRESS.md`'s Wave 6 checkboxes, that file. Do not touch anything under `showcase/migrated/`.

## Definition of Done — quoted verbatim, both sources

Two documents state the Definition of Done, worded slightly differently. Quote both here so you're not working from a paraphrase, and treat every clause in both as binding:

`PLAN-phase-4.md` (lines 132-134):

> ## Definition of Done
> All go-to-plan tasks closed or skipped-with-reason (logged in `docs/LOG.md`), typecheck + build + bench pass, final spot-check matches baseline.

`research/phase-4/go-to-plan.md` (lines 183-185, its own restatement):

> ## Definition of done
> All tasks closed or skipped-with-reason (logged in `docs/phase-4/LOG.md`), typecheck + build + bench pass, final spot-check matches baseline (which itself matches legacy after Batch 0/W1).

Note the path discrepancy: `PLAN-phase-4.md` says `docs/LOG.md`; the actual, consistently-used path throughout this repo is `docs/phase-4/LOG.md` (that's where every prior D-numbered entry actually lives, and it's what go-to-plan.md's own restatement correctly says). Use `docs/phase-4/LOG.md` — this is a documentation typo in the plan file, not a real second log.

## The four required checks

### 1. Every go-to-plan task confirmed closed or skipped-with-reason, traceable to a LOG.md entry

Walk `research/phase-4/go-to-plan.md`'s task table (all Batches/Waves) against `docs/phase-4/PROGRESS.md`'s tick-list and `docs/phase-4/LOG.md`. For every task: either it's `[x]` in PROGRESS.md with a corresponding closing LOG.md entry (cite the D-number), or it was explicitly skipped/deferred/reclassified with a LOG.md entry recording why (e.g. the D238a bar-family reclassification, the D239 sankey root-cause correction). **A task that is `[x]` in PROGRESS.md with no traceable LOG.md entry is a Definition-of-Done gap** — report it, don't wave it through because the checkbox looks done. Pay particular attention to P6.3's output (T-F2, the D-log package that should have run immediately before you) — confirm its doctrine-ruling and orphan-disposition entries are actually present before treating those items as closed.

### 2. Full `tsc --noEmit`

Run a full-repo typecheck (not a per-package scoped one) and report the exit code and any errors verbatim. Zero tolerance — the Definition of Done says "typecheck ... pass," not "typecheck mostly passes."

### 3. Production build

Run the production build and report the exit code and any warnings/errors verbatim.

### 4. Bench compare vs the frozen 4.4.1 baseline

The baseline lives in `docs/phase-4/BENCHMARKS.md` — HEAD `7d60a7f`, bench run id `bench/results/2026-08-22T15-46-19-823Z`, full headline median table (line/area/bar/scatter × bklit/tanstack × n=100/1000/10000, 24/24 cells). Run the full bench suite fresh and compare every cell against that frozen table. This is the same comparison D257 (Wave 2 gate) did, extended to cover whatever Wave 6 (P6.1 especially) touched — line/bar/scatter/composed at minimum, per P6.1's own bench requirement, plus anything else the plan's bench matrix covers.

### 5. ALL-chart parity spot-check — not a sample

The Definition of Done says "final spot-check matches baseline." Read that as **every chart type in the registry**, each at its own mandated density — not a representative sample, not "the ones that changed this wave." Mandated densities (non-negotiable, several deliberate):
- bar family: **never n≥1000** — use n=100 (bar, barsquares, bardepth all included in this rule).
- sankey: n=33.
- sunburst: n=27.
- heatmap: n=52.
- markers, liveline, barsquares, bardepth: n=100.
- legend: n=4.
- most others (line, area, scatter, candlestick, composed, pie, ring, radar, gauge, gaugelinear, funnel, funnelvertical, choropleth, reference-area hosts, etc.): n=1000.

Include hover captures where the chart's own QA history has ever required them (ring especially — D258 established ring must be gated with hover captures at n=1000 AND n=4, settled-only is insufficient for that chart specifically; check other charts' `qa/results/<chart>/` history for similar hover-mandatory precedent before assuming settled-only is enough for any given chart).

## Known open items — carry these forward verbatim, do not report them as new failures

These are pre-existing, already-classified findings. If your spot-check reproduces them, cite the existing classification rather than re-opening the investigation:

- **Markers n=100 hover-30 ~0.5507%** is chronic (D248) — reproduces the pre-Wave-1 approved-era profile capture-for-capture; n=1000 passes fully. Density-dependent marginal fail, not a regression.
- **Scatter M3a bench cell, n=1000: migrated went 34.3ms → 65.5ms against its own frozen baseline** (D257). This is a required bench cell, already identified, **owned by P4.2** (T-C2 focus-strategy work touches scatter's update path). Report the current number; do not treat it as a new discovery, and do not attempt to fix it yourself — it's not this package's ownership.
- **Funnel and funnelvertical, n=1000: ~1000 `viewBox` negative-value console errors** are symmetric with bklit (bklit emits 1017 of the byte-identical error string; both charts clean at n=100 on both impls) and allowlisted in the console sweep (D261, `NEGATIVE_VIEWBOX` pattern). Not a defect.
- **The `state=loading` pixel gate is near-blind** (D257 §4) — it can PASS while visibly diverging (bold/letter-spaced label text, a missing diagonal sweep glint, and different gridline y-positions were all found hiding under a 0.0531% PASS). **A loading-state PASS in your spot-check proves nothing about loading-state parity.** If you spot-check any loading states, open the actual diff PNGs rather than trusting the percentage, the way D257 did — and if you find something, it's a finding to report, not something this near-blindness excuses you from checking.

## `docs/phase-4/BENCHMARKS.md` — fill in the stubs, don't replace the file

The file already has two stub sections waiting for your output. Fill them in place:

The Wave 6 gate table (currently all `—` cells):

```
### Wave 6 — multi-axis + closing

| Pkg | Tasks | typecheck+build | Q1/Q2 | CE | bench | Notes |
|---|---|---|---|---|---|---|
| P6.1 | T-F1 multi-axis yAxisId (scale layer + 6 consumer clusters + AX7 filter + AX4 re-add) | — | multi-series-per-axis per part | — | line/bar/scatter/composed | DOC-8 |
| P6.2 | deferred scale-adjacent items (T-C10 xForDate/tokens, T-E1b wrappers) | — | line/area/composed | — | — | |
| P6.3 | T-F2 D-log entries D240–D245 + unlogged ACCEPTs | — | review only | — | — | |
| P6.4 | T-F3 final pass (4.4.5) | — | ALL-chart parity spot-check | — | full compare vs this baseline | full gate board |
```

Fill your own row (P6.4) with your actual results; fill the other three rows too if their own dispatches (P6.1/P6.2/P6.3) left them blank — cross-check against those packages' own reports rather than re-deriving their results.

The final-pass section (currently three `_pending_` bullets):

```
## 4.4.5 Final pass

- full build: _pending_
- bench compare vs baseline: _pending_
- parity spot-check ALL charts: _pending_
```

Replace each `_pending_` with the actual result (pass/fail + evidence pointer — a bench run id, a `qa/results/` path, the tsc/build exit code).

## Gate

There is no further gate above this one — this package IS the gate. `qa/screenshot.mjs` compares **`bklit` vs `migrated`**. Never probe `impl=tanstack`.

## Report back

1. Task-closure audit: any go-to-plan.md task that is `[x]` in PROGRESS.md but has no traceable `docs/phase-4/LOG.md` entry, named plainly.
2. `tsc --noEmit`: exit code, and full error text if nonzero.
3. Production build: exit code, and full error/warning text if nonzero.
4. Bench compare: which cells moved outside acceptable variance from the 4.4.1 baseline, beyond the already-known scatter M3a item — do not just say "pass," give the actual deltas.
5. ALL-chart parity spot-check: pass/fail per chart at its mandated density, explicitly confirming the checklist covered every chart type in the registry, not a subset. For any new (not-already-listed-above) failure: full evidence, and your best read on which package should own the fix — but do not fix it yourself.
6. Explicit confirmation that the four "known open items" above were checked, still reproduce as described (or, if any of them has changed — e.g. scatter M3a moved further, or a chronic item resolved itself — say so precisely, since a changed number for a "chronic" item is itself a finding).
7. `docs/phase-4/BENCHMARKS.md`: confirm both stub sections are filled in, not left as `—`/`_pending_`.
8. Overall Definition-of-Done verdict: met / not met, with the specific unmet clause(s) if not met. This is a report to the lead, not a self-certification — state your evidence, let the lead make the call on anything borderline.
