# P1.2 — Sankey label default, controlled hover, fidelity fixes (Wave 1)

Repo root: /Users/tomasdomingos/bklit-tanstack-charts-migration

## WRITE INCREMENTALLY
Write each edited file to disk as soon as its edit is complete — do not batch work to the end. Uncommitted work on disk survives interruption; a resumed session picks up from the working tree.

## Rules
- MAIN GOAL: exact same frontend design/interactivity as legacy bklit; TanStack-native backend; identical public component API.
- D232: fixes live in committed showcase/ code ONLY — never patch the clones (`repos/bklit-ui/`, `showcase/repos/tanstack-charts/`; both gitignored, Vercel re-clones fresh). Clones are read-only reference.
- Do NOT touch: `qa/`, `bench/` (running their scripts is allowed), the clones, /tmp. A guard hook denies these writes.
- Do not commit; leave changes in the working tree.
- go-to-plan.md is the source of truth (staleness rule 4.4.4); legacy reference for pixel/behavior parity is the bklit clone source.
- Migrated code root: `showcase/migrated/charts/` + `showcase/migrated/charts/internal/` (consumed by `showcase/` via the `@showcase/migrated-charts` wrapper package).

## D239 handoff (read `docs/phase-4/notes-p01b-sankey.md` + LOG.md D239 FIRST)

Wave 0's P0.1b (D239, 2026-08-22) already landed sankey changes in the working tree:
- Label-baseline fix in `internal/sankey-mark.ts` `pushLabel()` (rotation-aware dy=0.35em
  emulation, `baseline:"middle"` removed) — settled gate now PASSES all densities
  (n=4 0.0008%, n=33 0.1092%, n=1000 0.3041%). Do NOT rework this path.
- **SK1 is ALREADY DONE**: `labelOrientation` default flipped to `"horizontal"` in both
  `sankey-chart.tsx` and `internal/sankey-mark.ts` as part of D239. Verify, don't re-apply.
- **NEW IN-SCOPE ITEM — hover/dim-state divergence** (deferred to this package by D239):
  hover captures FAIL at n≥33 both pre- and post-fix (n=33 hover-30 1.0540%, n=1000
  hover-30 2.8392%, gate 0.5%; n=4 passes). Root-cause territory: link/node dim-highlight
  rendering path on hover (NOT label paint — that's fixed). Historically n=33 hover never
  gated green, so this is a parity gap to CLOSE here, not a regression to bisect.
  Evidence dirs: `qa/results/sankey/2026-08-22T12-39-35-219Z` (n=33),
  `…12-37-42-696Z` (n=1000) — diff PNGs show where the dim states diverge.

## Tasks

### T-W1-3 (verbatim from go-to-plan.md, Batch W1)
- **Task:** Sankey `labelOrientation` default → horizontal (SK1)
- **Files:** sankey-chart.tsx:386
- **Acceptance:** default labels horizontal (legacy sankey-node.tsx:314 parity)

Grounding: **already applied by D239** (see handoff above) — verify the fallback reads `"horizontal"` in `sankey-chart.tsx` AND `internal/sankey-mark.ts`, confirm against legacy `repos/bklit-ui/.../sankey-node.tsx` (line ~314), and close as pre-completed.

### T-W1-4 (verbatim)
- **Task:** Sankey controlled hover (SK2) `hoveredNodeIndex`/`onNodeHoverChange`
- **Files:** sankey chart + coordinator
- **Acceptance:** legend-driven highlight scenario works as legacy

Coverage resolution (OQ parity 4 / SK1+SK2, quoted): "FIX both — one-line default restore; controlled hover via existing coordinator controlled mode."

Grounding: hover coordination lives in `charts/sankey-chart.tsx` + `internal/sankey-hover-chrome.ts` — verify against the legacy controlled-hover contract in the bklit clone.

### T-W1-8 (verbatim)
- **Task:** Sankey fidelity trio: reveal keyed signature+duration only (SK6), tooltip dot tokens (SK7), node.value source (SK8), label ±8px slide (SK10)
- **Files:** sankey chart/tooltip
- **Acceptance:** new-array data does NOT replay reveal; dots/labels match legacy

Related ruling (OQ parity 9, sankey items, quoted): "FIX … SB12 (cull, + header), SB13 (interpolate), SB14 (commit immediately…), SB15 (350ms fade + svg pointerleave)" — those SB* items belong to P1.7 (sunburst), NOT this package.

Scenario caveat: if no existing sankey scenario exercises controlled hover, demonstrate via the QA hover captures and report the scenario gap — do not edit `bench/` to add one.

## Gates (all must pass before reporting done)
- `cd showcase && npx tsc --noEmit` exits 0.
- **Wave gate-runner (D246 impl/gate split)**: do NOT run QA pixel compares or console checks yourself — swept once for the whole wave. Report for the gate-runner: sankey at **n=33** (sankey density rule per AGENTS.md), including hover captures (hover-30/50/70 currently FAIL at n=33 per the D239 handoff above — this package must bring them under the 0.5% gate, not just settled). Sankey is not in `qa/console-errors.mjs`'s fixed target list — flag that gap to the gate-runner rather than covering it yourself.

## Report back
- What changed per task (files + behavior).
- Gate evidence: typecheck exit status. List the affected chart(s)/scenario(s)/density for the wave gate-runner to sweep (QA pixel compare + console errors).
- The specific density/scenario notes below MUST be included in that list (do not let the gate-runner default to n=1000 where a different density is required).
- Anything unresolved or intentionally skipped, with reason — report failures honestly; do not omit them.
