# P6.2 — deferred scale-adjacent clean-up: T-C10 remainder + T-E1b wrapper hooks (Wave 6, post-T-F1)

Repo root: /Users/tomasdomingos/bklit-tanstack-charts-migration

## WRITE INCREMENTALLY
Write each edited file to disk as soon as its edit is complete — do not batch work to the end. Uncommitted work on disk survives interruption; a resumed session picks up from the working tree.

## Rules
- MAIN GOAL: exact same frontend design/interactivity as legacy bklit; TanStack-native backend; identical public component API.
- D232: fixes live in committed showcase/ code ONLY — never patch the clones (`repos/bklit-ui/`, `showcase/repos/tanstack-charts/`; both gitignored, Vercel re-clones fresh). Clones are read-only reference.
- Do NOT touch: `qa/`, `bench/` (running their scripts is allowed), the clones, /tmp. A guard hook denies these writes.
- You cannot delete files (guard policy D216). If this task orphans anything, un-wire it, leave the file, report it to the lead.
- Do not commit; leave changes in the working tree.
- go-to-plan.md is the source of truth (staleness rule 4.4.4); legacy reference is the bklit clone source.
- Migrated code root: `showcase/migrated/charts/` + `showcase/migrated/charts/internal/`.

## Hard prerequisite — do not start until P6.1 is done and gated

Both items in this package read the scale-resolution API that **P6.1 (T-F1, multi-axis `yAxisId`)** rewrites in `showcase/migrated/charts/internal/y-domain.ts`. That is precisely why these two items were pulled out of their original packages (P4.4 and P5.2) and deferred all the way to Wave 6, after T-F1, rather than left where they were chartered:

> go-to-plan.md: "DEFERRED 2 items of T-C10 (xForDate, reveal-constant tokens) from P4.4 → P6.2 to avoid double-touching line/area/composed hosts before T-F1 rewires their scale consumption."
>
> go-to-plan.md dependency graph: "T-F1 → T-C10? no — T-C10's `xForDate`/scale-adjacent items in line/composed touch the same hosts F1 rewires → sequence C10 after F1 | Avoid double-touching scale-resolution consumers."
>
> go-to-plan.md dependency graph: "T-F1 → T-E1b (`useYScale`/`useAnimatedYDomains` wrappers) | Wrappers wrap the post-multi-axis scale layer."

Read P6.1's own report before starting — specifically its statement of the final scale-layer API shape (function names/signatures) and whether it declared that API stable. If P6.1 reported the API as still in flux or ambiguous, **stop and report that to the lead rather than guessing at what to wrap** — wrapping a moving target produces wrappers that need to be rewritten the moment the target settles, and that is exactly the outcome P6.1 was warned to avoid causing.

## Scope

This is pure clean-up of items deferred out of earlier waves for sequencing reasons — nothing here is new behavior discovered in Wave 6 itself.

### 1. T-C10 remainder — `xForDate` helper, deferred from P4.4

Origin: `T-C10` (go-to-plan.md line ~148) is a large inline-dedup batch (`shortDateFmt` ×5, `prefersReducedMotion()` ×~10 sites, `useSanitizedId` ×20 sites, reveal-guard convergence, `xForDate` ×6, reveal-constant tokens, `SegmentComponent`/`ChartMarker` canonical imports, heatmap levelStyles builder, choropleth zoom-writer merge, brush clamp helper, sunburst `maxRevealDelayMs`, `Symbol.for` → `CHART_ROLE`). Most of T-C10 runs in **P4.4** (already dispatched separately — not your concern). Two specific items were carved out of P4.4 and pushed here:

- **`xForDate` helper, duplicated ×6** across the same three hosts P6.1 rewrites. Confirmed still duplicated as of this writing (unchanged by P6.1's scale-layer work, since `xForDate` computes an x-pixel position from a date, not a y-domain):
  - `showcase/migrated/charts/line-chart.tsx:777` and `:825` — two separate inline definitions in the same file.
  - `showcase/migrated/charts/area-chart.tsx:438` and `:466` — same pattern.
  - `showcase/migrated/charts/composed-chart.tsx:686` and `:714` — same pattern.

  Extract one canonical `xForDate` helper (an `internal/` module is the natural home — check whether `internal/` already has an x-scale-adjacent module you should extend, e.g. wherever the shared x-scale/x-domain logic lives, before creating a new file) and replace all six inline definitions with imports of it. Diff the six bodies first — parity.md's cross-reference (`redevelopment...xForDate row names composed-chart only`, per go-to-plan.md's coverage-resolution note on C15) suggests the duplicates may not be byte-identical; if they diverge, treat any divergence as a real behavior question (which version is correct against bklit) rather than silently picking one arbitrarily.

- **Reveal-constant tokens recheck**: `T-C10`'s reveal-constant-tokens item (the `1100`/easing/`500`/margin-`{40×4}` family of magic numbers scattered across reveal logic) needs a recheck specifically because P6.1's scale-layer rewrite may have touched files that also contain reveal-constant literals in the same consumer clusters (line/area/composed). This is explicitly scoped as a **recheck**, not a full re-run of T-C10's token work (that ran in whichever package actually owns the bulk of T-C10 — P4.4/P4.5, not this one). Confirm the constants weren't reintroduced or duplicated as a side effect of P6.1's edits to the same files; if they were untouched, say so and move on — do not expand this into a general audit.

### 2. T-E1b wrapper hooks — `useYScale`/`useAnimatedYDomains`, deferred from P5.2

Origin: `T-E1b` (the "port/wrapper tier") was split in **P5.2** into two sub-runs — (a) visx `Gradient*`/`Pattern*` + presentational components, (b) hook wrappers. Sub-run (b) explicitly excluded these two hooks and pushed them here:

> `docs/phase-4/PROGRESS.md`, P5.2 row: "T-E1b — port/wrapper tier, 2 sub-runs: (a) visx Gradient*/Pattern* + presentational components (incl. `Background`, post-P1.13); (b) hook wrappers (excl. useYScale/useAnimatedYDomains → P6.2)"

**These two hooks do not exist anywhere in the migrated codebase today** — confirmed by grep, zero occurrences of either name under `showcase/migrated/`. You are writing them from scratch, not extending or fixing existing wrappers. Their job (by name and by legacy precedent) is to expose the post-multi-axis scale-resolution layer (P6.1's `internal/y-domain.ts` successor) and the animated-y-domain machinery through the barrel-hook surface, the way legacy exposes equivalent concepts — check `repos/bklit-ui/packages/ui/src/charts/` for legacy hooks with these or similar names (read-only reference; do not port verbatim, migrated has no `ChartProvider` context to hang a hook off of the way legacy might) before designing the wrapper signatures. Match P6.1's actual final scale-layer API — do not design against the pre-P6.1 single-axis `resolveTimeSeriesYDomain`/`createNicedYScale` signatures, they will already be gone.

Where these wrappers get exported from (which barrel file) should follow whatever pattern P5.2's sub-run (a) established for the rest of the wrapper tier — check that P5.2's output before choosing a location, for consistency rather than inventing a new convention.

## Already ruled out — do NOT re-investigate

- Do not touch `internal/y-domain.ts`'s actual scale-resolution logic (grouping, domain math, multi-axis behavior) — that's P6.1's completed work, not yours. You are wrapping/consuming it and deduplicating an unrelated x-axis helper, not modifying the y-scale math itself.
- Do not expand into the rest of T-C10 (`shortDateFmt`, `prefersReducedMotion`, `useSanitizedId`, etc.) — those items belong to P4.4/P4.5, dispatched separately. Touching them here risks colliding with a concurrently- or already-run package's edits to the same lines.
- Do not touch T-E1b sub-run (a) (visx `Gradient*`/`Pattern*`, presentational components, `Background`) — that's P5.2's completed scope.

## Gate

- **line, area, composed** — Q1/Q2 at n=1000 (their mandated density), confirming the `xForDate` extraction is pixel-neutral (it's a pure refactor unless you found a genuine divergence between the six duplicates, in which case report the divergence and treat the fix as behavior-affecting, gating accordingly against bklit).
- Typecheck clean across all touched files, including the new hook module(s).
- If the two new hooks are consumed anywhere as part of this package (vs. simply exported and unconsumed), gate whatever consumes them; if they are exported but not yet consumed by any component (a plausible outcome — nothing in migrated currently calls a `useYScale`/`useAnimatedYDomains` hook), say so plainly rather than implying they're wired in.

`qa/screenshot.mjs` compares **`bklit` vs `migrated`**. Never probe `impl=tanstack`.

## Report back

1. The `xForDate` extraction: where the canonical helper lives, confirmation all six call sites now import it, and whether the six original bodies were identical or diverged (and if they diverged, what you did about it).
2. The reveal-constant-tokens recheck: confirmed untouched/unduplicated by P6.1's edits, or what you found and fixed.
3. The two new hooks: final signatures, where they're exported from, what (if anything) currently consumes them, and confirmation their shape matches P6.1's actual final scale-layer API (cite P6.1's report).
4. Anything contradicting this brief, stated plainly with evidence — including if P6.1's scale-layer API was not as stable as expected.
