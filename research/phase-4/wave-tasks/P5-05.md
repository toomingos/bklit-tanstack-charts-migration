# P5.5 — T-E3 (stub-prop family + K4 candlestick tween) + T-E6 (sunburst Hint/Breadcrumb) (Wave 5)

Repo root: /Users/tomasdomingos/bklit-tanstack-charts-migration

## WRITE INCREMENTALLY
Write each edited file to disk as soon as its edit is complete — do not batch work to the end. Uncommitted work on disk survives interruption; a resumed session picks up from the working tree.

## Rules
- MAIN GOAL: exact same frontend design/interactivity as legacy bklit; TanStack-native backend; identical public component API.
- D232: never patch the clones (`repos/bklit-ui/`, `showcase/repos/tanstack-charts/`; both gitignored, read-only reference).
- Do NOT touch: `qa/`, `bench/` (running their scripts is allowed), the clones, `/tmp`. A guard hook denies these writes.
- Executors cannot delete files (guard policy D216).
- Do not commit; leave changes in the working tree.
- go-to-plan.md is the source of truth (staleness rule 4.4.4).
- Migrated code root: `showcase/migrated/charts/` + `showcase/migrated/charts/internal/`. The shared reveal-timing engine is `internal/enter-transition.ts`.
- `qa/screenshot.mjs` compares **`bklit` vs `migrated`**. Never probe `impl=tanstack` (D258).

## Dependency you MUST check before starting — P3.2 (T-D1)

**T-E3 wires stub props into the same reveal/easing channel P3.2 (Wave 3, T-D1: `motion()` default replacement) rewrites.** `internal/enter-transition.ts`'s own header comment already forward-references this package by name: *"Parts that currently accept-and-ignore easing stubs (bar/composed/scatter/…, P5.5 T-E3) forward their prop into channel 1's call sites once wired; P3.2's motion() default replacement must keep producing `ResolvedTiming.easingCss` strings so both channels stay WAAPI-native."* (`showcase/migrated/charts/internal/enter-transition.ts:1-40` region).

**⚠️ SUPERSEDED 2026-08-25 — P3.2 has since CLOSED, and the resolution collapses this whole branch: you are unambiguously in the "pre-P3.2 shape" world, permanently.** P3.2 closed **skipped-with-reason** (LOG.md D264, `PROGRESS.md:68`) — non-viable as chartered. Native `defaultDuration`/`defaultEasing` are module-private (`motion.ts:207,209`) with an unexported call chain, and the resolved native easing is a numeric `(progress) => number`, never a CSS string, so no read-back path exists even by invoking `motion()`. Critically for you: the values **already matched exactly**, so the deliverable was provenance comments only — **zero value changes, and `internal/enter-transition.ts` was left untouched**. The structural `{tween|spring}` reshape DOC-7 describes therefore never happened and is not going to. Consequence: `EnterTransition`/`ResolvedTiming` today are the same shapes this brief and `synthesis/parity.md` describe, those descriptions are **not** stale, and there is nothing to re-derive. Wire against today's shape, with no flag and no escalation. The rest of this section is retained for provenance only — ignore its two-option fork. *Original text:* As of this dispatch, P3.2 has NOT landed — `docs/phase-4/PROGRESS.md`'s Wave 3 section shows only P3.1 checked off; P3.2 through P3.13 are all still open. This matters directly: DOC-7's ruling (`go-to-plan.md`) describes the target shape as *"framer `Transition` → structural `{tween|spring}` with same fallback timing"* — that structural shape is what P3.2 is chartered to establish. If P3.2 has landed by the time you execute this package, **re-derive the current easing/reveal-timing shape from the actual post-P3.2 `internal/enter-transition.ts` and `EnterTransition` type before wiring anything** — do not trust this brief's or `synthesis/parity.md`'s description of the stub shapes, both of which predate P3.2 and describe the *pre-refactor* tree. If P3.2 has **not** landed when you start, you have two options: (a) wait / escalate to the lead — wiring stub props into a channel that's about to be restructured risks double-work and merge conflict, or (b) if the lead directs you to proceed anyway, wire the props using today's pre-P3.2 `EnterTransition`/`ResolvedTiming` shape and flag clearly in your report that this wiring will need re-verification once P3.2 lands. Do not silently guess which world you're in — check `docs/phase-4/PROGRESS.md` for P3.2's status the moment you start, and again before your final gate run (it can change mid-session if another package lands it concurrently).

## Scope

### Strand 1 — T-E3: the stub-prop family

> **⚠ ADDENDUM 2026-08-25 (D311) — scatter's reveal path changed under this charter. Read before touching S3.**
>
> P4.6 landed a reveal-replay guard in `scatter-chart.tsx` (`handleRender`,
> ~`:711-756`). It fixed a real parity defect: the TanStack scene is rebuilt on
> every data swap, so the `.ts-chart__marks` node — and with it the DOM-side
> `dataset.bkmRevealed` flag — was destroyed each tick and the **mount reveal
> replayed on every update** (~2000 `Element.animate()` calls per tick at
> n=1000). bklit never does this: its reveal is keyed on `revealEpoch`, bumped
> only by an effect on **`[animationDuration, revealSignature]`**
> (`scatter-chart-shell.tsx:146-154`), and `revealSignature` defaults to `""`
> — so a plain `data` change replays nothing.
>
> **The coupling you must handle:** P4.6's guard is a **boolean**
> (`hasRevealedRef`) plus a deadline timer. That is correct *today* precisely
> because migrated scatter has no `revealSignature` prop. **The moment you wire
> S3, a boolean can no longer express the contract** — after the deadline
> fires, the guard snaps forever, so a caller changing `revealSignature` would
> get nothing and S3 would land inert while typechecking clean. **Nothing gates
> this**: the prop would be accepted, forwarded, and silently ignored.
>
> **Do not invent a mechanism — migrated already has the right one.** Sankey
> keys on a replay *key*, not a flag: `seenRevealKeyRef.current = { signature,
> duration }`, compared and consumed at `sankey-chart.tsx:515-534`, with the
> effect deps `[revealSignature, animationDuration, …]` at `:543`. That is a
> direct translation of bklit's epoch effect. Convert scatter's
> `hasRevealedRef` boolean to that shape and the deadline logic keeps working
> unchanged (it bounds the *current* reveal window; the key decides whether a
> *new* window opens).
>
> Same applies to **bar (B3)** and **composed (C4)** if they carry equivalent
> guards — re-grep for `bkmRevealed` / `hasRevealedRef` in each host before
> wiring, rather than trusting this note's file list.
>
> Related, already-tracked and NOT yours: **SK6** is the inverse bug in sankey
> (its key wrongly includes data identity, so new arrays replay). Scatter had
> the same defect by a different route. If SK6 is still open when you run, the
> two share a root cause and the fix shapes should match.


Every row below is currently the same shape: a prop exists on nothing, or exists typed but is silently dropped/never read. FIX-stub means: accept the prop, wire it through to the part's reveal orchestration exactly as the already-working parts do (line's `enterTransition`/`revealSignature` pattern is closest to done — L1/L2 are the reference implementation to copy the *shape* of, even though they too need wiring). Do not invent a new reveal-timing mechanism per part — every part should route through `internal/enter-transition.ts`'s shared engine.

- **line** (`showcase/migrated/charts/line-chart.tsx`) — L1 `enterTransition` (missing — prop should drive the entrance tween; parity.md notes the WAAPI path already does a fixed tween, so this is "accept+ignore preserves swap signature" today), L2 `revealSignature` (missing — replay-epoch input; parity.md notes "the orchestrator already takes the field; wire through" — check whether the orchestrator-level plumbing already exists and this is purely a prop-forwarding fix).
- **bar** (`showcase/migrated/charts/bar-chart.tsx`) — B1 `animationEasing` (absent), B2 `enterTransition` (absent), B3 `revealSignature` (absent). All three FIX-stub. **Do not confuse this with P5.4's orientation/stacked/etc. dev-warn props** — those stay inert-by-design (DOC-9); these three are genuine FIX targets.
- **composed** (`showcase/migrated/charts/composed-chart.tsx`) — C3 `animationEasing` (absent), C4 `enterTransition`/`revealSignature` (absent, transition + replay epoch), C8 `SeriesBar.animate` (dropped reveal toggle — gate the reveal, per parity.md's phrasing).
- **scatter** (`showcase/migrated/charts/scatter-chart.tsx`) — S1 `animationEasing`, S2 `enterTransition`, S3 `revealSignature` (all absent), S7 `<Scatter> animate` (missing reveal toggle).
- **choropleth** (`showcase/migrated/charts/choropleth-chart.tsx`) — CP1 `enterTransition` (currently typed `unknown`, never read — confirmed this is worse than "absent," it exists as a type-erased stub that drove a tween in legacy but does nothing now), CP2 `revealSignature` (never read; migrated currently runs a fixed 1100ms reveal regardless of epoch — wire the epoch through), CP3 `patterns` prop (defs passthrough never mounted — `getFeaturePattern` URLs are dangling references to nothing; either render the `<defs>` block that makes those URLs resolve, or strip the dangling prop entirely — parity.md leaves both options open, pick one and justify it).
- **sunburst** (`showcase/migrated/charts/sunburst-chart.tsx`) — SB2 `enterTransition`/`enterStaggerScale` (dropped tuned-entrance props) — FIX-stub/plumb. **Careful: do not touch the sunburst geometry/reveal-math helper block** (`arcPath`, `buildRevealDelays`, `buildSunburstEnterTiming`, etc. in `internal/sunburst-geometry.ts`) beyond what SB2 strictly requires — that block is held for P5.1's post-P3.6 pass; if wiring SB2 requires changing geometry-helper internals rather than just the prop-acceptance layer, stop and flag it rather than reaching into held territory. **⚠️ 2026-08-25: the hold is RELEASED — P3.6 landed (LOG.md D276) and P5.1 now exports the surviving block. Two corrections to the parenthetical above: `buildRevealDelays` and `buildSunburstEnterTiming` no longer exist at all (retired by P3.6; the latter leaves a stale doc-comment at `internal/sunburst-reveal.ts:42`), and `internal/sunburst-geometry.ts` did survive with 10 exported helpers plus a module-private `lerpGeometry`. The "stop and flag rather than reach in" instruction still stands on its own merits — SB2 is a prop-acceptance job and reaching into geometry math is out of scope regardless of who holds it — but flag it to the lead as scope, not as a hold-back breach.**

### Strand 2 — K4: candlestick's sampled-keyframe tween (deferred from P1.9)

DOC-7's family-wide ruling has one named exception: *"candlestick K4 gains sampled-keyframe tween support (legacy tweens dropped entirely there = behavior gap)"* (`go-to-plan.md` DOC-7). Every other part's `enterTransition` reshape is an ACCEPTED structural simplification (framer `Transition` → `{tween|spring}`); candlestick alone is a **real regression**, not an accepted simplification, and gets fixed rather than documented.

Verified current state: `showcase/migrated/charts/candlestick-chart.tsx` (~lines 99-114) types `CandlestickEnterTransition` as a narrow spring-only subset — `{ duration?: number /* seconds, framer Transition.duration unit, default 0.8 */; bounce?: number /* default 0.15 */ }`. Legacy (`repos/bklit-ui/packages/ui/src/charts/candlestick-chart.tsx:54,84`) imports `type { Transition } from "motion/react"` and types `enterTransition?: Transition` — the **full** framer `Transition` type, which supports tween, spring, *and* keyframe-sampled animations. Migrated's narrowed type structurally cannot express a keyframe tween at all today — this isn't a missing default, it's a missing capability.

Your task: extend candlestick's enter-transition handling to support a **sampled-keyframe tween**, matching what legacy's full `Transition` type allowed callers to configure. This does NOT mean reintroducing framer-motion or the full `Transition` type (that would violate the WAAPI-native backend requirement) — it means adding a tween/keyframe variant to `CandlestickEnterTransition`'s structural shape (spring stays as-is, add a `{ type: "tween", duration?, ease?, keyframes?: ... }`-shaped alternative or similar, consistent with whatever `{tween|spring}` structural convention P3.2 establishes elsewhere — see the P3.2 dependency note above, this is exactly where that shape needs to match). Sample the keyframes into whatever the WAAPI renderer's channel expects — check how the shared `internal/enter-transition.ts` engine's tween path already samples/renders for other parts before inventing new sampling logic.

### Strand 3 — T-E6: sunburst Hint + Breadcrumb (SB4 / SB8)

- **SB4 — `Hint` render-prop + context.** Current state: `showcase/migrated/charts/internal/sunburst-hint.tsx` has `SunburstHint(_props: {...})` — the leading underscore means the prop parameter is **unused**, it's a stub — and a separate `SunburstHintDisplay({text})` that presumably does the actual rendering. Port the legacy render-prop/context pattern so `SunburstHint` actually consumes its props (check the legacy source under `repos/bklit-ui/packages/ui/src/charts/` for the exact contract — likely a context feeding hover/focus state into a render-prop children function) and wires through to `SunburstHintDisplay` rather than sitting inert.
- **SB8 — `SunburstBreadcrumb` family, full port.** Confirmed **zero** `Breadcrumb`-named symbols exist anywhere in `showcase/migrated/charts/` today (grep, no hits) — this is a from-scratch port, not a wiring fix. Legacy source fully read at `repos/bklit-ui/packages/ui/src/charts/sunburst-breadcrumb.tsx` (49 lines):
  - `SunburstBreadcrumbItem { id, label, isCurrent }`
  - `useSunburstBreadcrumbItems()` — reads `useSunburstStable()` for `{data, focus, focusById, rootId, zoomTo}`, walks `focusById.get(cur.parentId)` to build the crumb trail from the current focus back to root, returns `{items, zoomTo}`.
  - `SunburstBreadcrumbProps { className?, children: ReactNode }`
  - `SunburstBreadcrumb` — a memo'd `<nav aria-label="Drill-down path">` wrapper.
  
  > **⚠ LEAD RULING 2026-08-25 (D323) — `useSunburstStable` DOES NOT EXIST IN MIGRATED. Read this before writing any SB8 code.**
  >
  > The sentence below tells you to port "against migrated's actual `useSunburstStable` shape"
  > and to coordinate with P5.2 on it. **Both halves are false.** Verified:
  > - `useSunburstStable` / `SunburstStable` — **zero hits anywhere** in `showcase/migrated/charts/`.
  >   It exists only in the clone (`repos/bklit-ui/.../sunburst-context.tsx`). There is no
  >   migrated sunburst context, provider, or hook of any kind.
  > - **P5.2 has CLOSED and did not export it** (it cannot export what does not exist), so there
  >   is nothing to coordinate with.
  >
  > **The state you need is real, but it is PROP-DRILLED, not contextual.** `sunburst-chart.tsx`
  > holds `focusById`/`rootId`/`focus` as component-local state (`:239`, `:261-262`) and hands
  > them to `SunburstChartInner` as an explicit `layout={{ arcs, maxDepth, focusById, rootId,
  > sortedArcs }}` prop (`:273`). Children are **null-returning config carriers** classified by
  > `displayNameOf` (`:179-185`, `:367`) — nothing is provided to them, so a `SunburstBreadcrumb`
  > rendered as a child **cannot** read focus state.
  >
  > **DO NOT CREATE A SUNBURST CONTEXT OR PROVIDER TO SOLVE THIS.** DOC-4 rules context/provider
  > architecture removals ACCEPTED; reintroducing one here would reverse a deliberate project
  > decision to satisfy a charter sentence written before that decision took effect. If you find
  > yourself reaching for `createContext`, stop and re-read this box.
  >
  > **RULING — build SB8 on the ALREADY-PUBLIC controlled-focus API, with no new state:**
  > `SunburstChartProps` already exposes `focusId?: string` and
  > `onFocusChange?: (focusId: string) => void` (`:125-126`), with controlled/uncontrolled
  > handling at `:251-253`. That is a complete substitute for legacy's context:
  > 1. Export a **pure function** `buildSunburstBreadcrumbItems(data: SunburstNode, focusId: string):
  >    SunburstBreadcrumbItem[]` that walks the tree from `focusId` to root. It needs only `data`
  >    and `focusId` — both already public — so it does not need `focusById`, which stays internal.
  >    This replaces legacy's `useSunburstBreadcrumbItems()` hook.
  > 2. Ship `SunburstBreadcrumb` as a **presentational** `<nav aria-label="Drill-down path">`
  >    taking `items` plus an `onNavigate(id)` callback. Legacy's `zoomTo` is satisfied by the
  >    caller wiring `onNavigate` to `onFocusChange` — same capability, no hidden coupling.
  > 3. Keep `SunburstBreadcrumbItem { id, label, isCurrent }` exactly as legacy defines it.
  >
  > This is a **deliberate API shape change from legacy** (pure helper + presentational component
  > instead of context hook), so it is an ACCEPT-with-reason, not a silent divergence: state the
  > deviation plainly in your report and I will log it. Do not re-litigate this ruling; if you
  > find hard evidence it cannot work, stop and report rather than improvising a context.
  >
  > **[SUPERSEDED]** Port this against migrated's actual `useSunburstStable` shape (confirm its field names match — P5.2 is wrapping/exporting this hook in the same wave, coordinate rather than duplicate the lookup work). `useSunburstBreadcrumbItems` is itself in the worksheet's HOOK(22) list and `SunburstBreadcrumb`/`SunburstBreadcrumbItem`/`SunburstBreadcrumbProps` are in the COMPONENT/TYPE lists — but go-to-plan.md's own cross-ref (`go-to-plan.md:155`, "`SunburstBreadcrumb` = T-E6") explicitly routes the whole family here to P5.5, not to P5.2's mechanical hook-wrapper pass. Port the whole family in this package; do not split it across P5.2 and here.

## Gate

- `pnpm typecheck` exit 0 + production build exit 0.
- **Strand 1 (stub props) and Strand 2 (K4) are genuinely visual** — these change what actually reveals/tweens on screen. Real `qa/screenshot.mjs` Q1/Q2 gates per part touched, at each part's mandated density: line/composed/scatter/choropleth 1000 (choropleth may differ — check `qa/screenshot.mjs`'s density table if unsure), bar family **never n≥1000, use 100**, sunburst 27.
- **Strand 3 (sunburst Hint/Breadcrumb)** needs its own scenario — a sunburst hint/breadcrumb interaction capture (hover/drill-down triggering the hint, breadcrumb trail rendering and its `zoomTo` click behavior), at sunburst's mandated density (27).
- K4 specifically: capture a candlestick chart configured with a **keyframe-style enterTransition** and confirm the tween samples visibly match legacy's framer-driven keyframe tween, not just that it typechecks. A tween that compiles but renders as a plain linear fade would pass typecheck and fail the actual DOC-7 exception's intent.
- `qa/screenshot.mjs` compares **`bklit` vs `migrated`**. Never probe `impl=tanstack` (D258).
- Known chronic: markers n=100 hover-30 ~0.5507% (not this package's chart family, but check `qa/results/<chart>/` history before calling any failure new, generally).

## Report back

1. Per part touched in Strand 1 (line/bar/composed/scatter/choropleth/sunburst): what was wired, `file:line`, and the Q1/Q2 result at the correct density.
2. P3.2's actual status when you started and when you finished (did it land mid-session; did you have to re-derive the easing shape).
3. K4: the exact structural shape you added to `CandlestickEnterTransition`, and visual confirmation (not just typecheck) that the keyframe tween samples correctly.
4. SB4: what `SunburstHint` now actually consumes, and its render-prop/context contract.
5. SB8: confirmation the full breadcrumb family is ported and matches `useSunburstStable`'s actual current field names (not parity.md's or the legacy description, if they've drifted).
6. Anything contradicting this brief, stated plainly with evidence.
