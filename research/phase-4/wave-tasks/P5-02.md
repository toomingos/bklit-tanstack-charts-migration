# P5.2 — T-E1b: barrel restore, port/wrapper tier (COMPONENT + HOOK) (Wave 5)

Repo root: /Users/tomasdomingos/bklit-tanstack-charts-migration

## WRITE INCREMENTALLY
Write each edited/ported file to disk as soon as its edit is complete — do not batch work to the end. Uncommitted work on disk survives interruption; a resumed session picks up from the working tree. This package runs as **two sub-runs**: (a) COMPONENT tier, (b) HOOK tier. Gate (a) before starting (b); land and gate each symbol's port as you finish it rather than holding the whole sub-run in your head.

## Rules
- MAIN GOAL: exact same frontend design/interactivity as legacy bklit; TanStack-native backend; identical public component API.
- D232: fixes live in committed showcase/ code ONLY — never patch the clones (`repos/bklit-ui/`, `showcase/repos/tanstack-charts/`; both gitignored, Vercel re-clones fresh). Clones are read-only reference — this is where you read the legacy implementation you are porting from.
- Do NOT touch: `qa/`, `bench/` (running their scripts is allowed), the clones, `/tmp`. A guard hook denies these writes.
- Executors cannot delete files (guard policy D216). If a legacy-parity port makes an existing internal helper redundant, leave it in place and report it.
- Do not commit; leave changes in the working tree.
- go-to-plan.md is the source of truth (staleness rule 4.4.4); legacy reference is the bklit clone source.
- Migrated code root: `showcase/migrated/charts/` + `showcase/migrated/charts/internal/`. Public barrel: `showcase/migrated/charts/index.ts`.
- `qa/screenshot.mjs` compares **`bklit` vs `migrated`**. Never probe `impl=tanstack` — it is the performance-ceiling reference and deliberately ports none of bklit's chrome (D258).
- **Do not attempt T-E4's correctness fixes while you're in this file.** Several of the hooks you are wrapping here (`useRingStable`, `useRingHoverCoordinator`/legacy `useRingHover`) have known payload/rename gaps (R2/R3 — reduced payload, renamed contract) that are explicitly chartered to **P5.6** (T-E4), not this package. Wrap and export the hooks **as they exist today** — do not restore missing fields or rename anything here, or you will duplicate P5.6's work and create merge conflicts. Same boundary applies to `useChoropleth`/`useChoroplethZoom` (CP9/CP10 field-restore is P5.6) and `MarkerTooltipContent` (barrel-wiring only happens here if the *component* falls in your scope — its P5.6 cross-ref is about the compat rename, not the initial export).

## ⚠️ SUPERSEDED 2026-08-25 — read this before the Scope section below

Lead re-verified every locator in this charter against the live tree (D301
discipline: a charter written during planning states dependency premises that
the repo then invalidates). **Most of it holds. Four things do not.**

### 1. `internal/visx-pattern-bridge.tsx` NO LONGER EXISTS — the Scope section's central precedent

It was **folded into `internal/pattern-preset.tsx`**, which says so itself at
`:7`: `// ── visx pattern passthroughs (folded from ./visx-pattern-bridge) ──`.
The wrappers are now `pattern-preset.tsx:9-19`. The charter's `:1-17` and
`:7-17` line references are dead, but **the shape it describes is exactly
right** — named function component delegating to the aliased visx import, with
`.displayName` set. Read `pattern-preset.tsx:1-19` and follow that.

### 2. `PatternCircles`/`PatternLines` are NOT a pure barrel-line addition — they are module-private

The charter says these two "already exist as wrappers … just never reach the
public barrel — for these two it is a pure barrel-line addition, not new code."
**That is now false.** On disk they are `function PatternLines(` (`:9`) and
`function PatternCircles(` (`:14`) — **no `export` keyword**. The fold dropped
it because nothing outside the file consumed them. A bare barrel line will not
compile.

This is the same two-line shape as `lerpGeometry` in P5.1: add `export` at the
definition site, then the barrel line. `internal/index.ts:168` currently
re-exports only `renderPatternPreset` and `type PatternPresetId` from this
module, so the internal barrel likely needs a line too — check which barrel the
public one actually sources from before adding either.

### 3. `@visx/gradient` IS available — the Gradient×11 plan is viable

`showcase/package.json:22` pins `"@visx/gradient": "4.0.1-alpha.0"`, so the
dependency is real and you do not need to add it. The charter's other claim also
still holds: `grep -rn "visx/gradient" showcase/migrated/charts/` returns **zero
hits**, so all 11 are genuinely net-new wrappers, not hidden somewhere.

### 4. Small drift + one gate error

- `Background`'s real implementation is `internal/background.tsx:`**`28`** — not
  `:26`. P4.4 edited that file (`useSanitizedId` at `:48`). The other two
  `Background` locators are exact: `children.tsx:133` and `index.ts:214`. The
  three-way collision the charter describes is **real and unchanged** — it is
  still the most interesting judgment call in this package.
- **The Gate section says `pnpm typecheck`. Wrong for this repo.** Use
  `npx tsc --noEmit` **run from `showcase/`** — the repo root has a decoy `npx`
  that prints a joke and exits 0. **Never pipe tsc into `head`**: this is zsh,
  so `${PIPESTATUS[0]}` comes back empty and you would read a failure as a pass.
- Do not confuse `PatternArea` (already public, `index.ts:203`, with
  `PatternAreaConfig` at `:323`) with the `Pattern*`×4 set in scope here. It is
  a different component and is not yours.

### 5. `ChartBrushLayout` is NOT a port — it already exists and is already public

The Scope section calls for porting `ChartBrushLayout`, "the component, distinct
from the already-migrated `internal/brush-layout.tsx` layout math". **That
sentence is wrong on both halves.** `internal/brush-layout.tsx` is not layout
math — `:31` is `export const BrushLayout = React.memo(function BrushLayout({…})`,
a real render-prop component taking `children(layout)` and `brushStrip(layout)` —
and it is **already exported from the public barrel at `index.ts:344`**.

So the only open question for this symbol is a **naming/compat decision**: legacy
calls it `ChartBrushLayout`, migrated calls it `BrushLayout`. Decide whether the
barrel should also expose the legacy name as an alias, and say why. **Do not
build a second component.**

The same correction applies to the charter's "(orphan)" framing inherited from
`research/phase-4/internal-brush.md` — `chart-brush.tsx`, `brush-chrome.tsx` and
`brush-layout.tsx` are all wired, and `ChartBrush` is public at `index.ts:346`.
`index.ts:341-355` already exports `BrushLayout`, `BrushLayoutProps`,
`BrushLayoutState`, `ChartBrush`, `ChartBrushProps`, `BrushSelectionPattern`,
`ChartBrushSelectedBoxStyle`, `BrushHost`, `useBrushSelection`,
`resolveBrushTrackXExtent`.

**Genuinely absent, so these three ARE real ports:**
`ChartBrushSelectionOverlay`, `ChartBrushTrackOverlay`, `ChartBrushHandle` —
zero hits anywhere in `showcase/migrated/charts/`.

### Verified as still TRUE (do not re-derive)

`useRingStable` `ring-chart.tsx:125` and `useRingHoverCoordinator` `:135`, both
exported, neither barrel-exported — P4.3 rewrote this file and they survived
intact. `useActiveMarkers` `internal/marker-tooltip.tsx:148`, exported, exactly
as described. `BarYAxis` — zero hits repo-wide, genuinely absent.

### Sequencing

**This package edits `showcase/migrated/charts/index.ts`, which P5.1 owns while
it runs.** P5.2 must not be dispatched until P5.1 has closed and its barrel
additions are on disk — otherwise the two packages write the same file. Expect
the barrel to be ~200 lines longer than any line number in this charter implies;
re-grep rather than trusting an offset.

*The original Scope text follows unchanged for provenance.*

## Scope

This is the **T-E1b tier** of the FD12 barrel worksheet (`research/phase-4/fd12-barrel-worksheet.md`): the 40 COMPONENT + 22 HOOK symbols (62 total) that need real port/wrapper work, not a bare re-export, because their backing implementation either doesn't exist yet in migrated code, or exists only as an internal (non-legacy-named) equivalent.

### Sub-run (a) — COMPONENT (40)

**Verified via grep across `showcase/migrated/charts/` — zero implementations currently exist** for: `TooltipBox`, `TooltipContent`, `TooltipDot`, `TooltipIndicator`, `DateTicker`, `ChartRevealClip`, `ChartStatFlow`, `SeriesMarkers`, `SeriesPointMarker`, `MarkerGroup`, `ChartBrushSelectionOverlay`, `ChartBrushTrackOverlay`, `ChartBrushLayout` (the component, distinct from the already-migrated `internal/brush-layout.tsx` layout math), all 11 `Gradient*` visx re-exports, and 2 of the 4 `Pattern*` symbols. Port each from its legacy source under `repos/bklit-ui/packages/ui/src/charts/` (read-only reference).

- **`Gradient*` ×11** (`LinearGradient`, `RadialGradient`, `GradientDarkgreenGreen`, `GradientLightgreenGreen`, `GradientOrangeRed`, `GradientPinkBlue`, `GradientPinkRed`, `GradientPurpleOrange`, `GradientPurpleTeal`, `GradientSteelPurple`, `GradientTealBlue`) — none exist anywhere in migrated code (confirmed by grep for `visx/gradient`, zero hits). visx is still a project dependency via the pattern bridge, so these should be thin re-export wrappers following the exact precedent already in the codebase: `showcase/migrated/charts/internal/visx-pattern-bridge.tsx:1-17` wraps `@visx/pattern`'s `PatternCircles`/`PatternLines` in a named function component (`export function PatternLines(props: ComponentProps<typeof VisxPatternLines>) { return <VisxPatternLines {...props} />; }`, with `.displayName` set) rather than a bare re-export. Create a sibling `visx-gradient-bridge.tsx` (or extend the existing file) using the same shape for all 11 gradient symbols, sourced from `@visx/gradient`.
- **`Pattern*` ×4** — `PatternCircles` and `PatternLines` **already exist** as wrappers in `internal/visx-pattern-bridge.tsx:7-17`, just never reach the public barrel — for these two it is a pure barrel-line addition, not new code. `PatternHexagons` and `PatternWaves` have no wrapper yet; add them to the same bridge file, same shape.
- **Presentational components** (`TooltipBox`/`TooltipContent`/`TooltipDot`/`TooltipIndicator`/`DateTicker`/`ChartRevealClip`/`ChartStatFlow`/`SeriesMarkers`/`SeriesPointMarker`/`MarkerGroup`/`ChartBrush*` overlays) — genuine ports. Find each legacy source file under `repos/bklit-ui/packages/ui/src/charts/`, reproduce its rendering behavior against migrated's existing primitives/design tokens (do not reintroduce framer-motion or other legacy-only dependencies migrated has already replaced — check `internal/design-tokens.ts`, `internal/tooltip-chrome.ts` and similar internal modules for the migrated-native equivalents these should build on before writing net-new logic).
- **`Background`** — **do not treat this as a clean miss.** `showcase/migrated/charts/index.ts:214` already exports a symbol named `Background`, sourced from `showcase/migrated/charts/children.tsx:133`. Read that definition: `export function Background(_props: BackgroundConfig): null { ... }` — it is a **role-carrier marker** that always returns `null` and exists purely so JSX children-detection (`children.tsx:328`, `role === "background"`) can find it; it does not render anything itself. The actual presentational implementation — the one that paints the pattern fill — lives at `showcase/migrated/charts/internal/background.tsx:26` (`export function Background({ pattern, color, ... }) { ... }`, full visual props) and is consumed internally by `internal/background-layer.tsx:9`, but that module is never exported. Two same-named components exist, only the inert one is public. This is not a T-E1a-style "add a barrel line" fix — it's already "done" by the letter of the worksheet (a symbol called `Background` is exported) while functionally still missing the real component. Decide and document how you resolve this naming collision (e.g. does the real one get a different public name, does the marker move to an internal-only export path, etc.) rather than silently leaving both as-is; report your reasoning either way.
- **`ChartBrush*` overlays** — go-to-plan.md notes these are "live per D228"; check `docs/phase-4/LOG.md` D228 before assuming from scratch — it likely already establishes the pattern/precedent these overlay components should follow (probably a naming or architecture ruling from the original `chart-brush.tsx` refactor work).
- **`BarYAxis`** — go-to-plan.md cross-refs this one to DOC-9 (`go-to-plan.md:155`, "`BarYAxis` = DOC-9"): *"`BarYAxis` (B13) ports with orientation later; barrel gets the type surface now."* Confirmed zero implementation anywhere in `showcase/migrated/charts/` (grep, no hits) — this is a genuine COMPONENT-tier port, same as the presentational list above. Your job here is the **type surface only** per DOC-9's own wording — port `BarYAxis` and export `BarYAxisProps` so the barrel has the shape, but do not wire the not-yet-supported `orientation` behavior into it (that's a later, post-pilot-scope task; B13 in DOC-9's own text is explicitly deferred, "ports with orientation later"). If porting a inert/vertical-only stub is awkward without the orientation behavior it's named for, say so in your report rather than guessing at scope.

### Sub-run (b) — HOOK (22)

Full list: `useBarDepthEntries`, `useChart`, `useChartHover`, `useChartStable`, `useYScale`\*, `useActiveMarkers`, `usePie`, `usePieHover`, `usePieStable`, `useRadar`, `useRadarHover`, `useRadarStable`, `useRing`\*\*, `useRingHover`\*\*, `useRingStable`, `useSankey`, `useStaticChartPreview`\*\*\*, `useSunburstBreadcrumbItems`, `useSunburstHover`, `useSunburstStable`, `useAnimatedYDomains`\*, `useChartInteraction`.

\* **`useYScale` and `useAnimatedYDomains` are OUT OF SCOPE for this package** — go-to-plan.md defers them to **P6.2**, explicitly "post-T-F1" (T-F1 is the Wave-6 multi-axis `yAxisId` rework; these two hooks wrap the post-multi-axis scale layer, which does not exist yet). Do not wrap them now against the current single-axis scale code — you would just have to redo it once T-F1 lands. Confirm this exclusion in your report; do not silently include them.

\*\* **`useRing`/`useRingHover` — wrap the hooks as they exist today, do not fix them.** `showcase/migrated/charts/ring-chart.tsx` already has `export function useRingStable(): RingStableValue` and `export function useRingHoverCoordinator(): RingHoverCoordinator` (both throw outside `<RingChart>`), neither barrel-exported. There is no `useRing` combiner and no `useRingHover` at all in migrated code today — legacy's `useRing()` (`repos/bklit-ui/packages/ui/src/charts/ring-context.tsx:184-185`) is `{...useRingStable(), ...useRingHover()}`. Per the Rules section above, building that combiner and reconciling the renamed/reduced-payload hooks is **P5.6's job (R2/R3/R4)**. Your job here is narrower: barrel-export `useRingStable` and `useRingHoverCoordinator` under their **current** migrated names/shapes, so P5.6 has something to build on. Do not add a `useRing` or `useRingHover` export yourself — that would collide with P5.6's naming work.

\*\*\* **`useStaticChartPreview`** — cross-check against P5.3 before wrapping. The worksheet's CONTEXT-PROVIDER (8) list includes `StaticChartPreviewProvider`, and go-to-plan.md's T-E1c text names `useStaticChartPreview`+`Provider` as an **ACCEPT-per-DOC-10 candidate** (`LineLoadingSweep`/`BarLoadingSkeleton`/`generateChartSkeletonData`/`getSkeletonHeights`/`GenerateChartSkeletonDataOptions`/`useStaticChartPreview`+Provider → ACCEPT per DOC-10, go-to-plan.md:156). If P5.3 rules this hook ACCEPT (i.e., stays cut, logged not restored), do not wrap it here — check `docs/phase-4/LOG.md` for whatever D-entry P5.3 produces before finishing this sub-run, and skip it if so. If P5.3 hasn't run yet when you do, make your own call from the same evidence, document it, and flag the ordering risk in your report (go-to-plan.md itself says "P5.3 runs last of E1 trio but may start once P5.1/P5.2 sub-runs settle their dispositions" — the dependency is meant to run the other way, so if you hit this, say so).

For the remaining hooks (`useBarDepthEntries`, `useChart`, `useChartHover`, `useChartStable`, `useActiveMarkers`, `usePie`, `usePieHover`, `usePieStable`, `useRadar`, `useRadarHover`, `useRadarStable`, `useSankey`, `useSunburstBreadcrumbItems`, `useSunburstHover`, `useSunburstStable`, `useChartInteraction`): follow the `useRingStable`/`useRingHoverCoordinator` precedent — locate each part's internal `*Stable`/`*Hover`/`*Context` hook (they exist per-chart, e.g. `pie-chart.tsx`, `radar-chart.tsx`, `sankey-chart.tsx`, `sunburst-chart.tsx`, `children.tsx` for the generic `useChart*` family), confirm each throws a clear error outside its provider (do not weaken that contract), and add a barrel re-export under its current name. `useActiveMarkers` is already fully implemented and exported at `showcase/migrated/charts/internal/marker-tooltip.tsx:148` — this one really is a pure barrel-line addition, not a port. DOC-4 governs all of these: `*Provider` internals themselves stay unexported (context/provider architecture removal is ACCEPTED, not a gap) — only the **hook** gets a legacy-named public wrapper "where trivial" (go-to-plan.md DOC-4). If a hook's wrapper isn't trivial (i.e. it would require restoring removed context fields, not just re-exporting), stop and route it to P5.3/P5.6 instead of inventing new behavior.

## Gate

- **`pnpm typecheck` exit 0** and a **production build exit 0** after each sub-run, same as P5.1.
- Every new/ported COMPONENT gets a **`qa/screenshot.mjs` visual spot-check** against its host chart at that chart's mandated density (see densities below) — these are genuinely new render paths (unlike P5.1's inert barrel lines), so a pixel check is warranted wherever the component actually renders something visible (`Background`'s real implementation, the `Tooltip*` family, `SeriesMarkers`/`SeriesPointMarker`/`MarkerGroup`, `ChartBrush*` overlays). Pure re-exports with no new render path (e.g. `PatternCircles`/`PatternLines` barrel-line-only) don't need a pixel gate — same "pixel-inert" reasoning as P5.1.
- Mandated densities if you spot-check: bar family NEVER n≥1000 (use 100); markers/liveline/barsquares/bardepth 100; most others 1000. `qa/screenshot.mjs` compares **`bklit` vs `migrated`**. Never probe `impl=tanstack` (D258).
- Every wrapped HOOK: confirm it throws outside its provider exactly as its internal counterpart does (do not accidentally make it permissive), and confirm the barrel export's inferred TS return type matches the internal one (no `any` leakage through the wrapper).
- Known chronic: markers n=100 hover-30 ~0.5507% — check `qa/results/<chart>/` history before calling any failure new.

## Report back

1. COMPONENT sub-run: every symbol ported, its legacy source `file:line`, its new migrated `file:line`, and which internal building blocks it reused vs what was written from scratch.
2. The `Background` naming-collision finding: how you resolved it, and why.
3. HOOK sub-run: every symbol wrapped, `file:line` of the internal hook it wraps, confirmation of the throw-outside-provider contract.
4. Confirmation `useYScale`/`useAnimatedYDomains` were excluded, and confirmation of what you did with `useRing`/`useRingHover`/`useRingStable` (wrapped current shape only, no combiner) and `useStaticChartPreview` (wrapped, or skipped pending/per P5.3's ruling — cite the LOG.md entry if one existed).
5. `ChartBrush*` — what D228 actually said, and whether it changed your approach.
6. Anything contradicting this brief, stated plainly with evidence.
