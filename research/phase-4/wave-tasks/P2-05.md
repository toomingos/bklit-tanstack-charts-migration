# P2.5 — Wave 2 orphan residue (lead addition, D252)

Repo root: /Users/tomasdomingos/bklit-tanstack-charts-migration

## WRITE INCREMENTALLY
Write each edited file to disk as soon as its edit is complete — do not batch work to the end. Uncommitted work on disk survives interruption; a resumed session picks up from the working tree.

## Rules
- MAIN GOAL: exact same frontend design/interactivity as legacy bklit; TanStack-native backend; identical public component API.
- D232: fixes live in committed showcase/ code ONLY — never patch the clones (`repos/bklit-ui/`, `showcase/repos/tanstack-charts/`; both gitignored, Vercel re-clones fresh). Clones are read-only reference.
- Do NOT touch: `qa/`, `bench/` (running their scripts is allowed), the clones, /tmp. A guard hook denies these writes.
- Do not commit; leave changes in the working tree.
- go-to-plan.md is the source of truth (staleness rule 4.4.4).
- Migrated code root: `showcase/migrated/charts/` + `showcase/migrated/charts/internal/`.

## Provenance — why this package exists
This is a **lead addition**, not a go-to-plan row. Three orphans were found *by* Wave 2 packages while doing their own work, each correctly left alone because it fell outside that package's fences:

- items 1 and 3 were reported by **P2.1** (T-W2-1),
- item 2 was reported by **P2.2** (T-W2-2/5/6),

and none of them is owned by any remaining live dispatch. P2.1 and P2.2 have both **finished** (typecheck exit 0, build exit 0), so all three files below are now unfenced.

## Wave 2 is DELETE-ONLY — no behavior change
Every deletion must be provably behavior-neutral. The lead has already grepped each item and found no consumer, but **that evidence is reported to you, not proven for you** — re-run the greps yourself across `showcase/` (excluding `node_modules`, `repos/`, `.next/`) before deleting. If any item turns out to be live, STOP and report it; do not delete it anyway.

## Tasks

### 1. `paintHeatmapLegendSwatchHover` — `internal/heatmap-hover-chrome.ts:139`
Dead export. Same orphan class as `paintHeatmapCellHover`, which P2.1 deleted in this wave after D250's rewrite made the hover-paint helpers redundant (heatmap dimming now tweens the cell rect's own opacity).

**Cascade check required:** it calls `resolveHeatmapHoverStyle(...)` and reads `HEATMAP_INACTIVE_TRANSITION_CSS`. After deleting it, check whether those two — and the `HeatmapHoverStyleParams` interface directly above it — still have live consumers. Delete only what genuinely becomes orphaned; a symbol with a remaining reader stays. Do not delete a symbol just because it is *near* the one you removed.

### 2. `SharedTooltipChromeOptions.tooltipBoxSpring` — `internal/tooltip-chrome.ts:98`
A type-only field with no reader in the file body. This is the **fifth** per-chrome `tooltipBoxSpring` option; P2.2 deleted the other four under RULING K-1 but was fenced out of this file.

**RULING K-1 (go-to-plan.md:218) applies verbatim:** delete the per-chrome options, **KEEP `ChartTooltipConfig.boxSpringConfig`**, thread nothing new this wave.

Critically, `tooltipBoxSpring` is **still live elsewhere and must not be chased**:
- `internal/chart-config-context.tsx` — a real public `ChartConfig` field with a `TOOLTIP_BOX_SPRING` default, read at :64/:74/:79. **Live. Public API. Do not touch.**
- `scatter-chart.tsx:578-579` — reads `chartConfig.tooltipBoxSpring` and threads it into `boxSpringConfig`. **Live. Do not touch.**
- `internal/tooltip-chrome.ts:86` — `cfg.boxSpringConfig` is the consuming path K-1 preserves. **Live.**

Your deletion is exactly one line: the field on `SharedTooltipChromeOptions`. Confirm nothing constructs that options object with the field before removing it.

### 3. Dead `stroke` local — `internal/series-marker-mark.ts` (`buildMarkerMarks`)
Inside `buildMarkerMarks`, `const stroke = s.markers?.stroke ?? s.markers?.fill ?? s.stroke;` is computed, the `dot(...)` mark hardcodes `stroke: "none"`, and the local is discarded by a bare `void stroke;` at the end of the loop body. Delete both the binding and the `void`.

**Two guards:**
- There is a **different, live** `stroke` in `buildMarkerGradientDefs` higher in the same file (it goes into the gradient defs). Do not touch that one — the ring color reaches the marker through the gradient, which is why the mark's own stroke is `"none"`.
- The `void stroke;` reads like a deliberate marker that someone kept the computation on purpose. If, reading the file, you conclude it documents a real parity intent (e.g. a stroke path still to be wired), then say so and **leave it with a comment explaining why** instead of deleting. Report either way.

## Gates (all must pass before reporting done)
- `cd showcase && npx tsc --noEmit` exits 0.
- Production build must succeed.
- **Wave gate-runner (D246 impl/gate split)**: do NOT run QA pixel compares, console checks, or bench yourself — swept once for the whole wave. Report the affected charts + densities instead.
- **Density rules the gate-runner must be told, and must not default away from:** heatmap `--n 52` (at `n=1000` the hover probe lands in empty space and the run fails for a harness reason unrelated to your code — a known symmetric artifact, must not be "fixed"); markers `--n 100`; bar family NEVER `n≥1000` (degenerate at that density); most others `--n 1000`.

## Live fences
**P2.3** is running and owns `styles.css` plus comment-only edits in `gauge.tsx`, `pie`, `internal/grid.ts`. **P2.4** is running and owns pie hover parity. None of your three files overlap either — but if you hit a conflict in those files, they are not yours: report rather than edit.

## Report back
- What was deleted, and the deadness grep that proved no consumer, per item.
- For item 1: what the cascade check found — which adjacent symbols you removed and which you kept, with the reader that kept them alive.
- For item 3: whether you deleted or documented, and why.
- Anything you found was NOT actually dead — a genuinely useful finding; report it, do not delete it anyway.
- Gate evidence: typecheck exit status, build exit status, plus the affected chart/scenario/density list for the gate-runner with the density notes above verbatim.
- Anything unresolved or intentionally skipped, with reason — report failures honestly; do not omit them.
