# P1.13 — Children CH cluster + Background wire + AX5 label tweens (Wave 1)

Repo root: /Users/tomasdomingos/bklit-tanstack-charts-migration

## WRITE INCREMENTALLY
Write each edited file to disk as soon as its edit is complete — do not batch work to the end. Uncommitted work on disk survives interruption; a resumed session picks up from the working tree.

## Rules
- MAIN GOAL: exact same frontend design/interactivity as legacy bklit; TanStack-native backend; identical public component API.
- D232: fixes live in committed showcase/ code ONLY — never patch the clones (`repos/bklit-ui/`, `showcase/repos/tanstack-charts/`; both gitignored, Vercel re-clones fresh). Clones are read-only reference.
- Do NOT touch: `qa/`, `bench/` (running their scripts is allowed), the clones, /tmp. A guard hook denies these writes.
- Do not commit; leave changes in the working tree.
- go-to-plan.md is the source of truth (staleness rule 4.4.4); legacy reference for pixel/behavior parity is the bklit clone source.
- Migrated code root: `showcase/migrated/charts/` + `showcase/migrated/charts/internal/` (consumed by `showcase/` via the `@showcase/migrated-charts` wrapper package).
- Finding IDs (CH3, B12, …) resolve in `research/phase-4/children.md` — use part reports to LOCATE code only; behavior rulings come from go-to-plan.md.

## Tasks

### T-W1-18 (verbatim from go-to-plan.md, Batch W1)
- **Task:** Children: CH3 accept both `numTicksRows`/`numTicks`, CH4 `numTicksColumns` plumb, CH5 `tickMode` domain (+`tickerHalfWidth` forward B12), CH13 wire `<Background>`
- **Files:** children.tsx, grid, x-axis, background
- **Acceptance:** legacy child props effective

Coverage resolution (orphan AX5, quoted): "AX5 axis label position tweens | **FIX** — restore CSS position transitions on axis labels; added to T-W1-18 (P1.13, axis/children cluster)." Fold this in: restore CSS position transitions on axis labels (left/top currently jump statically).

Centralize ruling folded into this cluster (OQ centralize 1–2, quoted): "`Background`: FIX-wire (CH13 — legacy had it wired; add carrier + host slot)." Grounding: `charts/internal/background.tsx` exists; wiring target is the chart-host slot per legacy behavior.

Downstream dependency (do not break): "T-W1-18 → T-E1b (`Background` re-export) — Barrel re-export needs the wired module."

Grounding for main task: `charts/children.tsx`, `charts/internal/grid.ts` (consumer sites), `charts/internal/x-ticks.ts` / `x-axis-overlay.tsx`, plus `y-axis-overlay.tsx` for AX5. Legacy child-prop semantics referenced against the bklit clone.

## Gates (all must pass before reporting done)
- `cd showcase && npx tsc --noEmit` exits 0.
- **Wave gate-runner (D246 impl/gate split)**: do NOT run QA pixel compares or console checks yourself — swept once for the whole wave. Report for the gate-runner: line incl. hover captures (`--n 1000`), plus legend scenario if children props affect it (`--n 4`); console entries line + legend.

## Report back
- What changed per item (CH3/CH4/CH5+B12/CH13/AX5: files + behavior).
- Gate evidence: typecheck exit status. List the affected chart(s)/scenario(s)/density for the wave gate-runner to sweep (QA pixel compare + console errors).
- The specific density/scenario notes below MUST be included in that list (do not let the gate-runner default to n=1000 where a different density is required).
- Anything unresolved or intentionally skipped, with reason — report failures honestly; do not omit them.
