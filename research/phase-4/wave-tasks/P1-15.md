# P1.15 — Area A-cluster (A2/A4/A5/A8/A9/A3) (Wave 1)

Repo root: /Users/tomasdomingos/bklit-tanstack-charts-migration

## WRITE INCREMENTALLY
Write each edited file to disk as soon as its edit is complete — do not batch work to the end. Uncommitted work on disk survives interruption; a resumed session picks up from the working tree.

## Rules
- MAIN GOAL: exact same frontend design/interactivity as legacy bklit; TanStack-native backend; identical public component API.
- D232: fixes live in committed showcase/ code ONLY — never patch the clones (`repos/bklit-ui/`, `showcase/repos/tanstack-charts/`; both gitignored, Vercel re-clones fresh). Clones are read-only reference.
- Do NOT touch: `qa/`, `bench/` (running their scripts is allowed), the clones, /tmp. A guard hook denies these writes.
- Do not commit; leave changes in the working tree.
- go-to-plan.md is the source of truth (staleness rule 4.4.4); legacy reference for pixel/behavior parity is the bklit clone source.
- Migrated code root: `showcase/migrated/charts/` + `showcase/migrated/charts/internal/` (consumed by `showcase/` via the `@showcase/migrated-charts` wrapper package).

## Tasks

### T-W1-20 (verbatim from go-to-plan.md, Batch W1)
- **Task:** Area: A2 animationEasing consumed, A4 showLine toggle, A5 gradient stops, A8 fadeEdges directional, A9 touchAction, A3 type declaration
- **Files:** area chart/marks
- **Acceptance:** legacy props effective; D13c respected

Coverage resolutions folded in:
- OQ parity 10 / `animationEasing` policy (quoted): "FIX — area consumes like line (A2); parts gaining stub props (W5) forward easing into WAAPI/motion timing where the engine supports it; document the mapping in one place (`enter-transition`)."
- D13c precedent context (from `docs/LOG.md`, area `fadeEdges` directional gating partial precedent): A8 implements directional masks for `"left"`/`"right"` while preserving the boolean-all-true aggregate path that D13c established.

Grounding: `charts/area-chart.tsx`, `internal/area-fill-mark.ts`, plus `internal/types.ts` (A3 declaration surface) and `internal/fade-mask.ts` as discovered. The easing-mapping documentation deliverable ("document the mapping in one place") belongs to the `enter-transition` module documentation — add it there only if it doesn't expand scope into P3.2/P5.5 territory; otherwise flag it.

## Gates (all must pass before reporting done)
- `cd showcase && npx tsc --noEmit` exits 0.
- **Wave gate-runner (D246 impl/gate split)**: do NOT run QA pixel compares or console checks yourself — swept once for the whole wave. Report for the gate-runner: area incl. hover captures (`--n 1000`); console sweep must cover both the area entry (n=100) and its `state=loading` load.

## Report back
- What changed per item A2/A4/A5/A8/A9/A3 (files + behavior).
- Gate evidence: typecheck exit status. List the affected chart(s)/scenario(s)/density for the wave gate-runner to sweep (QA pixel compare + console errors).
- The specific density/scenario notes below MUST be included in that list (do not let the gate-runner default to n=1000 where a different density is required).
- Anything unresolved or intentionally skipped, with reason — report failures honestly; do not omit them.
