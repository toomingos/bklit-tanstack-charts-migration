# P6.1 — T-F1 (DOC-8): multi-axis `yAxisId` — scale-resolution layer + 6 consumer clusters + AX7 + AX4 (Wave 6)

Repo root: /Users/tomasdomingos/bklit-tanstack-charts-migration

## WRITE INCREMENTALLY
Write each edited file to disk as soon as its edit is complete — do not batch work to the end. Uncommitted work on disk survives interruption; a resumed session picks up from the working tree.

## Rules
- MAIN GOAL: exact same frontend design/interactivity as legacy bklit; TanStack-native backend; identical public component API.
- D232: fixes live in committed showcase/ code ONLY — never patch the clones (`repos/bklit-ui/`, `showcase/repos/tanstack-charts/`; both gitignored, Vercel re-clones fresh). Clones are read-only reference.
- Do NOT touch: `qa/`, `bench/` (running their scripts is allowed), the clones, /tmp. A guard hook denies these writes.
- You cannot delete files (guard policy D216). If this task orphans anything, un-wire it, leave the file, report it to the lead.
- Do not commit; leave changes in the working tree.
- go-to-plan.md is the source of truth (staleness rule 4.4.4); legacy reference is the bklit clone source.
- Migrated code root: `showcase/migrated/charts/` + `showcase/migrated/charts/internal/`.
- `qa/screenshot.mjs` compares **`bklit` vs `migrated`**. Never probe `impl=tanstack` — it is the performance-ceiling reference and deliberately ports none of bklit's chrome (this caused a full wrong-impl investigation once already, D258).

## Why this package is different from every other Wave 6 package

This is the largest single behavior task in the whole plan and it has its own batch for that reason. Quote, verbatim, the ruling that charters it (go-to-plan.md doctrine table, DOC-8):

> **Single-axis reality → FIX multi-axis** (L10 family): per-series `yAxisId` resolved in the one scale-resolution layer (custom; TanStack has no axis-id concept — confirmed). Largest single behavior task; own batch.

And the open-question resolution that scopes it precisely (go-to-plan.md, parity item 3 / L10):

> FIX per DOC-8 (batch W6). Types stay; per-axis domains resolved in `resolveTimeSeriesYDomain` layer; `createTickColorResolver` per-axis filter (AX7) fixed in same task.

Do not reinterpret either of these. In particular: **types already exist** — `yAxisId?: string | number` is already declared on the relevant config interfaces in `showcase/migrated/charts/internal/types.ts` (six separate locations: lines 41, 85, 203, 426, 497, 515, 523). Your job is to make the scale machinery actually *read* that field, not to add it.

**Why this is repo-wide risk, not local risk:** `resolveTimeSeriesYDomain` and `createNicedYScale` in `showcase/migrated/charts/internal/y-domain.ts` are the shared y-domain layer for every cartesian time-series chart in the migrated codebase. A defect introduced here does not stay inside "the multi-axis feature" — it can silently shift the y-scale of every line, area, bar, scatter, candlestick, composed, and live-line chart, whether or not that chart ever uses `yAxisId`. Treat this package with the same caution as a change to a shared reconciler internal, not as a feature add.

## Mandatory sequencing — gate each stage before starting the next

Do not implement this as one big diff. There are two hazards that require staged gating:

1. **Scale-resolution layer first, gated alone, before any consumer is touched.** Rewrite `showcase/migrated/charts/internal/y-domain.ts` to group series by normalized `yAxisId` and resolve one domain per axis group (see "Legacy reference pattern" below), and re-add the missing `yScaleDomainMax` param (AX4, see below). Run a typecheck against this file's own module boundary and, once wired into at least one consumer for a smoke test, run that consumer's Q1/Q2 scenario before moving on. Do not touch the remaining five consumer clusters until the scale layer's shape is settled — every consumer will read this layer's return type, so an unstable layer means five wasted rewrites.
2. **Consumers, one cluster at a time, each gated (typecheck + that chart's Q1/Q2 multi-series-per-axis scenario) before the next cluster starts.** Do not open edits across two consumer files concurrently. If a defect in cluster 2 turns out to require reshaping the scale layer from step 1, fix the layer, then re-gate cluster 1 before proceeding — do not assume cluster 1 is still correct against a changed layer.

## The six consumer clusters (go-to-plan.md, Wave 6 package table, verbatim list: "consumers line/area/bar/scatter/composed/reference-area")

Work them in this order (the order they're listed in the source table) unless a dependency you discover forces a reorder — if you reorder, say so and why in your report.

1. **line** — `showcase/migrated/charts/line-chart.tsx`. Currently calls `resolveTimeSeriesYDomain(data, lines)` once for the whole chart (call site `:229`) and feeds the single result to `createNicedYScale`. No `yAxisId` grouping today. Also imports `createTickColorResolver` at `:28` for its reference-area/axis-label integration — see AX7 below, this consumer's tick-color usage must switch to the per-axis-filtered version.
2. **area** — `showcase/migrated/charts/area-chart.tsx`. Same single-call pattern at `:334`. Also has two duplicated inline `xForDate` closures (`:438`, `:466`) — these are **NOT your job**; they're explicitly deferred to P6.2 to avoid double-touching this same host twice. Leave them as-is; do not opportunistically dedupe them here even though you'll be in this file.
3. **bar** — `showcase/migrated/charts/bar-chart.tsx`. Currently has NO `yAxisId` handling at all — grep confirms zero occurrences in this file. Its y-domain is computed independently at `:286-294` (`yDomain` → `createNicedYScale`) with a second, separate `yDomainBar` at `:980` (`[0, maxValue * 1.1]`, consumed at `:1030`) used in a different nested context — check whether that second domain also needs per-axis awareness or is legitimately axis-independent (e.g. driving something that isn't a rendered scale) before you decide it's out of scope. Parity rows **B7** (`<Bar>` yAxisId, missing, med) and **B10** (`BarSquares.yAxisId`, missing, med) both cross-reference this ruling directly — both must be resolved by this cluster's work, not deferred further.
4. **scatter** — `showcase/migrated/charts/scatter-chart.tsx`. Has its own scatter-specific y-domain rule (max floored at 0, negatives silently ignored, no padding — `docs/LOG.md` D14, comment at `:330`), computed at `:335` and scaled at `:462-488`, consumed at `:936`. **Do not disturb the D14 scatter-specific domain math** (floor-at-0, no padding) while adding per-axis grouping on top of it — the multi-axis fix must compose with D14, not replace it. Parity row **S6** (`<Scatter>` yAxisId, missing, med) is this cluster's charter.
5. **composed** — `showcase/migrated/charts/composed-chart.tsx`. Same single-call pattern at `:586`. This file also has a *separate*, already-yAxisId-aware subsystem for **projection lines** (`internal/projection-config.ts`, `mergeProjectionYDomain(domain, configs, yAxisId)`, consumed at `composed-chart.tsx:598-604,769,885`) — that machinery is unrelated to this task (it already handles its own axis filtering) and must not be conflated with the main-series scale rewrite; leave it alone unless your rewrite of the shared domain it reads from requires a compatible signature change, in which case update the call sites only, not the projection logic itself. Parity rows **C13+C14** are explicit: "yAxisId → L10 ruling" — composed's dropped per-series `yAxisId` on the *main* series (not projections) is folded into this task, not deferred.
6. **reference-area** — `showcase/migrated/charts/internal/reference-area-layer.tsx` (component-level types at `:23` and `:290`, single `yDomain: [number, number]` prop threaded at `:345`, internal `scaleLinear().domain(yDomain).range(...)` at `:109-111`) plus `showcase/migrated/charts/reference-area.tsx` (`yAxisId` typed at `:15`) and `showcase/migrated/charts/internal/reference-area-config.ts` (already calls `normalizeYAxisId(props.yAxisId)` at `:43` — there is already SOME yAxisId-normalization infrastructure in this file; check what it currently does with the normalized id before assuming you're starting from zero, it may already be plumbed partway). Parity row **RA2** (`yAxisId`, behavior, med, "Cross-ref multi-axis ruling"). This cluster must select the correct per-axis `yDomain` for a reference area based on its own `yAxisId`, not always receive the chart's single domain.

### AX7 — fold into the reference-area cluster (per the OQ resolution text above, "fixed in same task")

`createTickColorResolver` in `showcase/migrated/charts/internal/reference-area-geometry.ts:122-134` takes one flat `domain` and returns the first matching band's `axisLabelColor` regardless of which axis a value is actually on:

```typescript
export function createTickColorResolver(
  configs: Array<{ y1?: number; y2?: number; axisLabelColor?: string }>,
  domain: [number, number],
): (value: number) => string | undefined {
  return (value: number): string | undefined => {
    for (const area of configs) {
      if (!area.axisLabelColor) continue;
      const [low, high] = resolveReferenceDataRange(area.y1, area.y2, domain);
      if (value >= low && value <= high) return area.axisLabelColor;
    }
    return undefined;
  };
}
```

Parity row **AX7**: "Filtered by normalized yAxisId" (legacy) vs "First matching band regardless of axis" (migrated), ruled "ACCEPT single-axis reality (revisit with L10 ruling)" — this task IS that revisit. Consumers of this resolver: `area-chart.tsx:978`, `line-chart.tsx:953`, `live-line-chart.tsx:707`. **`live-line-chart.tsx` is a consumer of this function but live-line itself is explicitly OUT of the T-F1 consumer clusters** (go-to-plan.md dependency graph: "T-W1-16 (LiveYAxis right) → T-F1? no | Independent: live-line axis position doesn't use the y-axis-id scale layer; parallel-safe" — and that item already landed independently in Wave 1, P1.11). Update the resolver's signature to filter per-axis, and update all three call sites' arguments accordingly — but do not otherwise touch live-line's scale logic.

### AX4 — re-add in the scale-resolution layer itself, not in a consumer

Parity row **AX4**: `resolveTimeSeriesYDomain`'s `yScaleDomainMax` param (drove the skeleton-domain short-circuit `[0, max * 1.1]`) is simply gone from the current signature in `internal/y-domain.ts`. Re-add it as an optional param. Do this as part of stage 1 (the scale-layer rewrite), since every consumer's call site needs to agree on the final signature before you touch them.

## Legacy reference pattern (read-only — do not port verbatim, the migrated architecture has no `ChartProvider` context to thread scales through, but replicate the *behavior*)

`repos/bklit-ui/packages/ui/src/charts/y-axis-scales.ts` — normalizes an id, groups series by it, and builds one scale per group with a "left" default and single-scale fallback for the un-multi-axis case:

```typescript
export const DEFAULT_Y_AXIS_ID = "left";

export function normalizeYAxisId(id?: string | number): string {
  if (id == null || id === "") return DEFAULT_Y_AXIS_ID;
  return String(id);
}

export function groupLinesByYAxisId(lines: LineConfig[]): Map<string, LineConfig[]> { ... }

export function buildYScalesFromDomains({
  lines, innerHeight, domainsByAxis,
}: {
  lines: LineConfig[]; innerHeight: number; domainsByAxis: Record<string, [number, number]>;
}): Record<string, YScale> {
  const groups = groupLinesByYAxisId(lines);
  const scales: Record<string, YScale> = {};
  for (const [axisId] of groups) {
    const domain = domainsByAxis[axisId] ?? domainsByAxis[DEFAULT_Y_AXIS_ID] ?? ([0, 100] as [number, number]);
    scales[axisId] = scaleLinear({ range: [innerHeight, 0], domain });
  }
  if (!scales[DEFAULT_Y_AXIS_ID]) {
    scales[DEFAULT_Y_AXIS_ID] = scaleLinear({ range: [innerHeight, 0], domain: domainsByAxis[DEFAULT_Y_AXIS_ID] ?? [0, 100] });
  }
  return scales;
}
```

Also present in the clone (not yet read by the researcher who wrote this brief — check it yourself before designing your resolution layer): `repos/bklit-ui/packages/ui/src/charts/y-domain-utils.ts` (`computeYDomainsByAxis`) and its test `__tests__/y-domain-utils.test.ts`. The migrated equivalent should produce, at minimum: a normalized-id grouping function, a per-axis domain resolver (`resolveTimeSeriesYDomain`'s multi-axis successor), and a per-axis scale builder — but you are free to shape the actual API differently to fit the migrated codebase's non-context architecture, since nothing downstream in migrated threads a `ChartProvider`-style context the way legacy does. State your final API shape plainly in your report (see below) — P6.2 depends on it being stable.

`showcase/migrated/charts/internal/reference-area-config.ts:43` already has a `normalizeYAxisId(...)` call — find where that function is defined/imported from and consider reusing or extending it rather than inventing a second normalizer.

## Sequencing hazard for P6.2 — read this before you finish

P6.2 (a separate, later package) will write brand-new `useYScale`/`useAnimatedYDomains` wrapper hooks around whatever scale-resolution API you leave behind — these hooks **do not exist anywhere in the codebase today** (confirmed: zero occurrences of either name under `showcase/migrated/`), so P6.2 is writing them from scratch, not wrapping something already stable. If you leave the scale-layer's public shape (function names, param order, return shape) in flux, ambiguous, or split across multiple half-finished variants, **say so explicitly in your report** rather than letting it look finished. P6.2 will treat your final state as the contract; an unstated ambiguity here becomes a wrapper-around-a-moving-target problem for them, silently.

Also for your awareness (not your job): `research/phase-4/go-to-plan.md`'s "Moves, splits, merges" log records that this package was deliberately **not** split further — "SPLIT T-F1 → P6.1 stays whole (scale layer + consumers are one coherent change set); its downstream dependents split out instead (P6.2). Candidate split not taken." Do not propose re-splitting P6.1 itself; the split question was already decided against for the six consumer clusters. If you find a *specific* reason mid-implementation that the six-cluster-in-one-package shape is actually unworkable, report the evidence — don't just split unilaterally.

## Already ruled out / already landed — do NOT re-investigate

- **T-W1-16 (LiveYAxis `position="right"`)** already landed in Wave 1 (P1.11, gate PASS, liveline n=100 ≤0.1368%) and is explicitly independent of this task per the dependency graph quoted above. Do not touch `live-line-chart.tsx`'s axis-position logic. You will touch its `createTickColorResolver` call site only (see AX7 above), nothing else in that file.
- Parity.md's open-question list (line ~306) names "L10/B7/B10/S6/RA2/CH7" together in one line about multi-axis strategy. **CH7 is not a seventh consumer cluster** — it is `YAxis`/`LiveYAxis` orientation="right" rendering (children part), and that item was already resolved independently via T-W1-16/P1.11 as noted above. Don't let that line send you looking for a "children" cluster; go-to-plan.md's actual task table is unambiguous about the six clusters named above, and the parity.md line is simply an un-pruned cross-reference written before T-W1-16 was split out and landed.

## Gate

This touches the shared y-domain layer, so the sweep is **broad, not just the six clusters that gain multi-axis support**. Run Q1/Q2 for every cartesian chart that reads the shared scale layer or is otherwise plausibly exposed to a regression here, at its mandated density:

- **line, area, composed, scatter** — n=1000 (their mandated density), plus a **multi-series-per-axis scenario** for each (at least two series/marks on two different `yAxisId` values) — this is the actual new-behavior gate, not just a regression check.
- **bar** — n=100 (bar family NEVER runs n≥1000), plus its own multi-series-per-axis scenario (`<Bar>`/`BarSquares` yAxisId).
- **candlestick** — n=1000. Not one of the six consumer clusters, but shares cartesian y-domain wiring closely enough to be worth a broad-sweep regression check; not expected to change, confirm it doesn't.
- **liveline** — n=100. Also not a consumer cluster (explicitly independent per the dependency graph), included in the broad sweep for the same shared-infrastructure caution, not because you're expected to touch it.
- **reference-area**: gate through its host charts (line/area, whichever scenarios exercise a configured `<ReferenceArea yAxisId=...>`) rather than as a standalone density — it has no independent mandated density of its own in the plan.

Per go-to-plan.md's own Wave 6 gate column: **"Q1/Q2 multi-series-per-axis scenario per part; bench line/bar/scatter/composed."** Run those four bench comparisons (line/bar/scatter/composed) against the frozen 4.4.1 baseline in `docs/phase-4/BENCHMARKS.md` before reporting done — a shared-scale-layer change is exactly the kind of thing that can silently regress mount/update timing across charts that don't even use the new feature.

`qa/screenshot.mjs` compares **`bklit` vs `migrated`**. Never probe `impl=tanstack`.

## Report back

1. The final public shape of the scale-resolution layer (function names, signatures, return types) in `internal/y-domain.ts` — be explicit and complete, this is P6.2's contract. State plainly whether you consider it stable/final or still in flux, and why.
2. Confirmation of the staged-gate order you actually used: scale layer gated alone first, then each of the six consumer clusters gated individually before the next started. If you deviated from the listed order (line/area/bar/scatter/composed/reference-area), say what order you used and why.
3. Per cluster: what changed, the multi-series-per-axis scenario result, and the parity row(s) it closes (L10/B7/B10/S6/C13+C14/RA2).
4. AX7: the resolver's new per-axis-filtered signature and confirmation all three call sites (area/line/live-line) were updated without touching live-line's own scale logic.
5. AX4: confirmation `yScaleDomainMax` is re-added and behaves as the skeleton-domain short-circuit `[0, max * 1.1]` describes.
6. The broad regression sweep results (line/area/bar/scatter/candlestick/composed/liveline) at mandated densities, plus the four bench comparisons (line/bar/scatter/composed) against the 4.4.1 baseline.
7. Anything contradicting this brief, stated plainly with evidence — including if the six-clusters-in-one-package shape turned out to be a mistake (do not silently re-split; report and let the lead decide).
