# P4.1 — T-C1 (tooltip mapper kit) + surviving T-C5 folds (Wave 4)

Repo root: /Users/tomasdomingos/bklit-tanstack-charts-migration

## WRITE INCREMENTALLY
Write each edited file to disk as soon as its edit is complete — do not batch work to the end. Uncommitted work on disk survives interruption; a resumed session picks up from the working tree.

## Rules
- MAIN GOAL: exact same frontend design/interactivity as legacy bklit; TanStack-native backend; identical public component API.
- D232: fixes live in committed showcase/ code ONLY — never patch the clones (`repos/bklit-ui/`, `showcase/repos/tanstack-charts/`; both gitignored, Vercel re-clones fresh). Clones are read-only reference.
- Do NOT touch: `qa/`, `bench/` (running their scripts is allowed), the clones, /tmp. A guard hook denies these writes.
- You cannot delete files (guard policy D216). If this package requires deleting a module (e.g. a fold leaves a file empty), do the un-wiring, leave the orphaned file in place, and report it to the lead for deletion.
- Do not commit; leave changes in the working tree.
- go-to-plan.md is the source of truth (staleness rule 4.4.4); synthesis docs (`research/phase-4/synthesis/centralize.md`) may be stale relative to the current working tree — **this package is a live example of that**, see Hazard below.
- Migrated code root: `showcase/migrated/charts/` + `showcase/migrated/charts/internal/`.

## HAZARD — read before touching anything

**T-D8 (native tooltip cutover, P3.9) has NOT landed.** Per `docs/phase-4/PROGRESS.md`, Wave 3's `P3.9` row is still unchecked. T-C5's original charter in `research/phase-4/synthesis/centralize.md` assumed T-D8 would run first and reshape/delete large parts of the pre-cutover tooltip-chrome family; go-to-plan.md's Moves log records `MERGE T-C1 + surviving-half of T-C5 → P4.1 (tooltip-chrome-centric)` — i.e. this package only inherits whichever T-C5 folds *survive* T-D8. Since T-D8 has not run, **every tooltip-chrome-family file currently exists in its pre-cutover form**, and centralize.md's importer lists were written against a pre-refactor tree that may already have moved (Wave 2/Wave 3 landed in between).

**Do not trust centralize.md's importer/duplication lists as-is.** Re-derive the live importer set from the working tree at dispatch time (`grep -rn` each candidate module's name across `showcase/migrated/charts/`), and **report back which T-C5 folds actually survived** — some folds this brief lists as "confirmed safe" were re-verified during drafting (see Scope below), but re-verify again at dispatch since more of Wave 3 may land before you start.

One fold centralize.md and go-to-plan.md both describe as single-importer is **not**, as of this drafting — see Strand 2, `radar-spring.ts`. Do not assume any "single-importer" claim in either doc without grepping it yourself first.

## Scope

Two strands.

### Strand 1 — T-C1: tooltip mapper kit

Five hover-chrome modules under `internal/` each define (or omit) three mapper functions — `toDotConfig`, `toIndicatorConfig`, `toBoxConfig` — that translate a `ChartTooltipConfig`-shaped config into visual-primitive configs (`DotConfig`, indicator config, box config), duplicated near-verbatim:

- `internal/hover-chrome.ts` — canonical shape. `toDotConfig` (line 111), `toIndicatorConfig` (123), `toBoxConfig` (137), local `gradientCounter` (109), consumed by `attachHoverChrome` (170) and used at 180/205/207/312/597. Importers of this file: `pie-chart.tsx`, `internal/ring-hover-chrome.ts`, `internal/funnel-hover-chrome.ts`, and others — re-grep at dispatch, do not assume this list is exhaustive.
- `internal/bar-hover-chrome.ts` — mappers at lines 83/96/110, `gradientCounter` at 81, `BarHoverChromeOptions` interface at 77. **As of this drafting, `toDotConfig`'s signature is `(cfg?: ChartTooltipConfig | null): DotConfig` — already normalized to match the other chromes.** centralize.md's claim that bar's mapper "takes extra square-size args" is stale (Wave 2/P2.2 already normalized it — see D252/D253 in `docs/phase-4/LOG.md`). The only remaining `bandWidth` reference in this file is a **dead comment** at line 376 ("Ring-dot sizing is inert (bar.md deviation): bandWidth was never..."); the phantom-read code itself was already deleted in Wave 2. Do not re-fix what's already fixed — just fold the (now-identical-shaped) mapper into the kit and drop the stale comment if it no longer describes anything real.
- `internal/candlestick-hover-chrome.ts` — has **no** `toDotConfig` (candlesticks have no dot marker); only `toIndicatorConfig`/`toBoxConfig` exist. The kit's shared surface for this chart is a strict subset — don't force a no-op `toDotConfig` into candlestick's usage just to make the kit uniform.
- `internal/scatter-hover-chrome.ts` — all three mappers, at 92/103/116, `gradientCounter` at 90.
- `internal/live-hover-chrome.ts` — all three mappers (103/115/125) but keyed off a **different config type**, `LiveHoverConfig`, not `ChartTooltipConfig`. This is a genuine signature divergence, not a copy-paste artifact — the kit's exported function signatures need to be generic enough (or overloaded) to cover both `ChartTooltipConfig` and `LiveHoverConfig` inputs, or live-line's usage stays outside the shared kit with a documented reason.

Also duplicated near the same family: `MARKER_DIM_OPACITY`/`MARKER_DIM_BLUR_PX`/`MARKER_DIM_TRANSITION` constants (`internal/hover-chrome.ts:38-40`, used at 493-495 and 664-666 — **only within this one file**, no matching declarations found in bar/scatter/candlestick/live's hover-chrome modules at drafting time). centralize.md's inline-duplication table implies this is cross-file; grep at dispatch did not confirm a second site. If you don't find a genuine cross-file duplicate, don't invent a fold — report it as a stale citation instead.

### Strand 2 — surviving T-C5 single-importer folds

Four folds confirmed single-importer by direct grep against the current tree (safe to execute as chartered, pending your own re-grep at dispatch since Wave 3 work may land in between):

| Module | Sole importer (grep-confirmed) |
|---|---|
| `internal/tooltip-scheduler.ts` | `internal/tooltip-chrome.ts:19` |
| `internal/hover-reanchor.ts` | `internal/hover-chrome.ts:28` |
| `internal/x-ticks.ts` | `internal/x-axis-overlay.tsx:17` |
| `internal/visx-pattern-bridge.tsx` | `internal/pattern-preset.tsx:2` |

Fold each into its sole importer's file per T-C5's mechanical-consolidation charter, then leave the now-empty source file in place (cannot delete — D216) and list it in your report for the lead to delete.

**One claimed single-importer fold is NOT single-importer — contradiction, do not fold naively:**

`internal/radar-spring.ts` exports exactly `estimateSpringSettleMs` and `sampleSpringProgress`. Both centralize.md ("Evaluated, NOT recommended" section: `"Rename/move `radar-spring` | Subsumed by fold row 13 (single importer)"`) and go-to-plan.md describe this as a single-importer fold. **It is not, as of this drafting**: it is imported by both `radar-chart.tsx:27` and `internal/enter-transition.ts:37`, and `internal/enter-transition.ts` does **not** re-export either function (confirmed via `grep -n "export" internal/enter-transition.ts`). Re-verify this at dispatch time (imports can move). If it is still a 2-importer module when you start:
- either fold `radar-spring.ts` into `enter-transition.ts` and have `enter-transition.ts` re-export `estimateSpringSettleMs`/`sampleSpringProgress` so `radar-chart.tsx:27`'s import keeps working,
- or leave `radar-spring.ts` standalone and only fold the 3 confirmed single-importer modules above.

Do not silently fold it as if the single-importer claim were true — that will break `radar-chart.tsx`'s import.

## Gate

This package touches hover-chrome mapper internals for pie/ring/funnel (via `hover-chrome.ts`), bar, candlestick, scatter, and live-line — gate all of them.

Mandated densities: bar family NEVER n≥1000 (use **100**); markers/liveline **100**; others not listed here at their mandated density (scatter/candlestick/pie/ring/funnel **1000**).

`qa/screenshot.mjs` compares **`bklit` vs `migrated`**. Never probe `impl=tanstack` — it is the performance-ceiling reference and deliberately ports none of bklit's chrome (this caused a full wrong-impl investigation once already, D258).

Known chronics — check `qa/results/<chart>/` history before reporting any failure as new: pie hover-30/hover-70 ~3.18% is **RETIRED** (fixed by P2.4, D254), do not re-report it; markers n=100 hover-30 ~0.5507% is **chronic and still open** — expected, not new.

Since this package is purely internal-module consolidation (no visual-logic change intended), a passing gate at the *same* percentages as your dispatch-time baseline is the goal — any percentage shift is a signal the fold silently changed behavior and needs investigation before you report success.

## Report back

1. Which T-C5 folds you executed, and the sole-importer grep confirming each was actually safe at dispatch time — not just this brief's citations, since the tree may have moved.
2. The `radar-spring.ts` finding: whether it was still 2-importer when you started, and which resolution you chose.
3. T-C1: what the unified mapper kit's exported surface looks like, how you handled candlestick's missing `toDotConfig` and live-line's `LiveHoverConfig` divergence.
4. The `MARKER_DIM_*` constants and the stale bar-hover-chrome.ts comment: whether you found a genuine second site, and what you did.
5. Confirmation this package did not step on T-D8/P3.9 — if P3.9 landed mid-dispatch, say so and report what changed underneath you.
6. Any orphaned files left for lead deletion (D216).
7. Anything contradicting this brief, stated plainly with evidence.
