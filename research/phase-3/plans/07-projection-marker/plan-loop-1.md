# Initiative 7 — ProjectionLine + TerminalMarker (plan-loop-1) — FINAL

Status: FINAL (synthesized 2026-08-19 by the lead from `groundtruth-loop-1.md`, whose claims are file:line-verified; supersedes the thin `research/phase-3/audits/07-projection-marker.md`). Executor channel: cmd-executor primary (standing priority opencode → cmd-executor → Sonnet fallback, D220; the boundary opencode re-probe was skipped at user direction — user moved the session straight to initiative 7 after the last probe attempt; last known state remains `401 CreditsError`, workspace `wrk_01KZ1A33YS0SNYM8NFN2VZK1X5`).

## Rulings (lead; record in the gate D-entry)

1. **ProjectionLineEndMarker — FULL port, not type-only.** Zero demo usage, but bklit exports it publicly and it renders real DOM when used (one edge-clamped `<circle r={radius*0.85}>` at the last projection point, phase-gated). A type-only stub would be a runtime-parity lie for ~25 lines of geometry. Port fully. The bklit inconsistency stands as-is: `ProjectionLine.showEndMarker` affects ONLY `resolveVisibleEndX` clamp padding and never draws a dot — replicate that exactly, no "fix".
2. **Post-overlay placement — dedicated post-overlay SVG sibling.** `LineSeriesTerminalMarker` + `ProjectionLineEndMarker` are `__isPostOverlay` in bklit (outside the reveal clip, after overlay). Migrated equivalent: an absolutely-positioned sibling `<svg>` overlay ABOVE the TanStack svg (`position:absolute; inset:0; pointer-events:none` — the established layering convention, cf. the hover-chrome/axis overlays), NOT inside the hover chrome modules (these markers are phase-driven, not hover-driven). `ProjectionLine` itself is preOverlay INSIDE the reveal clip in bklit → migrated renders it as a TanStack custom mark in the spec's marks group (the `internal/area-fill-mark.ts` createMark precedent, precomputed `M…L`/`M…C` path string), so the hosts' existing `clipPath: inset()` reveal grows it for free — exactly bklit's clip-reveal behavior.
3. **Terminal-marker replay — WAAPI keyed on revealEpoch.** Port bklit's `motion.g key={revealEpoch ?? 0}` remount-replay as a WAAPI animation re-run on `revealEpoch` change (same mechanism as the radar/sankey reveal fixes): opacity 0.55-scale entrance `{opacity:0, scale:0.55} → {opacity:1, scale:1}`, `transform-box: fill-box`, origin at (cx,cy). Timing: caller-supplied chart `enterTransition` object resolved via `internal/enter-transition.ts` if present, else the HARDCODED bklit fallback `{duration: 0.28s, ease: cubic-bezier(0.22,1,0.36,1)}` (deliberately ≠ the 1.1s default — parity). Visibility phases are NARROWER than ProjectionLine: marker visible only in `ready`/`exitingReady` (excludes `revealing`); ProjectionLine's stroke swaps to `"transparent"` (color swap, NOT opacity) outside `revealing`/`ready`/`exitingReady`. `prefers-reduced-motion`: snap to final frame (established migrated WAAPI doctrine, init-6 ruling 4 class; bklit has no reduced-motion code and the snap is gate-invisible).
4. **Domain extension is REAL (contrast init-6 premise correction) — ONE shared merge module.** Port `mergeProjectionYDomain` (0-floor/10%-headroom/5%-padding widening), `mergeProjectionXDomainMax`, `ProjectionLineConfig` + extraction into ONE `internal/projection-config.ts`. Hosts (line/area/composed ONLY) extract configs from children and (a) extend the x-scale `maxTime` memo, (b) merge into the `resolveTimeSeriesYDomain` result BEFORE nicing, (c) fabricate the [0,100]-based domain for a `yAxisId` referenced only by a projection (bklit shell :337-371 behavior). With zero projection children, extraction returns `[]` and every merge is an identity — the hot render path for all existing scenarios is unchanged. `internal/y-domain.ts` itself is NOT modified (it stays the series-only bklit port; the merge composes around it in the hosts).
5. **Brush integration deferred to initiative 9.** x-axis projection tail ticks + brush-track extent exist in bklit but migrated has no brush, and bklit itself documents "Projections and brush zoom are not supported together" (`chart-examples.tsx:3093`). Goes on the deferred register with the init-9 ChartBrush work.
6. **Extraction wiring**: `children.tsx` gains three role carriers — `ProjectionLine` (`"projectionLine"`), `ProjectionLineEndMarker` (`"projectionEndMarker"`), `LineSeriesTerminalMarker` (`"terminalMarker"`) — null-render config carriers like every other role. `extractChildren` collects them (Line/Area consumers); composed's own `extractComposed` gains the same three roles. Because these are distinct roles (not `<Line>`), the terminal marker's `dataKey` never registers as a series — bklit's `LINE_DOMAIN_EXCLUDED_NAMES` exclusion is parity-for-free; note that in the code comment. LiveLine/Bar/Scatter/Candlestick/polar: NO support (bklit parity by omission — their extractors silently drop unknown roles already).
7. **Gradient stroke**: TanStack `SceneStyle` has no defs — per-instance `linearGradient` (`gradientUnits="userSpaceOnUse"`, x1=startX y1=startY x2=visibleEndX y2=endY, 0%/100% stops, `useId()`-derived id sans colons) in the established sibling `<svg width={0} height={0}><defs>` pattern (`area-chart.tsx` ~:518-540). No new gradient helper unless it can live in `internal/fade-mask.ts`-style shared code — inline per bklit is acceptable since bklit itself inlines it per instance.
8. **Public exports (type parity)**: `ProjectionLine` (+ `ProjectionLineProps`), `ProjectionLineEndMarker` (+ props), `LineSeriesTerminalMarker` (+ props typed on `internal/types.ts` `SeriesPointMarkerStyle`), and the projection-utils surface bklit exports: `buildProjectionPath`, `computeProjectionAnchorTangentSlope`, `buildHorizontalTangentBezierPath`, `projectionValueExtents`, `projectionDateExtents`, types `ProjectionMode`/`ProjectionAutoMethod`/`ProjectionCurveKind`/`ProjectionPathDensity` (@deprecated, "stepped" still live)/`ProjectionPoint`. Deprecated aliases (`showEndpoints`) kept with `@deprecated` tags — bklit resolution order `showEndMarker ?? showEndpoints ?? true` verbatim.
9. **Out of scope**: brush/tail ticks (ruling 5); dash-tail (`LineConfig.dashFromIndex` stub is initiative 10); showcase demo-PAGE listing (init 12, per the init-6 deferral); the practically-dead `curve` CurveFactory branch — port the type + the visx-LinePath fallthrough semantics (i.e. ignored unless cast), do NOT invent a d3-curve renderer for it.

## Deliverables (ONE cmd-executor dispatch, ONE diff)

**D1 — `internal/projection-utils.ts`**: 1:1 port of bklit `projection-utils.ts` full export surface (ruling 8 list): `buildProjectionPath` (manual/target/auto modes; `autoMethod` linearRegression|lastSegment; `pathDensity` endpoints|stepped; `intervalMs` resolution chain adjacent-delta → span-average → 86_400_000), `computeProjectionAnchorTangentSlope`, `buildHorizontalTangentBezierPath` (`t = clamp(tension,0.05,0.5)` default 0.45, horizontal tangents, |dx|<1e-6 degeneracy), extents helpers, all types.

**D2 — `internal/projection-config.ts`**: `ProjectionLineConfig {yAxisId, data}`; `extractProjectionLineConfigs(children)` — role-based walk (Fragment unwrap) matching `CHART_ROLE === "projectionLine"`, date normalization, `data.length ≥ 2` filter; `mergeProjectionYDomain`; `mergeProjectionXDomainMax`; `resolveVisibleEndX(endX, innerWidth, endpointRadius, strokeWidth, showEndMarker)` (edgePadding = endpointRadius·[showEndMarker] + strokeWidth/2 + 1).

**D3 — public components in `children.tsx` + mark/overlay renderers**: three role carriers (ruling 6); ProjectionLine geometry/DOM as a TanStack custom mark module `internal/projection-line-mark.ts` (area-fill-mark precedent): linear `M…L` / bezier via D1, `strokeLinecap:"round"`, dash default `"6,4"`, defaults `stroke var(--chart-3)`, gradientEnd `var(--chart-5)`, strokeWidth 2, opacity 1, `<2` points → no mark; gradient defs per ruling 7; phase color-swap per ruling 3. Marker overlay module `internal/terminal-marker.tsx` (or .ts + host wiring, executor's structural call within the single-source rule): last-DATA-point anchor (`dataKey` last row must be number else nothing), `StaticSeriesPointMarker`/`MarkerCircles` port (fill circle r=radius + ring r=radius+ringGap+strokeWidth/2, defaults fill transparent / stroke var(--chart-1) / radius 5 / ringGap 0 / strokeWidth 1.5 = hollow ring), WAAPI entrance per ruling 3; `ProjectionLineEndMarker` (ruling 1) in the same post-overlay svg.

**D4 — host threading (line/area/composed ONLY)**: extraction wiring (ruling 6), x/y domain merge (ruling 4), mark insertion inside the reveal clip, post-overlay svg sibling for the markers (ruling 2), revealEpoch plumb to the marker WAAPI.

**D5 — showcase demos**: mirror `projection-line-demo.tsx` (2 demos: target-mode bezier gradient `strokeDasharray="1,4"` + terminal marker `ringGap={6}`; auto-mode `lastSegment` + `showEndMarker={false}` + default ringGap) in `showcase/components/demos/` (executor-permitted path), data via public `buildProjectionPath` exactly as the bklit docs do.

**D6 — exports**: ruling 8 surface through `migrated/charts/index.ts`; no other new public exports.

## Executor constraints (verbatim into the task file)
Standard boundary set: may NOT touch qa/, bench/, docs/, research/, repos/, showcase/repos/, .claude/, .opencode/, /tmp; git read-only (no commit/stash/checkout/restore/reset/clean); no file deletion; report must quote changed lines per file + verbatim `check-types`/`lint` tails with exit codes + `git diff --stat`; deviations explicitly flagged; STOP and report if out-of-scope work appears.

## Gates (1.5, in order)
1. `pnpm --dir showcase check-types` + `pnpm --dir showcase lint` — exit 0.
2. Q3 greps: no per-chart forks of projection geometry/merge (ONE projection-utils, ONE projection-config, ONE mark module, ONE marker module consumed by all three hosts); no motion/react; no ms/easing literals outside `internal/` (0.28s/[0.22,1,0.36,1] live in ONE internal site); no deep `@tanstack/*/src` imports; no rendered-DOM reach-ins.
3. Q1 FULL sweep unchanged at frozen baselines (all new components default-off / absent from existing scenarios; any drift = defect): line 0.0000/≤0.0005; area 0.0002/≤0.0017; bar 0.0000; scatter 0.1230; candlestick 0.0354/0.2345/0.2341/0.2325 EXACT; composed settled 0.27–0.42 (D218 band)/≤0.0041; liveline ~0.019; heatmap 0.4277; pie 0.0000; refarea 0.0017/≤0.0009; segment 0.0000/≤0.0008; loading line ≤0.1345/area ≤0.0490/heatmap ≤0.1948 **at n=52** (D220 harness note). Known waivers: heatmap hovers (D208-5), pie hover-30 (D208-2). Run ids on EVERY line (D215 countermeasure).
4. NEW scenario Q1 (lead adds bench scenario pair post-implementation — executor cannot touch bench/): bklit vs migrated line + LineSeriesTerminalMarker + two ProjectionLines with the docs-demo props (target bezier gradient + auto lastSegment) ≤0.5% settled + hovers; lead PNG eyeball of dash/gradient/marker/edge-clamp; reveal replay eyeballed via the deferred-reveal path.
5. Q2 `qa/console-errors.mjs` zero errors ×13 + the new projection scenario checked manually.
6. G1–G4: identity-merge + default-off is expected render-path-inert; lead reviews the diff — the x/y-domain memo edits in three hosts are the risk surface. If inert: D210-class waiver + same-day spot-check (line + composed, m1a/m3a/m3c/heap vs D218/D220 values). Any in-scope regression beyond documented session drift → full 16-run sweep (init-5 protocol).
