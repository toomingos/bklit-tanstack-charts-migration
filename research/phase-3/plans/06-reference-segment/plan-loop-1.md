# Initiative 6 — ReferenceArea + Segment + selection state (plan-loop-1) — FINAL

Status: FINAL (synthesized 2026-08-19 from an Explore ground-truth survey of bklit `reference-area*`/`segment.tsx`/registration context + migrated internals + TanStack mark primitives; supersedes the thin `research/phase-3/audits/06-reference-segment.md`). Executor: cmd-executor fallback (opencode re-probed at this boundary: identical `401 CreditsError`, workspace `wrk_01KZ1A33YS0SNYM8NFN2VZK1X5`; cmd channel probe returned PONG post-15:16 reset).

## PLAN-PREMISE CORRECTION (ruling 1, D210-D4 class)

The initiative title's "y-domain registration" is a misnomer. Verified in bklit source: `ReferenceAreaRegistrationContext` registration feeds **Y-axis tick-label coloring ONLY** (`y-axis.tsx:48-75` `resolveTickLabelColor` via `resolveReferenceDataRange`). bklit's y-domain computation (`y-domain-utils.ts:77-97` `computeYDomainsByAxis`) is series-data-only and **never reads `referenceAreas`** — the context field's own doc comment scopes it to "drives y-axis label colors in range" (`chart-context.tsx:150`). Migrated `internal/y-domain.ts` is already a faithful port of the same series-only logic → **parity by omission; DO NOT add a y-domain extension point.**

## Ground truth (bklit, verified file:line)

### ReferenceArea — `repos/bklit-ui/packages/ui/src/charts/reference-area.tsx` (PUBLIC)
- Props (`:18-62`): `y1?/y2?: number`, `x1?/x2?: Date|number`, `yAxisId?` (default `"left"`), `fill?/fillOpacity?`, `pattern?: PatternPresetId` + 8 pattern options (`patternColor/Scale/StrokeWidth/Radius/Complement/Fill/DotFill/TileBackground`), `stroke?/strokeWidth?`, `strokeStyle?: "solid"|"dashed"` (+ `strokeDasharray?` default `"4,4"`), `fadeEdges?` (default TRUE) + `fadeEdgesLength?` (default 10, clamped 0–45), `axisLabelColor?`, `showMarkers?` (default false) + `markerColor?` (default `"var(--chart-1)"`) + `markerSize?` (default 6), `ifOverflow?: "hidden"|"visible"|"discard"` (default `"hidden"`), `className?`.
- SVG structure (`:200-280`): one fading `<g aria-hidden>` containing: optional `<defs>` linearGradient+mask pair for horizontal edge fade; optional pattern `<defs>` via `renderPatternPreset`; ONE `<rect>` (pattern URL or solid fill, edge-fade-masked); ONE `<g mask>` with TWO `<line>`s (top/bottom edges, dashed/solid); optional two bracket-marker `<path>`s at the band's horizontal center (top points down, bottom points up).
- Geometry — `reference-area-geometry.ts`: `computeReferenceAreaRect` (`:79-123`) with omitted-bound → plot-edge fallbacks (`x1→0, x2→innerWidth, y1/y2→plot top/bottom`); `ifOverflow` semantics: `"visible"` unclamped, `"discard"` → `null` unless `isFullyInsidePlot`, `"hidden"` → `clampRectToPlot` (null when clipped rect collapses); `resolveReferenceDataRange(y1,y2,domain)` (`:126-136`) for the axis-label consumer.
- Enter/exit: `motion.g` opacity 0→1 gated on `isReferenceAreaVisiblePhase(chartPhase)`, transition = chart `enterTransition` else `{duration: 420ms (ENTER_FADE_MS), ease: "easeOut"}`.
- Registration (`reference-area.tsx:119-130`): `useLayoutEffect` → `registerReferenceArea(uniqueId, { yAxisId: normalizeYAxisId(yAxisId), y1, y2, axisLabelColor })`, unregister on cleanup. Context: `reference-area-registration-context.tsx`; config type `reference-area-config.ts:9-14` (`ReferenceAreaConfig { yAxisId, y1?, y2?, axisLabelColor? }`); static JSX scan `extractReferenceAreaConfigs(children)`.
- Threading: Line/Area/Composed via `TimeSeriesChartInner` — provides the registration context AND merges `extractReferenceAreaConfigs(children)` with registered entries (`time-series-chart-shell.tsx:456-507`, `646-696`). Bar (`bar-chart.tsx:578-580`, categorical `fakeTimeScale`), LiveLine (`live-line-chart.tsx:556-598` → drives LiveYAxis label colors), Candlestick (`candlestick-chart.tsx:267-292`), Scatter (`scatter-chart-shell.tsx:181-233`): **static child scan only** — no provider, dynamic registration is a deliberate no-op there.
- Sole registration consumer: `y-axis.tsx:48-75` `resolveTickLabelColor` — recolors ticks whose y falls inside a band's data range with `axisLabelColor`.

### Segment — `repos/bklit-ui/packages/ui/src/charts/segment.tsx` (PUBLIC: barrel `index.ts:514-521`)
(Distinct from `highlight-segment.tsx` — that hover-highlight band is NOT this initiative.)
- `useSegmentVisibility` (`:7-13`): visible iff `selection?.active === true && Math.abs(selection.endX - selection.startX) > 5` — the 5px activation threshold.
- `SegmentBackground { fill? }` (default `var(--chart-segment-background)`): AnimatePresence `motion.rect`, full `innerHeight`, `x=min(startX,endX)`, `width=|endX-startX|`, opacity fade 0.15s.
- `SegmentLineFrom` / `SegmentLineTo` `{ stroke? (default var(--chart-segment-line)), strokeWidth? (default 1), variant?: "dashed"|"solid"|"gradient" (default "dashed") }`: full-height line at `selection.startX`/`endX`; `"gradient"` = per-instance linearGradient (0/10/90/100% opacity stops); `"dashed"` = `4,4`. No built-in motion on the lines (snap to pointer).
- Selection source: `ChartSelection { startX, endX, startIndex, endIndex, active }` (`use-chart-interaction.ts:13-19`), populated by mouse-drag + 2-touch handlers inside `useChartInteraction`; **mouseup ALWAYS `setSelection(null)`** (selection is transient, per init-5 plan ground truth). Hosts exposing `selection` in context: TimeSeriesChartInner (line/area/composed) + CandlestickChart. Bar/Scatter/LiveLine/polar: no selection state — Segment children silently no-op there (bklit behavior; preserve).

### Required API surface (bklit docs app = the demo spec)
- `reference-area-demo.tsx`: band (`showMarkers strokeStyle="dashed" y1 y2`), pattern (`axisLabelColor pattern patternColor patternScale patternStrokeWidth showMarkers stroke y1 y2`), markers (`markerColor showMarkers stroke strokeStyle="dashed" y1 y2`) — all on LineChart.
- `segment-demo.tsx`: `<SegmentBackground/><SegmentLineFrom/><SegmentLineTo/>` after `<Line>`, plus a headless child reading `selection.{active,startIndex,endIndex}` to drive external stats.
- `chart-examples.tsx`: ReferenceArea variants on Area/Bar/Composed/Line; Segment selection on Area (`:1735-1753`) + Line (`:3138-3155`); LiveLine reference band (`:3627-3635`: `<ReferenceArea y1={138} y2={148} strokeStyle="dashed" showMarkers/>` among live children).

## Migrated today (verified)
- ReferenceArea/Segment/selection: **absent entirely** — zero matches in migrated/showcase; `children.tsx:146-158` role vocabulary has no `referenceArea`/`segment`; `extractLiveLineChildren` (`live-line-chart.tsx:292-316`) silently DROPS a `<ReferenceArea>` child (the audit's "LiveLine child extraction" gap).
- Already-landed leaf modules from initiative 3 that THIS initiative must consume (single-source gate — no forks): `internal/pattern-preset.tsx` (`PATTERN_PRESET_IDS`, `PatternPresetId`, `renderPatternPreset`, tile helpers) + `internal/visx-pattern-bridge.tsx`; `internal/fade-mask.ts` (`clampFadeLength` semantics via `resolveFadeEdgesMask`/`fadeGradientStops`); `internal/background.tsx` = the accepted behind-the-chart SVG layer convention (absolutely-positioned sibling BELOW the TanStack svg, scale-synced) — the band must render UNDER the series exactly as bklit does.
- Axis labels: HTML overlay divs (`internal/y-axis-overlay.tsx`, per-tick positioning) — the `axisLabelColor` consumer belongs there (color override per tick), plus the liveline y-axis path in `live-hover-chrome.ts`/live axis overlay.
- TanStack natives: `rect` (`rect.ts:78-94`), `ruleX/ruleY` (`rule.ts:12-28`), `link` (`link.ts:22-65`), `ChartSpec.clip`/`SceneGroup.clip` (`types.ts:336,569`) — raw geometry only; NO pattern fills, edge-fade masks, bracket markers, ifOverflow semantics, omitted-bound fallbacks, or drag selection. A compatibility layer is required (audit confirmed).

## Rulings (lead; record in the gate D-entry)
1. **Premise correction** (top of file): registration = axis-label color only; NO y-domain extension point.
2. **Selection scope**: initiative 6 ports ONLY the `ChartSelection` drag sub-machine (mouse drag + 2-touch + transient mouseup-clears semantics + the 5px Segment visibility threshold) as shared internal code consumed by Segment on line/area/composed/candlestick hosts. The full `useChartInteraction` public hook and brush/zoom-commit machinery remain initiative 9 (ChartBrush) per the init-5 ruling 1 — initiative 9 REUSES this module.
3. **Single-source**: pattern fills MUST go through `internal/pattern-preset.tsx`; edge fades MUST go through `internal/fade-mask.ts`; any new gradient/mask helper needed lands in fade-mask.ts, not inline. New geometry module is the only new geometry source.
4. **Enter fade**: bklit `motion.g` opacity port = WAAPI/CSS transition (420ms ease-out, phase-gated visibility, `prefers-reduced-motion` snap) — no motion/react dependency (established doctrine). SegmentBackground's 0.15s AnimatePresence fade likewise CSS/WAAPI.
5. **Rendering layer**: ReferenceArea renders in a behind-the-chart SVG layer following `internal/background.tsx`'s layering convention (band sits UNDER series, matching bklit SVG order); Segment visuals render in the interactive overlay layer (they sit over the plot like bklit's in-SVG order after series). No reach-ins into TanStack's rendered SVG.
6. **Public exports** (type parity): `ReferenceArea` (+ `ReferenceAreaProps`, `ReferenceAreaStrokeStyle`, `ReferenceAreaIfOverflow`), `SegmentBackground`, `SegmentLineFrom`, `SegmentLineTo` (+ prop types, `SegmentLineVariant`) — mirrors bklit barrel `index.ts:514-521`. `ChartSelection` type export deferred to initiative 9 with the hook.
7. **Out of scope**: `highlight-segment.tsx` (separate mechanism, initiative 10 family); polar/hierarchical charts (bklit unsupported there); y-domain changes of ANY kind.
8. **QA approach**: all new components are default-off → the standard Q1 sweep must stay at frozen baselines untouched. Visual 1:1 of the band itself is gated on a NEW bench scenario pair (bklit vs migrated line + ReferenceArea with the docs-demo pattern/markers props) added by the LEAD post-implementation (executor cannot touch `bench/`); Segment drag behavior is gated by a lead-run interactive probe (drag mid-chart → background+lines appear past 5px, vanish on mouseup) + Q2 zero errors.

## Deliverables (one executor dispatch, one diff)

**D1 — `internal/reference-area-geometry.ts`**: 1:1 port of bklit `reference-area-geometry.ts` — `ReferenceAreaIfOverflow`, `computeReferenceAreaRect` (omitted-bound plot-edge fallbacks, all three ifOverflow modes, null-collapse), `isFullyInsidePlot`, `clampRectToPlot`, `resolveReferenceDataRange`.

**D2 — `internal/reference-area-config.ts` + registration**: `ReferenceAreaConfig`, `extractReferenceAreaConfigs` (static JSX scan matching bklit's), registration context (register/unregister, layout-effect timing) — provider mounted ONLY by line/area/composed (bklit parity); bar/scatter/candlestick/liveline use the static scan only.

**D3 — `reference-area.tsx` public component + threading**: full bklit props; renders rect + edge lines + bracket markers + pattern defs + edge-fade mask in the background SVG layer (ruling 5), consuming pattern-preset/fade-mask leaf modules (ruling 3); 420ms phase-gated enter fade (ruling 4). Thread through: line/area/composed (context + scan), bar (categorical x mapping like bklit's fakeTimeScale contract), scatter, candlestick, liveline (`extractLiveLineChildren` gains the role — the child must stop being dropped). `axisLabelColor` consumer: per-tick color override in `y-axis-overlay.tsx` (+ liveline y-axis path) via `resolveReferenceDataRange`.

**D4 — `internal/chart-selection.ts` + `segment.tsx` public components**: ChartSelection state machine port (mouse drag + 2-touch, indices via the charts' existing bisector, transient mouseup-clear) exposed to chart hosts line/area/composed/candlestick; `useSegmentVisibility` 5px threshold; `SegmentBackground`/`SegmentLineFrom`/`SegmentLineTo` with variants (dashed 4,4 / solid / gradient 0-10-90-100 stops), defaults `var(--chart-segment-background)`/`var(--chart-segment-line)`. Charts without selection: children silently no-op (bklit parity).

**D5 — showcase demos**: mirror the bklit docs surface — ReferenceArea band/pattern/markers variants (line), ReferenceArea on area/bar/composed, Segment selection on line + area, LiveLine reference band — in `showcase/components/demos/` (executor-permitted path).

**D6 — exports/types**: ruling 6 exports; props 1:1 with bklit (type-parity gate); no other new public exports.

## Gates (1.5, in order)
1. `pnpm --dir showcase check-types` + `pnpm --dir showcase lint` — exit 0.
2. Q3 greps: no pattern/fade forks (all fills via pattern-preset, all fades via fade-mask); no ms/easing literals outside `internal/` (420ms/150ms live in one internal constants site); no deep `@tanstack/*/src` imports; no rendered-DOM reach-ins into the TanStack svg.
3. Q1 FULL sweep unchanged at frozen baselines (components default-off; any drift = defect): line 0.0000/≤0.0005; area 0.0002/≤0.0017; bar 0.0000/≤0.0017; scatter 0.1230/0.0000; candlestick 0.0354/0.2345/0.2341/0.2325; composed settled 0.27–0.42 (D218 band)/≤0.0041; liveline 0.017–0.046/≤0.15; heatmap 0.4277; pie 0.0000; loading line ≤0.1345/area ≤0.0490/heatmap ≤0.1948. Known waived: heatmap hovers (D208-5), pie hover-30 (D208-2). Run ids on EVERY line (D215 countermeasure).
4. NEW scenario Q1 (lead adds bench scenarios post-implementation): bklit vs migrated line+ReferenceArea (docs-demo props incl. pattern + markers + axisLabelColor) ≤0.5%; lead eyeball of the band PNGs.
5. Q2 `qa/console-errors.mjs` zero errors (D208-8 warnings excluded) + lead segment drag probe.
6. G1–G4: default-off components + context threading are expected render-path-inert for existing scenarios; lead reviews the diff — if any hot render path materially changed, full 16-run sweep (init-5 protocol); else D210-class bench waiver recorded in the D-entry.
