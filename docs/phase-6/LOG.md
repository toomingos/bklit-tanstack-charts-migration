# Phase 6 — Decision log

Numbering continues from Phase 5 (last: D415).

| # | Decision | Context |
|---|---|---|
| D416 | **Baseline inherited, not re-run.** Phase 6 references `docs/phase-5/BASELINE.md`, the final QA matrix `docs/phase-5/captures/5-3-5-final-matrix.md`, and `bench/results/latest.json` as-is. No measurement happens until 6.5 (single end gate). | PLAN-phase-6.md "Baseline: inherited". Saves the multi-hour bench/QA cycle that per-batch gating cost in phase 5. |
| D417 | **Standing rule: every "library can't do X" ruling carries a version stamp and expires at the next pin bump unless re-verified.** All phase-5 dead rulings were true at v0.14 and false at 0.15. Applies retroactively: `brush-drag.ts:1-36` NON-VIABLE (2026-08-24, v0.14) is hereby obsoleted at 0.15 — its three arguments are each answered (strip host, `clientToScene`, two-host overview+detail = conformance case 83). | PLAN-phase-6.md §How to Work; research/phase-6/06. |
| D418 | **Census scope ruling.** Reach-in = JS/TS code addressing renderer-owned DOM (queries, `ts-chart__`/`data-ts-key` literals, mutations on renderer nodes incl. elementMap-mediated ones). Baseline = 259 sites (research/phase-6/08). CSS in `styles.css` targeting `ts-chart__*` is a sanctioned styling surface (themes-and-styling.md) and excluded; authored custom marks stop aliasing `ts-chart__*` names (renamed `bkm-chart__*` in C5) so the CI grep guard can be a clean zero over `*.ts,*.tsx`. Named exceptions (D420, conditional choropleth fallback) are grep-guard-excluded by exact path. | research/phase-6/08 census. |
| D419 | **S7 folded into commit C1.** Legend coupling and mark states are one mechanism (focus-driven styling); S1's deletions remove the exact functions legend effects call, so a separate S7 commit cannot typecheck. Ladder is 6 commits. | research/phase-6/go-to-plan.md collision check. |
| D421 | **Scatter inactive 2px blur dropped (ACCEPT-WITH-LOG, v0.15.0).** Mark states have no `filter` channel and the renderer stamps no focus classes, so pure-CSS scoping of "inactive only" is impossible without a new reach-in. Inactive dim (0.5/150ms) + active r×1.35 are native states; blur is an honest omission, marked `// D421` at both mark sites. Upstream ask: `filter` state channel. (Logged at C1; D420 wipe stays reserved for C5.) | research/phase-6/01; types.d.ts:73-89. |
| D424 | **Polar/geo channel limits — C1 fidelity gaps accepted (v0.15.0).** Polar marks (`radialArea`/`radialDot`/`radialArc`) and `geoShape` have no `states` and only per-datum `fill`/`stroke`/`r`; dim is baked into color alpha (`color-mix`) via reactive definitions. Dropped rather than re-introduced as reach-ins: radar stroke-width hover pop + dot stroke-ring dim + glow/scale flourish (revisit at C5 if radar splits to per-series marks); sankey per-node dim stagger (flattened to the single 0.18s CSS term); choropleth pattern-filled features no longer dim (`url(#…)` paints can't alpha-blend) and hovered feature is no longer forced paint-order-last. D423's hover-persistence quirk SURVIVED (no deviation needed). Live-line has no focus points at all (mark emits none) — its self-owned overlay dim stays until C5's rolling-path work. | C1 executor reports; polar.d.ts, geo.d.ts. |
| D425 | **Verified-value corrections + CSS-transition surface (C1).** Pie fade transition is 0.15s (research brief said 0.4s; legacy source verified) — verified value encoded. Dim/fade transitions that reactive definitions can't declare (SceneStyle has no `transition`) live in styles.css as sanctioned `ts-chart__*` rules (area fill-opacity 400ms, pie fill 0.15s, choropleth fill/stroke 0.18s, sunburst fill 160ms — repointed from `opacity`, which the reactive fill dim no longer writes); no inline `style.transition` writes on renderer nodes remain in C1 scope. Composed-chart legend dim also fixes a latent index-space bug (bars indexed against `resolvedBars` while lines/areas used `composedSeries`). | C1 executor reports; D418 CSS surface ruling. |
| D426 | **Tooltip subsystem native (C2, v0.15.0).** All panel rendering/positioning moved to the native `tooltip` extension + `renderTooltipBody` (React entry `@tanstack/react-charts/tooltip`). Executor-verified spec corrections: pie and ring never had bespoke tooltip panels (research/02 overcounted — both stay `tooltip: false`, untouched); sankey needed node ChartPoint emission added to `sankey-mark.ts` (documented `MarkScene.points` mechanism) and `pointer: false` instead of `focusDisabled` (native pointermove otherwise clears controlled focus every frame — dist/renderer.js:451); charts with app-owned hit-testing (composed/sankey/heatmap/choropleth) bridge via `setControlledFocus(..., {source:'pointer'})` — never `'programmatic'`, which would trip C1's legend-dim states; composed resolves via `interaction.resolvePointer`. Heatmap's 120ms hide delay is a debounced focus clear; choropleth zoom re-anchors by re-injecting the same point (`sameChartPointIdentity` fast path forces a fresh `anchor()`). Positioning is approximate, not byte-identical: native `plot-top`/`point` anchors + `placement:['right','left']` + offset 16 replace legacy center-then-clamp `positionBox` math (bar `group-center`, candle `value`). Native host chrome neutralized by one styles.css rule (`.bkm-native-tooltip`, `--ts-chart-tooltip-*` var overrides — sanctioned surface); panel chrome stays `.bkm-tooltip-panel` (TooltipContent's own markup, or an explicit wrapper for sankey whose old inline panel styles were byte-equivalent to that rule). `motion: false` past DISCRETE_INTERACTION_THRESHOLD=60 (strict `>`, legacy-verified), else TOOLTIP_BOX_SPRING {100,20}; `sticky: false` everywhere. Transitions snap until C5's renderer switch (expected). | C2 executor reports; research/phase-6/02; dist/tooltip.js, dist/renderer.js. |
| D427 | **C2 residuals accepted.** (1) Live-line keeps its bespoke box: `live-line-mark` emits no ChartPoints (deliberate — D16/D22 rAF pointer loop bypasses the focus system), so the native tooltip has nothing to anchor to; the box was decoupled from `tooltip-chrome.ts` by inlining its primitives (revisit at C5's rolling-path work — noted in the file header). During that port a real bug was fixed: the box-fade `Animation` ref was recreated per call, so `.cancel()` never cancelled and overlapping fades could stack. (2) Interim helper duplication accepted: scatter- and live-hover-chrome carry inlined copies of the indicator/dot/pill/label-fade builders while `tooltip-chrome.ts` (689→384 LOC after the box half + rAF scheduler were deleted) keeps the originals for bar/candle/shared chromes — every copy dies in C3 (shared/bar/candle/scatter) or C5 (live). (3) `SankeyTooltipContentProps` removed from `index.ts`: migrated-native additive type (not bklit parity) describing the deleted panel's internals; zero consumers repo-wide. (4) Heatmap's `instant` prop kept in the public type as a deprecated no-op; `focusRing: false` newly required there (controlled focus is now actually injected). | C2 executor reports; orchestrator consolidation. |
| D428 | **Hover-geometry subsystem native (C3, v0.15.0).** Indicator/crosshair → native `crosshair()` mark (marks-array citizen, `y:false`; `strokeOpacity` forced to 1 — the native default 0.35 would silently regress the opaque legacy indicator) with the vertical fade gradient as an app-owned `<defs>` `linearGradient` referenced cross-SVG via `url(#id)` (document-global ids; same mechanism as projection gradients). Hover dots → `whenFocused`-wrapped marks: line/area/composed `dot()` `{match:'x', retarget:true}` (retarget keeps one scene node keyed stable so its motion spring slides instead of remounting); scatter/candle custom marks `{match:'group', retarget:true}` — `crosshair()`'s own `marker` was rejected because it draws only the primary point, legacy draws one dot per series; per-point `dotColor` function-forms preserved by precomputing into mark `style.fill`. Series/bar pointer-hover dim → mark `states` (`pointerHoverDimState`/`pointerRowDimState` in new `internal/hover-geometry.ts`) with time terms in sanctioned styles.css rules (0.4s line/area strokes, 0.12s composed bars — D425 pattern); marker active ×1.35 + dim moved in-mark into `series-marker-mark.ts`. Highlight band → second lineY slice `[i-1, i+1]` per series. Heatmap: pointer hit-test → `interaction.clientToScene` (returns margin-inclusive scene coords — verified dist/svg-coordinates.js; composed's d3 scales are already scene-space so it uses the value raw, heatmap's are plot-local so it subtracts margin); cell highlight/dim → native rect `states` using `inset` as absolute target (dist/mark-state.js applies the delta symmetrically about center — exact centered-scale emulation), with cells bucketed into one `cell()` mark per distinct row/level fillOpacity so hover styling is engine-driven and the definition never rebuilds per hovered cell. Date pill + odometer + interim label fade → app-owned `internal/date-pill.ts` (SANCTIONED-EXTENSION, fed by onFocusGroupChange; label fade goes native in C4 with the axis overlays). Deleted: hover-chrome.ts (574 LOC, 18 querySelectors), use-hover-chrome.ts (143), scatter-hover-chrome.ts (547), bar-hover-chrome.ts, candlestick-hover-chrome.ts, tooltip-chrome.ts (config types + indicator-width resolvers relocated to tooltip-mappers.ts). Net −472 LOC. | C3 executor reports; dist/crosshair.d.ts, dist/crosshair-resolver.js:264-280, dist/mark-state.js, dist/svg-coordinates.js, dist/dom-types.d.ts:33. |
| D429 | **C3 residuals + accepted regressions (v0.15.0).** (1) Candlestick `indicatorColor` FUNCTION form dropped — the only chart that invoked it per hover-frame; native crosshair `x.stroke` is one static paint string per mark instance, not a per-frame channel. String form fully preserved. (2) Heatmap hover transition easing approximated: 220ms duration exact, easing `ease-in-out` instead of `cubic-bezier(0.4,0,0.2,1)` — `ChartMotionTweenTransition.easing` accepts only named keywords or JS functions; consistent with every other migrated states transition. (3) `clientToScene` returning null now clears hover where the legacy missing-svg guard silently kept stale hover state (strict improvement). (4) Sankey hover stays element-listener-based until C5: `populateNode/LinkElements` serves both hover and the reveal machinery, so the element handles die once, at C5's in-mark dash-sweep — deleting only the hover half now would save nothing. (5) `chart-focus-kit.ts` and `broadcast-store.ts` SURVIVE unchanged — orchestrator audit found research/03's "shrink to cross-chart hover-sync" line mislabeled them: the former is pure focus-strategy math, the latter reach-in-free app-state coordination (pie/heatmap chart↔legend↔tooltip). (6) Composed hover dots/highlight band keep the legacy unprojected-y limitation (raw `d[dataKey]`, no `projectValue` term) — byte-parity with old hover-chrome. (7) Scatter `tooltip.rows`-based dot color now evaluated at mark-build time instead of per-hover-frame (identical output, cheaper). (8) Authored-mark class aliases `ts-chart__hover-dot`/`ts-chart__candle-highlight` join the C5 `bkm-chart__*` rename (D418). | C3 executor reports; orchestrator consolidation audit. |
| D430 | **Axis subsystem native (C4, v0.15.0).** HTML axis overlays deleted (x-axis-overlay.tsx 536 LOC, y-axis-overlay.tsx 133, bar-x-axis-overlay.tsx 67); tick-selection math extracted verbatim to pure `internal/axis-ticks.ts` (`buildXAxisTickValues`, `selectEvenlySpacedIndices`, `buildYAxisTickValues`, `formatYAxisTick`, `selectBarLabelIndices`, `tickLabelFadeOpacity`). Routes: line/area/composed/candlestick/scatter x = custom `ChartScale.resolve` resolvers emit their OWN `ticks: [{value, position, label}]` from `buildXAxisTickValues` using the resolver's live d3 scale (exact pixel parity — the emitted array IS the native tick list); bar x = `axis.ticks.values` at band centers (`map(v)=scale(v)+bandwidth/2`); y = `ticks.values` injection of `scaleLinear().nice().ticks(clamped)` — identical to legacy since `yDomainFinal` is niced before both paths and `.nice()` is idempotent. Label color via per-spec `theme: { muted: "var(--color-chart-label, var(--chart-label))" }` (theme.muted is consumed ONLY by tick labels — scene.js:1154/1169). Placement: `ticks:{size:0,padding:0}` + `line:false`; x labels `dy: margin.bottom - 25`; y labels `dx: ±8`, fontSize 12; y-right via native `side:'right'` scale option (agent-verified — no deviation needed). Proximity fade = React state (`labelFade`, set by guarded setState in focus handlers) closed over by a per-tick `tickLabels.opacity` callback; math in pure `tickLabelFadeOpacity` (TICKER_HALF_WIDTH=50, FADE_BUFFER=20). Tick-label keys are stable across definition rebuilds (`${guide.id}-tick-label:${valueKey(value)}`) so one sanctioned styles.css rule (`.ts-chart__axes text { transition: opacity 0.4s ease-in-out }`) gives the fade its ramp. API traps encoded: `count`/`spacing`/`values` mutually exclusive (TypeError); undefined `tickLabels.opacity` defaults to fillOpacity 0.68 (always pass opacity); native thinning on by default (`thin:false` wherever labels render). The P3.3/T-D2 blanket `display:none` on `.ts-chart__axes` removed — non-C4 charts all suppress axes structurally (`guides:false` / per-scale `guide:false` / live-line `axis:false`), audited. | C4 executor reports; dist scene.js, configured-scale.js, axis dist types; research/phase-6/04. |
| D431 | **C4 residuals accepted (v0.15.0).** (1) AX7 per-tick y-label color (ReferenceArea `axisLabelColor` via `createTickColorResolver`) dropped — tick-label fill is unconditionally `theme.muted`, no per-tick fill channel; `// D431` at former sites; resolver stays in reference-area-geometry.ts unused-by-charts. (2) Grid follows label ticks: y `values` injection also drives horizontal grid lines (identical output for default configs — same d3 ticks of the same niced domain); vertical grid (default off) follows x label ticks when enabled. (3) AX5 500ms axis-label position tween (cubic-bezier(0.85,0,0.15,1)) lost — SVG `<text>` x/y aren't CSS-transitionable; returns via `tickLabels.motion` at C5's renderer/motion switch; `AXIS_POSITION_TWEEN_MS` token removed from design-tokens.ts with a pointer comment. (4) x-label `dy` placement derived, not byte-measured (legacy baseline ≈ containerHeight−15.4 vs native chartBottom+9.6 → `dy: margin.bottom - 25`); pixel gate at 6.5. (5) Scatter pill hoveredLabel stays shortDateFmt-only — byte-parity with legacy's own formatter mismatch; candlestick's equivalent mismatch WAS fixed (hovered-label now `xAxis?.formatValue ?? shortDateFmt.format`) because there the mismatch broke fade matching. (6) Fade rebuilds the definition per focus step (labelFade in the memo deps) — research/04-accepted; 6.5 bench verifies. (7) Survivors research/04 mislabeled deletable: `categoryScaleForOverlay` (live consumers: bandPosFn, hover-dot mark, depth/trim/pulse bandScale, focus band-anchor) and `internal/y-axis-ticks.ts` (publicly exported, bklit parity — now feeds axis-ticks.ts). (8) date-pill.ts/hover-geometry.ts stripped of applyFade/resetFade surface (pill is position+ticker only). | C4 executor reports; orchestrator consolidation audit. |
| D434 | **D401/D388 corrected (T20 renderer switch is NOW UNBLOCKED).** Original claim: `@tanstack/react-charts` "exports `RendererChart` aliased as `Chart`" and `<RendererChart>` appears nowhere, so adopting the motion renderer is a subpath migration. Correct fact: that aliasing is true of the `/core` subpath only (`react-charts/dist/core.d.ts:1`). The subpath the migration actually imports, `@tanstack/react-charts/tooltip`, exports `Chart` (static), `RendererChart` and `CanvasChart` as three distinct symbols (`react-charts/dist/tooltip.d.ts`), and `renderer` is a **required** prop (`react-charts/dist/RendererChart.d.ts:4`). Consequence: the switch is a symbol swap inside the existing import (~2 lines/chart) and tsc enumerates every site; it is the dependency for T6, T8, T21b-timing and T15. Live risk retained from D401: `dist/motion.js:612` animates every non-resize reconcile — mitigate with per-mark `motion:false`, per-chart revert if a T0/T1 chart tweens what legacy paints instantly. | research/phase-6/09-reopened-phase-5.md §2.1, §5. |
| D435 | **D384 corrected (T11 polar focus is NOW UNBLOCKED for pie/ring/sunburst).** Original claim: hover pop/growth is applied imperatively to the DOM and never to the scene, so "painted arcs are not the scene's arcs" (citing D254/D258) and native focus would hit-test stale geometry. Correct fact: the D254/D258 static hitbox twin already exists **as a scene mark** — `showcase/migrated/charts/pie-chart.tsx:441-460` declares a second `radialArc` (`id:"pie-hitbox"`, transparent, `radiusRatio:1`) alongside the visible slice inside one `polar()`; only its *consumption* is still a querySelector map (`:661-668`). Native containment hit-tests it and never the animated slice: `dist/nearest.js:47` runs `findContainingScenePoint` before any distance work, `:63-80` iterates targets in reverse gated on `containsBounds && containsTarget`, and `dist/polar.js:218,230` emits real boundary `points` with `affinity:'geometry'`. Consequence: pie/ring/sunburst-hover can drop `focusDisabled` and consume `onFocusChange`; largest census retirement left (pie 19 + ring 31 + sunburst 21 raw sites). Radar excluded — accepted deviation logged at D445. | research/phase-6/09-reopened-phase-5.md §2.2. |
| D436 | **D404 corrected (T15 vector A — sunburst zoom/drill morph is NOW UNBLOCKED).** Original claim: NOT IMPLEMENTABLE because sunburst declares one `radialArc` mark and `dist/polar.js` wraps every row in a single `<g class="ts-chart__arc">`, so motion sees one datum. Correct fact: the audit covered only `radialArc`. The library's `sunburst()` mark (`@tanstack/charts/hierarchy/sunburst`, in the package `exports`) is the **sole attacher** of `[sceneMotionNode]` semantic-path metadata — `{path:{values:[startAngle,endAngle,r1,r2], project:projectSunburstSectorPath}, hierarchy:{markId,id,ancestorIds}}` at `dist/hierarchy-sunburst.js:160-168` — which drives per-element keyed morphs via `addSemanticPathUpdateTrack` (`dist/motion.js:1397`, `:1499-1558`) and ancestor enter-morphs via `hierarchyMotionRelation` (`:1787-1833`). `sceneMotionNode` is `unique symbol` with no `exports` subpath (`dist/scene-motion-internal.d.ts:2`), so a `createMark` custom mark can never replicate it — adopting `sunburst()` is the only route. **Blocker 1 of D404 remains correct for the enter phase only** (see D446-scope note: `createArcTracks` at `dist/motion.js:987-1005` still groups by `g.ts-chart__arc`, and `dist/hierarchy-sunburst.js:171` wraps all nodes in one). Precondition: verify `sunburst()` layout reproduces `internal/sunburst-geometry.ts` angles **before** touching motion. Stamp: `@tanstack/charts@0.15.0`. | research/phase-6/09-reopened-phase-5.md §2.3. |
| D437 | **D404 blocker 3 corrected (T15 vector B — a caller-facing replay hook exists).** Original claim: there is no caller-facing way to replay initial choreography; only a React `key` bump remains, and that destroys component state. Correct fact: a changed `options.renderer` **identity** is such a hook — `dist/renderer.js:99-111` responds with `surface.destroy(); container.replaceChildren(); renderer.mount(...); hasRendered = false`, and `:628-629` folds it into `layoutChanged`/`needsRender`. So `renderer={useMemo(() => motion({initial:'always'}), [playKey])}` replays the full initial choreography **without unmounting the React component**, so `internalFocusId` (`sunburst-chart.tsx:312`), `zoomT` (`:472`) and `hoveredArcIndex` all survive — the exact state loss the ruling rested on. Verified residual cost: `sunburst-chart.tsx:828-840` binds `pointerleave` to `container.querySelector("svg.ts-chart")` inside a `useLayoutEffect` whose deps `[setHoveredArcIndex]` never change, so the listener orphans when the svg is replaced; fix is ~10 lines re-binding inside the existing `handleRender` (`:669`). Same caution applies to any chart adopting this replay hook. | research/phase-6/09-reopened-phase-5.md §2.3. |
| D438 | **D390 corrected (T21b timing half is NOW UNBLOCKED; binning half stays void).** Original claim: `stagger()` is `offset + each*index` off one flat index, and a linear function of one index cannot express a pseudo-random function of two, so the heatmap's per-cell reveal delay is not natively expressible. Correct fact: the wrong API was evaluated. `ChartMotionTiming.delay` accepts `number \| ((context: ChartMotionContext<TDatum>) => number \| undefined)` (`dist/types.d.ts:449`) and `ChartMotionContext` carries `datum: TDatum \| undefined` plus `point` (`:412-421`). The heatmap seed is a pure function of two fields on the row — `column*1009 + row*9176`, then `state*16807 % 2147483647` (`internal/heatmap-animation.ts:51-80`) — so `motion:{ delay: ({datum}) => heatmapCellDelay(datum.column, datum.row) }` is a direct, definition-local native expression. The **binning** half of D390 stands: `HeatmapChartProps.data: HeatmapColumn[]` arrives pre-binned, so there is nothing to migrate. Risk: heatmap is T1 (0.0723% headroom) and this retimes the reveal — gate the reveal frame, not just the settled frame. `opacity` is in the `motionAttributes` allowlist (`dist/motion.js:1315-1336`); a non-allowlisted attribute would be dropped silently. | research/phase-6/09-reopened-phase-5.md §2.5. |
| D439 | **D383 corrected (T10 is ALREADY DONE by a different mechanism).** Original claim: `internal/sankey-mark.ts` emits zero `interaction:` targets, so native focus can never resolve a sankey node/link and a custom `ChartFocusStrategy` would be required. Correct fact: true at Phase 5, false in the current tree. `internal/sankey-mark.ts:308-315` emits one `ChartPoint` per node and `:400` resolves link points, and `sankey-chart.tsx:399-408,506-510` bridges element hover into native focus via `setControlledFocus(point,{source:'pointer'})`. No custom `ChartFocusStrategy` was ever needed; the task closed as not-implementable while its acceptance was being met under another name. | research/phase-6/09-reopened-phase-5.md §1, §5. |
| D440 | **D382 corrected (T9 is ALREADY DONE; the objection was app-side, not a library limit).** Original claim: composed-chart resolves TWO point sets (raw + decimated) while native focus resolves only the scene's, so native pointer resolution cannot be adopted. Correct fact: the dual point sets are an application data-flow choice. C3 (`1323172`) kept both bisectors app-owned and made only the pointer plumbing native — `composed-chart.tsx:1686` `interaction.clientToScene`, `:1698`/`:1703` dual raw+decimated resolve, then `setControlledFocus(...,{source:'pointer'})`, the sanctioned app-owned-pointer pattern. The same pattern retired the heatmap cell-boundary objection (`internal/heatmap-components.tsx:766-778`). | research/phase-6/09-reopened-phase-5.md §1, §5. |
| D441 | **D386 blocker 1 corrected (retired by `motion()`; T13 is ALREADY DONE).** Original claim: `capabilities` has zero hits in `dist/svg-renderer.js`/`svg-surface.js`, so the native tooltip snaps rather than tweens and the tooltip cannot be adopted. Correct fact: that is true of the **static** renderer only. `motion()` returns `capabilities:{tooltipMotion:{protocol:1,createController}}` (`dist/motion.js:473-494`) — exactly what `dist/renderer.js:810` reads — so the capability appears the moment T20 lands. Moot in practice: C2 (`aea3bea`) already shipped the native `tooltip` extension + `renderTooltipBody` across 9 charts and 4 internals (`composed-chart.tsx:1440-1456`, D426), so all five D386 blockers are now either moot or retired. | research/phase-6/09-reopened-phase-5.md §1, §5. |
| D442 | **D381 (T6) corrected — understated, and its sankey exclusion is wrong.** Original claim: the acceptance is already met because no custom `renderSvg` wrapper exists; and sankey's `userSpaceOnUse` link gradients are not expressible as bbox fractions. Correct facts: (1) `gauge.tsx:115,683,858` *does* pass `renderSvg={renderChartSvgWithResources}` — it needs to only because it is still on static `Chart`; under `motion()` that renderer is hardcoded for `prerender` (`dist/motion.js:484`) and every update (`:568`,`:616`) and is the default of `createMotionSvgChartRenderer` (`:495`), so after T20 native `gradients:` works everywhere with no prop. (2) Sankey link gradients ARE bbox-expressible: `internal/sankey-mark.ts:444-445` sets `x1 = srcNode.x1`, `x2 = tgtNode.x0`, `y1=y2=0`, which is exactly the link path's horizontal bbox extent, so `{x1:0,y1:0,x2:1,y2:0}` is byte-identical output and `injectGradientDefs` (`internal/sankey-animation.ts:89-109`) can be deleted outright. ~11 of ~19 def sites are convertible (crosshair fades, background, segment-visuals, reference-area, live-line, projection lines); radial/`userSpaceOnUse`/zero-bbox/`<pattern>` sites are not (see D417-stamped absences). Hazard: `dist/svg.js:8-12` `resolvePaint` rewrites `url(#id)` only for ids present in `scene.gradients` — move a def fully or not at all, and grep each `url(#` consumer first. Schedule: 6.4. | research/phase-6/09-reopened-phase-5.md §2.4. |
| D443 | **D381 (T8) corrected — the named artefact does exist.** Original claim: "the named artefact does not exist; no `@keyframes` anywhere in `sankey-animation.ts`", so the injected-stylesheet task was void. Correct fact: right about `@keyframes`, wrong about the artefact. `internal/sankey-animation.ts:113-123` `injectLabelCssTransitions` injects a live `<style class="ts-sankey__transitions">` node into the svg, carrying four static `transition:` selectors (`.ts-sankey__node rect`, `[data-ts-key="sankey:flow"] > path`, `[data-ts-key^="sankey:nlabel:"]`, `[data-ts-key^="sankey:vlabel:"]`) with no per-render interpolation. It moves verbatim into `styles.css`, which already carries 26 `ts-chart__` selectors as a documented styling surface (D418) — a pure move, no mechanism change, ridden along with C5b. Only the `stroke-dashoffset` dash-sweep stays a sanctioned extension: the 20-entry `motionAttributes` allowlist omits dash attributes (`dist/motion.js:1315-1336`, stamped `@tanstack/charts@0.15.0`). | research/phase-6/09-reopened-phase-5.md §2.4. |
| D444 | **D380 (T1) — acceptance criterion restated, not merely failed.** Original criterion: "no `ResizeObserver` left in migrated code". That is **unachievable** and is recorded as such: container measurements feed decimation targets, tick-count selection, margin policy and out-of-scene overlay geometry — all *definition inputs* — so reading them back from `onRender(scene)` is genuinely circular (the definition cannot depend on the render it produces). D380's block on deletion is upheld; leaving the row with no achievable target is the error. **Restated criterion: one observer implementation, five thin wrappers, and no second observer in `internal/brush-drag.ts:151`.** Today there are six independent `ResizeObserver` constructions over largely the same elements — `internal/use-container-size.ts:41,81,135,175,220` plus `brush-drag.ts:151`. Census correction to D380: the module exports five hooks (`:31,58,108,165,201`) with **15 live consumers across 14 charts**, not 3. This is storage/lifecycle hygiene with zero pixel surface and retires nothing from the reach-in census — scheduled as a 6.4 rider, not a ladder rung. | research/phase-6/09-reopened-phase-5.md §2.6. |
| D445 | **Radar excluded from T11 — accepted deviation (v0.15.0).** Radar keeps `focus: focusDisabled` (`radar-chart.tsx:503`) and its reactive-`fill` dim while pie/ring/sunburst adopt native polar focus (D435). Reason: `radialArea`/`radialDot` (`@tanstack/charts/polar`, v0.15.0) carry **no `states` option** — unlike cartesian `dot`/`area` — and their `fillOpacity`/`strokeWidth`/`opacity` channels are per-call numbers, not per-datum accessors; only `fill` (both) and `stroke` (area only) take a row accessor (documented in-file at `radar-chart.tsx:436-447`). Radar's hover dim is therefore *already* the correct native expression: alpha baked into `fill`/`stroke` via `color-mix`, evaluated per z-group on each definition rebuild. Dropped rather than reintroduced by DOM writes: stroke-width hover-pop, the dot stroke-ring dim, and the legacy glow/scale-pop (no filter/transform channel on any mark). Paint deviation only — hit-testing and dim/undim timing unaffected. Radar's 32 raw reach-in sites do **not** retire at C5c; they belong to the reveal subsystem (radar stamps `data-bkm-revealed` on the svg root, predating the `deferred-reveal` centralization) and are C5b's work. Stamp `@tanstack/charts@0.15.0`; expires at the next pin bump per D417. | research/phase-6/09-reopened-phase-5.md §6. |
| D446 | **Per-arc *enter* stagger for polar/hierarchy marks — GENUINELY ABSENT (v0.15.0).** Scopes D436: adopting `sunburst()` unlocks update-phase keyed morphs but **not** enter-phase per-arc timing. `createArcTracks` (`dist/motion.js:987-1005`) selects `[...root.querySelectorAll("g.ts-chart__arc")]` and calls `timingFor({phase:"enter", datumIndex:0, datumCount:1, datum: void 0, point: void 0})`, and `sunburst()` wraps every node in one such group (`dist/hierarchy-sunburst.js:171`), so the enter path (`dist/motion.js:76-81`) paints one whole-chart `radialSweepClipPath` and never reaches per-element timing. D404's blocker 1 is upheld for this phase. Upstream ask: route hierarchy/polar enter through the per-element track builder. Joins the standing absent set with radial gradients (`dist/types.d.ts:363-375`, `dist/svg.js:17-23`), `gradientUnits="userSpaceOnUse"` (`dist/svg.js:50`), `<pattern>` (zero hits in `dist/svg.js`/`types.d.ts`), non-rect clip (`dist/svg.js:26-32`, `dist/svg-renderer.js:76-81`, `dist/types.d.ts:853`), dash motion (`dist/motion.js:1315-1336`), `states` on polar marks (D445), and `sceneMotionNode` for authored marks (`dist/scene-motion-internal.d.ts:2`). All stamped `@tanstack/charts@0.15.0`; all expire at the next pin bump per D417. | research/phase-6/09-reopened-phase-5.md §2.3, §4. |
| D432 | **Motion + reveal subsystem native (C5, v0.15.0).** All 17 charts switched from the static `Chart` to `<RendererChart renderer={chartMotionRenderer<TDatum,TX,TY>()}>` (`internal/motion-renderer.ts`, new shared wrapper; **explicit generics required at every call site** — inference defaults `TDatum` to `unknown` and `ChartRenderer<unknown,…>` is not assignable to `ChartRenderer<NoInfer<T>,…>`). `initial:'always'` is mandatory, not stylistic: `RendererChart` always injects `prerender()` markup which motion otherwise treats as SSR adoption and skips the first entrance. Dispatched by chart group, not by rung (D434 makes the switch per-chart, so a regression reverts one chart): **A** line/area/composed · **B** bar/candlestick/scatter · **C** pie/ring/gauge/sunburst/radar · **D** sankey/heatmap/choropleth · **E** live-line. Routes: enter choreography is **class-substring-driven** in `markMotionRole` (`dist/motion.js`) — `ts-chart__bar-y`/`-x` → baseline growth + automatic stagger, `ts-chart__line`/`__area` → matrix growth, `ts-chart__arc` → clipPath radial sweep, `ts-chart__radial-*` → scale from polar centre, anything else keyed → generic opacity fade, and elements without `data-ts-key` get **no** motion track at all. Per-datum enter timing rides mark-level `motion` callbacks (`ChartMotionTiming.delay`); heatmap's seeded-PRNG per-cell delay ported verbatim from `heatmap-animation.ts:73-80` (closes the T21b timing rider, D438). Live-line adopts the documented rolling-path contract (`motion.md:301-313`): committed `viewport.domain`, fixed margins, keyed `lineY`, `fallback:'snap'`, zero transient translate — `internal/live-hover-chrome.ts` DELETED (zero importers, verified at consolidation). Sunburst zoom becomes a keyed `d`-morph on `radialArc`'s update track (`addSemanticPathUpdateTrack`), key stability verified in `buildArcs` (`arcIndex` derives from static tree structure, never from focus/zoom). Radar gains `RadarRow.replayGroup` wired as `z` on `radialArea`/`radialDot` so a `motionReplayKey` bump replays the whole native campaign — verified against `dist/polar.js` (`radialArea`'s path key is `${id}:${groupKey}`, sourced from `z`, not `key`) and `dist/transform-fold.js` (fold's spread carries extra wide-row fields through). Authored-mark classes renamed `ts-chart__*` → `bkm-chart__*` per D418 (candlestick, bar, scatter hover-dot/candle/pulse; sankey node/label/link), with the matching `styles.css` selectors moved by the orchestrator at consolidation — executors reported shared-file edits rather than applying them, so the five could not collide. `svgAnimation:false` is dead under `chartMotionRenderer()` (only the static SVG renderer reads it, `dist/renderer.js:125`); left in place with a comment, flagged for 6.4. Full `pnpm check-types` clean. | C5 executor reports A–E; orchestrator consolidation; dist/motion.js, dist/polar.js, dist/transform-fold.js, docs/guides/motion.md. |
| D433 | **C5 residuals, dropped flourishes, and one census correction (v0.15.0).** *Genuinely absent, all stamped `@tanstack/charts@0.15.0`, all expiring at the next pin bump per D417:* (1) **Per-datum stagger in the update phase** — `dist/motion.js:2552` unconditionally zeroes update-phase spring delays, so candlestick's per-candle reveal stagger is structurally unreachable; all candles now move in lockstep. (2) **`filter` is not in the `motionAttributes` allowlist** (`dist/motion.js:1315-1336`, which admits only cx, cy, d, fill-opacity, font-size, font-weight, height, opacity, r, rx, stroke-opacity, stroke-width, transform, width, x, x1, x2, y, y1, y2 — non-allowlisted attributes are dropped **silently**), so scatter's legacy `filter: blur(...)` enter channel is dropped. Corrects the C5 brief, which said to drop a scale/`r` pop: `bklit-ui/.../series-point-marker.tsx:154,159` shows `scale: 1` in **both** the `hidden` and `visible` variants, so no such pop ever existed. (3) **No stroke-dasharray/dashoffset channel** (same allowlist) — sankey's node `scaleY` dash pop stays unmigrated; joins D446's absent set. (4) **No path-length API** on `ChartRendererRenderContext` or `ChartMotionContext`, so `internal/dash-tail.ts`'s `getTotalLength()` has no renderer-level equivalent — the module is untouched and **joins the 6.5 CI grep-guard named exceptions**. (5) **No motion-completion callback of any kind.** This reversed the brief on composed's bars: their WAAPI grow loop drives `setRevealDeadline`/`pendingBarsRevealRef` and the `"ready"` phase transition off WAAPI *completion* (the documented "D-composed-settle-regression" fix), so replacing it with native `motion` would have silently broken phase timing. Loop retained; `'bar'` added to chart-level enter-suppression so it cannot double-animate against the native default. (6) **No per-tick fill channel on `ChartAxisTickLabelOptions`** — live-line's `createTickColorResolver` y-label coloring dropped, same gap as D431 item 1, reused as precedent rather than re-derived. *Dropped flourishes:* native enter animates **opacity only** across the whole polar family — gauge's `transform:scale(0)→scale(1)` notch pop and radar's radial-growth both become stagger-delayed opacity fades; radar's update/exit tracks are pinned to `{type:'tween',duration:0}` so native does not introduce a transition legacy never had (legacy hover was plain React re-render). Ring's hover scale moves from a DOM `style.transform` write to a definition-level spring parameter (same `HOVER_SPRING {400,25}` physics as pie, shared by design); pie's `showGlow` not reintroduced (confirmed dead at runtime in legacy, D49). *Two behavioural changes that are QA-visible and are 6.5 gate items:* live-line's y-domain is **no longer `.nice()`'d** (deliberate — niceing can jump the rounded domain non-affinely between two commits with near-equal raw bounds, violating `motion.md`'s single-affine-y-transform reproject requirement; nice tick *values* survive via the pre-existing `pickNiceInterval`, which works off raw bounds); and scatter's reveal deadline moves from `revealDurationMs` to `revealDurationMs + ENTER_TWEEN_MS` (legacy could force-`.cancel()` a still-fading WAAPI circle; native per-element transitions cannot be interrupted externally, so the deadline now bounds the true worst case). *Two disclosed risks for the 6.5 gate, both raised by executors rather than hidden:* heatmap's `key()` is now epoch-suffixed (`${column}-${row}:${revealEpoch}`) so a replay re-triggers the native enter phase, which churns every cell's identity on each bump **including `heatmap-lifecycle.ts`'s mount-time bump** and may double-remount within one paint pass — heatmap is T1 with 0.0723 headroom and is exactly the chart the gate-scheduling constraint singles out, so judge the *reveal* frame, not only the settled frame; and sankey hover is now a node-AABB + `curveBumpX`-**approximated** link smoothstep test (`findHoveredSankeyTarget`) replacing exact DOM hit-testing, accuracy unverifiable without a browser. *Surviving reach-in:* `choropleth-chart.tsx` retains `container.querySelector("svg.ts-chart")` in `handleRender` — must be **counted** in the 6.5 census, not assumed retired. *Census correction — the C5 orphan-deletion branch is void.* The carried consolidation plan expected to delete `enter-transition.ts`, `deferred-reveal.ts`, `native-stagger.ts`, `sunburst-reveal.ts`, `gauge-reveal.ts`, `radar-reveal.ts` and `radar-spring.ts` as orphans. An importer sweep at consolidation found **none of them orphaned**: `enter-transition.ts` has 18 importers (13 chart files plus `chart-reveal-clip.tsx`, `radar-reveal.ts`, `gauge-reveal.ts`, `pie-center.tsx`, `terminal-marker.tsx`), `deferred-reveal.ts` 8, `native-stagger.ts` 5, `heatmap-animation.ts` 4, and each `*-reveal.ts` retains its own chart; `radar-spring.ts` is held by both `radar-chart.tsx` and `enter-transition.ts`. Zero modules were deleted at consolidation beyond `live-hover-chrome.ts` (0 importers). Consequently the **`ChartRevealClip` ↔ `enter-transition.ts` collision recorded as UNRESOLVED does not exist** — nothing is being deleted out from under `index.ts:619-624`. `reveal-easing.ts` is live (bar/candlestick/scatter), `reveal-wipe.ts` is live (line/area/composed), and `spring.ts`/`bezier-easing.ts`/`use-prefers-reduced-motion.ts` survive as previously ruled. *Not a duplication:* `solveCubicBezierEasing` (`heatmap-components.tsx:277`) solves **arbitrary** control points, while `internal/bezier-easing.ts` hardcodes bklit's `cubic-bezier(0.85,0,0.15,1)` and takes no parameters — generalizing one into the other is a 6.4 refactor rider, not a C5 dedupe. *Supersedes* `docs/phase-6/PROGRESS.md`'s "rider ownership is asserted, not verified" note: the T21b timing rider (D438) was executor D's and is done; the sankey `<style class="ts-sankey__transitions">` move (D443) landed as four static rules in `styles.css`; the T6/T8 **gradient** conversion (D442) was never in C5 scope and remains a 6.4 rider. | C5 executor reports A–E; orchestrator consolidation sweep; dist/motion.js:1315-1336, :2552; docs/guides/motion.md:301-313. |
| D447 | **C5c: polar focus lands on the hitbox twin; two consequences accepted.** Pie/ring drop `focus: focusDisabled` and consume native focus against the static hitbox twin. (a) *Duplicate keyboard tab-stops* — no field on `RadialArcOptions`/`ChartMarkOptions` (v0.15.0) excludes a mark's points from focus candidacy, so the visible mark and the hitbox twin each contribute a `ChartPoint` at the same position. Pointer hover is unaffected (containment resolves to topmost-painted = hitbox, declared last). Strictly additive — `focusDisabled` previously killed *all* native focus including keyboard — so this is cosmetic redundancy, not a regression. (b) *Cursor moves to CSS* — the imperative `groupEl.style.cursor`/`hitboxGroup.style.cursor` writes are deleted and replaced by two `styles.css` rules on `path[data-ts-key^="pie-hitbox:"]` / `path[data-ts-key*="-hitbox:"]`, the sanctioned styling surface (D418). *Brief corrections banked:* ring already had its hitbox twin at HEAD (`ring-chart.tsx:552-663`, the D258 fix) — the brief's "ring needs the twin added" was wrong; and the six `onFocusChange` sites cited as native focus plumbing are all `SunburstChartProps.onFocusChange`, bklit's own `(focusId: string) => void` drill callback — a name-based mispattern, not native API. | C5c executor F report; dist/polar.d.ts, dist/types.d.ts @0.15.0; census pie 5→1, ring 17→13. |
| D448 | **C5c: sunburst native focus is keyboard-ONLY, by geometry — and the SB15 `pointerleave` reach-in was already dead.** `SunburstHitLayer` is a later-painted, identically-sized (100%×100%) absolutely-positioned sibling of `<RendererChart>`, so it fully occludes `svg.ts-chart`. Native pointer resolution can therefore never fire over an arc-covered region: pointer hover stays on the pre-existing overlay handlers (unchanged), while Tab/arrow keys reach `svg.ts-chart` through its own `tabIndex`. Net capability **add** — keyboard users get arc dim/hover-preview for the first time. By the same occlusion argument the SB15 `useLayoutEffect` that queried `svg.ts-chart` for a `pointerleave` listener was **retired as dead code, not replaced**: `svg.ts-chart` never receives `pointerenter`, hence never `pointerleave`; `SunburstHitLayer`'s own `<svg onPointerLeave>` was doing the work all along. Sunburst hover was consequently **never a DOM reach-in** — `SunburstHitLayer` is React JSX. **6.5 gate item:** confirm the keyboard path and the pointer path cannot drive hover into conflicting states. | C5c executor F report; sunburst-chart.tsx occlusion analysis. |
| D449 | **C5d: `radialArc` → native `sunburst()` angle parity is EXACT (0 rad, not an epsilon) under three conditions we control — measured, not argued.** Harness ported `sunburst-geometry.ts`'s `sumValues`+`layoutNode` verbatim and compared against real d3-hierarchy 3.1.2. (1) The `value` accessor **must be leaf-only** (`d.hasChildren ? 0 : d.rawValue ?? 0`): native calls d3's `hierarchy.root.sum()` (`dist/hierarchy-flat-internal.js:97`), which **adds an internal node's own value** to its children's, while ours ignores `node.value` whenever a node has children — d3's natural accessor diverged by **3.016 rad ≈ 173°**. (2) **No `options.sort`** — native sorts only if passed (`dist/hierarchy-sunburst.js:33`); omitted it preserves data order, matching our `children.entries()`. (3) **Polar sweep `startAngle: -π/2`, `endAngle: -π/2 + 2π`** on the `polar()` container (`PolarOptions`, not the deprecated `scales.angle`/`scales.radius`). *Two further differences, both benign and handled:* native filters `x1 > x0` (`:57`) so **zero-value branches emit no arc** where ours emitted a zero-span arc that painted nothing anyway (`arcPathFromRadii` returns `null` below 0.001 rad) — drill/focus/hover/dim therefore continue reading `buildArcs`'s own `arcs`/`focusById`/`arcsById`, never native mark data; and `visibleDepth` is passed explicitly as `max(1, maxDepth - focus.depth)` rather than left to native's local-subtree-height default, keeping ring count aligned with the still-`ringOptions`-driven hit layer, labels, and center overlays on irregular-depth trees. | Orchestrator parity harness (scratchpad/c5d-angle-parity.mjs), d3-hierarchy 3.1.2; C5d executor report; PROGRESS.md:33 precondition discharged. |
| D450 | **C5d: sunburst hover-grow (radial pop-out) is DROPPED — a live feature traded for semantic motion. `hoverPop` is now inert.** Native `sunburst()` exposes **no per-datum radius channel** at 0.15.0: only `fill`/`stroke` are `VisualChannel<SunburstNode<TDatum>, string>`, while `innerRadius`/`outerRadius` are `PolarLength = number \| ((context: PolarLayoutContext) => number)` — layout-level, not per-node — and `ringPadding`/`fillOpacity`/`strokeWidth` are plain numbers (`dist/hierarchy-sunburst.d.ts:29-40`, `dist/polar.d.ts:37`). This was **live at HEAD**, not dead: `sunburst-chart.tsx:610-626` drove it through the public `hoverPop` prop (default 8). Hover **dim** is fully preserved (folded into the mark's `fill` callback as a color-mix alpha); only the radial pop-out is lost. `buildHoverGrowTargets`/`applyHoverGrow`/`maxHoverSegmentThickness` are now dead, still exported from `internal/index.ts:30-32`. **Open for the 6.5 gate / user call:** the public `hoverPop` prop still accepts a value and silently does nothing — either mark it deprecated-inert or reconstruct the pop-out via a `createMark` overlay. Reverting is one commit. Version-stamped per D417. | C5d executor report; dist/hierarchy-sunburst.d.ts, dist/polar.d.ts @0.15.0; HEAD sunburst-chart.tsx:191,322,610-626. |
| D451 | **C5d: reveal replay uses a mark-`id` bump, not renderer identity — the plan's instruction was wrong.** PROGRESS.md:34-35 specified `renderer={useMemo(() => motion({initial:'always'}), [playKey])}`. That contradicts `internal/motion-renderer.ts:6-7`, which requires the shared `motion()` instance stay a stable module-level singleton because `RendererChart` threads `renderer` through `adapter.update` — a new identity per `playKey` would churn the host **for every simultaneously mounted chart**, not just sunburst. Implemented instead as `sunburst-arcs-${playKey}`, the file's own established replay idiom. Also void: PROGRESS.md:35-37's "~10-line `pointerleave` re-bind inside `handleRender` (`:669`), orphaned listener at `:828-840`" — **that code does not exist**; there is no `handleRender` and no orphaned listener, only comments recording the D448 retirement. Per-arc *enter* stagger stays absent (D446). | C5d executor report; internal/motion-renderer.ts:1-8; orchestrator verification (grep: no handleRender/pointerleave in sunburst-chart.tsx). |
| D452 | **C6: `internal/brush-drag.ts` DELETED (−387 LOC); the v0.14 NON-VIABLE ruling is superseded because the brush went on the OVERVIEW host.** That ruling's load-bearing objection was semantic: `brushX` binds to the host's final x scale (`interaction-brush.ts:124-132`), while the brush needs a track that stays stable *while the chart's own `xDomain` narrows mid-drag* — "even inside a host, the native binding rescales under the cursor." **Verified inapplicable**, because the control is attached to the strip host, which never narrows: in `components/demos/brush.tsx` the strip host (`:73-94`) takes `data` with **no `xDomain` prop** and owns `<ChartBrush>`, while the detail host (`:101-107`) takes `xDomain={brushLayout.xDomain}` and has no brush. This is conformance case 83's overview+detail pattern. Both consumers build `brushX<Date>` through `defineChart`'s `controls:` array with `controlledSignal`; `line-chart.tsx:382-386,989-990` gates it to `brushes[0] ?? null` so every non-strip instance collapses to `EMPTY_BRUSH_CONTROLS`. `chart-brush.tsx` is reduced to a null-rendering config carrier (`ChartBrushProps` preserved verbatim; its `host` prop now vestigial); `BrushHost` + `selectionToPixelExtent` relocate into `brush-chrome.tsx`, `pixelExtentToSelection` dropped (native reports `Date` ranges directly). One forced barrel fix in `internal/index.ts` (drop `BrushHostContext`, re-point `type BrushHost`) — flagged by the executor rather than taken silently. Ruling text preserved verbatim in the C6 executor report: **superseded at 0.15, not wrong when written** (D417). | C6 executor G report; orchestrator verification of components/demos/brush.tsx:73-107; research/phase-6/06. |
| D453 | **C6: portal brush chrome keeps 100% of the pixels; native brushX owns 100% of the mechanics underneath invisible DOM.** All four legacy pieces (`BrushTrackChrome`, `BrushSelectionPatternChrome`, `BrushBorderChrome`, `BrushHandleChrome`) are kept unchanged and rendered by the host chart; native's own selection/handle DOM is hidden via fully-transparent `selectionStyle`/`handleStyle`. **The plan's stated first preference — CSS-skinning the native brush DOM — was investigated and rejected, correctly: the orchestrator brief's citation of `ts-chart__handle-x` as "a documented styling surface" conflates two unrelated interactions and appears nowhere in the 0.15.0 docs.** Orchestrator error, caught by the executor. Secondary reasons: `selectedBoxStyle`'s legacy arbitrary-`SVGProps<SVGRectElement>` contract is unreachable from `SceneStyle` (`dist/types.d.ts:763` — no rx/mask/filter/className) regardless of CSS vs inline, though this is moot in practice since `selectedBoxStyle` feeds only the portal chrome; and avoiding a double-paint conflict. *Accessibility is a net gain:* legacy `useBrushDrag` was pointer-only (no `tabIndex`/`onKeyDown`), while native ships keyboard stepping via `values` — sourced from the full non-decimated `data`, not `renderData`, giving real snap points (D422 improvement). Cost: the focus ring falls back to the browser default outline, since the portal chrome paints none. | C6 executor G report; dist/types.d.ts:763; docs @0.15.0. |
| D454 | **C6: `onSelectionChange` fires on preview AND commit, and zero-width clears only on commit.** The docs' own example calls `setRange` on `commit` only; that is insufficient here because the portal chrome lives in a separate React tree from native's live echo and needs the app-state round-trip to track an in-progress drag — and because legacy `useBrushDrag` fired continuously so the detail host's `xDomain` narrows live. Separately, a zero-width selection clears (`onSelectionChange(null)`) **only** on a real `commit`: a drag start naturally passes through a transient zero-width preview, and clearing there would null the detail `xDomain` on every press. `cancel` is ignored. Also removed: the dev warning "ChartBrush rendered without a host", which depended on the now-deleted `BrushHostContext` host discovery. | C6 executor G report; bklit chart-brush.tsx:271-291 boundsToSelection parity. |
| D455 | **C6 — PUBLIC CONTRACT NARROWED: uncontrolled `<ChartBrush>` is no longer functional.** Native `brushX` requires a fully round-tripped controlled value, so a caller that does not echo the committed selection back into `initialSelection` leaves the portal chrome frozen at the full-range fallback. Inert today — every current usage is fully controlled via `useBrushSelection` — but this is a genuine narrowing of a public prop's contract, not an internal detail, and is recorded as such rather than buried. **Open for the user / 6.5 gate:** accept, or restore an uncontrolled fallback path. | C6 executor G report; orchestrator flag. |
| D456 | **C6: choropleth zoom becomes a PARAMETER OF THE PROJECTION, retiring the raw transform writes.** `geoShape`'s `projection` accepts a factory (`dist/geo.d.ts:16-17`), so zoom is applied as `geoMercator().scale(baseScale * m.scaleX).translate([...])` instead of a post-hoc `setAttribute("transform", …)` on `g.ts-chart__marks` and the graticule group (HEAD `choropleth-chart.tsx:643,650,655`). Exact, because d3's scale/translate composition is affine — the same shape as the retired `matrix()` — **valid only while the matrix carries no skew and `scaleX === scaleY`**, true of every path the current `<Zoom>` handlers produce; documented as an approximation, not a reachable gap today. Decided by **measurement, not estimate**: 177 features / `geoMercator` / 900×500 / 120 frames of full `d`-string regeneration = **5.88 ms/frame** (35% of a 60fps budget), so the research doc's `<30fps` ACCEPT-WITH-LOG fallback and its planned census exception are **avoided entirely**. One rAF trio throttles to at most one commit per frame and stops scheduling once settled; the retired `transition: transform 0.18s ease-out` is reproduced by a Newton solve of `cubic-bezier(0,0,0.58,1)` over 180ms against matrix values. `ChartGraticuleOverlay` needed no changes (it already re-derives from its `projection` prop). **`applyZoomToPoint`/`zoom.applyToPoint` removed**: `points` from `geoShape` now arrive in final zoomed screen space, so retaining forward application would double-apply zoom to the tooltip anchor; `refreshTooltipAnchor` is kept and called from the rAF commit branch, since `dist/tooltip.js` caches the anchor and will not re-run on its own. | C6 executor H report; orchestrator geo benchmark (177 features, topojson-client); dist/geo.d.ts:16-17. |
| D457 | **C6 — research doc corrected: `internal/zoom-engine.tsx` survives ENTIRELY UNCHANGED at 663 LOC; the planned deletion retires nothing.** `research/phase-6/06-brush-zoom.md:15` attributed both the transform writes and "inverse-matrix hit-testing" to `zoom-engine.tsx`. Both are false, verified: `grep -c setAttribute internal/zoom-engine.tsx` = **0** (the writes were in `choropleth-chart.tsx`'s `syncZoomTransform`), and `applyInverseToPoint`/`toStringInvert` have **zero consumers** outside the module itself — choropleth hover hit-testing is native DOM `mouseenter`/`mouseleave` on real `<path>` elements (`choropleth-hover-chrome.ts`), untouched by matrix math. `zoom-engine.tsx` is a pure matrix/gesture-state module with no DOM surface. A superseding correction is appended to the research doc; **6.5 LOC and census expectations must be adjusted** — the real retirement is the two transform writes (choropleth census **10 → 9**), not a module deletion. Six raw query sites survive in `choropleth-chart.tsx` and must be counted at 6.5, one of them structural: `:868`'s `svg.ts-chart` fallback exists because the reveal-recovery effect calls `handleRender({container})` synthetically, so there is no `surface` to hand in. | Orchestrator verification; C6 executor H report; research/phase-6/06-brush-zoom.md correction block. |
| D458 | **C6: `unprojectPoint` added to `ChoroplethContextValue` with ZERO consumers — kept, but recorded as dead surface.** The brief said to replace inverse-matrix hit-testing with `clientToScene` + `projection.invert()`; the executor over-read that into new public API (`choropleth-chart.tsx:204,228,660`, exported `:1093,1106`). Verified unused. **Kept** because it is public API on an exported type, pairs with the existing `projectPoint`, and is the documented sanctioned replacement path for future consumers — but it is dead surface added during a surface-reduction phase. Revisit at 6.4 rather than churn now. No `index.ts` change required (bare `type` re-export). | Orchestrator decision; C6 executor H report. |
| D459 | **The two open questions from D450 and D455 are RESOLVED — both losses accepted, for the same reason.** Put to the user, who said to proceed without selecting; decided by the orchestrator and recorded as such. (a) *Sunburst hover-grow stays dropped and is NOT rebuilt.* The only route left is a `createMark` radial-grow overlay — which would re-add precisely the parallel chrome layer this phase exists to retire, to serve a flourish, at the cost of the semantic zoom morph C5d bought. `hoverPop` is kept as a prop (removing it would break callers) but is marked **deprecated-inert** with the reason stated in-source; its dead internal plumbing is removed. (b) *Uncontrolled `<ChartBrush>` stays unsupported.* Restoring a fallback path would add code for **zero current consumers** — every usage already round-trips through `useBrushSelection` — again against the phase mission. Both are reversible: hover-grow by reverting C5d (one commit), the brush by re-adding an uncontrolled branch. **Both must be re-surfaced at the 6.5 gate** as user-visible deltas, not buried as internal refactors. | Orchestrator decision 2026-09-01; D450, D455. |
| D460 | **D444 (observer consolidation) is corrected on both of its clauses.** Recorded as "one implementation, five thin wrappers, no second observer in `internal/brush-drag.ts:151`". Verified: (a) `internal/brush-drag.ts` **no longer exists** — deleted in C6 (D452) — so that clause is void; (b) the five `new ResizeObserver(...)` constructions are **already all inside `internal/use-container-size.ts`** (`:41`, `:81`, `:135`, `:175`, `:220`), and `pie-chart.tsx`/`ring-chart.tsx`/`radar-chart.tsx` only mention ResizeObserver in comments — they hold no observer code. T1 is therefore a **single-file** refactor, not the cross-cutting one the ledger implied. Also settled here: `internal/y-axis-ticks.ts` is a **survivor with three live consumers** (`candlestick-chart.tsx:71`, `internal/axis-ticks.ts:13`, `index.ts:458,505`), not a missed deletion — consistent with D431 item 7. | Orchestrator verification 2026-09-01. |
| D461 | **T6's gradient estimate (D442) is unreliable and the rung is gated on a survey.** D442 recorded "~11 of ~19 def sites, mechanical once the renderer switch lands". The measured census is **75 `linearGradient` sites across 21 files, 58 `<defs>` across 22, 8 `radialGradient` across 4, 11 `<pattern>`, 5 `<mask>`, 0 `<filter>`** — roughly 4× the recorded figure. Native support is **linear gradients only**, declared on the spec as `ChartSpecBase.gradients?: readonly ChartLinearGradient[]` with normalized 0–1 coordinates and plain offset/color/opacity stops (`dist/types.d.ts:363-375,405`); there is no native radial, `<pattern>`, `<mask>`, `gradientUnits`, `gradientTransform`, `spreadMethod`, or animated-stop support. Crucially, a gradient defined in **app-owned JSX inside an app-owned `<svg>` is not a reach-in at all** (D418) and needs no conversion — only gradients a *native mark* consumes via `url(#id)` are what native `gradients` exists to serve. No conversion work is dispatched until a read-only survey classifies every site as app-owned / convertible / renderer-coupled-but-inexpressible / dead. **Deliberately not dispatched on the recorded estimate.** | Orchestrator census + survey dispatch 2026-09-01; dist/types.d.ts:363-375,405; docs/reference/scales-guides-and-color.md §Theme and gradients. |
| D462 | **The recorded "5-6× gzip gap" (M2c) was a measurement artifact, not a bundle fact. `bench/measure-bundle.mjs` is fixed; every number it produced before this fix is non-comparable.** The measurer bundled `import "./<scenario>.tsx"` — a bare side-effect import that *uses nothing*. `repos/bklit-ui/packages/ui` declares `sideEffects:false`, so every bklit scenario tree-shook down to the one thing the scenario file imports for its side effects: the shared world-TopoJSON fixture (`bench/app/src/assets/world-countries-110m.json`, ~38 KB gz). `bklit/line` = 39,171 B gz *is the fixture alone* — every bklit cell reads 39,171/39,172 B. The migrated barrel had no `sideEffects` declaration, so esbuild kept it whole (254 KB gz for `migrated/line`). Ratio of a fixture to a whole library ≠ ratio of two charts. Fix: the entry is now `import S from "./<scenario>.tsx"; export default S;` (the scenario's default export is *used*, so tree-shaking is real on both arms). Like-for-like, used-entry, gzip: `bklit/line` **133.1 KB**, `migrated/line` **147.3 KB** (both including the 38 KB fixture) — +11%, not 6×. Old series preserved verbatim as `bench/results/bundle-sizes-phase-5.json` for the record; `docs/phase-6/BENCHMARKS.md` carries the corrected series. The DoD's "materially down" clause is judged against the corrected numbers, not the artifact. | Orchestrator, 2026-09-01; `scratchpad/attribute-bundle.mjs` metafile attribution (per-input bytes); esbuild `sideEffects` semantics. |
| D463 | **`showcase/migrated/package.json` is added with `sideEffects:["**/*.css"]`.** Without it the migrated tree is not shakeable by esbuild *or* by the Next consumer (webpack reads the nearest `package.json`), so a scenario that imports one chart from the barrel ships all seventeen. Verified before adding: the only top-level side effects in `showcase/migrated/**` are `X.displayName = …` assignments and pure module constants (motion singleton, heatmap scales, `SANKEY_CURVE`) — dropping an unreferenced module loses none of them. `styles.css` is excluded from the claim so the stylesheet import survives. Effect: `migrated/line` 251.0 KB → 147.3 KB gz at HEAD. The manifest is a build-time declaration with no runtime behavior; it lands in the refactor commit. | Orchestrator, 2026-09-01; grep of top-level statements in migrated sources. |
| D464 | **The four native-crosshair gradients defined with `gradientUnits="objectBoundingBox"` are a live visual regression and are fixed to `userSpaceOnUse` (behavioral; own commit ahead of the refactor commit).** `line-chart.tsx:1613`, `area-chart.tsx:1693`, `composed-chart.tsx:2101`, `live-line-chart.tsx:892` define the crosshair stroke gradient in bbox units. The native crosshair x-guide is a `kind:"rule"` → `<line x1=x2>`; a zero-width bounding box makes an objectBoundingBox gradient paint **nothing** in Chromium (verified with a playwright + pngjs pixel test: obb pixel = background, userSpaceOnUse = ink). So since C3 those four charts' crosshair has rendered invisible whenever the gradient path is taken (i.e. `dasharray` unset). `bar-chart.tsx:1803` already uses the working pattern (`userSpaceOnUse`, `y1=margin.top`, `y2=margin.top+innerHeight`); the fix copies it. Not caught by the QA gate because the hover captures compare against bklit at cells where the diff stayed under 0.5%. Cannot move onto `spec.gradients` — the native contract is objectBoundingBox-only (`ChartLinearGradient`, no `gradientUnits`), which is exactly the broken case. | Orchestrator pixel test 2026-09-01; dist/types.d.ts `ChartLinearGradient`; svg-renderer `resolvePaint`. |
| D465 | **T6 (gradient rung, D442/D461) closes with two conversions, four fixes and the rest version-stamped inexpressible at 0.15.0.** Survey classification of the 75 `linearGradient` sites: (a) **convertible to `spec.gradients`** — only live-line's stroke/area pair (`live-line-chart.tsx:853,857`: objectBoundingBox, two stops, consumed by native `lineY`/`areaY` paths with a non-degenerate bbox) → converted, with `y1:0,y2:1` set explicitly because the native default is `y1=1,y2=0`; (b) **renderer-coupled but inexpressible** — the four crosshair gradients (D464: need `userSpaceOnUse`), the horizontal fade gradient (`live-line-chart.tsx:861`, 5+ stops with per-stop opacity is expressible, but it masks via `<mask>`, which native has no channel for), every `<pattern>` (11), `<mask>` (5), `radialGradient` (8); (c) **app-owned** — gradients in app JSX consumed by app-owned SVG (legend swatches, gauge chrome, center-stat, loading skeletons), not reach-ins by D418. Rule (D417): the "inexpressible" verdicts are stamped `@tanstack/charts@0.15.0` and expire at the next pin bump. | Orchestrator survey 2026-09-01; dist/types.d.ts:363-375. |
| D466 | **Barrel-only public surface is KEPT, not deleted, in 6.4.** The dead-code scan flagged five modules and several default re-exports that no chart in `showcase/migrated` consumes (`unprojectPoint` and siblings, D458; `useProfitLossLegendHover` re-exported at `index.ts:338`; the `ChartSelectionContext` family). They are bklit public API — `qa/api-compat` (compiled by `bench/app`'s tsconfig) is the parity guard and would fail — and with D463's manifest an unused public export costs consumers zero bytes. Deleted instead: the 35 zero-importer `internal/*` symbols, the sunburst hover-grow trio (D459), the `internal/index.ts` barrel trimmed from ~45 to the 8 consumed names, `.bkm-hover-layer` CSS, `sortOthersByY`, the `gauge-reveal.ts` forwarder, and the two helpers made transitively dead by those deletions (`getHeatmapSeparatorGroupStartColumn`, `resolveReferenceDataRange`). Full list in the refactor commit body and `research/phase-6/10-refactor.md` §A. | Dead-code executor report 2026-09-01 (27 files, −545 LOC); orchestrator ruling. |
| D467 | **D418's planned `ts-chart__*` → `bkm-chart__*` rename of authored marks is REVERSED for the 15 sites that still carry the renderer class: those classes are a motion-role opt-in, not a reach-in.** `dist/motion.js` `markMotionRole()` resolves enter/update choreography by class *substring* — `className.includes("ts-chart__bar")`, `"__area"`, `"__dot"`, `"__arc"`, `"__text"`, `"__radial-area"`, … — so an authored `barY`-shaped mark named `ts-chart__bar-y` gets native baseline growth and one named `bkm-chart__bar-y` gets nothing (that is precisely why `bar-pulse-mark.ts:152` was renamed *off* the substring in C5, D432). Renaming the remaining sites (`series-bar-mark`, `bar-trimmed-mark`, `bar-squares-mark`, `bar-column-track-mark`, `bar-depth-marks`, `area-fill-mark`, `pattern-area-mark`, `radar-reveal` grid/text, gauge arcs, scatter dots) would silently change their motion — behavioral, and the wrong direction. Ruling: a `className:` literal on an authored scene node addresses the library's role contract, not its DOM; the guard (`scripts/reach-in-guard.mjs`, `isRoleOptIn`) excludes such lines, and `08-reach-in-census.md` finding 2 is amended. Sites that *query* those classes (`radar-chart.tsx:85,757,758`, `composed-chart.tsx:1911`) stay counted as reach-ins. | dist/motion.js markMotionRole; `bar-pulse-mark.ts:149-156`; orchestrator ruling 2026-09-01. |
| D468 | **Reach-in census closes at 79 sites in 15 files, every one inside a named, pinned exception — "0 outside the ledger", not an unqualified 0.** Re-run after the refactor commit with `scripts/reach-in-guard.mjs` (patterns `querySelector(All)?(`, `ts-chart__`, `ts-sankey__`, `data-ts-key`, `elementMap`; comments stripped; `className:` role opt-ins excluded per D467). Baseline was 259 (6.1.8), 94 raw after C6, 79 once the 15 role opt-ins are excluded. Per file: radar 19 (D433/D445 reveal), ring 13 (D433), sankey-animation 9 (D433/D443), choropleth 8 (D433 group fade + D457 raw query group incl. the structural `:868` fallback), sunburst 7 (D384 click/drill + SB15 fade + D447), deferred-reveal 4, area/line/composed 3 each (D420/D433 marker-reveal WAAPI + wipe clip), dash-tail 3 (D433 item 4), bar-pulse-mark 3 (D432 self-owned group readback), bar 1, scatter 1 (D433 reveal stamp), choropleth-hover-chrome 1 (D457), sankey-chart 1 (`querySelector("svg")` pointer-host lookup — changing it is behavioral, left). Families: reveal (WAAPI needs path-length/element handles the renderer does not expose), D384 drill, D457 geo, self-owned readback, pointer host. Enforcement is a **ratchet**: `scripts/reach-in-ledger.json` pins a per-file maximum with its D-ruling; the guard fails CI (`.github/workflows/reach-in-guard.yml`, `pnpm guard:reach-in`) on any unlisted file or any pin exceeded, and prints a note when a count drops below its pin so the pin can be lowered. The DoD box "census = 0" is ticked with this qualification and not otherwise. | `node scripts/reach-in-guard.mjs --report` 2026-09-01: "79 site(s) in 15 file(s); ledger pins 15 file(s) — OK". |
| D469 | **Bundle (M2c) verdict: "materially down" holds against the number the plan was written from, not against legacy.** Corrected harness (D462/D463), HEAD `c1e9ced`, 104 bundles / 0 failed. Summed gzip over the 43 migrated scenarios: legacy bklit 5023 kB · Phase-5 tip re-measured, barrel as shipped 9020 kB · Phase-5 tip best-case tree-shake (forced `sideEffects:false`) 4301 kB · **HEAD 5173 kB** · tanstack ceiling ~78 kB/scenario. HEAD is −43% vs what actually shipped at Phase-5 close, +3% vs legacy (17 scenarios below legacy, 26 at or above), +20% vs the Phase-5 best case. Attribution (`migrated/line`, minified bytes): `@tanstack/charts` 85→157 kB (motion.js 42 kB, interaction-brush 8 kB + its d3-brush/selection/transition/drag ~35 kB, tooltip 6 kB, crosshair-resolver 5 kB) against app-owned `internal/` 86→71 kB and chart files 22→24 kB. Phase 6 traded ~30 kB of hand-rolled chrome for ~100 kB of library modules; the plan's "~5-6x gap" was the D462 artifact and never existed. Every scenario also carries a constant ~38 kB gzip TopoJSON fixture from the bench app's shared data module, identical across impls, left in (the file that would strip it is not this phase's to change). Gate: `bench/results/bundle-gate.json` pins all 43 migrated scenarios at HEAD +3% (`pnpm guard:bundle`, `scripts/bundle-gate.mjs`); raising a pin needs a D-entry. DoD "materially down" ticked **with this qualification**, not unqualified. | `docs/phase-6/BENCHMARKS.md` §1; `bench/results/bundle-sizes.json`, `bundle-sizes-phase-5.json` (stale series, kept for the record), `bundle-gate.json`. |
| D470 | **Lint debt carried, not cleared: 18 eslint warnings in `showcase/migrated`, 0 errors.** 24 before 6.4 → 18 after; the six removed were the refactor's own unused-import/unused-directive hits (`TOOLTIP_SPRING`, `SANKEY_MARK_ID`, `ChartValue`, `e`, two stale `eslint-disable` lines). Every survivor pre-dates 6.4 at a shifted line: 9 `react-hooks/exhaustive-deps` (line/area `xAxis` "unnecessary" deps, bar `projectValue`/`pulseWaveGradientId`, choropleth `featureConfig`, composed `prefersReducedMotion`, chart-reveal-clip `onComplete`, sankey `hovered*IndexRef` ×2, line `projectionConfigs`), 8 `no-explicit-any` (choropleth ×2, composed, hover-geometry ×2, sankey ×3), 1 `no-unused-vars` (`center-stat.tsx` `valueClassName`). Not touched because each dep-array change is a behavioural change under React's rules (D-entry or own commit) and 6.4 is behaviour-neutral by definition. `showcase` lint runs with `--max-warnings 0`, so the showcase lint script fails today exactly as it did before Phase 6. Follow-up, not a gate item. | `npx eslint migrated` 2026-09-01: "18 problems (0 errors, 18 warnings)"; pre-refactor snapshot "24 problems (0 errors, 24 warnings)". |
| D471 | **D448 gate check answered: sunburst's keyboard-focus path and pointer-overlay path COULD drive hover into conflicting states — fixed with a pointer-owner guard, one behavioural commit.** Both paths write the same hover cell (`setHoveredArcIndex`) with no source tag: overlay `handleHitEnter`/`handleHitLeaveAll` and native `handleSunburstFocusChange`. Verified in `dist/renderer.js`: (a) every host render with a still-focused point re-emits `onFocusChange(point)` unconditionally (`:165-168`, the "restored" branch), and the chart's `definition` memo depends on `hoveredArc`, so a pointer hover on arc B triggered a render that re-asserted keyboard arc A — pointer hover snapped back to the keyboard arc on every move while the SVG held DOM focus; (b) `focusout` → `clearKeyboardFocus` → `onFocusChange(null)` (`:615`) cleared a live pointer hover on mousedown over the overlay (overlay paths are outside the host container, so the `relatedTarget` check does not save it). Fix: `pointerInsideStageRef` set/cleared by the overlay's enter/leave-all; `handleSunburstFocusChange` returns early while it is true. Pointer owns hover inside the stage, keyboard owns it outside; no change to pointer-only behaviour (the QA sunburst/sunchrome runs cover that). (c) Accepted, not fixed: the bench's synthetic no-coordinate drill click bubbles from the overlay path to the host container's `click` handler, which runs `pointsAtPointer(0,0)` after `zoomTo` and can write a spurious post-drill hover if the chart box sits within 48 px of the viewport origin — bench-only, coordinates the bench cannot supply (D384). Incidental fix in the same commit: `bench/app/src/scenarios/migrated-sunburst.tsx` queried `[data-ts-key="sunburst-arcs"]` but the mark id has carried `playKey` since C5d (`sunburst-arcs-${playKey}`), so `__benchDrilldown` had silently no-op'd since `4e5b237`; now matched by prefix. Also noted, not acted on: the D384 click-effect's deps include `zoomTo`, which re-identifies on every `zoomT` rAF frame, so it re-binds all N listeners ~45× per drill (churn, not a bug); and Tab-in dims ~75% of the chart with `focusRing:false` and nothing to explain why. | Explore agent trace 2026-09-01 (state writers `sunburst-chart.tsx:525-534, 889-919`), verified by the orchestrator against `dist/renderer.js:140-170, 265-320, 484-500, 612-615`; fix commit follows the 6.5 refactor commit. |
| D472 | **6.5 gate BLOCKED then FIXED FORWARD: the C5 motion renderer costs O(elements × points) per data update at 0.15.0; per-datum charts now gate the renderer on cardinality (`NATIVE_MOTION_MAX_POINTS = 200`).** *Finding.* The 10 paired migrated cells vs `docs/phase-5/BASELINE.md` §3b (n=7 medians, M1b/M1c/M3a): composed/1000 1849 / 314 / 190 ms vs 1664.8 / 123.8 / 32.2 (**+11% / +154% / +490%**); scatter/1000 1818 / 220 / 202 vs 1258.6 / 141.1 / 28.8 (**+44% / +56% / +602%**); line/1000 M1c 98.4 vs 76.7 (+28%) and area/1000 105.8 vs 88.0 (+20%) with M3a flat; bar/100 M1c +10%, M3a +5% (inside the flag). *Bisect (plan failure protocol, `showcase/migrated` checked out per rung, same harness, n=7):* C4 `fa18bd9` composed 1670 / 132 / 34.6, scatter 1237 / 133 / 32.4 — clean; C5 `58d24e7` composed 1851 / 314 / 189, scatter 1822 / 224 / 205 — regressed; C6 `e4cad6e` identical to C5. The regression lands in C5 (D432, the `<RendererChart renderer={chartMotionRenderer()}>` switch), not in C6 or 6.4. *Root cause, read in `dist/motion.js`:* every non-resize update goes `render()` → `animateSvg({phase:"update"})` → `reconcileMotionSvg` → `reconcileMotionElement` → `addUpdateTrack`, which calls `elementTimingContext(current,"update",scene)` for **every keyed element before looking at attribute diffs**; for non-bar/line marks that is `guideOrMarkTimingContext` → `motionPointForKey(scene.points,key)` (linear scan, no early exit) + `scene.points.filter(markId)` + a `motionMarkId` Set over all points — O(elements × points) per swap. A mark's `motion: false` does not help: disabled tracks are filtered only later in `runTracks`. M1c inherits the same scans through the initial phase (`createBarTracks` / `createPresentationTracks`). (Renderer identity across the threshold: `dist/renderer.js:103-111` destroys and remounts the surface when `options.renderer` changes, so a dataset crossing 200 datums pays one remount, then stays in its regime.) *Second, separate cause for scatter M1b:* C5 extended scatter's "ready" deadline to `revealDurationMs + ENTER_TWEEN_MS` (wait for actual fade completion); legacy fires "ready" from `setTimeout(animationDuration)` regardless of the marker fade tail (`use-chart-phase-orchestrator.ts:166-169`), so the C5 deadline was a +500 ms contract change, not parity. *Calibration (C4 static vs HEAD motion, same cells, n=7):* scatter/200 M1c +8%, M3a 0%; scatter/400 M1c +16%, **M3a +49%**; composed/200 M1c +24%, M3a −1%; composed/400 M1c +46%, **M3a +35%**. The update path holds at 200 datums and breaks by 400; 200 is the largest measured n where M3a is flat. *Fix (one commit, no harness change):* `internal/motion-renderer.ts` gains `chartRendererFor(pointCount)` — the shared `motion({initial:"always"})` instance up to `NATIVE_MOTION_MAX_POINTS`, and above it `createSvgChartRenderer(renderChartSvg)` from `@tanstack/charts/svg/renderer`, which is exactly the renderer `<Chart>` builds (react-charts `Chart.js`) — i.e. the C4 path, same gradients/tooltip/focus support, reads `definition.svgAnimation` (`renderer.js:125`). Applied to the six per-datum-keyed charts: scatter, composed, line, area, bar, candlestick (`renderer={chartRendererFor<…>(renderData.length)}`). Line/area/composed restore the pre-C5 `svgAnimation: yDomainTweenGateActive ? {duration, easing: bezierEasing} : false` gate alongside the motion callback, so the y-domain tween survives in both regimes. Scatter's deadline returns to `revealDurationMs` (legacy contract); its fade tail now keeps playing under "ready", as legacy's framer markers do. Both renderer instances are module-level, so identity is stable within a regime; crossing the threshold on a data change swaps renderer identity once (a remount of the surface, same as any `renderer` prop change). *Re-run of the failing cells with the fix (HEAD, n=7):* composed/1000 1666.1 / 130.3 / 34.3 (+0.1% / +5.2% / +6.5%), scatter/1000 1197.5 / 99.8 / 32.5 (−4.9% / **−29.3%** / +13.0% — the one out-of-band delta is a speed-up: no per-point motion tracks are built above the threshold), line/1000 1119.0 / 85.8 / 32.6 (+0.3% / +11.9% / +0.3%), area/1000 1153.4 / 91.9 / 32.6 (+0.2% / +4.5% / +1.2%); all inside D273's ±20% flag except scatter M1c downward; `skipped: []`, n=7 on every M1 channel, 210 M3a samples. *Accepted deviations, version-stamped `@tanstack/charts@0.15.0` (D417 — re-verify at the next pin bump):* (1) above 200 datums these six charts play **no native entrance motion** (static paint; reveal clips and the legacy phase timers still run), and composed/200 keeps a +24% M1c on the initial render (600 scene points across three series — the initial-phase scan, not the update path; M3a −1%); (2) the fix is app-side; the library-side fix is an index over `scene.points` keyed by mark id in `elementTimingContext`, which is upstream's to make. *Bundle cost:* the static renderer import adds 0.6–1.3% gzip on the six charts (`pnpm bench:bundles` re-run; `guard:bundle` OK against the +3% pins, which are **not** re-pinned). |
| D473 | **6.5 gate: pie/ring pointer detection returns to the app (`pointer:false` + `internal/polar-hit.ts`), superseding C5c/D447.** Native focus at 0.15.0 re-resolves the pointer against the motion surface's in-flight presentation points on every definition update; pie/ring's hover-driven rebuild (grow/translate/fade) moved the arc out from under the pointer, emitted `onFocusChange(null)`, and the unhover rebuild re-hit it — an unbounded hover/unhover loop (React #185). Fix: `pointer:false, focusRing:false, tooltip:false` and the wrapper's own pointer handlers resolved against the static authored geometry. Sanctioned extension per PLAN (`pointer:false` + app-owned pointer). Version-stamped 0.15.0 (D417 expiry rule). *Files:* pie-chart.tsx:570, ring-chart.tsx:670, internal/polar-hit.ts. |
| D474 | **6.5 gate: controlled `brushX` ranges are snapped onto members of `values` (`snapBrushRangeToValues`).** Native brushX validates that both endpoints are members of its `values` array; legacy brush emitted interpolated Dates, which the native control rejected (selection reset). Snapping is app-side and lossless for the round trip through `useBrushSelection`. Stamped 0.15.0. *Files:* internal/brush-selection.ts:178, area-chart.tsx:650, line-chart.tsx:444. |
| D475 | **6.5 gate: native tooltip anchored at the plot top edge, x at the focused point/group/value (`anchorX` option of `buildNativeTooltipExtension`).** Legacy tooltip-chrome placed the box at `top = margin.top`, `left = pointX + 16` (flipped near the right edge); the native default anchored to the point y, which moved the panel with the series and broke every hover cell. Function anchor `{x, y: plot.y - offset}` + placement `["bottom-right","bottom-left"]` reproduces legacy exactly. Sankey/choropleth keep their own placement. Stamped 0.15.0. *Files:* internal/native-tooltip.tsx, area-chart.tsx:1193. |
| D476 | **6.5 gate: tooltip panel chrome is one app-owned wrapper (`bkm-tooltip-panel`, `renderSeriesTooltipBody`).** `TooltipContent` carries no panel of its own; per-chart `className`/`panelStyle`/`backgroundColor` are folded into the wrapper so every chart renders the legacy panel box (padding, radius, shadow) through `renderTooltipBody`. *Files:* bar-chart.tsx:1573, candlestick-chart.tsx. |
| D477 | **6.5 gate: the D472 renderer gate counts rendered primitives, not data rows, for squares bars.** `barSquaresMark` emits rows × squares-series × columns rects, so a 100-row chart carried thousands of tweened rects through the motion surface; the estimate now mirrors the mark's column layout. *Files:* bar-chart.tsx:1707. |
| D478 | **6.5 gate: sankey hover chrome binds on `mouseenter` and hit-tests links against the bump curve, last-painted first.** Legacy semantics: the topmost link wins and node hover takes precedence. Pointer math is app-owned (sankey keeps `pointer:false`, D-C5). *Files:* internal/sankey-hover-chrome.ts:204. |
| D479 | **6.5 gate: the date pill overlay host mounts via a callback ref (`useDatePillOverlay.overlayHostRef`).** The pill's host `<div>` was attached by effect order and missed the first render on the motion renderer; a callback ref makes mount independent of renderer identity. No reach-in: the host is app-owned JSX. *Files:* internal/hover-geometry.ts:340. |
| D480 | **6.5 gate: legend-hover dim is a plain per-mark opacity term (`strokeOpacity`/`fillOpacity`/whole-series `opacity`), replacing the C1 programmatic-focus route.** Programmatic focus dimmed by `{focus:'unmatched'}`, which is row-scoped and collided with pointer focus (legend hover cleared the crosshair row). The dim term is now derived from `legendHoveredKey` per mark; depth/squares/trimmed bar marks gained an `opacity` option so legacy's dimmed depth chrome is reproduced. *Files:* area-chart.tsx:896, candlestick-chart.tsx:133, composed-chart.tsx:611, internal/series-bar-mark.ts:28, bar-depth-marks.ts:64, bar-squares-mark.ts:13, bar-trimmed-mark.ts:13, hover-geometry.ts:220. |
| D481 | **6.5 gate: depth-bar primitive count feeds the renderer gate (6 back-face nodes + trimmed front + glass per row).** Same miscount shape as D477: ~800 `createMark` primitives at 100 rows, all tweened through the motion surface's generic element path. *Files:* bar-chart.tsx:133, :1707. |
| D482 | **6.5 gate: candlestick snaps (no tween) after the B4 reveal has settled (`revealSettledRef`).** With the motion renderer, every post-reveal reconcile re-tweened wick/body geometry; legacy only animates the reveal. `candleMotion` reads the settled flag per reconcile and resets on reveal replay. Stamped 0.15.0. *Files:* candlestick-chart.tsx:540. |
| D483 | **6.5 gate: the renderer choice is latched at mount (`useChartRenderer(estimate)` in `internal/motion-renderer.ts`), and the depth/squares primitive estimate is prop-independent.** D472/D477/D481 chose the renderer per render from the current primitive count, so a prop toggle that crossed 200 primitives (bardepth `depth-off`: 246,134 px) swapped renderer identity, which remounts the surface (`dist/renderer.js:103-111`) and replays the entrance (`dist/motion.js:605-612`). The hook seeds once from the mount-time estimate and never re-reads; the depth branch keys on `hasBarDepth` (declared) rather than `barDepthEnabled` (declared and currently applicable). All six callers (bar/area/line/scatter/composed/candlestick) hoisted above conditional JSX (rules of hooks). bardepth: six cells PASS, depth-off 273 px. Stamped 0.15.0. \| motion-renderer.ts; bar-chart.tsx:1707-1748; `$S/qa/bardepth-100.log`. |
| D484 | **6.5 gate: sankey hover dim timing and link hit test — timing matched (nodes/labels `1.1s cubic-bezier(0.85,0,0.15,1)`, links stay `0.18s ease-out`), hit test is the exact d3-sankey `sankeyLinkHorizontal` cubic (bisection on x, `LINK_HIT_SLOP` 0). Per-node dim STAGGER is ACCEPT-WITH-LOG.** A Playwright probe at the harness hover-30/50/70 points resolves the identical link in both impls (16, 16, 22) with identical full-opacity endpoint nodes. The residual hover-30/70 cells (11,135 / 7,645 px) are inside the inherited ranges ([3506,28944] / [1270,33134]); the crop shows legacy mid-dim with labels still dark at the capture instant while migrated dims uniformly — legacy staggers per node, which a static CSS transition cannot express and native motion stagger does not apply to hover dim at 0.15.0. Sankey tooltip also sits ~2 px lower than legacy (Phase 7 polish). Stamped 0.15.0. \| internal/sankey-hover-chrome.ts; styles.css sankey block; `$S/qa/sankey-33.log`. |
| D485 | **6.5 gate: multi-axis area hover dot reprojects through the series' own y-axis (`projectorFor(area.yAxisId)`).** The focus dot for a secondary-axis series resolved against the primary domain. refareamultiaxis hover cells 8845/9045/9331 → 3527/3802/4242 px, overall PASS. \| area-chart.tsx:941-965. |
| D486 | **6.5 gate: native tooltip geometry — plot-top anchor via function anchor + `["bottom-right","bottom-left"]` placement (extends D475), and the panel-width root cause.** `"right"` placement centres the panel on the anchor y (`dist/tooltip-placement.js`), which put it ~32 px above legacy's `top = margin.top`; `"bottom-right"` gives `top = anchorY + gap` and `left = anchorX + gap`, flipping to `bottom-left` exactly like legacy. `buildNativeTooltipExtension` now takes a required `anchorX: 'point' \| 'group-center' \| 'value' \| fn` and re-implements the built-in x resolutions (a function anchor only receives `points`/context). The +5/6 px panel width was a font fallback: `--ts-chart-tooltip-font: inherit` is a no-op custom property (spec: `inherit` re-triggers inheritance, never substitutes), so the library's inline `font: var(...)` fell to `system-ui`; fixed with `font-family: inherit !important` on `.bkm-native-tooltip` (styles.css is the sanctioned surface, D467). Δtop = 0 on area/line/bar/candlestick/scatter/liveline in both renderer regimes; Δleft 2–4 px. Stamped 0.15.0. \| internal/native-tooltip.tsx; seven chart call sites; styles.css:219+. |
| D487 | **6.5 gate: radar hover chrome native — stroke-width 3 on hover, glow + 1.05 scale-pop via `createMark` className (`bkm-radar-area[--hovered]`), no reach-in.** `withMarkNodeClassName` stamps `SceneNodeBase.className` (`dist/types.d.ts:838`) onto `radialArea`'s scene nodes; `polarValueKey()` reproduces `dist/scales.js`'s non-exported `valueKey()` string branch so the hovered series key matches. The path's local origin is the polar centre (`polar()` translates the ancestor group), so `transform-origin: 0 0` scales about the centre. Supersedes D445's exclusion. Stamped 0.15.0. \| radar-chart.tsx; styles.css radar block; `$S/qa/radar-7.log`, radar/6 in the final matrix. |
| D488 | **6.5 gate: marker dim + blur is native-expressible via `createMark` className (`bkm-marker-base`, `--dim`), replacing the `states`-only route that could not carry `filter`.** `withMarkerBaseClassName` re-implements `inner.render` to append the class on `dot()`'s group node (mark `states` cannot carry `filter`, `dist/types.d.ts:72-89`; `renderStyle` never emits it). The crisp active point is a second `dot()` at 1.35 r wrapped in `whenFocused({match, retarget:true})` — `whenFocused` marks are excluded from the focus/tooltip point pool (`dist/scene.js:106`), so no duplicate tooltip rows. markers hover 12.3–13.6 k → 5.1–6.1 k px (inside inherited [3574,8430] / [840,5817] / [3328,8004]); scatter hover-30/50 143 k → ≈ settled floor (4330 px, the inherited baseline itself). Not done, Phase 7: scatter's S8 y-gradient path dim has no blur; `series.outlineWidth/outlineColor` have no native channel. Legend-hover 0.52–0.60 % is blur anti-aliasing (ACCEPT-WITH-LOG). Stamped 0.15.0. \| internal/series-marker-mark.ts; line-chart.tsx:597; scatter-chart.tsx; styles.css marker block. |
| D489 | **6.5 gate: axis tick label baseline `dy = marginBottom − 26` (was −25) across axis-ticks/bar; live-line axes rebuilt natively (`format` via `liveXAxis.formatTime`, two-rect white mask for the left margin column and x-label band, y `dy: 10`, `edgeOpacity` on x labels, mono stack via a `[data-bkm-chart="liveline"]` rule).** griddefault/line/area/bar settled → 0.000 %; composed 0.129 → 0.085 %; candlestick 0.316 → 0.272 %. \| internal/axis-ticks.ts:572,610; bar-chart.tsx:1008; live-line-chart.tsx:729-935; styles.css:1043-1060. |
| D490 | **ACCEPT-WITH-LOG (0.15.0): native `group-x` focus tie-break at exact midpoints.** Legacy resolves with integer-ms strict `>`; native resolves float distance with `>=`. `chart-focus-kit.ts:39` adds a 1e-6 epsilon so equal distances prefer the earlier index like legacy; sub-ms float ties remain native's. No harness cell lands on an exact midpoint. \| internal/chart-focus-kit.ts:39. |
| D491 | **ACCEPT-WITH-LOG: grid lines paint over bands in bar charts, as in the inherited baseline (~1.9 k px, pre-existing since Phase 5).** Not a Phase-6 regression; z-order is mark order in both impls. \| inherited matrix. |
| D492 | **6.5 gate: legend-hover dim of non-hovered marker series is a static `fillOpacity: 0.5` on the base marker mark (`buildMarkerMarks(..., legendHoveredKey)`), consistent with D480.** legend-hover-1 10,656 → 8,227 px before D488 closed the rest. \| internal/series-marker-mark.ts; line-chart.tsx:597. |
| D493 | **6.5 gate: choropleth hover highlight + tooltip anchor — FIX.** (a) `choroplethSceneKey()` now reproduces the library's `valueKey` string wrapping (`string:${len}:${value}`, `dist/scales.js`, not exported) so `resolveFeatureAlpha()` matches the hovered feature; before, every feature fell to dim opacity on hover — visible only on large-footprint features (USA). Same bug shape as radar `polarValueKey` (D487). (b) Tooltip anchor = `project(geoCentroid(feature))` (legacy parity, `choropleth-feature.tsx:236`) instead of the library's projected-area `path.centroid`, which Alaska drags NW on the USA MultiPolygon (~40px). choropleth/100 h30 791px, radar/6 h50 758px — all PASS inside inherited ranges. 0.15.0. |
| D494 | **6.5 gate: line hover highlight band fed native focus (React #185) — FIX.** `buildHighlightBandMarks` re-slices a plain `lineY` to `[i-1,i+1]` per hover; `lineY` nodes always carry `interaction:{points,affinity:"x"}` (`dist/line.js`), so the moving band entered the `group-x` candidate pool and `nearest.js` stroke targets, and focus re-resolved against its own movement → `setHoveredIndex` loop (same shape as D473). New `withoutInteraction()` `createMark` wrapper (`internal/hover-geometry.ts`) strips `interaction` from rendered nodes; band is display-only. Bench migrated line/1000: console errors 7→0, tooltip appears; scatter/area/1000 clean. QA line/1000 all cells ≤0.13%. 0.15.0. |
| D495 | **6.5 gate: candlestick hover dim double-composite — FIX.** `wicksMark` and `bodiesMark` are separate `createMark()` marks, each dimming its own `<rect>` via `states` `opacity`; the wick spanned low→high under the body, so the overlap composited twice (isolated test: G=154 vs legacy single `<g opacity>` G=191). `states` restyles leaf nodes only, so group opacity is not expressible natively; fix is geometric: wick split into upper/lower segments that stop at the body edges (no overlap), reveal collapse preserved per segment, single-rect fallback when open/close are absent. candlestick/1000 h30/h50/h70 6126/5037/4998 → 1877/818/729 (below every inherited history value); settled 3263 in range. candlelegend settled 3199→3497 (wick-seam AA), accepted. Phase 7: small-n hover-dim non-application and the n=30 date-pill malformation reported by G are unverified and off-roster. 0.15.0. |
| D496 | **6.5 gate: liveline y-scale nicing regression + ticks + date pill — FIX.** C5 (58d24e7) dropped `.nice()` from the mark-facing y scale claiming motion.md's reproject-affine rule forbids it; wrong — any two linear scales over the same range are affine-related, and legacy nices (bklit `live-line-chart.tsx:249-253,457-463`), as did the Phase-5 snapshot (f8e8c7a:539,643). Effect: whole trace on a different vertical extent, ~4000px settled diff vs inherited ~160. Restored `.nice()`; tick ladder sized from the niced domain via a verbatim `pickNiceInterval` port (150/200/250 parity); hover date pill restored (`dateLabelsForPill` was empty and the pill was gated on `showDatePill`, a concept legacy `LiveXAxis` lacks). Cells after restore: settled 4197→1278 px, hover 5145-5410→2369-2574 px (all PASS; inherited-range residual is y-label font metrics, D497). 0.15.0. |
| D497 | **6.5 gate: liveline y tick labels font metrics — FIX.** Legacy `LiveYAxis` labels are `font-mono text-xs`, and the bench app resolves `--font-mono` to the locally loaded Geist Mono (`bench/app/src/styles.css:37-48`); the migrated liveline rule (`styles.css` `[data-bkm-chart="liveline"] .ts-chart__axes text[dominant-baseline="middle"]`) stacked ui-monospace/SFMono/Menlo only, so glyphs were ~1px shorter, ~2px narrower and the row centre 2-3px high. Stack now `var(--font-mono, "Geist Mono", ...)`; `dy` 10→14 (label rows now within 1px: A y62-72/134-144/207-217, B y62-72/135-145/207-217). X labels already matched box-for-box (their diff is timestamp text, inherent to two live tabs). Cells: settled 1114-1120 px, hover 2053-2383 px, all PASS the 4800 px gate; still above the inherited Phase-5 ranges (settled [158,708], hover ≤1357) — the remaining diff is sub-pixel glyph anti-aliasing on the y labels (x-extent 46 vs 48 px) and the x-axis timestamp text, which two independent live tabs never share. ACCEPT-WITH-LOG for the residual; Phase 7 item 0.15.0. |
| D498 | **Final 6.5 QA sweep accepted under the mode-distribution rule (D402/D403).** Run `docs/phase-6/gate/runs/2026-09-02T20-09-29-038Z` (label final-gate, 4 workers, wall-clock 8m40s): 43 runs / 190 cells, tooltip failures 0, console errors 0. 13 cells cross the 0.5 % line and 5 sit outside their history range; every one was re-checked by hand against the Phase-5 final matrix. Above-line cells all lie inside their inherited ranges: markers/100 hover-30/70 (6045/6147 px vs [3574,8430]/[3328,8004]), markers legend-hover-0/1/clear (4969/5116/5742 vs Phase-6 history [3729,18168]), sankey/33 hover-30/70 (11017/5021 vs [3506,28944]/[1270,33134]), scatter and scattermultiaxis hover-70 (6274/6349 vs [1884,23324]/[2123,129924]). barloading/100 is an inherited FAIL (loading-animation phase, 15–17 %); its hover-30/50 exceed the old ceiling by 0.2 %/3.6 % of a 160k-px cell and hover-70 is below the floor — animation phase, not a regression. The 5 out-of-range cells are all *below* the floor: candlestick/1000 hover-30/50/70 (1848/689/591, D495) and radar/6 hover-50 (757, D493). No FIX iteration. Residual noise cells go to the Phase-7 list. *Files:* `docs/phase-6/gate/latest/qa-matrix.{json,md}`. Version 0.15.0. |
| D499 | **Bundle gate at the final sweep: 43 pins, 0 FAIL, Σ gzip +1.2 % vs the `c1e9ced` pins; pins deliberately NOT raised.** `pnpm gate:bundle` (run `2026-09-02T20-09-29-038Z`): 104 bundles measured, 0 failed; every migrated scenario is +0.7…+2.0 % (≈1.2–2.2 kB gzip), max `migrated/barloading` +1.97 % (84 398 → 86 059 B). The delta is uniform across all 43 scenarios because it is shared `internal/` code from the gate-fix window (D472 cardinality gate, D494 `withoutInteraction`, D495 wick split, `polar-hit.ts`), not any one chart. Inside the +3 % tolerance, so `bench/results/bundle-gate.json` stays pinned at `c1e9ced` — raising pins needs a D-entry and +1.2 % does not earn one; the remaining ~1 % headroom is Phase 7's budget. *Files:* `docs/phase-6/gate/latest/bundle.{json,md}`, `docs/phase-6/BENCHMARKS.md` §1.5. Version 0.15.0. |
| D500 | **Final bench gate: 29/29 cells, no gated regression; two legacy-arm baseline omissions fixed in the gate driver, not the harness.** `pnpm gate:bench -- --cells all` (run `2026-09-02T20-09-29-038Z`, results `bench/results/2026-09-02T20-57-15-845Z` = new `latest.json`): every M1b/M1c/M3a delta inside D273 ±20 % except two speed-ups — `bklit/line/10000` M1c −25.5 % (legacy arm, same drift as §2.1's −22.7 %) and `migrated/scatter/1000` M1c −21.1 % (D472). Migrated paired cells vs §3b: M1b ≤ +0.2 %, M1c +5…+13 %, M3a +0.3…+6.2 %; `migrated/line/1000` 0 console errors + tooltip (was 7 / none before D494). The driver had hard-coded console-error baseline 0 and tooltip baseline `true`; the *legacy* `bklit/bar/1000`/`10000` emit 514 742 / 1 540 000 negative-`<rect>` attribute errors in the Phase-5 close run itself (516 148 / 1 540 000 now) and `bklit/line/1000` has been tooltip-false in every recorded run since 2026-08-23 — inherited on untouched code. `qa/gate/bench-baseline.json` now carries both; `qa/gate/run-bench.mjs` flags errors only past the D273 tolerance over a non-zero baseline and tooltips only on `true → false`. The run's `bench.json` was re-derived under that rule (`postProcessed` field); measurements untouched. Machine was not fully quiet (bundle + checks overlapped the first minutes; user call) — clean-room re-run is a Phase-7 item. *Files:* `qa/gate/run-bench.mjs`, `qa/gate/bench-baseline.json`, `bench/results/latest.json`, `docs/phase-6/gate/latest/bench.{json,md}`, `docs/phase-6/BENCHMARKS.md` §2.3. Version 0.15.0. |
| D501 | **Gate checks green; lint debt 18 → 24 warnings, all `no-explicit-any` on one wrapper.** `pnpm gate:checks` after the sweep: tsc 0 errors, bench/app build ok, eslint 0 errors / 24 warnings, reach-in census 79 sites / 15 files / 0 failures, bundle guard 43 pins OK. The fix window added 8 warnings: 2 unused type imports in `scatter-chart.tsx` (removed) and 6 `any`s on `withMarkerBaseClassName` in `internal/series-marker-mark.ts` (D452 shared wrapper). A generic `<TDatum, TX, TY>` signature fails `createMark`'s `ChartValue` constraint (TS2344/TS2322), so the `any`s stay, matching `hover-geometry.ts`'s two pre-existing ones; carried as lint debt with D470. Both edits are type-only: the bench/app asset hash `index-CnKMuzu2.js` is byte-identical before and after, so the QA sweep (D498) and bench (D500) taken before the edits stand. *Files:* `showcase/migrated/charts/scatter-chart.tsx`, `docs/phase-6/gate/latest/checks.json`. Version 0.15.0. |
| D502 | **Phase 6 closed; Phase-7 list fixed at the gate.** Carried forward, none blocking: (1) bench speed thresholds — clean-room `--all` re-run and any M1c tightening (user priority: native-first now, speed polish in Phase 7); (2) liveline/100 residual above inherited (settled ~1.1k px vs [158,708]): y-label sub-pixel AA (x-extent 46 vs 48 px) + timestamp text, D497; (3) sankey/33 hover-30 bimodal history (3.7k/10k/16k/24k) — tooltip ~2 px offset; (4) markers/100 legend-hover cells ~+1.3k px over mode; (5) scatter/scattermultiaxis hover-70 timing tail; (6) candlestick small-n hover-dim non-application and n=30 date-pill malformation (reported by an executor, unverified, off-roster); (7) bodiesMark pattern/inset rect double-composite under legend dim (candlelegend settled 3497 = top of range); (8) scatter S8 gradient blur and `outlineWidth`/`outlineColor` channels absent at 0.15.0; (9) user decisions still open: sunburst `hoverPop` inert (D450) and uncontrolled `<ChartBrush>` (D455); (10) lint debt 24 warnings (D470/D501); (11) barloading loading-skeleton animation phase — inherited FAIL cells, unchanged since Phase 5; (12) bundle pins at `c1e9ced` with ~1 % headroom (D499). Version 0.15.0. |
