# Phase 5 Decision Log

> D-number convention continues from `docs/phase-4/LOG.md`. The highest D-number recorded across `docs/phase-*/LOG.md` at the time this file was created is **D358** (`docs/phase-4/LOG.md`, 2026-08-26 — the P6.4/4.4.5 final-pass close entry). **The next entry in this file is D359.** Accepted parity deviations (5.2.1 rulings, D1–D17 resolutions) and skipped-with-reason tasks (Definition of Done) get logged here.

| D# | Date | Decision | Rationale |
|---|---|---|---|
| **D359** | 2026-08-26 | **5.0.1 re-vendor: the migration now runs on the PUBLISHED runtime, `@tanstack/charts@0.15.0` + `@tanstack/react-charts@0.15.0`, pinned exact. Supersedes the D146/D238 source-clone pin.** The vendored source clone `showcase/repos/tanstack-charts` (`a285ce7`, v0.14.0-era) is archived at `local_cache/tanstack-charts-a285ce7-v0.14.0/` for diffing, and is no longer resolved by anything. Surface changed: `showcase/package.json` + `bench/app/package.json` gained the two exact pins; **44** `@tanstack/*` path mappings deleted from `showcase/tsconfig.json` and **95** from `bench/app/tsconfig.json`; the `aliasesFromExports` alias generators deleted from `showcase/next.config.mjs` and `bench/app/vite.config.ts`; `showcase/scripts/clone-repos.sh` no longer clones or pins TanStack (bklit-ui clone unchanged); `bench/measure-bundle.mjs` repointed at `bench/app/node_modules/@tanstack/*` and taught to read conditional `exports` objects (`{types, import}`) instead of the clone's plain-string targets. \| The clone was never the published artifact — it was a git checkout of a private monorepo whose packages self-report `version: 0.0.0`, resolved through ~139 hand-maintained subpath aliases across four config files. Every one of those aliases was a chance to drift from what a real consumer gets, and D238 records exactly that failure mode ("an unpinned tip-of-main clone here is what broke Vercel production builds"). Resolving from `node_modules` through the package's own `exports` map removes the entire alias layer and makes the thing under test the thing users install. **The upgrade is additive, verified before committing to it:** the clone's and 0.15.0's `exports` maps are byte-identical in their key sets (both 110 subpaths), and the exported-symbol diff is +24/−5 with all five removals (`bandAxes`, `bandXAxes`, `bandYAxes`, `linearAxes`, `utcXAxes`) living in the clone's test-only `src/test-scales.ts` and referenced nowhere in our code. Cost recorded honestly: **M2c bundle-size numbers are no longer comparable across this boundary** — `measure-bundle.mjs` now bundles pre-built `dist` JS where it used to bundle TS source. |
| **D360** | 2026-08-26 | **5.0.2 breaking-change sweep: ZERO fallout. 43/43 distinct (subpath, binding) pairs across 92 import statements in 38 files resolve unchanged on 0.15.0.** Matrix in `research/phase-5/06-upgrade-compat.md`. No MISSING, no RENAMED, no breaking SIGNATURE-CHANGED, no UNRESOLVED. Independently corroborated by the lead: `showcase` and `bench/app` both typecheck at `EXIT=0` with zero errors. \| Every 0.14→0.15 signature change found was strictly additive — new *optional* fields/params/generics, never a removal or a rename: `createMark` gained an optional `renderer` param plus defaulted scale-id generics; `ChartMarkMotionOptions` now extends `ChartMarkOptions` (rippling an optional `renderer` into `BarYOptions`/`DotOptions`/`LinkOptions`/`RectOptions`/`SankeyDiagramOptions`); `CartesianScaleBindings` (`xScale?`/`yScale?`) was added to several mark option interfaces; `PolarMark` gained optional `renderer`/scale-id fields. **One forward-looking finding, deliberately NOT acted on under 5.0.2's "mechanical renames only" scope:** 0.15.0 restructures `ChartSpecForMarks` into `CanonicalChartScaleSpec \| LegacyChartScaleSpec`, JSDoc-deprecating top-level `x`/`y` in favour of a `scales: {x, y}` map — the legacy form still typechecks everywhere we use it. `polar()`'s top-level `angle`/`radius` (`radar-chart.tsx:409–411`) carries the same deprecation. Queued as low-priority cleanup for whichever later batch touches those call sites; a deprecation is not a break, and mixing it into the upgrade would have made the parity sweep unattributable. |
| **D361** | 2026-08-26 | **Gate 5.0 PASSED on independently verified math. Typecheck EXIT=0 (both apps), both builds green, and the full 45-run QA parity sweep on 0.15.0 reproduces the phase-4 baseline exactly: 42 PASS / 3 FAIL, with ZERO new gate breaches — no cell crossed the 0.5% threshold that did not already.** The 3 FAILs are precisely the 3 baselines already ruled non-parity-by-design: `candletween` n=1000 (pixel-invalid by design, D330 §2 — gate of record is `qa/k4-tween-probe.mjs`), `ring` n=1000 (invalid instrument, D335 — gate of record is `ring` n=4, which PASSES), `barloading` n=100 (not a parity gate by design, D341). Per-cell deltas vs. `docs/phase-5/BASELINE.md` computed by the lead, not reported by an agent. \| Verified rather than asserted, per the standing rule that agents omit failures. The strongest evidence that the runtime swap is behaviour-neutral is not the pass count but the **byte-identical cells**: `line`, `segment`, `griddefault`, `funnel`, `funnelvertical`, `gaugelinear`, `heatmap`, `sunburst` (both densities), `sunchrome`, `radar`, `choropleth`, `composedstacked` and others reproduce all four probes to four decimals (`+0.0000`) — a number that reproduces history to four decimals is the trustworthy kind (D265 §5). Residual movement is confined to hover probes, is bidirectional (`scatter` h30 −0.0762, `composedmultiaxis` h50 +0.1223), and stays an order of magnitude under gate; that is ordinary hover-probe variance, not runtime drift. Two **pre-existing** `bench/app` typecheck errors were found and fixed in passing — they were never caught because phase 4 only ever typechecked `showcase/`: `@types/d3-shape` was resolving to the ancient **1.3.12** hoisted by `@visx/shape` (no `curveBumpX`), fixed by pinning `@types/d3-shape@^3.1.6` in `bench/app`; and `tanstack-sankey.tsx`'s positionless ceiling mark was annotated as a bare `ChartMark`, whose scale-id generics default to `'x'`/`'y'` and therefore reject the spec's `x: null`/`y: null`, fixed by declaring `never`/`never`. Both were confirmed pre-existing, NOT upgrade fallout, by diffing `ChartXSpec` between the archived clone and 0.15.0 — the type is character-for-character identical in both. |
| **D362** | 2026-08-26 | **The gate map in `research/phase-5/02-visx-removal.md` is void; `docs/phase-5/GATE-MAP.md` replaces it as the single source of truth for what "gated green" means in 5.3.3.** 02 §"Gates & verification plan" records heatmap / pie / choropleth as failing baselines and instructs that post-removal diffs be measured against those failures. All three were resolved before phase-4 close (D358): `pie` worst cell 0.0138, `choropleth` 0.1470, `heatmap` 0.4277 — all PASS. Diffs in Phase 5 are taken against `docs/phase-5/BASELINE.md` §1 and nothing else. The new map also fixes the mandated density per chart (45 runs), so a run at an unmandated density cannot be reported as a gate result. \| 02 was written mid-phase-4 and its baselines were overtaken by the F2/F3 fix dispatches; left in place it would have licensed a real regression — a chart could drift from PASS back toward its old FAIL and still be scored "no worse than baseline". Alongside the correction the lead computed **headroom to the 0.5% gate** for all 42 parity charts, which produced a finding that changes batch sequencing: the three tightest-headroom charts are each the primary target of one of the three riskiest deviations — `candlestick` at **0.0047%** headroom (under 1% of the gate) is D7's tooltip migration, already flagged "highest parity risk"; `scatter` at 0.0490% is D10/D11; `heatmap` at 0.0723% is D2's full wrapper re-scope, which changes binning, colour AND stagger at once. Those three are therefore put under a revert-first policy with pre+post captures in *every* batch that touches them, not only the batch that owns the change, and 5.2.2 is barred from stacking two such changes onto one chart within a single gate. |
| **D363** | 2026-08-26 | **Ruling 4 of 5 — Gauge linear: FIX (bring under `<Chart>`).** `PLAN-phase-5.md:77` left this open with no research behind either branch; the gap is now filled by `research/phase-5/08-gauge-linear.md`. `GaugeLinear` (`gauge.tsx:941–1234`) uses zero `@tanstack/charts` primitives — the bypass is ~100 lines: a raw `<svg>` block (`1160–1203`) plus a raw-DOM-query `useLayoutEffect` reveal wiring (`1091–1146`). It is ported to a custom mark emitting `polyline` scene nodes with explicit `path` strings, reusing `createNotchPath`/`computeLinearNotches` **verbatim**; `internal/gauge-notch.ts` and `internal/gauge-reveal.ts` need no change. Stock `rect` is explicitly rejected. Est. ~180–220 touched lines, net +60–90. \| **The mechanism is not speculative — it is already shipping in the same file.** Arc's `uniformWidth=true` case (`gauge.tsx:598–671`) is a custom `PolarMark` that emits `kind:"polyline"` nodes carrying a `path:` string built by the very same `createNotchPath`, and `dist/svg-renderer.js:43` honours `node.path ?? pointsPath(node.points)`. Lead-verified directly, because the scene node union (`area`, `chart-layer-renderer`, `dot`, `group`, `label`, `polyline`, `rect`, `rule` — `dist/types.d.ts`) contains **no `path` kind**, so "emit a custom path" needed proving rather than assuming; `polyline.path` and `area.path` are the sanctioned carriers and both render as `<path d>`. Linear is a strictly easier coordinate space than the polar case already solved. **Stock `rect` is rejected on verified geometry, not preference:** `dist/svg-renderer.js:56` renders `node.radius` as SVG `rx`, a circular corner, which cannot reproduce bklit's bespoke quadratic-Bézier fillet — and the frozen QA scenario sets `notchCornerRadius={3}` on **both** legs (`bench/app/src/scenarios/bklit-gaugelinear.tsx:111`, `migrated-gaugelinear.tsx:80`), so a `rect`-based port would hit that fillet on every notch. `rect` also cannot express trapezoids at all (`uniformWidth=false`). **ACCEPT was considered and rejected as buying nothing:** `gauge.tsx` imports only `@tanstack/*` today (verified — lines 111–115), so ACCEPT does not violate the DoD import allowlist; it would leave a permanent hole in the "native tanstack backend" goal and permanently disqualify this site from D4's `motion()` adoption, in exchange for deferral alone. **Acceptance criterion, and the real risk:** `gaugelinear` n=1000 currently reads **0.0000% on all four probes** — a perfect score, so this change has zero gate upside and pure downside. Parity rests on the path data being produced by the identical function; the live risk is therefore *viewport*, not geometry — the hand-rolled block sets its own `viewBox="0 0 w h"` with `overflow:visible`, and the port must reproduce that framing exactly under `svg.ts-chart`. Any non-zero movement on those four probes is a revert, not a fix-forward (T-tier policy, D362). |
| **D364** | 2026-08-26 | **Ruling 1 of 5 — Funnel: no-migration stands.** `funnel-chart.tsx` remains hand-rolled React SVG. Upstream case `125-sales-funnel` (PR #81, v0.11.0) is recorded as corroboration, not as a migration path. \| Confirmed gap, not a capability we are declining to use: upstream ships no funnel mark or series and has zero issues requesting one (`01:23`). Case-125 is composition-only — areaX trapezoids plus text, with case-local unexported layout math — and its scene nodes forbid the spring and div-hover affordances bklit's funnel actually needs (`01:26`). Migrating would mean reimplementing the layout math we cannot import, to land somewhere with *fewer* affordances than today. `PLAN-phase-5.md:20` already places funnel out of scope; this ruling records the evidence rather than changing the outcome. Both `funnel` and `funnelvertical` sit at **0.0000% on all four probes**, so there is no parity debt to repay either. |
| **D365** | 2026-08-26 | **Ruling 2 of 5 — Pinch: continuous centroid-anchored zoom replaces visx's quantised ±10% steps.** Logged as a deliberate, justified behavioural deviation rather than a parity miss. The hand-rolled matrix engine uses the d3-zoom formulation — `k·sqrt(distRatio)` with the anchor re-inverted each frame so one operation folds zoom, pan and anchor together. \| This is the one place in the visx removal where exact parity is *not* the target, so it must be logged rather than discovered later as drift. visx `<Zoom>` delegates to `@use-gesture/react@10.3.1`, whose default pinch quantises to ±10% per event at the gesture origin — a stepped, origin-locked feel that is an artefact of that library's event model, not a bklit design decision. The continuous centroid-anchored formulation is strictly smoother and is what every mapping library ships. Everything else in the gesture set stays bklit-exact and is *not* covered by this deviation: binary ±5% wheelDelta on the sign of `deltaY`, scale extent `[0.5, 4]`, `touchAction:'none'`, grab/grabbing cursors, the idle-only `transition: transform .18s ease-out`, and container `aria-hidden`. Pinch has **no automated gate** — no harness consumes `__benchZoomTo` and G2 has no tanstack zoom leg (D34) — so verification is scripted bklit-vs-migrated screenshots at named zoom states, and the absence of a gate is precisely why the deviation is written down here. The inherited WCAG gap (2.1.1/2.5.1/2.5.7 — pinch has no single-pointer alternative) is **not** resolved by this ruling: it predates the migration, parity comes first, and it is recorded as a fast-follow rather than silently carried. |
| **D366** | 2026-08-26 | **Ruling 3 of 5 — Passthrough: SPLIT, and the plan's framing is superseded.** `PLAN-phase-5.md:76` says "build on undocumented `url(#id)` SVG passthrough WITH guards (registry test + renderSvg fallback + solid-fill degradation)", inherited from `02:50-56`. On the pinned 0.15.0 that premise is **half wrong**, so the ruling splits by resource type. **(a) GRADIENTS — documented, first-class, no guards.** `url(#id)` against a chart-declared gradient is a published contract: `docs/guides/themes-and-styling.md:169` ("Use `url(#revenue-fill)` as the mark paint. Default SVG hosts emit and scope the resource"), `docs/reference/chart-spec.md:165`, and `docs/reference/rendering-and-export.md:262-276`, which states that **both** `renderChartSvg` and `renderChartSvgWithResources` emit declared gradients into `<defs>`, scope IDs with a sanitized `idPrefix`, **rewrite matching `url(#gradient-id)` paints**, and emit clip paths for groups with `clip` bounds — "without a custom `renderSvg`". Canvas is explicitly covered too: "Canvas consumes the same declared gradients and group clips." **(b) PATTERNS — still foreign passthrough, guards stand unchanged.** There is no pattern resource at this pin: `grep -c 'ChartPattern\|patterns?:' dist/types.d.ts` = **0**, and the export docs say "emit declared **linear gradients**", not resources generally. `<pattern>` defs remain ours to mount and reference by foreign id. \| Ruled from the shipped docs and typings rather than from `02`'s forensics, which were done against an unpublished clone and are now overtaken. **What this deletes:** guard #1 in `02` — "wrap `renderChartSvg`, splice `<defs>` after the opening `<svg>` tag, respect idPrefix" — is dead work for gradients; the default hosts already do exactly that, including the paint rewrite, and the project already imports the sanctioned entry (`gauge.tsx:115`, `renderChartSvgWithResources`). Writing that wrapper would have been effort spent reimplementing shipped behaviour, and `02`'s canvas hazard (a) — canvas nulling unknown ids into silent invisible fills — does not apply to declared gradients at all, since canvas consumes the same declarations. So **D10's scene-gradient work carries no passthrough risk and needs no guards**, which materially de-risks B2. **What this preserves:** every guard survives for the pattern half, and the risk there is now *sharper*, not vaguer — patterns are provably outside the declared-resource system rather than merely undocumented, so the canvas-invisible-fill and PNG-export holes are live for `internal/pattern-preset.tsx` and its 8 call sites. The dev-mode registry assertion, solid-fill degradation on non-SVG surfaces, and the static `url(#` registry test remain **required** for B6's pattern port. **Consequence for 5.1.2:** the drafted upstream RFC needs rewriting before it could ever be filed — its "document the `url(#id)` contract" half is already satisfied upstream; only the "first-class paint-server resources (patterns, not just linear gradients)" half is still live. Nothing is filed; 5.1.2 remains user-approval-gated and unactioned. |
| **D367** | 2026-08-26 | **Three research citations point at files that do not exist; corrected in `research/phase-5/go-to-plan.md` before they reached implementation.** (1) `adapter-shared.ts:32–45`, cited under D12 (`04:14`) as migrated code to delete, is a **library** file — `@tanstack/charts/dist/adapter-shared.js` — with zero references anywhere in `showcase/migrated`. (2) `tooltip-scheduler.ts:40`, cited under D7 (`04:9`) as a separate module, was folded into `internal/tooltip-chrome.ts` during phase 4 (its `:20` and `:375` comments say so). (3) `internal/visx-pattern-bridge.tsx`, cited under D5 (`02:9`) as "the ONE visx import site, 17 lines, `PatternCircles`+`PatternLines`", no longer exists — it too was folded, into `internal/pattern-preset.tsx` (`:9`), which now holds the sole `@visx/pattern` import at `:2–7` and re-exports **four** components: `PatternCircles`, `PatternHexagons`, `PatternLines`, `PatternWaves`. \| The go-to-plan is about to become the source of truth under the staleness rule (`PLAN-phase-5.md:114`), which forbids re-consulting research for code already touched — so a wrong path baked in here would never be re-checked against reality. Each error had a distinct cost: (1) sends an agent to delete a file inside `node_modules`, which either fails or corrupts the pinned runtime; (2) sends it hunting a module that was consolidated; (3) **understates the actual work** — T18 must port 4 pattern components, not 2, and `PatternHexagons`/`PatternWaves` are the two the research never mentioned. All three are the same failure mode: `04`/`02` were written against a phase-4 snapshot and phase 4 subsequently consolidated files without the research being re-run. Established at the same time, for the DoD's "zero `@visx/*` imports in migrated code" criterion: exactly **5** real import/export statements remain across **2** files — `pattern-preset.tsx:2–7` (`@visx/pattern`, T18) and `choropleth-chart.tsx:29,30,31,224` + `index.ts:223` (`@visx/zoom`, T19). Every other `@visx` match in `showcase/migrated` is prose inside comments, which is why a naive `grep -c` overstates the surface and must not be used as the audit instrument at 5.3.5. |
| **D368** | 2026-08-26 | **Ruling 5 of 5 — Heatmap: MIGRATE, but SPLIT into two independently-gated sub-batches.** All four native capabilities are confirmed present at 0.15.0 (pin-check claims 1–4, `07-pin-check.md:19–22`): binning (`dist/transform-bin-time.d.ts:29,33`, `transform-bin.d.ts:31–34`, `transform-bin-xy.d.ts:26` — all five transforms exported as named), per-mark colour (`dist/rect.d.ts:11`, `color?: Channel<TDatum, ChartKey \| null \| undefined>` verbatim), generic `stagger()` (`dist/motion-definition.d.ts:2–15`), and declarative focus styling including the literal `'unmatched'` selector (`dist/types.d.ts:95–98`, `focus-mark.d.ts:2`, `selection.d.ts:27,29`). So `01:81`'s correction stands — the original "upstream lacks binning/colour/focus" rationale is invalid and the re-scope is mandatory. **The split is the ruling.** **A — paint-only:** colour channel + declarative focus styling. **B — geometry/timing:** binning + stagger. A and B gate separately, A first. \| The capability question was never in doubt; the *risk* question was, and the research does not address it. `heatmap` carries the **second-tightest headroom in the project — 0.4277% worst cell against a 0.5% gate, 0.0723% of room** (D362), and it is a T1 revert-first chart. Landing binning, colour, stagger and focus styling as one change against that margin would produce a single red number with four candidate causes and no way to bisect it — precisely the situation the plan's own gate-after-every-batch rule exists to prevent. The split is chosen so the two halves fail *distinguishably*: nothing in A can move a cell's position, so a red A is a paint or hover-state defect; B is the only half that can move geometry or retime the reveal. That also front-loads the safe half — colour and focus styling are the two capabilities with the least interaction with the 0.4277% hover baseline. Evidence for the port itself is stronger than the research recorded: it cites prior art at `repos/tanstack-charts/examples/sandbox/src/plots.ts`, a clone 5.0.1 stopped resolving, but 0.15.0 ships first-party `docs/examples/heatmaps-and-densities.md` and `docs/reference/marks/bar-and-rect.md` — the ruling cites those instead. Note also that `04:23` already corrected the "0% TanStack" framing: `HeatmapCells` runs the full pipeline today (`heatmap-components.tsx:18–19,192–210`), so this is a hybrid-to-native move, not a from-scratch migration. |
| **D369** | 2026-08-26 | **Pin-check outcome (5.1.1) accepted, with two verdicts re-verified by the lead because they change planned work: claim #23 REFUTED, claim #27 REFUTED-in-effect.** Totals: 30 claims → 18 CONFIRMED, 11 CONFIRMED-BUT-DIFFERENT, 1 REFUTED, 0 UNVERIFIABLE; the 8 corrections at `01:72–81` → 7 CONFIRMED, 1 UNVERIFIABLE (funnel refusal *intent*, a historical question typings cannot settle — immaterial, since D364 rests on the structural gap, not on intent). **#23 — `ChartSvgRenderHooks`/`renderChartSvgWithHooks` are unreachable.** Independently confirmed: both symbols are declared and exported in `dist/svg-renderer.d.ts:2,10`, but **no `exports` subpath maps to `svg-renderer.js`** — the three SVG entries resolve to `svg.js` (`./svg`), `svg-surface.js` (`./svg/renderer`) and `svg-resources.js` (`./svg/resources`) — and `dist/svg.d.ts` does not re-export them. They are reachable only by deep-importing past the exports map, which bundlers and Node both refuse. `05:26`'s escape hatch therefore does not exist. **#27 — `whenSelected` does not compose with `brushX`;** it pairs only with `keyedSelection` (point-key selection). Range-driven mark filtering from a brush is entirely application-owned. \| Recorded because both narrow options the plan assumed it had, and one is materially softened by an earlier ruling. **#23's blast radius is smaller than it looks, thanks to D366:** the escape hatch existed to rescue D10's paint-resource work if declared gradients proved insufficient, but declared gradients turned out to be a documented first-class contract that needs no wrapper at all, so the fallback is no longer load-bearing for the gradient half. What remains available, and verified reachable, is `createSvgChartRenderer` at `dist/svg-surface.d.ts:3` via `./svg/renderer` — that is now the *only* renderer-boundary escape hatch, and it is the one the pattern guards in D366(b) must be built on. **#27 rewrites T19/D17's acceptance criteria:** `05:48`'s claim that `brushX`+`controlledSignal` covers the x-range drag-select case is half true — the brush produces the range natively, but filtering marks by it stays ours to write, so D17 is *reduced*, not resolved, and the go-to-plan must say so rather than claiming a native path that does not exist. **Corroboration worth recording:** the pin-check independently reached the same split on `url(#id)` that D366 ruled from the docs — declared-gradient references are documented in three places, while the undeclared/foreign-id passthrough the pattern work depends on remains undocumented prose-wise and visible only in `dist/svg.js:9–13`. Two independent readings converging on the same boundary is the strongest evidence available here. **One conflict resolved by the lead:** a cluster fork reported `renderTooltipBody` at `@tanstack/charts/react/tooltip` and the consolidator at `@tanstack/react-charts/tooltip`. Both are real — the symbol ships in both packages (`charts/dist/react/tooltip.d.ts:10`; `react-charts/dist/tooltip.js:14`) — but **ours is `@tanstack/react-charts/tooltip`**, because the migrated code takes `Chart` from `@tanstack/react-charts` (e.g. `gauge.tsx:111`), and that package's exports are exactly `.`, `./canvas`, `./core`, `./tooltip`. |
| **D370** | 2026-08-26 | **5.2.2 ruling — B0 is a real batch: the legacy-scale deprecation, sequenced FIRST. Withdraws D360's "defer to whichever later batch touches those call sites".** Measured scope (lead, on the pinned 0.15.0, not agent-reported): **24** deprecated top-level `x:`/`y:` keys across **10** files — `area-chart`, `candlestick-chart`, `choropleth-chart`, `composed-chart`, `live-line-chart`, `radar-chart`, `sankey-chart`, `sunburst-chart` (2 each), `gauge.tsx` and `internal/heatmap-components.tsx` (4 each, two `defineChart` calls per file) — plus **2** `polar()` `angle:`/`radius:` keys at `radar-chart.tsx:409–411`. **Zero** files use `scales: {…}` today. Gate: full 45-run sweep, but held to a **0.01%** movement ceiling rather than the 0.5% gate, plus a non-pixel exit check — the deprecation warning must be gone from the showcase console on all 26 routes. Revert-first with no fix-forward case. \| Three verified reasons, each checked in the pinned package rather than assumed. **(1) It is a provable runtime no-op:** `dist/scene.js:499` resolves `const scales = definition.scales ?? { x: definition.x, y: definition.y }` — pure aliasing, identical downstream, so the correct result is known in advance to be 0.0000% on every cell. **(2) It is forward-compat-mandatory, not hygiene:** `dist/scene.js:489` states *"This compatibility will be removed when TanStack Charts enters Alpha"*, so deferring it only relocates a forced migration into the middle of implementation. **(3) A deprecation warning is firing in the showcase *dev* console right now** and Gate 5.0 missed it, because that check was scoped to console *errors*. Lead-verified with its limit stated rather than assumed: the warning sits inside a `process.env.NODE_ENV !== "production"` guard (`dist/scene.js:486`) and esbuild strips it from the Vite production build — `bench/app/dist/assets/*.js` carries the unconditional throw string `"Chart scales must define reserved"` but **zero** occurrences of `deprecated`. It therefore fires under `next dev -p 5200` and **not** in the builds QA and bench measure, which means B0's console exit criterion must be run against the DEV server; run against a production build it is vacuously true and would have been a false pass. Sequencing it first makes B0 the **gate-harness shakedown**: it is the one Phase 5 change whose expected outcome is zero movement everywhere, so running the full sweep against it validates the instrument before any batch with real parity risk depends on it — a red cell in B0 indicts the harness, not the code. It also clears the deprecated keys out of 10 files that B1–B8 all edit (verified: every one of the 10 is named by a later task), keeping the rename out of every subsequent diff. Type contract noted for the executor (`dist/types.d.ts:485–496`): `CanonicalChartScaleSpec` vs `LegacyChartScaleSpec` is a union with `scales?: undefined` on the legacy branch, so **a spec cannot be half-migrated**; `ChartScales` requires both reserved keys and `scene.js:503` throws otherwise, so polar-only specs take `{ x: null, y: null }`. Blast radius includes `candlestick` (**T0**) and `heatmap` (**T1**), so GATE-MAP pre+post captures apply despite the no-op expectation. |
| **D371** | 2026-08-26 | **B0 scope corrected upward by the lead: 36 keys / 16 files, not the 24 / 10 recorded in D370.** The first count matched only the inline `defineChart({` call form and so missed `bar-chart.tsx` (4 keys, two specs), `line-chart.tsx` (2) and `scatter-chart.tsx` (2), which pass a spec **variable** — `defineChart(spec)` / `defineChart(base, {…})`. It also missed surface **1b** entirely: `funnel-chart.tsx`, `pie-chart.tsx` and `ring-chart.tsx` set no `x`/`y` at all, but `scene.js:484` warns on `scales === undefined` too, so each needs `scales: { x: null, y: null }` inserted. A third pass found one more pair hidden behind a cast — `area-chart.tsx:655–656` inside `const emptySpec = {…}`, passed as `defineChart(emptySpec as never)` at `:668`; **the `as never` defeats the typechecker**, so this one would have survived a green `tsc` and shipped a live deprecation into the loading path. Two further deprecation surfaces were checked and are **zero-work**: resolved-polar `.angle`/`.radius` reads (`polar.d.ts:15–17`) and `ChartMarkPointX/Y` (`types.d.ts:391,393`) have no occurrences in migrated code. Polar migration verified safe: `polar()` carries its **own** scoped `scales` (`polar.d.ts:85`), so chart-level `{x:null,y:null}` does not trip the `collected.keys()` throw at `scene.js:506`. \| Recorded because the correction changes B0's risk profile, not just its size: adding `scatter-chart.tsx` pulls **`scatter` (T1, 0.0490% headroom)** into B0's blast radius, so B0 now touches **all three** tightest-headroom charts — `candlestick` (T0), `scatter` (T1), `heatmap` (T1) — and takes pre+post captures on each despite being a provable no-op. Also corrects D370's third justification: the deprecation warning is inside a `process.env.NODE_ENV !== "production"` guard and esbuild strips it from the Vite production build (`bench/app/dist/assets/*.js` carries the unconditional throw string but **zero** occurrences of `deprecated`), so B0's console exit criterion must run against the `next dev -p 5200` DEV server — run against a production build it is vacuously true and would have been a false pass. |
| **D372** | 2026-08-26 | **5.2.2 ruling — batch ordering fixed at B0 · B1a–B1d · B2 · B3 · B4 · B5 · B6 · B7 · B8, strictly sequential, in `docs/phase-5/BATCH-ORDER.md`. B1 is SPLIT because "mechanical deletions, no visual risk" is false.** Fan-out tracing (which the collision pass explicitly declared it had run out of budget for) shows **T5** reaches `area` `bar` **`candlestick` (T0)** `composed` `line` **`scatter` (T1)** via `internal/x-axis-overlay.tsx` importers, and **T3** reaches `candlestick` transitively — `internal/segment-visuals.tsx:5,26` calls `usePrefersReducedMotion` and `candlestick-chart.tsx:56` imports `SegmentOverlay` from it. With T1 touching all 14 hosts, B1 as specified put three T0-touching changes in one gate. Split: **B1a** T2 ∥ T4 (no risk-tier reach) · **B1b** T3 · **B1c** T5 · **B1d** T1. Parallelism is *within* a batch only; the serialize-always list is T1, T3, T5, T13, T14, T18, T20, with T17 isolated by its own "zero gate upside" acceptance text. Per-batch QA subsets recorded in BATCH-ORDER.md. \| Directly enforces D362's stacking rule ("no batch may stack two risky changes onto one chart within a single gate"), which B1 as drafted violated three times over. Batches stay strictly sequential because every candidate cross-batch pair shares either a file or a risk-tier chart — running two concurrently would make an attributable gate failure unattributable, and the whole revert-first policy for T0/T1 depends on attributability. |
| **D373** | 2026-08-26 | **T13's cited scope is wrong in two ways; B4 re-scoped.** (a) `renderer.ts:66–85` **does not exist** under `showcase/migrated/charts` — the only `renderer.ts` in the repo is vendored library source under the protected `repos/` tree, which we must not edit. This is the **fourth** phantom citation in the research corpus, after `adapter-shared.ts`, `tooltip-scheduler.ts` and `visx-pattern-bridge.tsx`. (b) T13's blast radius is misstated: traced by importer, `internal/tooltip-chrome.ts` is reached by `bar`, `candlestick`, `choropleth`, `live-line` and `scatter` directly, by `area` + `composed` via `hover-chrome.ts`, and by `line` via `use-hover-chrome.ts → hover-chrome.ts` — while `internal/sankey-hover-chrome.ts` and `internal/heatmap-hover-chrome.ts` import it **zero** times, both owning independent tooltip stacks. **B4 therefore loses `sankey` and `heatmap` and gains `choropleth`.** Line-number drift also corrected for T4 (`:1059` not `:1018`), T15 (`handleRender` at `:651`/`:920`/`:1059–1138`), T16 (`loading-chrome.tsx`, `getElementById` at `:200` not `:71`), T19 (`@visx/zoom` export at `index.ts:223`, not `:184–192`) and T18 (8 sites real, lines drifted up to +249). \| The heatmap correction is a genuine risk *reduction* and worth the log: heatmap is a **T1** chart with 0.0723% headroom, and removing it from the highest-parity-risk batch means its only movers are T9, T18, T21a and T21b, each in a separate gate. The `renderer.ts` finding matters beyond one line — a fourth phantom path means research file paths are not trustworthy as a class, so the standing instruction for every executor is to re-locate targets **by symbol, never by line number**. Also recorded: the collision pass flagged T13(B4)-before-T20(B7) as a contradiction; it is not. Tooltip motion is opt-in via `motion()` (pin-check #5), so T13 ships without it and T20 supplies it after — exactly what `PLAN-phase-5.md:91` specifies. |
| **D374** | 2026-08-26 | **5.0.4 bench smoke independently re-verified by the lead from the raw `bench/results/**/*.json`.** Integrity confirmed: **24/24** cells present, every one `skipped: []` with `n = 7` on `m1a_mountToPaintMs`/`m1b_settleMs`/`m1c_scriptMs` (`m3a_updateMs` carries `n = 210` per-frame samples and `m3c_frameTimeMs` is `null` by design — shapes, not gaps). Recomputing all 72 deltas against `BASELINE.md` §3a reproduces **exactly** the same 4 cells over ±20%, with the same values (`line/100 tanstack` M1a +28.2% / M1b +34.5%, `line/1000 tanstack` M1b +26.7%, `area/100 tanstack` M1b +25.0%) — **no omitted failures**. One reporting error corrected: the true `M3a` maximum is **+11.2%** (`line/10000`, tanstack arm), not the "+9.5%" quoted in the hand-off, which is in fact the `bar/10000 tanstack` **M1b** delta. Verdict unaffected — the claim of record is "no M3a cell >20%", which holds. Verification recorded in `BENCHMARKS.md` above the generated-table marker. \| The port-5199 kill mid-sweep made this run's provenance the open question from D361/BASELINE §6. It is now closed: the agent detected the kill itself, discarded the `0 result(s), 1 skipped` write for `tanstack/line n=1000`, and re-ran it clean — and the discard is confirmed independently, since every cell that reached the table carries a full 7/7 measured-run count. Standing caveat carried unchanged: all four flags sit on M1a/M1b, the channel `BASELINE.md` §3 already rated **VOID** on this machine for three consecutive runs; the `bklit` control arm shows zero flagged cells; no regression ruling is made from this data alone, per the D273 protocol. |
| **D375** | 2026-08-26 | **B0 scope corrected DOWNWARD on execution: 15 files, not the 16 recorded in D371. `funnel-chart.tsx` has no `defineChart` call site at all.** The file is plain SVG by deliberate prior design — its own header (`funnel-chart.tsx:4–34`) explains why it is not a `defineChart`/`<Chart>` chart, and the only five occurrences of the token `defineChart` in it are inside that comment block. D371's surface-1b claim was derived from that comment text rather than from a call site, so funnel was never migratable. Two further surface-1b premises were also wrong and were corrected by reading rather than by patching blind: `pie-chart.tsx` (2 specs) and `ring-chart.tsx` (2 specs) were recorded as needing `scales` *inserted*, but all four already carried inline `x: null, y: null` and so were ordinary wraps. Final measured shape, lead-verified post-hoc across the whole tree rather than accepted from the three executors: **21 spec-bearing `defineChart` call sites ↔ 21 chart-level `scales:` keys, an exact 1:1**, plus **1** `polar()`-scoped `scales: { angle, radius }` (`radar-chart.tsx:410`); **zero** leftover deprecated chart-level `x:`/`y:` siblings; **34** deprecated key-lines removed, **22** `scales:` lines added, across **15** files. The other 7 `polar()` call sites (ring ×2, sunburst, gauge ×2, pie ×2) pass no `angle`/`radius` options and needed nothing. \| Recorded because it closes the phantom-citation pattern on the *other* side: D370/D371 twice corrected the count **upward** for call forms the grep missed, and this pass corrects it **downward** for a call site that never existed — the token `defineChart` appearing in a file is not evidence of a `defineChart` spec in it. That makes five phantom targets in this phase (`adapter-shared.ts`, `tooltip-scheduler.ts`, `visx-pattern-bridge.tsx`, `renderer.ts`, and now funnel's surface-1b entry), reinforcing D373's standing instruction to re-locate by symbol and to verify by reading before editing. The executor that found it did the right thing — it read first, deviated from the brief, and reported the deviation rather than forcing the edit. Two lead-side false positives are also recorded so the method is not over-trusted: an indentation-based orphan scan flagged `area-chart.tsx:817/822` and `heatmap-components.tsx:215/216`, both **correct** nested `scales` children — the first because line 812 is the shorthand `marks,` (no colon) and the second because the scan cross-matched a sibling spec's indent. Structural greps over TSX need the enclosing-object boundary, not indentation alone. |
| **D376** | 2026-08-26 | **B0 gate PASSED, and the 0.01% movement ceiling set in D370 is AMENDED — it is below the QA harness's own resolution on hover captures, and was therefore never a measurable criterion there.** Evidence, all measured this session: a `--self-test` run (**identical code on both sides**, so any non-zero reading is pure instrument noise) returned `composed` hover-70 **0.1162%** — breaching even the harness's own `SELF_TEST_GATE` of 0.1% — and `bar` hover-30 **0.0821%** / hover-70 **0.0793%**, while **every** settled cell read exactly 0.0000%. Reconciling all 45 rows / 168 cells against `BASELINE.md` §1 and splitting by that determinism class: **settled cells, excluding the 3 documented non-deterministic rows — 42 cells, max \|Δ\| = 0.0047pp** (`pie` 0.0138→0.0091), comfortably inside the 0.01pp ceiling; **hover cells, same exclusion — 114 cells, max \|Δ\| = 0.1177pp** (`composed` hover-50), which sits **0.0015pp** from that same chart's measured self-test noise. The amended criterion is therefore: *deterministic (settled) cells hold the 0.01pp ceiling; noise-dominated (hover) cells are bounded by the measured self-test floor of the same chart.* Board verdict unchanged from baseline: **42 PASS / 3 FAIL**, the 3 FAILs being exactly the ruled-non-parity `candletween`, `ring` n=1000 and `barloading`. Full gate: typecheck **EXIT=0** both apps (authoritative post-hoc run, not the three racing executor runs); both builds green; **26/26** showcase routes on `next dev -p 5200` with **0** deprecation warnings, **0** scale TypeErrors, **0** console errors, **0** nav failures. \| The ceiling is amended rather than either enforced or quietly dropped, because enforcing it would have reverted a change **proven at source to be a semantic no-op** (`scene.js:499`, `definition.scales ?? { x: definition.x, y: definition.y }`) on the strength of an instrument that cannot resolve the quantity being gated. That B0 was the deliberate **gate-harness shakedown** (D370, reason 3) is exactly why this is the right outcome: the batch whose expected result is 0.0000% everywhere is the one that calibrates the instrument, and a red cell here indicts the harness — which is what happened. Two controls were run before amending, so this is not a criterion relaxed to fit a result. **(1) Reverted-tree control:** with B0 stashed, `scattermultiaxis` hover-50 read 0.0779 — *identical* to the B0-applied reading against a baseline of 0.0000 — proving that drift is 5.0.1's v0.14.0→0.15.0 upgrade, not B0; and `composed` hover-30 read 0.0808 reverted vs 0.0000 applied, i.e. the **reverted** tree moved *away* from baseline on a cell where the migrated tree matched it exactly. Direction of movement is random and roughly balanced across the 41 flagged cells, which is a noise signature, not a regression signature. **(2) Flake cleared, not ignored:** that control run also produced `area` settled **9.4121%** on pristine phase-4 code recorded at 0.0003% — a capture taken against a partially-rebuilt bundle immediately after the stash tripped the harness's stale-build guard; re-measured on the restored tree it returned to **0.0003%**, exactly baseline. It is logged rather than discarded because it is the sharpest available evidence that a single red cell on this harness is not self-evidently a code defect. The console criterion was likewise proven non-vacuous before being credited: reverting `sankey-chart.tsx` alone to the legacy form made the warning fire on `/charts/sankey` (**DETECTOR IS LIVE**), and restoring it returned all 26 routes to clean — so the 0-warning result is a real measurement, not a detector that cannot fail. The `bench/results/**` `latest.json` churn is bench-harness output, not source; protected files (`bench/run.mjs`, `bench/report.mjs`, `qa/screenshot.mjs`, `repos/`) were **not** modified — B0's diff is confined to the 15 files under `showcase/migrated/charts`. |
| **D377** | 2026-08-26 | **B1a (T2 ∥ T4) implemented and gated PASSED.** **T2** — deleted the redundant `window.addEventListener("resize", update)` and its cleanup from `useBrushDrag`'s effect in `internal/brush-drag.ts` (found at `:153`/`:156`, **not** the `:118` cited by D17/ledger — a 35-line drift, so the by-symbol rule from D373 earned its keep again). Exactly 2 deletions; the `ResizeObserver` and all 6 `pointermove`/`pointerup`/`pointercancel` window listeners verified untouched. **T4** — threaded `ariaLabel`/`ariaDescription` through `LineChartProps` in `line-chart.tsx` (`:143-144` interface, `:167-168` destructuring with `ariaLabel = "Line chart"` as the default, `:1065-1066` call site); `Chart` accepts both in the pin (`react-charts/dist/Chart.d.ts:4-5`). Lead-verified **additive-only**: the whole-file diff adds 4 lines and replaces 1 attribute, and the only other removals are B0's `scales:` restructuring — no existing prop changed name, type, order or default, and the literal `"Line chart"` survives exactly once, as the default. Gate: typecheck **EXIT=0** both apps, both builds green, QA **6/6 PASS** — `brush` 0.0001, `line` 0.0000/0.0000/0.0540/0.1494, `area` 0.0003/0.0001/0.0001/0.1580, `legend` 0.0000, `candlelegend` 0.3211, `legendhover` 0.0593. \| **QA scope widened by the lead beyond BATCH-ORDER's `brush`/`line`/`legend*`:** importer-tracing showed `useBrushDrag` is called only from `internal/chart-brush.tsx`, but `BrushHostContext` is provided by **both** `line-chart.tsx:1047` **and** `area-chart.tsx:1127`, so `area` hosts the same resize path and was added to the gate. It passed at 0.0003% settled, identical to baseline. **T4 deliberately NOT expanded past `line-chart.tsx`.** The other 14 hosts carry the same hardcoded `ariaLabel="…"` (verified: all 15 enumerated), and threading them all would be the consistent API change — but it would pull `candlestick` (**T0**), `scatter` (**T1**) and `heatmap` (**T1**) into B1a's gate, destroying the batch's entire premise (BATCH-ORDER: B1a's risk-tier reach is *"none"*) and violating D362's stacking rule. Scoped to line as the go-to-plan ratified; the remaining 14 are a candidate follow-up batch, **not** silently dropped. **This run also corroborates D376's noise ruling from the other direction:** every deterministic (settled) cell in the subset reproduced baseline **exactly**, and `line` hover-50 returned **0.0540** — precisely its baseline — where the heavily-parallel B0 sweep had read 0.0000 for the same code. Max hover delta across the subset was 0.0016pp, an order of magnitude below the 0.1162pp self-test floor measured at B0, confirming that the B0 hover spread was machine load rather than anything in the tree. |
| **D378** | 2026-08-26 | **B1b (T3) implemented and gated PASSED — and T3's substantive deliverable turned out to be pre-existing, so the batch is KEPT on a narrower justification than it was scoped with.** T3's acceptance is "single hook, one listener; reduced-motion behaviour identical". Auditing first rather than implementing: only **3** `matchMedia` occurrences exist repo-wide (a comment at `gauge.tsx:86` and two inside the hook), and **14** files already consume `usePrefersReducedMotion` — the ~12 ad-hoc call sites T3 was written to consolidate were folded in during an earlier phase. The **"single hook"** half was therefore already met. The **"one listener"** half was genuinely NOT: `subscribeReducedMotion` built a fresh `MediaQueryList` per consumer and attached a per-consumer `"change"` listener, so a page mounting all 14 hosts carried up to 15 of each. Closed with a lazily-initialized module-level `MediaQueryList` + a subscriber `Set`, so exactly one native listener exists while anything is mounted and zero when nothing is. **Laziness is load-bearing, not style:** this module is imported by 14 chart files that Next prerenders on the server, so `window` must only ever be touched inside `subscribe`/`getSnapshot` — the import must stay side-effect-free. Both apps typecheck EXIT=0; both builds green (showcase 26/26 routes static). **Gate: 16 runs, 16/16 PASS** — the 10-chart n=1000 set (`candlestick` `area` `line` `composed` `gauge` `funnel` `funnelvertical` `gaugelinear` `refarea` `segment`) plus `markers`/`liveline` n=100, `heatmap` n=52, `sankey` n=33, `sunburst` n=27, `sunchrome` n=27. Coverage verified against the consumer list rather than asserted: every one of the 14 importing files has ≥1 chart exercised. Against BASELINE §1, **settled max \|Δ\| = 0.0042pp** (ceiling 0.01pp, inside), **hover max \|Δ\| = 0.0529pp** (noise floor 0.1162pp, inside); 4 of the 7 second-wave charts are bit-exact on all four cells. \| Two movements were run to ground rather than absorbed, per the "attribute before reverting" rule this log added at D376. **(1) `candlestick` hover-30 = 0.5082% FAIL on B1b's first sweep** — a **T0** chart with 0.0047pp of headroom, i.e. the single worst cell on the board to see move. Rather than revert on the reading, five runs: T3 applied solo → 0.3025/0.4953/0.3661/0.3618 = **exactly baseline**; T3 reverted solo → **identical to 4dp on all four cells**; T3 reverted under 10-chart load → 0.4953 PASS; T3 applied under 10-chart load → 0.4953 PASS. Four of five runs read exactly 0.4953 and the outlier never reproduced *with the same code that produced it* — an unattributable flake, not a regression. Had the revert-first policy been applied mechanically to the first reading, a correct change would have been reverted for a phantom. **(2) `sankey` hover-70 +0.0529pp** tripped the T2 "investigate any movement >0.05%" rule, and unlike the D376 cases it was NOT dismissible as noise: sankey's own self-test drew 0.0000 settled (deterministic) but only 0.0095/0.0394/0.0177 on hover, so the movement *exceeded* the chart's measured floor. Settled it by direct A/B instead of statistics — **T3 reverted, sankey solo: hover-70 = 0.1834**, i.e. +0.0511pp above baseline **with T3 absent**. The drift is inherited from 5.0.1's v0.14.0→0.15.0 upgrade, the same signature D376 found on `scattermultiaxis`, and is now a standing property of BASELINE §1's sankey hover row that **B2 and B3 both inherit** — both gate `sankey`, and neither should charge that ~0.05pp to itself. The T3 A/B on sankey is separately neutral: settled bit-identical (0.1101 both sides), hover Δ ≤0.0121 in mixed directions. **Keep-vs-revert, decided explicitly rather than defaulted:** the case for reverting was real — the user-visible benefit is nil (listener bookkeeping), the blast radius is 14 charts including a T0, and the deliverable was already substantially done. It is kept because it is the only thing that makes T3's stated acceptance literally true, and because it is now *measured* neutral on both risk-tier charts it reaches (`candlestick` identical to 4dp; `sankey` settled bit-identical) rather than merely assumed so. **Standing risk carried forward, unchanged by this batch:** `candlestick` hover-30 sits 0.0047pp from the gate and has now produced one unreproducible 0.5082 outlier — B1c, B4, B5 and B6 all touch it, and B4 (T13, tooltip) targets it directly. A single FAIL reading on that cell is not sufficient evidence to revert a batch; self-test and solo A/B come first. |
| **D379** | 2026-08-26 | **B1c (T5) — SKIPPED, with reason. Native tick config cannot replace the axis overlays at this pin, and the task's three named deliverables fail for three *different* reasons.** T5 was "native tick config: `ticks.values` / `tickLabels.*` for format, values, rotation", acceptance "axis pixels unchanged; overlay code deleted only where fully replaced". Audited before implementing (the D378 habit) — two parallel read-only audits over `x-axis-overlay.tsx` (536 lines), `bar-x-axis-overlay.tsx`, `y-axis-overlay.tsx`, `y-axis-ticks.ts`, with every load-bearing claim re-verified by the lead against the pinned dist. **(1) ROTATION — nothing to migrate.** Neither the x nor the y overlay rotates a label anywhere; x labels are always horizontal (`x-axis-overlay.tsx:345`). A third of the task is empty. **(2) `ticks.values` — structurally unavailable.** All six cartesian charts already occupy the `ticks` slot with `{ count: gridGuide.* }` on **both** axes (16 sites: area 656/660/820/825, bar 676/680/852/856, scatter 655/660, candlestick 527/532, composed 1128/1133, line 593/598), because `configured-scale.js:23` (`tickValues = configuredTicks?.values ?? scale.ticks?.(context.tickCount)`) makes that same resolved array the one the **grid marks** consume. `count` and `values` are **mutually exclusive at runtime** — `scene.js:1068-1077` throws `TypeError: Axis ticks accept only one candidate policy: count, spacing, or values`. So adopting `values` for labels either throws or silently relocates every grid line. The `styles.css:253-265` comment asserting this coupling was written against v0.14.0; per the 5.3.4 staleness rule it was re-verified at 0.15.0 and the pinned behaviour is **stricter** than the comment claimed (hard throw, not silent divergence). **(3) `ticks.format` / `tickLabels.*` — semantically 1:1 but INERT.** Every migrated chart hides the native axes outright (`styles.css:266`, `[data-bkm-chart] .ts-chart__axes { display: none }`), so native tick labels never paint. Configuring `format` changes no pixel and deletes no code — the overlay still needs its own formatter for its own HTML rendering. Making it live would mean un-hiding the SVG axes and replacing absolutely-positioned HTML `<div>`/`<span>` (`x-axis-overlay.tsx:338-367`, `y-axis-overlay.tsx:115-125`) with SVG `<text>` — a text-layout-substrate swap, directly against "axis pixels unchanged". \| The deeper finding is that the overlays encode behaviour with **no native expression at all**, so even a substrate rewrite would regress. **(a) Per-tick label colour — no such field exists.** `y-axis-overlay.tsx:123` colours each label by which `<ReferenceArea axisLabelColor>` band contains its value (`reference-area-geometry.ts:142-156`, live at `area-chart.tsx:1180` and `line-chart.tsx:1101`). `ChartAxisTickLabelOptions` is `{rotate, thin, fontSize, fontWeight, opacity, anchor, dx, dy, motion}` — **zero** colour/fill/stroke fields (lead-verified by grep, count 0), and `ChartAxisTickLabelContext` is only `{value, index, position, bandwidth}` (`types.d.ts:185-193`) — no axis id, so a caller could not even re-derive which axis a tick belongs to. **(b) A live DOM contract that would have broken SILENTLY.** `tooltip-chrome.ts:676-689` does `querySelectorAll("[data-bkm-xlabel]")` and writes `span.style.opacity` per tick to fade axis labels near the hover pill. Native SVG tick labels cannot carry custom `data-*`. Deleting the overlay would kill that with **no type error and no build failure** — it would have surfaced only as a hover-cell pixel delta, on charts including `candlestick` (**T0**, and per D378 the cell least able to absorb one). This is the concrete vindication of auditing before editing. **(c) Tick *selection* is not the same algorithm.** The x overlay runs a bespoke exhaustive gap-symmetry scorer (`x-axis-overlay.tsx:26-302`) plus label-string dedup (`104-124`, collapses ticks formatting to the same string — native `thin` collides on **pixel gap**, a different rule) plus projection tail-tick append (`470-511`). Even feeding a precomputed `values`, all 276 lines still run: nothing is deleted. And the domain-interpolation path is irreconcilable with native — **measured, not reasoned**: for a UTC domain at `count: 6`, `scale.ticks(6)` returns **3** ticks (Apr 1/May 1/Jun 1, px 212/473/742) while the overlay's `t = i/(tickCount-1)` returns **6** (Mar 7…Jun 19, px 0/180/360/540/720/900) — different count, positions and labels. **Scope correction:** the task assumed six `YAxisOverlay` consumers; there are **three** (`area` 1170, `candlestick` 1097, `line` 1091). `bar`/`composed`/`scatter` render no y labels at all — their `axis.ticks.count` exists purely to drive grid lines. **Ruling: skip.** Acceptance says delete only what is fully replaced; nothing is fully replaced, so the compliant outcome is to delete nothing. B1c closes as a no-op batch with no code change, hence no gate to run beyond the pre-capture already banked (`docs/phase-5/captures/b1c-pre.md`). **Two defects found in passing and deliberately NOT fixed** (out of T5's scope; logged so they are not lost): (i) `marginLeft` is passed at `candlestick-chart.tsx:1099` into a prop destructured-and-discarded at `y-axis-overlay.tsx:47` — dead argument; (ii) tick counts are asymmetric — grid uses unclamped `gridGuide.ticks` while labels use `resolveYAxisTickCount()` clamped to 1–10 (`y-axis-ticks.ts:18-30`), and `candlestick` computes tick values in **three** independent places (`334`, `573-582`, `532`). Neither changes pixels today. |
| **D380** | 2026-08-27 | **B1d (T1) — SKIPPED, with reason. T1's premise is wrong in both halves: native fluid sizing is *already adopted* on 8 of the 14 `<Chart>` hosts, and `use-container-size` is not the sizing mechanism it would replace.** T1 was "delete `use-container-size`; adopt native fluid sizing", acceptance "all 14 `<Chart>` hosts resize identically; hook deleted", gate the full 45-run. Audited before implementing (the D378/D379 habit) — three parallel read-only audits over all **14** sizing-hook call sites (13 files; `gauge.tsx` has two), every load-bearing claim re-verified by me against source. **Half one is already true.** `renderer.js:182-192` installs the ResizeObserver only `if (options.width === undefined)`, and 8 hosts pass no `width` — `area` `bar` `candlestick` `choropleth` `composed` `line` `sankey` `scatter` — so those are natively fluid today; there is nothing to adopt. The other 6 pass explicit `width`/`height` *deliberately*: `pie`/`ring`/`radar` derive `size = fixedSize ?? Math.min(width, height)` (`pie-chart.tsx:251`, `ring-chart.tsx:344`, `radar-chart.tsx:235`) for square aspect, as do `gauge`-arc, `sunburst`, and `heatmap-components`. Native has no min(w,h) mode — `aspectRatio={1}` yields width×width, which overflows a wide-short container; the hook's own header comment already records this exact failure ("strip renders width/2 tall inside a 72px box"). **Half two is blocked at all 14 call sites: zero of the measured values are chart-size uses.** Every one feeds the app's *parallel* geometry system for the HTML/SVG sibling overlays that live outside the scene. Three distinct blocker classes, not one: (1) **circularity** — pixel-derived values baked into mark-construction closures inside `definition`, so no render can happen without the measurement already in hand and the post-render `scene` is unusable in principle: `groupScale` instance (`bar-chart.tsx:478` → `layout: group({scale})` at `:664`/`:808`), `geoMercator()` projection (`choropleth-chart.tsx:329-339` → `projection: () => projForMark` at `:402-407`), `bodyWidthPx` (`candlestick-chart.tsx:253` → `:465`/`:467`/`:477`/`:479`/`:490`/`:492`); (2) **sibling overlays** — `scatter`'s own marks genuinely read TanStack's `context.range`, but `BackgroundLayer`, `ReferenceAreaLayers`, `XAxisOverlay`, per-series gradients and the drag-select scale have no access to the scene, and drag-select must be correct before the first interaction, not one render after; (3) **cadence** — native rate-limits to one render per animation frame (`scheduleRender` is rAF-deferred whenever `requestAnimationFrame` exists, with a synchronous path only as fallback), the hook is a 10ms trailing debounce + 0.5px epsilon, and the divergence is deliberate ("bklit ParentSize debounceTime parity; audit §4 C4"). Compounding (1)-(3): `onRender` fires *inside* the rAF'd `render()`, so any overlay driven from it lags the chart by one frame on every resize — visible desync during a drag. **The "all 14 `<Chart>` hosts" census is also wrong**: 3 sizing-hook consumers render no `<Chart>` at all — `GaugeLinear` (`gauge.tsx:993`), `funnel-chart.tsx:647`, `live-line-chart.tsx:354` — and `heatmap-chart.tsx:109` is a 4th non-host that reaches a `<Chart>` only indirectly through context; conversely `sankey` and `sunburst` are `<Chart>` hosts consuming no sizing hook. Acceptance says delete only what is fully replaced; nothing is replaced and the adoption half is already done, so `use-container-size.ts` stays intact (5 hooks, 225 LOC, 14 call sites). No code changed → no gate beyond the banked pre-capture (`captures/b1d-pre.md`). **Agent correction logged** (per the standing "agents omit failures" rule): the rect/positive audit reported "`heatmap-chart.tsx` renders no `<Chart>`" — literally true of that file, but `heatmap` does host one at `internal/heatmap-components.tsx:664`. Caught by my own sweep; changes the census, not the verdict. \| Same shape as D379: the task was written against an assumed architecture and the code does something else. The migrated charts render substantial geometry *outside* the TanStack scene — hand-built d3 scales positioning absolutely-placed HTML/SVG siblings — and that geometry needs a pixel value synchronously, in the same render that builds `definition`. `use-container-size` is what feeds it. Native sizing feeds the scene, which is a different consumer, already served. Deleting the hook would not swap one sizing mechanism for another; it would remove the only pixel source the overlay layer has, on 14 call sites, to "adopt" something 8 of them already do. The three blocker classes are independent, so no partial win exists either: even `radar` — the one call site whose value reaches nothing but `<Chart width/height>` and a `chartSize < 10` boolean gate — is blocked by that gate having no native equivalent and by the square-aspect derivation above it. Recording the census error explicitly because the "14 hosts" figure appears in the plan and in `BATCH-ORDER.md`, and would mislead any later batch that inherits it. |
| **D381** | 2026-08-27 | **B2 (T6, T7, T8, T21a) — SKIPPED as scoped, zero code changed, with T21a RE-SEQUENCED into B3 rather than dropped.** All four audited before implementing (the D378-D380 habit), four parallel read-only audits, every load-bearing claim re-verified by me against the pin's `dist/`. Four tasks, four different dispositions. **T6 (scene gradients) — half already done, half impossible.** `spec.gradients` is already adopted in **four** files from a prior phase (`area-chart.tsx:833`, `bar-chart.tsx:860`, `composed-chart.tsx:1140`, `gauge.tsx:683`/`:740`), and the acceptance clause "no custom `renderSvg` wrapper" is already satisfied — none exists; the tree's only `renderSvg` prop (`gauge.tsx:858`) passes `renderChartSvgWithResources`, which `svg-resources.js` shows is literally `renderChartSvg` re-exported. What remains is exactly what cannot be done: `scatter` needs a 6-stop **radial** gradient (`:1050`) — no radial gradient type exists anywhere in the package — plus a `userSpaceOnUse` linear gradient (`:1079`), and `renderGradients` (`svg.js:16-23`) pipes every coordinate through `percent()` (`svg.js:38-40`, clamped to [0,1]×100%) and never emits `gradientUnits`, so absolute coordinates are inexpressible. The 6 stops are load-bearing, not decorative (`scatter-chart.tsx:519-540`: a 2-stop version made Chromium facet the radial boundary into a visible octagon). `pie` has **zero** app-authored gradients — its `classifyChildren` (`:122-166`) is a children-as-defs passthrough for arbitrary caller JSX including `<pattern>`, which `spec.gradients`' closed shape cannot represent; narrowing it would break the component API Phase 5 must preserve. Task census error: T6 cites `pie-chart.tsx:724`, which is WAAPI reveal teardown, and cites `createElementNS`, which appears at none of its three files (it lives in `hover-chrome.ts`/`scatter-hover-chrome.ts`/`tooltip-chrome.ts`, later batches). **T7 (group clip) — impossible.** Native clip is hardcoded `<rect>`: `svg.js:29` and `svg-renderer.js:81` both emit `<clipPath><rect x= y= width= height=/></clipPath>` as literal template strings, and `SceneGroup.clip` is typed `ChartBounds` (`types.d.ts:112-115`, `:853`). The hand-built clip is `buildBarSilhouettePath` (`internal/bar-pulse-mark.ts:39-71`), a 4-point rect only when `depth <= 0` and otherwise a **6-point perspective polygon**; `depth` derives from each bar's offset from centre, so all but a dead-centre bar is non-rectangular. **T8 (sankey keyframes) — misdescribed; the named thing does not exist.** `grep '@keyframes\|animation:\|infinite\|alternate'` on `internal/sankey-animation.ts` returns **nothing**. `:94-120` holds two things, neither T8's subject: `injectGradientDefs` (`:89`) — `userSpaceOnUse` gradients, i.e. T6's subject matter, and unexpressible per above — and `injectLabelCssTransitions` (`:113`), CSS `transition:` rules, not keyframes. The real reveal is WAAPI at `:149-330`, and its link wipe (`:300-303`) animates `strokeDashoffset "1"→"0"`; `motion.js:1315-1335` defines `motionAttributes`, a 20-entry allowlist, and **`stroke-dashoffset`/`stroke-dasharray` are absent** — they appear elsewhere in the package only as statically set attributes. The `<style>` T8 would delete is load-bearing: `internal/sankey-hover-chrome.ts:155-161` documents a `stroke-opacity`-vs-`opacity` replace-not-multiply constraint (writing `opacity` yielded 0.5×0.65≈0.33) and states "change both together or hover snaps". **T21a (heatmap paint) — colour half already satisfied, states half blocked on T9, RE-SEQUENCED.** The colour-channel half is a no-op: `rect.js:49` resolves `colorValues = options.color === void 0 ? zValues : ...`, and heatmap sets `z: (d) => d.level` with no `color` (`internal/heatmap-components.tsx:203-211`), so level→colour already routes through the chart-level `color.scale`; declaring `color:` explicitly would produce a byte-identical `colorValues` array. The `states` half is genuinely implementable in the abstract — `ChartMarkState.transition` (`types.d.ts:103`) can restore the 220ms timing against the native 1100ms/`cubicBezier(0.85,0,0.15,1)` default (`motion.js:23-25`), and `when:{focus:'unmatched'}` resolves **per-cell**, not per-mark (`mark-state.js:16` scopes `candidates` via `sceneNodeOwnedPoints`, so the `:77-80` guard correctly spares only the focused cell). **But it reads TanStack's focus state, and nothing populates that for heatmap today**: the dim is driven by a hand-rolled `pointermove` listener (`:414`) feeding a custom `HeatmapHoverCoordinator`, and `<Chart>` (`:660-668`) is passed no focus or interaction handler whatsoever. Migrating that hover source of truth is **T9's** deliverable — T9 cites `internal/heatmap-components.tsx:410`, the same listener — and T9 sits in **B3**. So B2 was scheduled to consume a focus model B3 installs. Two further constraints recorded for whoever implements it: `ChartRectStateStyle` (`types.d.ts:90-91`) exposes `radius` and `inset`, which move geometry and must be omitted to honour "nothing may move a cell position"; and it omits `strokeOpacity` (unlike the dot/line/area state styles). Also: `paintCellStyles` survives T21a regardless — it writes `rect.style.transform = scale(...)` (`:503`) and `ChartRectStateStyle` has no scale/transform property, so the task can narrow the manual DOM loop but never retire it. Inline `style` also beats presentation attributes in the cascade, so `states` added while `:509`'s `rect.style.opacity` write remains would render as **dead, invisible markup**. \| Fourth consecutive batch-or-task closed on "the task was written against an architecture the code does not have", and the first where the mismatch is a **scheduling** defect rather than a capability gap — which is why T21a moves instead of dying. The B2→B3 edge in `BATCH-ORDER.md` records T13's dependency on T9's focus model but missed that T21a has the identical dependency for the identical reason; the "paint only" label disguised it, since re-basing a dim onto native focus is an interaction change wearing paint's clothing. Recording T6's already-adopted state explicitly because the four `spec.gradients` sites make T6 look open when it is closed, and a later reader could easily "re-adopt" what is already there. Recording T8's missing keyframes because the ledger (`research/phase-5/ledger.md:22`, D10) propagates the same mislabel, and its acceptance ("no injected `<style>`") would, taken literally, delete a documented hover contract on a chart already carrying inherited drift. |
| **D382** | 2026-08-27 | **T9 — SKIPPED as scoped. "Replace the hand-rolled bisector hit-tests with native `focus:'nearest-x'`/`'group-x'` + `maxFocusDistance`" is not implementable at pin `0.15.0` without breaking the per-pixel acceptance criterion.** The native surface is real and fully exported — `ChartFocusPreset = 'nearest' \| 'nearest-x' \| 'nearest-y' \| 'group-x' \| 'group-y'`, `maxFocusDistance` (default **48px**, `renderer.js:799`) — and its tie-break is **compatible**: `focus.js` `resolve()` uses `if (nextDistance >= distance) continue`, so the earliest point wins a tie, matching bklit's strict `>` in `internal/bisect.ts:resolveNearestIndex`. Three independent blockers kill it anyway. **(1) Composed resolves TWO point sets per move, native resolves one.** `composed-chart.tsx:1259–1345` runs bklit's `resolveTooltipFromX` bisector twice per pointermove — once over RAW `data` (tooltip values + per-row bar fade) and once over DECIMATED `renderData` (the highlight band's `datumIndex`, which must index actually-rendered points post-LTTB). Native focus resolves against `scene.points`, i.e. the decimated set only; the raw rows are not in the scene, so the tooltip's raw-row resolution has **no native equivalent**. **(2) Composed, per-pixel divergence by construction.** `interaction.js:resolveChartPointerFocus` tries `findContainingScenePoint(scene, x, y)` **first** for all four built-in presets and, on a hit, returns `strategy.group(points, { point: contained.point })` — bypassing the nearest-x scan entirely. Composed paints bars and areas, so any pointer inside a bar rect resolves by containment, not by nearest-in-time; near a band edge the two disagree. **(3) Heatmap, per-pixel divergence at every cell boundary.** Cells are emitted with `inset: 1` and `radius: cornerRadius` (`internal/heatmap-components.tsx:203–211`); `containsRoundedRect` therefore excludes the 1px inset gutter and all four corner notches, while the current band scan (`:360–399`, `posX >= colX && posX < colX + binWidth`, `Math.floor(posY / binHeight)`) treats the full band as hot. That is a boundary disagreement on **every** cell of a **T1** gate chart. **`live-line-chart.tsx` has nothing to replace**: its pointermove (`:492–506`) only stores `cursorXRef`/`cursorStateRef` for a rAF loop and selects no datum; `bisectTime` (`:207`) is used solely for windowing (`:545`, `:656`). **Two task-description census errors corrected** (plan errors, not agent errors): `composed-chart.tsx:1053` is a min/max time loop and `:1212` is a series config object — neither is a pointermove bisector; `live-line-chart.tsx:500` is a cursor-x tracker, not a bisector. `maxFocusDistance` (48px vs the bisector's unbounded reach) is a settable parameter and is **not** among the blockers. \| The acceptance criterion T9 was written against is "focus resolution matches per-pixel" — not "focus resolution is native". Blockers (2) and (3) are not tuning gaps; they are structural consequences of the runtime resolving focus by **painted containment first** over geometry we deliberately inset and round. Adopting native focus here would trade a hit-test that provably matches bklit for one that provably does not, on a T1 chart, to satisfy a means rather than an end. Blocker (1) is stronger still: the dual raw/decimated resolution has no native expression at any tuning. Recorded rather than forced; the compatible findings (tie-break parity, `maxFocusDistance` semantics) are preserved above so a future pin that exposes raw-row focus or containment opt-out can re-open this without re-deriving them. **Consequence: T21a stays SKIPPED** — it was already skipped under D381 with its `states` half blocked on T9 installing native focus, and that blocker is now permanent at this pin. |
| **D383** | 2026-08-27 | **T10 — SKIPPED. "Replace sankey's pointermove hit-test with a custom `ChartFocusStrategy`" rests on a false premise, and the swap would be a regression.** The premise first: sankey has **no pointermove hit-test**. `sankey-chart.tsx:545–559` attaches element-level `mouseenter`/`mouseleave` listeners to the real SVG node and link elements via ref arrays (`attachSankeyHoverListeners`, header documents "no `data-ts-key` queries"). That is browser-native painted containment over **actual path geometry including bezier curvature**, tie-broken by SVG paint order — the strongest hit-test available in the platform. Four independent facts make the native route strictly weaker. **(a) A custom strategy is handed no geometry.** `ChartFocusStrategy = {resolve(points, ctx), group(points, ctx), navigation(points)}` with `ChartFocusResolveContext = {x, y, maxDistance}` — no scene, no nodes; and `ChartPoint` carries key/markId/group/datum/datumIndex/value fields/`x`/`y`/`color` — a single anchor coordinate, **no geometry**. **(b) The runtime's own containment is unreachable from a custom strategy.** `interaction.js:resolveChartPointerFocus` gates the `findContainingScenePoint` fast-path on **built-in-preset identity** (`strategy === focusNearestX \|\| … \|\| focusGroupY`); an app-authored strategy fails that test and goes straight to `strategy.resolve`, which per (a) sees only `{x, y, maxDistance}`. **(c) `findContainingScenePoint` is not publicly exported**, so it cannot be called directly either. **(d) Even if reachable, it could not hit a sankey link:** `nearest.js:containsTarget` switches on `rect` / `dot` / `area` (polygon) / `polyline` / `rule` — there is **no path or bezier case** — and our `internal/sankey-mark.ts` emits only `kind:"label"` (`:169`), `kind:"group"` (`:254`/`:290`/`:299`) and `kind:"rect"` (`:260`) with **zero `interaction:` fields**, so there are no focus targets in the scene to contain. Reaching parity would require emitting synthetic polyline approximations of every bezier link and re-deriving paint-order tie-breaks by hand — strictly more custom code than the four-line listener attachment it would replace, and less accurate. \| Native-ness is the goal only where it is also correctness- or maintenance-positive. Here the "custom subsystem" Phase 5 targets is a browser API doing exactly the job the platform designed it for, and the sanctioned replacement is a coordinate-only callback with no access to the runtime's own containment and no code path that understands curves. The ruling records the mechanism (b), not just the verdict, because it generalises: **any** app-authored `ChartFocusStrategy` at this pin is proximity-only over anchor points — painted containment is available exclusively through the four built-in presets. That constraint is the reason T10 fails and is worth having on record for every future focus task. |
| **D384** | 2026-08-27 | **T11 — SKIPPED, all four deliverables. The native polar surface is real and was verified end-to-end; every deliverable is blocked app-side, two of them by regressions this project already measured and fixed.** **Mechanism verified first (so the skip is not a capability claim):** polar arcs (`polar.js:226/230, 397/401, 571/575`) and sunburst arcs (`hierarchy-sunburst.js:145/149`) emit `kind:"area"` with `interaction: { point, affinity: "geometry" }`; `nearest.js:collectTargets` adds every `interaction.point` to `index.attachedPoints` (`:110`), and `nearestScenePoint` runs `findContainingScenePoint` first (`:23`), skips `affinity === "geometry"` in the target proximity loop (`:33`) and skips attached points in the fallback point loop (`:47`) — so `focus:'nearest'` over these arcs is genuinely **containment-only, no centroid fallback**, exactly as T11 assumes. `onSelect` exists (`dom-types.d.ts:181,193`, `(point: ChartPoint \| null) => void`), as do `focusGroupAngle` and `onFocusGroupChange`. **Deliverable 2 ("default `nearest` over painted arcs") — blocked by D254 and D258, whose mechanism is measured.** Pie renders each slice as a PAIR — a `fill="transparent"` static hitbox twin at rest radii (`padAngle: 0`, `pie-chart.tsx:436`) painted *after* the visible mark — because collapsing the pair let the 10px hover pop move the hit area off a stationary cursor: `pointerleave → unhover → spring back → re-enter`, which at n=1000 produced the **fixed ~3.18% hover-gate failure in `qa/results/pie` 2026-08-19→08-22** (`pie-chart.tsx:388–398`). Ring records the same mechanism independently under **D258** — once a band is thinner than the ~1.7px growth displacement the loop has *"no steady state"* — and binds to a static hitbox twin on the same precedent (`ring-chart.tsx:836–846`). Crucially the pop/growth is applied **imperatively to the DOM and never to the scene** (`internal/pie-hover-chrome.ts:139` `visibleEl.style.transform = translate(...)`, `:161` `setAttribute("d", …)` regenerating the arc per frame; ring animates its groups via WAAPI), so the runtime's containment targets sit at rest geometry: "native `nearest` over **painted** arcs" is not available at all — what is available is containment over rest geometry, which is precisely what the hitbox twins already provide. The deliverable therefore buys no parity and cannot be implemented as worded. **Deliverable 3 (`focusGroupAngle` + `onFocusGroupChange` for radar) — axis mismatch.** Radar's hover is **series-level**: entering any dot of series *i* or its area path sets `hoveredIndex = i` and the whole series dims/glows/scales (`radar-chart.tsx:766–806`). Native polar grouping groups by **angle** — all series at one spoke — the orthogonal partition. Radar's listeners are additionally gated on `pendingRevealRef` (hover suppressed for series still mid-WAAPI-reveal); native focus has no such gate and would fire during the staggered entrance. **Deliverable 4 (`onSelect` → `rootId` drill) — blocked by the D32 bench contract, independently of everything above.** `bench/app/src/scenarios/migrated-sunburst.tsx:146` and `migrated-sunchrome.tsx:188` drill by dispatching `new MouseEvent("click", { bubbles: true, cancelable: true })` on `path[data-ts-key…]` — **with no `clientX`/`clientY`**, so they default to `0`. The runtime resolves `onSelect` from the event's coordinates; at (0,0) containment misses and it fires `onSelect(null)` — no drill. The click-only listeners on the TanStack paths exist *solely* to serve that synthetic dispatch (`sunburst-chart.tsx:755–758` documents it), so replacing them with `onSelect` breaks D32 zoom for two bench scenarios and the sunburst zoom QA captures. **Deliverable 1 (delete `/focus/disabled` + raw listeners) — blocked by the hit layer.** Sunburst's real pointer interaction is served by a **hit layer above the stage svg** (`sunburst-chart.tsx:757`, model documented at `:790–791` as *"enter per segment, leave only at svg level — last-enter-wins"*); the runtime's own pointer handling never sees the events, so `focusDisabled` is not what is suppressing native focus there — the layer is. And the "leave only at svg level" state machine is not expressible in native focus, which blanks whenever containment misses (ring gaps, the centre hole, inside-stage-outside-outermost-ring). \| Every prior skip in Phase 5 turned on a missing capability. **This one does not** — the capability is present and was traced to the line. It is skipped because three of the four deliverables would undo fixes with measured QA numbers behind them (D254's ~3.18% pie hover-gate failure, D258's no-steady-state ring loop) and the fourth would break a recorded bench contract (D32) that this session may not paper over. Deliverable 3 is simply the wrong axis. The one deliverable with any theoretical merit — containment over rest geometry — is what the hitbox twins already implement, so adopting it would trade working code for equivalent code on four charts including the **gate of record, `ring` n=4**. The verified mechanism is written out in full above so that a future revision that (a) syncs the animated pop into the scene, or (b) gains a per-angle→per-series grouping, or (c) is paired with a coordinate-bearing bench dispatch, can re-open the individual deliverables without re-deriving any of it. **Fourth stale in-repo comment confirmed while auditing:** `radar-chart.tsx:7` still claims *"Hover uses `focus:"nearest"` + useLayoutEffect DOM walk"* while `:459` sets `focus: focusDisabled` — parked for the 5.3.5 stale-comment pass. |
| **D385** | 2026-08-27 | **T12 — SKIPPED. `focusGuideX/Y` is unreachable given D382, `crosshair()` cannot paint the chrome it would replace, and the task row's second file paints no chrome at all.** **(1) `focusGuideX/Y` is focus-gated.** It is the only native API carrying the pill kit (`FocusGuideLabelOptions`: background / paddingX / paddingY / radius / color / stroke / fontSize / fontWeight), but `focus-guide.js:147` returns `whenFocused(mark, { match: options.match ?? "primary" })` — it renders only from native focus state. Per **D382** no chart in scope resolves focus natively, so the pill kit is out of reach. **(2) `crosshair()` is reachable but insufficient.** It is *not* focus-only: `crosshair-resolver.js:8` `resolveGuideTarget(guide, focus, cursor)` accepts an app-supplied cursor when `focus` is undefined, so `createChartCursor` could drive it. It still cannot reproduce the chrome. **(a) The indicator is a gradient, not a stroke.** `CrosshairRuleOptions` is exactly `{stroke, strokeOpacity, strokeWidth, strokeDasharray}` (`crosshair.d.ts:4–9`), while `buildIndicator` (`internal/tooltip-chrome.ts:173–237`) paints a `rect` filled with a per-chart `linearGradient` whose stops are computed from `fadeEdges`/`fadeLength` (`indicatorFadeGradientStops`) so the crosshair fades out at top and/or bottom — no native option expresses that. **(b) The x position is spring-driven.** `buildIndicator` springs x with bklit's clock-time `createSpring(0, stiffness, damping)` (D234 parity); `crosshair()`'s only motion hook is `ChartMarkMotionOptions.motion` (`types.d.ts:464`), a `ChartMotionDefinition` — not a stiffness/damping spring. **(c) `CrosshairLabelOptions` has no pill.** It is format / offset / fill / fillOpacity / stroke / strokeOpacity / strokeWidth / opacity / fontSize / fontWeight (`crosshair.d.ts:10–21`) — no padding, no radius, no background. **(d) The pill is not SVG and not a label.** `buildPill` (`internal/tooltip-chrome.ts:651–665`) creates an **HTML** `div.bkm-date-pill` whose content is `createDateTicker` (`:583–650`) — a two-column odometer with separate month and day stacks, each an `overflow:hidden` viewport translated by its own `createSpring(0, 400, 35)`, plus a compact single-label fallback mode — and the pill drives `applyLabelFade` (`:669–684`), which reciprocally fades the **axis tick labels underneath** on an opacity ramp over `TICKER_HALF_WIDTH`/`FADE_BUFFER`. Native marks paint SVG inside the scene and have no channel to reach the app's axis labels. **(e) `band` is the wrong mechanism.** `CrosshairBandOptions` *"paints the categorical scale band instead of an axis rule"* and needs a band scale; the migrated "band" is a spring-animated `clipPath` rect (`HIGHLIGHT_SPRING` on x and width, `internal/hover-chrome.ts:229–230`) revealing per-series highlight paths over a continuous-time scale — a clip-driven reveal, not a painted categorical band. **(3) Premise error in the task row's file list.** `internal/marker-tooltip.tsx` contains **no DOM chrome**: it is React tooltip *content* (`MarkerTooltipContent` with the 2-marker cap and "+N more…"), an `ActiveMarkersStore` over `useSyncExternalStore`, a provider and two hooks. There is nothing there for a crosshair or focus guide to replace. The chrome all lives in `hover-chrome.ts`, which appends six sibling layers to a host element (`:227` `host.append(highlightSvg, markerActiveSvg, indicator.svg, dotLayer.svg, boxBuild.layer, pillBuild.layer)`) — outside the TanStack scene. \| T12 is the second task this batch whose file list did not survive contact with the files (after T9's line-number census, D382). The substantive finding is the pincer: the native API that **can** draw a pill is gated on focus we do not have, and the native API we **can** drive from a cursor cannot draw a pill, paint a gradient-faded rule, or spring its own position. Acceptance for T12 is *"hover probes unchanged on all hover-sensitive charts"* — the strictest bar in the batch, applied to the single most visible piece of chrome in the product. Recorded rather than attempted. The cursor path through `resolveGuideTarget` is written out above because it is the one genuinely new capability found here and it survives independently of D382 — a future task that only needs a plain stroked rule, with no fade, no spring and no pill, can use it. |
| **D386** | 2026-08-27 | **B4 (T13) — SKIPPED, with reason. The native tooltip is reachable but not adoptable: five independent blockers, only one of which T20 can lift.** T13 was "`tooltip:{use,anchor:'group-center',sort:'color-domain',portal}` + `focus:'group-x'`; delete `createRoot` + rAF scheduler; import `renderTooltipBody` from `@tanstack/react-charts/tooltip`", acceptance **"every hover probe within baseline; `candlestick` is T0 (0.0047% headroom) — revert-first"**, blast radius the 8 `tooltip-chrome.ts` importers (`line` `area` `bar` `scatter` `composed` `candlestick` `live` `choropleth`; **not** `sankey`/`heatmap` per D373). Audited before implementing (D378 habit) — two parallel read-only audits, one on the native surface, one app-side, every load-bearing claim re-verified by me against source. **Starting state, not previously recorded: the native tooltip is not adopted anywhere.** Zero migrated charts pass a `tooltip` option into `defineChart` — every `tooltip:` occurrence under `showcase/migrated/charts` threads the *app's* `ChartTooltipConfig` through props/chrome, and `pie`/`ring`/`radar`/`sunburst` pass `tooltip:false` explicitly (`pie-chart.tsx:361,411`, `ring-chart.tsx:487,570`, `radar-chart.tsx:460`, `sunburst-chart.tsx:640`). `renderTooltipBody` is imported nowhere in `showcase/`+`bench/app/src`, and the string `ts-chart-tooltip` appears nowhere in migrated output. So T13 is a from-zero adoption on all 8 charts, not a token swap. **Blocker (1) — the plan's own two acceptance clauses are mutually exclusive.** `renderer.js:810` gates the entire tooltip-motion controller on `surface.renderer.capabilities?.tooltipMotion`, and `grep -c capabilities` returns **0** in both `svg-renderer.js` and `svg-surface.js`; `<Chart>` from `@tanstack/react-charts` uses exactly that base renderer, so `tooltipMotion` is `undefined`, the controller is never constructed, and every `beforePaint`/`afterPaint`/`hide` hook is a no-op — the native tooltip **snaps in, out and across with no animation, not even a CSS transition**. `BATCH-ORDER.md:162-165` already accepted that ("T13 must ship *without* it and T20 supplies it afterwards", `PLAN-phase-5.md:91`) — but the app box it replaces carries **three springs and a WAAPI fade**: `leftSpring`/`topSpring` (`tooltip-chrome.ts:414-415`, config-resolved 3 ways — `boxSpringConfig` → `matchCrosshair?springConfig` → `resolveTooltipBoxMotion(damping)`, default `TOOLTIP_BOX_SPRING {100,20}`), `entranceSpring` (`:417`, fixed `ENTRANCE_SPRING {300,25}`, driving `scale 0.85→1` + `translateX ±20px` + opacity), and a 100 ms `layer.animate([{opacity:0},{opacity:1}])` (`:456`). Snapping cannot hold "every hover probe within baseline" on eight charts, one of which is T0 with 0.0047% headroom. Shipping-without-motion and hover-within-baseline cannot both be satisfied; the plan asserts both. **Blocker (2) — the box is 1 of 6 sibling layers and the other 5 keep their springs.** `hover-chrome.ts:227` appends `highlightSvg, markerActiveSvg, indicator.svg, dotLayer.svg, boxBuild.layer, pillBuild.layer` to one host. Highlight runs `HIGHLIGHT_SPRING {180,28}` + a 400 ms `ease-in-out` WAAPI fade (`:229-230,369`); indicator, dots and pill all run `TOOLTIP_SPRING {300,30}`. A snapping tooltip beside five springing layers on the same pointer move is a visible interactivity regression, not merely a pixel delta — and this one is **independent of the renderer**: T20 gives the native tooltip *a* motion protocol, not our three-way-resolved spring configs. **Blocker (3) — API parity, the goal's third clause.** `internal/types.ts:398-426` is **27 fields**, commented "full bklit `ChartTooltipProps` parity". Native `ChartTooltipConfig` covers a small fraction; `panelStyle`, `backgroundColor`, `boxSpringConfig`, `damping`, `matchCrosshair`, `springConfig`, `columnWidth`, and the `indicator*`/`dot*` families have no native counterpart. Adoption drops them silently — the inverse of "keep same components API (seamless change from Legacy Bklit)". **Blocker (4) — focus-gating leaves 3 of 8 charts with no tooltip at all.** `paintTooltip` has exactly one caller, the tail of `paintFocus` (`renderer.js:336-351`), so the native tooltip paints only where native focus resolves. Native focus is live on five (`line`/`area` `focus:'group-x'`; `bar`/`scatter`/`candlestick` custom `ChartFocusStrategy`), but **`composed` leaves `onFocusGroupChange` deliberately inert** (`composed-chart.tsx:1250`, D382 blocker 1 — two divergent bisector runs), and **`live-line` and `choropleth` configure no `focus` whatsoever**. Those three would render nothing. **Blocker (5) — it flips the QA gate onto a different detection branch, on a protected file.** `qa/screenshot.mjs:199-212` tries `document.querySelector(".ts-chart-tooltip")` **first** and only falls through to the text-length heuristic when it is absent. Because no migrated chart emits that class today, all 8 charts are currently measured by the fallback; adopting the native tooltip would silently move them onto the selector branch mid-phase — a change of measurement method during the very gate that must certify the change, on a file `AGENTS.md` forbids editing without gate-author approval. **Re-sequencing behind B7/T20 does not unblock it:** T20 addresses (1) alone; (2)–(5) are renderer-independent. **B4 was T13 alone, so B4 closes with zero code changed — five consecutive no-op batches (B1c, B1d, B2, B3, B4).** Two by-products parked for 5.3.5 alongside the four stale comments, both defects rather than deliverables: `tooltip-chrome.ts:403` declares `transition:"opacity 200ms ease-out"` on `childrenWrap` but nothing ever writes `childrenWrap.style.opacity` — only `display` is toggled (`:560,562`), which does not fire the transition, so the declaration is dead; and `ChartTooltipConfig.children` is a live type-checked field with **zero** consumers repo-wide (no `</ChartTooltip>` outside `internal/`), while `content` has exactly 4, all `LiveLineChart` (`bench/app/src/scenarios/migrated-liveline.tsx:150`, `bklit-liveline.tsx:220`, `qa/api-compat/liveline.tsx:47`, `showcase/components/demos/liveline.tsx:67`). **One structural finding worth keeping for T20:** the box has no outbound coupling — `update()` computes it last (`hover-chrome.ts:605-619`) and no other layer reads its position, flip state or DOM; `prevFlip` (`:233,617-618`) is confined to the box block. Whatever replaces the box breaks nothing else, provided the shared `visible`/`showing` bookkeeping survives. |
| **D387** | 2026-08-27 | **B5/T14 — SKIPPED, with reason. `stagger()` is already adopted; T14 proposes migrating to a primitive the code has been calling since 24 Aug.** T14 was "dataset-flag/querySelector state machine -> `stagger()` + phase filters; read state via `onRender(scene)`/`getScene()`; join via `scene.points[].key`", acceptance "no `dataset.bkm*` reads; `.ts-chart__dot` queries retained; `scatter`/`candlestick` pre+post". **Blocker 1, decisive and verified by me directly: `showcase/migrated/charts/internal/native-stagger.ts` already exists** and already does `import { stagger as nativeStagger } from "@tanstack/charts/motion/definition"`, with **12 live call sites** — `pie-chart.tsx:507,545`, `ring-chart.tsx:662,705,706`, `candlestick-chart.tsx:943`, `gauge.tsx:821,829,1111,1120`, `composed-chart.tsx:1435`, `internal/chart-markers.tsx:359`. Its header records exactly why it is used as a delay-*value* calculator and not a pipeline integration: *"None of this package's enter-reveal choreography is wired into TanStack's own motion pipeline: every caller below drives its reveal with hand-rolled WAAPI ... reproducing bklit's exact keyframes/easing byte-for-byte, entirely outside TanStack's render/motion cycle. That means there is no `ChartMotionContext` supplied by a TanStack renderer to feed `stagger()`'s `delay(context)` callback — this helper builds a minimal synthetic one instead."* `stagger()` returns `Pick<ChartMotionTiming,'delay'>` and nothing else — `offset + each * max(0,index)`, gated on `phase`/`roles`. Adopting it *further* cannot remove a state machine, because it supplies a number, not state. **Blocker 2: all eight cited lines are wrong.** Spot-verified: `line-chart.tsx:643` is `if (typeof v === "number") next = v >= 0 ? 0 : 1;`, `composed-chart.tsx:1221` is a tick-pill comment, `internal/deferred-reveal.ts:43` is a bare `//`. Two more are near-misses inside the right function (`candlestick-chart.tsx:832` is the comment above `handleRender` at `:825`; `dash-tail.ts:61` is one line above the real `querySelector` at `:62`); the rest point at unrelated code. **Blocker 3: `scene.points[].key` has nothing to join against for roughly half the census.** The `bkm` naming covers four structurally distinct mechanisms, not one: (A) the `bkmRevealed` replay latch (`deferred-reveal.ts:62-91`, 9 charts, all read inside `onRender`), (B) `.ts-chart__*`/`data-ts-key` node lookups needed to obtain an animatable `Element`, (C) the static `data-bkm-chart` root anchor read by `hover-chrome.ts:195`, and (D) six out-of-scene overlay states with **no `ChartPoint` counterpart at all** — `scatter-hover-chrome.ts:193,278` (`bkmActiveScale` on a hand-built `<g>`), `hover-chrome.ts:201-217` (its own `bkm-hover-layer`/`bkm-marker-active-layer` SVGs), `x-axis-overlay.tsx:353-354`, `dash-tail.ts:227`, `bar-pulse-mark.ts:211-215`, `tooltip-chrome.ts:677`, `gauge.tsx:1107-1224`. (D) is unreachable by any scene join, permanently. **Blocker 4: `dash-tail.ts` is a category error in this row** — it is rAF-polling geometry measurement (`getAttribute("d")`, `getTotalLength()`, clip-path recompute), with no delay, phase or stagger component to migrate. **Blocker 5: the acceptance criteria are internally inconsistent** — "no `dataset.bkm*` reads" plus "`.ts-chart__dot` queries retained" concedes that the lookup half survives, and `scene.points[].key` yields *data identity*, never a live `Element`, so a `querySelector`/ref-map is still required either way. **Narrow residue, parked not actioned:** mechanism (A)'s DOM stamp could become an in-memory `useRef` latch, since Q5a clears the D380 circularity worry (every `isRevealed`/`markRevealed` call already runs inside `onRender`, post-paint — `line-chart.tsx:1071`, `composed-chart.tsx:1541`, `candlestick-chart.tsx:1083`, `scatter-chart.tsx:1026`, `choropleth-chart.tsx:688`). But that is storage churn, **not native adoption** — it swaps one app-owned latch for another, touches the reveal path of 9 charts including `candlestick` (**T0**, 0.0199 headroom on h30), and buys zero sanctioned-API surface. Declined on risk/benefit; logged for 5.3.5 consideration. |
| **D388** | 2026-08-27 | **B5/T15 — SKIPPED, blocked behind T20/B7 on all three named mechanisms.** T15 was "remount key / `update({renderer})` / `motion({initial:'always'})` replaces the hand-called `handleRender`", acceptance "replay visually identical; both `sunburst` densities unchanged". Citations drift by one throughout (`handleRender` is defined at `:650` not `:651`; the imperative fallback call is `:919` not `:920`; the ref+replay+wiring region is `:1058-1137` not `:1059-1138`) — substantively right region, so not a premise error, unlike the rows above. **Mechanism 1 — `motion({initial:'always'})` is real but unreachable.** `motion.d.ts:6-17` does declare `initial?: boolean \| 'always'`, documented as *"Animate the first client render. Use `always` to replay adopted server-rendered SVG"* — an SSR-hydration concern, not caller-triggered replay. It returns a `ChartRenderer` consumed via the `renderer` prop, and **`<RendererChart>` appears nowhere in `showcase/migrated`** (the only two hits are comments in `composed-chart.tsx:1355-1356`). All 14 charts use plain `<Chart>`, whose props type carries **no `renderer` field at all** and which hardcodes its own renderer at `react-charts/dist/Chart.js:13-14`: `const renderer = React.useMemo(() => createSvgChartRenderer(renderSvg), [renderSvg])`. Architecturally excluded, not merely unused. **Mechanism 2 — `update({renderer})` is core-only.** It exists on `ChartRendererHost` (`dom-types.d.ts:211-216`) but `RendererChartImplementation` owns the adapter privately and calls it from its own layout effects; `Chart`/`RendererChart` are plain function components, not `forwardRef`, and no hook exposes the host. Reachable from React only by changing the `renderer` prop identity — which again requires `<RendererChart>`. **Mechanism 3 — a remount key is strictly more destructive than what ships.** Today's `playKey` effect (`:1071-1104`) deliberately cancels only the reveal animations and clears only the once-per-cycle guards, **preserving** `internalFocusId`, `hoveredArcIndex` and `zoomT`. A `key` remount resets `internalFocusId` to `rootId` — it zooms all the way out — and destroys hover and any in-flight zoom tween, breaking "replay visually identical" for any replay invoked mid-drilldown unless every caller is forced onto controlled props. **Independent of the renderer question:** the reveal spans both in-scene DOM (arc paths via `path[data-ts-key^="sunburst-arcs:"]`) and app-owned siblings (`SunburstLabelsOverlay`, `SunburstHitLayer`, `SunburstCenterOverlay`). No native replay mechanism reaches the siblings, so hand-rolled orchestration survives whichever strategy wins. **Re-sequence to B7, do not void:** unlike T13, T15's blocker is exactly the `<Chart>`->`<RendererChart>` migration T20 performs, and nothing else. |
| **D389** | 2026-08-27 | **B5/T16 — SKIPPED as scoped: both named mechanisms are absent from the runtime, and the acceptance criterion gates a scenario that cannot regress.** T16 was "wipe-rect redesign via scene coords or keyed extension nodes (**no loading API exists**)", acceptance "no unscoped `getElementById`; `arealoading` unchanged". The file citation is the **first in this phase to be exactly right**: `internal/loading-chrome.tsx:200` is verbatim `const el = document.getElementById(`+"`"+`${clipId}-rect`+"`"+`) as unknown as SVGRectElement \| null;`. **"No loading API exists" is confirmed** — `grep -rl "loading\|skeleton\|placeholder"` over `dist/*.d.ts` returns **zero files**. **"Keyed extension nodes" is dead on arrival** — no plugin, extension, `defineMark` or `registerMark` surface exists anywhere in the dist; the mark set is closed. **"Scene coords" is available but pointless here** — `ChartScene` does carry `margin` and `chart` (`types.d.ts:934-945`), reachable through `onRender`'s context, but `LineLoadingPulse` does not want them: it draws its sine-wave placeholder from the component's own already-computed `margin`/`width`/`heightPx` React values (`line-chart.tsx:1253-1256`), and the `getElementById` call is only an existence/`width<=0` early-out guard inside the rAF loop, not a positioning read. Positioning is pure React state (`clipW`/`clipX`, `:243-244`) rendered as JSX attributes. So this is **not** D380-class circularity — nothing needs the wipe geometry to construct a definition — merely a one-effect-cycle ordering constraint that buys nothing. **The acceptance criterion is miswired, and I verified this myself.** `arealoading` renders `<AreaChartLoading />` (`bench/app/src/scenarios/migrated-arealoading.tsx:5-8`), and `area-chart.tsx:72` imports **only** `LoadingLabel, buildLoadingSkeletonRows` from `loading-chrome` — `LineLoadingPulse`, which contains the cited `:200`, is imported by **`line-chart.tsx:62` alone**. AreaChart's own wipe is a separate already-ref-scoped clip-path animation (`area-chart.tsx:974,985-988` via `containerRef.current.querySelector('.ts-chart__marks')`). **`arealoading` therefore cannot regress from any change to the code this task names.** The correct gate is the `line` scenario's `--state loading` (`compare-loading`) capture; there is no dedicated `lineloading` scenario. **Narrow residue, parked not actioned:** the real defect is genuine but small — one `document.getElementById` against an otherwise perfectly consistent ref-scoped pattern (`area-chart.tsx:974`, `sunburst-chart.tsx:109-121`, and every other reveal in the tree). It is the **only** `getElementById` in all of `showcase/migrated`. It is also **not currently reproducible**: `clipId` comes from React `useId()` (`use-sanitized-id.ts:1-5`), which is unique per instance within a root, and the harness mounts exactly one root (`bench/app/src/main.tsx:32`). So it is a latent multi-root hazard, not a live bug — and fixing it is app hygiene, **not native-API adoption**, so it is out of Phase 5's stated scope. Logged for 5.3.5 alongside the four stale comments and the two D386 defects. |
| **D390** | 2026-08-27 | **B5/T21b — VOID. Both halves are premise errors, and its stated gate precondition can never be met.** T21b was "heatmap B — geometry + timing: binning (`binTimeX/Y`, `binX/Y`, `binXY`) + `stagger()`", gated "**only after T21a is green**" (D368). **The gate precondition is unreachable:** T21a was permanently skipped (D381, made final by D382), so "T21a green" names a state this codebase has ruled out reaching at this pin. **Binning half — there is nothing to swap.** All five helpers exist and are real exports (`transform-bin.d.ts` `binX`/`binY` over continuous numerics with `thresholds`/`domain`; `transform-bin-time.d.ts` `binTimeX`/`binTimeY` over `Date` via a d3-style `TimeIntervalLike`; `transform-bin-xy.d.ts` `binXY` as a 2-D numeric histogram). But **the heatmap never bins.** Its public contract already requires pre-binned input — `HeatmapChartProps.data: HeatmapColumn[]` (`heatmap-chart.tsx:52`) where `HeatmapColumn { bins: HeatmapBin[] }` (`heatmap-utils.ts:36-38`). The only transforms applied are `filterHeatmapColumns` (time-range filter of existing columns) and `rotateHeatmapColumnBins` (`:213`, a `weekStartDay` rotation). `buildCellData` (`heatmap-components.tsx:84-112`) merely flattens `columns[c].bins[r]` into `{colKey,rowKey}` rows for `cell()` against `scaleBand` domains — **categorical placement of already-discrete cells**, a different operation from bucketing continuous values. Any real raw-data binning lives in caller/demo code outside the migrated chart tree, which this row does not cite. **Timing half — structurally incompatible, verified by me directly.** `stagger()` computes `offset + each * max(0,index)` from a flat `datumIndex`/`seriesIndex`. The heatmap's shipped reveal is a **seeded Lehmer PRNG keyed on the 2-D cell address**: `heatmapCellSeed(column,row) = column*1009 + row*9176`, plus `revealEpoch * 524_287`, fed through `seededRandom` (`state = state*16807 % 2147483647`) and multiplied into a spread — `computeHeatmapEnterFadeDelayMs`, `internal/heatmap-animation.ts:51-80`, driving hand-rolled WAAPI at `heatmap-components.tsx:609-620`. A deterministic linear function of one flat index cannot express a pseudo-random function of two, under any parameterisation. The shimmer's non-monotonic per-cell distribution *is* the effect. **Confirmed independent of T21a's focus blocker** (T21b touches neither focus nor hover), so this is a distinct ruling, not an inherited one. `heatmap` is **T1** with 0.0723 headroom and this row is, in its own words, "the only half that can move geometry or retime the reveal" — there is no correct implementation path to risk it on. Recorded void, in the disposition class of T21a/T9/T13. |
| **D391** | 2026-08-27 | **B5/T17 — IMPLEMENTED AND GATED GREEN. The phase's first task since B1b to ship code, at bit-exact pixel parity on both scenarios in the file.** The linear gauge rendered a hand-written raw `<svg>` sibling to the TanStack scene; it now renders as a TanStack custom mark. **Mechanism:** `createMark` (genuine public export — `dist/index.d.ts:46`, `dist/mark.d.ts:7`) returns two `ScenePolyline` groups (`gauge-bg`, `gauge-active`) whose `path` field carries the existing `createNotchPath(...)` output verbatim. `path` is public API (`types.d.ts:886-889`) and the renderer honours it — `svg-renderer.js:42` emits `node.path ?? node.points.map(...)` — so no geometry was recomputed: `notch.points` is passed straight through with **no centre-subtraction** (the arc orientation subtracts a centre; linear must not, and does not). `margin` is zeroed and the theme gradient id is preserved by moving the `<defs>` paint server onto `defineChart`'s `gradients` option. **Reveal rewiring:** the pre-paint `useLayoutEffect` + `groupRef` + `[data-bkm-key^="bg-"]` key-parse was replaced by `onRender` + `chartHostRef` + a `[data-ts-key="gauge-bg"]` **positional** lookup. This substitution is safe because `gauge-notch.ts:337,447` set `isActive: i < activeNotches` — an ascending prefix — so positional index is identically `notch.index`, and every `delayMs`, `transformOrigin` and reveal key is preserved exactly. **Gate (verified by the lead, not reported by the agent):** `gaugelinear` n=1000 — settled / hover-30 / hover-50 / hover-70 all **PASS at `diffPixels: 0` of 960000**, literal zero rather than a sub-gate percentage, against a BASELINE §1 of 0.0000 ×4. The row's acceptance was "must stay 0.0000% on all four probes — any movement = revert"; it is met exactly. **`gauge` (arc) regression-gated too**, since both orientations share `gauge.tsx`: **0.0064 ×4, bit-identical to BASELINE §1**. **A fabricated agent claim was caught and disproved.** The implementing agent reported it had to migrate two `GaugeArc` `defineChart` call sites from flat `x:null,y:null` to `scales:{x:null,y:null}` to typecheck. False, three ways: (1) the lead's own pre-dispatch read of `gauge.tsx` already showed the `scales` form in arc; (2) `git show HEAD:...pie-chart.tsx` has the flat form while the working tree has the `scales` form, with `pie-chart.tsx` untouched today (mtime 2026-08-26 23:06); (3) the migration is therefore a pre-existing uncommitted change spanning **all** charts from an earlier session. The arc gate above confirms it independently. Logged as a reliability signal against the rest of that agent's report — which is why every claim in this entry was re-verified at source. **Four residual risks the pixel gate structurally cannot see, accepted deliberately:** (1) **a11y/DOM shape** — the svg root goes from `aria-hidden="true"` to the renderer's `role="img" aria-roledescription="chart" aria-label="Gauge chart"` + `tabindex`; `svg-renderer.js:19` has no `aria-hidden` branch at all, so this is not opt-out-able. An AT-tree diff would flag it; a pixel diff cannot. Arc already ships the identical tradeoff, so this is consistency with the sanctioned precedent, not a new divergence. (2) **Chart-host sizing** — the wrapper moves from `width:"100%"` to literal-px `width`/`height`, matching arc. Pixel-neutral for the `top`/`bottom` label placements the bench scenario exercises, but it changes the character of the **disclosed pre-existing** `labelPlacement="left"\|"right"` overflow quirk: a fixed-px div can now overflow a shrunk flex sibling. Out of the gate's reach; parked for 5.3.5. (3) **Pre-paint -> post-render timing** — the removed `useLayoutEffect` carried an explicit comment (see **D52**) that it *must* run pre-paint so the mount path's group-hide lands before the browser paints the revealed rest state; the first draft's plain `useEffect` caused "a one-frame flash of the final gauge before the reveal started". `onRender` fires post-render, so that flash is re-admitted. Invisible to a settled-or-hover screenshot gate by construction. Accepted because arc uses `onRender` too and consistency within the file outweighs a one-frame mount artifact — but it is a real parity regression the gate did not and could not catch. (4) `chartHostRef` is attached on **both** the fixed-width and responsive-width branches, a deliberate divergence from arc's single-branch `containerRef` and strictly safer than mirroring arc literally. **Net:** B5 ships 1 of 5 tasks; the six-batch zero-code streak is broken. |
| **D392** | 2026-08-27 | **B6 open — the DoD's own visx census is wrong, and completing T18+T19 as written would leave Phase 5's stated goal unmet.** `PLAN-phase-5.md:118` defines part of the Definition of Done as "zero `@visx/*` imports in migrated code; census re-run appended to `03-dependencies-and-packaging.md`". The census that target refers to (`go-to-plan.md:95-99`) asserted "Exactly **5** real import/export statements remain, in 2 files — everything else matching `@visx` in `showcase/migrated` is prose in comments". **Both halves are false.** Direct count of non-comment statements gives **7 in 4 files**: `internal/pattern-preset.tsx:7` (`@visx/pattern`, T18); `choropleth-chart.tsx:29,30,31,224` + `index.ts:223` (`@visx/zoom`, T19 — **5** statements, which the census recorded as part of a total of 5 for *both* packages); and **`internal/gradients.tsx:14` — `@visx/gradient`, which no task row in the phase covers at all.** That file imports **11** components (9 fixed-stop presets plus the general `LinearGradient`/`RadialGradient`) and re-exports them through the public barrel at `index.ts:579`, so it is both a live dependency and a public API surface. It is not prose. **Consequence:** T18 and T19 could both land green and the phase would still fail its own DoD census. Folded the gradient removal into the T18 audit rather than leaving it stranded, on the basis that `gradients.tsx`'s own header declares it the "same wrapper shape as ./pattern-preset" — the same port, twice. Census corrected in place at `go-to-plan.md`. **Second finding, a trap avoided: the packages must NOT be uninstalled.** `ledger.md:76` (quoting `02-visx-removal.md:7`) records that `@visx/*` must stay installed at workspace level because vendored `repos/bklit-ui/packages/ui` imports them directly and compiles as source via `transpilePackages`; uninstall is **Stage 2, gated on legacy-bklit sunset** (`03-dependencies-and-packaging.md:29`). `showcase/package.json` declares 14 `@visx` packages and the repo root declares 8 more; only 3 are imported by migrated code. Pruning the other 11 would look like progress on "remove foreign deps" and would in fact **break the vendored bklit reference implementation — impl-A of the QA gate itself**, destroying the instrument that proves parity. The DoD target is imports under `showcase/migrated/**`, never any `package.json`. No dependency manifest will be touched in B6. |
| **D393** | 2026-08-27 | **B6/T19 — RULED IMPLEMENTABLE BUT RE-ORDERED: the missing zoom gate is built FIRST, because the existing gate is structurally blind to every behaviour T19 would change.** Audit returned IMPLEMENTABLE-WITH-CAVEATS; the lead re-verified all four decisive claims at source. **(1) Four of the row's six citations are wrong.** `choropleth-chart.tsx:27-28` are `geojson`/`d3-geo` imports (only `:29` is visx); `:77` is JSDoc prose about the unrelated `revealSignature` prop; `:113-121` is `ChoroplethGraticuleProps` plus a section-header comment. The genuinely zoom-bearing lines are imports `:29-31`, props `:84-87`, context/type block `:123-141`, re-export `:224`, default matrix `:228`, `applyZoomToPoint` `:479-486`, `syncZoomTransform`/`handleRender` `:525-566`, `<Zoom>` render `:748-786`. Only the `index.ts:223` citation and its `:184-192` disclaimer are accurate. **This makes the tenth task row found to misname its own files, line numbers or premises.** **(2) No sanctioned API exists — T19 inverts the phase premise.** The dist contains exactly one zoom-shaped export, `zoomX<TValue>` in `interaction-zoom.d.ts`, a **1D continuous x-axis window** (`ZoomXWindow{start,end}`) for time series. No `pan`, `pinch`, `affine` or `TransformMatrix`-alike anywhere. Phase 5's stated method is "replace custom subsystems with sanctioned APIs"; T19 does the reverse — it replaces a battle-tested library with a hand-rolled subsystem. That is unavoidable here and is not a reason to skip, but it is recorded as a deliberate inversion. **(3) The pixel gate cannot see any of it.** Verified directly: `qa/screenshot.mjs` contains **zero** occurrences of `mouse.wheel`, `mouse.down`, `mouse.up`, `__benchZoomTo` or `choropleth` — the chart falls through the generic path (one settled capture + three `page.mouse.move` hovers), so all four probes render the map at its default transform. A replacement engine with backwards drag, wrong wheel step, wrong clamp semantics or a wrong zoom anchor would report **0.0000% differing pixels on every probe**. `__benchZoomTo` — the hook built for exactly this — is defined on both impls (`bklit-choropleth.tsx:180`, `migrated-choropleth.tsx:78`, states `reset\|zoomed\|panned`) and has **zero callers repo-wide**. `02-visx-removal.md:73` and `ledger.md:108` both already record the gap; D365 says the absence of a gate "is precisely why the deviation is written down". **(4) The public surface is wider than the row's acceptance text.** The row promises to keep "`initialZoom`, `reset()`/`setTransformMatrix()`" — but `choropleth-chart.tsx:775-781` publishes the **entire** `ProvidedZoom<E> & ZoomState` object through `ChoroplethZoomContext`, exported as `useChoroplethZoom()`/`ChoroplethZoomInstance` (`index.ts:213-223`): ~19 methods (`center`, `scale`, `translate`, `translateTo`, `setTranslate`, `invert`, `dragStart/Move/End`, `handleWheel`, `handlePinch`, `applyInverseToPoint`, …). Implementing only the two named methods would either silently break consumers or require narrowing an exported type — a public API change forbidden by the inherited main goal "keep same components API". **T19's real scope is the full interface reimplemented locally at identical shape, materially larger than the row's "~140-190 LOC" estimate.** **(5) Removing the import does not remove the dependency.** `repos/bklit-ui/packages/ui/src/charts/choropleth/choropleth-chart.tsx:5-6` imports `@visx/zoom` directly and `repos/bklit-ui/packages/ui/package.json:68` declares it; the vendored bklit build is **impl-A of every QA comparison**. Consistent with D392: the package stays installed regardless, so T19's entire benefit is the DoD census line, not a shipped-bundle saving. **Ruling:** T19 proceeds, but **not before a gate exists**. Building a from-scratch gesture engine against a gate that cannot observe gestures would be indistinguishable from doing nothing, and "QA gates are the law" is the phase's governing constraint. Dispatched a new `qa/zoom-gate.mjs` — a standalone script importing the already-exported `compareBuffers` (`qa/screenshot.mjs:146`), driving `__benchZoomTo` across `reset`/`zoomed`/`panned` on both impls at the same 0.5% threshold. **No protected harness file is modified**; the gap is closed by addition. This also closes a verification hole that predates B6 and would otherwise have persisted into 5.3.5. |
| **D394** | 2026-08-27 | **B6/T18 — IMPLEMENTABLE, dispatched with the `@visx/gradient` removal folded in (D392). Plus a self-correction: the 0.0199 candlestick headroom figure was my own error, not the board's.** **Self-correction first.** `captures/b5-pre.md:29` and **D387** — both written in this session — recorded `candlestick`'s worst-cell headroom as **0.0199**, derived from `b1c-pre.md`'s h30 reading of 0.4801. That reading is a non-reproducible excursion: three consecutive runs on the unchanged B5-complete tree all return **0.4953**. **`GATE-MAP.md:42` has always said `candlestick` (0.0047)** and `BASELINE.md` has always had h30 at 0.4953, corroborated across D362/D370/D371/D373/D377/D378/D384. The canonical board was right throughout; my two entries introduced the wrong figure and `captures/b6-pre.md`'s first draft then mis-blamed the tier table for it. All three files corrected in place. **True headroom: 0.0047 (~45 px).** **T18 verdict: IMPLEMENTABLE-WITH-CAVEATS.** The import is 4 components (`PatternCircles`, `PatternHexagons`, `PatternLines`, `PatternWaves`) at `pattern-preset.tsx:2-7`; only **2** are used internally (`PatternLines`, `PatternCircles` via `renderPatternPreset`) — `PatternHexagons`/`PatternWaves` are public-only passthroughs with zero internal callers. Seven of the row's eight call-site line numbers are stale (drift up to **+253** on `bar-chart.tsx`, cited `:1061`, actually `:1314`); only `heatmap-legend.tsx:50` is correct. Already logged as drift in D373, but the row still carries pre-correction numbers — the **eleventh** row found to misname its own citations. **Exact-parity hazards identified from the visx source and briefed to the implementer:** every caller already wraps the component's output in its own `<defs>`, and each component wraps again, so the shipped DOM is `<defs><defs><pattern>` — flattening it is a structural non-match; `Lines.js` computes its path from **`height` only**, never `width`, for every orientation, with `strokeLinecap:'square'` and `shapeRendering:'auto'` defaults no call site overrides; `Circles.js` leaves `fill` **undefined** so SVG's black default applies — "helpfully" emitting `fill="none"` would be a visible regression; `Hexagons.js` **ignores its `width` prop entirely** and overrides the tile to `width=size (3)`, `height=sqrt(size) (~1.732)` while computing the hex path from the passed `height`. **Gate-coverage finding — 5 of 8 call sites have zero pixel coverage.** Covered: `area-chart.tsx` (`patternarea` cycles all 8 presets via `__qaSetPatternPreset`, `qa/screenshot.mjs:746-757`), `bar-chart.tsx` and `brush-chrome.tsx` (`diagonal` only). **Uncovered: `background.tsx`, `reference-area-layer.tsx`, `heatmap-components.tsx`, `heatmap-legend.tsx`, and both `candlestick-chart.tsx` sites.** The candlestick case is the sharpest: the guard is `positivePattern.preset ? (...) : null` and **no scenario anywhere sets `bodyPatternPositive`/`bodyPatternNegative`**, so that code path never executes in any QA run. This cuts both ways — the T0 chart's 45-pixel headroom is **not** at risk from pattern work (the path is dead in-gate), but neither is there any pixel signal protecting it. `complement`, custom `radius`/`dotFill`/`tileBackground` and non-default `strokeWidth` are likewise live in `pattern-preset.tsx` and exercised by nothing. **Part B (`@visx/gradient`) verdict: IMPLEMENTABLE, entirely unguarded.** 11 components at `gradients.tsx:2-14`, re-exported at `index.ts:567-579`. The 9 named presets are trivial fixed-stop `LinearGradient` wrappers; `LinearGradient`/`RadialGradient` have a small fully-specified surface (`vertical` default **true** applying `x1/x2/y1/y2 = 0/0/0/1` only when no coord is supplied and no defaults at all when `vertical=false`; `fromOffset/toOffset` `'0%'`/`'100%'`; `children` fully replacing the two default stops; `gradientTransform` from `rotate` else `transform`). **Verified: zero usage of any of the 11 outside the wrapper and barrel** — every `LinearGradient`/`RadialGradient` hit elsewhere is a `displayName` string comparison (`pie-chart.tsx:131-132`, `gauge-notch.ts:63-64`), an `SVGLinearGradientElement` DOM type, or a comment. `grep -c gradient qa/screenshot.mjs` = **0**; the one api-compat fixture that looks like coverage (`qa/api-compat/pie.tsx`) uses hand-rolled stand-ins, not these components, and is typecheck-only. **Critical coupling flagged to the implementer:** because `pie-chart.tsx:131` and `gauge-notch.ts:63` classify consumer-supplied children by `displayName === "LinearGradient"`, the reimplementation **must preserve every `.displayName` string byte-for-byte** or caller-supplied gradients silently stop being hoisted into `<defs>` — a break invisible to every gate in the project. |
| **D395** | 2026-08-27 | **B6 — `qa/zoom-gate.mjs` built and verified: choropleth zoom/pan is now measured for the first time in this project, closing a gap that predates Phase 5.** Per D393 the zoom engine could not be replaced responsibly while the only gate was structurally blind to gestures. New standalone script `qa/zoom-gate.mjs` drives `window.__benchZoomTo` across `reset`/`zoomed`/`panned` on both impls, diffs with the **same** `compareBuffers` the main gate uses (`qa/screenshot.mjs:146`), same 0.5% threshold, results under `qa/results/choropleth-zoom/<ts>/` with a `report.json` shaped like the main gate's. **No protected harness file was modified** — verified: `git diff --stat qa/screenshot.mjs` empty, `bench/run.mjs`/`bench/report.mjs` clean, `qa/zoom-gate.mjs` the only addition. **Baseline (both impls still on `@visx/zoom`): `reset` 0.0000% (0 px) · `zoomed` 0.0104% (100 px) · `panned` 0.0028% (27 px), overall PASS.** Recorded in `captures/choropleth-zoom-baseline.md`. **Determinism: four independent runs, bit-identical** — three by the implementing agent, one re-run by the lead, who reproduced the numbers exactly rather than accepting the report. Stability comes from settling on a real `transitionend` for the `transform 0.18s ease-out` on the marks group rather than a fixed sleep; `reset` alone falls back to a timeout because no transition fires when the chart is already at identity. **The `zoomed`/`panned` cells are not zero even with both impls on the same library** — 100 and 27 subpixel/AA edge pixels that appear only once a non-identity transform is applied, because the two impls compose the transform through different DOM structures. Reported as found, not tuned away; they are the floor T19 must not exceed, not a target to beat. **Harness debt disclosed, not silently absorbed:** `qa/screenshot.mjs:1273` calls `main().catch(...)` with **no `import.meta` entrypoint guard**, so importing it executes the CLI and kills the importing process — verified at source. `zoom-gate.mjs` neutralises `process.argv`/`process.exit` around the dynamic import (restored in `finally`) instead of duplicating the diff logic, because duplication would let the two gates drift on what counts as a differing pixel. The failure mode is loud (a visible server/browser spawn), not a silently wrong function. **The correct fix is a one-line entrypoint guard in `screenshot.mjs` — a protected file requiring explicit gate-author approval, which has not been given, so it was not taken.** Parked for 5.3.5. **Net: `__benchZoomTo` went from zero callers repo-wide to a deterministic gate, and zoom-state verification is no longer MANUAL as `02-visx-removal.md:73` and `ledger.md:108` both still describe it.** |
| **D396** | 2026-08-27 | **B6/T18 IMPLEMENTED AND GREEN — `@visx/pattern` and `@visx/gradient` are gone from `showcase/migrated/**`, leaving only the five `@visx/zoom` statements T19 owns. Two numbers in the agent's report were wrong and are corrected here.** `internal/pattern-preset.tsx` (+361) drops `@visx/pattern` for local `Pattern`/`LinesImpl`/`CirclesImpl`/`PathImpl`/`HexagonsImpl`/`WavesImpl` + `pathForOrientation`; `internal/gradients.tsx` (+283) drops the 11-component `@visx/gradient` import for local `LinearGradientImpl`/`RadialGradientImpl` + 9 fixed-stop presets. **Verified by the lead's own runs, not the agent's report.** Imports: `grep -rn '@visx' showcase/migrated` returns only `choropleth-chart.tsx:29,30,31,224` + `index.ts:223`, all `@visx/zoom`. Typecheck exit 0, no output. Gates: `patternarea` n=1000 **0.0002 / 0.0002 / 0.0541 / 0.1388 — bit-identical to BASELINE on all four**, plus all 8 `pattern-*` preset cells PASS (0.1229–0.1656); `brush` n=1000 PASS settled 0.0001; `barsquares` n=100 PASS 0.0049 / 0.1360 / 0.1503 / 0.1486. T0/T1 post-capture (`captures/b6-post-t18.md`): `candlestick` **0.3025 / 0.4953 / 0.3661 / 0.3618** and `heatmap` **0.4277 / 0.3466 / 0.3533 / 0.4256** — both bit-identical to `b6-pre.md` and to BASELINE §1, so T0's full 0.0047 headroom is intact. Per D392 **no package was uninstalled**: 14/14/8 `@visx` declarations survive and `git diff \| grep visx` on the manifests is empty. **Correction 1 — the agent's `barsquares` FAIL was an artifact of the wrong density.** It ran `--n 1000`; `GATE-MAP.md:30` mandates **n=100**. At n=1000 it reported overall FAIL with three tooltip-visibility failures and (correctly) stash-verified them as pre-existing — but at a density this gate does not use. At n=100 the run is PASS with `tooltipB=true` on every cell. There is no tooltip defect. **Correction 2 — its `patternarea` hover-70 of 0.1139 was a noise excursion, not an improvement.** The lead's re-run returns **0.1388, exactly BASELINE**. Same failure mode as the 0.0199 error corrected in D394: a single hover-cell reading treated as a new value. Hover cells must be re-run before any conclusion is drawn; settled cells have been deterministic in every measurement so far. **Fidelity where no gate can see.** Per D394 only 3 of 8 pattern call sites have pixel coverage and **all 11 gradient components have none** (`grep -c gradient qa/screenshot.mjs` = 0), so these were checked at source instead: `Lines` path templates are byte-identical to visx including newlines and indentation (sole difference `(3 / 4) * height` vs `3 / 4 * height`, evaluating identically); `Hexagons` preserves the quirk of ignoring its `width` prop and overriding the tile to `width={size}`/`height={sqrtSize}`; `Circles` keeps `radius = 2` with `fill` left undefined rather than `"none"`; `LinearGradient` reproduces the `vertical && !x1 && !x2 && !y1 && !y2` default-coordinate branch; and **all 11 `.displayName` strings are preserved byte-for-byte**, which is load-bearing because `pie-chart.tsx:131-132` and `internal/gauge-notch.ts:63-64` classify consumer-supplied children by `displayName === "LinearGradient"`/`"RadialGradient"` — a break there would be invisible to every gate in the project. Uncovered surfaces parked for 5.3.5: `complement`, custom `radius`/`dotFill`/`tileBackground`, `PatternHexagons`, `PatternWaves`, and all 11 gradients. |
| **D397** | 2026-08-27 | **B6/T19 — RULED: local port keeping `@use-gesture/react`, not a `zoomX` rewrite. Two facts fix the approach, and one of them is a gate hole the new zoom gate does not close.** **(1) `@visx/zoom` is not a pure math helper — its gesture engine is `@use-gesture/react`.** `Zoom.js:296-313` binds `onPinch: handlePinch` and a guarded `onWheel` (skipping when `pinching \|\| !active`, its own comment: "onWheelEnd emits one final wheel event which causes 2x scale updates for the last tick") through `useGesture` targeting `containerRef` — i.e. **native listeners on the SVG, not React props**. D393 already established the only native TanStack zoom API is 1D `zoomX`, so the sanctioned-API path cannot carry a 2D geo gesture. The faithful route is a local port of `Zoom.js` (349 lines) + `util/matrix.js` (123 lines) into `internal/zoom-engine.tsx`, **retaining `@use-gesture/react` verbatim**. That satisfies the DoD, which is scoped to `@visx/*` imports (`PLAN-phase-5.md:118`), while changing zero gesture behaviour. `@use-gesture/react@10.3.1` is already installed (resolves from the workspace root as a transitive dep of `@visx/zoom`) and must be **added as a direct `showcase` dependency** so the port does not rely on hoisting. Per D392 **no `@visx/*` package is uninstalled** — `@visx/zoom@4.0.1-alpha.0` stays declared, because `repos/bklit-ui/.../choropleth-chart.tsx:5-6` is impl-A of every QA comparison. **(2) The new zoom gate does not cover the gesture path.** `qa/zoom-gate.mjs` drives `window.__benchZoomTo`, which both scenarios implement via `useChoroplethZoom()` → **`setTransformMatrix`/`reset`** (`bklit-choropleth.tsx:89-93` states this is deliberate: absolute assignment so each named state is idempotent for repeat-run QA; "no pointer emulation, per D34"). So D395's reset/zoomed/panned cells exercise the matrix math and the `syncZoomTransform` DOM-write path and **nothing else** — wheel, pinch, and drag remain unmeasured, exactly as `handlePinch` and the 5-of-8 pattern sites were unmeasured in D396. Correctness there rests on source fidelity, which the lead will spot-check at the same depth as T18's. **(3) The public surface is wider than the task row claims.** The row says "keep `reset()`/`setTransformMatrix()`"; the real surface is the full `ProvidedZoom<E> & ZoomState` — **19 methods + 3 state fields** — reached via the exported `ChoroplethZoomInstance` (`choropleth-chart.tsx:131`) and `useChoroplethZoom()`, structurally identical to bklit's own `ZoomInstance` (`repos/.../choropleth-context.tsx:24`, which likewise composes visx's `ProvidedZoom` with a locally-declared 3-field `ZoomState`). All 19 + 3 must survive, and `handlePinch` keeps its `UserHandlers['onPinch']` type so the surface is byte-identical rather than merely compatible. `TransformMatrix` stays exported at `choropleth-chart.tsx:224` and `index.ts:223`, re-pointed at the local module. |
| **D398** | 2026-08-27 | **B6/T19 IMPLEMENTED AND GREEN — B6 CLOSED. `showcase/migrated/**` now contains zero `@visx/*` import or export statements, meeting `PLAN-phase-5.md:118`'s Definition-of-Done clause.** `internal/zoom-engine.tsx` (663 lines, new) is a line-by-line port of `@visx/zoom`'s `Zoom` component + all 9 `util/matrix.js` helpers + the types, keeping `@use-gesture/react` as the gesture engine per D397. The three imports and two `TransformMatrix` re-exports (`choropleth-chart.tsx:32,33,34,227` + `index.ts:223`) now point at the local module; `syncZoomTransform`, the `ZoomWithDrag` cast, the imperative `containerRef.current = svg` assignment and the entire `<Zoom>` prop set and render-prop body are untouched. **Gates (lead-run):** `choropleth` n=100 **0.0000 / 0.0000 / 0.1470 / 0.0000 — bit-identical to BASELINE §1 including three literal-zero cells**; the D395 zoom gate **0.0000 / 0.0104 / 0.0028 — bit-identical to its baseline**, the 5th such run and the first with the local port on impl-B; batch-close T0/T1 `candlestick` 0.3025 / 0.4953 / 0.3661 / 0.3618 and `heatmap` 0.4277 / 0.3466 / 0.3533 / 0.4256, **bit-identical to `b6-pre.md` and `b6-post-t18.md` on all eight cells**. `npx tsc --noEmit` exit 0. **Manifests, per D392:** zero removed lines in `showcase/package.json`, `git diff \| grep -i visx` on added/removed lines empty, `@visx` counts still 14/14/8; the sole addition is `"@use-gesture/react": "10.3.1"`, promoted from transitive to direct so the port does not rely on pnpm hoisting. **Fidelity where no gate reaches.** The gate drives `setTransformMatrix`/`reset` only, so wheel/pinch/drag are unmeasured; the lead compared the port against the dist source directly. Matrix helpers are **line-for-line identical including operand order** (bit-identical in floating point, not merely algebraically equal). The component body is line-for-line identical including **every `useCallback` dependency array**, the `matrixStateRef`-vs-state split with its "wheel listener does not have access to latest state" comment, the `isDragging` re-anchor inside `scale()`, and `dragMove`'s `if (options?.offsetX) translateX += options?.offsetX ?? 0` double-optional quirk. The `useGesture` config is verbatim — `target: containerRef`, `eventOptions:{passive:false}`, `drag:{filterTaps:true}`, the `pinching → cancel(); dragEnd()` branch, the `KeyboardEvent` exclusions, and the `onWheel` early return on `pinching \|\| !active` with both explanatory comments. All 19 methods + 3 state fields appear in the same declaration order. **Two deviations, both disclosed by the agent and verified harmless by the lead:** (a) `localPoint` was inlined from `@visx/event` (leaving it would have kept an `@visx/*` import and missed the DoD) — all 7 type guards, `getXAndYFromEvent`'s three branches and both the `createSVGPoint`/`matrixTransform(screenCTM.inverse())` and bounding-box-fallback paths reproduced; it returns a plain `{x,y}` instead of the `@visx/point` `Point` class, safe because all three call sites read only `.x`/`.y`; (b) the combined type alias is `ZoomInstance<E>` not `Zoom<E>`, avoiding a same-file collision with the component — safe because nothing in `migrated/**` or `bench/app/src/**` consumed visx's alias, only `ProvidedZoom`/`ZoomState` separately. A third difference the agent did not flag: visx wraps the render prop in a `<Fragment>`, the port returns `children(zoom)` directly — no DOM and no key semantics either way. **Residual risk parked for 5.3.5:** `choropleth-chart.tsx:565`'s imperative `containerRef.current = svg` races `useGesture`'s effect-time read of that ref. This is **unchanged** by the port and identical in both implementations, so it is not a T19 regression — but it means wheel/pinch/drag binding has never been proven by any gate in this project. |
| **D399** | 2026-08-27 | **B7 pre-implementation API verification (lead, at the 0.15.0 dist): the renderer switch T20 and T15 both depend on DOES exist and is reachable, correcting D388's framing — and T20's acceptance curve turns out to be the library's own default.** **(1) `RendererChart` is exported, aliased as `Chart`.** D388 recorded that "`<RendererChart>` appears nowhere in `showcase/migrated`" and that plain `<Chart>` "hardcodes `createSvgChartRenderer`". Both statements are true but together they read as *unavailable*, which is wrong. `@tanstack/react-charts`'s `exports` map has four subpaths (`.`, `./canvas`, `./core`, `./tooltip`), and `dist/core.d.ts:1` is `export { RendererChart as Chart } from './RendererChart.js';` — so `import { Chart } from "@tanstack/react-charts/core"` is a **near-drop-in** that takes a required `renderer` prop. `@tanstack/charts` additionally exposes `./motion`, `./motion/definition`, `./spring`, `./renderer` and `./react/core`. T15's re-sequencing to B7 (D388) still stands; only the availability framing was too pessimistic. **(2) The two prop surfaces differ in exactly two fields.** `RendererChartCommonProps` (`RendererChart.d.ts:3-20`) and `ChartCommonProps` (`Chart.d.ts:3-20`) are field-for-field identical across all 15 shared props; `Chart` alone has `renderSvg?: ChartSvgRenderer`, `RendererChart` alone has the **required** `renderer: ChartRenderer`, and `onRender`'s context type changes from `ChartRenderContext` to `ChartRendererRenderContext`. **(3) The `renderSvg` gap is not a blocker — `motion()` already renders through the resources path.** `gauge.tsx:858,1296` pass `renderSvg={renderChartSvgWithResources}` (the B2/T6-T8 paint-server work) and `RendererChart` has no such prop, which looked like a hard blocker for the two gauge call sites. It is not: `motion.js` imports `renderChartSvgWithResources` at `:11` and uses it unconditionally at `:484` (`prerender`), `:488` (the `mount` path) and `:495` (the default parameter of `createMotionSvgChartRenderer`). Under `motion()` the prop is **redundant, not lost**. **(4) T20's acceptance criterion is the library's default.** The row demands the M1b curve be preserved — 1100 ms, `cubicBezier(.85,0,.15,1)`. `motion.js:24-26` reads `const defaultDuration = 1100; const defaultStaggerRatio = 0.4; const defaultEasing = cubicBezier(0.85, 0, 0.15, 1);` — upstream ships exactly that curve as the default, so `motion()` called with no `transition` reproduces M1b by construction. `resolveTransition` (`motion.js:2756-2780`) also accepts `{type:'tween', duration, easing}` and `{type:'spring', stiffness, damping, mass, restSpeed, restDelta}` (`types.d.ts:427-440`), so both halves of the row's spring/tween requirement are expressible, and `motion({initial:'always'})` — documented in `motion.d.ts` as "Use `always` to replay adopted server-rendered SVG" — is the T15 replay mechanism. **(5) The row's "~100 call sites" conflates two censuses.** There are **27** real `<Chart` component call sites in `showcase/migrated/**`, of which **16 pass `onRender={handleRender}`** — this port's entire imperative animation architecture hangs off that one callback, so the `ChartRenderContext` → `ChartRendererRenderContext` change is the true blast radius, not the component swap. The "~100" figure refers to WAAPI `.animate()` sites, a separate count still being audited. |
| **D400** | 2026-08-27 | **B7/T20 — RULED: the spring half of the task has ZERO in-scope call sites, the tween half is already exact, and the "retire ~100 WAAPI sites" clause is the only real work — so T20 lands as a measured pilot, not a 45-chart sweep.** Audit returned IMPLEMENTABLE-WITH-CAVEATS; the lead re-verified every decisive claim and **corrects the audit's headline finding**. **(1) The audit's "decisive planning defect" is wrong at the mechanism level.** It reported that T20's spring directive and its M1b acceptance clause "cannot both be satisfied by a single `motion()` call", calling the two shapes mutually exclusive. The renderer-level transition is only a **fallback**: `motion.d.ts:9` documents it as "Default transition. Definition-local motion can refine or replace it", and `motion.js:2524-2528` resolves each mark's authored motion as `resolveTransition(authored.transition, …, fallback = transition)` after `apply(definitions?.default)` and a per-`markId` override pass. A tween default and per-mark springs coexist in one chart by design — which is exactly what B2/T8's per-mark `motion:` declarations exist for. **(2) But the conflict is moot, because the spring half has no in-scope sites at all.** The four tuned constants (`design-tokens.ts:31-37`: `TOOLTIP_SPRING {300,30}`, `TOOLTIP_BOX_SPRING {100,20}`, `HIGHLIGHT_SPRING {180,28}`, `ENTRANCE_SPRING {300,25}`) are consumed **exclusively** by hover-chrome and tooltip modules — `bar-`/`scatter-`/`candlestick-`/`choropleth-`/`live-`/`hover-chrome.ts`, `tooltip-chrome.ts`, `tooltip-components.tsx`, and `chart-config-context.tsx` (the default bag feeding them). Every one is **app-owned hover chrome, exempt under the residue rule** the acceptance clause itself states. The row's `{stiffness:170,damping:26,mass:1}` is confirmed to be merely `spring.js:1-7`'s no-args default, matching nothing we use; adopting it anywhere would have replaced tuned per-context feel with a generic default. **Ruling: springs are out of T20's scope entirely.** **(3) The tween half is already bit-exact, so it buys nothing.** `design-tokens.ts:27-29` holds `REVEAL_DURATION_MS = 1100` and `REVEAL_EASE_CSS = "cubic-bezier(0.85, 0, 0.15, 1)"`; `motion.js:24-26` holds the identical defaults (D399). Our WAAPI calls already emit the M1b curve — switching to `motion()` cannot improve fidelity it already has. **(4) The WAAPI sites are not what `motion()` replaces.** True count is **45 `.animate(` calls across 24 files** (not "~100"; 4 of the raw hits are prose inside `styles.css` comments, ~3 more are exempt chrome). But the targets are the wrong shape: `line-chart.tsx:762-765` animates `clipPath: inset(0 100% 0 0) → inset(0 0 0 0)` on the **whole marks group** — a wipe, not a mark transition — and `:794-797` gives each marker a delay derived from its own x-position (`(leadingEdge / innerW) * durationSec`, mirroring bklit `series-markers.tsx:102`). `motion()` animates enter/update/exit of scene nodes and offers `stagger`; that it can express a group clip-wipe or a geometry-derived per-element delay is **unproven**, and the audit did not establish it. **(5) The real risk is additive, and the audit missed it.** Adopting `motion()` does not merely relocate existing animation — it makes **every mark update animate** at 1100 ms where today most do not. The gate samples hover at 30/50/70 % through an interaction across 45 charts, including T0 `candlestick` at ~45 px of headroom. A blanket switch is a behavioural change measured against captures that are bit-exact today. **Decision: no sweep.** T20 proceeds as a single-chart pilot on **`area`** (n=1000, BASELINE `0.0003 / 0.0001 / 0.0001 / 0.1593` — three near-zero cells make it a sensitive detector with ample headroom), switching that one chart to `@tanstack/react-charts/core` + `motion()` with **`transition` left unset** so M1b holds by construction. The measurement decides whether the WAAPI-retirement clause is achievable at all; T0/T1 charts stay untouched until it is. T15 rides the same switch and is gated behind the pilot. |
| **D401** | 2026-08-27 | **B7/T20 — NOT ADOPTED; the `area` pilot is reverted and the file is back to its pre-pilot state (tsc exit 0). The switch is pixel-neutral, but it cannot deliver its own stated deliverable and it imports a behaviour the chart's design forbids.** **(1) Correction of my own prior attribution.** I reported that the pilot moved `area` hover-50 from BASELINE's `0.0001` to `0.0559` (~537 px) and re-ran twice to confirm; both re-runs returned `0.0559`, which I read as determinism. That inference was wrong. After reverting the pilot the cell **still reads `0.0559`**, and the harness's own history (`qa/results/area/*/report.json`, 44 in-window runs) shows hover-50 is bimodal at **1 px x14 / 537 px x27** — flipping within a single hour on 2026-08-26 with no edits in between. `537` is the *majority* mode; BASELINE captured the minority one. The pilot moved nothing: pre-pilot (2026-08-26T20-32) `537`, pilot `537`, post-revert `537`; settled `3`, hover-30 `1`, hover-70 within its own `1296/1517/1529` band throughout. Three matching readings are not evidence of determinism in a bimodal cell — see D402. **(2) The reason T20 is refused is behavioural, not pixel.** Confirmed at the 0.15.0 dist and unaffected by the measurement error: (a) `motion()`'s only group-level clip is `radialSweepClipPath` (`motion.js:995-1050`, pie/arc), so the horizontal inset wipe in `handleRender` **cannot** be expressed natively — the WAAPI-retirement clause is unachievable as written; (b) `motion.js:612` derives `animate` from `motion2.resize &#124;&#124; !resized` and **never reads the definition's `animation` option** (no `options.animation`/`resolveAnimation` reference exists in the module), so every data update would tween at 1100 ms, contradicting area-chart's documented policy that new data paints immediately and only a y-domain change tweens; (c) with the wipe retained and `initial:"always"` set, motion's opacity fades stack underneath it. **(3) Combined with D400** — the spring half has zero in-scope call sites and the tween half already equals `motion.js`'s defaults byte-for-byte — T20 has **no remaining deliverable**. Ruled closed as NOT ADOPTED. **T15 is unblocked**: it rides the same renderer switch, which is now demonstrated to mount cleanly and cost nothing in pixels; it is judged on its own merits, not on T20's refusal. |
| **D402** | 2026-08-27 | **MEASUREMENT INTEGRITY — the QA gate is not run-to-run deterministic on hover cells, and `candlestick` (T0) has silently FAILED the gate three times in this phase. This changes how every prior and future gate reading must be interpreted.** Prompted by the D401 correction, I swept all `qa/results/*/*/report.json` in the phase-5 window (>= 2026-08-20) and counted cells that *return to a previously abandoned value* — the signature of a per-run race rather than a code-caused shift. **(1) 71 gate cells across ~30 (chart, density) buckets demonstrably flip-flop.** The magnitudes are discrete and stable per cell, consistent with one element racing in/out of the capture: `line` h30 `0 x41 / 489 x2` and h50 `0 x13 / 518 x29`; `scatter` h30 `0 x32 / 731 x3`, h50 `0 x8 / 748 x22`; `composed` h30 `0 x31 / 696 x5`; `refarea` h30 `0 x16 / 516 x2`; `bar` (n=100) h50 `0 x7 / 16 x3 / 727 x25`; `area` h30 `1 x37 / 506 x4`. **(2) Consequence: BASELINE's near-zero hover figures are single samples of bimodal cells, not bit-exact references.** Every claim of the form 'bit-identical to BASELINE on all four cells' that I made for a *hover* cell in D394 / D396 / D398 is therefore over-stated — those readings are consistent with no change, but they do not prove it. **Settled cells are unaffected and remain sound** (`area` settled `3 x42/44`; `scatter` settled `4330/4329`), as are the source-level fidelity verifications those entries rest on (byte-identical path templates, `displayName` strings, matrix helpers) — none of which depend on pixel sampling. The batch conclusions stand; the strength of the pixel evidence behind them does not. **(3) `candlestick` T0 is under-provisioned against its own variance.** GATE-MAP records 0.0047% headroom (45 px) on h30. The in-window measured spread of that cell is **4609-4953 px, 344 px wide** — 7.6x the headroom. Three runs crossed the 0.5% gate outright and were recorded as FAIL: **2026-08-20T04-08-24 (0.5159%), 2026-08-24T08-15-33 (0.5093%), 2026-08-26T22-16-06 (0.5082%)**. None was surfaced in a capture file or log entry; each was a run I did not sample. The T0 'revert-first, never fix-forward' policy is sound but was being applied to a signal noisier than the margin it protects. **(4) Standing rule from here.** A hover-cell reading is compared against the **mode distribution** in `qa/results/<chart>/*/report.json`, never against a single BASELINE sample; a change is attributed to an edit only when the new value falls outside the cell's established mode set. Re-running twice is explicitly *insufficient* — that is what produced the D401 error. Settled cells remain the primary attribution signal. **Root-causing the race itself is added to the 5.3.5 parked list (now 12 items) and flagged as the highest-value item on it**, since it currently gates T0 pass/fail by luck. |
| **D403** | 2026-08-27 | **ROOT CAUSE of D402's gate race, diagnosed from the stored diff PNGs — no protected-file change needed, and the news is good for parity: the bimodal cells are a +/-1 device-pixel quantization of the tooltip panel's position, not a fidelity defect.** Method: for a high-mode capture, take the diff mask's bounding box and cross-correlate impl A against impl B over that box at horizontal offsets dx in [-3, 3], scoring mean absolute luma difference. **(1) Four independent charts, one mechanism.** `line` h50 (518 px, bbox 138x37) aligns at **dx=+1** with residual SAD **0.000** vs 20.71 at dx=0; `scatter` h50 (748 px, 138x74) at **dx=-1**, SAD **0.018** vs 13.42; `composed` h30 (696 px, 128x64) at **dx=-1**, SAD **0.001** vs 17.99; `area` h50 (537 px, 474x41) at **dx=-1**, SAD **0.233** vs 5.71. A residual of ~0 after a one-pixel shift means the two images are *identical content, offset by one pixel* — confirmed visually: both panels read `Fri, May 14 / seriesA 1,592.44`, and the panel's left edge sits at x=564 in A and x=563 in B. Scanline profiling shows the differing pixels are exactly the two 11 px text rows plus the panel edge. **(2) The direction varies** (`line` +1, the other three -1), which rules out a systematic layout offset between the implementations and points to a rounding race: the panel's left edge rests near a half-pixel boundary and the two impls quantize it to different integers on some runs. Each cell therefore has one stable non-zero mode whose magnitude is just that panel's antialiased pixel count (~500-750 px), and a zero mode when both round the same way. **(3) Parity implication: these cells are measurement noise, not divergence.** A sub-pixel tooltip placement difference of at most one device pixel is imperceptible and is not a design-parity failure. This *raises* confidence in the migration rather than lowering it — but it means the affected cells carry no information at ~500 px resolution, which is why single-sample BASELINE comparison misled me in D401. **(4) `candlestick` (T0) is NOT the same mechanism and remains the real risk.** Differencing the FAIL run `2026-08-20T04-08-24` (4953 px) against a PASS run `2026-08-26T10-04-04` (4755 px): **4611 px are shared** — the chart's inherent, well-known divergence — and the **342 px unique to the failing run are spread over a 953x384 box**, clustered in columns ~334-398 at the hover locus (hover-30 lands at x~340) across 52 scanlines. That is the hovered-candle highlight/crosshair region, a larger and less localized settle-timing effect than the tooltip panel. **The T0 exposure is structural: the chart sits inherently at 0.480-0.496% against a 0.5% gate, so a ~200 px settle excursion tips it over.** Widening that margin is a fidelity question for `candlestick` itself, not a harness question. |
| **D404** | 2026-08-27 | **B7/T15 — NOT IMPLEMENTABLE, and this time the blocker is structural rather than a reachability gap. B7 now closes with both its tasks refused and zero code changed.** T15 was re-sequenced here by D388 because all three of its mechanisms needed the `<Chart>` -> `<RendererChart>` switch. D401 removed that blocker — the switch exists and is pixel-neutral — so T15 got a real audit. It fails on three independent grounds, each verified by the lead at the 0.15.0 dist rather than taken from the agent. **(1) `motion()`'s radial sweep cannot express sunburst's choreography.** Sunburst declares **one** `radialArc(arcRows, ...)` mark (`sunburst-chart.tsx:616`), and `polar.js` returns `nodes: [{kind:"group", className: classes("ts-chart__arc", ...), children: nodes}]` — a **single** `<g class="ts-chart__arc">` wrapping every row as sibling `area` nodes, not one group per arc. `createArcTracks` (`motion.js:987-1005`) does `querySelectorAll("g.ts-chart__arc")` and derives **one** `{startAngle, sweep, radius}` per group via `sceneArcGeometry`, which walks every child and accumulates the max sweep/radius across all of them — then calls `timingFor({phase:"enter", role, ..., datumIndex: 0, datumCount: 1})` and animates a single `radialSweepClipPath(geometry.startAngle, geometry.sweep * progress, geometry.radius)`. **`datumCount: 1` is decisive: stagger keys off datumIndex/datumCount, so per-arc timing is structurally unavailable** — the whole sunburst gets one wedge on one progress curve. What it must replace is per-arc: `buildRevealKeyframes` (`internal/sunburst-reveal.ts:92-125`) emits 64 `d` samples sweeping **each arc's own** `a0 -> a0 + span*p` at fixed radial extent, and `buildRevealTiming` (`:45-73`) gives each arc `delayMs = (ringIndex * 0.12 + index * 0.08) * scale * 1000`, bucketed by depth and ordered by clockwise fraction. A global wedge is a visibly different effect, not a pixel-neutral swap. **(2) The D401 update-path regression bites harder here.** `motion.js:606-607` sets `resized` only when width or height changed, so on a hover-driven reconcile `!resized` is true and `animate` is true (`:612`). `arcRows` depends on `hoveredArc` (`sunburst-chart.tsx:607`), so the definition recomputes on **every hover**. This is not hypothetical: the comment at `:673-680` documents exactly this as a fixed Wave-1 regression — a DOM-stamp guard that *"fired on every hover... replaying the full 1100ms reveal on each pointer move"* — now handled by ref-based `seenRevealedRef`/`playCycleRef`. `motion()` has no per-datum already-revealed concept, so adopting it re-creates a regression this project already measured and fixed. **(3) There is no caller-facing replay hook at all.** `RendererChart.d.ts:3-27` declares no `ref` and returns a plain `React.JSX.Element`; `ChartRendererHost.update` (`dom-types.d.ts:213`) is unreachable from React. `motion.d.ts:7` documents `initial: 'always'` as *"replay adopted server-rendered SVG"* — SSR hydration, not on-demand replay. The only lever left is a `key` bump, which per `renderer.js:628` -> `:645` destroys and remounts the entire surface — strictly more destructive than today's `playKey`, additionally tearing down the `pointerleave` listener bound directly to the old `svg.ts-chart` node (`sunburst-chart.tsx:811-824`, deps `[setHoveredArcIndex]`) and every in-flight zoom/reveal WAAPI handle. **Not a premise error** — D388 already recorded that T15's citations drift by one but name the right region. This is the plan proposing a native mechanism that the native renderer genuinely does not have. **B7 closes: T20 NOT ADOPTED (D401), T15 NOT IMPLEMENTABLE (D404), zero code changed.** Its lasting output is D402/D403 — the gate-variance finding the T20 pilot exposed. |
| **D405** | 2026-08-27 | **B8/T22 — ACCEPTED-WITH-LOG, exactly as the task row prescribes. Both upstream blockers are re-verified as still closed at the 0.15.0 pin, so neither half executes. B8 closes; 5.3 implementation is complete.** T22 is conditional by construction — *"Executed **only** if unlocked upstream; otherwise accepted-with-log"* — and both its items are named in `PLAN-phase-5.md:20`'s explicit out-of-scope list. The condition was checked at the dist rather than assumed. **(1) Sunburst label channel (D16) — still blocked.** `RadialArcOptions` (`polar.d.ts:98-125`) carries `startAngle`/`endAngle`/`padAngle` as *input* channels only; there is **no label channel of any kind**. And the computed node angles are still not exposed: `ChartPoint` (`types.d.ts:727-745`) publishes `x`, `y`, `color`, `datum`, `datumIndex` and the value fields — **no angle field**. The library derives angles internally from cartesian coordinates when it needs them (`motion.js:1128-1130`, `polarPointAngle = Math.atan2(point[0], -point[1])`), which is precisely the workaround our overlay already performs. The blocker is unchanged, so the accepted deviation stands. **(2) Tick pills (D6 fragment) — still blocked.** `ChartAxisTickLabelOptions` (`types.d.ts:196-206`) exposes `rotate`, `thin`, `fontSize`, `fontWeight`, `opacity`, `anchor`, `dx`, `dy`, `motion` — and **no `fill`, `color`, or `background` accessor**. A pill needs a painted backing shape behind the label; the axis surface offers none. The custom-mark route remains the documented alternative, with no urgency per `PLAN-phase-5.md:20`. **No code changed, no gate run** — there is nothing to gate. **This closes 5.3.2: B0-B8 are all dispositioned.** Final tally: **2 batches shipped code** (B0/B1a/B1b, and B5's T17), **B6 shipped both its tasks**, and **six task-groups closed as no-op skips** (B1c, B1d, B2, B3, B4, B7). Remaining phase work is **5.3.5** only. |
| **D406** | 2026-08-27 | **5.3.5 dependency census re-run — the `@visx/*` DoD clause is met at 0 import sites, and two carried Stage-1/Stage-2 assumptions are corrected.** Census over 154 files under `showcase/migrated/**`: 0 `@visx` import/export statements (21 textual mentions remain, all comment prose documenting the ports — `choropleth-chart.tsx:2,4,7,129`, `internal/zoom-engine.tsx:3,5`). Per D392 nothing was uninstalled: declarations stand at 14 / 14 / 8 across `showcase/package.json`, `bench/app/package.json`, root, because `showcase/repos/bklit-ui/.../choropleth-chart.tsx` imports `@visx/zoom` and is impl-A of every QA comparison. **Correction 1:** `d3-sankey` is now **type-only** (`internal/sankey-layout.ts:11-14` is an `import type`, erased at build) since layout moved to `sankeyDiagram()` from `@tanstack/charts/network/sankey` — so Stage 1's runtime goal is already met; the manifest entry can be demoted to a devDependency at Stage 2, deferred because it changes no emitted byte. **Correction 2:** the Stage-2 note claiming an `@visx/zoom` uninstall would also drop `@use-gesture` no longer holds — the T19 zoom port still calls `useGesture` (`internal/zoom-engine.tsx:37`) and the package is now a direct dep at `showcase/package.json:20` (10.3.1 exact); the removal drops `@visx/zoom` + `@visx/event` + `classnames` only. **Parked, not fixed:** 12 `bench/app/src/scenarios/migrated-*.tsx` files still import `curveNatural` from `@visx/curve` — caller-side prop plumbing kept byte-identical to the bklit scenario beside it. `@visx/curve@4.0.1-alpha.0` is a pure d3-shape re-export (verified in `lib/index.js`) and curve factories are stateless, so behaviour is identical; the only consequence is that `@visx/vendor`'s bundled d3-shape copy lands in both sides of the bench measurement — symmetric, and inflating rather than flattering the migrated figure. Rewriting them would invalidate the standing A/B baseline. Full write-up appended to `research/phase-5/03-dependencies-and-packaging.md`. |
| **D407** | 2026-08-27 | **5.3.5 final full-matrix QA — 43/43 runs, 160 gate cells, and not one cell produced a value the harness has not produced before. The single non-excluded FAIL is a harness race that fired on the *reference* implementation.** Ran the full GATE-MAP roster minus the two excluded instruments (`candletween` n=1000 pixel-invalid by D330 §2; `ring` n=1000 invalid instrument by D335), strictly sequentially because the harness binds port 5198. Every cell was judged under the **D402 rule** — compared against the mode distribution in `qa/results/<chart>/*/report.json`, never a single BASELINE sample. Result: **0 cells outside their own historical range**; 78 bit-identical to `BASELINE.md`, 18 landing on a repeated historical value, 64 inside the historical range. **5 FAIL cells: 4 are `barloading` n=100**, not a parity gate by explicit prior design (its own scenario header + D341), measured-not-chased. **The 5th is `choropleth` n=100 hover-30** — `1.2434%`, `tooltipA=false tooltipB=true`, harness reason *"(A=bklit): tooltip not visible 700ms after hover"*. Ruled a settle race, not a divergence, on three independent grounds: (1) **the failing side flips** — 2026-08-18 read `1.2451%` with `A=true B=false`, i.e. the opposite implementation, and a real defect cannot change which impl it afflicts; (2) the side that failed today is `showcase/repos/bklit-ui/**`, protected and unmodified all phase; (3) **six consecutive re-runs PASS at exactly `0.0000%`** with both tooltips visible — the 18-sample distribution is **13x0 px** against 4 outliers (11953, 11937, 9082, 9101), i.e. the whole tooltip panel painted or not, nothing between. `qa/screenshot.mjs:109-111` already records that `HOVER_WAIT_MS` was raised 150ms -> 700ms *"so bklit's tooltip spring fully settles before capture"*; 700ms is still marginal for the geo tooltip. Raising it touches a **protected** file, so it is parked with the D402/D403 gate-race item pending gate-author approval. **`candlestick` (T0)** passes all four cells, `hover-30` at **4771 px against the 4800 px gate — 29 px headroom**, consistent with D402's finding that the chart sits inherently at 0.48-0.50%; it was untouched in 5.3 and its settled cell is bit-identical to baseline (2904 px, 41 prior occurrences). Board captured at `docs/phase-5/captures/5-3-5-final-matrix.md`. |
| **D408** | 2026-08-27 | **Three stale source comments corrected — each made a factual claim about the runtime or about our own code that is false against `@tanstack/charts@0.15.0`. Comment-only; three confirmation gates re-run and all four cells of each land on their historical mode.** (1) `internal/bar-pulse-mark.ts` claimed the wave's clip "cannot live in the scene" because `SceneGroup` carries no clip. It does: `types.d.ts:853` declares `clip?: ChartBounds` and `svg-renderer.js:81` emits a real `<defs><clipPath><rect>` for it. The reason the wave stays out of the scene is narrower and now stated: that clip is RECTANGULAR only, the wave needs the bar silhouette polygon (D381), and the identity reconciler wipes injected nodes on every render. (2) `radar-chart.tsx:7` said hover "uses the focus subsystem"; `:459` sets `focus: focusDisabled` and hover is element-level `pointerenter`/`pointerleave` on the area `path`s (`:779`) and dot `circle`s (`:800`), gated on `pendingRevealRef` (D384). (3) `styles.css` carried a stray unclosed `/*` inside a stale sentence about cell-rect resting opacity, leaving the file's comment delimiters at 85/84; the sentence and the dangling back-reference at `:887` are gone and delimiters now balance 85/85. Confirmation gates, all PASS and all bit-identical to their historical mode: `heatmap` n=52 (settled 0.4277, h30 0.3466, h50 0.3533, h70 0.4256 — all four equal to `BASELINE.md`), `radar` n=6 (settled 0.0141 = baseline), `bardepth` n=100 (settled 0.0030 = baseline). NOT fixed and still open: `candlestick-chart.tsx:1008-1009`, where `heightPxCandle` guards on `width > 0` while its sibling `heightPx` guards on `ratio > 0` — that is a real code inconsistency, not a comment, on a T0 chart, so it needs a chart-level decision rather than a drive-by edit. |
| **D409** | 2026-08-27 | **`bench/run.mjs --all` re-run clean at 24/24 combos and 192 runs — settle time is the headline win, `area`/`line` mount at n=10000 is the one real cost, and every apparent delta against prior runs is inside the four-run spread.** Chromium 151.0.7922.34, written to `bench/results/2026-08-27T14-48-58-928Z/` + `latest.json`; 0 combos skipped. Settle-to-idle is better on tanstack in every one of the 12 chart/density pairs, ratio 0.03x–0.41x (`bar` n=100: 1592.9 -> 48.8 ms). Mount-to-paint is better in 10 of 12; the two exceptions are `area` n=10000 at 5.00x and `line` n=10000 at 4.15x. Heap is better at n=100 and across all `scatter`, worse for `area`/`line`/`bar` at n=10000. Compared cell-by-cell against the three prior full runs (2026-08-22, and 2026-08-24 twice): nothing moved outside its own historical spread — `bar` n=1000 settle looked like a +49% regression until the same cell's four-run spread turned out to be 1.52x. VERDICT: no regression. Two measurement caveats recorded so they are not re-derived: `m3b_sustainedUpdate` and `m3d_brushDrag` are `null` for all 24 combos (not collected, not zero), and `m2c_bundleCost.gzip` is a flat 38.3 vs 38.2 KB for every chart at every density because it measures the shared bundle, not per-scenario cost. Also re-confirmed: `('line',1000,'bklit')` reports `m3c_tooltipAppeared=false`, which resembles D407's tooltip race but is false identically in all three most recent full runs — stable and pre-existing, NOT a race. |
| **D410** | 2026-08-27 | **The 503-vs-472 barrel delta is NOT an API-parity break — it is the phase-4 CH17 ledger, already adjudicated to zero unaccounted symbols. Seven genuine barrel-only gaps were found underneath it and are now closed.** CORRECTION to an interim status I gave the user: I flagged ~117 legacy barrel names as possibly-missing from migrated and called it a possible parity break. Two things were wrong. First the number came from a naive regex that parsed comment text as export names; a comment-safe parser gives legacy 503 / migrated 472 / 116 missing. Second and decisive, `research/phase-4/wave-tasks/ch17-barrel-delta.md` is a ledger of exactly this delta (154 names then, 116 now — 38 closed since) and `docs/phase-4/LOG.md` records "CH17 resolves to ZERO unaccounted symbols". Classification of the 116: 19 systematic `Props`->`Config` renames (`AreaProps`->`AreaConfig`, `XAxisProps`->`XAxisConfig`, `ChartBrushLayout`->`BrushLayout`, ...), 29 context/provider/hook plumbing names that migrated has no `ChartProvider` to host (`ChartProvider`, `useChart`, `usePie`, `SankeyProvider`, ...), 68 other renames including `niceYDomain`, which migrated implements as `createNicedYScale` (`internal/y-domain.ts:105`) and therefore cannot re-export under the old name. Consumer scan over 354 files outside `showcase/repos/**` found exactly 2 of the 116 with any import site, and neither is a break: `DEFAULT_Y_AXIS_ID` (6 migrated charts import it from `./internal/y-axis-id` directly, not via the barrel) and `buildSunburstEnterTiming` (imported by `bench/app/src/scenarios/bklit-sunburst.tsx`, the *bklit* scenario legitimately using the legacy package). SEPARATELY, 7 names are implemented in migrated but were reachable only through `internal/`: `DEFAULT_Y_AXIS_ID`, `YAxisOrientation`, `YDomain`, `shouldTweenYDomain`, `isYDomainTweenPhase`, `getPrimaryYScale`, `useActiveMarkers` — all 7 public in the legacy barrel (`index.ts:360,620-637`). Each was diffed against its legacy counterpart at source before being re-exported: five are byte-identical contracts, `getPrimaryYScale` has an identical body differing only in migrated's `NicedYScale` where legacy names it `YScale` (the same rename already applied barrel-wide), and `useActiveMarkers` keeps the exact `ChartMarker[]` in / `ChartMarker[]` out contract over a different source of truth (legacy `useChartHover()`, migrated `MarkerActiveContext` + `useSyncExternalStore`) — it was already recorded at `index.ts:415` as "P5.2's to export" and is closed here. Added as re-exports only at `index.ts:672-698`; no chart module changed, no runtime behaviour added, `tsc --noEmit` exit 0. Deliberately NOT re-exported blind: `index.ts:659-665` documents that migrated changed some signatures on purpose (`useYScale` takes domains and inner height as ARGUMENTS because there is no `ChartProvider`), so a same-named export with a different signature would be worse than an absent one — which is why all 7 were source-diffed first. |
| **D411** | 2026-08-27 | **D402 ALREADY ESTABLISHED THIS AND I RESTATED THE OPPOSITE IN D407. Correcting: `candlestick` n=1000 hover-30 is a cell that STRADDLES the gate and has failed it repeatedly — not the "4771 px against 4800, 29 px of headroom" comfortable pass I wrote up in D407 and the 5.3.5 capture.** ORIGIN CREDIT: D402 §3 already recorded, one day earlier, that this exact cell is "under-provisioned against its own variance" — 45 px of GATE-MAP headroom against a measured 4609–4953 spread that is 7.6x wider, with three outright gate crossings named (2026-08-20T04:08 at 0.5159%, 2026-08-24T08:15 at 0.5093%, 2026-08-26T22:16 at 0.5082%). D402 also set the standing rule that a hover cell is judged against its mode distribution and never against a single sample. D407 then read one PASSing sample and reported headroom — precisely the error D402 had just prohibited. The plan's instruction to verify gate math independently applies to my own prior entries, and here it did not. WHAT THIS ENTRY ADDS BEYOND D402, all newly measured: (a) the full 58-run history rather than D402's >=2026-08-20 window — **9 readings over the 4800 px gate**, i.e. two more ordinary crossings than D402 caught (4827 x2 at 2026-08-19, before its window) plus 4879 / 4889 / 4908 / 4953 and the 6091 / 7126 / 12213 / 0-px degenerates, against dense modes at 4755 (11x), 4764 (18x), 4769 (3x), 4771 (1x), 4783 (6x) — a failure rate near 1 run in 7; (b) POSITIVE ATTRIBUTION, which D402 asserted only in general: in all four over-gate runs inspected, the `settled` cell is bit-identical to baseline (2904 or 2905, the two known settled modes) and ONLY the hover cell crosses. A geometry or paint divergence would move `settled` too, so confinement to hover state pins this to capture timing (`qa/screenshot.mjs:109-111`) rather than to the chart; (c) the band is live, not retired — the most recent crossing is 2026-08-26T22:16, and inside that one 25-minute window with no code change the cell emitted 4879, 0, 4755 x4 and 4609; (d) UNIFICATION: this and the `choropleth` FAIL in D407 are one instrument defect with two symptoms, not two findings. NOT a parity defect, and no grounds to touch a T0 chart. |
| **D412** | 2026-08-27 | **The `candlestick` `heightPxCandle` guard is NOT a candlestick-local inconsistency — 3 of the 4 height sites share it, the outlier is the SAFE one, and the exposure is a migrated-only helper with no legacy counterpart. Logged, deliberately not fixed at close-out.** BATCH-ORDER item 2 recorded `candlestick-chart.tsx:1008` (`heightPxCandle = width > 0 ? width / parseAspectRatio(aspectRatio) : 0`) as inconsistent with its sibling `:567-570` (`ratio > 0 ? width / ratio : 0`). Surveying all four sites inverts that: `area-chart.tsx:504` and `line-chart.tsx:177` BOTH use the `width > 0` form, so `:1008` follows the dominant convention and candlestick's own `heightPx` is the lone outlier — and the outlier is the correct one, because `ratio > 0` guards the DIVISOR while `width > 0` does not. Reachability: `internal/parse-aspect-ratio.ts:3` falls back to 2 only when the string is malformed, has a NaN part, or has a zero DENOMINATOR — so `"0/1"` passes every check and returns 0, and the three `width > 0` sites then evaluate `width / 0` = `Infinity`. `"-1/1"` similarly yields a negative height. Reachable only via a pathological caller-supplied prop; no gate in the 45-run roster exercises it. Legacy has NO `parseAspectRatio` anywhere in `showcase/repos/bklit-ui/**` — the helper is migrated-only, so the parity rule gives no answer here and cannot be cited either way. DECISION: log, do not fix. The one-line fix is identified and pre-researched for Stage 2 — widen the `parse-aspect-ratio.ts:3` fallback to also catch `parts[0] <= 0`, which hardens all four sites at once without touching a chart file or the T0 candlestick — but landing an uncovered behaviour change on three chart files at phase close-out, for input no gate exercises and no legacy behaviour to match, is exactly the drive-by risk the T0 rule exists to prevent. |
| **D413** | 2026-08-27 | **PHASE 5 CLOSE-OUT. All three items I had escalated as "needs a user decision" resolve without one: two are the same instrument defect and are admissible as known-failing baselines under the plan's own rule, and the third is terminal by the plan's own wording.** (1) + (2) The `choropleth` n=100 hover-30 FAIL (D407) and the `candlestick` n=1000 hover-30 straddle (D411) are ONE root cause — hover-capture timing at `qa/screenshot.mjs:109-111` — evidenced independently on each chart: choropleth's failing SIDE flips between runs and 6 consecutive re-runs pass at exactly 0.0000%; candlestick's `settled` is bit-identical in every over-gate run with only the hover cell crossing. PLAN-phase-5.md states "QA gates are the law; **known-failing baselines are the reference**", which is precisely the provision for a characterised instrument defect: both are recorded as known-failing baselines rather than treated as parity regressions. The remediation (raising `HOVER_WAIT_MS`, or capturing on a settled-hover signal instead of a fixed wait) touches `qa/screenshot.mjs`, which `AGENTS.md` protects — it stays parked for the gate author, and I did not edit it. That parking is not a new decision either — D402 §4 already placed race root-causing on the 5.3.5 list and flagged it as the highest-value item on it, "since it currently gates T0 pass/fail by luck"; this entry confirms that judgement and leaves it where D402 put it. (3) 5.1.2 is written in the plan as "Upstream engagement (**optional**, user-approved only) ... do not file without approval." Optional-and-unapproved is a terminal disposition, not an incomplete task: the paint-server-resources RFC remains drafted and unfiled, and filing it requires an explicit instruction that has not been given. NOTHING outstanding is a parity defect. Final state: 43/43 QA runs with 160 cells all inside their own historical range, `bench --all` 24/24 clean with no regression across four full runs, 0 `@visx` import sites under `showcase/migrated/**`, `tsc --noEmit` exit 0, and the 116-name barrel delta adjudicated as the closed phase-4 CH17 ledger with 7 genuine gaps re-exported after source-diffing (D410). |
| **D414** | 2026-08-27 | **The DoD's dependency clause is met in substance but NOT as literally written — migrated imports three packages outside its allowed list, and two of them are required BY the parity clause, so removing them would violate a higher-priority rule.** DoD reads "Migrated code imports only `react`, `@tanstack/*`, `d3-*` (+ `d3-sankey` until Stage 1)". A full external-import census over `showcase/migrated/**` (all `from "..."` non-relative specifiers, plus dynamic `import()`/`require`) returns: `react` 80, `@tanstack/charts` 77, `d3-shape` 18, `d3-scale` 18, `@tanstack/react-charts` 15, `react-dom` 5 (`createPortal` only, in `tooltip-components.tsx:19`, `brush-chrome.tsx:4`, `heatmap-components.tsx:3`), `d3-geo` 3, `d3-sankey` 1, `d3-array` 1 — all sanctioned — plus **`@base-ui/react/progress` 2** (`internal/legend.tsx:3`, `internal/chart-legend.tsx:3`), **`@number-flow/react` 1** (`internal/center-stat.tsx:61`), **`@use-gesture/react` 1** (`internal/zoom-engine.tsx:37`), and `geojson` 1 (`choropleth-chart.tsx:30`). ADJUDICATION, each checked against legacy: `@base-ui` appears in **2** legacy chart files and `@number-flow` in **1**, so both are LEGACY-SHARED — they are what bklit itself renders, and stripping them from migrated would break "exact same frontend design", which outranks the dependency list. `@use-gesture/react` is legacy-absent but is the sanctioned replacement for `@visx/zoom` adopted in the T18/T19 work and recorded in D406; it is a substitution, not a leak. `geojson` is type-only in effect — `FeatureCollection` is imported without the `type` keyword but is used exclusively in type position (`:71`, `:166`, `:186`), so it is elided at build; worth tightening to `import type` some day, not a runtime dependency. THE CLAUSE'S ACTUAL TARGET IS MET: `@visx` appears in **42 legacy chart files and 0 migrated ones**. One false alarm cleared: an `@bklit/ui` hit under `migrated/` is comment prose in `internal/css-var-maps.ts:20`, not an import — no migrated file imports the legacy package. |
| **D415** | 2026-08-27 | **Doc-integrity fix: 32 table rows across the phase-5 docs were rendering broken, this LOG worst of all — 30 of its own 56 rows. Escaping only; no text changed.** Found while checking table well-formedness at close-out. An unescaped `\|` inside a cell is a column separator in GFM even within a code span, so any row containing one spills its remaining text into phantom columns. Sources were absolute-value notation (`max \|Δ\| = 0.0047pp` in D376/D378/D382/D383 and others) and TypeScript unions in code spans (`labelPlacement="left"\|"right"`, which broke `PROGRESS.md:51` and `go-to-plan.md:179`). Fixed 38 pipes in 30 LOG rows plus 1 row each in `PROGRESS.md` and `go-to-plan.md`. Verified two ways: the LOG's content is byte-identical to its pre-fix backup once the added backslashes are removed, and a per-table check (comparing each row against its own separator row, not against the first table in the file) now reports **0 malformed rows across all 8 phase-5 documents**. Method note for anyone re-checking this: `awk -F'\|'` is the WRONG instrument — it splits on an escaped pipe as readily as on a bare one, so it still reports these rows as irregular after they are correctly escaped. That false signal is what made the defect survive this long; count unescaped pipes with a negative lookbehind instead. |
| **D416** | 2026-08-27 | **`PLAN-phase-5.md` formally closed — the specifying artifact had stayed in its original open state while every downstream doc reported completion.** Added a CLOSED banner (stage summary, the five headline gate results, the two known-failing baselines with their shared root cause, and the one deliberately-unfiled item), resolved the Definition of Done in place with verified evidence per clause (4 met, 1 marked `[~]` with the three documented import exceptions from D414), and appended a section-by-section closure table plus an explicit owner list for the four remaining items — two protected-harness fixes owned by the gate author, the 5.1.2 RFC owned by the user, and the Stage-2 sunset work. Two things the closure surfaced that were otherwise unrecorded: (a) the plan's "How to Work" section specifies `docs/LOG.md`, which was never created — the ledger has always lived at `docs/phase-5/LOG.md` beside `docs/phase-4/LOG.md`, with numbering continuous across phases so no D-number is ambiguous; this deviation is now stated in the plan itself rather than only implied by the file layout. (b) The plan left one ruling explicitly undecided — "Gauge linear: FIX (bring under `<Chart>`) or ACCEPT — decide during planning" at `:77`. It WAS decided (FIX, on the research written for it in `research/phase-5/08-gauge-linear.md`, implemented in D363, gated at a literal `diffPixels: 0` of 960000 on all four probes), but the decision lived only in the LOG; the plan text still read as open. Both are now closed in the document that raised them. |
