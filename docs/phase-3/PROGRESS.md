# Migration Progress Tracker — Phase 3

> **Phase 1** is frozen — see `docs/phase-1/PROGRESS.md` (17/17 charts approved, tag `phase-1-complete-2026-08-07`). **Phase 2** is frozen — see `docs/phase-2/PROGRESS.md` (15 charts re-gated, tag `phase-2-complete-2026-08-08`). This file tracks **Phase 3** only.

Source of truth for status: this table. Update status/QA/benchmark columns as work proceeds; log decisions and rationale in `docs/phase-3/LOG.md`, not here.

## Phase 3 — Harden + Close ALL Utility Gaps (PLAN-phase-3.md)

Goal: harden migrated charts + close every remaining bklit utility gap to TanStack-native backend — remove remaining wrappers/unnecessary complexity/duplicated internals while preserving bklit design, animation, and API parity. Gains measured on M1/M2/M3 vs bklit and closeness to TanStack native (research/phase-1/05-qa-and-benchmark-gates.md — frozen gates).

| # | Initiative | Audit | Plan | Refactor | Q3 boundary | Type parity | QA (Q1/Q2) | Benchmarks (G1–G4) | Notes — single impl + propagate to all consumers (cross-chart gate) |
|---|---|---|---|---|---|---|---|---|---|
| 1 | Internals consolidation — spring / reveal / hover-chrome + ChartScale/y-scales | complete | complete | complete | pass (D207) | pass | pass — Q1 waived pixel-neutral (stash-proven, 5 pre-existing defects → D208), Q2 zero errors ×18 combos | pass — 10/18 fully green; rest pre-existing waived classes + drift artifacts (D207) | **approved (D207)**; ChartScale deferred to initiative 2 (D110); defect register D208 — bar fixed (D209, Q1 0.0000%) |
| 2 | Sizing + contexts host — ResizeObserver/ParentSize + ChartProvider/ChartConfigProvider/static-preview | complete | complete | complete | pass (D210) | pass | pass — Q1 ×7 changed charts ≤0.5%, settled diffs byte-identical to pre-initiative baselines; Q2 zero console errors/pageerrors ×7 | pass — no render-path change, bench re-run waived by ruling (D210); D207/D209 benches stand | **approved (D210)**; D1 ChartConfigProvider on design tokens + options-threaded chrome; D2 no-ChartProvider ruling recorded; D3 sizing sweep verified; D4 plan-premise corrected (no shared helper — canonical D110 stash idiom, dead scatter stashes deleted); vendored bar/composed WAAPI height-warning registered (D208 item 8) |
| 3 | Grid + Background + FadeEdges/IndicatorFade + YAxis + Reveal/Animation/Loading | complete | complete | complete | pass (D214) | pass | pass — settled Q1 ×9 at frozen baselines; loading Q1 line 0.1345/area 0.0490/heatmap 0.1948 ≤0.5% + lead PNG eyeball (D213 defects fixed, residuals deferred to init 12); Q2 zero errors incl. loading states (`qa/console-errors.mjs`) | pass — G1 green (heatmap m1a ~18–19% waived noise-adjacent, re-check at init 4); G3/G4 pass; composed idleTask pre-existing → D208-9 | **approved (D214)**; executed via cmd-executor fallback (opencode still credit-blocked); executor QA batch invalidated by stale `bench/app/dist` — all gates re-run by lead on fresh build; stale-build guard now automated in `qa/screenshot.mjs` + `bench/run.mjs`; background/pattern-preset landed as unwired leaf modules for initiatives 6/11; area gained `loadingLabel` (parity) |
| 4 | ChartTooltip + custom indicator + StatFlow | complete | complete | complete | pass (D215) | pass | pass — Q1 ×9 lead-run at/better than frozen baselines (scatter hovers improved to 0.0000; candlestick pill fixed to exact baseline); Q2 zero errors incl. loading states | pass — G1 green (scatter m1a 16.2% ruled noise-adjacent, re-check init 5; heatmap m1a waiver stands at 81.6%); G2 60fps/0-drop m3b; G3 idleScript pass; G4 heap 35–93% | **approved (D215)**; executor false-baseline incident → run-id-citation countermeasure now standard; 3 surgical fixes (date-pill Date formatting ×4, double-capsule ticker root, candlestick dead `hoveredIndex`, live pill spring → `tooltipSpring`); `indicatorFadeEdges` boolean dropped (type parity); rulings: indicatorWidth/Span/columnWidth = intended D3 adaptation of bklit's separate `TooltipIndicator`; pie center on `CenterStat` approved |
| 5 | Chart hooks + interaction phase — useChartInteraction/useChartPhaseOrchestrator/useScheduledTooltip | complete | complete | complete | pass (D218) | pass | pass — Q1 all charts at frozen baselines (run-id-cited per D215 countermeasure; composed settled band re-frozen 0.27–0.42% as capture-phase artifact — rested raster proven pixel-identical to pre-initiative reference); loading ×3 PASS; Q2 zero errors ×13 scenarios | pass — G1 m1a 12–78% all ≥20% incl. both D215 re-checks (scatter 72.5% flag cleared; heatmap 67.1%, D214 waiver retired); G3/G4 pass; m2c stands per D207(f) | **approved (D218)**; executed via cmd session `09148d4e` (guard's first live save: denied executor `git stash`) + user-authorized Sonnet subagent fallback (D217, expired at 15:16 reset); composed reveal-restart + memo-dep drift root-caused and fixed; `tooltip-chrome.ts.bak` cleanup deferred to init 12 |
| 6 | ReferenceArea + Segment + y-domain registration | complete | complete | complete | pass (D220) | pass | pass — Q1 full sweep at frozen baselines incl. new `refarea` 0.0017/`segment` 0.0000 scenario pairs (run-id-cited); heatmap loading gate pinned to n=52 (harness note); Q2 zero errors ×13; drag-parity probe: geometry exact, mid-drag diff 0.077% post-fix | pass — D210-class waiver (no render-path change); same-day spot-check line/candlestick m1a/m3a/m3c/heap at D218 values, 0 console errors | **approved (D220)**; loop-1 rejected (D219) → fix-loop-2 (cmd, all 9 fixes source-verified; ONE SegmentOverlay/ReferenceAreaLayers/fade helper, underlay via isolation+zIndex) → fix-loop-3 (Sonnet, user-authorized): drag suppresses hover chrome (bklit parity) + pointer-focus ring removed; 2 lead surgical fixes (liveline shared resolver, area style cruft); executor report-accuracy ding #2 (false `require` excuse); demo listing deferred to init 12; standing channel priority: opencode → cmd-executor → Sonnet fallback |
| 7 | ProjectionLine + TerminalMarker | complete | complete | complete | pass (D221) | pass | pass — Q1 full sweep at frozen baselines incl. new `projection` pair 0.0164/≤0.1446 (run-id-cited); composed settled baseline re-frozen 0.2748→0.0845 (premature-settle capture bug fixed); refarea band + terminal-marker PNG eyeballs PASS; Q2 zero errors ×16 scene-loads | pass — D210-class waiver (projection marks null at rest); same-day n=100 spot-check: line m1a 42.4ms M/B 75.0% (D218 71.8%), composed 48.7ms M/B 79.6% (77.5%), m3a/m3c vsync ties | **approved (D221)**; loop-1 executor incident #2 (node-heredoc `git show HEAD:` + `fs.writeFileSync` destroyed 3 hosts; guard correctly DENIED the direct `git checkout` and the sole /tmp attempt — forensics D222) — full lead recovery via checkpoint-blob reconstruction + census-verified graft (area byte-identical to gold); anomaly dispositions: candlestick hover-30 + scatter settled = n=100-baseline-vs-n=1000-run mismatch (D208-10; scatter n=1000 baseline frozen 0.4509), pie = open defect D208-2 (not a waiver); guard-hook hardening LANDED (D222, 29/29 synthetic-payload tests); brush + tail-ticks deferred to init 9 |
| 8 | Legend + ChartLegend + ProfitLoss | complete | complete | complete | pass (D224 loop-1; D226 loop-2 greps clean) | pass | pass — loop-1 (D224): legend 0.0000%x4, profitloss n=1000 <=0.1481%; loop-2 (D226): legendhover settled 0.0291% hovers <=0.0290%, candlelegend <=0.1323%, bar n=1000 hover-70 0.0391%, candlestick n=100 <=0.2345% vs frozen n=100 baseline, profitloss legend probe <=0.0041%; two-writer-race probe PASS post hide() fix; Q2 zero errors x16 targets incl. candlelegend+legendhover | pass — loop-2 full G1-G4 n=100 sweep at parity with frozen medians (migrated <= bklit on every headline metric; candlestick dead-on 36.6/32.1/0.71/4.30MB = legend dim + reveal guard cost zero); NEW candlelegend pair render-path-neutral at rest (D210-class); n=1000 not triggered (no diff beyond noise) | **APPROVED (D226)**: loop-2 Tier B legend-hover dim landed via 2 executor dispatches (Dispatch B faithful zero-fix, found bklit 0.15s itself); TWO lead-fixed defects: reveal replay on marks-group recreation (data-identity guard, bar+candlestick; data-update replay preserved = frozen baselines valid) + hide() lifting active legend dim (syncDim re-apply); NEW legendhover composite pair demonstrates bklit two-index-space quirk; point-markers deferred to init 10 (D225 r3); candlestick n=1000 hover-30 0.50% boundary pre-existing, disclosed |
| 9 | ChartBrush + BrushLayout | complete | complete (D227) | complete | pass (D228 — 0 boundary refs ×5 brush modules) | pass (showcase tsc clean) | pass — brush Q1 lead-eyeballed (2026-08-20T05-50-16-874Z: settled 0.0002%, states ≤0.2516%); self-tests 0.0000% ×2; line/area/profitloss/loading regressions at baselines; Q2 zero errors incl. brush | pass — first-time G1–G4 characterization n=100 (06-04-24-933Z/06-05-59-288Z): migrated ≤ bklit on m1a/settle/heap/idle/m3c; m3d drag valve NOT tripped (4.467 vs 4.608ms, 0.97×, domainChangedEveryRun both); m1c task-time 158 vs 135ms disclosed (script parity) | **approved (D228)**: Dispatch C no-report + 3 lead-fixed C defects (props-vs-elements dispatch, hooks violation, area `!definition` gradient gate) + systemic host-sizing fix (measured container height → `height` prop, bklit-parity, Q1-proven neutral); tick-source + harness largest-svg fixes; tail-ticks (D221 deferral) landed verified; showcase demo → init 12 |
| 10 | Markers + Series chrome (Markers/SeriesMarkers/Highlight/DashTail) | complete | complete (D229) | complete | pass (D230 — 0 non-comment boundary refs ×3 marker modules) | pass (showcase tsc clean) | pass — markers compare 2026-08-20T10-12-14-857Z: settled 0.3514%, hover-50/70 ≤0.4945%, legend-hover ≤0.4951%, fan-open 0.3446% informational (bklit lacks fan hook — D230); hover-30 0.5501% WAIVED (flip-boundary probe: inter-impl snap deltas ≤1.0px non-uniform = knife-edge sub-pixel bisection, not fidelity); self-test ALL PASS ×8 (worst 0.0015%, fan-open 0.0000%); Q2 zero errors ×17 targets incl. line/area loading post-#185 fix | pass — first-time markers G1–G4 n=100 (09-52-47-428Z/09-54-08-758Z): migrated ≤ bklit EVERY median — script 0.61×, task 0.77×, idle script 0.34×, heap 0.90×, mount/settle 0.97–0.98×; existing line/area pairs D210-waived (props default off) | **approved (D230)**: 3 executor dispatches + Sonnet gate harness (lead-reviewed); FOUR post-dispatch defects lead-ruled/fixed — React #185 dash-tail livelock (pending lanes defeat eager bailout; setState removed from empty branch), overlay double-margin (TanStack bakes margins into scale ranges), ChartMarkers xScale margin double-add (line-chart call site), fan-open cross-impl → informational (harness gap); MarkerTooltipContent + showcase demos → init 12 |
| 11 | PatternArea + BarSquares/BarDepth + misc series | complete | complete (D231) | complete | pass (D235 — 0 non-comment boundary refs ×11 init-11 modules) | pass (showcase tsc clean) | pass — patternarea compare `15-42-27-858Z` ALL PASS ×8 presets post phase-shift fix + self-tests ALL PASS; barsquares compare `15-53-46-991Z` + self-test 0.0000% post keyframe fix, regression re-confirm `16-42-58-029Z`; bardepth compare `16-35-54-502Z` ALL PASS (settled 0.0030%) + self-test 0.0000% ×6 post fidelity port; bar off-state 0.0000% ×3 runs; Q2 zero errors ×21 targets, bardepth/barsquares 0 warnings | pass — G2 waived ×3 (custom feature marks); patternarea parity + m1a loading-shell waiver; barsquares sustained-cost sweep (script 0.19×, heap 0.064×, frame 0.68×) with m1a 1.23×/m3a 1.51× absolute-premium waiver; bardepth beats bklit every axis (m1a 0.59×, script 0.30×, heap 0.62×, perMove 0.26×, m3a/m3c vsync ties) | **approved (D235)**: v1–v6 gate campaign; harness query.ts gap + bar settle regression (reveal-deadline ownership fix) + patternarea userSpaceOnUse margin phase-shift (patternTransform bridge) + barsquares unitless-keyframe fix (11126 warnings → 0) + bardepth ×3 defects (Provider-as-wrapper, raw-vs-resolved group scale `r.map` crash, depth-chrome fidelity port by Sonnet, lead-reviewed); latent onRender double-fire guard pattern noted ×4 charts (init 12); showcase demos → init 12 |
| 12 | Showcase build + remaining type-debt + shell polish | complete | complete (D236) | complete | pass (D237 — 0 forbidden imports across migrated/charts; React state confined to 2 sanctioned islands; no /tmp, no console.log) | pass (showcase tsc 0 / lint 0 / build 0) | pass — composed `2026-08-20T17-54-08-625Z`, area `17-54-28-135Z`, bar `17-54-36-669Z` (0.0000–0.0383%) all PASS; markers `17-54-20-585Z` PASS except hover-30 0.5489% = D230-waived class; ring `22-33-33-392Z` settled 0.0675%/hover-50 0.2025% PASS, hover-30/70 ~1.72% WAIVED (D237: bklit hovers data[97] with cursor on data[40]'s band — hit-test artifact; migrated geometrically correct; parity-with-a-bug declined); console-errors 21/21 zero; routes 9×200 | pass — G1–G4 D210-class waiver (D236); liveline same-day twins m1c 1.43× ≈ registered 1.38× with migrated idle 24× better (D208-6 accepted); sankey idleScript 0.027 vs 1.83 (D208-7 FIXED) | **approved (D237)** — PHASE 3 COMPLETE 12/12: 8 demos landed, build/lint/type debt zero, D208 register fully dispositioned (-1/-2/-7/-8 fixed, -3/-4/-5/-10 waived, -6 accepted, -9 deferred), ring D208-3 waiver with 4-probe diagnostic chain, temp diagnostics deleted |

> **Phase 3 loop rule (PLAN-phase-3.md 1.4): each initiative builds ONE standardized utility (tanstack native + bklit 1:1) on a single import path — then propagates it to EVERY migrated chart that consumes it (delete per-chart forks). QA + Benchmarks in `1.4` are cross-chart gates: run `qa/screenshot.mjs` + `bench/run.mjs` across ALL affected charts, not just one. No chart may keep a duplicate tooltip/grid/etc. after its initiative is marked approved.**

## Research tracker (Phase 0)

| Step | Description | Status | Output |
|---|---|---|---|
| 0.0 | Phase 3 docs (PROGRESS / BENCHMARKS / LOG D200+) | done | this file + BENCHMARKS.md + LOG.md |
| 0.1 | All bklit utilities + deferred gaps + duplicated internals catalog | done | research/phase-3/inventory/01-04 + post-freeze D137-D146 delta note + 05-consolidated-internals.md |
| 0.2 | Consolidated internals + every deferred chrome family catalog | done | research/phase-3/inventory/06-deferred-chrome.md + 00-layer-contract.md |
| 0.3 | Per-initiative audits (native vs custom vs broken vs design 1:1) | done | research/phase-3/audits/ (×12 initiatives) |

## Legend

**Phase 3 status values**: `not started` → `auditing` → `planned` → `refactoring` → `QA` → `benchmarking` → `approved` — or `blocked`. Research columns may use `complete` when their deliverable exists; `complete` does not mean the initiative is approved.

**Current blocker**: NONE — **Phase 3 complete: all 12 initiatives approved** (D207/D210/D214/D215/D218/D220/D221/D226/D228/D230/D235/D237). Historical channel note: the opencode executor workspace ran out of credits (`401 CreditsError`, D212 — billing: `https://opencode.ai/workspace/wrk_01KZ1A33YS0SNYM8NFN2VZK1X5/billing`); the authorized channel priority was opencode → cmd-executor → Sonnet fallback (D220). Executor `git` write incidents have now occurred TWICE (D216 initiative 5, D221 initiative 7 — both recovered in full); the hardened guard hook LANDED with user ack (D222): tool-agnostic PreToolUse deny over shell text AND written file content — git write verbs anywhere-anchored, `git show`/`cat-file` rev:path content extraction, shell + API deletions, /tmp — verified 29/29 synthetic payloads incl. the exact D221 incident heredoc.

**Contract review**: the architecture contract requires public TanStack APIs and prohibits rendered-DOM reach-ins. Existing migrated-chart DOM queries and `onRender` styling paths remain to be reviewed or explicitly waived before affected initiatives can pass Q3.

**QA gates** (research/phase-1/05-qa-and-benchmark-gates.md §QA):
- **Q1 — Visual parity**: ≤ 0.5% differing pixels per screenshot.
- **Q2 — API compatibility**: public props/callbacks typecheck with zero runtime console errors.

**Benchmark gates** (research/phase-1/05-qa-and-benchmark-gates.md §Benchmark gates; `B`=bklit, `T`=native TanStack, `M`=migrated):
- **G1 — Improvement**: `M` beats `B` on M1a/M1c/M2a/M3a/M3c, ≥20% on M1a/M3a/M3c.
- **G2 — Closeness**: `(B−M)/(B−T) ≥ 0.6` on M1a/M3a/M3c (waived if no native `T`).
- **G3 — Steady state**: M2a ≤50% of `B`, within 2× of `T`.
- **G4 — Memory/bundle**: M2b/M2c ≤110% of `B`.

Phase 3 gates are identical to Phase 2 (research/phase-1/05 is frozen ground truth). Waivers require lead ruling in `docs/phase-3/LOG.md` (Phase 3: D200+).
