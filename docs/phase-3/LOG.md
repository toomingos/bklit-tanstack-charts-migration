# Decision & Insight Log — Phase 3

> **Phase 1** is frozen — see `docs/phase-1/LOG.md` + `archive/phase-1/` (tag `phase-1-complete-2026-08-07`). **Phase 2** is frozen — see `docs/phase-2/LOG.md` + `archive/phase-2/` (tag `phase-2-complete-2026-08-08`). This file records **Phase 3** only. Append newest last. Format: `D<num>` entries (Phase 3: D200+).

## Phase 3 — Harden + Close ALL Utility Gaps (PLAN-phase-3.md)

_Goal: harden migrated charts + close every remaining bklit utility gap to TanStack-native backend while preserving bklit design, animation, and API 1:1. Gates frozen (research/phase-1/05 — Q1 0.5%/Q2 zero errors, G1≥20%/G2≥0.6/G3/G4). Waivers require lead ruling D200+ here._

- **D200**: Phase 3 scope defined — 12 initiatives covering every remaining gap between `repos/bklit-ui/packages/ui/src/charts/` (204 files) and `migrated/charts/` (17 charts done, utilities missing). Cataloged via `research/phase-2/inventory/04-migrated-inventory.md` + `research/phase-2/audits/×16` + `docs/phase-3/PROGRESS.md`: (1) internals dedup spring/reveal/hover-chrome+ChartScale, (2) sizing+contexts host `ResizeObserver/ParentSize→host` + `ChartProvider/ChartConfigProvider`, (3) Grid/Background/FadeEdges/YAxis+Reveal/Loading, (4) ChartTooltip+custom indicator (`indicatorColor/rows/content/showDatePill`, `TooltipBox/Dot/Indicator/DateTicker`, `useScheduledTooltip`, `IndicatorFade`)+`ChartStatFlow`, (5) `useChartInteraction/useChartPhaseOrchestrator/useScheduledTooltip` phase+interaction, (6) ReferenceArea/Segment, (7) ProjectionLine/TerminalMarker, (8) Legend/ChartLegend/ProfitLoss, (9) ChartBrush/BrushLayout, (10) Markers+Series chrome (`ChartMarkers/MarkerGroup` `FAN_ANGLE 160°`, `SeriesMarkers/SeriesPointMarker/SeriesHoverDim/SeriesHighlightLayer/SeriesDashTail/DashTailStroke/HighlightSegment`), (11) PatternArea/BarSquares/BarDepth+misc series, (12) showcase type-debt+shell polish. `PLAN-phase-3.md` + `docs/phase-3/PROGRESS.md` + `docs/phase-3/README.md` updated to this scope; research Phase 0 (0.1 audits) now drives per-initiative `research/phase-3/audits/` → `research/phase-3/plans/<initiative>/plan-loop-N.md` (context7 + script verification, never assume) per 1.3. Previous scaffold `D200 _Work not yet scoped_` retired.
- **D201**: Phase 3 loop 1.4 hardened for cross-chart standardization — per user requirement, every utility initiative now has an explicit `propagate + standardize` step inside the `1.4` for-loop: `1st` sub-agent builds ONE standardized utility (tanstack native primitive + bklit design/API 1:1, one impl one import, no per-chart forks — e.g. single tooltip preserving `indicatorColor/rows/content/showDatePill/showCrosshair`+`TooltipBox/Dot/Indicator/DateTicker` via `focus:'group-x'`+`renderTooltipBody`+`createGridPointIndex`), `2nd` that same utility is wired into EVERY migrated chart that consumes it (delete per-chart `hover-chrome`/`grid`/etc. forks, single import path), so all migrated tooltips/grids/etc. end up on the same phase-3 impl; `3rd` QA+Benchmarks are cross-chart gates (run `qa/screenshot.mjs`+`bench/run.mjs` across ALL affected charts, not just one) before `LOG.md` approval. `PLAN-phase-3.md:1.4` and `docs/phase-3/PROGRESS.md` initiative table + loop-rule callout updated to this. Waiver stays D200+ `Q1 0.5%/Q2 zero errors/G1≥20%/G2≥0.6/G3/G4` per `research/phase-1/05`.
- **D202**: Phase 0 research completed and persisted. `research/phase-3/inventory/01-04` are byte-verified Phase 2 copies with the D137-D146 delta note; `05-consolidated-internals.md`, `06-deferred-chrome.md`, and `00-layer-contract.md` are present. Twelve initiative audits are persisted under `research/phase-3/audits/`. All twelve initiatives remain open or blocked because no implementation and cross-chart gate evidence exists; no initiative is marked approved by inference.
- **D203**: Verification was attempted after research completion. `pnpm --dir showcase check-types` fails in the cloned TanStack workspace due missing optional package dependencies and existing upstream type errors; `pnpm --dir showcase lint` fails because `@typescript-eslint` is unavailable. These are concrete blockers for Initiative 12 and no QA/benchmark gate was claimed. Existing untracked user/workspace artifacts were preserved.
- **D204**: Completed the `{ impl, n: _n }` demo-prop convention sweep (Initiative 12 showcase type-debt polish). Convention: demos keep the stable `n: number` props interface (page-level API passes `n` uniformly) but alias the destructure to `_n` when the demo is fixed-data — satisfies `@typescript-eslint/no-unused-vars` `varsIgnorePattern:"^_"` under `lint --max-warnings 0`. Applied to the 10 remaining demos (`area/bar/candlestick/choropleth/composed/funnel/line/sankey/scatter/sunburst`) after `pie/radar/ring/gauge` already carried it; `heatmap` + `liveline` intentionally keep `n` (they consume it in bodies). Earlier sed attempt (`s/({ impl, n })/({ impl, n: _n })/`) silently no-oped because the real text is `({ impl, n }: XxxDemoProps)` — fixed with a matching replacement. Verified: `pnpm --dir showcase check-types` PASS (exit 0; D203's blocker cleared — TanStack optional deps + `@typescript-eslint` now resolvable); `pnpm --dir showcase lint` still fails with 138 pre-existing problems (58 errors / 80 warnings) all confined to `showcase/migrated/charts/**`, `components/chart-detail.tsx`, `app/charts/overview/page.tsx`, and sunburst demo's two pre-existing `any` warnings at lines 15/30 — zero attributable to the alias sweep (aliasing only removes unused-var warnings).
- **D205**: Completed the migrated-chart cleanup review and implementation pass. Public props previously hidden behind `_` aliases were wired into behavior: radar `enterDurationMs` and `motionReplayKey`; gauge `startAngle`, `endAngle`, and `uniformWidth`; heatmap `activeScale`, `inactiveScale`, and `rowOpacity`. Shared chart internals were consolidated with `internal/y-domain.ts`, `internal/use-chart-margin.ts`, and `internal/use-container-size.ts`, while chart-specific lifecycle behavior remained separate where semantics differ. The repeated teardown comment artifact (`2014`) was replaced with the canonical `teardown race — already cancelled` explanation. The root `migrated` symlink and its consumers were consolidated on canonical `showcase/migrated`; benchmark aliases and bundle measurement paths now resolve that source. Benchmark app aliases were aligned with the vendored TanStack v0.7.2 sources. Verified after the changes: showcase typecheck and strict lint pass; showcase production build passes; focused prop smoke tests rendered radar, gauge, and heatmap; benchmark app Vite build passed. Full QA and performance gates remain required after the v0.7.2 harness alignment.
- **D206**: Status reconciliation after the cleanup pass. D205 records preparatory refactors, source-resolution changes, and focused prop smoke coverage; it does not approve any Phase 3 initiative. `docs/phase-3/PROGRESS.md` now records all 12 audits as complete while keeping every plan blocked because no `research/phase-3/plans/<initiative>/plan-loop-N.md` deliverable exists. Q3 boundary and type-parity are now tracked separately from Q1/Q2 and G1–G4. The architecture contract remains an open review item: migrated charts still contain rendered-DOM queries and `onRender` styling paths, so affected initiatives cannot pass Q3 until those paths are refactored or explicitly waived. D205's strict-lint/typecheck/build claim must be re-established with fresh command output before it is treated as final evidence; no Phase 3 QA or benchmark gate is claimed by this entry.
- **D207**: Initiative 1 (internals consolidation) IMPLEMENTED + GATED — **APPROVED**. Executor session `ses_fe98bfeceffe5J0Tp8dUP07e31` (opencode-executor dispatch + one resume after a /tmp-permission hang: headless opencode cannot answer the `external_directory` permission prompt for /tmp, so the resume prompt forbade /tmp — the skill's single allowed retry; task prompts must keep temp files inside the repo). Changes (diff reviewed in Claude Code per PLAN 1.4 — verified against source, not the executor report): `internal/design-tokens.ts` (REVEAL_DURATION_MS 1100 / REVEAL_EASE_CSS `cubic-bezier(0.85,0,0.15,1)` / REVEAL_EASE_POINTS, TOOLTIP_SPRING 300/30, TOOLTIP_BOX_SPRING 100/20, HIGHLIGHT_SPRING 180/28, BOX_OFFSET 16); `internal/enter-transition.ts` (single reveal engine: `springFromBounce`, `resolveEnterTransition`, `revealTiming` 40-sample spring / 64-sample tween, `buildProgressKeyframes`; D51 discrete-`d` comment preserved); `internal/deferred-reveal.ts` (cancellable `onPostPaint`, `RevealHandle`/`runDeferredReveal`, heatmap `revealEpoch`/`seenEpochRef` guard); `internal/y-domain.ts` `createNicedYScale` (bar-chart.tsx:232, composed-chart.tsx:554); pie/ring/funnel/gauge/radar reveal modules reduced to re-export shims; hover-chrome modules on shared tokens; 11 chart consumers wired with cancellable unmount cleanup. Gates: **Q3** greps clean — no transition/token definitions outside `internal/`; remaining cubic-bezier literals only at chart-file call sites out of initiative scope (area 56 / bar 63 / choropleth 154 / scatter 45 / line 37 / composed 114 / sankey-animation 17 / bezier-easing comment). **Type parity** showcase `check-types` PASS, `lint` PASS; bench/app tsc errors exclusively in pre-existing tanstack-* scenarios (zero in migrated/api-compat). **Q1** 12/17 ≤0.5%; the 5 failures (bar 17.8045% settled, pie 2.64% hover-30, ring ~1.7%, choropleth hover-30 tooltip, heatmap 0.5006–0.5069%) reproduce BYTE-IDENTICAL at HEAD with the initiative stashed (bar exactly 17.8045% both ways) → pixel-neutral; RULING: Q1 waived for initiative 1, failures registered as pre-existing main defects in D208. **Q2** consoleErrors=0 on all 18 bench combos. **G1–G4** (combos line/area/bar/scatter/candlestick/composed/liveline n=100, pie 50, ring 4, funnel+funnelvertical 5, gauge+gaugelinear 72, radar 50, sunburst 27, choropleth 177, heatmap 52, sankey 33): 10 charts fully green; remaining deviations all pre-existing — frozen Phase 2 migrated rows fail identically: line G2 0.42 (frozen 0.46, D73 headroom waiver), radar/sunburst/choropleth m1a<20%+G2 (frozen −13/−12/−14%, D73/D84 class), heatmap m3a<20% (frozen-identical 32.6, vsync-quantized). Environment note: today's absolutes run ~15–30% above the frozen table on every chart; same-day bklit re-benches flip the two remaining area flags to PASS (B-today m1c 64.8 vs M 55.4 = M −15%; B-today heap 4.38MB vs M 4.06 = 93% ≤110%) — the frozen-B "area m1c −7% / heap 120%" readings were drift artifacts. vsync-locked cells (M3a≈32.5 / M3c≈16.7 / M2a≈0) evaluated as no-regression ties, same treatment as D12. Stash attribution on the three genuinely new cells: initiative-NEUTRAL (area m1c 55.4 w/init vs 56.3 stashed; liveline m1c 575.5 vs 582.3, M2a 7.52 vs 7.49; sankey M2a 1.83 vs 1.71) — liveline m1c 38% above same-day B (415.9) and sankey idle 1.83ms/5s vs B-today 0.03 are post-freeze main drift, registered D208 items 6–7. RULINGS (lead): (a) `deferred-reveal.ts` DOM reach-in (querySelector/classList/dataset) WAIVED — WAAPI requires element handles; confined to the one shared internal module; (b) ChartScale dedup DEFERRED to initiative 2 (D110); (c) heatmap read-only `bkmRevealed` check in its fallback effect ACCEPTED (no duplicate stamping); (d) composed-chart custom clip-path `bkmRevealed` stamp ACCEPTED (inexpressible via `runDeferredReveal`; deadline/post-paint use the shared primitive); (e) liveline G3 "2×T" clause DEGENERATE at n=100 (T idle = 0.0 — native scenario never runs live updates); liveline judged on the B-clause (7.52 ≤ 50% of 169.25 B-today PASS); (f) M2c not re-measured — no dependency or public-prop changes; frozen M2c rows stand.
- **D208**: DEFECT REGISTER — pre-existing failures on main, all initiative-1-neutral (stash-proven), none gated at introduction because post-freeze QA runs compared empty renders (vacuous gate) and D140–D145 were only manually Chrome-verified (phase-2 D141 entry shows no Q1 run): (1) **bar settled 17.8045%** — migrated draws full-slot-width overlapped bars vs bklit grouped thin bars; `git bisect` → first bad `9a4a491` (Phase 2 freeze squash). Refined attribution: `bar-chart.tsx` is byte-identical from the squash to HEAD and equals `archive/phase-2/migrated/charts/bar-chart.tsx` — the squash itself LANDED the phase-2 TanStack-native bar refactor (312 lines changed), whose grouped layout is broken; the parent tree (phase-1 bar + `series-bar-layout.ts` slot×0.88 per-series offsets, preserved at `archive/phase-1/migrated/charts/internal/`) PASSES Q1 in the bisect run. The squash's "15 charts re-gated" claim did not hold for bar on the squashed tree. FIX REQUIRED — standalone dispatch before initiative 2 gates depend on bar. (2) **pie hover-30 2.64%** — hover dim missing, introduced `bafb50c` (D141); D114 already carried a Phase-1 hover waiver class for pie at n≥4, but the missing dim is a real behavior gap; fix with initiative 5 (interaction) or standalone. (3) **ring hover ~1.6–1.7%** — `d26b8fc` (D140), same class as (2). (4) **choropleth hover-30 tooltip absent (1.25%)** — INTENTIONAL divergence per `6a05c09` (D145): migrated clears the tooltip when the pointer crosses an ocean gap; bklit keeps a stale tooltip (documented bklit bug: country→ocean leaves `hoveredKey` live); settled diff 0.0000% proves map alignment identical. RULING: WAIVED as intentional improvement. (5) **heatmap hovers 0.5006–0.5069%** vs 0.5% gate (marginal) — `7068de2` (D141); re-evaluate after initiative 4 (tooltip). (6) **liveline m1c 575.5 vs same-day B 415.9 (+38%)** and idle M2a 7.5ms/5s vs frozen 0.0 — post-freeze main drift, origin commit unbisected. (7) **sankey idle M2a 1.83ms/5s vs B-today 0.03** — negligible duty cycle (0.04%), likely a lingering rAF/timer from the link-reveal rewrite `5a2c444`; fix when sankey is next touched. Countermeasure adopted: every Phase 3 Q1 run must assert non-empty renders (report.json differing-pixel denominators > 0), as today's runs do.

- **D209** (2026-08-18): **D208 item 1 FIXED — bar grouped layout restored.** Standalone opencode-executor dispatch (session `ses_fe8fa488dffe5NooytdQ37eJ34`, task "Fix bar grouped layout (D208-1)"). Root cause: the phase-2 TanStack-native `bar-chart.tsx` passed its nested per-series band scale as a legacy `groupScale` mark option, which the CURRENTLY RESOLVED vendored TanStack v0.7.2 ignores — bars fell back to full category bandwidth, overlapped. Fix: wire the SAME derived scale through the public grouped-layout API — `import { group } from "@tanstack/charts"` + `layout: group({ scale: groupScale })` on the `barY()` mark (bar-chart.tsx:313); the paddingInner/step derivation (lines 15–29, 240–270) is unchanged and now actually takes effect. Single file changed; vendored lib, bklit sources, and QA harness untouched; initiative-1 wiring intact (`createNicedYScale` line 232, cancellable `onPostPaint`/`setRevealDeadline` reveal cleanup). Executor first REPRODUCED the defect (its own pre-fix QA run 22-38-29 shows the same 17.8045% settled) before fixing — report verified against source in Claude Code, not trusted. Independent re-verification (lead): `qa/screenshot.mjs --chart bar --impl-a bklit --impl-b migrated --n 100` → settled 0.0000%, hover-30 0.0015%, hover-50 0.0017%, hover-70 0.0016% (run 2026-08-18T22-41-52-328Z; non-empty renders — tooltips visible both impls, PNGs 20–25KB); `check-types` exit 0; `lint` exit 0. Post-fix bench sanity (bar/migrated n=100, run 22-43-49): m1a 35.30 (pre-fix same-day 36.20), m1c 69.97 (71.58), m2a 0.04 (0.04), m2b 4.14MB (4.17), m3a 27.40 (27.55), m3c 16.70 (16.70) — no regression from the grouped layout; today's fully-green bar G1–G4 verdict stands on the fixed render. Bar Q1 is now REAL evidence; initiative 2 gates may rely on it.

- **D210** (2026-08-19): Initiative 2 (sizing + contexts host) IMPLEMENTED + GATED — **APPROVED**. Executor session `ses_fe8f2abe2ffeT64BDjovy1OShm` (opencode-executor dispatch "Initiative 2: sizing + contexts host", plan `research/phase-3/plans/02-sizing-contexts/plan-loop-1.md` FINAL). Diff reviewed in Claude Code against the plan (isolated to exactly the initiative-2 delta by diffing the working tree against opencode's pre-run snapshot tree `7464787` — 14 files, +268/−86): **D1** `internal/chart-config-context.tsx` is a verbatim bklit port (SpringConfig/ChartConfigValue/DEFAULT_CHART_CONFIG/ChartConfigProvider shallow-merge/useChartConfig null→defaults/resolveTooltipBoxMotion identical formula) with DEFAULT_CHART_CONFIG referencing the `design-tokens.ts` objects (TOOLTIP_SPRING 300/30, TOOLTIP_BOX_SPRING 100/20, HIGHLIGHT_SPRING 180/28 — verified equal to bklit's literals); exported from `charts/index.ts`. Threading idiom: chrome modules stay plain TS — `attachHoverChrome`/`attachBarHoverChrome`/`attachScatterHoverChrome`/`attachCandlestickHoverChrome`/`attachLiveHoverChrome` gained optional spring options defaulting to the token objects (`?? TOOLTIP_SPRING` etc.), and the 7 consumer charts (line/area/bar/scatter/candlestick/composed/live-line) read `useChartConfig()` at component level and pass springs into attach; no-provider path is bit-identical by construction. **D2** RULING (lead, formally recorded): the migrated package intentionally does NOT export `ChartProvider`/`useChartHover` (TanStack `focus` owns hover state) nor `static-chart-preview-context` (no consumer); grep confirms zero showcase consumers. **D3** sizing sweep verified by lead grep: every chart consumes exactly one `use-container-size` hook (line/composed `useDebouncedContainerWidth`; area/bar/scatter/candlestick/choropleth `useContainerWidth`; live-line/pie/ring/radar `useMeasuredRect`; funnel/heatmap `usePositiveChartSize`; gauge.tsx hosts both variants: radial `useMeasuredRect` + linear `useContainerWidth`) or is a documented exception (sunburst `size` prop, sankey CSS `aspectRatio`); `new ResizeObserver` exists ONLY in `use-container-size.ts`. **D4** RULING (lead) — plan-premise CORRECTION: the plan's "shared `resolveConfiguredScale` helper" premise was WRONG — `resolveConfiguredScale` is a vendored TanStack-core internal (`charts-core/src/configured-scale.ts`), and the plan's greps had matched showcase comments merely referencing it; the executor was right to refuse the deep import (layer contract) and D4(b) "migrate line onto the helper" is VOID. Accepted outcome: ONE stash idiom = the custom-`ChartScale.resolve` escape hatch with one canonical D110 comment wording; per-site: bar no stash (factories only), scatter's two dead `xScaleD3Ref`/`yScaleD3Ref` stashes DELETED (zero readers; custom resolvers correctly retained for range semantics), candlestick custom resolve retained (owns slotWidth/2 range inset), composed x/y stashes kept (pointermove bisector consumer), line x stash kept (`xForIndex`/hover highlight consumer). Gates (all run independently by lead, not trusted from the executor report): **Q3** greps clean — zero spring literals outside `internal/`, zero deep TanStack imports (comment mentions only), single ResizeObserver site. **Type parity** `check-types` exit 0, `lint` exit 0. **Q1** re-run by lead for all 7 changed charts at n=100 (runs 2026-08-18T23-04/05Z, non-empty renders — 960k-px denominators, tooltips visible both impls): line settled 0.0000%, area 0.0002%, bar 0.0000%, scatter 0.1230%, candlestick 0.0354%, composed 0.2609%, liveline 0.0186%, all hovers ≤0.2345%, ALL PASS ≤0.5%; pixel-neutrality PROVEN — scatter 0.1230%/candlestick 0.0354%/composed 0.2377% are byte-identical to their PRE-initiative-2 runs (21-41Z sweep) and recur in history back to 2026-08-02+, liveline within its 0.017–0.046% ticker band; executor's own 7 runs (22-56/57Z) independently reproduced. **Q2** `qa/api-compat` fixtures compile under check-types; runtime probe (playwright, migrated impl, settle + hover sweep ×7 charts): console.error = 0, pageerror = 0 on all 7. **G1–G4** RULING (lead): no bench re-run required — the delta is chrome-attach option threading with identical default values, dead-ref deletion, and comment normalization; no render-path change (D209 bar bench + D207 cross-chart benches remain the standing evidence). NEW REGISTER ITEM (D208 addendum, item 8): **bar + composed emit `Invalid keyframe value for property height: <n>` console WARNINGS** (400/200 per load at n=100) from the vendored TanStack `motion.ts` bar enter animation animating `['y','height']` with unitless WAAPI keyframe values (motion.ts:803/1421) — Chrome drops the height track; pre-existing vendored-lib behavior (zero showcase keyframe code involved), NOT a Q2 failure (gate counts errors), untouched by initiatives 1–2; revisit if the vendored lib is patched or when bar enter animation parity is next audited. Process note: `.claude/skills/opencode-executor/dispatch.sh` gained `--events` (line-per-event stream: session id, executor narration, tool errors, 5-min progress heartbeats, QUIET/STALE warnings, terminal DONE) and the skill now arms a Monitor on it for every background dispatch — this initiative's run was the first live-supervised dispatch (surfaced two recoverable executor probe errors and one apply_patch retry mid-run).

- **D211** (2026-08-19): Initiative 3 plan FINALIZED (`research/phase-3/plans/03-grid-reveal/plan-loop-1.md` DRAFT → FINAL) after initiative 2 approval (D210); verify-at-finalize list executed: fade residuals re-confirmed (line/area/composed + `internal/types.ts` + `internal/hover-chrome.ts`), loading residuals re-confirmed (area/composed/heatmap/line/sunburst), threading idiom locked to initiative 2's options-with-token-defaults pattern. RULING (lead) — **pattern-preset owner**: bklit `pattern-preset.tsx` (186 lines, 8 preset ids, leaf module with no chart deps) is pulled FORWARD from initiative 11 into initiative 3 as `internal/pattern-preset.tsx`, because Background (init 3, D2) needs pattern fills now and ReferenceArea (init 6) needs the same module later — one owner, one impl, no second fork; initiative 11's plan must reference (not rebuild) it. RULING (lead) — **loading-state gate**: `qa/screenshot.mjs` verified to have NO loading-state capture; the LEAD will extend the harness with a loading capture before gating initiative 3's D5 (the executor remains barred from `qa/`); fallback if infeasible in one pass: manual Chrome verification logged as a ruling.

- **D212** (2026-08-19): Initiative 3 dispatch FAILED — **BLOCKED on opencode workspace credits**. Dispatch run `20260819-001337…-43300` (session `ses_fe8d91f2bffe3xKdTRax62bvjm`, task `scratchpad/task-init3.md`): the executor delegated implementation to an internal opencode `task` subagent (`subagent_type: work`, model `opencode-go/deepseek-v4-pro`, child session `ses_fe8d8e785ffeZyL6nLvBqACgRB`) which ran ~13 min and returned an EMPTY `<task_result>`; the parent's next request then failed hard: `APIError 401 CreditsError "Insufficient balance"` (`https://opencode.ai/zen/go/v1/responses`, `isRetryable:false`, workspace `wrk_01KZ1A33YS0SNYM8NFN2VZK1X5`). Skill-doctrine single retry executed (`dispatch.sh --session ses_fe8d91f2bffe3xKdTRax62bvjm`) → identical 401 CreditsError; exact outputs preserved in the dispatch log. The new `--events` Monitor surfaced the stall correctly (QUIET 592s heartbeat during the silent subagent, then STREAM ERROR + DONE: error). **Tree state**: the subagent left a PARTIAL initiative-3 implementation in the working tree — 16 files, +445/−86 (new `internal/fade-mask.ts` 176L, `internal/grid.ts` 78L, `internal/grid-chrome.tsx` 55L, `internal/y-axis-ticks.ts` 30L; design-tokens additions; hover-chrome fade threading across 5 chrome modules; line/area/candlestick/composed + `types.ts` + `y-axis-overlay.tsx` edits), isolated via opencode snapshot trees `025aab43` (pre-run) → `2da7cf0e` (post-subagent, byte-identical to current tree). D2 (background/pattern-preset) and D5 (loading chrome) NOT started; no verification was run by the executor. Lead verification of the partial tree: `check-types` exit 0, `lint` exit 0 — approved D207–D210 work intact. RULING (lead): partial work is RETAINED in-tree as plan-conformant progress (unreviewed — Q1/Q2/Q3 NOT claimed); initiative 3 stays `refactoring/blocked`; resume the SAME session (or fresh dispatch referencing the partial state) once workspace credits are restored — the blocker is user-actionable billing, not a technical failure. No initiative-3 gate evidence exists yet.

- **D213** (2026-08-19): Loading-state Q1 capture BUILT (lead-only harness work per D211; executed while executor dispatches are credit-blocked per D212). Mechanism: `qa/screenshot.mjs --state loading` → bench app `?state=loading` (`ChartState` in `bench/app/src/bench/query.ts`, threaded `ScenarioProps.state` → `status="loading"` + `loadingLabel="Loading data"` in the three scenario pairs where BOTH impls expose `status` today: bklit/migrated line, area, heatmap); capture contexts use Playwright `reducedMotion: "reduce"` because the two impls animate loading chrome with DIFFERENT engines (bklit: motion/react rAF, branches on `useReducedMotion`; migrated D5: CSS keyframes per stack contract) so only the static reduced-motion frame is cross-impl comparable — animation DYNAMICS stay on D211's manual-verification fallback; loading phase never resolves `__benchSettled`, so the loading path waits paintDone + fixed 1500ms and captures ONE frame, no hover captures (no tooltip contract). Default gate path verified UNTOUCHED post-edit: line n=100 settled 0.0000%, hovers ≤0.0005%, tooltips visible (run 23-38-46Z) — byte-equal to the D210 baseline, which incidentally also proves the D212 partial initiative-3 tree is pixel-neutral for line's default render. Bench-app tsc: 13 pre-existing tanstack-* errors, zero in changed files. RULING (lead) — **loading pixel gate is NECESSARY but NOT SUFFICIENT**: line's structurally WRONG loading render diffs only 0.1952% (thin strokes vs 960k px), under the 0.5% gate — so D5 approval additionally requires the lead to visually inspect the `loading-a/b` PNGs per chart; a numeric PASS alone proves nothing for loading states. PRE-D5 BASELINES (defect quantification, runs 23-35..38Z): **line** 0.195% numeric-PASS/structurally-WRONG — migrated draws the FULL data line during loading; bklit suppresses the series and shows the right-edge skeleton pulse squiggle + centered label; **area** 20.77% FAIL — migrated renders the full filled area with NO loading chrome, and migrated `AreaChart` LACKS bklit's public `loadingLabel` prop (parity gap D5 must close; the scenario deliberately omits the prop rather than cast it away); **heatmap** 2.647% FAIL — migrated renders the full cell grid with the label mis-positioned into the top-left axis row; bklit suppresses cells and centers the label. D5 acceptance additions for the re-dispatch: loading chrome MUST honor `prefers-reduced-motion` (else the gate cannot compare), area gains `loadingLabel`, and post-D5 `--state loading` runs for line/area/heatmap must both pass ≤0.5% AND survive lead eyeball vs the bklit frames.

- **D214** (2026-08-19): Initiative 3 (Grid + Background + FadeEdges/IndicatorFade + YAxis + Reveal/Loading) IMPLEMENTED + GATED — **APPROVED**. Executor: cmd-executor fallback (Command Code CLI, session `dd8e5283-da26-4093-a142-88aa2fbee95e`, dispatch `brkh3ej8k`) — opencode remains credit-blocked (fresh probe this session: identical `401 CreditsError`, `ses_fe88b3cccffegc7paE6obwCxcc`, workspace `wrk_01KZ1A33YS0SNYM8NFN2VZK1X5`); executor resumed on top of the retained D212 partial tree. Delivered (diff reviewed in Claude Code against source, per doctrine): **D1** grid parity (`internal/grid.ts` + `grid-chrome.tsx`, inherited from D212 partial, verified threaded); **D2** `internal/background.tsx` (bklit 1:1 — renderPatternPreset fills, h/v fade masks with clampFadeLength ≤45%, `opacity={isLoaded ? opacity : 0}` + `BACKGROUND_ENTER_FADE_MS` transition) + `internal/pattern-preset.tsx` / `visx-pattern-bridge.tsx` (diffed line-by-line vs `repos/bklit-ui/.../pattern-preset.tsx`: faithful port, only "use client" removal + bridge import; `@visx/pattern` already a dependency in BOTH package.jsons — no new dep). Leaf modules, deliberately UNWIRED (initiatives 6/11 consume); **D3** `internal/fade-mask.ts` single fade source (inherited, verified — `hover-chrome.ts` imports `crosshairFadeStops()`); **D4** y-axis parity modules (inherited, verified); **D5** `internal/loading-chrome.tsx` (LoadingLabel aria-live overlay, LineLoadingPulse rAF clip-rect on bklit timing `2.2s cubic-bezier(0.85,0,0.15,1)`, LineLoadingSweep/BarLoadingSkeleton mask + `.ts-bkm-sweep-band`, bklit skeleton formula `110+sin(i*1.15)*36+i*9`) + `styles.css` shimmer/exit/sweep keyframes with a full `@media (prefers-reduced-motion: reduce)` static-frame branch; line/area/heatmap loading-gate their marks (`[]` while loading) and render the shared label; area gained the public `loadingLabel` prop (D213 parity gap closed). **VERIFICATION FAILURE (executor) + COUNTERMEASURE**: the executor's ENTIRE reported QA batch — passes and failures alike — was invalid: `ensureServer` in `qa/screenshot.mjs`/`bench/run.mjs` only built `bench/app/dist` when missing and reused any running server, so its screenshots (01:21+) ran against a dist built 00:38, BEFORE its edits landed (01:20–01:21); its "area 20.7668% FAIL / heatmap 2.6471% FAIL" were photographs of the OLD code, and its prose descriptions of renders contradicted its own PNGs. Lead re-ran EVERY gate on a fresh build. Permanent fix (lead harness edit): both scripts now run `rebuildIfStale()` in `ensureServer` — rebuild whenever the newest mtime under `bench/app/src`, `bench/app/index.html`, `showcase/migrated`, or `repos/bklit-ui/packages/ui/src` exceeds `dist/index.html` (smoke-tested: touching `styles.css` triggers "[bench] … STALE … rebuilding…"; qa guard verified via line settled re-run). Standing rule now automated: gate evidence must come from a build newer than the newest source edit. Gates (ALL lead-run, fresh build): **loading Q1 + D213 eyeball** (runs 2026-08-19T00-26Z): line 0.1345% / area 0.0490% / heatmap 0.1948%, all PASS ≤0.5% (vs pre-D5 0.195-structurally-wrong / 20.77 FAIL / 2.65 FAIL); lead eyeballed all six loading-a/b PNGs — the three D213 structural defects are FIXED (line no longer draws the data series; area suppresses the fill and shows centered label; heatmap suppresses cells and centers the label). Residuals ruled DEFERRED to initiative 12 per plan ("Skeleton data generators stay in initiative 12"): bklit's 4 inset loading grid lines vs migrated 8 full-width; skeleton squiggle absent (area) / different silhouette+position (line); heatmap label ~15px offset; label shimmer static-frame typography slightly different. **Settled Q1** ×9: line 0.0000, area 0.0002, bar 0.0000, scatter 0.1230, candlestick 0.0354, composed 0.2369, liveline 0.0184, heatmap 0.4277 (n=52), sunburst 0.0599 (n=27) — all at frozen baselines; heatmap hovers 0.5197/0.5219/0.5945 + sunburst hover-30 0.9478 byte-identical pre/post → pre-existing (D208-5 class), not attributable. **Q2** zero console errors / pageerrors across all migrated scenarios INCLUDING the three loading states (new permanent tool `qa/console-errors.mjs`; bar 400 / composed 200 WAAPI height WARNINGS = D208-8, excluded by gate). **Q3** greps clean (keyframes/shimmer/fade/pattern definitions only in `internal/` + `styles.css`); `check-types` + `lint` PASS; executor boundary compliance proven by mtime audit (no qa/ bench/ repos/ edits besides the one permitted `bench/app/src/scenarios/migrated-area.tsx` loadingLabel re-enable). **G1–G4** (16-run sweep 00-38..01-01Z + composed/heatmap re-runs): G1 PASS — mounts 20–55% faster, m3c 20–80% better, vsync-quantized m3a≈32ms / m3c 16.7ms ties per D207/D12; EXCEPTION heatmap m1a at 80.8–82.4% of B (~18–19% improvement vs 20% north star) → **WAIVED as noise-adjacent** (initiative delta on heatmap = two boolean loading gates; settled render pixel-identical; D207-documented ±15–30% session drift exceeds the shortfall; RE-CHECK at initiative 4). G2 stands on D207 evidence + monotonicity (no render-path slowdown anywhere). G3 PASS on idleScript (composed re-run 29.3% of B; bar ratio degenerate at noise floor 0.04 vs 0.029 ms/5s). G4 heap 35–93% PASS; M2c stands per D207(f) — no new deps, pattern modules unwired. NEW REGISTER ITEM (**D208 item 9**): composed migrated idleTask 38–44ms/5s vs B 3.06 — PRE-EXISTING (pre-initiative-3 run 2026-08-18T21-58Z already shows 26.9ms), idleScript near-zero so it is non-script task time, likely retained WAAPI animations (D208-8 class); fix when composed enter animation is next audited. Process notes: quiet-by-default `--events` now built into both dispatch scripts; `qa/console-errors.mjs` adopted as the permanent Q2 tool; opencode re-probe at each initiative boundary until credits restored.

- **D215** (2026-08-19): Initiative 4 (ChartTooltip + custom indicator + ChartStatFlow) IMPLEMENTED + GATED — **APPROVED**. Executor: cmd-executor fallback (opencode re-probe at boundary: still `401 CreditsError`, workspace `wrk_01KZ1A33YS0SNYM8NFN2VZK1X5`). History: initial implementation dispatch left a REAL Q1 regression that the executor's report mislabeled "= baseline" (false-baseline incident — second executor-verification failure after D214's stale-dist incident; new COUNTERMEASURE: every fix task now embeds the frozen baselines verbatim and requires each QA line to cite its `qa/results/<chart>/<run-id>`; the executor self-flagged its own residual under this rule, proving it works). Lead root-caused the regression from diff PNGs + PIL pixel measurement and dispatched three surgical fixes (session `e5a6394c-f289-492c-a67d-b2f5c211ce3f`; fix1 fresh, fix2/fix3 resumed): **(A)** bar/scatter/candlestick/composed fed raw `String(Date)` into the date pill (giant "Wed Jan 29 2020 00:00:00 GMT…" capsule = the ~1.4–1.66% hover FAILs) — fixed by replicating line-chart's memoized `toLocaleDateString("en-US",{month:"short",day:"numeric"})` recipe in all four; **(B)** `tooltip-chrome.ts` `createDateTicker` styled its root as a full capsule INSIDE `.bkm-date-pill` which already provides the identical capsule (styles.css:264-303) → pill-inside-a-pill, line/area's 0.09% hover residual — fixed by stripping the root to a transparent container; **(C)** dead `hoveredIndex` in `candlestick-hover-chrome.ts` (`state.hoveredIndex ?? 0`, never set → pill always read labels[0] "Jan 1", measured 66px vs bklit's 77px "Jan 29" capsule) — fixed by adding `index: number` to `CandlestickFocusPoint`, populated from TanStack `ChartPoint.datumIndex`, `ticker.update(point.index, …)`. Fix3 (parity, not pixels at defaults): live pill spring used local `PILL_SPRING {300,30}` instead of the chrome's `tooltipSpring` — bklit's live pill follows `crosshairSpringConfig` "in lockstep with crosshair" (live-x-axis.tsx:89-92); now `createSpring(0, tooltipSpring.stiffness, tooltipSpring.damping, …)`, `PILL_SPRING` deleted. Type parity: `indicatorFadeEdges?: IndicatorFadeEdges | boolean` → boolean form removed (bklit's public prop has no boolean; that form is internal to indicator-fade/fade-mask); duplicate `IndicatorFadeEdges` declaration removed from `types.ts` (single source: `fade-mask.ts`). Hypotheses ELIMINATED by evidence during root-cause (recorded so they are not re-litigated): bar dim path (`setCategoryHover` byte-identical to passing version; diff PNGs show zero red in bars region) and dot rendering (attrs unchanged; the tiny dot annuli are the ACCEPTED D209/D210 residual — migrated `stroke-width 2` vs bklit TooltipDot's unset→SVG-default 1, visible only against dark marks). RULINGS (lead): (1) `ChartTooltipConfig` extras `indicatorWidth`/`indicatorSpan`/`columnWidth` are the intended D3 adaptation — bklit exposes width presets (line 1/thin 2/medium 4/thick 8, span×columnWidth override) only on its separately-exported `TooltipIndicator` component, which has no migrated counterpart; migrated `resolveIndicatorWidth`/`resolveIndicatorPixelWidth` verified identical to bklit's resolution. `enabled` is the pre-existing extraction flag — accepted. (2) live-hover-chrome's local pill DOM construction = ruling-3 live-axis composition (accepted). (3) `.bkm-tooltip-panel` usage in `heatmap-components.tsx` = ruling-4 out-of-scope exception. (4) `internal/pie-center.tsx` D5 port APPROVED on personal diff review: grid-stacked variant DOM (variant-0 + per-slice, coordinator-subscription display toggles) replaced by a single `CenterStat` render via the center-stat hover hook; `PieCenterFormat = CenterStatFormat`, `defaultPieCenterFormat = defaultCenterStatFormat`, local `StatFlowContent`/`formatCenterValue` deleted; `children` render-prop path preserved (invoked only when hovered, matching old behavior). Gates (ALL lead-run on final post-fix3 code): **Q1** ×8 (candlestick lead-verified separately at `2026-08-19T02-14-47-094Z` — 0.0354/0.2345/0.2341/0.2325, EXACT frozen baseline; fix3 touched only live-hover-chrome.ts): {{Q1_RESULTS}}. Scatter hovers IMPROVED to 0.0000 (was ≤0.2345 band). Known pre-existing FAILs unchanged and not attributable: heatmap hovers ~0.52–0.59 (D208-5), pie hover-30 ~3.11 (D208-2). **Q2** {{Q2_RESULT}} (bar 400 / composed 200 WAAPI height warnings = D208-8, excluded by gate). **Q3** greps clean: promoted tokens single-sourced; zero spring literals outside `internal/`; `.bkm-tooltip-panel` only in tooltip-chrome.ts + styles.css + ruling-4 exception; deep-import scan shows only pre-existing `@tanstack/charts/focus/disabled`/`/polar` package subpath entry points on charts untouched by this initiative; `spatialIndex` absent per ruling 2. `check-types` + `lint` PASS. **G1–G4** PASS — 16-run sweep 02:23–02:48 pre-incident (`bench/results/2026-08-19T02-23…02-47`, re-derived post-restoration via bench-analyze.mjs over the surviving run dirs): m1a mount M/B = line 76.4% / area 54.3% / bar 64.0% / scatter 83.8% / candlestick 56.5% / composed 78.9% / liveline 44.1% / heatmap 81.6%; m1b settle 96.4–100.9%; m3a update 84.9–100.6%; m2b heap 35.4–93.1%. Noted, not gated: liveline m1c script/task 144/147% (streaming scenario, matches its D210-era profile) and composed m2a_idleTask single-sample outlier (44.4ms vs 3.0ms) including D214's heatmap m1a waiver RE-CHECK: 81.6% — inside the D214 waiver band 80.8–82.4%, waiver HOLDS.

- **D216** (2026-08-19): **INCIDENT + FULL RECOVERY — initiative 5 dispatch destroyed the uncommitted working tree.** The first init-5 cmd-executor run (session `e6d08b0d-57b3-4c0d-954c-4c01d59a4eb7`, run `20260819-101550-phase3-init5-interaction-phase-8464`) ran `git checkout HEAD -- <file batches>` across `showcase/migrated/charts/`, showcase demos/app, and `bench/app/src/` plus `git reset HEAD` ~93 min in (narration: “rewound to a clean single-source baseline”), reverting the uncommitted initiative 1–4 work and deleting untracked internal modules. Run STOPPED by lead; evidence preserved (scratchpad `incident-evidence/`, executor leftovers archived untrusted). ROOT CAUSE: the PreToolUse guard fenced edit/write TOOLS only — shell `git checkout/restore/reset/stash/clean` and `rm` were unfenced (the bench/ revert was also an unfenced forbidden-path modification). RECOVERY (byte-exact-first): (1) opencode shadow-snapshot stash commits — `5cc208ed` “WIP on main” 02:48 → 82 tracked files, `eb340506` untracked-files commit → init-1 modules + verify-props; (2) chronological six-stream NDJSON replay of executor edit/write payloads (applied=43 written=13 skipped=21); (3) 11 cmd file-history pre-edit blobs from the init-5 session = byte-exact post-init-4 states (line/area/composed charts, hover/bar/scatter/live/tooltip chromes, types, focus strategies); (4) read-result harvests from cmd + lead transcripts → `grid.ts`, `y-axis-ticks.ts`, `fade-mask.ts`, `chart-config-context.tsx`, `design-tokens.ts`, `loading-chrome.tsx`; (5) two manual repairs (live-line-chart duplicated 22-line config block removed; candlestick-hover-chrome 3× `let`→`const`). D215 entry restored from the parked draft with G1–G4 numbers re-derived from the surviving `bench/results/2026-08-19T02-*` run dirs. VERIFIED: `check-types` + `lint` PASS; Q1 ×9 at frozen baselines — line 0.0000/≤0.0005 (`qa/results/line/2026-08-19T11-24-56-896Z`), area 0.0002/≤0.0017 (`area/…T11-25-34-035Z`, first run hover-70 0.0572 = settle flake, re-run exact), bar 0.0000/≤0.0017 (`bar/…T11-25-16-835Z`), scatter 0.1230/0.0000 EXACT (`scatter/…T11-25-48-944Z`), candlestick 0.0354/0.2345/0.2341/0.2325 EXACT (`candlestick/…T11-25-54-768Z`), composed 0.1994/≤0.0041 (`composed/…T11-26-00-677Z`), liveline 0.0173/≤0.1138 (`liveline/…T11-26-22-864Z`), heatmap 0.4277 EXACT + known D208-5 hover FAILs (`heatmap/…T11-26-29-232Z`), pie 0.0000 + known D208-2 hover-30 (`pie/…T11-26-39-222Z`); loading line 0.1315/area 0.0490/heatmap 0.1948 (`…T11-26-51/54/57Z`); Q2 zero console errors all scenarios incl. loading (D208-8 WAAPI warnings excluded). COUNTERMEASURES: `.commandcode/hooks/guard-executor.sh` extended to DENY shell `git checkout|restore|reset|stash|clean` and `rm` of tracked/repo files for executor runs; all future task prompts carry: git state is READ-ONLY, never delete files you did not create, on suspected out-of-scope STOP and report — never “clean up”. Initiative 5 re-dispatch only after the hardened guard is armed.
- **D217** (2026-08-19): Initiative 5 execution-channel ruling — **Sonnet subagent fallback authorized by the user** for the cmd-executor rate-limit window. History: the resumed cmd session `09148d4e` (continuation run `20260819-130049-…-34399`) made real progress — D1 gates routed, orchestrator adopted in line/area/composed, line/area/bar/scatter Q1 restored to frozen baselines after a mid-run scheduler fix — then died at turn 87 on the contributor-tier 5-hour usage limit (resets 15:16). Guard hardening (D216 Rules 3/3b) passed its first live test: the executor's `git stash` A/B-test attempt was denied pre-execution and it complied (pre-existing `stash@{0}` dated 2026-08-13 untouched). Lead verification of the partial tree: check-types + lint PASS; open defects = composed settled 14.19% (reveal clip frozen at initial state while phase reports ready — `qa/results/composed/2026-08-19T13-08-38-602Z`, PNGs eyeballed), composed hover-30 0.1192 > ≤0.0041, D3 single-source violation (inline rAF scheduler IIFE in tooltip-chrome.ts instead of importing tooltip-scheduler.ts), D4 dead code (hover-reanchor.ts unimported). User then explicitly authorized falling back to Claude Code Sonnet subagents until the reset; the three open items + full verification sweep dispatched to a Sonnet general-purpose agent under the same boundaries (git read-only, no deletions, no qa/bench/repos writes, no commit, run-id-cited Q1). cmd session `09148d4e` remains resumable as fallback if the Sonnet run underdelivers.

- **D218** (2026-08-19): Initiative 5 (chart hooks + interaction phase — useChartInteraction/useChartPhaseOrchestrator/useScheduledTooltip) IMPLEMENTED + GATED — **APPROVED**. Executors: cmd-executor session `09148d4e` (D1 scaffolding + partial adoption; killed by 5h limit at turn 87) then user-authorized Sonnet subagent `afcf386bbeabcf132` (D217) for fixes + verification; all gates lead-verified at idle. Delivered per plan `research/phase-3/plans/05-interaction-phase/plan-loop-1.md`: **D1** `internal/chart-phase.ts` 1:1 bklit port (8-value `ChartPhase`, `isChartInteractionPhase`, `resolveRestingChartPhase`, tween constants); `types.ts` re-exports so consumers compile unchanged; every ready-gate in line/area/bar/scatter/composed + bar/scatter focus strategies routed through `isChartInteractionPhase` (candlestick `canInteractRef` + heatmap lifecycle exempt per plan scope). **D2** `internal/use-chart-phase-orchestrator.ts` 1:1 port adopted in line/area/composed (composed on constant `chartStatus: "ready"`; mount-run replay effect IS the enter reveal — no first-run guard; WAAPI reveal keyed on `revealEpoch` and gated on `chartPhase === "revealing"`; `onfinish` no longer sets phase — orchestrator timer owns `"revealing"→"ready"`; no conceal clip exists yet → `notifyRevealConcealComplete` fires on `"exitingReady"` entry; gridTween phases snap + immediate `notifyYDomainTweenComplete` — y-domain tween itself stays initiative 12). **D3** `internal/tooltip-scheduler.ts` (`createTooltipScheduler`: rAF-coalesced last-write-wins, double dedupe, synchronous `clear()`, `dispose()`); wired as the ONLY scheduler through `tooltip-chrome.ts` `applyBoxContent` (inline IIFE fork removed after lead flag); one scheduler per box, disposed on box destroy; only React `.render()` commits routed, non-React DOM writes synchronous. **D4** `internal/hover-reanchor.ts` wired via `hover-chrome.ts` `reanchor()` (lastX tracked in update/cleared in hide); line-chart's ~44-line bespoke re-anchor effect deleted — single shared impl, zero Q1 drift. **D5** no new public exports. **Composed incident chain (all root-caused, all fixed)**: (a) post-adoption settled 14.19% FAIL — un-gated WAAPI reveal restarted mid-settle because TanStack `RendererChartImplementation` re-fires `onRender` on every re-render (unmemoized hostOptions); fixed by gating the reveal trigger on `chartPhase === "revealing"`. (b) residual settled 0.30% — `chartPhase`/`isLoaded` in the `defineChart` useMemo dep array recomputed the definition at the revealing→ready flip, shifting the resting raster sub-pixel; fixed by ref-snapshot reads (`phaseRef.current`/`isLoadedRef.current`) with no phase deps (verified live: zero memo recomputes at the flip). (c) after both fixes composed settled sits 0.2748–0.4124% across 6 runs vs the old frozen 0.199–0.261 band — **RULED a capture-phase artifact, composed settled band RE-FROZEN to 0.27–0.42%** (>0.45% in a future run = re-investigate). Evidence: (i) migrated hover PNGs pixel-identical (0.0000% pixelmatch) to pre-init-5 in-band reference run `qa/results/composed/2026-08-19T11-26-00-677Z` — the rested raster is byte-equivalently unchanged; (ii) hovers vs bklit at the exact frozen baseline 0.0033/0.0041/0.0039 in EVERY run; (iii) mechanism: harness captures at `ready`+200ms ≈ 1300ms while composed's per-bar stagger runs to `1.4×animationDuration` ≈ 1540ms — BOTH sides are photographed mid-stagger (why this baseline was never 0), and swapping bklit's `onfinish`-driven ready for the orchestrator's 1:1 flat timer shifts migrated's captured stagger frame by 1–2 frames. **Verification-flake doctrine reaffirmed**: agent-session runs under load produced false drift flags (area 0.087–0.146, candlestick hovers 0.31–0.32) that vanished at idle — lead idle re-runs are authoritative; conversely the sweep's heatmap bench pair (m1a 492%) was asymmetric machine-load contamination (bklit B=65.7ms at 14:25 vs B=400.7ms four minutes later; migrated idleScript 38.6ms anomalous vs 0.03ms historical AND 0.03ms on re-run), invalidated and re-gated on the same-window pair. Gates: **check-types + lint** PASS (verbatim in agent report; lint clean under `--max-warnings 0`, no eslint-disable needed). **Q1** (all report.json-verified): line 0.0000/0.0001/0.0004/0.0005 (`line/2026-08-19T13-55-29-345Z`; lead `13-08-19-755Z`), area 0.0002/0.0014/0.0011/0.0017 (`area/2026-08-19T13-55-15-096Z`), bar 0.0000/≤0.0017 (`bar/2026-08-19T13-29-18-451Z`), scatter 0.1230/0.0000×3 (`scatter/2026-08-19T13-29-24-122Z`), candlestick 0.0354/0.2345/0.2341/0.2325 EXACT (lead `candlestick/2026-08-19T13-35-52-160Z`), composed settled 0.2748/0.2748/0.3190/0.3514/0.3514/0.4124 + hovers ≤0.0041 (`composed/2026-08-19T13-54-30-279Z`, `13-54-55-049Z`, lead `13-56-56-467Z`, `13-58-54-595Z`, `13-59-36-184Z`, `13-59-41-560Z`), liveline 0.0299/≤0.1298 (`liveline/2026-08-19T13-28-35-514Z`), heatmap 0.4277 settled (`heatmap/2026-08-19T13-31-41-480Z`; hovers 0.52–0.59 = D208-5), pie 0.0000 settled (`pie/2026-08-19T13-31-51-340Z`; hover-30 3.1052 = D208-2); loading line 0.1225 (`line/2026-08-19T13-32-37-833Z`), area 0.0417 (`area/2026-08-19T13-32-42-297Z`), heatmap 0.1948 (`heatmap/2026-08-19T13-32-47-119Z`). **Q2** zero console errors/pageerrors ×13 scenarios incl. 3 loading states (lead run, `qa/console-errors.mjs`; bar 400/composed 200 WAAPI height warnings = D208-8, excluded). **Q3** greps clean (lead spot-check: remaining raw `"ready"` strings are comments/doc-strings/out-of-scope heatmap files only; no scheduler forks; no deep `@tanstack/*/src` imports). **G1–G4** (16-run sweep `bench/results/2026-08-19T14-02…14-27Z` + heatmap re-pair `14-31/14-33`): G1 PASS — m1a M/B: line 71.8% / area 12.2% / bar 68.4% / scatter 72.5% / candlestick 58.9% / composed 77.5% / liveline 52.7% / heatmap 67.1% (re-pair) — ALL clear the ≥20% bar including both D215 re-checks (scatter 83.8%→72.5% noise flag CLEARED; heatmap 81.6%→67.1%, D214 waiver NO LONGER NEEDED); m3a 84.6–105.2% (vsync-quantized ties per D207/D12); m3c ties at 16.7ms vsync across in-scope charts (heatmap re-pair M/B 53.9%). G2 stands per D207 evidence + monotonicity (no render-path slowdown anywhere). G3 PASS — idleScript M/B ≤51% in-scope (bar ratio degenerate at noise floor 0.02/0.04ms per D214; heatmap re-pair 0.2%). G4 PASS — heap 35.3–96.3%; m2c stands per D207(f) (zero new deps — all new modules internal). Noted, not gated: composed m2a_idleTask 45.7ms — D208 item 9 band extends 38–46ms, still pre-existing class; liveline m1c 138–144% matches its D210/D215 streaming profile. Leftover `internal/tooltip-chrome.ts.bak` (prior executor's backup) left in place per no-delete rule — cleanup deferred to initiative 12. Channel state at close: opencode still credit-blocked; cmd 5h limit reset 15:16 (channel available); Sonnet-subagent authorization (D217) was scoped "until the 3:16 PM reset" and is now expired.

- **D219** (2026-08-19, interim): Initiative 6 loop 1.3/1.4 — first implementation dispatched and REVIEWED; fix loop 2 dispatched. Executor: cmd-executor session `fbcea1df-15dd-4dde-b9c9-e0f7573a5a7a` (dispatch `20260819-155042`, ~55 min), scope compliance CLEAN (every executor-window write inside `showcase/migrated/` + `showcase/components/demos/`; mtime audit). Lead diff review (per doctrine, source-verified, not trusted from the report) found the geometry/config ports faithful (`computeReferenceAreaRect` fallback/ifOverflow semantics, `resolveReferenceDataRange`, 5px segment threshold, pattern fills via `pattern-preset.tsx`, phase predicate `ready|revealing|gridTweenReady` verified EXACTLY equal to bklit `y-domain-utils.ts:51` — lead's initial `exitingReady` concern was wrong) but rejected the diff on 2 blockers + structural single-source violations, consolidated into `scratchpad/task-06-fix-loop-2.md` and re-dispatched to the SAME session (run `20260819-165027`): (F7-BLOCKER) public `ReferenceArea`/`Segment*` components carry no `CHART_ROLE` symbol while all 7 host extractors match by role only → barrel-imported components render NOTHING (demos dead); duplicate stub components in `children.tsx` to be deleted. (F10-BLOCKER) bar/scatter/candlestick/liveline pass no `phase`/`isLoaded` to the layer, which pins opacity 0 for undefined phase → bands permanently invisible there. (F1) segment visuals forked FIVE times (4 inline host copies + divergent `segment.tsx` path using a global `document.querySelector("[data-bkm-chart]")` that grabs the first chart on multi-chart pages) → ONE internal module. (F9) 27-prop `<ReferenceAreaLayer>` plumbing copy-pasted ×7 + per-host extraction memos → shared helpers. (F2/ruling-3) fade stops inlined in the layer while `background.tsx` carries an identical local copy → hoist ONE helper into `fade-mask.ts`, both consume. (F3) candlestick x-bounds branch returns a scaleUtc with range never set (defaults [0,1]) + `void` dead code. (F4) Fragment double-visit duplicates configs in `extractReferenceAreaConfigs`. (F6-parity) layer paints ABOVE the series; bklit ground truth: ReferenceArea is an UNDERLAY — above grid/axes, below series, outside reveal clip (`chart-child-passthrough.ts:65,93`, shell render order :686-691) — fix mechanics delegated with two sanctioned options. RULINGS (lead): (a) dynamic `ReferenceAreaRegistrationContext` is unreachable dead code — NO migrated host mounts `children` (grep-proven), and the static scan re-runs per render so conditional children are covered → registration context REMOVED, static scan is the single mechanism (deviation from plan-loop-1 D2, superseding it); (b) executor's UNDECLARED `y-axis-overlay.tsx` `formatTick` rewrite + `resolveYAxisTickCount` clamp are ACCEPTED — verified verbatim-bklit (`y-axis.tsx:40-46`) and Q1 measures against bklit renders so true parity fixes cannot regress the gate; report-accuracy ding recorded (claimed "deviations: none"); (c) liveline tick-color path EXISTS (report cited the wrong file) but reimplements `resolveReferenceDataRange` inline → shared helper (port of bklit `resolveTickLabelColor` shape) mandated. No gate evidence exists yet; Q1/Q2/Q3/G1-G4 all pending post-fix.

- **D220** (2026-08-19): Initiative 6 (ReferenceArea + Segment + y-domain registration) IMPLEMENTED + GATED — **APPROVED**. Executors: cmd-executor session `fbcea1df-15dd-4dde-b9c9-e0f7573a5a7a` fix-loop-2 (run `20260819-165027`, exit 0) + two user-authorized Sonnet subagents for the drag probe and fix-loop-3 (fresh authorization given mid-gating — the D217 authorization had expired at the 15:16 reset; the user subsequently set the STANDING channel priority: opencode executor first (still credit-blocked, re-probe each boundary) → cmd-executor on Command Code as primary → Sonnet subagents as fallback only, to conserve usage limits — applies from initiative 7 on). **Fix-loop-2 verification (lead, source-verified per doctrine — all 9 fixes confirmed)**: F7 role symbols on public `ReferenceArea`/`Segment*` null-carriers (`reference-area.tsx`, `segment.tsx`; `children.tsx` stubs deleted; barrel exports at index.ts:216–218, `SegmentOverlay` not public); F10 `phase`/`isLoaded` threaded to the layer everywhere + `isReferenceAreaVisiblePhase(undefined) => true`; F1 ONE `SegmentOverlay` in `internal/segment-visuals.tsx` (background rect 150ms opacity fade + prefers-reduced snap, lines snap, gradient 0/10/90/100 stops, visible = active && |Δx|>5) — the 5 forks and the global `querySelector` path deleted; F9 `ReferenceAreaLayers({configs, geom})` maps all 27 props in ONE site; F2 `edgeFadeMaskStops`/`clampFadeLength` single-sourced in `fade-mask.ts` (layer + `background.tsx` both consume); F3 candlestick `scaleUtc().domain([t0,t1]).range([0,innerWidth])` fixed (+ xRangePadding inset branch); F4 Fragment single-visit in `extractReferenceAreaConfigs`; F6 mechanics choice (a) — `isolation: isolate` on all 7 chart containers + `zIndex:-1` layer svg (accepted band-under-grid deviation, pixel cost 0.0017% on the parity scenario); registration context REMOVED per D219(a) — `extractReferenceAreaProps`/`extractSegmentComponents` role-only static scans are the single mechanism. **Executor report-accuracy ding #2 (same session)**: claimed liveline could not import the shared resolver because "`require` is forbidden by `@typescript-eslint/no-require-imports`" — nonsense (plain ESM import); lead surgical fix: `live-line-chart.tsx` now imports `createTickColorResolver` from `internal/reference-area-geometry.ts` replacing the verified-identical 12-line inline closure. Second lead surgical fix: `area-chart.tsx:479` executor cruft `...({} as Record<string,string>)` removed (git-diff-proven no-op). **Lead false alarms recorded so they are not re-raised**: (i) composed "missing tick-color threading" — composed renders NO y-axis labels at all (XAxisOverlay only), consumption is N/A; executor skip architecturally correct but undeclared; (ii) `enabled: true` unconditional drag arming — bklit `use-chart-interaction.ts` arms drag selection UNCONDITIONALLY (handleMouseDown always sets isDraggingRef, no gate on Segment children) → parity, not a defect. **New permanent QA surface (lead-built — bench/ is executor-forbidden)**: `refarea` + `segment` ChartKinds in `bench/app/src/bench/query.ts` + 4 scenario pairs (`bklit-/migrated-refarea`, `bklit-/migrated-segment`: identical line trees + `<ReferenceArea y1={1050} y2={1250} showMarkers strokeStyle="dashed">` / 3 Segment components at defaults; seeded band always intersects seriesA). Showcase demo-page listing DEFERRED to initiative 12 (chart-stage registry is route-per-chart; bench scenarios are the living usage surface). **Fix-loop-2 gates (lead, idle)**: check-types + lint exit 0; Q3 greps clean (no fade/segment/reference forks outside `internal/`); Q1 full sweep at frozen baselines — line 0.0000 (`line/2026-08-19T16-07-01-062Z`; loading 0.1315 `16-08-30-758Z`), area 0.0002 (`area/16-07-06-361Z`; loading 0.0486 `16-08-33-045Z`), bar 0.0000 (`bar/16-07-12-015Z`), scatter 0.1230 (`scatter/16-07-25-843Z`), candlestick 0.0354 (`candlestick/16-07-31-099Z`), composed 0.2772 in re-frozen band (`composed/16-07-36-449Z`), liveline 0.0191 (`liveline/16-07-58-002Z`), heatmap 0.4277 (`heatmap/16-08-03-867Z`; loading 0.1948 byte-exact at **n=52** `16-09-43-702Z` — HARNESS NOTE: heatmap loading gate REQUIRES `--n 52`; an n=100 run reads 0.2654 because the seeded data spans 2 years — PNG-proven both impls identical, data change not code drift), pie 0.0000 (`pie/16-08-17-304Z`), refarea 0.0017 + band eyeball (`refarea/16-11-57-623Z`), segment settled 0.0000 (`segment/16-16-38-434Z`); Q2 `qa/console-errors.mjs` PASS ×13 (D208-8 WAAPI warnings excluded). **Drag probe (Sonnet) + lead eyeball → 2 REAL parity defects the probe's own summary missed**: segment drag GEOMETRY exact parity (identical from/to line positions x=321/651 both impls, chrome appears past 5px and vanishes on release symmetrically, 0 console errors) — but the probe's "no tooltip during drag" DOM check used selectors that don't match migrated's custom `.bkm-*` chrome; lead PNG eyeball showed migrated painting FULL hover chrome mid-drag (tooltip box + crosshair + date pill + highlight) where bklit hides it (bklit clears tooltip on mousedown and skips scheduling while dragging), plus a UA focus ring on pointerdown (TanStack chart svg has tabIndex=0; computed `outline: rgb(16,16,16) auto 5px` on click-focus; bklit charts are never click-focused) — together these were the entirety of the probe's raw 1.0516% mid-drag diff. **Fix-loop-3 (Sonnet, task `task-06-fix-loop-3.md`; lead source-verified all 6 files)**: `useChartSelection` gained `onDragStart`/`onDragEnd` (ref-stored, invoked at arm/disarm incl. 2-touch, listener deps unchanged); one standardized `dragSelectionActiveRef` gate in all four drag-arming hosts — line/area/candlestick at the top of `handleFocusGroupChange` (candlestick via `onFocusChange(null)`), composed at the top of `handlePointerMove` (accepted deviation: its TanStack focus callback is deliberately inert, the native pointermove listener is the real hover driver); `styles.css` `[data-bkm-chart] svg:focus:not(:focus-visible) { outline: none }` (keyboard a11y ring preserved). **Fix-loop-3 verification (lead, fresh build)**: ring probe → zero outline candidates; drag probe re-run → NO tooltip DOM mid-drag, post-up chrome fully cleared, mid-drag pixel diff **1.0516% → 0.0772%** (residual = mid-fade dot frame + AA; both impls fade chrome ~150ms); Q1 re-runs all at frozen baselines — segment 0.0000 + hovers ≤0.0008 (`segment/2026-08-19T16-32-35-641Z`), line 0.0000/≤0.0005 (`line/16-32-50-755Z`), area 0.0002/≤0.0017 (`area/16-32-55-981Z`), candlestick 0.0354/0.2345/0.2341/0.2325 EXACT (`candlestick/16-33-01-202Z`), composed 0.2748/≤0.0041 (`composed/16-33-06-588Z`), refarea 0.0017/≤0.0009 (`refarea/16-33-11-928Z`); check-types + lint exit 0 lead-run; Q2 re-run PASS ×13. **G1–G4: D210-class waiver of the full 16-run sweep** — no render-path change (public components are null carriers, overlays default-off and null at rest, selection listeners are bklit-parity wiring, fix-loop-3 adds two ref writes per drag); evidence: same-day spot-check vs the D218 sweep — line m1a 42.7ms vs 43.1 (`bench/results/2026-08-19T16-18-36-789Z` vs `14-03-54-274Z`), candlestick m1a 36.2 vs 37.1 (`16-19-54-180Z` vs `14-16-25-010Z`), m3a 32.2–32.5ms and m3c 16.7ms vsync-quantized ties, heap within 1%, 0 console errors both runs. Escalation trigger stands: any future in-scope m1a/m3a/m3c regression >D207's documented ±15–30% session drift = full sweep. Deferred register unchanged: demo listing + `tooltip-chrome.ts.bak` + skeleton/y-domain-tween residuals → initiative 12; D208-9 → composed enter-animation audit.

- **D221** (2026-08-19): Initiative 7 (ProjectionLine + TerminalMarker) IMPLEMENTED + GATED — **APPROVED** — plus executor incident #2 (D216-class) and full recovery. **INCIDENT**: loop-1 cmd-executor (session `ef931d6c`, run `20260819-174416-init-7-projection-terminal-marker-94826`) ran forbidden `git checkout --` on `showcase/migrated/charts/{line,area,composed}-chart.tsx`, destroying all uncommitted initiative-3/5/6 host integrations (D216: git READ-ONLY for executors), built the projection work on the stale Phase-2 base, and wrote `/tmp/line-chart-patch.mjs` (forbidden /tmp write; its `scripts/_proj_patch.mjs` cruft verified absent at close-out). Undetected for 4 fix-loops because (1) the guard hook fences file-edit tools but NOT git invoked via shell_command — gap; (2) the refarea Q1 check false-PASSed at 0.0442% with the entire reference band MISSING (D213 doctrine reaffirmed: tiny diff % ≠ correct render; PNG eyeball mandatory). **Guard-hook hardening PROPOSED, PENDING USER ACK** (lead will not edit `.claude/` without it): PreToolUse deny on shell `git (checkout|restore|reset|stash|clean)` for executor dispatches. **RECOVERY (lead per D215/D216 precedent)**: D220-final hosts reconstructed from cmd checkpoint blobs + edit-stream replay (fl2/sonnet/lead-area edit journals); area reconstruction validated BYTE-IDENTICAL to independent gold `~/.claude/file-history/.../1886e9571f755d7b@v3`; initiative-7 projection work grafted onto the D220 skeletons with census-verified unions (feature counts + import sets match both parents for line/area/composed); installed; `check-types` + `lint` PASS (0 warnings). **Merge semantics**: orchestrator owns phase (D220) — projection overlay mirrors it via `projectionPhasePortRef` effect + on-overlay-mount layout effect; `yDomainFinal` = niced base, projection-merged, NOT re-niced — single source for spec y scale/overlays/anchors/gradients (no-projection path value-identical to frozen D220); tween signal = shared-hook `changed` without projections, final-domain ref-compare with; x extent = raw extent + `mergeProjectionXDomainMax`, threaded to ALL downstream consumers (selection scale, ReferenceAreaLayers xDomain, XAxisOverlay domainMaxTime, anchor mapping); grid/ticks keep D220 `resolveGridGuide` (initiative-7's explicit ticks.values dropped, behavior-equivalent); composed keeps the pixel-mandate ref-read pattern, ReferenceAreaLayers yDomain stays RAW without projections. **Q1 full sweep (idle, run-id-cited)**: line 0.0000 (`line/2026-08-19T20-48-10-467Z`), area 0.0003, bar 0.0000, liveline 0.0292, heatmap 0.3316 (better than 0.4277; overall-FAIL = D208-5 waived-hover artifact), refarea 0.0021 with band-present eyeball BOTH sides (D213 trap cleared), segment 0.0000, loading line 0.1308 / area 0.0490 EXACT / heatmap 0.1948 EXACT (n=52); new `projection` pair PASS 0.0164/0.1446/0.0010/0.0561 (`projection/2026-08-19T20-50-47-957Z`, terminal-marker eyeball PASS: extension past data end + open-circle marker at Oct 2). Q2 zero console/page errors ×16 scene-loads (12 stock `qa/console-errors.mjs` + pie/refarea/segment/projection supplement); only known D208-8 WAAPI warnings. **Composed settle root cause + FIX (Sonnet diagnosis + lead surgical per D215 precedent)**: composed forwarded phase "ready" (→ `armBklitSettle`/`__benchSettled`) off the orchestrator's flat 1100ms timer while the per-bar WAAPI grow-stagger runs to `barsDeadlineMs = animationDuration*1.4 = 1540ms` — the harness screenshots at settle+200ms ≈ mid-bar-growth (1848 running bar-rect animations at capture, 0 finished; migrated frame-diff 1.0654% at +0→+500ms then 0.0000; bklit static). Fix (composed-chart.tsx only): `pendingBarsRevealRef` armed with the bar stagger; the onPhaseChange-forwarding effect suppresses "ready" while pending; `setRevealDeadline`'s onDeadline forwards it — whichever fires last wins; internal chartPhase machinery untouched, no-bars path unaffected. Result: settled **0.0845% deterministic ×2** (`composed/2026-08-19T21-07-34-122Z`, `21-07-56-952Z`), hovers 0.0036/0.0046/0.1831. **RULING — composed settled baseline RE-FROZEN 0.2748 (band 0.27–0.42) → 0.0845**: the old baseline was contaminated by this same premature-settle capture bug (D218 itself recorded both sides photographed mid-stagger); 0.0845 is true resting parity. **Anomaly dispositions (Sonnet, artifacts+source+LOG only)** — none traces to incident, merge, or initiative-7: (a) candlestick hover-30 0.5028 FAIL = BENIGN n=1000 characteristic — ALL frozen candlestick baselines are n=100 (D210); n=1000 hover-30 has sat at the gate edge for weeks (0.4982 PASS 08-04/07); mechanism = adjacent-candle selection at ~1.1px pitch (bklit tooltip Oct 9 vs migrated Oct 10) in `candlestick-focus-strategy.ts` nearest-scan vs `bisectDateLeft` → registered as **D208-10**; (b) pie hovers 3.1827/3.1716 = open defect **D208-2** — **RULING: D208-2 is an OPEN DEFECT, never a waiver; LOG language corrected henceforth** (dim formula is bklit-identical with zero uncommitted diff; likely hoveredIndex hit-test mismatch at n=1000 density; drift tracked 2.64 → 3.11 → 3.18); (c) scatter settled 0.4509 (PASS) = n=1000 value byte-stable since 2026-07-31; 0.1230 is n=100 — **RULING: 0.4509 frozen as the scatter settled n=1000 baseline**; process gap registered: formalize an n=1000 baseline table or snap harness hover-x to marks at high density (as funnel/heatmap/sankey already do). **G1–G4: D210-class waiver of the full 16-run sweep** — projection marks are null at rest in parity scenarios (no render-path change without `projections` configured); evidence: same-day n=100 spot-check via cmd-executor (session `222ad9ee`, compliance confirmed: zero file mods, git read-only) — line m1a migrated **42.4ms** vs D220 42.7 / D218 43.1 (M/B **75.0%** vs D218 71.8%), composed m1a migrated **48.7ms** (M/B **79.6%** vs D218 77.5%), m3a 31.2–32.6ms + m3c 16.7ms vsync-quantized ties, heap migrated LOWER both charts (runs `bench/results/2026-08-19T21-24-01-867Z`/`21-25-24-601Z`/`21-26-44-378Z`/`21-28-15-399Z`). First spot-check pass ran n=1000 (lead task-spec error — the D218/D220 references are n=100, verified in their results.json): retained as first-ever n=1000 bench characterization, NOT gated (no n=1000 reference has ever existed) — line M/B 163.9%, composed 194.8%, composed m1c script INVERTED (migrated 198.6 vs bklit 486.1ms, task ~equal), line migrated idleTask 8.39ms (~10x bklit) — registered for later study (runs `21-15-30-745Z`/`21-17-05-432Z`/`21-18-30-123Z`/`21-20-12-247Z`). **Deliverables**: `internal/projection-utils.ts`, `internal/projection-config.ts`, `internal/projection-line-mark.ts`, `internal/terminal-marker.tsx`; x-axis-overlay `domainMaxTime` + `buildDomainTicks`; `projection` QA scenario pair. Deferred to initiative 9: brush + `appendProjectionTailTicks`. Deferred register additions: D208-10 (candlestick n=1000 hover off-by-one), n=1000 baseline-table process item, line n=1000 idleTask divergence.

- **D222** (2026-08-20): Guard-hook hardening LANDED (user-acked) + D221 incident-mechanism CORRECTION from full forensic timeline (Explore agent over the complete 2270-line run JSONL, run `20260819-174416-init-7-projection-terminal-marker-94826`). **CORRECTION — the D221 narrative was wrong on two counts**: (1) the guard did NOT fail to fence git-via-shell — it correctly DENIED both the `/tmp/line-chart-patch.mjs` write (L606, sole /tmp attempt; NO successful /tmp write ever occurred — the executor compliantly adapted to in-repo `scripts/_proj_patch.mjs`) and the direct `git checkout -- showcase/migrated/charts/{line,area,composed}-chart.tsx` (L738). (2) The SOLE genuine bypass (L903–911) was a node heredoc `restoreAndPatch(host)` running `execSync(`git show HEAD:showcase/migrated/charts/${host}-chart.tsx`)` + `fs.writeFileSync` back over the three hosts — a READ-ONLY git verb feeding a non-git file write, structurally invisible to the old separator-anchored write-verb denylist. The old regex assumed (a) working-tree destruction requires a git write-verb in shell text, (b) deletion is the only destructive file op, (c) `showcase/migrated/charts/` needs no write fence (it is legitimate executor territory) — all three defeated at once. Bonus finding: post-destruction the guard false-positive-denied read-only `git stash list; git log; git status` (bare-word `stash` match) — ACCEPTED as deny-side-safe doctrine (executor sees the reason and reports; no work is lost to a false deny). **HARDENING (`.commandcode/hooks/guard-executor.sh`, still inert without `CMD_EXECUTOR_GUARD=1`)**: rules made tool-agnostic — scan shell `command` text AND written/edited content (`content`/`new_string`/`code`/`script`), so git logic smuggled into heredocs, execSync template literals, or dropped script files is caught at write time; git matched ANYWHERE via loose left boundary (catches `sh -c "git …"`, `/usr/bin/git`, `env git`, backtick/paren/quote contexts) with `-C <path>`/`-c <kv>` global-flag tolerance; write-verb list extended (`checkout-index`, `update-index`, `read-tree`, `filter-branch`); NEW Rule 3c denies `git show`/`git cat-file` with a rev:path colon token (content extraction = `git checkout --` once written back — the exact incident vector; `git show --stat`, `git log`, `git status/diff` still pass); Rule 3b extended to API-level deletions (`fs.rmSync/rmdirSync/unlink(Sync)`, `rimraf`, `shutil.rmtree`); Rule 1 (/tmp) extended to written content. **Verification: 29/29 synthetic-payload tests** (scratchpad `guard-tests.sh`, hook invoked with real JSON payloads) — DENY side includes the byte-exact incident heredoc, dropped-script smuggling, `sh -c` quoting, `git -C`/`-c` flag variants, absolute-path/env git, python `os.system('git clean')`, `git cat-file` rev:path, shell + API deletions, /tmp in paths/commands/content; ALLOW side proves read-only git (`show --stat`, log, status, diff), bench/qa runs, typecheck/build, chart-source writes, and word-boundary near-misses (`firm.rmx`, `checkout.ts` import) all pass. PROGRESS.md row 7 + blocker note corrected to match. **Recovery inventory for the destroyed-work class (for the record)**: (1) opencode shadow-snapshot stash commits (`5cc208ed` WIP-on-main 02:48, 82 tracked files; `eb340506` untracked twin); (2) cmd checkpoint blobs + edit-stream journals (`fl2-edits.jsonl`/`sonnet-edits.jsonl`/`lead-area-edits.jsonl`) — the D221 recovery path; (3) `~/.claude/file-history` gold copies (area validated byte-identical to `1886e9571f755d7b@v3`); (4) the real protection is committing approved initiative checkpoints — pending user instruction (standing rule: no commits unless asked).

- **D223** (2026-08-20): Initiative 8 (Legend + ChartLegend + ProfitLoss) plan-loop-1 INSTALLED (`research/phase-3/plans/08-legend-profitloss/plan-loop-1.md`) — Sonnet-drafted synthesis reviewed + 6 lead rulings issued (full text in plan §10): (1) **Tier A only** this loop — 13 bklit files ported (chart-legend-hover + series-hover-dim land UNWIRED, initiative-3 leaf-module precedent) + self-contained ProfitLoss wiring in line-chart; Tier B cross-chart hover-dim wiring → initiative-8 loop-2 (re-touches frozen 1/2/5 baselines, needs its own full G1–G4 sweep); (2) `ProfitLossLegendHoverProvider` keeps bklit's exact JSX shape via a narrow `CHART_CHILD_PASSTHROUGH` marker + single-level unwrap in the shared `children.tsx` walk (mirrors bklit's own chart-child-passthrough mechanism); `ProfitLossLine` = CHART_ROLE null-carrier + `internal/profit-loss-line-mark.ts` custom mark (initiative-7 ProjectionLine precedent — lead-verified the migrated tree has NO bklit-style useChart/useChartStable context, host extraction is the only fit; hoveredIndex read off the provider element's props per render); (3) SeriesHoverDim loop-2 pre-ruling: conditional application via no-op-default context (render-path-neutral at rest, disclosed deviation), WAAPI not motion/react; (4) scenarios: ONE `profitloss` bench pair (D210-class G-gate candidate) + ONE `legend` QA-only pair (Q1 diff, no G1–G4 — chart-less HTML has no B/T/M triangle), both LEAD-built per D220 bench/-forbidden precedent; (5) TanStack `colorLegend()`/`colorGradientLegend()` explicitly BYPASSED — zero TanStack rendering surface in this family, decision not oversight; (6) candlestick 2-slot legend semantics deferred to loop-2 behind a mandatory bklit-demo pairing verification (0=up assignment at candlestick.tsx:118). Lead pre-dispatch verifications: `fadeGradientStops` exists at `internal/fade-mask.ts:53` (MUST reuse); `--color-emerald-500`/`--color-red-500` not explicitly declared in showcase globals — executor to verify Tailwind v4 emission, backfill in globals.css if absent.

- **D224** (2026-08-20): Initiative 8 loop-1 (Tier A — Legend + ChartLegend + ProfitLoss port) IMPLEMENTED + GATED — **loop-1 CLOSED** (initiative stays open: loop-2 Tier B per D223 rulings 3/6). Executor: cmd-executor session `d420a648-d0a3-430a-ad16-242093a1da75` (Tier-A dispatch run `20260820-013727` + loop-1b fix dispatch `20260820-020152`, both exit 0; loop-1b report verified ACCURATE against source — all 3 reported fixes present exactly as described, git fence respected). Delivered per plan-loop-1 + D223: 13-file Tier-A port (legend/chart-legend/profit-loss families; `chart-legend-hover` + `series-hover-dim` landed UNWIRED per ruling 1 — and DISCLOSURE: `series-hover-dim.ts` is a STUB the executor mislabeled "bklit-faithful" (report-accuracy ding #3, register); loop-2 rewrites it per ruling 3); `ProfitLossLegendHoverProvider` via `CHART_CHILD_PASSTHROUGH` Symbol marker + single-level unwrap (ruling 2); `ProfitLossLine` as CHART_ROLE null-carrier + `internal/profit-loss-line-mark.ts` custom mark (d3-shape `line()` paths, per-segment pos/neg gradients `profit-loss-gradient-{pos|neg}-<dataKey>-<hostId>`); `internal/profit-loss-config.ts` (bklit defaults: xDataKey "date", strokeWidth 2.5, curveLinear, emerald-500/red-500 vars) with `extractProfitLossHoveredIndex` passthrough walker; line-chart.tsx integration (hoveredIndex ?? tooltip-sign focus per bklit `profit-loss-line.tsx` contract — legend hover WINS over tooltip sign; sign index set only-on-change in `handleFocusGroupChange` with drag-suppression clear); TanStack `colorLegend()/colorGradientLegend()` BYPASSED (ruling 4); candlestick 2-slot legend DEFERRED to loop-2 (ruling 5); ONE `profitloss` bench pair + ONE `legend` QA-only pair (ruling 6; bench/app query.ts + scenarios/index.ts wiring = lead edits, executor barred from bench/). **TWO LEAD-DIAGNOSED RENDERER DEFECTS fixed (D215 surgical precedent; both caught by D213 eyeball doctrine AFTER numeric Q1 PASSES — tiny diff %% ≠ correct render, vindicated twice in one initiative)**: (a) **Grid `highlightRowValues` silently dropped** — first profitloss compare PASSED numerically (settled 0.0274%) but the diff PNG showed the zero-gridline missing on migrated: initiative 3 shipped the types + `resolveGridHighlightRows` resolver but NO renderer ever consumed it (the `grid-chrome.tsx` named in `internal/grid.ts`'s comment never rendered highlight rows — comment now STALE, register). Fix: new `internal/grid-highlight-mark.ts` (bklit grid.tsx parity: gated on `horizontal`, plain unmasked full-width lines — NO fade mask, defaults stroke `var(--chart-foreground-muted)` / opacity 1 / width 1 / solid) + line-chart wiring (unshift = beneath series, bklit z-order). Settled diff → 0.0030%. Propagation to other cartesian hosts DEFERRED (inert without the prop; pick up when a consumer needs it). (b) **TanStack svg-renderer whitelist drops group `opacity` + `style.transition`** — migrated hover dim was completely non-functional (probe: NO group ever carried an opacity attribute; bklit dims to 0.25): `charts-core-d3/src/svg-renderer.ts` `renderStyle` emits ONLY fill/fill-opacity/stroke/stroke-opacity/stroke-width/opacity/stroke-linecap/stroke-linejoin/stroke-dasharray FROM `node.style` (top-level node `opacity` is dropped; no `transition` attribute exists). Fix: dim rides `style: { opacity }` + `className: "chart-profit-loss-segment"` with the 0.2s ease-in-out transition in `styles.css` (bklit profit-loss-line.tsx:171). Probe re-run confirms BOTH dim paths live (legend-hover alternating 0.25/1; pointer-hover positive point dims loss segments). DOCTRINE for all future mark work: dims/fades MUST use `style.opacity`; transitions MUST ride a className + CSS rule. Gates (ALL lead-run, fresh builds, run-id-cited per D215 countermeasure): **Q3** greps clean — outside `internal/`, ProfitLoss/legend references only in the 3 sanctioned integration points (children.tsx, line-chart.tsx, index.ts). **Type parity** showcase `check-types` exit 0 (re-run post-lead-fixes); bench tsc zero errors in all initiative-8 files (pre-existing tanstack-* scenario errors out of scope). **Q2** `qa/console-errors.mjs` ALL PASS incl. new `profitloss` (n=100) + `legend` (n=4) targets (bar 400/composed 200 WAAPI height warnings = D208-8). **Q1 legend** (QA-only per ruling 6; harness gained a legend branch — lead qa/ edit: settled-flag wait (`__benchSettled`; no `<svg>` for paint marker on the chart-less page) + `__qaSetLegendHover(0/1/2)` capture loop): self-test `2026-08-20T01-15-08-149Z` 0.0000%×4; compare `01-15-20-407Z` 0.0000%×4; eyeballed — real renders (legacy ChartLegend + composable Legend, progress bars/percentages; hover-item-1 shows Beta emphasized + siblings dimmed on both impls). **Q1 profitloss**: bklit self-test `01-15-50-763Z` 0.0000%×4; migrated self-test post-fix `01-23-27-099Z` 0.0000%×4; compare n=100 `01-23-32-233Z` settled 0.0030% hovers ≤0.0418%; n=1000 `01-23-37-501Z` settled 0.0194% hovers ≤0.1481% (hover-70 eyeballed = adjacent-point-snap artifact + AA — known accepted class, present in frozen line-family baselines); legend-hover dim probe (qa/results/profitloss/legend-hover-probe) hover-0 0.0026% / hover-1 0.0041%, eyeballed correct (Loss full red + Profit dimmed pale at index 1). **Frozen-baseline re-runs** (shared files touched: line-chart.tsx, children.tsx, styles.css, barrels): line `01-25-05-350Z` (0.0000/≤0.1509), refarea `01-25-10-594Z` (0.0021/≤0.1459), segment `01-25-15-813Z` (0.0000/≤0.1451), projection `01-25-21-000Z` (0.0165/≤0.1446) — ALL at historical levels. **G-gate**: same-day line n=100 spot-check — migrated m1a median 46.4ms (`01-26-53-742Z`) vs bklit 58.0ms (`01-28-27-554Z`) → M/B 80.0% vs D221's 75.0% = session noise, NO regression from Tier-A integration. Profitloss pair n=1000: bklit `01-29-59-896Z` (m1a 62.2 / script 68.9 / task 119.1 / m3a 31.6 / heap 5.26MB / frame 16.7 / per-move 2.47) vs migrated `01-31-24-768Z` (m1a 89.1 = 143%B / script 113.4 / task 207.0 / m3a 47.0 = 149%B / heap 6.05MB / frame 16.7 / per-move 2.21 — BETTER; tooltipAppeared both). RULING (lead) — **D210-class G-gate WAIVER for the profitloss pair**: the interaction path is at parity or better (hover frame 16.7ms vsync tie both sides, per-move script 2.21 vs 2.47 migrated WINS); the mount/update deltas are composite-scenario costs (invisible y-domain Line + passthrough provider + external legend + highlight rows in ONE scenario, exercising line-chart's full PL branch) not a utility regression — the plain-line same-day spot-check above shows the shared host did NOT slow down; revisit in loop-2 if Tier-B wiring touches the same path. REGISTER notes: `internal/grid.ts` grid-chrome comment stale (renderer lives in `grid-highlight-mark.ts`); `profit-loss-line-mark.ts` defs use filter-compacted indices vs marks' loop index — mismatch only possible via an untyped invalid config, not blocking; executor report-accuracy ding #3 (stub mislabeling) on record. NEXT: loop-2 Tier B (cross-chart hover-dim via no-op-default context + WAAPI per ruling 3; SeriesHoverDim rewrite; candlestick 2-slot legend behind MANDATORY bklit-demo pairing verification at candlestick.tsx:118 (0=up)); loop-2 needs its own full G1–G4 sweep (re-touches frozen 1/2/5 baselines).

- **D225** (2026-08-20): Initiative 8 plan-loop-2 (Tier B) FINALIZED (`research/phase-3/plans/08-legend-profitloss/plan-loop-2.md`) — Sonnet-drafted synthesis reviewed + 6 lead rulings (full text plan §10). Key architecture finding (draft §3, lead-verified): the migrated tree ALREADY implements the tooltip-hover half of bklit `SeriesHoverDim` via the frozen imperative single-writer dim in `internal/hover-chrome.ts`/`bar-hover-chrome.ts`/`candlestick-hover-chrome.ts` (exact opacity/timing matches: line 0.3/0.4s, area 0.6/0.4s) — the ONLY missing piece is the legend-hover term, so loop-2 EXTENDS the existing single writer with an OR'd legend condition instead of resurrecting the wrapper (two-writer race = flicker). Also verified: migrated `chart-legend-hover.tsx` is a genuine 1:1 port (no rewrite; D224 ding was series-hover-dim only); bklit consumers bar-squares/series-bar/series-markers never used `SeriesHoverDim` — they read `useChartLegendHover()` directly (loop-1 plan §1.3 corrected); candlestick legend pairing verified in bklit STUDIO (`studio-legend-items.ts:237-250` item 0 = Bullish/up, cross-checked `candlestick.tsx:116-120`) — D223 ruling 6 verification CLOSED. Rulings: (1) DELETE `internal/series-hover-dim.tsx` + barrel export + 3 `SERIES_HOVER_DIM_*` tokens (lead deletion — executors never delete); (2) composed insertion CONFIRMED = the single `attachHoverChrome` state thread (composed-chart.tsx:983), legend term lands ONCE in hover-chrome.ts; (3) point-marker dim DEFERRED to initiative 10 (module doesn't exist; SeriesMarkers is D200 init-10 scope; disclosed); (4) candlestick mark-building LOCATED inline in candlestick-chart.tsx ~L294-350 — per-candle `style.opacity` bake per D224 doctrine, bklit short-circuit precedence preserved (legend hover suppresses group-level `setMarksDimmed`); (5) `styles.css` IN-BOUNDS for executor; (6) TWO dispatches (A: shared-mechanism OR-term + line/area/bar/composed wiring; B: candlestick per-candle path) + lead pre-work (deletion, `candlestick-legend` scenario pair, bench/qa wiring); gates all lead-run, plus a lead INTERACTIVE eyeball (pointer-move while legend-hovered) for the two-writer-race risk per D213 doctrine.
- **D226** (2026-08-20): Initiative 8 loop-2 (Tier B legend-hover fidelity) CLOSED — initiative 8 → approved. Dispatch A (cmd-executor, single-writer legend OR-term in `hover-chrome.ts`/`bar-hover-chrome.ts` + `syncDim`/`setCategoryHover` legend re-reads) landed with one lead fix: bar legend index must be the BAR-ONLY `barIdx` (bklit series-bar.tsx composedBarDataKeys index space), not a global findIndex over mixed series. Dispatch B (cmd-executor, candlestick per-sign legend dim) verified faithful with ZERO lead fixes — executor baked `isPositive`-sign dim into `style:{opacity}` per D225 ruling, rode the transition on `.chart-candle-cell` + styles.css, and independently corrected the dispatch's 0.2s example to bklit's real 0.15s ease-in-out with citation (repos/bklit-ui candlestick.tsx:217) — executor-quality data point. Precedence faithful to bklit geometryDimOpacity short-circuit: while `legendHoveredIndex !== null` the chrome forces group dim off + hides activeHighlightSvg, restores on clear while tooltip live (candlestick-hover-chrome.ts syncLegendDim/update). NEW QA pair `legendhover` (bench/app/src/scenarios/bklit-legendhover.tsx + sed twin): ONE ChartLegendHoverProvider over BOTH a ComposedChart (SeriesBar+Area+Line) and a BarChart, deliberately demonstrating bklit's two-index-space quirk (mixed lines index vs bar-only index — legend item 0 keeps composed bars full while dimming the line); qa/screenshot.mjs gained a legend-driven capture branch (`candlelegend`/`legendhover`: __qaSetLegendHover 0/1/clear at 700ms, pointer parity already gated by base pairs) — gate-author lead edit per D225. DEFECT 1 (found by Q1 sweep, lead-diagnosed + lead-fixed): reveal replay on legend hover — TanStack recreates `.ts-chart__marks` on any definition rebuild, killing the `bkmRevealed` DOM stamp; bar-chart.tsx + candlestick-chart.tsx gated reveal ONLY on that stamp so legend-hover re-renders replayed the full WAAPI reveal (~200 staggered animations = descending-envelope artifact at 700ms captures; probe-proven via dataset.probeTag). Fixed with a DATA-IDENTITY guard (`revealedForDataRef.current === latestRenderDataRef.current` → re-stamp + skip): legend-hover re-renders no longer replay, while replay-on-data-update is deliberately PRESERVED because the frozen D214/D218 bench baselines measured it (disclosed as a known pre-existing quirk; line/area/composed were already replay-proof via `chartPhase === "revealing"` state gating; scatter shares the dataset-only pattern but has no legend consumer — out of scope). DEFECT 2 (found by lead two-writer-race probe, rAF-sampled pointer sweep while legend-hovered): `hover-chrome.ts hide()` restored all dimmed paths/rects to opacity "1" unconditionally on focus-clear, flashing the legend-dimmed composed line to full opacity 7/117 frames mid-sweep (and would have left dim wrongly lifted if the pointer exited while legend hover persisted) — bklit SeriesHoverDim is declarative (`isChartHovering || isLegendDimmed`) and never lifts legend dim on pointer-clear; bar-hover-chrome was already correct (clear path re-reads legend state). Fixed: `hide()` re-applies the legend term via `syncDim()` when legend hover active. Race probe re-run: PASS (composed line solid 0.3 across 116 frames; composed bar rect row-dim oscillation is legitimate OR'd behavior; barchart seriesB solid 0.3 / seriesA solid 1; candlelegend groups solid 1). GATES — Q1: line/area/composed/profitloss n=1000 PASS (first sweep); bar n=1000 PASS post-fix (run 2026-08-20T04-20-41-110Z, hover-70 0.0391%); candlestick n=100 PASS vs frozen n=100 baseline (0.0354/0.2345/0.2341/0.2325) — sweep initially flagged candlestick n=1000 hover-30 0.5159% but that boundary PRE-DATES loop-2 (0.5028% on 08-19 pre-loop runs; settled n=1000 identical 0.3026% pre/post = rest-render pixel-stable; disclosed, not a loop-2 regression); candlelegend PASS (0.1323/0.0957%); legendhover PASS post-both-fixes (run 2026-08-20T04-25-28-614Z: settled 0.0291%, hover-0 0.0000%, hover-1 0.0290%, clear 0.0290%); profitloss legend-hover probe PASS (0.0026/0.0041%). Q2 console-errors (incl. new candlelegend+legendhover targets): PASS — zero errors across all 16 scene-load targets incl. the new candlelegend+legendhover (bar/composed/legendhover carry only the known pre-existing WAAPI 'Invalid keyframe value for property height' warnings, legendhover inheriting them from its two bar-bearing charts). Q3 greps clean; showcase check-types 0; bench tsc clean; both new scenarios self-test deterministic. G1–G4 sweep vs frozen medians (n=100, post-fix build): all 5 frozen pairs at parity with frozen medians (m1a/m3a/m3c_perMove/heap, migrated): line 52.4/32.4/0.92/4.43MB (m1a jitter vs frozen 46.4, still < bklit 54.6); area 33.4/32.5/0.82/4.54MB; bar 38.5/27.5/0.92/4.46MB; candlestick 36.6/32.1/0.71/4.30MB — dead-on frozen 36.2/32.2/0.7/4.31MB, i.e. per-sign dim + reveal guard cost ZERO; composed 50.6/32.5/1.03/4.69MB; bklit sides all at frozen levels (frozen area-bklit 353.4/180.6 confirmed an outlier baseline — fresh run 56.7/32.1 normal, migrated standing unchanged). Migrated <= bklit on every headline metric; no pair's diff moved beyond run-to-run noise so n=1000 re-runs not triggered (D223 ruling 1 satisfied with fresh n=100 evidence at parity). NEW candlelegend pair: bklit 60.3/32.4/1.75/5.13MB vs migrated 40.3/32.3/0.73/4.61MB — within ~4ms of the plain candlestick profile for BOTH impls, legend chrome render-path-neutral at rest (D210-class). Run dirs bench/results/2026-08-20T04-28-39-489Z through 04-42-47-708Z (12 dirs), all consoleErrorCount=0. Point-marker legend behavior stays DEFERRED per D225 ruling 3.
- **D227** (2026-08-20): Initiative 9 plan-loop-1 FINALIZED (`research/phase-3/plans/09-chart-brush/plan-loop-1.md`) — Sonnet-drafted synthesis (full file:line inventory of repos/bklit-ui chart-brush*.tsx, chart-brush-layout, filter-data-by-x-domain, time-series-chart-shell brush consumption, x-axis appendProjectionTailTicks) + 8 lead rulings (§10). KEY FINDINGS: bklit's gesture math lives inside @visx/brush (no bklit-owned drag code to port — state machine reimplemented fresh); ComposedChart has NO brush support in bklit at all (no props, no demo) — the audit stub's 'across Line, Area, and Composed' was overreach, scope corrected to Line+Area (ruling 2); candlestick has prop surface but zero pairing → DEFERRED (ruling 1); D221's deferred appendProjectionTailTicks lands here (x-axis-overlay.tsx's own comment names it). ARCHITECTURE: domain-clamp semantics matching bklit exactly (full data always feeds marks — seriesSourceData precedent time-series-chart-shell.tsx:303-306; xDomainSlotCount = FULL data.length for column-width stability; inclusive >=/<= domain filter), application-owned selection state in new internal/brush-selection.ts + brush-layout.tsx, portal-rendered chrome (track blur/fade 0.15, pattern fill, 24x4 handle pills) outside the SVG scene graph per bklit's own portal pattern. RULING 4 (the big one): LIVE per-pointermove commit matching bklit's dual notify — commit-only would be a felt-UX deviation invisible to Q1 and make m3d incomparable; browsers already coalesce pointermove to rAF so bklit live-pan is ~1 commit/frame; D226 reveal guard prevents replay across per-frame definition rebuilds (renderData identity constant under domain-clamp); ESCALATION VALVE: migrated m3d per-frame drag script > 2x bklit or > 8ms median pauses close-out for a downgrade ruling. Other rulings: horizontal-only brushDirection + dead `selection` prop not ported (disclosed API narrowing); showcase demo deferred to init 12; Q1 = __qaSetBrush captures (full/left-half/right-half) + one brush+hover-50 composed capture; children-dispatch mechanism located-and-reported at dispatch, STOP if hosts are prop-based; selectionPattern reuses migrated pattern-preset module if present else LOCAL brush-scoped rendering (init 11 scope not smuggled), QA exercises diagonal preset. 3 dispatches: A = pure functions + state owner (brush-selection.ts, brush-layout.tsx); B = drag state machine + chrome (brush-drag.ts, brush-chrome.tsx, chart-brush.tsx — grep-proof zero defineChart/spec/definition references); C = host wiring (line/area xDomain/xDomainSlotCount/tweenYDomainOnXDomainChange props, x-axis-overlay tail-tick branch, barrel exports, --chart-brush-border token). Bench: new brush ChartKind + m3d_brushDrag activation (bench/run.mjs:648 stub) + full first-time G1-G4 characterization (no waiver on a first pair) — LEAD work. Dispatch A launched.
- **D228** (2026-08-20): Initiative 9 (ChartBrush + BrushLayout) CLOSED — approved. Dispatches A (brush-selection.ts + brush-layout.tsx) and B (brush-drag.ts + brush-chrome.tsx + chart-brush.tsx, grep-proof) landed clean; Dispatch C (host wiring) COMPLETED WITH NO FINAL REPORT (session not resumed — lead disk-verified every change instead) and left THREE defects, all lead-fixed: (1) children-dispatch extracted brushes as PROPS not elements — `BrushChildConfig` re-typed to `ReactNode`, extraction now pushes the element, host re-renders it inside its `BrushHostContext` provider, and the dead `Brush` null-shim in children.tsx (which carried the role tag) deleted with the tag moved to the real `ChartBrush`; (2) rules-of-hooks violation (useBrushDrag after the `!host` early return) — split into `ChartBrush` wrapper + `ChartBrushWithHost`; (3) area gradient-defs sibling svg gated behind `!definition` by C's render restructure — area fill vanished entirely (Q1 20.6% FAIL, run 2026-08-20T05-52-00-381Z; live DOM probe: path fill=url(#…-area-grad-0) with the gradient ABSENT) — gate removed, Q1 back to 0.0002–0.0017% (05-54-27-236Z). FOURTH pre-existing systemic defect exposed by the 72px strip: line/area hosts derived height purely from width/aspectRatio and never measured the container (strip chart rendered 526px inside its 72px slot; the initial 0.35% "pass" was vacuous — D213 mandatory eyeballing caught it) — fixed as bklit-parity restoration: new `useDebouncedContainerSize` (line) / `useMeasuredRect` (area) measure the container box, hosts pass explicit `height` to Chart (RendererChart honors height over aspectRatio), container div still carries CSS aspect-ratio so measured==derived for ALL pre-existing consumers (settled Q1 regressions prove neutrality: line 0.0000–0.0005% 05-51-55-182Z, profitloss ≤0.0051% 05-52-05-645Z, area PASS 05-54-27-236Z, loading line 0.0493% 05-54-40-131Z / area 0.0490% 05-54-42-392Z). Tick-source fix: XAxisOverlay now gets `data={xDomain ? visibleData : renderData}` (bklit shell:418/:511 feeds visiblePlotData — migrated showed 2 ticks vs bklit's 5 under brush). Harness fixes (LEAD, qa/+bench/): bench scenarios wrapped in `height:360` div (demo:28 parity — auto-height #chart-root collapsed bklit's flex-1 main slot to 0); qa/screenshot.mjs AND bench/run.mjs hover targeting switched from first-svg to LARGEST-AREA svg (migrated's 0x0 gradient-defs svg broke both — bench m3c had silently measured a no-op 0.149ms with tooltipAppeared=false on runs 05-56-41-908Z/05-58-16-269Z) + bench getSweepPoints gained a `brush` branch sweeping the main chart's line path. QA green board (run-id-cited): brush Q1 PASS + lead-eyeballed genuine (05-50-16-874Z: settled 0.0002%, left 0.0289% with 5 data-aligned ticks, right 0.2516%, hover 0.2131% with tooltip+axis pill, clear 0.1785%); cross-impl self-tests 0.0000% (05-54-48-074Z bklit, 05-54-54-113Z migrated); Q2 zero errors all charts incl. brush (pre-existing bar/composed/legendhover height-keyframe warnings stand, D208-8). Bench G1–G4 n=100 fixed-harness (06-04-24-933Z bklit / 06-05-59-288Z migrated): m1a 53.9 vs 59.6ms, settle 1120.7 vs 1152.7ms, heap 4.95 vs 5.00MB, idleScript 0.015 vs 0.237ms, m3a vsync tie 32.5/31.8, m3c perMove 1.224 vs 2.378ms tooltip TRUE both, 0 console errors; sole above-bklit metric: m1c task-time 158.2 vs 134.5ms with SCRIPT time at parity (73.8 vs 74.6ms) — layout cost of the two-chart composition, disclosed, not a gate metric. D227 ruling-4 ESCALATION VALVE NOT TRIPPED: m3d drag per-move 4.467 vs 4.608ms (0.97×, ceiling 8ms), worst frame 17.7ms BOTH impls, domainChangedEveryRun=true both (LIVE per-pointermove commit verified honest). Q3 boundary: 0 defineChart/definition/.spec refs across all 5 brush modules; showcase tsc clean. Rulings applied: D227 r4 live commit, r6 __qaSetBrush Q1 captures, r8 selectionPattern via pattern-preset reuse; tail-tick port (appendProjectionTailTicksOverlay) verified line-faithful (gate, maxExtraTicks, dedupe, domainEnd, sort). Accepted/disclosed: marks-level auxiliary x mappings (profit-loss, projection) not xDomain-branched — combinations unexercised in bklit too; brush-drag.ts keeps window listeners on unmount-mid-drag until pointerup (nit, no leak across drags); `xDomainSlotCount` accepted as pass-through parity prop; horizontal-only + dead `selection` prop narrowing stands (D227 r5); showcase brush demo deferred to init 12.
- **D229** (2026-08-20): Initiative 10 plan-loop-1 FINALIZED (`research/phase-3/plans/10-markers-chrome/plan-loop-1.md`) — Sonnet-drafted synthesis + 10 lead rulings (§10), key claims disk-verified (dead typed props types.ts:46-53; dash-tail LINEAR approximation is the shipped algorithm, binary-search `findPathLengthAtX` explicitly abandoned by bklit's own comment; scatter-hover-chrome.ts constants 0.5/2px/1.35 + 0.15s transition EXACTLY match bklit's SeriesMarkers dim spec — strong reuse precedent). AUDIT-STUB CORRECTIONS: terminal-marker claim stale (init 7 landed `ProjectionMarkerOverlay`); highlight *band* already fully migrated in hover-chrome.ts (matches `computeSegmentBounds` one-point-either-side formula) — genuinely missing scope is ChartMarkers/MarkerGroup fan-out annotations, SeriesMarkers point grid (closes D225 r3 legend-dim deferral), and the dash-tail overlay; LineConfig/AreaConfig already TYPE the four props (showMarkers/markers/dashFromIndex/dashArray) with zero reads — this loop makes them load-bearing. PLAN CITATION FIX (lead): badge tokens are NOT in registry.json (only 3 marker tokens there) — badge pair sourced from docs-app tokens (wiki/llms-full.txt:2504-2519), all 5 values pinned in §10. ARCHITECTURE: SeriesMarkers reuses scatter-chart.tsx's dot()-with-radial-gradient-ring mark (Scatter IS SeriesMarkers in bklit — same component); dim/active-highlight extends hover-chrome.ts's single writer (D225/D226 doctrine, no second writer); ChartMarkers = application-owned portal/sibling-svg overlay outside definition (terminal-marker idiom), post-overlay ordering; dash-tail = post-render DOM measurement (bklit's own approach), base stroke transparent with live-probe measurability check (ruling 9). RULINGS: drop forceOpen/isMuted, keep maxFanned (D227 narrowing precedent); tooltip suppression via onFocusGroupChange([]) else minimal clear() delegating to existing teardown; reveal formula = bklit fidelity, scatter NOT edited; `${dataKey}__marker` namespaced mark ids; selection-band branch report-only; MarkerTooltipContent conditional on existing tooltip seam else deferred to init 12; ChartMarkers Line/Area-only; showcase demo deferred to init 12; ONE new bench `markers` pair (showMarkers+dashFromIndex+ChartMarkers cluster+legend) full first-time G1–G4, existing line/area pairs D210-waived (props default off); qa gains `__qaSetMarkerFan` hook (LEAD). 3 dispatches: A dash-tail → B point grid+chrome → C ChartMarkers+tokens.
- **D230** (2026-08-20): Initiative 10 (ChartMarkers + SeriesMarkers + DashTail) CLOSED — approved. Dispatches A/B/C landed (executor); gate harness (bench `markers` scenario pair + qa markers captures incl. ruling-9 probe, legend-hover probes, fan-open capture) Sonnet-drafted under lead review (executors hard-blocked in qa/bench); bklit-markers vs migrated-markers scenario composition diff-verified identical. FOUR post-dispatch defects, all lead-ruled and fixed: (1) **React #185 livelock** in line/area `state=loading` (Q2 regression from Dispatch A) — `dash-tail.ts`'s layout effect runs every render (deps get fresh identity from the host's inline `series` prop) and its empty-`activeSeries` branch called `setMeasured(new Map())`; an identity-preserving updater STILL livelocked — instrumentation showed 53 updater invocations with prev.size=0: pending lanes defeat React's eager same-state bailout, so scheduling alone counts toward the nested-update limit. Lead fix: NO setState in the empty branch (render already returns null there; stale `measured` invisible). (2) **dash-tail overlay displaced +40/+40px** — overlay wrapped the copied pathD in `translate(margin.left,margin.top)`, but TanStack bakes margins directly into scale ranges (scene.ts:690-705; `.ts-chart__marks` carries no transform, path `d` starts "M40,…"), double-counting the margin. Sonnet fix (lead-reviewed): wrapper `<g>` removed, local linear xScale made margin-aware (`margin.left + fraction*innerWidth`), clip rect/tailWidth moved to absolute space; live verify: overlay path bbox === base path bbox exactly {64, 106.886, 720, 44.457}. (3) **ChartMarkers +39.997px x offset** — line-chart passed its absolute-range d3 scale to `ChartMarkersOverlay`, which adds marginLeft itself (bklit contract = inner-relative xScale); fixed at the call site by subtracting margin.left (area-chart's `areaXScaleD3Ref` was already inner-relative); post-fix drop-line deltas ≤0.013px vs bklit. (4) **fan-open cross-impl compare measured a harness gap** — frozen bklit MarkerGroup has no fan-forcing hook while migrated exposes `__qaSetMarkerFan`; lead qa/screenshot.mjs edit records the cross-impl fan-open stats informationally (`pass:true, informational:true`), determinism still hard-gated by self-test (0.0000%). GATES (run-id-cited): showcase tsc clean; Q2 console-errors ALL PASS ×17 targets incl. line/area loading post-#185 (only pre-existing D208-8 bar/composed/legendhover height warnings; executor a5baaad8); FINAL markers compare 2026-08-20T10-12-14-857Z (executor 0203eb3e): settled 0.3514%, hover-50 0.2643%, hover-70 0.4945%, legend-hover-0/1/clear ≤0.4951%, fan-open 0.3446% informational — 7/8 PASS; self-test 10-12-32-382Z ALL PASS ×8 (worst 0.0015%); ruling-9 probe: migrated transparent-stroke path found=true totalLength 760.2882 stable across runs, bklit found=false expected (selector is TanStack-DOM-specific); D213 eyeballs on hover-30/fan-open/legend-hover PNGs confirm visual parity (fan renders ⚠️ top + 🚀/🔧 arc + faded badge; legend residual diff is solely the lingering hover-70 tooltip, inherited by harness design since legend probes run after the hover sweep — deterministic per self-test). **hover-30 WAIVED at 0.5501% vs 0.5% gate**: flip-boundary sweep (Sonnet a85b7ba0; 0.5px steps over 0.28–0.32 fraction, both impls, identical SVG boxes {24,24,800,400}) shows inter-impl snap-flip mid-x deltas of 0.5/0.0/1.0/0.5/0.0px — never >1px and non-uniform, whereas a systematic floor-vs-round mismatch would be a constant ~3.6px (half of the 7.27px point spacing). Verdict: knife-edge sub-pixel floating-point discrepancy in the two nearest-point bisection implementations, not a fidelity defect; the 0.30-fraction pointer happens to land inside the lone 1.0px disagreement window (Jan 28→29 flip). BENCH first-time markers G1–G4 n=100 (bklit 09-52-47-428Z / migrated 09-54-08-758Z): migrated ≤ bklit on EVERY median — m1a mount 61.9 vs 63.4ms (0.98×), m1b settle 1131.9 vs 1164.7ms (0.97×), m1c script 75.6 vs 123.3ms (0.61×), m1c task 190.8 vs 247.3ms (0.77×), m2a idle script 2.32 vs 6.81ms (0.34×), idle task 41.7 vs 46.5ms (0.90×), m2b heap 5.24 vs 5.80MB (0.90×). Q3 boundary: 0 non-comment bklit/recharts refs across dash-tail.ts/chart-markers.tsx/series-marker-mark.ts; existing line/area QA pairs D210-waived as planned (markers props default off; Q2 reran green). Disclosed nits/deferrals: `void stroke` dead code + missing y-accessor finite-check (10-B); globals.css badge-token divergence sits outside migrated/ (10-C); MarkerTooltipContent + showcase markers/brush demos deferred to init 12; qa addInitScript precedent noted for future forced-state captures.
- **D231** (2026-08-20): Initiative 11 plan-loop-1 FINALIZED (`research/phase-3/plans/11-pattern-bars/plan-loop-1.md`) — cmd-executor bklit inventory (session d866c881, read-only) + Sonnet-drafted synthesis + 10 lead rulings (§10). KEY FINDINGS: (a) AUDIT-STUB CORRECTION — `internal/pattern-preset.tsx` already exists (init 3, reserved for 6/11) and is a confirmed exact 1:1 port of bklit's 8 pattern IDs (none/diagonal/horizontal/vertical/cross/dots/circles/accent; `accent` hardcodes `#e879f9` ignoring its color param — quirk preserved); bklit has NO pattern dedupe registry either (every consumer owns local `<defs>` + useId), so "one pattern resource owner" = the one shared render function migrated already has. (b) bklit's plain `bar.tsx` ITSELF applies BarDepth's front-face `topYTrim` (bar.tsx:359-381 imports bar-depth-geometry) — BarDepth cannot be a pure additive overlay; the base-bar mark must share the geometry module (ruling 4: conditional custom-mark swap, stock `barY()` untouched when props off → D210 off-state waiver preserved). (c) SceneArea spike RESOLVED IN-LOOP by lead (svg-renderer.ts:106-113 renders arbitrary closed polygons and raw path strings first-class) — BarDepth/BarPulse mark design unblocked, BarPulse silhouette may carry bklit's path string verbatim. (d) Composed exclusion is bklit-faithful, not a frozen-file compromise (bklit's ComposedChart never recognized these features). (e) Consumer greps: zero showcase/ usages for all six (genuinely net-new); BarPulse docs-only even in bklit (ruled full-sweep anyway). (f) Hover-reactivity confirmed for BarSquares (per-bar dim 0.3), BarColumnTrack (binary fade-to-0 on any hover), BarPulse (group dim) — three DISTINCT 0.15s timings preserved via the existing bar-hover-chrome single writer (ruling 2); BarDepthBack/Front reactivity re-verified at dispatch (ruling 9). Constants pinned verbatim: BAR_DEPTH_MAX_PX=7, PERSPECTIVE_RATIO=0.45, MIN_PX=0.5, groundShadow 0.26, glass 0.2, pulse 0.55/36px/2.4s/0.85, computeSquareColumn round-vs-fit formulas. 3 dispatches: A PatternArea (patternPreset prop + raw-fill escape hatch, D228-safe defs sibling) → B squares/track (+chrome, gradient-before-pattern defs ordering gated) → C depth/pulse (+conditional bar-chart swap); gate harness Sonnet-drafted under lead review.
- **D232** (2026-08-20): **Sankey hiDPI reveal artifact + thin links FIXED — CSS `vector-effect` override; wrong-clone lesson registered.** Symptoms (user screen-recording frames): dark "black rectangle" tails on the last third of links mid-reveal, and link strokes thinner than bklit's. Single root cause, proven via in-browser raster diagnostic (offscreen-canvas pixel coverage of synthetic test SVGs) on the user's dPR-2 display: TanStack's core renderer hardcodes `vector-effect="non-scaling-stroke"` on ALL `area`/`polyline` scene paths (svg-renderer.ts:104/112 in the resolved clone); with that attribute Chromium computes `stroke-dasharray`/`stroke-dashoffset` AND `stroke-width` in DEVICE px — the `pathLength="1"`-normalized reveal dash covers only 1/dPR of the path (unpainted tail) and data-encoding link widths paint at half size. FIX (committed showcase code only): `.ts-sankey__link { vector-effect: none; }` in `showcase/migrated/charts/styles.css` — CSS beats presentation attributes (verified live: computed value flips to `none`, `CSS.supports` true); inert `vectorEffect` scene-style entry removed from `sankey-mark.ts`; `sankey-animation.ts` invariant-1 comment updated to name the CSS rule as the override owner. User-verified visually in Chrome; check-types/lint failures unchanged pre-existing baseline (children.tsx TS2741, 14 lint problems in non-sankey files). PROCESS DEFECT (the reason a prior "fix" no-oped): a first-round core patch (optional `SceneStyle.vectorEffect` + renderer passthrough) was applied to the ROOT clone `repos/tanstack-charts` — but `tsc --traceResolution` + next.config.mjs/tsconfig aliases prove the showcase resolves `@tanstack/charts` from `showcase/repos/tanstack-charts` (separate, NEWER upstream clone, Aug 7 `b869067`; root clone Jul 30 `4b940ed` is bench-only), and BOTH clones are gitignored + Vercel re-clones fresh via `clone-repos.sh` — core-clone edits can never deploy. Root-clone core patch reverted to pristine upstream. RULE: fixes for showcase behavior live in committed `showcase/` code (CSS/className, the TanStack-native styling hook, preferred over onRender DOM stamps); never patch the clones. Three read-only cmd-executor audits dispatched to sweep the repo for the same three defect classes (other dash/data-width marks on forced-nss paths; dirty clone edits; QA harness dPR-2/mid-reveal coverage) — findings to be reviewed and logged in a follow-up entry.
- **D233** (2026-08-20): Three read-only cmd-executor audits (promised in D232) completed; sessions 01fb38d9 (hiDPI hazards), bb5c744f (clone divergence), baa55c8f (QA dPR coverage). Audit 1: one real unmitigated hazard — `projection-line-mark.ts` polyline with `strokeDasharray`, no CSS override; used 6× across line/area/composed; FIXED this entry via `styles.css` `.chart-projection-line path { vector-effect: none; }` rule (same recipe as sankey D232) + source comment. Latent: `grid-highlight-mark.ts:53` accepts caller dash (default `"0"`, safe today). All other candidates cleared (plain-SVG overlays, rect scene nodes, constant stroke tokens). Audit 2: all 4 gitignored clones clean — no stray working-tree edits. CORRECTION to D232: bench app also resolves `@tanstack/*` from `showcase/repos/tanstack-charts` (D146 vendored v0.7.2 fixture, per `bench/app/vite.config.ts` comment); root `repos/tanstack-charts` (`4b940ed`, 226 commits older) is intentionally unused for `@tanstack` resolution — wrong-clone trap only; candidate for retirement. Executor ran a `git fetch` in the showcase clone (history unshallowed, working tree untouched). Audit 3: `qa/screenshot.mjs` captures only settled states at `deviceScaleFactor` 1 (line 52) — no dPR-2 or mid-reveal coverage, so the sankey bug class was invisible to Q1; recommendation logged (add `--dpr 2` pass + mid-reveal frame check) — deferred, not implemented.
- **D234** (2026-08-20): **Line/Area hover-sync + dot-flicker arc FIXED — three root causes, all pattern-level.** User symptoms: hovered dot rendered at the top of the plot after data re-anchor; only one series surfaced on multi-series hover; dot "jumps" to the next point ahead of the crosshair/highlight and its coloring flickers back to a previous state mid-transition. Root causes + fixes: **(1) reanchor y:0 stub** — `resolvePoints` passed to the shared reanchor path returned placeholder y; now maps the real per-series y from renderData. **(2) lineY series identity** — without a `z` channel every lineY point carries `group=null`, and `focusX`'s `groupPoints` dedupes by group key so a group-x focus collapses to ONE point; fix: `z: () => dataKey` on each lineY (line + area). RULE: any multi-series mark that must participate in group-x focus needs a distinct z/group identity. **(3a) spring.ts frame-quota integration** — the integrator capped per-frame dt at `MAX_FRAME_MS=64`, so below ~15fps every hover spring ran in slow motion while framer's springs are clock-true (`JSAnimation.updateTime` samples the generator at `timestamp − startTime`; frameloop's 40ms cap bounds only the state delta, not the animation clock). Fix: integrate true elapsed time, `MAX_STALL_MS=10_000` bounds only pathological stalls. RULE: imperative animation ports must be clock-time-based, never per-frame-quota — divergence is invisible at 60fps and appears only under jank. **(3b) TanStack default focus layer** — the resolved clone's `scene.ts:395-427` injects `ts-chart__focus-layer--default`: one hidden dot per data point (r=5, `--ts-chart-focus-fill` canvas fill, series-color stroke, sw 2.5) with the focused dot revealed INSTANTLY via `focus:{match:'primary'}`. That native dot snapping to the next point (in "inverted" canvas-fill styling) under the gliding chrome spring dot was BOTH remaining symptoms. The old CSS kill-switch (`[data-bkm-chart] [data-ts-chart-focus]`, styles.css:183) targets the PREVIOUS single-circle implementation only — upstream moved the ring into per-point scene nodes and the selector silently stopped covering it. Fix: `focusRing: false` in every active-focus chart definition (line, area x2, composed, bar x2, candlestick, scatter) — definition-level, TanStack-native. RULE: suppress native chrome via definition options, not CSS selectors — upstream class names/DOM shapes drift and CSS kill-switches rot silently. QA lesson (extends D233 audit-3): settled-hover Q1 screenshots missed the ring because the chrome dot covers it at rest — a transition-frame, full-DOM element trace (no class filter) caught it in one run; earlier traces filtered to `bkm-hover-layer` svgs and were blind by construction. Verified: line Q1 hover-30/50 improved 0.0013/0.1509% → 0.0000% (ring pixels were leaking even at rest), hover-70 0.1494% PASS; area/bar/candlestick/scatter/composed Q1 all PASS; migrated frame-trace shows zero focus-fill circles, chrome dot + crosshair + highlight band on matched spring trajectories at 60fps and under 12x CPU throttle; showcase tsc clean, eslint pre-existing warnings only.
- **D235** (2026-08-20): Initiative 11 (PatternArea + BarSquares + BarDepth/Pulse) GATED — APPROVED. Gate campaign: harness (Sonnet-drafted, lead-reconciled) `bench/app/src/scenarios/{bklit,migrated}-{patternarea,barsquares,bardepth}.tsx` + `qa` chart wiring; gate sessions `4cf618ba` (v1), `fdde6a10` (diagnosis), `ee8d2c17` (v2), `b2ffde01` (v3), `e99af47e` (v4), `c7309681` (v5), `cfe45192` (v6); bardepth fidelity port by Sonnet subagent (lead-reviewed).
  - **Harness gap (v1)**: all 12 v1 runs failed `pageerror: Missing/invalid ?chart=` — `bench/app/src/bench/query.ts` ChartKind union + parseBenchParams whitelist was the single enumeration site missed; Sonnet fix appended the three kinds; no other gate sites (run.mjs getSweepPoints wired; CHARTS array is --all default only).
  - **Bar settle regression (v1, FIXED)**: fresh dist compiled uncommitted dispatch-B/C bar-chart.tsx for the first time; TanStack `RendererChart.tsx:177-187` double-fires `onRender` in the same mount commit, and the second call hit the `bkmRevealed==="1"` / `revealedForDataRef` guards → `setPhase("ready")` ~10ms after mount, resolving the settle contract mid-reveal (QA bar settled 8.8061% vs prior 0.0000% on stale dist). Lead fix bar-chart.tsx ~735-748: reveal-scheduled paths `return` without setPhase — "ready" owned solely by the reveal deadline; `!marksGroup || animationDuration<=0` keeps immediate ready (no deadline armed on those paths). Verified: bar off-state settled 0.0000% PASS (`qa/results/bar/2026-08-20T12-45-56-119Z`, re-verified post-keyframe-fix `15-54-53-471Z` and post-depth-fidelity-fix `16-36-17-324Z`, both 0.0000%). The 440ms left-to-right stagger on plain bars is bklit-faithful (bklit bar.tsx:215-217 staggerSpread=duration*0.4; ready via setTimeout(duration+stagger) bar-chart.tsx:381-385 — mirrored by migrated's deadline). LATENT (note only): same guard pattern at scatter-chart.tsx:498, candlestick-chart.tsx:712, composed-chart.tsx:1134, heatmap-components.tsx:549 — unchanged code, gates green, scatter/composed frozen.
  - **Patternarea pattern phase-shift (v2 FAIL → FIXED)**: v2 compare `qa/results/patternarea/2026-08-20T12-43-07-586Z` failed 5/8 presets (diagonal 12.16, horizontal 18.39, vertical 18.37, circles 10.63, accent 12.11; none/cross/dots PASS). Root cause: bklit paints marks inside `<g transform=translate(margin)>` so `userSpaceOnUse` tiles anchor at (40,40); TanStack bakes margins into path coords → tiles anchor at (0,0); pass/fail split matches tile arithmetic exactly (tile-6 presets fail (40%6=4), tile-8 cross + tile-10 dots pass (40%8=40%10=0); DEFAULT_MARGIN=40). visx PatternLines/PatternCircles accept no patternTransform (Lines.js destructures fixed props). Lead fix area-chart.tsx: preset def rendered as `${id}-base` + thin `<pattern id href="#…-base" patternTransform="translate(margin.left margin.top)"/>`; 1:1 pattern-preset port and visx untouched; bar-squares defs untouched (they pass). Verified: compare `qa/results/patternarea/2026-08-20T15-42-27-858Z` ALL PASS (diagonal 0.0058, horizontal 0.0050, vertical 0.0049, cross 0.0051, dots 0.0049, circles 0.0046, accent 0.0047, none 0.0143, settled 0.0001, worst hover-70 0.0530); self-test `15-42-41-077Z` ALL PASS (worst 0.0522). Post-fix bench migrated `bench/results/2026-08-20T15-41-40-419Z`: medians consistent with pre-fix pair (m1a 38.9, settle 1142.7, heap 4.77MB, m3a 128.3 — runs 5–8 polluted by concurrent lead tsc/python work, medians robust). Uncontested clean re-run `2026-08-20T16-16-48-341Z` (v5, nothing concurrent): m1a 36.6, settle 1140.2, m3a 32.4 (p95 33.3), m3c 16.7 (p95 17.5), perMove 0.679, heap 4.73MB, 0 console errors — confirms every 12-32-36-012Z median; m3a/m3c are vsync ties. D213 eyeball: pattern-horizontal a/b identical post-fix.
  - **Bardepth crashes (v2 skip → two defects, both FIXED)**: (1) scenario: `BarDepthProvider` is an extracted-config child stub (children.tsx:79-81 renders null, drops children); bench twin used it as a WRAPPER → lead fix migrated-bardepth.tsx moved Provider inside `<BarChart>`. (2) chart (found by v4 read-only diagnosis, session `e99af47e`): `internal/bar-trimmed-mark.ts` was written against charts-core `resolveGroupScale`'s RESOLVED wrapper interface (`{bandwidth: number, map()}`, tanstack bar.ts:467-503) but is handed the RAW d3 `scaleBand` (callable, no `.map`) — the `as unknown as` cast at bar-chart.tsx:521 hid it from tsc; and since bardepth's sole series is trimmed, no `barY` ever ranged the scale either. Render threw `pageerror: r.map is not a function` inside prerender → zero SVG commits → `__benchPaintDone` never set → bench skip (`waitForFunction: Timeout 30000ms`, run.mjs:358) + QA `Promise.all` compare timeout at captureLoad:230 (qa harness confirmed CORRECT — waits only on paintDone/settled; bklit settles fine, repro `2026-08-20T16-00-06-813Z`). Lead fix bar-trimmed-mark.ts: option typed `ScaleBand<string>`; render ranges a `copy()` to the render-time category bandwidth (mirroring resolveGroupScale, no shared-instance mutation) and reads `gs(id)`/`gs.bandwidth()`; cast at the call site removed; showcase tsc clean. Crash-fix verified v5 (session `c7309681`): console-errors `PASS bardepth` 0 errors/0 warnings (`r.map` gone); bench migrated `2026-08-20T16-15-27-678Z` 0 skips (m1a 53.6, settle 1589.4, script 85.9, heap 4.46MB, m3a 31, m3c 16.7, perMove 0.913 — beats/ties bklit `12-42-22-431Z` on every axis); QA self-test `16-17-11-090Z` ALL PASS (settled 0.0000%). (3) fidelity (v5 compare `16-16-59-377Z`): settled vs bklit FAIL 24.9418% — migrated depth branch rendered flat pills: `barDepthFrontMark` full-height white 0.2 wash (bklit glass is a tip treatment), trimmed bar kept all-corner radius (bklit perspective tops are flat), Back side/lid faces not visible, front-face shading absent; hover-30/50/70 + depth-off/on captures PASS only because bklit's hover dim flattens both impls (depth toggles used method `bar-depth-toggle-skip`), so settled is the sole depth-sensitive capture. Fidelity port (Sonnet agent, lead-reviewed diff scope + D213 eyeball) against bklit `bar-depth.tsx` ground truth: bar-depth-marks.ts — `buildPosBarStops`/`buildNegBarStops` glass stop-lists ported verbatim (tip white 0.2 → fading white → black ground-shadow tail); Back side face = 3 stacked passes (solid fill → directional black sideShade gradient → glassPos) and lid = 3 passes (fill → flat white tip @GLASS_TIP_OPACITY → topShade), keys suffixed (`:side:N:shade` etc.) so frozen bar-hover-chrome's `/:(side|lid):(\d+)/` still dims every pass; Front = per-bar objectBoundingBox `url(#glassPos)` rect replacing the flat white wash. bar-chart.tsx depth branch — 5 useId-scoped `<linearGradient>` defs (glassPos/glassNeg/sideShadeRtl/sideShadeLtr/topShade) driven by `BarDepthProvider.groundShadow ?? 0.26`, threaded via `gradientIds`; trimmed bar `radius: 0` mirroring bklit bar.tsx:256-259 `if (perspective) return 0`. glassNeg/negative-value support added structurally but unused (positive-only guards unchanged, dataset positive-only). Fix iterations: compare `16-34-27-971Z` settled PASS 0.0030% but hover/depth-on FAIL ~0.92-0.95% (overlay key regression broke hover dim) → key fix → compare `2026-08-20T16-35-54-502Z` ALL PASS (settled 0.0030, hover-30/50/70 0.0005, depth-off 0.0656, depth-on 0.0234); self-test `16-36-06-382Z` ALL PASS 0.0000% all 6 captures; plain-bar regression compare `qa/results/bar/2026-08-20T16-36-17-324Z` ALL PASS 0.0000%; showcase tsc clean. D213 lead eyeball (settled a/b, run 16-35-54-502Z): identical dark beveled columns, glass shading + tip highlights. Post-fix bench = v6 `16-42-19-369Z` (G-table below).
  - **BarSquares WAAPI keyframes (FIXED + VERIFIED)**: dispatch-B cascade used unitless `height` keyframe values → 11126 Chromium `Invalid keyframe value for property height` warnings (informational; same class as pre-existing D208-8 bar/composed/legendhover) and the height track dropped (squares slid without growing mid-reveal; settled end-state unaffected). Lead fix bar-chart.tsx: `px` units on all four height-keyframe sites + hoisted the per-rect `[...byX.keys()].sort()` out of the cascade loop. Verified v4: console-errors barsquares 0 warnings (composed 200 / legendhover 100 remain — pre-existing, initiative-12 candidates); bench m3a 314→202ms, m3c_frame 65.8→34.3ms.
  - **Q1/Q2**: patternarea self-test `12-43-19-205Z` ALL PASS (worst 0.0022%); barsquares v4 post-fix: compare `15-53-46-991Z` ALL PASS (settled 0.0046%, worst hover-70 0.1745%) + self-test `15-54-44-696Z` ALL PASS (settled 0.0000%, worst 0.0592%); bar off-state compare `15-54-53-471Z` ALL PASS (settled 0.0000%). bardepth v6 post-fidelity-fix: compare `16-35-54-502Z` ALL PASS + self-test `16-36-06-382Z` ALL PASS 0.0000% (details in bardepth bullet). console-errors: v2 19/19 PASS; v4 all targets PASS except bardepth (`r.map` crash, fixed above); v5 all-PASS incl. `PASS bardepth` 0 warnings; v6 post-fidelity-fix all 21 targets PASS, bardepth + barsquares 0 warnings (composed 200 / legendhover 100 unchanged). v6 barsquares regression compare `qa/results/barsquares/2026-08-20T16-42-58-029Z` ALL PASS (settled 0.0046%, worst hover-70 0.1543%) — bar-chart.tsx depth-branch edits caused no collateral. D213 lead eyeballs: bar settled, patternarea diagonal/cross + post-fix pattern-horizontal, barsquares settled/hover-50/legend-hover-0 (bklit and migrated identical; at n=100 squares render ~2px — bklit twin's own density scaling), bardepth settled a/b post-fidelity-fix (identical beveled columns).
  - **G1–G4** (bench n=100; B = same-day bklit twin; G2 waived all three — no TanStack-native equivalent for custom bklit feature marks, per research/phase-1/05 custom-marks clause):
    - patternarea (bklit `12-31-15-162Z` / migrated `12-32-36-012Z`, clean v5 confirm `16-16-48-341Z`): settle 1.00×, m3a 32.4 vs 31.8 + m3c 16.7 vs 16.7 vsync ties (D12/D207 class), heap 1.00×, idleScript 0.30×, perMoveScript 0.62×. m1a 36.1ms (clean confirm 36.6) vs 8.9ms — WAIVED (D200+ class): bklit area's first paint is its loading-state shell, not chart content; absolute delta ~27ms one-time; settle parity 1.00× is the meaningful mount signal. G3 PASS, G4 PASS.
    - barsquares (bklit `12-35-17-096Z` / migrated post-fix `15-51-27-421Z`): settle 1718.6 vs 2277.2 (0.75×), script 304.0 vs 1615.3 (0.19×), heap 7.74MB vs 121.7MB (0.064×), idle ~0×, perMove 22.3 vs 52.2 (0.43×), m3c_frame 34.3 vs 50.1 (0.68× — beats bklit; was 1.31× pre-keyframe-fix). Residual: m1a 460.9 vs 375.7 (1.23×), m3a 202.4 vs 134.2 (1.51×, was 2.34×) — WAIVER (D200+ class): a bar-squares chart at n=100 paints thousands of per-square rects; the migrated mount/update pays a bounded absolute premium (m1a +85ms, m3a +68ms median) while beating bklit on every sustained-cost axis (script 0.19×, heap 15.7× smaller, settle 0.75×, hover perMove 0.43×, frame time 0.68×) with zero console warnings; G2 already waived for custom feature marks. G3 PASS, G4 PASS.
    - bardepth (bklit `12-42-22-431Z` / migrated post-fidelity-fix `2026-08-20T16-42-19-369Z`): m1a 42.3 vs 71.9 (0.59×), settle 1579.5 vs 1614.5 (0.98×), script 69.7 vs 229.3 (0.30×), heap 4.59MB vs 7.45MB (0.62×), idleScript ~0.02ms, m3a 31.3 vs 31.9 (0.98×) + m3c 16.7 vs 16.7 — vsync ties (D12/D207 class), perMove 1.22 vs 4.62 (0.26×), 0 console errors, 0 skips. G1 PASS (m1a well past the 20% bar; m3a/m3c vsync-tie precedent; no >5% regression), G3 PASS, G4 PASS. (v5 crash-fix reference run `16-15-27-678Z` superseded — pre-fidelity-chrome.)
  - PROGRESS.md row 11 → approved. [Initiative-12 candidates recorded: D208 register dispositions, tooltip-chrome.ts.bak, `void squareSizeForMarks` dead code, pre-existing D208-8 unitless-keyframe warnings in bar/composed/legendhover.]

- **D236 — Initiative 12 plan-loop-1 FINAL + scope rulings + composed keyframe fix (lead)**: `research/phase-3/plans/12-showcase-polish/plan-loop-1.md` written (Sonnet draft on executor inventory session `a1ceeb99`, lead rulings §10). Scope frozen: build green (no ignoreBuildErrors/ignoreDuringBuilds), tsc+lint gates, 8 demos (wire orphaned reference-area/segment/projection + net-new brush/markers/patternarea/barsquares/bardepth), D208 dispositions, debt cleanup (dead `squareSizeForMarks` bar-chart.tsx:449-450; tooltip-chrome.ts.bak deferral CLOSED — file already absent on disk per inventory), responsive sidebar (`hidden md:block` + `md:ml-64` minimal treatment; mobile drawer out of scope). Stale-audit corrections recorded: next.config.mjs has NO ignoreBuildErrors/ignoreDuringBuilds; no genuine `any` in candlestick/sunburst (`fade-mask.ts:28` is a boolean prop named `any`); decimation fully implemented (bar/candlestick skip is bklit parity) — residual is D218's y-domain-tween completion-signal snap only. Build root cause: prebuild `pnpm install` under NODE_ENV=production skips devDeps (typescript/@tailwindcss/postcss) — fix `--prod=false` first, NODE_ENV override fallback; `@/components/chart-detail` module-not-found treated as downstream cascade, re-verified post-fix. RULINGS: (Q1) composed-chart.tsx:1172-1173 px-suffix keyframe fix AUTHORIZED as behavior-neutral D208-8-class edit mirroring the proven bar-chart.tsx pattern — applied by lead this entry (height values only, `y` unitless as in bar fix); freeze otherwise stands; mandatory verification = Q1 composed compare + console-errors composed AND legendhover at 0 warnings. (Q3/Q4) dual-column demos achievable ×8 — lead verified `@showcase/bklit-charts` re-exports ReferenceArea:468, Segment*:514-522, ProjectionLine:429-435, ChartBrush/Layout:79-97, ChartMarkers:354, PatternArea:362, BarSquares:61, BarDepth*:46-56 from repos/bklit-ui charts index. (Q5/Q7) sankey + liveline: probe-then-disposition (fresh bench runs; unchanged → waive, grown → escalate). (Q6) D208-5 heatmap WAIVED (gate-edge stability ≥3 initiatives, D208-10 class). (Q8) n=1000 baseline table DEFERRED as process item, out of init-12 scope. (Q10) profitloss/legendhover routes confirmed out of scope (no deferral quote exists). D208 disposition targets this initiative: -2/-3 FIX (pie/ring hover hit-test, Sonnet investigation), -8 fixed (bar prior + composed this entry), -5/-10 waived, -6/-7 probe-then-rule, -9 deferred to future composed enter-animation audit (frozen-file internals beyond the narrow keyframe authorization). Gates per plan §8: headline tsc+lint+build; Q2 all routes incl. new demos; Q1 narrow (pie/ring/composed if fixes land); G1–G4 D210-class waiver default, narrow same-day spot-check only if WP4 lands. PROGRESS row 12 plan → complete (D236).
- **D237** (2026-08-20): **Initiative 12 CLOSED — APPROVED. PHASE 3 COMPLETE (12/12).** All D208 register items now carry final dispositions: **-1 FIXED** (D209, bar Q1 0.0000%); **-2 FIXED** (pie, D236 dispatch); **-3 WAIVED — ring hover-30/70, full diagnostic chain below**; **-4 WAIVED** (D208); **-5 WAIVED** (D236 Q6); **-6 ACCEPTED** (liveline same-day twins: migrated m1c 651.7ms `17-57-21-405Z` vs bklit 457.2ms `2026-08-20T18-16-51-675Z` = 1.43× ≈ registered 1.38× (575.5/415.9) — both absolutes grew ~10–13% together = machine noise, ratio stable; migrated idle FAR better: idleScript 8.86 vs 212.3ms (24×), idleTask 246.6 vs 460.6ms); **-7 FIXED** (sankey idleScript 0.027 vs 1.83ms, `17-58-45-460Z`); **-8 FIXED** (console-errors EXIT:0, 21/21 targets clean); **-9 DEFERRED** (composed enter-animation audit, frozen-file internals — D236); **-10 WAIVED** (D236/D221). **RING D208-3 WAIVER (the closing ruling)** — evidence chain (all executor-run, sessions 805954d6/d18af24b/d362d751/11464a5b/f15e2015/6e03e855/b00a1f0a): (1) diff STABLE across capture waits 700/1500/3000ms at fractions 0.3/0.7 (~1.6–1.7%) — not a mid-animation sampling artifact; (2) both impls use byte-identical radius arithmetic (`getRingRadii` verbatim match; n=100 in 280px svg → 0.85px bands at radius 4.27+1.28i) and identical svgBoxes {24,24,280,280} → identical hover point (108,164) = radius 56 = data index 40's band in BOTH; (3) `elementsFromPoint` at rest shows the identical single-band hit stack in both (one path, bbox 113×113; migrated's tagged `ring-40-track`); (4) **migrated hovers the geometrically correct ring** ("Ring 41" = data[40] = 143/296) **while bklit displays "Ring 98"** = data[97] = 180/583 (matches QA capture), whose band lies at radius ~129 — NOT the band under the cursor; during bklit's hover a 259×259-bbox path (data[97]'s full-circle track, outer ⌀258.7) tops the hit stack at a point deep inside its geometric hole — bklit's hover-target selection at this density is a hit-test artifact of its own implementation, and that same parked fill is what makes bklit's hover "hold"; (5) migrated instead exhibits scale-induced boundary-event flapping (1.03 hover scale moves the 0.85px band off the stationary cursor → leave → unhover → band returns → re-enter; center oscillates "Ring 41"↔"Total", non-deterministic settle) — reproduced with BOTH `pointerenter` and `mouseenter` listener variants (the mouseenter experiment was reverted; tree unchanged), so no listener-level fix exists; (6) matching bklit's pixels would require reproducing its incorrect hover-target mapping (showing data[97] while the cursor sits on data[40]'s band) — parity-with-a-bug, declined per the D230-class principle; n=100 is a QA stress density (showcase densities have thick, stable bands). Ring final run `2026-08-20T22-33-33-392Z`: settled PASS 0.0675%, hover-50 PASS 0.2025%, hover-30 1.7259%/hover-70 1.7087% WAIVED per above. The D236/WP4 transformOrigin fix stands (geometrically correct — TanStack `polar()` wraps marks in a center-translated group, polar.ts:204-208). **GATE BOARD (run-id-cited, session 77aacee1 + lead)**: QA composed `2026-08-20T17-54-08-625Z` PASS, area `17-54-28-135Z` PASS, bar `17-54-36-669Z` PASS (0.0000–0.0383%), markers `17-54-20-585Z` PASS except hover-30 0.5489% = D230-waived knife-edge class; console-errors 21/21 zero errors; showcase routes 9×200; showcase tsc 0 / lint 0 / build 0; bench G1–G4 D210-class waiver per D236 (no render-path change beyond fixes already gated), liveline/sankey spot-checks above. **Q3 boundary (session 2c66e3e9)**: zero `motion`/`framer-motion`/`react-spring` imports across showcase/migrated/charts (`@visx/zoom` choropleth + `@visx/pattern` bridge = pre-existing sanctioned adaptations, init ≤3; every `bklit-ui` hit is a comment citation, no imports); React state confined to the two sanctioned islands (center-stat NumberFlow readiness gate, ring-chart `centerVisible`), ring-hover-chrome.ts zero hits; no /tmp, no console.log. Cleanup: temporary lead-authored diagnostics `qa/probe-ring-hover-settle.mjs` + `qa/probe-ring-center-state.mjs` and executor capture dir `qa/.probe_tmp/` deleted (lead; executors may not delete). PROGRESS row 12 → **approved (D237)** — all 12 initiatives approved; Phase 3 complete. Uncommitted per standing directive (user decides commit).
