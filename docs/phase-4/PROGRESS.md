# Migration Progress Tracker — Phase 4

> **Phases 1–3 frozen** — see `docs/phase-1/PROGRESS.md`, `docs/phase-2/PROGRESS.md`, `docs/phase-3/PROGRESS.md`. This file tracks **Phase 4** only (PLAN-phase-4.md).

Goal: refactor out bklit/custom code replaceable by TanStack-native functions, remove redundancies/unnecessary complexity, centralize duplicated logic, review component API parity with legacy — while preserving exact design/interactivity and the public component API.

Source of truth once implementation starts: `research/phase-4/go-to-plan.md` (staleness rule 4.4.4). Log decisions in `docs/phase-4/LOG.md` (D-number convention, continuing from phase 3).

## Phase status

| Step | Description | Status |
|---|---|---|
| 4.1.1 | Placeholders (docs/phase-4) | complete |
| 4.1.2 | Taxonomy (`research/phase-4/taxonomy.md`) | complete (import-graph verified 2026-08-21: 17 corrections, 11 orphans flagged) |
| 4.1.3 | Standardized report layout (`research/phase-4/report-layout.md`) | complete |
| 4.1.4.1 | Per-part reports (`research/phase-4/<part>.md`) | complete — all 25 reports written (2026-08-21) |
| 4.1.4.2 | Usage matrix (`research/phase-4/usage-matrix.md`) | complete (redone with shareability semantics) — 122 modules: 49 SHARED / 37 SHARED (shareable) / 36 UNIQUE, 9 ORPHAN flagged (2026-08-21) |
| 4.1.4.3 | TanStack-native candidate verification (per-part `TS-check` notes) | complete — all 25 reports verified, ~100+ contradictions logged in Notes (2026-08-21) |
| 4.2.1.1 | Synthesis: parity | complete — 235 gaps (83 missing/32 renamed/21 extra/99 behavior; 8 high/108 med/119 low), 33 D-refs, 14 open questions; reconstructed from recovered run logs after repeated API failures (2026-08-22) |
| 4.2.1.2 | Synthesis: centralize | complete — 15 opportunities in 10 families (~24 modules, ~12 parts), 8 open questions (2026-08-21) |
| 4.2.1.3 | Synthesis: redundancies | complete — 44 findings (~700–750 LOC deletable + ~470 LOC inlineable, 122→~109 modules), 6 open questions (2026-08-21) |
| 4.2.1.4 | Synthesis: tanstack-native | complete — 44 native/partial opportunities (19 native/24 partial, 18 from TS-check corrections) + 25-row confirmed-no-native table, 12 open questions (2026-08-21) |
| 4.3.1 | Go-to-plan | complete — `research/phase-4/go-to-plan.md`: 10 doctrine rulings, all 40 synthesis OQs resolved, ~60 tasks in 8 batches (0/W0–W6); 7/7 pre-plan verifications confirmed (FD12 = 276 missing barrel symbols, worksheet preserved) (2026-08-22) |
| 4.3.2 | Batch ordering (A/B/C/D) | complete — 7 waves / 33 packages appended to go-to-plan.md (dependency graph, splits/merges/moves logged); coverage audit: 4 orphans + 2 contradictions found and ruled (2026-08-22) |
| 4.4.1 | Baseline (bench + typecheck/build + visual reference) | **DONE** (2026-08-22, HEAD `7d60a7f`, per D246/D240): typecheck PASS, build PASS, 24-part visual refs 28/31 runs PASS — pie / choropleth / heatmap hover FAILs are deterministic pre-existing drift (T-02b confined to sankey), attention on scatter/candlestick near-gate margins, CE 21/21 clean, **bench 24/24 cells 0 skipped** (`bench/results/2026-08-22T15-46-19-823Z`, post-D240 bench-harness tooltip-input fix) — all in `docs/phase-4/BENCHMARKS.md`. **Baseline fully closed — Wave 1 unblocked.** |
| 4.4.2 | Implement batches | **Wave 1 DONE** (15/15 packages, settled tree typecheck exit 0, D247); **Wave 2 DONE** — all 5 packages landed *and gated* (P2.1/P2.2 D252, P2.3/P2.5 D253, P2.4 D254, P2.5b), lead-verified full-tree `tsc --noEmit` **exit 0** (**gate CLOSED D257**, session `b8405916`: 16 chart-densities / 14 PASS, console **21/21 0 err 0 warn**, 10 bench cells with both bklit anchors reproducing the frozen 4.4.1 medians, `state=loading` sweep run in full). The 2 FAILs (ring, markers) are both **pre-existing, not Wave 2 regressions** (D256). Waves 3–6 pending |
| 4.4.3 | Per-batch gates | **Wave 1 gate swept** (D248): 12/17 chart-densities PASS, console 21/21 + 5/5 ad-hoc clean, bench compares sane (bar parity clean; composed flagged M3a ~2.6× anchor, no baseline existed). 3 findings → fix dispatches F1 (sunburst regression) / F2 (choropleth CP7) / F3 (heatmap HM16). **All three fix dispatches CLOSED, all gates green** — F2/F3 (D250), F1 (D251). 2 classified chronic; `pie` assigned to new P2.4 (D249). **Wave 2 gate swept and CLOSED** (D257, session `b8405916`, D246 split worked as designed — zero QA contention, all 4 steps run, nothing skipped): 14/16 PASS, console 21/21, bench line+bar clean with anchors validating the machine. Both FAILs classified **pre-existing** off the full 48-run/12-run QA histories, not off this wave's runs (D256): **ring** → dispatched as **F4** (diagnosis-first; bimodal hover, predates every wave, corroborates D255's audit prediction independently); **markers** n=100 hover-30 byte-identical across the wave boundary → chronic, not dispatched. 3 new findings logged for ruling, none charged to Wave 2: scatter M3a 34.3→65.5 (→P4.2), **the `state=loading` pixel gate is near-blind** (510px/0.0531% PASS on visibly divergent renders — D257 §4), funnel-at-n=1000 console hole (symmetric, ~1000 errors both sides). **F4 CLOSED by the lead** (D258 — executor backend down, so run in-session; read-only, nothing written to `qa/`): verdict **(b) migrated-only**, which **overturns D255's ACCEPT** — at the mandated n=1000 bklit shows churn `at=0` and a byte-identical settled state across reps while migrated never settles (38 distinct transform signatures still cycling in the final second; reps freeze at different spring positions), fully explaining D256's bimodality. D255's structural claim was right but its inference was wrong: legacy does **not** oscillate at n=4/20/100/1000. Ring reclassified as a genuine defect → **P4.3**, carrying two extra findings (a static n=4 emphasis gap the prescribed wrapper fix does not address; migrated's wiring stamps being wiped by TanStack) and one ruled-out direction (pivot semantics — both impls pivot identically). Stale `qa/screenshot.mjs` header comment that caused a wrong-impl measurement **fixed**. **Console gate widened 21 → 30 jobs and re-closed (D261)**: the fixed target list was entirely cartesian, so the whole polar/flow family (pie/ring/radar/gauge/gaugelinear/funnel/funnelvertical/sankey/choropleth) had never been console-swept at any density. **28/30 clean, 0 errors 0 warnings** — including ring at n=1000 despite D258's oscillation, confirming the loop is a hit-testing artifact a console gate could never catch. The 2 failures are both the negative-viewBox class and **proven symmetric by measurement** (both impls × both charts × n=100/1000: bklit 1017 / migrated 1000 at n=1000 with **byte-identical** error strings, both clean at n=100) — `funnelvertical` is a **new find**, D257 only ever caught the horizontal one. Both allowlisted with per-entry bklit evidence. Also caught: my own allowlist regex never matched Chrome's actual message text, which was visible in run 1 **only because excused errors are still counted and printed** — the deliberate anti-silent-allowlist rule paid for itself immediately. **Wave 3 mid-wave sweep GREEN (D284)** — run exclusively after P3.12 landed: `TSC_EXIT=0`, `BUILD_EXIT=0`, **10/11 chart-densities PASS**. Chart list chosen by *mechanism* (the six `internal/spring.ts` consumers: ring 1000, pie 1000, funnel 1000, heatmap 52, liveline 100, candlestick 1000) plus Wave-1–3 regression cover (sunburst 27+33, sankey 33, line 1000, area 1000), and every chart captured at settled **+ hover-30/50/70** because P3.12's deliverable is a motion curve that a settled frame cannot see (the D279 blind spot, anticipated this time). Two reproductions validate the machine (D265 §5): heatmap `0.4277%`/`0.3466%` matches D250's F3 closing gate to four decimals, sunburst `0.0599%` matches D281 at both densities. Sole FAIL is **ring** (`hover-30 0.5656%`, `hover-70 0.5240%`, settled + hover-50 PASS) — pre-existing chronic per D256/D258's never-settles measurement, with `tooltipB=false` on all three hovers including the PASSing one; owned by **P4.3**, not charged to P3.12. **FULL 34-CHART-DENSITY SWEEP RUN, 32/34 PASS (D298)** — the widest parity measurement taken in this project, run exclusively to bound the blast radius of two lead CSS fixes (**D297**). Those fixes closed a defect that had been in every migrated capture since the pie/ring/gauge centres were ported *and* a second one in the harness itself: `.ts-bkm-center-stat` was missing the `width/height:100%` half of bklit's `size-full`, so wherever `centerSize = baseInnerRadius*2 − 16` goes negative (ring n=1000: `−15.1px`) React's inline width is rejected by the CSS parser and the box fell back to `auto` → **0px wide**, clipping the centre label away entirely and clamping the value's `cqw` type to its floor; and `bench/app`'s Tailwind `@source` glob scanned only `**/*.tsx`, so bklit's `chart-center-typography.ts` was never scanned and **the bklit REFERENCE had been rendering its centre at an unstyled 16px** — a render bklit never ships. Results: ring n=4 settled `0.0602% → 0.0000%`, gauge n=1000 `0.1034% ×4 → 0.0064% ×4`, pie n=1000 settled `→ 0.0094%`, ring n=1000 settled `0.0868% → 0.0100%`. **The doctrinal cost is real: every one of those "pinned identity" numbers that D265 §5 treated as trustworthy was this bug holding still** — identity proves nothing changed since the last capture, never that the baseline was right, and a *steady* non-zero diff is the most misleading case of all. Machine validated by three independent identity reproductions across the sweep (heatmap `0.4277`/`0.3466` = D250/D284/D293; sunburst `0.0599` at 27 **and** 33 = D281; pie hovers `0.0000/0.0000/0.0007` = D278). Only 2 FAILs, **zero regressions**: `markers n=100 hover-30 0.5570%` (unowned chronic, was 0.5507%) and **ring n=1000 hover-30/70 `0.7109`/`0.7389` — now ACCEPTED with reason (D298)**. Ring's oscillation is FIXED and measured so: three consecutive runs spread **0.0006** where the chronic swung ±0.14 across four samplings, and `tooltipB` flipped false→true. What remains is a hover-latch divergence at a pathological density — migrated latches `Ring 423`, which is the geometrically correct band at r=56 derived from bklit's own scale formula, while bklit latches `Ring 1000`, 76px from the cursor, **and does not reproduce that outside the QA harness at all** (3 fresh Playwright contexts × 2 fractions replaying QA's exact sequence: bklit latched *nothing*, label stayed `"Total"`; migrated returned `"Ring 423"` at every sample 0→1500ms). There is no stable legacy behaviour here to be in parity with, and matching it would mean re-introducing the D272-#9 ejection cascade. **`markers` chronic FIXED, not accepted (D299)** — and it was never a hover bug. The reported FAIL was a single phase (`hover-30 0.5570%`), but the report showed *every* phase within a hair of the gate (`settled 0.3493`, `hover-50 0.3530`, `hover-70 0.4859`, all three legend phases `0.4905–0.4914`): a defect that lifts one phase is a hover fault, a defect that lifts **all** of them is a render floor, and hover-30 was merely the phase whose own noise crossed 0.5%. That reading redirected the search away from hover code, and the diff PNG then named the mechanism — red massed at the left and right ends of the series band with grey through the middle, a horizontal-extent signature. Root cause: the `fadeEdges` edge-fade mask had been hoisted onto `g.ts-chart__marks`, the whole marks group, instead of the series paths — **two defects in one rule**. (1) It faded the **marker dots**, which bklit never fades: `fade-edges.ts` is consumed by `line.tsx:294` and `area-gradient-defs.tsx` as the *stroke of that series' path*, `fadeEdges` is a per-series prop on `<Line>`/`<Area>`, and dots carry a flat fill. (2) A CSS mask resolves its percentage stops against its own fill-box, and the r=9 dots bulge the group box 9px past the line at each end — measured `.ts-chart__marks` = `x31 w738` vs `.ts-chart__line` = `x40 w720` — so every stop was mis-pinned ~6px, where the series box matches bklit's `viewportFadeGradientAttrs(innerWidth)` **exactly**. Re-scoping the three rules to `.ts-chart__line, .ts-chart__area` corrects the gradient's subject *and* its geometry, and is a provable no-op where the marks group holds only lines (plain `line` chart measures `marks == line == x40 w972`). **markers n=100 now PASSES all 8 phases for the first time in the port**: hover-30 `0.5570 → 0.3870`, settled `0.3493 → 0.2397`, hover-50 `0.3530 → 0.1823`, marker-fan-open `0.1400`. Note the rule's own comment — *"Equivalent here: mask the marks group — its fill-box spans the same inner width"* — stated both halves of its wrong reasoning confidently and survived every review because the gate stayed green on the charts that happen to have no dots; the replacement comment records the measured boxes and the bklit call sites so the shortcut cannot be re-derived. This is the fourth chronic in a row to turn out a real defect sitting inside a passing gate, which sharpens D297's caveat to D265 §5: stability meant only that the bug was deterministic. **D300 logged OPEN and deferred** — migrated's marker-dot reveal finishes ~350ms *after* `window.__benchSettled` resolves (dots absent at +0ms, blurred at +150, crisp at +400; bklit complete at +0), so every QA `markers` capture is taken mid-reveal and those numbers are pessimistic. Found because a computed-style probe and a screenshot disagreed **on the same build** — which makes the difference state, not style. Not blocking (markers passes without it); unassigned because the dot-attach site `line-chart.tsx:652` is on P4.4's live list, and in-tree precedent for the fix already exists at `bench/app/src/scenarios/bklit-radar.tsx:93-124`. **D299 blast-radius sweep CLEAN — full 34-chart-density re-run, 33/34 PASS, zero regressions.** The fix re-scopes a selector that every line/area chart in the port matches, so it was gated at the same width as D298's sweep rather than spot-checked on `markers` alone. Sole FAIL is `ring n=1000 hover-30/70` (`0.7179`/`0.7420`, settled `0.0100` + hover-50 `0.0325` PASS) — the identical phase pair and the identical mechanism already ACCEPTED-with-reason under D298, and its spread against D298's `0.7109`/`0.7389` is `0.007`/`0.003`, i.e. run noise, not movement. Every other density holds or improves; `markers n=100` stays green across a second independent sweep, which is the first repeat confirmation that D299 is a fix and not a favourable sampling. Waves 4–6 pending | **WAVE 4 CLOSE-OUT GATE 2026-08-25 — GREEN.** `TSC_EXIT=0` (from `showcase/`, unpiped), `BUILD_EXIT=0`, 34-density sweep **33 PASS / 1 FAIL**. The sole FAIL is the D298-ACCEPTED `ring n=1000` hover pair (`0.7211%`/`0.7399%` vs D298's `0.7109`/`0.7389` and the D299 sweep's `0.7179`/`0.7420` — a ±0.01pp band = run noise, not drift). **P4.3's `useMeasuredRect` swap is pixel-neutral, proven per-chart:** ring n=4 settled **0.0000%** + hovers 0.3130/0.3120/0.3098 (matching D302's ~0.31% emphasis gap exactly); radar n=1000 all PASS; pie n=1000 settled 0.0075 + hovers 0.0000/0.0000/**0.0007** — reproducing D278/D288 to four decimals, which validates the harness as much as the change. markers n=100 **all 8 phases PASS**, including the four (`marker-fan-open`, `legend-hover-0/1/clear`) that the previous sweep's narrower `^  (settled|hover-|loading)` grep would have silently swallowed a FAIL in — the pattern is now `PASS|FAIL|overall|Error|error`. Wave 4 is CLOSED; Wave 5 opens with P4.6 + P5.1 dispatched in parallel under mutual file fences (P4.6 owns the 4 chart hosts + `use-hover-chrome.ts` + `funnel-chart.tsx`; P5.1 owns `charts/index.ts` + `live-line-chart.tsx` + `internal/sunburst-geometry.ts`). | **WAVE-5a CLOSE-OUT GATE 2026-08-25 — GREEN (D313/D316).** Quiet window run in the committed order: verify P5.1 -> land D305 -> `TSC_EXIT=0`/`BUILD_EXIT=0` -> exclusive bench cell -> 34-density sweep. **Sweep 32/34 PASS.** **P4.6's owed gates all clean:** `funnel` **and** `funnelvertical` n=1000 **byte-identical on all four phases each** (the `usePrefersReducedMotion` conversion is provably pixel-inert), scatter n=1000 re-confirmed. **Exclusive bench cell (D273) — P4.6's M3a target beaten, not merely met:** `m3a_updateMs` median migrated **67.5 -> 29.7 vs bklit 32.5**, i.e. migrated is now FASTER than legacy; the raw values show why — bklit is vsync-pinned at 32.4-32.7 (two frames every tick) while migrated now lands a large population at **~16-20ms, one frame**. Hover lead retained (`m3c_perMoveScriptMs` 0.919 vs bklit 1.536). Honest caveat recorded: migrated's **p95 is looser** (42.4 vs 33.2, three GC outliers in 210 samples) — faster but burstier, not a regression. **The 2 FAILs: one known, one mine.** `ring` n=1000 hover-30/70 `0.7159`/`0.7396` sits mid-band across four samplings (`0.7109-0.7211` / `0.7389-0.7420`) — the D298-ACCEPTED chronic, unmoved. `markers` n=100 **settled `0.2397 -> 0.7317` was MY D305 margin, and the gate caught the lead's own change.** The diff PNG showed red on the right two-thirds with grey on the left third — a stagger caught mid-flight, i.e. the margin was too SHORT. Corrected: each dot's `delay` counts from inside the double-rAF-deferred `doMarkerReveal`, not from the sweep start, so the tail past `ready` is the whole stagger (`1100 + 500 + 2 frames = 1633ms`), and the scenario moved to `armManualSettle(tail+3000)` because the shared 2500ms net would have silently reported settled mid-reveal again (phase trigger and `sawNonReady` latch preserved; `settle.ts` untouched). **Result: markers settled `0.7317 -> 0.1005`, all 8 phases PASS — better than the pre-D305 `0.2397`, confirming every previous markers capture was pessimistic.** `marker-fan-open 0.1400` reproduces D299 exactly. Also landed in-window: P5.1's routed `migrated-patternarea.tsx` mirror removed in favour of the now-exported `PatternPresetId` (12/12 phases PASS, all 8 presets resolving = proof the union matches).
| 4.4.5 | Final pass | pending |

## Go-to-plan task ticks

Packages per `research/phase-4/go-to-plan.md` § Batch execution order (4.3.2). Dispatch prompts for Wave 1: `research/phase-4/wave-tasks/P1-01.md` … `P1-15.md`.

### Wave 0 — pre-baseline
- [x] P0.1 — T-01 + T-02 — bar-family v0.14 regressions closed as n=1000 density artifacts (D238a); n=100 gates ALL PASS
- [x] P0.1b — T-02b — sankey settled regression CLOSED (D239): root cause commit `97aaf78` label-baseline, not v0.14; settled PASS all densities (n=4/33/1000); hover-state FAILs at n≥33 ruled separate/pre-existing, deferred to Wave 1 P1.2
- [x] P0.2 — T-03 — baseline capture (bench + typecheck/build + visual reference per chart) → `docs/phase-4/BENCHMARKS.md` — split per D246: lanes B1–B3 **DONE**; B4 **DONE** (D240): fixed a genuine v0.14.0 bench-harness regression (10 `bench/app` tanstack scenario files passed a bare `tooltip: true` boolean, crashing pre-paint under v0.14's stricter tooltip-input contract — bench-app-confined fix, zero clone/showcase edits), full exclusive rerun 24/24 cells 0 skipped (`bench/results/2026-08-22T15-46-19-823Z`); sankey visual refs verified against the 3 post-D239 QA dirs
- [x] LEAD-OWNED — prop-less `<Grid/>` QA scenario for T-W1-1 lives in `bench/app/src/scenarios/` (executor guard-fenced): lead adds it when P1.1's resolver fix lands

### Wave 1 — W1 parity fixes (dispatch files in `research/phase-4/wave-tasks/`)
- [x] P1.1 — T-W1-1 — grid `horizontal` default → true (AX2) + prop-less `<Grid/>` QA scenario — **gate PASS** (griddefault n=1000 max 0.1423%, lead-authored fixture)
- [x] P1.2 — T-W1-3 + T-W1-4 + T-W1-8 — sankey cluster: SK1 label default, SK2 controlled hover, fidelity trio SK6/SK7/SK8/SK10 — **gate PASS; D239 sankey hover gap CLOSED** (hover-30 1.0540% → 0.3675%, n=33)
- [x] P1.3 — T-W1-2 — composed stacked-sum branch (C5) + `stackGap` align (C6) — **gate PASS** (composedstacked n=1000 max 0.2706%, lead-authored fixture)
- [x] P1.4 — T-W1-5 + T-W1-6 + T-W1-9 (choropleth half) — CP11 sequential palette, CP4–6 formatter defaults, CP7 exit-fade restore via shared TooltipBox — **CLOSED via fix dispatch F2** (D250). CP7 itself was correct; the acceptance criterion was failing on a separate hover-*persistence* defect in the same cluster (bklit's feature unmount/remount swallows `mouseleave`, so hover sticks over empty ocean; migrated cleared it). Fix confined to `internal/choropleth-hover-chrome.ts`, no shared files touched. **gate PASS** — `qa/results/choropleth/2026-08-23T13-03-30-947Z` settled 0.0000% / hover-30 **0.0000%** (was 1.5345% FAIL) / hover-50 0.1529% / hover-70 0.0071%, tooltips true both sides at every probe; self-test run all four 0.0000%
- [x] P1.5 — T-W1-9 (heatmap half) + T-W1-7 — HM16 highlight-ring removal; HM14 pattern fills wired, HM7 useId separators, HM6 `var(--border)` stroke — **CLOSED via fix dispatch F3** (D250). Verdict (b): HM16 *was* genuinely removed by P1.5; the residual was a separate pre-existing **dim-mechanism** divergence — legacy tweens the cell rect's own opacity to 0.3, migrated composited a background-colored overlay at 0.7 on top (identical over white except at antialiased edges → hollow-square halos in the diff). Fix confined to `internal/heatmap-components.tsx`; T-W1-7 items all preserved. **gate PASS** — `qa/results/heatmap/2026-08-23T12-27-16-808Z` (n=52) settled 0.4277% / hover-30 **0.3466%** / hover-50 **0.3533%** / hover-70 **0.4256%**, all tooltips true both sides; hover dropped 0.16–0.17pp, closing a ~0.08pp overshoot with margin
- [x] P1.6 — T-W1-10 — reference-area `className` forward (RA1) — **gate PASS**
- [x] P1.7 — T-W1-11 — sunburst semantics quintet SB1/SB12/SB13/SB14/SB15 (gate spec for T-D12) — **CLOSED via fix dispatch F1** (D251), which fixed **two** defects: (1) the Wave 1 regression — TanStack's SVG reconciler (`reconcile.ts syncAttributes`) strips all `data-bkm-*` stamps on every hover re-render, which P1.7's new dataset-compare block read as "new cycle" → `seen.clear()` → full 1100ms reveal replay per hover; block deleted, cycle identity now wholly in `playCycleRef`; (2) a **pre-existing** hover-30 knife-edge defect — new `internal/sunburst-hit.tsx` ports bklit's transparent base-geometry hit model (depth-descending, no per-segment leave). SB12/13/14/15 + the playKey mechanism all preserved. **gate PASS** — n=27 `qa/results/sunburst/2026-08-23T14-28-35-953Z` settled 0.0599% / hover-30 **0.0780%** / hover-50 **0.1400%** / hover-70 **0.1466%**; n=1000 `…T14-20-54-053Z` 0.0599/0.0212/0.0199/0.0228% — both `overallPass=true`; console 0 errors; drilldown verified. hover-30 is now the *best* capture, beating the pre-Wave-1 PASS values (~0.32%)
- [x] P1.8 — T-W1-12 — radar RD5 base-delay formula, RD7 round caps, RD6 label spring (fallback ACCEPT+D245) — **gate PASS** (radar n=1000 ≤0.297%)
- [x] P1.9 — T-W1-13 — candlestick K7 animate-gates-reveal, K9 body patterns via pattern-preset, K10 insideStroke inset rect — **gate PASS** (candlestick n=1000 max 0.4962%, after solo repro cleared a batch-contention artifact — D248)
- [x] P1.10 — T-W1-14 + T-W1-15 — gauge G2/G3/G5; scatter S8–S12 (S12 wires single live `boxSpringConfig` path per K-1 ruling) — **gate PASS** (gauge n=1000 ≤0.297%, scatter n=1000 0.4509% after solo repro cleared a batch flake)
- [x] P1.11 — T-W1-16 — LiveLine V1 `LiveYAxis position="right"` (independent of T-F1) — **gate PASS** (liveline n=100 ≤0.1368%); `state=loading` console load covered ad-hoc (not in the fixed CE list)
- [x] P1.12 — T-W1-17 — markers LM6/LM7/LM8/LM11 + `__qaSetMarkerFan` dev/QA gating (LM1+LM13 fold-in) — implementation done, `__qaSetMarkerFan` gating verified inert-by-default AND functionally exercised; markers n=100 hover-30 0.5507% classified **chronic**, not a regression (D248)
- [x] P1.13 — T-W1-18 — children CH3/CH4/CH5+B12/CH13 `<Background>` wire + AX5 axis-label tweens fold-in — **gate PASS** (line n=1000, legend n=4 0.0000%); found the top-level `ticks:` axis key was dead across all 6 hosts (D247)
- [x] P1.14 — T-W1-19 — bar pulse wave resurrected into `barPulseMark` (overlay deletion deferred to P2.1) — **gate PASS** (bar/barsquares/bardepth n=100 ≤0.153%)
- [x] P1.15 — T-W1-20 — area A2/A4/A5/A8/A9/A3 (D13c respected on A8) — **gate PASS** (area n=1000 ≤0.158%)

### Wave 2 — W2 dead-code hygiene
- [x] P2.1 — T-W2-1 — delete loading sweep/skeleton surface, `resolveGridShimmer`, heatmap loading internals, `bar-pulse-overlay.tsx` (post-P1.14) — **IMPL LANDED** (D252, session `b2d436f1`, attempt 1 clean): FD7 surface + `resolveGridShimmer` + 3 orphaned `DEFAULT_SHIMMER_*` tokens + 2 heatmap loading constants + `paintHeatmapCellHover` + sweep CSS from both `styles.css` blocks; **typecheck exit 0, production build exit 0 (22 routes prerendered)**. `bar-pulse-overlay.tsx` deletion was hook-blocked (D216, executors cannot delete) — **deleted by the lead** after independently re-proving zero importers. **GATE PASS (D257)** — loading surface pixel-neutral vs pre-Wave-2 baselines (line 0.0531%, area 0.0525%, heatmap 0.3783%) and heatmap n=52 **byte-identical** to its pre-Wave-2 run; bar pulse verified live through `__qaSetBarPulsePaused(false)`. Caveat logged: the loading-state gate is near-blind (D257 §4).
- [x] P2.2 — T-W2-2 + T-W2-5 + T-W2-6 — dead vars/branches sweep (incl. per-chrome `tooltipBoxSpring` voids ×4 per K-1), MARKER_* un-export, live-line/ring cleanup — **IMPL LANDED** (D252, session `a717b45c`, attempt 2 after one truncation): ~90% was already deleted by a prior session but left **half-finished**, red at 6 errors; this session completed the K-1 supply-site deletions ×5, `LiveHoverFrameInput.width`, and the phantom-`bandWidth` ring branch in `toDotConfig`. **Tree now typechecks exit 0** — the D251 commitment on the 4 `TS2561 tooltipBoxSpring` errors is discharged. **GATE PASS (D257)** — bar/candlestick/composed/liveline all PASS; ring FAIL is pre-existing (D256, → F4) and scatter M3a 34.3→65.5 is cross-reffed to P4.2, neither charged to P2.2.
- [x] P2.3 — T-W2-3 + T-W2-4 — styles.css orphans + duplicate block + `:has()` scoping; stale comments batch — **IMPL LANDED** (D253, session `8d916e96`, attempt 1 clean): pie-center CSS + duplicate loading block deleted (subset relationship **re-verified on current bytes** after P2.1 shifted both blocks mid-session), 6 comment sites corrected. **FD16 `:has()` scoping SKIPPED-WITH-REASON** — legacy is *also* unscoped (lead-verified against the clone); applying the ruling would have created a divergence and broken standalone-legend hover-dim. `parity.md:244` is wrong. **typecheck exit 0, build exit 0, 29 routes.** **GATE PASS (D257)** — pie reproduced its P2.4 solo gate with no drift from the comment/CSS pass; legend pixel-perfect at n=4 incl. 3 hover-item captures.
- [x] P2.5 — **lead addition (D252)** — Wave 2 orphan residue found *by* P2.1 and P2.2 and correctly left outside their fences — **IMPL LANDED** (D253, session `9bed2065`, attempt 1 clean, +1/−43): `paintHeatmapLegendSwatchHover`, the 5th per-chrome `tooltipBoxSpring` type field, dead `stroke` local in `buildMarkerMarks`. Cascade check caught that `HeatmapHoverStyleParams` is **live** despite a same-named twin in `heatmap-utils.ts`. **typecheck exit 0, build success.** **P2.5b** (same session lineage, `5c81c45d`) then deleted the sixth orphan `SharedTooltipChromeOptions` — cascade check kept all four candidates (`IndicatorConfig`, `DotConfig`, `BoxConfig`, the `SpringConfig` import), each with named live readers; the interface was a genuine leaf. **typecheck exit 0, build exit 0, 29 routes.** **GATE PASS (D257)** — heatmap byte-identical to pre-Wave-2; markers FAIL is pre-existing and byte-identical across the wave boundary (D256).
- [x] P2.4 — T-W2-7 — **pie hover-state parity** (lead amendment D249; NOT delete-only) — **FIXED, gate PASS** (D254, session `34d4f2d3`, attempt 1 clean): cause was a **hover oscillation** — the 10px pop moved the hit area off a stationary cursor because the listener rode the visible `radialArc` mark; legacy uses a static-hitbox / animated-visual **pair**. Fix = static transparent `pie-hitbox` twin mark, `pie-chart.tsx` only (+43/−4), no shared file touched. Solo `qa/results/pie/2026-08-23T19-02-45-552Z` `overallPass=true`: settled 0.0054% / hover-30 **0.0000%** (was 3.18%) / hover-50 0.0000% / hover-70 0.0007%. Retires pie's unowned baseline debt; **clears the P3.1 precondition**.

- [x] **AUDIT (lead, D255)** — repo-wide moving-hit-area sweep, dispatched after pie and sunburst proved to be the same root pattern. Read-only, no code changed. **Result: the pattern is NOT systemic** — one AFFECTED site (ring), ruled **ACCEPT** because legacy is structurally identical (DOC-1); everything else SAFE by separation, positional hit-testing, or geometry. Cross-ref recorded in go-to-plan.md against **T-D10 (P3.12)** and **T-C4 (P4.3)**, requiring **hover** captures in P3.12's ring gate.

### Wave 3 — W3 native swaps (RF: revert-that-swap-on-fail)
- [x] P3.1 — T-D15 — palette layer (runs FIRST; prerequisite for palette-adoption swaps) — **IMPL LANDED + GATE PASS** (D260, agent `aff8682395abc7729`). **NOT the CSS alias go-to-plan.md prescribed**: D259 caught pre-dispatch that native `defaultChartTheme.palette` has **6** entries (`scene.ts:58-71`) vs legacy's **5**, so any alias diverges at index 5 and stays out of phase forever — cycle length is the defect, not token naming. Fix = shared `CHART_CATEGORY_PALETTE` (+`_WITH_FALLBACK`) in `internal/design-tokens.ts:65-83`, wired as an explicit 5-entry `theme.palette` override at pie/sunburst/sankey/scatter; sankey's literal fallbacks preserved. **choropleth EXCLUDED** — executor pushback verified and upheld: it cycles the separate **sequential** `--chart-scale-01..05` family (`globals.css:134`/`:205`, legacy `chart-scale.ts:3`), so routing it through the categorical palette would have *reintroduced* the CP11 deviation P1.4 already fixed. **Gate zero-drift, all five settled values byte-identical to history**: pie 0.0141 / scatter 0.4509 / sankey 0.1092 / sunburst 0.0599 / choropleth 0.0000. Honest caveat (D260): the wiring is **pixel-inert today** — every mark computes explicit per-datum fills and none resolve through `scales.ts:59/82`, so this is currently a contract swap; the 5-entry cycle becomes load-bearing only when a later package removes those fills.
- [x] P3.2 — T-D1 — motion() defaults replace reveal constants ×7 parts — **CLOSED skipped-with-reason (D264): non-viable as chartered.** Native `defaultDuration`/`defaultEasing` are module-private (`motion.ts:207,209`) and the full call chain to them is unexported; even the *resolved* native easing is a numeric `(progress) => number`, never a CSS string — **no read-back path exists even by invoking `motion()`**. And the values already match exactly, so there was nothing to correct. Deliverable = provenance comments naming `motion.ts:207,209` as the upstream these literals hand-mirror; **zero value changes**, `enter-transition.ts` engine untouched (its D51 header records that a native 2-keyframe tween cannot animate SVG `d` at all). Gated 7 parts (pie/funnel/ring/radar/line/area/composed — **radar included**, contra tanstack.md row 1, per D263). Two byproducts: **(a)** three of the seven parts (`line`/`area`/`composed`) carry their **own local copies** of the constant with no `design-tokens.ts` import — 4 independent copies total, violating the layer contract → **routed to P6.2, NOT P4.4** (corrected: this entry originally said P4.4, but `go-to-plan.md:373` explicitly deferred both reveal-constant tokens and `xForDate` *out of* P4.4 into P6.2, to avoid double-touching the line/area/composed hosts before T-F1 rewires their scale consumption in P6.1; `P4-04.md:22` agrees. go-to-plan is the source of truth per 4.4.4); **(b)** funnel's loading state, **never gated before**, diverges → **routed to P5.7**.
- [x] P3.3a — T-D2 — native focusDisabled + `focus-disabled.ts` deleted — **LANDED + ACCEPTED (D265)**. `gauge.tsx:113` imports native `focusDisabled`, used `:677/:735`; pie/ring/radar/sunburst were already native, so this closed the family exactly as D263 §5 scoped it. Local `internal/focus-disabled.ts` fully orphaned (grep-verified zero refs) → **lead-deleted per D216**; post-deletion typecheck **exit 0** from `showcase/` (repo root has no typescript — `npx` there returns the decoy package). Gate zero-drift, 6 of 7 charts **byte-exact** vs baseline (area 1000 `3/1/1/1517`, bar 100 all-`0`, candlestick 1000 `2904/4764/3103/3122`, composed 1000 `811/0/0/1742`, line 1000 `0/0/0/1434`, scatter 1000 `4329/0/748/2018`); heatmap 52 hover-70 `4086→4062` (−24 px, 0.0025% jitter).
- [x] **C11 — composed per-series `dimOpacity`** — **LANDED + ACCEPTED (D265)**, on the existing custom mechanism. `internal/hover-chrome.ts` per-series (`:62`) + top-level (`:113`) option, defaulted `:184`, resolved `:373/:428/:645`; `composed-chart.tsx` Area **0.6** (`:269`) / Line **0.3** (`:285`), bars `undefined` (`:257`). Closes the divergence `composed-chart.tsx:61-63` documented against itself. Pixel-unverifiable by the QA harness (see D265 §7 caveat: the only composed fixture shares one `dataKey` across Area+Line, so `extractComposed` collapses them) — executor proved the split instead by bundling `hover-chrome.ts` with esbuild and asserting both opacities against mock series in a live Playwright DOM.
- [x] P3.3b — **T-D11 native `states` swap — CLOSED NON-VIABLE (D269), zero files edited, zero possible.** Four blockers, all lead-verified at source: (1) `ChartMarkStateStyle` has **no `filter`** (`mark-state.ts:271-279`, zero grep hits) but marker/scatter dim is an inseparable opacity+`blur(2px)` pair → **scatter 100% excluded**; (2) the shipped renderer collapses per-mark durations to one `Math.max` (`mark-state.ts:292-306` → `svg-surface.ts:118-128`) → would have **retimed composed's bar 0.12s to 0.4s and broken C11**; (3) **decisive, previously unnoticed by everyone including me** — dim has a **second trigger, legend hover**, a React context (`chart-legend-hover.tsx:3-11`) outside the SVG pointer system, interleaved across 13 sites in `hover-chrome.ts`; native `states` sees only `ChartFocusState` and `ChartProps` exposes no controlled-`focus` input → **no chart is single-trigger enough for even a partial swap**; (4) native JS `ease-in-out` is quadratic (`reconcile.ts:334-341`) ≠ bklit's `cubic-bezier(0.42,0,0.58,1)`, risking the byte-exact 0-diff bar/line captures. **My D265 §2 "viability confirmed, do not re-litigate" ruling was WRONG and the executor correctly overturned it** — I had verified what the mechanism *targets* and one of its triggers, never what the replacement can *express* or whether that trigger was the only one. **Custom hover-chrome STAYS, now by documented argument rather than by default.** Future bridge `interaction.setControlledFocus` (`dom-types.ts:66-88`) is real but gates tooltip pinning → **Phase 5 focus-model territory**. **Method note (D269 §5):** every pre-settled viability ruling must now state its own falsifier. ~~**NOT DELIVERED by P3.3, no ruling recorded; RE-DISPATCHED (D265 §0/§2).**~~ Historical framing follows: The package shipped C11 and skipped the mechanism change that IS the task: **grep for `states:`/`unmatched` across `showcase/migrated/charts` returns zero hits.** Caught by checking the charter, not the gate — "dim pixels identical" is satisfied perfectly by not touching the mechanism, so **a green gate is what a mechanism-swap task looks like when the swap is absent** (mirror image of D264 §3). **Viability already settled affirmative by the lead**: dim targets *are* native marks (`hover-chrome.ts:330/336/342/552` query `.ts-chart__line`/`__area`/`__dot`/`__bar-y` by `data-ts-key`) and the driver *is* native focus (`onFocusGroupChange`, `:106`/`:747`), so `when:{focus:'unmatched'}` has real state to match; native `states` confirmed per-mark (`types.ts:1048/1067/1302`, `focus-mark.test.ts:800-803`). **Real risk is timing desync, not viability** — `DIM_TRANSITION` 0.4s / `BAR_DIM_TRANSITION` 0.12s / `MARKER_DIM_TRANSITION` 0.15s (which co-transitions opacity **and** `filter`) + `MARKER_DIM_BLUR_PX = 2` + `MARKER_ACTIVE_SCALE = 1.35` (`:35-41`) stay custom per go-to-plan's own scope line, so markers' currently-inseparable opacity/blur pair could drift once one half goes declarative. First question for the executor: is native `states` transition timing per-mark configurable? If not, a **partial** swap (bars/lines/areas, markers excluded) or documented non-viability is the honest outcome — not a forced cutover. C11's 0.6/0.3 values are the parity spec the swap must reproduce.
- [x] P3.4 — T-D3 — linear stagger formulas → `stagger({each,offset})` — **LANDED + ACCEPTED (D271)**. 11 call sites / 6 charts via new bridge `internal/native-stagger.ts` + tsconfig alias for `@tanstack/charts/motion/definition` (follows existing clone-alias convention, D238 — not a D232 breach). Equivalence proven by importing the **real** `stagger()` from clone source and diffing against the original formulas over 48 cases incl. both clamp boundaries: bit-identical or <1e-9. Gate: typecheck 0, **build 0 (lead-run; executor reported typecheck only)**, settled clean ×6. Remaining FAILs are byte-exact documented chronics (ring bimodality D256/D258 →P4.3; markers hover-30 0.5507%). Pie ~3.18% chronic **confirmed retired** (0.0007%). Non-linear formulas correctly untouched: sunburst (→P3.6), sankey, heatmap. ⚠️ Report's "other concurrent Wave-3 executors" claim is **false** — none ran; dirty tree is uncommitted Wave 1–3 work (HEAD `7d60a7f` predates them). My briefing gap, now standing boilerplate.
- [x] P3.5 — T-D4 — grid highlight → native `ruleY` (D274). Grid guides were ALREADY native (`resolveGridGuide()` → `axis.grid`, D272); funnel bands/lines closed NON-VIABLE under D30 with zero edits. First native-rule consumer in the tree. tsc 0 / build 0; profitloss 0.0192% (the real exercise — `line` sets no `highlightRowValues`), line 0.0000%, funnel 0.0000%
- [x] P3.6 — T-D12 — sunburst: **SPLIT OUTCOME (D276)**. Sub-part A (full native `sunburst()` mark swap) **NON-VIABLE** — native `path:'morph'` has no external progress input so it can't be sampled at arbitrary `zoomT` (`motion.ts:2394-2417`), and the native resolver hit-tests **grown** geometry (`nearest.ts:91` skips geometry-affinity) so `sunburst-hit.tsx` must stay. Sub-part B **LANDED** — `RadialArcOptions.generator` stub-dance → real configured d3 arc (`sunburst-chart.tsx:546-554`); degenerate cull re-added at `:507-509`, thresholds byte-identical to retired `sunburst-geometry.ts:277`. `TSC_EXIT=0`; **sweep now GREEN — build 0; sunburst settled 0.0599% at BOTH n=27 and n=33, matching the approved baseline to four decimals, hovers ≤0.1466%**. Reveal-replay ref-identity verified untouched. ⚠️ watch: arcs with `innerR < 1` shift from wedge to true annulus, sub-pixel at center
- [x] P3.7 — T-D13 — sankey native swap **LANDED (D279)**. `sankeyDiagram()` at `sankey-mark.ts:37-40,314`; links via native `link()`+`curveBumpX`; `sankey-layout.ts` 80→33 lines (wrapper + defensive clone retired). Wave-1 behaviors verified intact: D239 baseline `fontSize*0.35` (`sankey-mark.ts:157`), SK10 ±20px slide (`sankey-animation.ts:248-254`), controlled hover (`sankey-chart.tsx:388-399`). **Executor produced NO report (3rd D277) and was cut off before the tsconfig `paths` entry — lead wrote it.** ⚠️ **Shipped a hover regression the lead caught in the sweep and fixed:** native `link()` bakes `stroke-opacity` where the old mark used `opacity`, so the chrome's `style.opacity` multiplied instead of replacing (hover-30 1.3455% vs 0.3675% approved). Fixed in `sankey-hover-chrome.ts` (write `strokeOpacity`) + `sankey-animation.ts:119` transition re-keyed to the same property. tsc 0 / build 0; settled 0.1101%, hover-30 0.3824% — **PASS**
- [x] P3.8 — T-D6 + T-D7 — `findNearestPoint` hover resolution + y-domain `svgAnimation` tween — **SPLIT, both halves accepted, zero files changed (D282)**. **T-D6 NON-VIABLE** — native tie-break *matches* (`nearest.ts:44-52`, reverse traversal + `<=`), but the metric does not: native is 2D Euclidean over scene pixels, ours is a 1D x-only bisect; and the root export is scene-only while `ChartScene` greps to **zero** in `showcase/migrated/`. **T-D7 already landed** — composed `:1039-1042`, line `:486-489`, area `:734-737` populate native `svgAnimation`; reduced-motion snap holds natively via `respectReducedMotion ?? true` (`motion.ts:224`). Ruling: `research/phase-4/findings/P3-08-findNearestPoint-svgAnimation-ruling.md`. `bisect.ts` NOT orphaned. Lead removed the inert `Y_DOMAIN_TWEEN_SKIP_THRESHOLD` (zero consumers). `hover-reanchor.ts:16`'s duplicate `bisectDateLeft` left for **T-C5/P4.1** which already owns it — see D282 for the `lo=0` vs `lo=1` left-edge trap in that fold.
- [x] P3.9 — T-D8 — native tooltip cutover — **CLOSED NON-VIABLE, all six sub-runs, zero files touched (D285)**. Not the D272 pattern: native tooltip IS reachable and DOES paint on our renderer (`@tanstack/charts/tooltip` is a real subpath export, `charts-core/package.json:217`). Declined on parity, not architecture. **Blocker A:** `createSvgChartRenderer` declares no `capabilities` (zero hits in `svg-surface.ts`; `tooltipMotion` lives only on `motion.ts:765,805`) → `renderer.ts:1024-1037` passes `motion: undefined` → instant show/hide, losing bklit's spring(300/25) entrance + position springing on every hover. `Chart.tsx:101-109` force-overwrites `renderer`, so no injection route. **Blocker B:** cutover would silently no-op four public `ChartTooltipConfig` props — `content`, `children`, `springConfig`, `boxSpringConfig` (`internal/types.ts:376-382`); the React body portal exists (`react-charts/src/tooltip.tsx:85`) but is unexported. **Blocker C:** 8px vs bklit's 16px edge clamp. `tooltip-chrome.ts` + `tooltip-scheduler.ts` both STAY — no orphans. Revisit only if TanStack ships SVG-renderer `tooltipMotion` + exports the body portal; landing diff pre-written in D285 §4. **CORRECTION (D286 §1): its report's "no files touched" claim was FALSE** — it had added two dead imports at `bar-chart.tsx:71-72` (`@tanstack/charts/tooltip` + tooltip types) which broke `tsc`/`build`; lead deleted both. Interrupted-then-resumed dispatches report what they remember deciding, not what is on disk — always `git diff HEAD` a zero-edit claim. **SECOND CORRECTION (D287): the "zero files touched" claim was wrong by far more than two imports, and not the executor's fault.** `kill 6672` reaped only the `dispatch.sh` wrapper; the `cmd` child (pid 6706) survived and kept writing `bar-chart.tsx` for ~2.75h unmonitored, adding a 65-line `buildNativeTooltipOptions` / `NativeTooltipBoxState` / `ChartTooltipOptionsNative` block plus an `intFmt` import that referenced the very imports the lead had just deleted — leaving the file non-compiling *after* a green gate. Lead excised the block by anchor (67 lines), verified P3.10's `nativeDepthGradients` survived, re-gated `TSC_EXIT=0` / `BUILD_EXIT=0` / 29 routes, and re-ran `bar` (byte-identical: 0.0000/0.0000/0.0757/0.1243) + `bardepth` (PASS) — proving the zombie's code was dead and never painted. Bench `buct8rovs` discarded: it overlapped the live zombie, violating D273. **All ten charts passed on pixels throughout — QA is blind to this class of damage; see D287 §4.**
- [x] P3.10 — T-D5 — `spec.gradients` + brush `spec.clip` natives — **LANDED + ACCEPTED (D286)**. 8 gradients → native across 3 files: `area-chart.tsx:755` (`nativeAreaGradients`), `composed-chart.tsx:1054` (`nativeComposedGradients`), `bar-chart.tsx:712` (`nativeDepthGradients`, 6 gradients). `gauge.tsx:679/737` already native in HEAD — gated, not edited (coverage caveat: no gauge scenario sets `useGradient`, so it proves non-regression, not gradient pixels). **Old JSX `<defs>` for swapped ids removed — no double-definition; surviving `<linearGradient>` per file is the briefed `userSpaceOnUse` holdout only.** **Row 12 (brush `spec.clip`) CUT NON-VIABLE by lead ruling before dispatch** — `ChartSpecBase.clip` is `boolean` (`types.ts:641`), not a clip-path ref. **`segment-visuals.tsx:66,100` DROPPED non-viable** — consumer `<line>` lives in `SegmentOverlay`'s sibling `<svg>` outside the serialized scene, so `resolvePaint` never rewrites the ref (sixth D272-shape occurrence; corrects go-to-plan's row-5 eligibility list). Gate: `TSC_EXIT=0`, `BUILD_EXIT=0`, 10/10 PASS — **after lead removed two dead P3.9 leftover imports at `bar-chart.tsx:71-72` that broke the build (see D286 §0-1).**
- [x] P3.11 — T-D9 — `brushX` mechanics **CLOSED NON-VIABLE, skipped-with-reason (D280)**. Zero files changed; deliverable is `research/phase-4/findings/P3-11-brushX-ruling.md`. `brushX` returns a `ChartControl` whose `resolve(context)` needs a scene-supplied context (`interaction-brush.ts:101-107,122-134`) and materializes only via `renderer.ts:902`; the migrated brush mounts as a plain DOM **sibling** of `<Chart>` (`line-chart.tsx:998-1002`). Falsifier searched and empty — no standalone/headless control-resolution harness exists. **Forward falsifier (lead-found):** `createChartCursorHostSession` (`cursor-host-contract.ts:79`) IS a standalone extension driver but is cursor-only (throws unless `__chartExtensionType === 'cursor'`) — if a control analogue ships, T-D9 becomes viable. TanStack's own answer to this UX (conformance case 83) is two chart hosts = the forbidden option (b). ⚠️ **Brief correction to carry forward:** the 2-finger gesture is at `internal/chart-selection.ts:121,133,152`, NOT `brush-drag.ts:120-163`; `HANDLE_HIT_PX` is at `brush-drag.ts:202`, not `:167`
- [x] P3.12 — T-D10 — `createChartSpring` **LANDED, tolerance ruling (c) viable-broadly (D283)**. `internal/spring.ts:18` imports + `:51/:57` call `createChartSpring`; subpath mapped at `showcase/tsconfig.json:125`. `TSC_EXIT=0`. **Premise inverted:** framer-motion 12.43 is itself analytic closed-form and native matches it branch-for-branch — max|framer−native| = 0.0000 at every config incl. `{400,25}`, while the retired Euler integrator drifted up to 0.284 units / 170 px and ±45–171 ms on settle. Swap **buys** fidelity rather than spending it. Zero of the 8 consumers edited — push API (`set`/`jump`/`stop`/rAF/`onUpdate`) kept byte-compatible, only the math changed. Part B (host-sizing) **closed EMPTY**: all 11 remaining parts feed HTML overlay chrome via scale math and gauge's two hooks measure different containers, so "≥2 hooks removed" fails everywhere (count discrepancy resolved: **14**). Ruling in `research/phase-4/findings/P3-12-createChartSpring-ruling.md`. Lead removed the two `temp-p312-*.mjs` evidence scripts (D216)
- [~] P3.13 — T-D14 — misc natives bundle (tanstack rows 39/41/42/25/26/27/13/11) — **PARTIAL (D288). Run killed externally at ~62 min with a zero-byte report; recovered via session resume (2nd success of the D285 §6 pattern).** **Row 41 LANDED** — all 4 composed mark-channel sites → native `d[xDataKey] as Date` (`:849` `xAccessor`, `:873`, `:884`, `:896`); non-channel sites correctly stay on `toDate` (`:699,738,971,992,1108,1215,1226`); safe because composed's own `scaleUtc()` scale coerces in its `map` at `:991-995`, so the native all-string-domain trap never fires. **Row 42 NON-VIABLE** — 7th D272 closure: `funnel-chart.tsx` has zero TanStack machinery (no `<Chart>`/`defineChart`/marks/scene), renders hand-built `<svg>` + `FunnelSegment` at `:729-765`, per its own D30 header at `:4-34` — no native mark surface to carry a `key` channel. **Row 39 CUT** by lead ruling (D285 Blocker A: cannot move a spring onto a motionless mechanism). **Row 11 SKIPPED-WITH-REASON** (zero internal consumers → no gate possible → negative EV). **Rows 25 / 26 / 27 / 13 NOT REACHED — still open, carried to P3.13b.** Gate: `TSC_EXIT=0`, `BUILD_EXIT=0`, 27 routes, composed n=1000 PASS. **Row 41 proven pixel-neutral by revert-experiment** (D288 §4): reverting all 4 sites reproduced the post-change distribution exactly, so the apparent composed hover shift was a sweep-context artifact, not a regression. Attribution via `find -newermt` confirmed P3.13 wrote exactly one file.
- [x] P3.13b — T-D14 remainder — **T-D14 NOW FULLY CLOSED: row 13 ruled NON-VIABLE 2026-08-25 (D314).** `filterDataByXDomain` stays app-side, and the blocker is not `tanstack.md`'s recorded one ("row-filtering app-owned, docs pattern" — a convention) but a contract: the brushed subset has a **second consumer** the native primitive cannot reach. `line-chart.tsx:217-232` / `area-chart.tsx:324` implement bklit's deliberate split (commented at `:216` citing `shell:265-277`/`:341`/`:303-306`) — `visibleData` feeds **`resolveTimeSeriesYDomain` and nothing else**, `renderData` (full dataset) feeds every mark/grid/hover/tooltip — so brushing rescales Y to the brushed region while marks stay on the full series. `axis.viewport` clips the **view** while *retaining* `contentDomain` (`configured-scale.test.ts:98-119`) and hands no narrower row set to an independent y-domain; swapping it in leaves Y global — a parity break invisible to a `settled`-only gate on an unbrushed chart. Closed as **ACCEPT-custom, not deferred**; `filterDataByXDomain`/`createXAccessor` are public API (`index.ts:354`). Original entry follows. — **PARTIAL, gate-green (D291). Killed TWICE (35m zero files; 50m with files landed), zombie-free both times. Rows 25 NON-VIABLE / 26 LANDED / 27 LANDED / 13 NOT REACHED. Lead finished row 27's broken cast and ran the gate.** **Row 25 `binTimeX` → NON-VIABLE, the EIGHTH D272 closure:** `ChartMarkersOverlay` (`internal/chart-markers.tsx:315-389`) is a pure HTML/DOM overlay — `<div aria-hidden position:absolute inset:0>` at `:345-347`, imports zero TanStack machinery, and is handed `xScale`/`marginLeft`/`marginTop`/`innerHeight` as props by the host (`:316`, contract comment `:349-356`). It renders as a **sibling of the chart SVG**, so the transform surface `binTimeX` attaches to inside the scene pipeline is unreachable. **Independent second blocker:** the replaced code is not day-bucketing of scalars but arbitrary-item grouping of rich marker objects — `Map<string,Bucket>` keyed by `m.date.toDateString()` (`:323-332`), consumed for x-position (`:359`), active-state compare (`:361-362`) and N-marker fan-out with per-item icon/color/onClick/href. A time-bin transform emits binned rows for marks; it cannot emit grouped objects carrying interaction metadata. Zero files touched. **Row 26 LANDED** — `internal/projection-utils.ts:1` imports `linearRegressionRowsY` from `@tanstack/charts/regression`, called `:101`; slope recovered as Δy/Δx over `samples:2`, empty result → 0 (native returns `[]` exactly where custom returned 0). `buildAutoFutureValues`' extrapolation walk untouched. Needed a new `showcase/tsconfig.json` paths entry (`:119-121`) — TS2307 without it. **Row 27 LANDED** — `radar-chart.tsx:15` imports native `fold`, wide→long reshape at `:308-334`, `Z_PAD` applied to each wide datum **before** the fold so every long row inherits it. **Row 13 `axis.viewport` NOT REACHED — still open, carried forward.** **Gate (lead-run):** `TSC_EXIT=0`; radar n=1000 PASS all four phases with `settled` **byte-identical to the 04:25 baseline (2322 diffPixels)** and hover-70 identical (1347) → the fold swap is pixel-neutral; line/area/composed n=1000 all PASS, composed reproducing D288's recorded `settled 0.0845 / hover-50 0.0766 / hover-70 0.1386` exactly. Original scope was the four rows P3.13 never reached: **25** (`binTimeX` for `chart-markers.tsx` — reachability check FIRST, the D272 shape has closed 7×; treat as fully open, P3.13 never opened the file), **26** (`linearRegressionSlope` at `internal/projection-utils.ts:87`, called `:130` — `buildAutoFutureValues` at `:109` stays custom, its extrapolation-past-extremes walk has no native equivalent), **27** (`fold` for radar series — `Z_PAD` at `radar-chart.tsx:50`/`:304` must survive `fold`'s output or lexicographic sort stability breaks at 10+ series; gate at 10+ series or the changed path is never exercised), **13** (`axis.viewport` for brush — partial by design, row-filtering correctly stays app-side per TanStack's own documented pattern; `internal/brush-selection.ts` byte-identical to HEAD and uncontended after P3.11 closed with zero files changed, D280). Rows 39/41/42/11 are closed and explicitly out of scope. Carries the D288 §4 gating lesson: sweep-context numbers are not comparable to standalone numbers on hover phases. **Runs solo before Wave 4** — row 27 touches `radar-chart.tsx`, which collides with P4.1's optional radar-spring fold.

### Wave 4 — W4 centralization
- [x] P4.1 — **COMPLETE, all units landed, gates green (D292).** Session `8b160935-ea5f-46bf-98bc-f7b82d43d500`, run `20260825-004442-P4-1-resume-32412` (first run killed ~3 min in alongside P4.2; resumed with work intact — **the first executor run in this phase to reach a clean `state="completed"` with a real report**). **T-C1 mapper kit LANDED:** new `internal/tooltip-mappers.ts` exports `TooltipMapperSource` (`Omit<Partial<ChartTooltipConfig>,"indicatorFadeEdges"> & {indicatorFadeEdges?: IndicatorConfig["fadeEdges"]}`) plus `toDotConfig`/`toIndicatorConfig`/`toBoxConfig`, all null-tolerant, wired at **five** chrome sites (`hover-chrome.ts:27`, `bar-:20`, `scatter-:21`, `live-:24`, `candlestick-:14`). Candlestick imports the strict subset — no no-op `toDotConfig` forced; live-line handled generically with no overload, `LiveHoverConfig` satisfying the type structurally and staying outside the kit. **Four T-C5 folds LANDED:** `tooltip-scheduler`→`tooltip-chrome.ts:42/:434`, `hover-reanchor`→`hover-chrome.ts:60/:741`, `x-ticks`→`x-axis-overlay.tsx:246/:398`, `visx-pattern-bridge`→`pattern-preset.tsx:9-19` (now module-private). **`radar-spring.ts` SKIPPED-WITH-REASON** — re-verified still 2-importer (`radar-chart.tsx:26-28` + `enter-transition.ts:37`, no re-export), left standalone per wrapper §4; zero radar files touched. **`MARKER_DIM_*` STALE-CITATION** — same-name declarations exist only at `hover-chrome.ts:38-40`; `scatter-hover-chrome.ts:25-28` carries *value-identical twins under different names* (`DIM_OPACITY "0.5"`, `DIM_BLUR_PX 2`, `DIM_TRANSITION`, `ACTIVE_SCALE 1.35`) — a real duplication but not the same-name cross-file one centralize.md implies, so nothing folded and nothing renamed. **Gate lead-verified, not taken on report:** `TSC_EXIT=0`; zero surviving importers of all four folded modules; zero barrel references. QA all PASS — bar/100, candlestick/1000, scatter/1000, liveline/100, composed/1000, pie/1000. **`bar/100` matched the lead's own independent baseline digit-for-digit (settled 0.0000, hover-30 0.0000, hover-50 0.0757, hover-70 0.1243)**, and bar n=100 exercises three of the four units (chrome kit wiring, scheduler fold via `buildBox`, x-ticks fold via visible axis labels) — so identical pixels there are meaningful, not vacuous. **Four orphans deleted by the lead** after confirming zero importers: `tooltip-scheduler.ts`, `hover-reanchor.ts`, `x-ticks.ts`, `visx-pattern-bridge.tsx`; `TSC_EXIT=0` re-confirmed post-deletion. Executor judgment call accepted: kept the `bar-hover-chrome.ts:376` `bandWidth` comment because it documents a still-true deviation confirmed by the code at `:378`. — T-C1 + surviving T-C5 folds — tooltip mapper kit + single-importer folds. **Scope now FULL: P3.9 closed non-viable (D285) and retired nothing, so every tooltip-chrome site survives for the mapper kit — no post-P3.9 subtraction.** Also carries the `hover-reanchor.ts` T-C5 fold (`lo=0` vs `lo=1` left-edge trap: local yields `onClear()`, shared yields a live hover).
- [x] P4.2 — **COMPLETE, all three items landed, gate green on 7 chart-densities (D293).** Session `55986018-94df-45fc-aebb-b03c510111d6`, run `20260825-004709-P4-2-resume-33695`. **Killed twice** (once ~3 min in alongside P4.1; once during its own QA phase) — but the second kill cost nothing: the tree it left was **complete AND compiling** (`TSC_EXIT=0`, zombie check empty, D287 §5(ii) now 4-for-4), and its incrementally-written `P4-02-executor-notes.md` described the tree exactly. Only the gate was outstanding, and the lead ran it. **T-C2 LANDED** — new `internal/chart-focus-kit.ts` (~83 lines) exports `focusValueKey`, `findNearestPointByX` (strict `<`, ties keep the earlier-scanned point, mirroring bklit's `bisectDateLeft` left tie-break), `collectFocusGroup(points, primary, xKeyOf, memberKeyOf, sortOthersByY)`, `navigationOrder`; all three `*-focus-strategy.ts` rewritten onto it (`bar-:3`, `scatter-:3`, `candlestick-:20`), keyers hoisted to module level so no per-event closure is allocated; hosts and signatures untouched. **The two divergences are parameters, not accidents:** bar keys members by `group ?? markId`, scatter/candlestick by `markId`; bar/scatter y-sort non-primary members, **candlestick does not** — ruled from source, because bklit's `use-chart-interaction.ts:78-115` returns a **single** point with a `yPositions` record and `grep '\.sort\('` across `repos/bklit-ui/packages/ui/src/charts/**` finds **zero `a.y - b.y` sorts** anywhere, so the migrated bar/scatter y-sort mirrors TanStack `focusX`, **not legacy** → open parity question for P5.3's ledger, not a P4.2 defect. **T-C7 LANDED** — new `internal/broadcast-store.ts` `createBroadcastStore<T>({initial, equals?})`; **comparator omitted ⇒ always notify**, which is what `pie-hover-chrome.ts:72` needs (pie's *absence* of a dedup guard is load-bearing for controlled-mode re-dispatch), while `heatmap-hover-chrome.ts:68-84` builds three stores whose comparators replicate its prior guards byte-for-byte and reproduces the ONE-broadcast `clearInteraction` via `setSilent` ×3 + a single `cells.notify()` (`:98-104`), joining three subscriptions behind one unsubscribe (`:105-110`). Pie's export surface unchanged, so ring/funnel alias re-exports needed no edit (verified, not assumed). **Gate 7/7 PASS, lead-run**, with three cells proving neutrality by *identity* rather than by PASS (D265 §5): bar/100 `0.0000 · 0.0000 · 0.0757 · 0.1243` digit-identical to the lead's own baseline; **heatmap/52 `0.4277`/`0.3466` byte-identical to D250's F3 closing gate and D284's reproduction across three sessions**; pie/1000 hovers `0.0000/0.0000/0.0007` reproducing D278 exactly; funnel/1000 **0.0000 on all four phases**; scatter and candlestick settled/hover-30 identical to P4.1's run, movers all hover-phase and inside D288's band. **Ring PASSed all four phases this run** (hover-30 0.4275, hover-70 0.4685) where D284 recorded FAIL 0.5656/0.5240, and `tooltipB` flipped false→true on hover-30 — a third independent sampling of the D256/D258 **bimodality**, handed to P4.3 as proof a single PASS must not be read as a fix. **Scatter M3a stays open with no owner:** executor pushback accepted — T-C7 does not touch scatter at all, and T-C2's parameterized keyers are real but not the shape that moves a 2.07× hover cost, so **D289 §7's `migrated/scatter n=1000` M3a 67.5 vs bklit 32.6 needs a dedicated owner**. Two self-reported incidents, both benign: a corrupted draft written to a **typo path outside the repo** (`~/bklset-…`, inert, outside git, lead deletion refused by the permission classifier — left in place) and a lazy-init bug in the first store draft caught before wiring. **T-C8 CLOSED and gate-green, lead-verified.** The two byte-identical private `bandWidthForSquares(bandWidth, seriesCount, groupGap)` copies in `bar-squares-mark.ts` and `bar-column-track-mark.ts` are gone; a single shared export now lives at `bar-squares-layout.ts:1-5` and both consumers import it. Net **−6 lines**, `TSC_EXIT=0`. **Gate run by the lead** (both hosts of the changed marks are in `bar-chart.tsx:551,579`, so coverage is complete): `barsquares` n=100 PASS (settled 0.0046%), `bar` n=100 PASS with **settled 0.0000% and hover-30 0.0000%**. Resume brief fences those three files off explicitly. **Remaining scope: T-C2 (focus-strategy kit — not started, no module exists; hosts `bar-chart.tsx`/`scatter-chart.tsx`/`candlestick-chart.tsx`) and T-C7 (`createBroadcastStore<T>` — not started, zero repo-wide hits).** Carries the D289 §7 before-number: `migrated/scatter n=1000` **M3a 67.5 vs bklit 32.6 (2.07×)**, measured within one exclusive session so no drift explains it; both remaining items sit on scatter's hover path — T-C2 + T-C7 — focus-strategy kit, `createBroadcastStore<T>`, bar-squares math consolidation
- [x] **P4.6 — NEW (D294), scatter dataset-swap regression.** Staged, dispatches after P4.3 + P4.4 close. **The regression was misframed for days and is now correctly scoped:** `m3a_updateMs` is *dataset-swap→paint latency* (`bench/run.mjs:460-466`, zero pointer events), NOT hover — hover is the separate `m3c_*` family, and on it **migrated BEATS legacy: 0.834 ms/move vs bklit's 1.328, and 3.4× faster than the TanStack ceiling**. So interactivity parity is not at risk; the open item is a re-render cost: `migrated/scatter/1000` M3a **67.5 vs bklit 32.6 (2.07×)**. Item 1 (verified, mechanical): five sites hand-reproduce a shared formatter's config — `scatter-chart.tsx:570`, `bar-chart.tsx:751`, `candlestick-chart.tsx:666`, `composed-chart.tsx:1114`, `internal/use-hover-chrome.ts:70` all call `toLocaleDateString("en-US",{month:"short",day:"numeric"})` per row while `internal/formatters.ts:4` already exports the byte-identical prebuilt `Intl.DateTimeFormat`; scatter alone pays it 30,000× per bench cell, and legacy does the right thing (`scatter-chart-shell.tsx:141-144`). **This overturns D290's "`shortDateFmt` ×5 ALREADY SATISFIED"** — that check looked for local *redeclarations* and missed the inline-call shape; the item's own "×5" matches these five sites exactly. Item 2 (judgment): the `timeExtentScatter`→`xScale`→`definition` memo cascade rebuilds the whole scene spec every tick although the date extent is unchanged — premise-check and measure first, restructure only if it earns it. Wrapper staged at `scratchpad/p46-task.md` **DISPATCHED 2026-08-25 13:33** (run `20260825-133324-P4-6-34829`, session `eef9e713-276e-4a27-929d-e4057fd525f2`). Three items: (1) the five inline `toLocaleDateString` → `shortDateFmt` swaps, (2) the `timeExtentScatter`→`xScale`→`definition` memo cascade (premise-check first; a well-evidenced no is accepted), (3) the two `funnel-chart.tsx` `prefersReducedMotion` orphans inherited from P4.4. Fenced off P5.1's three files. **CLOSED 2026-08-25 (exit 0, ~92 min). Headline: the chartered cause was WRONG and the executor proved it by measuring — accepted (D311).** Item 1 landed and lead-verified: `grep -rn "toLocaleDateString" showcase/migrated/` → **0 hits**, all five sites on `shortDateFmt.format(v)`, `settled` byte-identical on scatter/bar/candlestick/composed. **Item 2 overturned my premise.** A CDP CPU+LoAF probe (`research/phase-4/tools/p46-m3a-attribution.mjs`) showed the `timeExtentScatter`→`xScale`→`definition` memo cascade is a fraction of a fraction — post-fix ALL JS per tick is **~12.7ms of ~32ms**, the rest native paint/raster+GC — so the restructure was **documented NO, accepted**. The real cause was a **reveal-replay defect that is also a PARITY defect**: the TanStack scene rebuild destroys `.ts-chart__marks` every tick, taking the `dataset.bkmRevealed` guard with it, so the mount reveal re-ran on **every data swap** (~2000 `Element.animate()` + blur rasterization per tick; 26 LoAF ≥20ms) — i.e. migrated scatter **visibly replayed its entrance on every update**, contradicting the file's own aspirational D14 snap comment. Scatter was the only exposed host (bar `:185` / candlestick `:169` already guard by data identity; line/area/composed gate on `chartPhase`). Fixed with a three-state guard (first call reveals + arms deadline; replacement while deadline pending restarts, preserving the mount-window self-heal; post-deadline snaps). **Lead verified the parity claim independently and it holds on a stronger mechanism than cited**: bklit keys framer `key={…-${revealEpoch}}` and bumps the epoch only from an effect on `[animationDuration, revealSignature]` (`scatter-chart-shell.tsx:146-154`) where `revealSignature` is a caller prop defaulting to `""` (`:60`) — a `data` change touches neither dep. Probe-measured M3a median **42.2 → 32.1 (−24%)**, animate/tick **2000 → 0**, LoAF **26 → 0**, against bklit's 32.6. Item 3 verified: both funnel sites on `usePrefersReducedMotion` (hook `:404`, deps `:476`/`:502`), no hoist needed, only surviving `matchMedia` in `charts/` is a comment at `gauge.tsx:86`. **Coupling routed to P5.5 before dispatch:** the new guard is a **boolean**, which is right only while scatter has no `revealSignature` prop — the moment T-E3/S3 wires it, a boolean cannot express the contract and **S3 would land inert while typechecking clean**; `P5-05.md` now carries an addendum pointing at sankey's `seenRevealKeyRef = {signature,duration}` (`sankey-chart.tsx:515-534`) as the shape to copy, and noting SK6 is the inverse bug sharing one root cause. **GATES DEFERRED to the post-P5.1 quiet window** — `qa/screenshot.mjs` aliases `@migrated/charts` → `charts/index.ts`, the file P5.1 is mid-rewrite of, so QA now would compile a half-finished barrel: file-disjointness fenced the two packages' *writes*, never P4.6's *reads through* P5.1's file. Owed: `funnel`+`funnelvertical` n=1000, scatter re-confirm, and the exclusive bench cell for the M3a argument. Orphan typo-dir `bklit-tanstack-**chars**-migration/` **moved to scratchpad, not `rm`'d** (its copy differed from the repo's by one comment line). Funnel n=1000's 1000 negative-`viewBox` console errors are **identical on bklit** (control-probed) — pre-existing, same class as bar n≥1000, not charged here.
- [x] P4.3 — T-C3 + T-C4 — reveal-shim collapse (post-P3.2) + CenterShell merge incl. R7 RingCenter real import. **Killed at ~58 min alongside P4.4 (external, simultaneous — same shape as the earlier P4.1+P4.2 double kill). Lead post-kill sequence run: zombie check empty (6-for-6), `TSC_EXIT=0` from `showcase/` — tree clean, no repair needed. Wrote ZERO files and produced NO notes file**, in violation of the incremental-write + notes mandate its own wrapper carried; last message was "the picture is nearly complete" on ring, so an hour of *findings* was lost, not idle time. **T-C4 LANDED + lead-gated pixel-neutral (D295)** on the first resume, which also proved the notes mandate outright: the brief's mandatory recovery-dump-before-design produced a **285-line** `P4-03-executor-notes.md` reconstructing the entire lost hour (QA capture mechanics, ring defect picture, T-C3 analysis, `useMeasuredRect` strand) **and** landed T-C4 in the same window. `center-stat.tsx:310` exports `CenterShell<T>` (+ `CenterShellProps:251`, `CenterShellRenderProps:244`) with the container box, CenterStat stack, generic children render-prop, and an **opt-in `intro`** carrying legacy `pie-center-shell.tsx:49-70`'s double-rAF verbatim; adapters delegate and keep per-part logic (`pie-center.tsx:102` scrubbing-null + `innerRadius<=0`; `ring-center.tsx:63` `baseInnerRadius*2-16` and **its absence of a zero-guard, preserved not "fixed"**; `gauge-center.tsx:78` sizing + intro, `GaugeLabelStat`/`GaugeLabelLayout` untouched). **R7 closed** — `ring-chart.tsx:50` real import edge + identity fast-path `:156`, displayName fallback retained. **Gate (lead-run, standalone): pie settled 0.0101% / hovers 0.0000·0.0000·0.0007; gauge 0.1034% digit-identical on all four phases; ring settled `0.0868%` — byte-identical to F4-ring's pin, neutrality proven by identity (D265 §5); ring n=4 all four PASS.** Sole FAIL is ring hover-70 0.5326% = the chronic bimodality, **fourth** independent sampling (D284 FAIL/FAIL, D293 PASS/PASS, lead re-run, now this) — not charged to T-C4, which its own settled+n=4 numbers exonerate. Killed a **second** time right after; zero loss both sides. **Resumed again** (run `20260825-052349-P4-3-resume2-2861`) at strand 2 (ring), carrying its own D272-shaped insight — *a bare SVG `<g>` wrapper cannot host the listeners, since SVG group hit-testing derives from painted descendants, so the fix must be a static transparent hitbox twin emitted through the pipeline, on pie's D254 precedent* — plus three threads it must still close: the dead stamps at `:714-738` vs the false comment at `:706-713`, the unresolved n=4 hole-retention mechanism, and radar's unverified ref-attachment. Original resume brief led on the contrast — P4.4 died in the same second and lost nothing because its notes file was on disk — and makes its **first** action "dump everything you already know into `P4-03-executor-notes.md` before designing any fix". Scope unchanged, order T-C4 → ring → T-C3 → `useMeasuredRect` → resize harness (`research/phase-4/tools/`, dir does not exist yet). Carries the third ring sampling from D293 as proof a single PASS is not a fix, and the D294 note that `pie-hover-chrome.ts` changed under it (now on `createBroadcastStore`) so it must re-read from disk. `funnel-chart.tsx:451,483` explicitly **not** its item despite the fence **CLOSED 2026-08-25 — all five items closed, every claim lead-verified on disk by grep before crediting (D265 §0 Rule A).** **Item 1, ring n=4 emphasis gap — ACCEPT-with-reason, and the mechanism is finally DOM-proven rather than inferred.** At the harness rest point (r=56, inside the hole) bklit's `elementFromPoint` returns ring 0's real track path while migrated's returns bare `svg.ts-chart`. A radial scan through that point shows bklit's ring-0 band hit-testing **r∈[40,76] — ~36px past its own painted inner edge** — while migrated's bands hit-test exactly their painted annuli. The control is what makes it conclusive: a *settled* bklit scan returns exact [60,72], so the fat surface is **hover-state-dependent**, not static geometry. bklit therefore retains emphasis and swaps the centre readout to "Ring 1" where migrated releases. **This is genuine legacy behaviour arising from an un-understood legacy hit-surface anomaly, and it was deliberately NOT ported** — the D299 lesson applied in the correct direction: reverse-engineering a bug into parity is not parity. n=4 hovers PASS (~0.31%, stable), settled 0.0000% post-D297, with D298 as precedent. **Item 3, `useMeasuredRect` — ruled FIX for both, and it widened the known gap.** The legacy clone confirms ring (`:465`) AND radar (`:237`) both measure through `ParentSize debounceTime={10}`, i.e. **pie's P9 debounce gap was in three charts, not one**. Both swapped to `useDebouncedContainerSize` (`ring-chart.tsx:66,:275`, `radar-chart.tsx:32,:231`, each carrying a P9 provenance comment citing the legacy line); the shared hook signature was left untouched and the enabled-param reasoning was checked rather than inherited. **Item 2** radar ref-attachment verified (single root div `:885`, only return branch). **Item 4** all three stale reveal-comments corrected to name `internal/enter-transition.ts`, verified on disk. **Item 5, resize harness — built AND run, all OK, and it exposed a structural blind spot first: no surface in this repo exercises fluid mode at all** — bench scenarios pass explicit `size={280}` and showcase demos use fixed sizes, so re-measurement had never been gated anywhere. It built `showcase/app/charts/resize-lab/page.tsx` (fluid pie/radar/ring × both impls) plus `research/phase-4/tools/temp-resize-probe.mjs`, found page-level viewport resizes are absorbed by the showcase layout (main pinned 744px) and so drove cell widths directly through 600→380→620: **svg tracks the commanded width exactly at every step, bklit ≡ migrated everywhere, zero stale measurements.** The lead's `qa/resize-probe.mjs` turned out to be sankey-specific and unreusable. **Typecheck:** its own surface clean; the raw run exits 1 only on `Margin`/`DEFAULT_MARGIN` TS2304s **migrating between files across two of its own checks** — correctly attributed to P4.4's live refactor and left untouched per D286/D287, which is the third time this phase a live sibling's damage was identified by an executor's forensics rather than by a gate. **Lead cleanup done:** stray typo dir `~/bklit-tanstack-chars-migration` and the four `temp-ring-*.mjs` probes deleted; `temp-resize-probe.mjs` kept as the resize gate. **Gate owed:** ring n=1000 + n=4 with hovers, plus pie/radar settled, to confirm the measuredRect swap is pixel-neutral — deferred until P4.4 closes, since its in-flight Margin refactor currently breaks the build QA depends on.
- [x] P4.4 — T-C6 + T-C9 + T-C10 (minus deferred items) — legend hover unify + intFmt, XAxisLabelOverlay layer, inline dedup batch (incl. C15 terminal-anchor helper). **Killed at ~58 min alongside P4.3 (external, simultaneous); lead post-kill sequence run — zombie check empty, `TSC_EXIT=0`. Lost nothing: its incrementally-written `P4-04-executor-notes.md` was on disk and its every claim survived lead verification.** **T-C6 `intFmt` LANDED and lead-verified** — both local decls gone, `import { intFmt } from "./formatters"` wired at `internal/chart-legend.tsx:5`/`:146` and `internal/legend.tsx:12`/`:176`, and the only surviving `new Intl.NumberFormat("en-US")` in the tree is canonical `formatters.ts:24`. **CHART_ROLE half done** — `internal/reference-area-config.ts` fully converted (`:2` import, used `:24`,`:69`); lead grep proves **exactly one** raw site remains, `internal/chart-selection.ts:193` (`Symbol.for("migrated.chartRole")` inline). **Its recon corrected the charter twice and both corrections stood**: `useSanitizedId` inline sites are **11 files, not 14** (helper itself has zero repo-wide hits → must be created), and `prefersReducedMotion` is **8 sites / 6 files, not 9** (gauge already canonical, so its fence costs nothing). Three further rulings banked: `checkRevealGuard` is a **STALE CITATION** (already satisfied); `profit-loss-legend-hover.tsx` is **DEAD** (consumers are barrels only — `charts/index.ts:300-302`, `internal/index.ts:141-143` — un-wire and leave the orphan, D216: lead deletes); `blurPx` has **TWO** `= 1.5` defaults, `chart-brush.tsx:41` **and** `brush-chrome.tsx:271`, the second missed by the draft grep. `DEFAULT_MARGIN`: 11 decls, fold only the **6 identical** `{40,40,40,40}`. **Three more T-C10 items LANDED on the first resume, all lead-verified by grep (D295): item 3 CHART_ROLE** — all 3 raw sites converted, `grep 'Symbol.for("migrated.chartRole")'` now returns **exactly one line**, `children.tsx:35`, the canonical definition; **item 4 `SegmentComponent`** — bodies byte-identical, canonical `chart-selection.ts:179` (the live one — all four hosts import only `extractSegmentComponents` from it), `segment-visuals.tsx:4` type-imports and `:8` re-exports; **item 2b `ChartMarker`** — **precisely the genuine orphan D290 found with no owning package**, declarations byte-identical across all 10 fields, canonical `types.ts:535` per barrel `index.ts:232`, `chart-markers.tsx:6` type-imports and `:8` re-exports. **All four landed items re-export rather than rename** — MAIN GOAL (c), tidying is not licence to narrow a public API — and the barrels were checked, not trusted. Killed a **second** time immediately after, again simultaneously with P4.3; zero loss, notes file intact for the second consecutive run. **Resumed again** (run `20260825-052354-P4-4-resume2-3010`) on the remaining ledger — `useSanitizedId` helper + 11 sites, `prefersReducedMotion` 8 sites (two of those files it has since edited, so line numbers have drifted — told to re-read), legend-hover un-wire, T-C9, T-C10 items 5/6, the `DEFAULT_MARGIN` 6-fold and the `blurPx` ruling — with P4.3's five T-C4 files fenced off. First resume fenced T-C6 as verified-done, the recon carried forward so it never re-greps, and T-C10's `shortDateFmt` inline-call item explicitly **removed from its scope** (it moved to P4.6). `funnel-chart.tsx:451,483` stay skipped — fenced to P4.3, **now re-assigned to P4.6 as its item 3** (2026-08-25): P4.6 dispatches only after P4.3 closes, which is exactly when the fence lifts, so the two sites need no package of their own. The brief tells it to re-grep rather than trust those line numbers — P4.3 has been editing the same file — and to gate with `funnel` + `funnelvertical` at n=1000 **CLOSED 2026-08-25 (exit 0, ~220 min, 4 kills survived via incremental notes).** All 9 ledger items LANDED plus the 3 'not confirmed as duplicated' ruled. Every claim verified by independent lead grep before crediting (D265 §0 Rule A) — all held. Highlights: `useSanitizedId` helper created (11 files / 16 sites, zero inline survivors); `DEFAULT_CHART_MARGIN` folded for 6 hosts — **deliberately not named `DEFAULT_MARGIN`** because the internal barrel already re-exports heatmap-context's different `DEFAULT_MARGIN {28,16,0,40}`; `XAxisLabel` shared leaf at `x-axis-overlay.tsx:322` with tick-selection untouched on both sides; choropleth zoom-writer merged into one `syncZoomTransform` with the load-bearing `marksGRef` side effect *strengthened*, not preserved-by-luck; C15 count corrected to 4 sites (`redundancies.md`'s 'x3' wrong). **Rulings that were nos:** `checkRevealGuard` = stale citation, already satisfied; `blurPx` = NO FOLD (parent→child layered default, two agreeing literals ≠ duplicated logic). **Its own correction that mattered most:** it reversed its DEAD ruling on legend-hover — its grep was scoped to `showcase/` and missed `bench/app/src/scenarios/migrated-profitloss.tsx:14`, which consumes both names through the barrel. That is D296's gate hole: **`bench/app` has its own tsconfig and is covered by NEITHER `tsc --noEmit` from `showcase/` NOR the showcase build.** It declined to run QA pixel gates — a deliberate, reported decision, correct under D288: four kills spread the edits across sessions so standalone same-shape *before* numbers are uncapturable post-hoc. The lead's consolidated sweep covers it. **Two orphaned `funnel-chart.tsx` `prefersReducedMotion` sites correctly SKIPPED** (fenced to P4.3 at the time) → re-assigned to P4.6 item 3. D300 noted, still unowned: `line-chart.tsx:652` `hasMarkers`.
- [x] P4.5 — T-C10 remainder — **CLOSED skipped-with-reason (D290), lead-executed, zero code changed. Run as a real audit rather than a rubber stamp, per the brief's own instruction to hunt for unaccounted orphans — and it found one.** Walked all 12 items of T-C10 (`go-to-plan.md:148`) against their owning packages: 9 had clean owners, **3 did not**. **`shortDateFmt` ×5 → ALREADY SATISFIED** (every consumer imports canonical `./formatters`: `bar-x-axis-overlay.tsx:12`, `scatter-hover-chrome.ts:1`, `hover-chrome.ts:1`, `candlestick-hover-chrome.ts:1`; zero local redeclarations survive). **heatmap `levelStyles` builder → ALREADY SATISFIED** (`resolveHeatmapLevelStyles` in `internal/heatmap-colors.ts`, called at `heatmap-chart.tsx:258`; tooltip JSX already single-branch at `heatmap-components.tsx:938-942`). **`ChartMarker` canonical imports → GENUINE ORPHAN, no package owned it** — T-C10 names "`SegmentComponent`/`ChartMarker` canonical imports" as one item and P4.4 took only the `SegmentComponent` half. Duplicate `export interface ChartMarker` at `internal/types.ts:535` **and** `internal/chart-markers.tsx:20`, **bodies byte-identical** (10 fields, same order/optionality) → a re-export, not a rename. Canonical is `types.ts` per the public barrel (`index.ts:232`). **Scoped into P4.4 as item 2b** with an explicit instruction to preserve the local `export` (MAIN GOAL (c): identical public API — tidying is not licence to narrow). Not fixed in-session because `chart-markers.tsx` was under concurrent P3.13b edit and D287's lesson is that concurrent writers corrupt silently. Also **corrected a stale routing claim**: PROGRESS's D264 entry said reveal-constant tokens went to P4.4; `go-to-plan.md:373` defers them (and `xForDate`) to **P6.2**, and go-to-plan wins per 4.4.4. Original row content: xForDate + reveal-constant tokens deferred to P6.2; sunburst `maxRevealDelayMs` **DONE early as SB18 (D281)** — closed as a dedup, not the queued deletion: the "orphan" was the second copy of live logic that `sunburst-chart.tsx:920` re-implemented inline under a shadowing local const. Chart now imports and calls the shared helper

### Wave 5 — W5 API surface restoration
- [x] P5.1 — T-E1a — mechanical barrel tier, 3 sub-runs: TYPES (97) / CONSTS (18) / HELPER-FNs (91, sunburst-geometry block held for post-P3.6). **Scope note (D290): the third sub-run's held T-C10 `maxRevealDelayMs` item is ALREADY DONE — closed early as SB18 (D281) as a dedup, not the queued deletion: the "orphan" turned out to be live logic that `sunburst-chart.tsx:920` re-implemented inline under a shadowing local const, and the chart now imports and calls the shared helper. Only the sunburst-geometry block remains held. Do not re-hunt `maxRevealDelayMs`.** P3.6 has landed, so the hold condition is satisfied — **and the released block was measured 2026-08-25 rather than taken from the charter's list, because P3.6's swap changed it (D301 §2): 13 symbols still exported (10 in `internal/sunburst-geometry.ts`, 3 in `internal/sunburst-colors.ts`), 1 alive but module-private (`lerpGeometry:314` — add the `export` keyword, then the barrel line), and 6 genuinely retired (`buildRevealDelays`, `buildRevealSchedule`, `buildSunburstEnterTiming`, `centroidAngle`, `localProgress`, `segmentRevealFromRingSweep`) which route to P5.3's ACCEPT ledger.** `P5-01.md`'s hold-back section is corrected in place with a `⚠️ SUPERSEDED` block; dispatch brief staged at `scratchpad/p51-task.md`. **DISPATCHED 2026-08-25 13:33** (run `20260825-133343-P5-1-35165`, session `d457b364-c7d4-4fd4-a5f8-a91cdf7dd85a`), in parallel with P4.6 under mutual fences. The sunburst hold-back is RELEASED — P3.6 landed (D276) — but with a survivor set the charter cannot know: **13 already-exported / 1 alive-but-private (`lerpGeometry`) / 6 retired**, measured by the lead and written into `P5-01.md` as a `⚠️ SUPERSEDED` block. The 6 retired route to P5.3's ACCEPT ledger; the executor is told not to invent implementations for them. **CLOSED 2026-08-25 (exit 0, ~191 min, run `20260825-133343-P5-1-35165`, session `d457b364-c7d4-4fd4-a5f8-a91cdf7dd85a`) — D312.** **76 of 206 worksheet symbols exported**: TYPES 18/97, CONSTS 12/18, HELPER-FNs 46/91, each sub-run gated `tsc 0` / `build 0` / a resolution spot-check that imported all 54 **as values** (the right test — `export type` creates no local binding). Lead-verified by grep: all **six** definition-site `export` keyword adds are live (`Momentum` + `detectMomentum` in `live-line-chart.tsx`, `lerpGeometry` in `internal/sunburst-geometry.ts`, `defaultPieColors`, `defaultRingColors`, `levelStylesFromColors`); `charts/index.ts` **+143 lines**; the chartered stale-comment fix at `internal/sunburst-reveal.ts:40-46` is correct and — the part that makes it useful — **names `buildRevealTiming` as `buildSunburstEnterTiming`'s successor** rather than merely recording the retirement. **It corrected my charter and was right:** the sunburst split is **13 exported / 1 private / 6 retired**, not my §0 table's 13/1/5 — my own `⚠️ SUPERSEDED` block had listed all six. **Its headline finding is a LEAD, not a work queue (D312):** *"99 of 206 names are fully absent from migrated"* is accurate **as a name match** and unsafe as a statement about functionality, because this port renamed aggressively — bklit's `y-domain-utils` names are absent while `internal/y-domain.ts` ships `resolveTimeSeriesYDomain`/`useNicedYDomainChanged`/`createNicedYScale`. `P5-01-executor-notes.md` carries a `⚠ LEAD CAVEAT (D312)` saying so. I over-claimed twice against that list (`chartCssVars`, the loading family) and **the executor was right on both as scoped**; only the y-domain hit stood. **Bonus findings routed:** `DEFAULT_ANIMATION_DURATION_MS` as **six private 1100-copies** across area/bar/candlestick/composed/line/scatter → **P5.2**; `IndicatorWidth`/`TooltipRow` duplicate-defined in `internal/tooltip-chrome.ts:92,111` **and** `internal/types.ts:354,356` → P5.2; `migrated-patternarea.tsx`'s local `PatternPresetId` mirror citing a barrel gap this run closes → **lead** (bench writes are lead-only). The 6 retired sunburst symbols route to P5.3's ACCEPT ledger. `./temp-p51/` cleaned up by the lead.
- [x] P5.2 — T-E1b — **CLOSED 2026-08-25 (D320).** Survived two kills (233m, then 10m) and completed on the third dispatch. LANDED: `ChartRevealClip` genuine port (`internal/chart-reveal-clip.tsx`, barrel `:541-545`); `ChartStatFlow` alias -> existing `CenterStat` (`internal/center-stat.tsx:161`, barrel `:550-553`); `BrushChrome`/`BrushChromePattern` public (`:558`); `BarYAxisProps` type surface (`internal/types.ts:323`, barrel `:560`). Plus earlier: `Pattern*` x4, `Gradient*` x11, `ChartBackground`, `internal/tooltip-components.tsx`, `getSeriesMarkerVisualExtent` alias. ROUTED TO P5.6: brush per-overlay public components (DOC-4 not-trivial). ROUTED to post-pilot bar package: `BarYAxis` behaviour. Executor gates `TSC_EXIT=0`/`BUILD_EXIT=0`; all lines lead-verified; orphan deleted. **Lead joint gate deferred until P5.3 lands.** (was: PARTIAL. Killed TWICE: first at 233m (D318), then the resume killed again at 10m (D319).** RESUME-2 LANDED: `internal/chart-reveal-clip.tsx` (126 lines, COMPLETE but **UNWIRED** — untracked, no barrel export, nothing imports it; a resume should wire it, not rewrite it) + `BarYAxisProps` type surface in `internal/types.ts`. Tree verified GREEN by lead after the second kill: `TSC_EXIT=0`, `BUILD_EXIT=0`. STILL REMAINING: barrel export for `ChartRevealClip`; `ChartStatFlow`; `ChartBrushSelectionOverlay`/`TrackOverlay`/`Handle`(+`HandleOverlay`); `BarYAxis` component. Resume session `5a4bc0cf-b6f7-49ea-8e50-88fc533cae6e`. **Do not auto-resume — awaiting user direction (D319 §4).**  Tree left GREEN (`TSC_EXIT=0`, `BUILD_EXIT=0`); clean boundary, nothing to revert. **LANDED:** visx passthrough tier (`Pattern*` x4 + `Gradient*` x11 via new `internal/gradients.tsx`); the `Background` collision RULED (marker keeps public name, real painter exported as `ChartBackground` — **P5.4/T-E2 binds the default-export slot to `ChartBackground`, not the marker**); `internal/tooltip-components.tsx` (~800 lines: `TooltipDot`/`TooltipIndicator`/`TooltipBox`/`TooltipContent`/`DateTicker`); `getSeriesMarkerVisualExtent` barrel alias; markers-family component renames routed to P5.6 (DOC-4 not-trivial). **REMAINING:** `ChartRevealClip`, `ChartStatFlow`, `ChartBrushSelectionOverlay`/`TrackOverlay`/`Handle`(+`HandleOverlay`), `BarYAxis`. **DEFERRED QA:** tooltip components have no consumer yet, so no chart renders them and a pixel check is not yet possible. Resume session `5a4bc0cf-b6f7-49ea-8e50-88fc533cae6e` — awaiting user call on resume vs fresh dispatch (D318 §4). **NOT YET SCOPE-ROUTED:** the 6 `DEFAULT_ANIMATION_DURATION_MS` copies + `IndicatorWidth`/`TooltipRow` dedup were verified but not executed. Charter staged at `scratchpad/p52-task.md` carrying **8 up-front corrections** (D304/D310/D312), incl. the `Background` collision reframed as a *public naming* problem not a plumbing one (the real `Background` is already default-exported at `internal/background.tsx:137`; only the barrel omits it) and its cross-package coupling to P5.4's T-E2, plus the D312 caveat that P5.1's 99-absent list is a lead and not a port list. New scope routed in from P5.1: centralize the **six** private `DEFAULT_ANIMATION_DURATION_MS` copies, and resolve the `IndicatorWidth`/`TooltipRow` double definition. — port/wrapper tier, 2 sub-runs: (a) visx Gradient*/Pattern* + presentational components (incl. `Background`, post-P1.13); (b) hook wrappers (excl. useYScale/useAnimatedYDomains → P6.2)
- [x] P5.3 — T-E1c — **CLOSED 2026-08-26 (D326). ALL PARTS DONE. Parts 1, 2, 3, 4A, 4C were completed BY THE LEAD after the dispatch was killed a third time — the third kill ran SOLO (19m49s), which eliminates concurrency as the cause.** Remaining work was audit and classification with zero code, so it was cheaper to do than to keep re-dispatching. Ledger of record: `research/phase-4/wave-tasks/P5-03-ledger.md`. **Part 1: 15 ACCEPT / 1 ROUTE.** The 15 are DOC-4 (provider/context architecture removals) across seven provider+context-value pairs; each was whole-tree grepped, not just barrel-checked, because a barrel miss is not an absence proof (D312) — `ChartProvider` proved to have 2 tree hits that are **both comments** (`candlestick-chart.tsx:158`, `internal/candlestick-focus-strategy.ts:5`). **The 1 ROUTE is `BarDepthProviderProps` and correction-7 caught it: it is RENAMED, not retired** — migrated ships `BarDepthProviderConfig` (`internal/types.ts:287`) behind the **public** `BarDepthProvider` (`children.tsx:80`, barrel `:208`), so the component ships and its props type does not. **The alias is not straight:** legacy's `BarDepthProviderProps extends BarDepthContextValue { children: ReactNode }` **includes required `children`**; migrated's Config does not (the component takes `Config & { children?: ReactNode }`), so a plain `as` re-export would ship a structurally different type and silently fail the parity it exists to restore → routed to P5.4 with the exact shape. **Part 3 (CH17) DONE, and it un-deferred cleanly:** legacy barrel 503 exports vs migrated 427, **154 missing (153 non-empty) / 78 extra**, delta at `ch17-barrel-delta.md`. Split 29 present-in-tree / 124 zero-hit, then the 29 split again — **20 appear ONLY in comments**, so the true tally is **4 real unexported declarations, 5 partial, 144 genuinely gone**. **Bucket 4 (UNACCOUNTED) is ZERO** — every symbol maps to an owner (P5.7 loading ×24, P5.6 brush ×10, P6.1/P6.2 y-domain ×11, P5.5 sunburst breadcrumb ×6, DOC-4 hooks ×12, Part 1 ×15, retired sunburst six) or a doctrine. **Two items genuinely need a lead ruling and are flagged, not buried:** the 7 `*CssVars` symbols (legacy generated CSS custom properties from TS constants, migrated declares them in `styles.css` — an architectural swap of exactly DOC-4's shape but **no doctrine names it**), and `TooltipContentProps` (migrated's is sankey-local at `sankey-chart.tsx:172`, legacy's is general — same name, different scope, so exporting as-is could be wrong). **`useSunburstBreadcrumbItems` being zero-hit independently confirms D323's ruling.** The 78 extras are grouped not enumerated: TanStack-native surface, internals promoted during the port, DOC-9 pilot renames — **an added export cannot break import parity, only widen it**. **Part 2:** next free D-number is **D326**; go-to-plan.md's D240/D243/D244 literals are all stale (D240 consumed at `LOG.md:79`, D243/D244 double-booked across T-E7/T-F2) — read every "Log as D24x" as "take the next free number and say which". **Part 4A:** `lerpGeometry` confirmed **public and correctly excluded** from the retired six (`internal/sunburst-geometry.ts:314` has the export keyword, barrel `index.ts:432`). **Part 4C:** the "99 absent" figure is superseded by Part 3, which asks the same question properly and answers zero-unaccounted; recommend retiring the number from future charters since it reads as a backlog and is not one. **Also resolved a three-way ownership collision the lead created:** `StaticChartPreviewProvider` was simultaneously in this ledger's 16, in its own non-scope note routing it to P5.7, and in a new P5-07 Strand 6. Split by kind and made binding — **the Provider is row 15 here (DOC-4), the `useStaticChartPreview` hook is P5.7's (DOC-10)**. A symbol with three owners usually ends up with none. **Part 4B** (was: **PART 4B IS DONE, GATED AND VERIFIED CORRECT (D324). Parts 1, 2, 4A, 4C remain.** Killed a second time mid-run, but this time it left real code: `internal/chart-focus-kit.ts` now hosts `focusValueKey`/`collectFocusGroup`/`navigationOrder`, the local `valueKey`/`collectPerGroup` copies are gone from all three `*-focus-strategy.ts`, and **all five `collectFocusGroup` call sites pass `sortOthersByY: false`** (bar `:80,:135,:143`; scatter `:44,:52`; candlestick `:50,:58`) where bar and scatter previously passed `true`. This answers the open parity question P4.2 handed forward. **Gates run by the LEAD at 23:50** against current file state: `TSC_EXIT=0`, `BUILD_EXIT=0`, bar n=100 **PASS** (0.0000/0.0000/0.0757/0.1243), scatter n=1000 **PASS** (0.4509/0.0000/0.0779/0.2966), all four hovers `tooltipA=true tooltipB=true`. **⚠ READ D324 BEFORE TRUSTING THOSE GREENS: the gate I mandated cannot observe this change.** `collectFocusGroup` returns `[primary, ...others]` and sorts only `others`; both fixtures have 2 series, so `others.length === 1` and the sort is a no-op. My "crossing-series" requirement was the wrong condition — the right one is **≥3 series**, and **no fixture in the harness has one** (bar 2, scatter 2, line 1, composed 1, barsquares 1; `bardepth`'s "8 series" are all `dataKey="seriesA"` decorative layers over one series). The greens prove no regression, not correctness. **Correctness is established from legacy source instead, lead-verified:** `use-chart-interaction.ts:99-106` builds `yPositions` as a `Record<string, number>` keyed by `dataKey`, iterating `lines` in declaration order — no array is built, so nothing can be sorted — and all eight `.sort(` calls in legacy charts are unrelated (sunburst arcs, pie `.sort(null)` "maintain data order", x-axis tick x-sorts, heatmap column index). **Zero y-sorts exist in bklit.** Follow-ups: `sortOthersByY` is now a **dead parameter** at every call site (flagged, not removed); a ≥3-series fixture is an uncovered gap. Its ledger `research/phase-4/wave-tasks/P5-03-ledger.md` exists (2284 bytes) with **0 filled rows** — the structural fix (create the file first, save after every row) did work: it was on disk at 3 minutes versus 55 minutes of nothing on the previous attempt. (was: **KILLED at 6m (D319), WROTE NOTHING** — it was still evidence-gathering, so there is no partial state to reconcile and a re-dispatch starts clean. It did establish one reusable fact before dying: **D240 is already consumed** (`docs/phase-4/LOG.md:79`, the bench fix), confirming the charter's own staleness warning. Session `2a265f0f-1806-4cdd-9544-27f9c94a9d5c`. Originally **DISPATCHED 2026-08-25 IN PARALLEL with P5.2-resume** (bg `bjkag8epj`, session `2a265f0f-1806-4cdd-9544-27f9c94a9d5c`, Monitor `b6l2qisl8`). Chosen as the only safely-parallelizable Wave-5 package: it is an AUDIT package ("you do not fix anything you find here"), so near-zero barrel footprint, whereas P5.4/P5.6 (default exports, compat renames) and P5.5 (breadcrumb family exports) all need `index.ts` writes that would collide with P5.2's live appends. **FENCED:** may not write `index.ts` at all (reports the export lines instead, lead applies them); may not touch P5.2's live files (`internal/brush-*`, `background.tsx`, `tooltip-components.tsx`, `gradients.tsx`, `ChartRevealClip`/`ChartStatFlow`/`BarYAxis`); and if tsc/build fails ONLY in `index.ts` that is P5.2 mid-edit — re-run once, report, **do not fix**. **⚠ PART 3 (CH17 barrel diff) DEFERRED BY LEAD — NOT an oversight.** The charter specifies it runs after P5.1+P5.2 land; P5.2 has not landed, and a diff taken against a half-written barrel yields a confident-looking "drift" list that is really just P5.2's unfinished work. **Lead must re-scope Part 3 once P5.2 closes.** Parts 1, 2, 4 are in scope; Part 4B (the y-sort fix) is the only code, and its gate REQUIRES a crossing-series fixture (bar n=100 / scatter n=1000) — a non-crossing fixture passes identically whether the fix is right or wrong, so a green gate on one is not evidence. — ACCEPT ledger + CH17 drift audit (runs last of E1 trio). **D266: must NOT label itself D240/D244** — those numbers are stale (D263 §7) and T-F2/P6.3 also claims them; split by content (this = symbol ledger of record; P6.3 = architectural rulings, cross-referencing this). **D267: CH17 is already specified** — `parity.md:193` defines it (audit barrel re-exports, account for every delta) and `wave-tasks/P5-03.md:45` operationalizes it as Part 3; the earlier "too vague" flag is retired.
- [x] P5.4 — T-E2 + T-E7 + Strand 3 — **CLOSED 2026-08-26 (D327, D328). ALL THREE STRANDS DONE, executed by the lead after the dispatch channel's sixth failure made re-dispatch pointless (D326 §3). `TSC_EXIT=0`, `BUILD_EXIT=0`, 11 chart gates.** **Strand 1 — the charter's "9 defaults across 8 files" was wrong in BOTH directions.** Re-derived mechanically: legacy has 79 files with `export default`; 57 of those symbols are public named exports in migrated with no default; filtering to what one-default-per-file actually permits (the file's own namesake) gives **14**, not 9. Added: `AreaChart`, `BarChart`, `CandlestickChart`, `ChartBrush`, `ComposedChart`, `HeatmapChart`, `HeatmapLegend`, `LineChart`, `PieCenter`, `PieChart`, `RadarChart`, `RingCenter`, `RingChart`, `ScatterChart` — each `export default X;` at file tail per the in-tree convention. **The six the charter missed include `BarChart`**, the flagship of the very family Strand 2 is about; had I trusted the list, nine greens would have closed the strand and left BarChart's gap reading as a deliberate exclusion (D325 §2). **The one item the charter called "barrel-wiring, not new code" does not exist:** legacy's barrel has NO `export default` either (it structurally cannot have fourteen), so `ChartLegend`'s existing definition-site default at `internal/chart-legend.tsx:226` **already is** full parity — nothing to wire. Non-namesake consolidated siblings (`Ring`, `RadarArea/Axis/Grid/Labels`, `Line`, `Bar`, `XAxis`, `Grid`, heatmap parts) stay named-only: option 1, generalized, documented as deliberate partial restoration. **Strand 2** — five accepted-but-inert props on `BarChartProps` with the CORRECTED signatures (`squareSnap?: { squareGap: number; groupGap?: number; fit?: boolean }`, not a boolean; `orientation?: BarOrientation` against a real `export type BarOrientation` now in the barrel mirroring legacy `index.ts:38`), following the existing `DOC-9 (B13)` JSDoc convention at `internal/types.ts:320`. **One combined dev warn** (module-scoped `didWarnInertBarProps`, `process.env.NODE_ENV !== "production"`, names exactly which props the caller passed), matching the in-tree idiom at `internal/chart-brush.tsx:29`. **Production silence MEASURED with a control, not assumed:** target string `accepted-but-inert prop` → 0 files in `.next/static` AND 0 in `.next/server`, while control `ts-chart__dot` → 1 file, proving the search can detect a presence. **Also landed the `BarDepthProviderProps` ROUTE from P5.3 ledger row 16** as `extends BarDepthProviderConfig { children: ReactNode }` (required, per legacy `bar-depth.tsx:264-265`) — NOT the plain alias D326 §6 warned would compile while shipping a different type. **Strand 3a resolved AGAINST my own brief (D327).** "Move it to a single shared home" would, taken literally, have collapsed the six copies into `design-tokens.ts`'s identical `REVEAL_DURATION_MS = 1100` and **reversed T-D1/P3.2**. Ruled: two constants that coincide are not one constant — the REVEAL_* pair hand-mirrors upstream TanStack `motion.ts:207,209`, the six copies track legacy `animation.ts:4,6`; each must be free to move without the other. New `internal/animation-defaults.ts` holds `DEFAULT_ANIMATION_DURATION_MS` + `DEFAULT_ANIMATION_EASING`, imported by all six charts and **exported from the barrel** (legacy `index.ts:18-19` parity, a CH17 gap — centralizing and exporting were the same edit). **The easing did NOT centralize and that is the finding:** `REVEAL_EASING` is the `animationEasing` prop default in area/line but the internal reveal ease in composed/bar/scatter, which have **no such prop** — two concepts under one name, so only two of five copies moved. **Strand 3b: my brief's count was under by one** — `TooltipRow` is declared **three** times (`internal/types.ts:366`, `internal/tooltip-chrome.ts:111`, `internal/tooltip-components.tsx:579`), all byte-identical; found only by re-deriving the declaration set instead of trusting notes I wrote hours earlier. All collapsed toward `types.ts` (lower-level: `tooltip-chrome.ts:18` already imports from it, so the reverse is a cycle), barrel `:384` repointed, its now-false "tooltip-chrome owns the runtime TooltipRow contract" comment corrected. **Gates:** line/area/scatter/candlestick/radar/heatmap(52)/pie(1000)/ring(4) PASS, bar(100)/composed(100) PASS. **ring n=1000 FAILS hover-30/70 — PRE-EXISTING, proven by history:** 12:29 → 0.7211/0.7399, 16:16 → 0.7159/0.7396, post-change → 0.7129/0.7398; the first two predate every P5.4 edit and my delta is ≤0.008pp, on the chart D258 already excludes pending P4.3's oscillation fix (`settled` passes at 0.01%, as a purely additive default export should). **Gate validity confirmed by mtime, not by directory name** — QA dirs stamp UTC and read a day stale against local WEST (D324 §5). (was: **Not yet re-dispatched;** previous attempt wrote nothing to `showcase/` in 25 min and was killed mid-run while resolving a defect in my own brief. Session `db4a3c8e-15de-43fd-a222-f230386ec96d`, brief staged at `scratchpad/p54-task.md`. **Charter defect FIXED before re-dispatch (`P5-04.md:61`):** legacy's `squareSnap` is `{ squareGap: number; groupGap?: number; fit?: boolean }` (`bar-chart.tsx:89`), **not a boolean**, and `orientation` takes a **named exported `BarOrientation`** type (`:57`, legacy barrel `index.ts:38`), not an inline union — my brief got both wrong and the executor caught it mid-run by reading legacy, which is exactly what it was told to do. DOC-9's "accepted-but-inert" governs behaviour, not type: a boolean would fail **loudly in consumer builds**, and an inline union breaks `import type { BarOrientation }` while still typechecking. `BarOrientation` independently shows up in the CH17 missing-barrel list, confirming it. **Strand 3 closes the last unowned scope from P5.1, both verified live by the lead:** (3a) `DEFAULT_ANIMATION_DURATION_MS = 1100` is declared privately and byte-identically in **six** chart files (`area-chart.tsx:95`, `bar-chart.tsx:77`, `candlestick-chart.tsx:71`, `composed-chart.tsx:144`, `line-chart.tsx:86`, `scatter-chart.tsx:54`), each used once as the `animationDuration` default — centralize, value must stay 1100; (3b) `IndicatorWidth`/`TooltipRow` are **each defined twice**, byte-identical, at `internal/types.ts:364`/`:366` **and** `internal/tooltip-chrome.ts:92`/`:111`. **The collapse direction is the non-obvious one and the lead checked it:** `tooltip-chrome.ts:18` already imports `ChartTooltipPoint` from `./types`, while `types.ts` imports only React/d3-shape/`./fade-mask` — so `types.ts` is the lower-level module and is canonical; collapsing the other way would invert the dependency into a cycle. Barrel `index.ts:384` must repoint from `./internal/tooltip-chrome` to `./internal/types`, and the now-false comment at `:382` updated. A duplicate `export type { TooltipRow }` in this same barrel was previously caught by `npm run build` and **missed by `npx tsc --noEmit`**, so the build is the gate that matters here.)
- [x] P5.5 — T-E3 + T-E6 — **CLOSED 2026-08-26 (D329, D330, D331, D332). ALL THREE STRANDS DONE, lead-executed (dispatch channel down, D326 §3). `TSC_EXIT=0`, `BUILD_EXIT=0`.** **Strand 1 — SEVEN charts, not the charter's four:** the list names line/bar/composed/scatter and silently omits `area`, which carries the same code (D329 §1 — a new premise-rot mechanism: a charter list wrong purely by OMISSION, undetectable by re-reading the list). Gates: line 0.1243% · area 0.1810% · composed 0.1478% · bar n=100 0.1593% · scatter 0.4509% · choropleth n=100 0.1470% · sunburst 0.1466%/0.1046%, all PASS. **Strand 2 (K4) — re-scoped, both stated premises false:** the charter's "structurally cannot express a keyframe tween" was wrong (five families already did), and `candlestick-chart.tsx`'s own comment claiming bklit has no `enterTransition` prop was wrong (bklit declares it at `candlestick-chart.tsx:54,84`, consumes it at `candlestick.tsx:239,400`). The real defect was a PRIVATE spring-only type silently coercing tween callers; fixed by aliasing to the shared `EnterTransition` and routing the tween through the shared sampler, spring branch untouched. **Gated by DOM readback (`qa/k4-tween-probe.mjs`), not pixels** — framer-rAF vs WAAPI do not share a start instant, so a mid-flight diff measures scheduler skew (D330 §2). **Strand 3 (SB4/SB8) — plus one unbriefed defect:** SB4 restored the hint's `ReactNode | ((ctx) => ReactNode)` children contract (the old `ReactNode`-only type dropped function children silently); SB8 ported `SunburstBreadcrumb` + `useSunburstBreadcrumbItems` keeping bklit's `children` shape (D332 §2 refines D323's `items`+`onNavigate` sketch, which would itself have broken the swap); and wiring the breadcrumb exposed `isChildOfKind`'s `typeof child.type === "function"` guard, which made **every memoised carrier silently unclassifiable** (D332 §3). New `sunchrome` scenario pair + `qa/sb-chrome-probe.mjs`: sunchrome n=27 PASS (max 0.1676%), sunburst n=27/33 re-gated PASS, probe PASS on all four states with all four presence assertions firing (D331).
- [x] P5.6 — T-E4 — **CLOSED 2026-08-26 (D333–D336). ALL SIX STRANDS DONE, lead-executed (dispatch channel down, D326 §3). `TSC_EXIT=0`, `BUILD_EXIT=0`.** **S1 CH2** — type-alias re-export in the barrel, no second implementation (typecheck-level). **S2 ring R2/R3/R4** — `useRingStable` regained `animationKey`/`isLoaded`/`containerRef`; new `useRingHover()` binds the imperative coordinator to React via `useSyncExternalStore` and returns legacy's `{hoveredIndex, setHoveredIndex}`; `useRing()` spreads both. It routes through `requestHover`/`requestUnhover`, never `setHovered` — the latter mutates the store without firing `onHoverChange`, so a legacy-shaped consumer would silently break the callback contract. P4.3's oscillation fix was **already present**; R2 did not re-add it. **⚠ The charter's ring n=1000 gate is an INVALID INSTRUMENT (D335)** — 3/3 runs FAILED deterministically (settled 0.7149/0.7164/0.7170%, hover 0.7399/0.7341/0.7454%); a disable-probe with the R2 state machine off reproduced it numerically unchanged (0.7162/0.2279/0.7394%), and the D331 control settled it: **`--self-test` bklit-vs-bklit at n=1000 also FAILED** (0.2263%, `tooltipA=false tooltipB=true`). At that density the rings are sub-pixel and the hit-test picks a different arc run to run (bklit hovered "Ring 1000", migrated "Ring 423"). **Gate of record: ring n=4 PASS, max 0.3128%.** **S3 choropleth CP9/CP10** — CP9 widened `ChoroplethContextValue` from `{width,height}` to 14 fields (`features`, `featureCollection`, `pathGenerator`, `rawPathGenerator`, `projectPoint`, `width`, `height`, `innerWidth`, `innerHeight`, `margin`, `containerRef`, `isLoaded`, `animationDuration`, `enterTransition?`, `revealEpoch`), `isLoaded`/`revealEpoch` newly plumbed as real state, wired into **both** provider sites; **CP10 was purely TYPE widening** — visx's `Zoom` render prop already hands `ProvidedZoom<E> & ZoomState`, so `initialTransformMatrix`/`transformMatrix`/`isDragging` were live at runtime and had merely been narrowed out by a cast. `featurePaths` deliberately NOT restored (ACCEPT — no consumer, and `geoPath` already covers it). Gate: choropleth n=100 PASS 0.1470%. **S4 `PieCenterShell`** — P4.3 HAD landed, so this was implemented, not deferred. Ported over migrated's split contexts (two providers where legacy has one: `PieStableContext` + an inert frozen `PieHoverCoordinatorContext`), reusing `center-stat.tsx`'s `useIntroFlowValue` instead of shipping a second copy of the double-rAF entrance, and collapsing legacy's two d3-`pie()` branches into the one hand-written arc they both reduce to at n=1. **Gated by mounting the shell on BOTH impls, then reading the captures per D331 — which caught a defect the passing gate could not (D336):** migrated's center typography is scoped under `[data-bkm-chart]` in `styles.css` while legacy's rides on Tailwind utilities, so a **standalone** shell lost every rule and rendered plain ~16px text against bklit's bold ~30px. The gate PASSED at 0.0850% with that defect live — two text blocks are ~800 of 960,000 px. Fixed with a `display:contents` `data-bkm-chart` wrapper (zero layout contribution); re-gated **PASS 0.0230% settled / 0.0152% hover**, a 3.7× improvement, diff PNG down to sub-threshold antialiasing. Gate vehicle reverted from both pie scenarios. **S5 `MarkerTooltipContent`** — barrel-line addition only; no `useActiveMarkers` re-export duplicated from P5.2. **S6 BR3** — EXTENDED, not ACCEPT-documented; the widened `selectedBoxStyle` surface was gated by patching both brush scenarios with an identical non-default style (brush n=1000 default PASS 0.0928%, styled PASS 0.1057% max across 5 captures), then reverted. **NOT fixed, routed forward:** ring's `isRingElement`/`isRingCenterElement` still carry the `typeof child.type === "function"` guard that silently drops `memo()` carriers — the **third** instance of the D332 §3 defect class (sunburst's was fixed there, SB4's was the second).
- [x] P5.7 — T-E5 — **CLOSED 2026-08-26 (D337–D341). ALL SIX STRANDS DONE, lead-executed (dispatch channel down, D326 §3). `TSC_EXIT=0`, `BUILD_EXIT=0`.** **Strand 5 produced the segment's biggest finding and it is not about loading at all — the QA harness is structurally BLIND to hairlines (D337).** `qa/screenshot.mjs:147-149` runs `pixelmatch(..., { threshold: 0.1, includeAA: false })`, and a 1-px-shifted or half-intensity gridline is exactly what its antialias detector discards. Proven with a D331 control rather than argued: re-diffing the SAME `settled-a/b.png` from a PASSING run, AA-inclusive, gave **3856 px (0.4017%) against `report.json`'s `diffPixels: 0, pass: true`**. Standing consequence: for any finding about LINES, a harness PASS is not evidence — run the raw diff alongside. **`includeAA` was deliberately NOT flipped globally**: every historical baseline is calibrated to it. **Strand 5 itself: the y-POSITIONS were already exact** — a DOM probe shows both impls emitting five y-gridlines at IDENTICAL y (138.33/231.25/324.17/417.08/510.00). What was wrong was rendering, in two ways the gate had never reported. **(1) D338 — missing `shape-rendering: crispEdges`:** visx `GridRows` carries it by default, TanStack's native grid lines do not, so every migrated gridline antialiased across TWO device rows at half intensity where bklit paints ONE crisp row (line n=1000, y=417.08: bklit row 417 `#E3E3E3` with 416/418 untouched, migrated 416/417 `#EDEDED`/`#EBEBEB`). **(2) D339 — no edge-fade mask at all:** bklit `grid.tsx:180-201` masks the horizontal rows with a left→right gradient (0/10/90/100%), migrated ran full-bleed; fixed with `mask-image` on `.ts-chart__grid`, guarded `:not(:has(line[data-ts-key^="x-grid"]))` because TanStack emits both axes into ONE `<g>` while bklit's `fadeVertical` defaults FALSE. Whole-frame raw AA-inclusive diffs: **line 0.4017% → 0.0467%, area 0.3560% → 0.0915%, bar 0.2747% → 0.1020%**. Regression sweep: **all 19 cartesian charts re-gated PASS**; scatter's chronic settled moved 0.4509% → 0.4510%, i.e. not at all. Accepted residual, measured not assumed: a CSS mask composites the group in its own raster pass and a gridline whose y lands mid-pixel loses ~5/255 there (416 px) — against a full-strength edge overshoot ~10× larger that the mask removes. `mask-clip: no-clip` was tried, moved it by ZERO, and was removed rather than left in as a no-op. **D340 is OPEN and deferred WITH a diagnosis**, not waved through: migrated paints a y-gridline on the plot's bottom edge (row 510, 448 px) that bklit's mask + `crispEdges` erase as an emergent side effect; the honest fix needs `axis.ticks.values` threaded from each chart's resolved `yDomain` across five charts, where a one-off divergence in tick generation would MOVE every gridline — it earns its own task. **Strand 1 (A10/B14) — the charter's demand for DEDICATED preset scenarios was right and it paid (D341).** `{bklit,migrated}-arealoading` + `{bklit,migrated}-barloading` render the PRESET COMPONENTS, not the deliberately-inert `state="loading"` prop (registration needs BOTH `scenarios/index.ts` and `bench/query.ts`'s `ChartKind` **and** its `chart !== …` chain, or the page throws). **A10 `arealoading` PASS 0.0273%.** **B14 `barloading` 15.0008% → 4.5259%**: fill was near-white against bklit's `var(--foreground)` @ 0.45, heights were a rising sine against bklit's deterministic `hashFract` silhouette, `barGap` was 0.2 against `DEFAULT_BAR_FRACTION 0.7`, corners were bandwidth-derived against `rx=2`. A fifth defect — stray gridlines bklit's skeleton does not draw — was **invisible to the gate** (removing them moved it 4.5265% → 4.5259%) and needed `<Grid horizontal={false} />`, because dropping the child is not enough: `internal/grid.ts:39` defaults `horizontal` to TRUE. The remaining 4.53% is irreducible without new API surface and both halves trace to composing the real `<BarChart>` rather than resurrecting the DOC-10-deleted `BarLoadingSkeleton` — a uniform 100/90 vertical stretch from d3 `.nice()` (no fixed point exists) and an 11 px inset from `.padding()` setting paddingOuter to paddingInner. **Strand 6 — seven DOC-10 symbols re-confirmed ACCEPT-deleted**, each with its doctrine: `LineLoadingSweep`/`BarLoadingSkeleton`/`generateChartSkeletonData`/`getSkeletonHeights`/`GenerateChartSkeletonDataOptions` (deleted surface, geometry ported INTERNAL where load-bearing — `buildLoadingSkeletonRows`, now `loadingSkeletonBarHeights`), `useStaticChartPreview` (DOC-10 per D326 §5's three-way ownership split), `StaticChartPreviewProvider` (**no backing implementation exists on either side**). FD7's sweep glint stays ACCEPTED — it was NOT resurrected. **FD5 (loading-label styling) stays open and is now QUANTIFIED rather than asserted: ~3400 px ≈ 0.35%**, localized to rows 281-292, which is the ENTIRE remaining loading-state red on line and area. `research/phase-4/wave-tasks/P5-03-ledger.md` was NOT written to.
- [x] P5.8 — T-E8 — **CODE LANDED (D278), lead-executed. Sweep GREEN: tsc 0 / build 0, pie settled 0.0108%, all hovers 0.0000–0.0007%** — the chronic ~3.18% pie hover stays retired. **Resize spot-check MOVED to P4.3, not dropped:** `qa/screenshot.mjs` has no resize state (only `settled`/`hover-N`/`loading`), so verifying behavior *during* the debounce window needs a bespoke harness — and the identical pattern in radar + ring means that harness should be built once and rule all three, in the package that already touches ring. Pie P9 swapped: `pie-chart.tsx:243` → `useDebouncedContainerSize(containerRef)`, import `:75`. Gauge G5 re-confirmed already done (`:383`/`:990`). `enabled`-param gap ruled harmless (containerRef on both branches `:757`/`:774`; `size = fixedSize ?? …` never reads it in fixed mode) — shared hook signature deliberately NOT changed. **Open finding for P4.3:** `radar-chart.tsx:220` + `ring-chart.tsx:260` still carry the identical undebounced `useMeasuredRect(containerRef, !fixedSize)` pattern — fix-or-ACCEPT unruled. Original charter text below.
  - **pie P9 only (D267)**. Gauge's G5 half **already landed in Wave 1** via T-W1-14: `gauge.tsx:151-152` imports `useDebouncedContainerSize`/`useDebouncedContainerWidth`, used `:382`/`:984` with explicit "G5 (bklit ParentSize debounceTime={10})" comments — do not re-implement. Pie's half verified genuinely open (`pie-chart.tsx` has no `useDebounced*` usage at all).

### Wave 6 — multi-axis + closing
- [x] P6.1 — **CLOSED 2026-08-26 (D342–D346).** Lead-executed, six clusters, each with its own control-bearing gate. **(1) Final public shape of the scale layer — this is P6.2's contract and I consider it STABLE/FINAL**, `internal/y-domain.ts`: `type YDomain = [number, number]`; `resolveTimeSeriesYDomain(data, series, yScaleDomainMax?): YDomain`; `useNicedYDomainChanged(yDomain): {niced, changed}`; `createNicedYScale(yDomain)` + `type NicedYScale = ReturnType<typeof createNicedYScale>`; `resolveYDomainsByAxis<T extends YAxisSeries>({series, resolveDomain, ensureDefaultAxis?}): Record<string, YDomain>`; `createNicedYScalesByAxis(domainsByAxis): Record<string, NicedYScale>`; `getPrimaryYScale(yScales, fallback): NicedYScale`; `createAxisValueProjector(domainsByAxis, primaryDomain): (axisId?) => (value: number) => number`; `domainForAxis(domainsByAxis, axisId): YDomain`; `useYScale(domainsByAxis, innerHeight, yAxisId?): NicedYScale`; `domainsEqual`, `shouldTweenYDomain`, `isYDomainTweenPhase`, `resolveAnimatedYDestinationDomains(chartPhase, skeletonByAxis, targetByAxis)`. Stable because the shape is forced by two constraints that will not move: `resolveDomain` is a **callback**, so each chart family keeps its own legacy domain rule (bar's `[0, (max||100)*1.1]`, scatter's D14 max-floor) instead of the layer growing per-chart branches; and `createNicedYScalesByAxis` deliberately **sets no `.range()`** — TanStack applies the margin-inclusive range, so a ranged scale here would be silently overwritten on the single-axis path and silently authoritative elsewhere, two behaviours from one builder. P6.2 consumed it unchanged and needed no wrappers, which is the practical confirmation. **(2) Staged-gate order: followed as listed** — scale layer alone first, then line → area → bar → scatter → composed → reference-area, each gated before the next started, no deviation. **(3) Per cluster.** *Line (L10, D342):* per-axis projector into the marks memo; new `linemultiaxis` fixture (two `<Line>` on two `yAxisId`, `seriesC = seriesB/50` so the axes differ 50×) PASS 0.0000/0.0000/0.1249/0.1794, control `projectorFor(undefined)` FAIL 0.6690%. *Area (D343):* `yAxisId` restored to `ResolvedArea` (it was being dropped), projector applied to BOTH the `areaFill` and the boundary `lineY` or the fill tears from its own outline; `areamultiaxis` PASS 0.0007/0.0003/0.0842/0.1869, control FAIL 2.7039%. *Bar (B7+B10, D343):* `yAxisId` on `BarConfig`/`BarSquaresConfig`; projector resolved **once per dataKey into a map**, not once per mark call, because FOUR mark families read the same series (`barY`, `barSquaresMark`, `barColumnTrackMark`, the two `barDepth*` faces) and two disagreeing would be a silent tear, not a type error; `barmultiaxis` n=100 PASS 0.0000/0.0454/0.0953/0.1218, control FAIL 6.2226%. *Scatter (S6, D344):* `yAxisId` on `ScatterConfig`, both y readers projected (`createYGradientScatterMark` 4th param and the `dot()` accessor), D14 rule passed in as `resolveDomain` rather than replaced; `scattermultiaxis` PASS 0.2898/0.0000/0.0779/0.2211, control FAIL 14.9969%. *Composed (C13+C14, D344):* `yAxisId` on `ComposedSeriesEntry`, merged by `upsertComposedSeries`, passed by `extractComposed` for the `area` and `line` roles; `composedmultiaxis` PASS 0.0890/0.0478/0.1253/0.2209, control FAIL 6.2183%. **`<SeriesBar>` gets NO `yAxisId` deliberately** — bklit's has no such prop and its `tryAppendSeriesBar` omits it where `tryAppendLine`/`tryAppendArea` pass it, so a bar always paints on the primary scale. *Reference areas (RA2, D345):* `<ReferenceArea yAxisId>` already reached `ReferenceAreaLayerProps` and was simply **never read**; now resolves via `domainForAxis(yDomainsByAxis, normalizeYAxisId(yAxisId))`, matching bklit's `useYScale(yAxisId)`, with `yDomainsByAxis` optional so single-domain callers are byte-unchanged; `refareamultiaxis` PASS 0.1983/0.1827/0.2800/0.3768, control FAIL 32.2364%. **(4) AX7 — found ALREADY CLOSED on inspection, not newly fixed.** `createTickColorResolver(configs, domain, axisId)` (`internal/reference-area-geometry.ts:142`) filters candidate bands on `normalizeYAxisId(area.yAxisId) !== axisId` before testing containment, mirroring bklit `y-axis.tsx:48-74`, and `axisId` is a **required** param on purpose — a silent default to the primary axis is exactly how a resolver ends up answering for the left axis when called for the right. All three call sites confirmed passing an axis id: `line-chart.tsx:1001`, `area-chart.tsx:1084`, `live-line-chart.tsx:708`. **live-line's own scale logic was not touched** — it still builds `yDomainLive`/`refConfigsLive` itself at `:707-708` and only hands them to the resolver. **(5) AX4 — re-added and gated (D344).** Composed's inlined `if (stackedMax != null && stackedMax > 0) return [0, stackedMax * 1.1]` now goes back through `resolveTimeSeriesYDomain`'s `yScaleDomainMax` param (`internal/y-domain.ts:53-55`, value-identical, same `> 0` guard) — but **now gated on `usesDefaultAxisOnly(composedSeries)`**, which is what legacy `time-series-chart-shell.tsx:214-226` does and what an inlined branch at the call site cannot express: a stacked total summed across all series is meaningless once some live on another axis, so multi-axis composed falls through to the per-axis scan. **(6) Regression sweep + bench.** Sweep all PASS: `line`/`area`/`composed`/`composedstacked`/`scatter`/`refarea` n=1000, `bar`/`barsquares`/`bardepth` n=100, plus `candlestick`/`liveline`. Bench = **8 exclusive paired cells** (`BENCHMARKS.md` §P6.1), **not void** (`bklit/bar` M1a −3.9%, M3a +0.6%; `bklit/scatter` M1a −1.2%). line and bar: no regression, migrated ahead on mount (M1a 51.7 vs 59.9; 36.8 vs 68.6), M3a at parity. **`migrated/scatter` M3a 31.5 vs a 32.5 control — parity, closing D289's 67.5-vs-32.6 (2.07×) watch, a 2.1× improvement.** Composed M3a 30.6 vs 21.2 (1.44×), down from Wave 1's 55.6-vs-21.1 (2.63×); **30.6 replaces 55.6 as the carried-forward watch**. **(7) Four things contradicting the brief, stated plainly.** *(a)* **The six-clusters-in-one-package shape was RIGHT, not a mistake** — the clusters share one contract and splitting them would have meant six separate revert-and-regate cycles against a layer still in flux; reported per the brief's instruction rather than silently re-split. *(b)* **AX4's behaviour was never missing** — the brief says "re-add `yScaleDomainMax`", but the `[0, max*1.1]` short-circuit was live all along as an inlined branch. What was genuinely missing is the **`usesDefaultOnly` gate around it**, which is the part that actually matters for multi-axis. *(c)* **AX7 needed no work at all**, contra the brief's "AX7 per-axis filter" framing; the parity row's "ACCEPT single-axis reality (revisit with L10 ruling)" had already been superseded. *(d)* **Legacy `wrapSingleYScale`'s comment is stale** and `reference-area-config.ts:24` is a **fourth instance of the D332 §3 silent-drop class** (`typeof child.type === "function"`) — noted, not fixed here. **Plus two findings the gates could not have produced.** *(i)* A **latent bar-tooltip defect** found by grepping `p.yValue` consumers: bar was the only chart reading `p.yValue` for a tooltip ROW, and `yValue` is scale-space once projection is live — correct for placing chrome, wrong for a displayed number, so a right-axis bar showed its reprojected value. Fixed to read the raw datum. A wrong four-digit number moved the frame by **0.045pp**, far under the 0.5% gate: D337 again. *(ii)* **The `refareamultiaxis` fixture took three attempts to become evidence** (D345) — a whole-plot band painted in the WRONG PLACE was invisible to the gate at attempts 1 and 2, because `<ReferenceArea>`'s default fill is a 12% `color-mix` and pixelmatch at `threshold: 0.1, includeAA: false` discards it. Only an explicit `fill="var(--chart-3)" fillOpacity={0.5}` moved the control to 32.2364%. **D337 extends to large low-contrast FILLS, not just hairlines and small text**, and the existing `refarea` scenario inherits that blindness — its PASS constrains band geometry far more weakly than the number suggests. Also fixed en route: **bar, scatter and composed were handing the reference-area layer the UN-niced domain while their marks paint niced**, misplacing every reference area by the nicing delta. Original charter text follows — T-F1 — multi-axis `yAxisId` (DOC-8): scale-resolution layer first, then 6 consumer clusters, AX7 per-axis filter, AX4 re-add
- [x] P6.2 — **CLOSED 2026-08-26 (D347, D348).** Lead-executed. The narrowed scope was "verify `xForDate` and close" — run as a real audit, it found **two live defects and one harness defect**, so it closed as a fix, not a rubber stamp. **(1) `xForDate` was never centralized and had SIX divergent copies**, all in `line-chart.tsx` plus its overlays; canonical is now `internal/x-time-scale.ts` (`timeToPixelX(date, minTime, maxTime, innerWidth)`), fed by a single `timeExtentRaw`/`timeExtent` memo pair that the spec's own `xScale.resolve` now reads too (the sixth copy, consolidated last and re-gated byte-identical → true no-op). Bodies had **diverged**, not merely duplicated: the terminal marker computed its extent from the unfiltered `data` while bklit's `visiblePlotData` (`time-series-chart-shell.tsx:418`) is the xDomain-FILTERED slice — a real parity bug, now fixed. bklit has exactly ONE x scale per chart (`shell:284-301`; when `xDomain` is set the domain IS `xDomain` and `mergeProjectionXDomainMax` is deliberately SKIPPED) and the migration now mirrors that shape. **(2) reveal-constant recheck: CONFIRMED EMPTY** per D329 §2 — B1/C3/S1's `animationEasing` prop retired all three `REVEAL_EASING` copies into `internal/animation-defaults` during P5.5; nothing left to do, and per D327 the surviving constants were correctly NOT aliased. `DATA_TWEEN_MS`/`SERIES_MARKER_ENTER_MS` were centralized as byproduct. **(3) T-E1b `useYScale`/`useAnimatedYDomains`: no wrappers needed** — P6.1 landed the scale layer with the hooks already at their final public shape; API matches. **(4) D331 control armed and it MOVED**: re-arming the legacy extent gave settled **0.3182%** vs **0.0723%** fixed = **4.4×** — above the 0.1% determinism floor, below the 0.5% gate, so recorded honestly as *a detector, not a tripwire*. **New fixture** `projectionxdomain` (`X_DOMAIN_FRACTION=0.3`, 600 horizon points, fat 10px projection per D337) built specifically because no existing fixture set `xDomain`. **Its first run failed the tooltip assertion on B only — and the chart was innocent (D348).** `qa/screenshot.mjs:458` selected `#chart-root svg` with `.first()`; migrated renders a **0×0 clipPath-defs `<svg>` BEFORE the chart `<svg>`** whenever `needsBrushClip` (`line-chart.tsx:1045`), so every hover point landed at the container origin. Harness now takes the LARGEST `#chart-root svg` by area — which the brush capture path at `:397` already did, and whose comment named the hazard. Impl-agnostic fix; the three chart-specific probes at `:485/:520/:559` still use `querySelector` but none of those charts sets `xDomain`. **GATES: 13 charts PASS** (line/area/composed/projection/profitloss/linemultiaxis/areamultiaxis/composedmultiaxis/refarea/refareamultiaxis/projectionxdomain n=1000, markers n=100, brush n=1000), every hover `tooltipA=true tooltipB=true`; `npx tsc --noEmit` rc=0 from `showcase/` and bench build rc=0 after every batch. **BENCH: 6 exclusive paired cells** (line/area/composed × bklit-control/migrated) under D273 conditions — see `BENCHMARKS.md` §P6.2. **Per D273 the M1a channel is VOID for this run**: the `bklit` control itself moved +8.0% (line 59.9→64.7) and +8.2% (composed 74.6→80.7), at the ±8% threshold the P6.1 section names as voiding. All other control channels held (M1b +0.5%/+1.9%, M3a −0.6%/+0.5%, heap identical), and on those valid channels migrated tracks P6.1 almost exactly (line M1b 1117.0→1116.1, composed M1b 1665.5→1672.3, M3a 30.6→31.3) — **P6.2 is flat, which is what a pure de-duplication should read as.** `area` is a first-time paired cell with no before-number, recorded as a new datum. **Carried forward and since EXECUTED (D356):** `centralize 5` (33 inline `dataset.bkmRevealed` sites across 9 files, needing an un-stamping affordance for `sunburst-chart.tsx:1021`) — it was scoped here on the reasoning that it must run after P5.5, which holds, but it is a 9-file rewire with its own gate surface and did not belong bolted to a scale-layer close-out, so it ran as its own task immediately after this one. See the P6.3b line below. Original charter text follows — deferred scale-adjacent items **(EASING HALF NOW EMPTY — D329 §2: B1/C3/S1 added the `animationEasing` prop that D327 §3's deferral said did not exist, so all three local `REVEAL_EASING` copies retired into `internal/animation-defaults` during P5.5. What remains is T-C10's `xForDate` helper alone; verify and close.)** — original scope below — T-C10 xForDate helper + reveal-constant tokens recheck **(SCOPE NARROWED AND CLARIFIED 2026-08-26, D327 — read before starting: the duration half is DONE and OUT of your scope. P3.2's byproduct (a) treated the local `DEFAULT_ANIMATION_DURATION_MS` + `REVEAL_EASING` pair as one item; D327 ruled they are two. The DURATION is a legacy-provenance PROP DEFAULT, now centralized in `internal/animation-defaults.ts` and barrel-exported. What remains yours is the EASING, and only three copies: `composed-chart.tsx`, `bar-chart.tsx`, `scatter-chart.tsx` — none of which has an `animationEasing` prop, so their `REVEAL_EASING` genuinely IS `design-tokens.ts`'s `REVEAL_EASE_CSS` concept and is the real T-D1 question. area/line's two copies are NOT: those were the prop default and have moved. Do NOT alias the survivors to `animation-defaults` — that would re-couple upstream-tracking and legacy-tracking constants that D327 deliberately separated.)**; T-E1b `useYScale`/`useAnimatedYDomains` wrappers (post-T-F1) **+ `centralize 5` (D315): converge ~30 inline `dataset.bkmRevealed` sites across 9 files on `deferred-reveal.checkRevealGuard`, plus the unwritten svg-root variant helper for ring/radar/sunburst/choropleth.** Assigned here rather than to a Wave-5 package **because it must run after P5.5**: T-E3 rewires the reveal contract on L2/B3/C4/S3/CP2/SK6, and P4.6 has already added a third guard shape (`hasRevealedRef` boolean + deadline in `scatter-chart.tsx`, D311). Centralizing first would converge on a contract about to change. The helper must expose **un-stamping** — `sunburst-chart.tsx:1021` does `delete svg.dataset.bkmRevealed` and `checkRevealGuard` has no such affordance today.
- [x] P6.3 — **CLOSED 2026-08-26 (D349–D354).** Lead-executed. **The charter's D-numbers were stale and the method had to change first:** D240 is occupied (`LOG.md:79`, the bench fix) and **D241–D245 were never written at all**, so "log D240–D245" was un-executable as literally specified. Per D266 the packages were split by CONTENT, not by number — this one owns the *architectural doctrine rulings*, P5.3's ledger owns the *symbol* accounting — and every entry was located by searching the log for its subject matter rather than for its suggested number. **Six doctrine rulings logged, all ACCEPT, each cited to its `go-to-plan.md` line: D349** DOC-4 context/provider removals (`:13`); **D350** DOC-6 event-model modernization (`:15`, open question parity-11 at `:35`) — noted as **the only one of the six with zero prior occurrences anywhere in the log**, i.e. genuinely unrecorded rather than merely un-numbered; **D351** DOC-7 `enterTransition` structural reshape, accepted family-wide with candlestick K4 as the stated exception (`:16`); **D352** DOC-9 bar-pilot scope cuts B4/B5 with dev-only `console.warn` (`:18`), already executed at D328/P5.4-Strand-2 — and its corroboration recorded **precisely**: the `composedstacked` fixture named in D247 gates *composed's* stacked branch (C5, a FIX), **not** bar's B5 cut, a conflation that would have manufactured evidence for a cut that has none; **D353** DOC-10 loading family (`:19`), ACCEPT half verbatim, corroborated at D257 §6 / `go-to-plan.md:256` / D341 / D266(a). **D354 closes the four Part-A orphans:** AX5 RESOLVED (`PROGRESS.md:53`; line n=1000 + legend n=4 both 0.0000%); LM1+LM13 RESOLVED (`:52`); C15 resolved by D347 above; **C11 needed no new row** — it is already fully logged at D265 §3 + D269, and writing a duplicate would have inflated the ledger with a re-statement. **D354 also carries a CORRECTION the audit turned up:** `PROGRESS.md:48` says radar's RD6 shipped the "(fallback ACCEPT+D245)" — **it did not; the PRIMARY transform-tween path shipped.** `radar-chart.tsx:663-696` implements legacy's spring (stiffness 80 / damping 15 / mass 1) sampled through `sampleSpringProgress` into WAAPI keyframes on `transform: translate(-(1-p)*x, -(1-p)*y)`, delay 0, settle window folded in at `:538-550`. There is therefore **no D245-equivalent ACCEPT to log** — the thing the charter asked to be recorded never happened. **ACCEPTs with no evidentiary support: none survived** — every one of the six is either gated, executed, or cited to source; the one that came closest (DOC-6) is supported by an open question that is explicitly still open, and is recorded that way. **Final D range: the log now runs through D354.** **Zero code changed.** (was: T-F2 — D-log entries D240–D245 + every unlogged ACCEPT row incl. Part-A orphan dispositions (C11/C15/AX5/LM1+LM13))
- [x] P6.3b — **CLOSED 2026-08-26 (D356).** Lead-executed. `centralize 5`, carried out of P6.2 into its own task. 33 raw `dataset.bkmRevealed` sites across 9 chart files converged onto primitives in `internal/deferred-reveal.ts`: `isRevealed` / `markRevealed` / `clearRevealed` / `findRevealRoot`, over a new `RevealRoot = HTMLElement | SVGElement` type; `checkRevealGuard` rebuilt on top of them. **Primitives, not one combined function, and the call sites say why:** `checkRevealGuard` already existed and was almost unused because it reads and stamps in ONE step, while nearly every chart must read first (`if (revealed && !keyChanged) return;`) and stamp only on a specific branch — stamping on the read would mark charts revealed that then decided not to animate, a behaviour change dressed as a refactor. **The rewire uncovered a latent dead-guard class:** `.ts-chart__marks` is an `SVGGElement`, which carries `dataset`/`classList` via the `HTMLOrSVGElement` mixin but fails `instanceof HTMLElement` — so every `marksGroup instanceof HTMLElement` guard inside `runDeferredReveal` was permanently false and **the stamp was never actually written**. Widening the type made the latch work for the first time, which then required a new epoch-advance `clearRevealed` before the guard so a heatmap epoch bump is not vetoed by the previous epoch's now-real stamp. **This is therefore not a pure refactor: it changed behaviour, because the code it centralized had never run.** **GATES: `tsc` rc=0, bench build rc=0, 10-chart QA board all PASS** (line/area/composed/scatter/candlestick n=1000, bar n=100, sunburst n=27 **and** 33, choropleth n=100, heatmap n=52) — heatmap `0.4277/0.3466/0.3533/0.4256` matches D250's F3 closing gate to four decimals and sunburst `0.0599` matches D281 at both densities. One anomalous reading (area settled 0.1286%) was chased down and is **capture variance, not a defect**: two gate batches were sharing the machine on ports 5196/5198; the solo re-run returned exactly `0.0003%`, P6.2's recorded number. **D331 control run — and it came back NEGATIVE.** With `isRevealed` stubbed to `return false` and `markRevealed` stubbed to a no-op — the latch fully disabled, reveals free to re-fire on every re-render — line/heatmap/sunburst reproduced **all twelve numbers identical to four decimals**. **The PASS board is therefore evidence that nothing else regressed, and is NOT evidence that the latch works.** Per D331, a gate that cannot detect the presence it seeks is not evidence. The blindness is structural, not a threshold problem: the reveal is a sub-frame WAAPI transient and the settled screenshot is taken after it completes, so no `pixelmatch` tuning would recover it. Recorded as **unverified-by-harness** rather than restated as a pass.
- [x] P6.4 — **CLOSED 2026-08-26 (D358).** Lead-executed. The 4.4.5 final pass, all three halves. **(1) Builds: `npx tsc --noEmit` rc=0 from `showcase/`, `npm run build` rc=0 in `showcase/`, bench app build rc=0.** (2) **Parity spot-check: 45 runs — every one of the 43 registered chart kinds at its mandated density, plus `ring` n=4 and `sunburst` n=33 as second densities. 41 PASS / 4 FAIL as captured; 42 PASS / 3 known-non-parity after one harness fix.** **All three 4.4.1 baseline gate failures are RESOLVED:** `pie` n=1000 — the phase's longest-standing unowned debt, hover-30/70 ≈3.18% since 2026-08-21 — reads **0.0138 / 0.0000 / 0.0000 / 0.0007 PASS**; `choropleth`'s 1.5345% hover-30 (migrated tooltip absent) reads **0.0000% with both tooltips asserting** at n=100; `heatmap`'s 0.5196/0.5218/0.5945% reads **0.3466/0.3533/0.4256%**. `sankey`, deferred out of the baseline at 2.84/1.94/1.43%, reads **0.3835/0.2782/0.1323%** at n=33. **(3) Each of the four FAILs was triaged with its own control, and only one was a real harness defect.** *`arealoading`* — **HARNESS GAP, fixed.** All four captures PASSED (≤0.0640%); the `overall: FAIL` was the tooltip assertion alone, firing **symmetrically on A and B**, which is the signature of a harness gap rather than a chart defect. The D331 control settles it: `--self-test` (bklit vs bklit) scores **0.0000% on all four captures and still reports `overall: FAIL`** — an instrument failing against itself. A loading skeleton has no data behind it, so neither impl renders a tooltip. `arealoading` and `barloading` added to `TOOLTIPLESS_CHARTS` in `qa/screenshot.mjs`; **the pixel gate is untouched and remains the real check for both**. Re-gate PASS. *`barloading`* — **not a parity gate by explicit prior design**, stated in `bench/app/src/scenarios/bklit-barloading.tsx`'s own header and ruled at D341. Settled has **improved 4.5259% → 1.8975%**. Self-test is 0.0000% on all captures, so the hover numbers are clean signal and are **new information D341 did not have** (D341 measured settled only): settled→hover movement is **bklit 3.05% vs migrated 16.35%**, i.e. the two skeleton animations diverge over TIME, not merely in geometry. Recorded, not chased. *`ring` n=1000* — **invalid instrument (D335), now corroborated structurally rather than only on pixels:** a DOM readback shows **bklit holding 962 of its 975 transformed groups at `scale(0)`** while migrated renders 40 groups at exactly `scale(1)`, and neither side exceeds 1.0 at capture — so the 0.72% is **not** a hover-scale mismatch, it is which arcs happen to be visible in a stagger that never completes. `ring` n=4, the gate of record, **PASSES**. Worth recording against D335: today's self-test *passed* (max 0.0935%) where D335's failed, so the instrument is **marginal, not reliably self-failing**. *`candletween`* — **pixel-invalid by design (D330 §2)**; two independent confirmations, the self-test **FAILING at 0.1883%** against a 0.1% floor and the two runs disagreeing (1.3619 vs 1.4408). The k4 DOM probe is the gate of record and reads migrated at **duration 1800 ms, `cubic-bezier(0.85, 0, 0.15, 1)`, 64 sampled frames, delays 0→21.6 ms** — K4's contract exactly — against **bklit `animCount: 0`**, framer driving rAF, so the probe can only ever read one side. **(4) DOM probes:** `sb-chrome-probe` **PASS** on all four sunburst states and all four presence assertions; `k4-tween-probe` and `reveal-probe` are readback dumps with no verdict lines, both consistent with their contracts (sankey: 33 flows, dur 1100, delays 0→561.3 ms). **(5) Bench: 10 exclusive paired cells** under D273 conditions, process table asserted empty first — see `BENCHMARKS.md` §P6.4. **M1a is VOID for the third consecutive run** (P6.1, P6.2, P6.4): the `bklit` control itself moved **+8.3%** on scatter, **+12.3%** on area and **+8.8%** on line, past the ±8% threshold — **M1a is not a stable channel on this machine and no M1a claim is made.** M1b/M1c/M3a/M3c controls all held (≤4.1%) and are valid; on them migrated is at or ahead everywhere except `composed`, with script cost down **−77% composed / −76% scatter / −63% bar** and heap down **12.2→6.8 MB** on composed and **8.1→5.3 MB** on scatter. **Two regressions stand, both on `composed`, both carried from P6.2 rather than new:** M1b +27% (1310.9→1664.8) and M3a ~1.5× (21.5→32.2) — and the M3a gap has a known shape, bklit's composed control at 21.5 ms being the outlier LOW against a 29–33 ms cluster on both impls everywhere else. Carried to Phase 5 with the numbers on record.
