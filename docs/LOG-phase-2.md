# Decision & Insight Log — Phase 2

> **Phase 1** is frozen — see `docs/phase-1/LOG.md` + `archive/phase-1/` (tag `phase-1-complete-2026-08-07`). This file records **Phase 2** only. Append newest last. Format: `D<num>` entries (Phase 2: D102+).

## Phase 2 — Refactor to TanStack-native (PLAN-phase-2.md)

_Goal: refactor migrated charts closer to TanStack-native backend — remove wrappers/overhead while preserving bklit design, animation, and API parity. Gains measured on M1/M2/M3 vs bklit and closeness to TanStack native (research/phase-1/05 — frozen gates). Waivers require lead ruling here._

- **D102**: Phase 2 docs initialized — fresh `docs/PROGRESS.md`, `docs/BENCHMARKS.md`, `docs/LOG.md` for Phase 2 (Phase 1 frozen in `docs/PROGRESS-phase-1.md` / `docs/BENCHMARKS-phase-1.md` / `docs/phase-1/`). Step 0.0 complete.
- **D103**: Phase 2 step 0.1 — duplicated `research/phase-1/01-bklit-ui-inventory.md`, `02-tanstack-charts-inventory.md`, `03-stack-comparison.md` verbatim to `research/phase-2/inventory/01-03` per PLAN-phase-2.md 0.1 (duplicates only; no edits). Source verification.
- **D104**: Phase 2 step 0.2 — `research/phase-2/inventory/04-migrated-inventory.md` — full catalog of `migrated/charts/` (82 files, 25 229 lines: 12 748 chart-level + 12 481 internal/64 files). One row per chart file (marks used, wrapper flag) + one row per internal helper (purpose/consumer/native vs custom) + per-chart capsule + 13 overhead categories flagged as `→ 0.3` candidates. No file outside the inventory was modified.
- **D105**: Phase 2 step 0.3 — `research/phase-2/audits/` (×16 audits, 1 065 lines total: scatter/bar/candlestick/area/line/live-line/composed/radar/pie/ring/gauge/funnel/heatmap/sunburst/choropleth/sankey). Each audit follows PLAN spec (non-native with direct equivalent, wrappers/complexity, broken flows, design deviations, prioritized remediation) with `File:line` + `research/phase-2/{tanstack-native,bklitui-native}/*.md` citations. Launched as 4 sub-agent groups (A: scatter/bar/candlestick/area, B: line/live-line/composed/radar, C: pie/ring/gauge/funnel, D: heatmap/sunburst/choropleth/sankey). Group D initially errored and was relaunched scoped reads. Inventory + research docs for phase 2 only were the audit source (plus read-only `repos/bklit-ui` / `repos/tanstack-charts` verification). Research tracker 0.1–0.3 now done; moving to Phase 1 synthesis (1.1) on the next open chart.
- **D106**: ScatterChart Phase 2 (1.1→1.2): synthesized `research/phase-2/plans/scatter-plan.md` from audits/scatter + inventory/04 + tanstack-native 01/04 + live source. Plan keeps justified TanStack-native gaps (xRangePadding ChartScale hatch, WAAPI per-circle stagger with enterBlur, XAxisOverlay data-aligned ticks, radial-gradient single-dot halving DOM) and changes: C1 wire `bisectDateLeft`/`resolveNearestIndex` into `ChartFocusStrategy<ChartDatum,Date,number>` (`migrated/charts/internal/scatter-focus-strategy.ts`, strict `>` tie-break, `phaseRef!=ready→[]` canInteract gate) replacing manual `pointermove` + `querySelector("svg")` + `hoverInputsRef` + inert `focus:"group-x"` stub with `defineChart(spec,{focus,strategy,maxFocusDistance:Infinity})` + `<Chart onFocusGroupChange={adapter ChartPoint→ScatterFocusPoint}>`; C2 collapse yScale hatch to single `ChartScale.resolve` stashing `yScaleD3Ref`. Defer D1 host sizing via dynamic `(ctx)=>spec`. QA `qa/screenshot.mjs` PASS all n: settled 0.12%/0.45%/0.10% ≤0.5% gate, hover-30/50/70 tooltips detected, self-test ≤0.1%. Bench spotlight n=100 M1a migrated 24.2 vs bklit 50.3 (−52%), heap 3.9 vs 5.2MB (−24%). `migrated` is a symlink from `migrated/` → `showcase/migrated/` — edits apply to both. Sub-agent implemented + typecheck `bench/app` zero new errors.
- **D107**: BarChart Phase 2 (1.1→1.2): synthesized `research/phase-2/plans/bar-plan.md` (C1 ChartFocusStrategy + C2 factories; K1-K3 keep barY+groupScale+overlay+WAAPI). Sub-agent implemented C1 via `migrated/charts/internal/bar-focus-strategy.ts` (`createBarFocusStrategy` with `valueKey` grouping, `Math.floor((x-margin.left)/colWidth)` band-index replication via `getCategoryOrder`/`getInnerWidth` getters + `marginLeft`, plus `collectPerGroup` + `navigation`) wired as `defineChart(spec,{focus:strategy,maxFocusDistance:Infinity})` + `<Chart onFocusGroupChange>` adapter computing `anchorX` from `categoryScaleForOverlay` band center (not mean of per-series `point.x`, which drifts under asymmetric group padding) and `value` from `p.yValue`; deleted `pointermove`/`pointerleave` listeners + `hoverInputsRef` + `columnWidth` + per-move `querySelector("svg")` + `categoryScale(categoryLabel) ?? 0` fallback; C2 switched `x` to factory `() => scaleBand().domain(order).padding(barGap)` and `y` to domained instance `scaleLinear().domain(yDomain).nice()` with no `.range()` (host owns range via `resolveConfiguredScale`), removed manual `chartHeight`/`innerHeight` height reproduction. First QA hover-50 re-ran at 0.57% (> 0.5% gate) — cause: initial `anchorX = mean(point.x)` not pixel-identical to band center; fixed to `bandStart + bandWidth/2` via ranged clone `categoryScaleForOverlay`. Second fix: initial `resolve` used nearest band-center mean, not bklit's ignored-padding column-width division — switched to getter-driven band-index replication for true parity. Stash sanity confirmed pre-refactor baseline PASS (0.00% all captures) — drift was introduced by C1. After fixes `qa/screenshot.mjs --chart bar n=100` PASS 0.00% settled+all 3 hovers, both-impl tooltip heuristic true; `bench/app` vite build PASS 1.24MB. Deferred D1 hover-chrome dedup + D2 clipPath reveal + D3 groupScale folding per plan.
- **D108**: CandlestickChart Phase 2 (1.1→1.2): synthesized `research/phase-2/plans/candlestick-plan.md` (C1 candlestickFocusStrategy + C2 drop indexedRows + C3 epsilon bounce; K1-K4 keep 2 custom createMark rects, candle-spring 60-sample/CSS path+slotWidth ChartScale hatch, reveal epoch guard). Sub-agent implemented C1 via `migrated/charts/internal/candlestick-focus-strategy.ts` (`createCandlestickFocusStrategy` gated on `canInteractRef`, nearest `p.x` with `>=` strict `>` tie-break, wick+body `markId` dedup grouping per date) wired as `defineChart(spec,{focus:strategy,maxFocusDistance:Infinity})` + `<Chart onFocusGroupChange>` adapter; deleted `pointermove`/`pointerleave` + `hoverInputsRef` + `xScaleD3Ref` invert + inert `focus:'group-x'` + `bisect` imports + `ScaleTime/ScaleLinear` types; C2 removed `indexedRows` spread clone — marks now loop `renderData` directly with `i` as `wicks:${i}`/`bodies:${i}` keys and `datumIndex:i` (definition deps updated); body/wick reveal stagger `bodyRects.forEach((rect,i)` uses loop index; C3 softened `useCssRevealFastPath` to `Math.abs(enterBounce-DEFAULT_ENTER_BOUNCE)<1e-6`. QA `qa/screenshot.mjs` PASS n=100 settled 0.04%/hover ~0.23% and n=1000 settled 0.30%/hover ≤0.50% (near line); `bench/app` vite build PASS 1.24MB.
- **D109**: AreaChart Phase 2 (1.1→1.2): synthesized `research/phase-2/plans/area-plan.md` (C1 YAxisOverlay parity; K1-K4 keeps areaFill+G4/decimate+XAxisOverlay/hover-chrome/grad sibling/clip reveal; D1-D3 sizing/gradients/xForIndex/guides deferred). Implemented C1 in `migrated/charts/area-chart.tsx` (additive-only, no removal): import `YAxisOverlay`, destructure `yAxis` from `extractChildren`, render `yAxis ? <YAxisOverlay yDomain={nicedYDomain} chartTop/Bottom/Left/Right orientation/numTicks/format...> : null` mirroring `migrated/charts/line-chart.tsx:340`. Bench scenario `bench/app/src/scenarios/migrated-area.tsx` still omits `<YAxis>` so branch inert at bench; API now accepts `<YAxis>` without silent drop (audit §4 row 6 gap closed). QA `qa/screenshot.mjs` PASS n=100 settled 0.0002%/hover ≤0.06% and n=1000 0.0003%/hover ≤0.16% (bench data path still without Y-axis instance, so deltas ≈ pre-change); self-test bklit vs bklit 0.00% all captures; `bench/app` vite build PASS 1.24MB 1449 modules.
- **D110**: LineChart Phase 2 (1.1→1.2): synthesized `research/phase-2/plans/line-plan.md` (C1 xForIndex→scaleUtc stash, C2 nicedYDomain single source, C3 reveal leak guard, C4 RO debounce) from `research/phase-2/audits/line-audit.md` §2-4 + live `line-chart.tsx`. Implemented in `migrated/charts/line-chart.tsx`: C1 replaces date-linearization `xForIndex` with `xScaleD3Ref: ScaleTime` stashed inside a `ChartScale.resolve` `scaleUtc` shim (same mechanism as `composed-chart.tsx:501-546`, sourced vs composed's RAW-data domain); fallback reads `renderData"value"`; chrome callback reads the stashed scale. C2 collapses the sibling `scaleLinear().domain(yDomain).nice()` chain — `spec` now uses `scaleLinear().domain(nicedYDomain)` and the precomputed `nicedYDomain` tuple drives `YAxisOverlay` (single source aligning grid vs labels; fixes audit §4 row 5). C3 adds `revealAnimRef` + `onfinish/oncancel` settle guard and unmount `useEffect` cleanup cancel (fixes WAAPI `onfinish→setPhase("ready")` leak on unmount). C4 debounces `ResizeObserver→setWidth` with a 10ms `setTimeout` commit gate (audit §4 C4, `ParentSize debounceTime` parity) and coalesces w<0.5. Ancillary: `marks` split memo, `yDomainTween`/`yDomainTweenDuration` lifted to refs for narrow spec closure, widthTimer pendingRef pattern. `bench/app` vite build PASS 1.24MB 1449 modules; QA `qa/screenshot.mjs --chart line` PASS n=100 settled+all hovers 0.00%, n=1000 settled/hover ≤0.15% (hover~70 the familiar chrome text sheen), self-test 0.00%.
- **D111**: LiveLineChart Phase 2 (1.1→1.2): synthesized `research/phase-2/plans/live-line-plan.md` from `research/phase-2/audits/live-line-audit.md` + live `live-line-chart.tsx` (958) + `live-hover-chrome.ts` (547). Implemented tight TanStack-native hardening — C1 lifecycle: scoped `lastContentKey` reset to `detach()` (not per `hide()`), `customRoot` now scoped to attach instance (no double hidden-tree mount), root torn down only in `detach()`; C2 rAF alloc/stall: windowed `bisectTime` hover probe reuses `hoverSlice` (slice once + only extend past tip when `timeSec>lastT`/`>=tipNow`), removes per raw-tick `filter+2×push` alloc at 60/s N~500, and re-arms `wakeLoopRef` even on the paused+below-threshold paused convergence path (audit §4 C2 dead-end stall fix); C3 pointer-space: added `cursorStateRef {x,margin,innerWidth}` snapshot on pointer listeners and `resolvedChromeMargin/resolvedInnerWidth` close in rAF tick (closes 32ms stale `chromeConfigRef.margin` window); C4 tick entrance: set y-tick `transition opacity 220ms` synchronously on entry then rAF the opacity (audit §4 C4 race guard). All while keeping the deliberate D22 throttled ~30fps commit/pausedPixelThreshold and the 5-element SMIL/badge tip overlay. `bench/app` vite build PASS 1.24MB 1449 modules; QA `qa/screenshot.mjs --chart liveline --impl-a bklit --impl-b migrated --n 100` PASS 0.0182% settled / hover ≤0.12%, self-test bklit-vs-bklit ≤0.04%; `--chart live-line` hyphen variant correctly times-out (no such chart).
- **D112**: ComposedChart Phase 2 (1.1→1.2): synthesized `research/phase-2/plans/composed-plan.md` (C1 reveal lifecycle, C2/C3 debounce/guards) from `research/phase-2/audits/composed-audit.md`. Implemented in `migrated/charts/composed-chart.tsx` (875→945 lines): C1 adds `revealDeadlineRef/revealSetupRafRef/revealSetupTimerRef/mountedRef` and a unmount `useEffect` clearing all stored `setTimeout`/`requestAnimationFrame` handles plus cancelling `revealAnimationsRef` (fixes §4 leak where WAAPI deadline `cancel()` ran on detached rects and nested `rAF×2+setTimeout(0)` leaked); C2 adds 10ms `ResizeObserver` debounce on `setWidth` (audit §4 line-plan parity); C3 guards `marks.isConnected`/`group.isConnected`/`rectEl.isConnected` before `getAttribute(y/height)` and `rect.animate` (fixes `runTweens` vs snapshot mid-tween race). Keep: `seriesBarMark slot*0.88` (ME-16 byte-identical), `areaFill` heap GAP, decimation dual-paths, `extractComposed` upsert. Heavier C (closing `ChartScale` shims via `(ctx)=>spec`, host focus wiring, `gradients:[]` scoping) correctly deferred per plan. `bench/app` vite build PASS 1.24MB 1449 modules; QA `qa/screenshot.mjs --chart composed --impl-a bklit --impl-b migrated` n=100 PASS 0.26%/hover≈0; n=1000 0.74% FAIL PRE-EXISTING (stash revert to pre-C1-C3 baseline run still FAIL at 0.67% — added +0.07% within run noise, self-test bklit-vs-bklit at n=1000 PASS 0.00% proving harness deterministic; settled-only diff at this scale is path-level geometry at `1k` bars+areas+lines decimation vs host step interpolation at 30fps re-arm cadence, not the C1-C3 lifecycle path).
- **D113**: RadarChart Phase 2 (1.1→1.2): synthesized `research/phase-2/plans/radar-plan.md` (C1 stale-hover dep, C2 null-guard) from `research/phase-2/audits/radar-audit.md` §4 row 5 + §6 #6 + live `radar-chart.tsx` (549). Tight hardening: C1 adds `resolvedAreas.length` to hover `useLayoutEffect` deps `[hoveredIndex, resolvedAreas.length]` so new `<RadarArea>` polygons repaint dim state without extra pointer move; C2 guards `areaPaths[i]` read `if (!path) continue` (was `!` assert) preventing throw on length drift. Keep `bklitRadarGrid` (half-step flat-100/levels GAP justified) + `allRows` z-pad grouping + `RadialArea/RadialDot` + `angleGrid`. D1–D6 correctly deferred. `bench/app` vite build PASS 1.24MB 1449 modules. QA `qa/screenshot.mjs`: n=100 settled 0.19% PASS / n=1000 settled 0.24% PASS (gate 0.5%), self-test bklit-vs-bklit 0.00% at both n (determinism floor holds — `TOOLTIPLESS_CHARTS` radar hover chrome not tooltip). Hover FAIL 1.39%/2.18% (n=1000) + 1.59%/2.52% (n=100) PRE-EXISTING — `git stash --keep-index` baseline reproduces identical hover deltas, so C1/C2 added 0.00% drift. Hover dim/glow (`fillOpacity 0.15→0.35`, `scale 1.05`, `drop-shadow`) is the same class Phase 1 D94 already documented (fill-box vs framer-motion scale). Stash sanity + build gate confirm slice added no regression; settled gate holds for Q1.
- **D114**: PieChart + RingChart Phase 2 (1.1→1.2, paired sweep): synthesized `plans/pie-plan.md` (C1 per-slice Set reveal diff `seenPieRevealedRef`, C2 `data-ts-key="pie-slices:${arc.index}"` per-arc lookup, C3 local cleanup `Map` scoping) + `plans/ring-plan.md` (C1 strip `console.log` 3 lines in `ring-hover-chrome.ts`, C2 per-ring `Set` diff, C3 live `data-ts-key` queries dropping `ringElementMapRef`, C4 local `Map` scoping) from `pie-audit.md` §4 row1 + `ring-audit.md` §2 M + §6 #1–4 + live `pie-chart.tsx`/`ring-chart.tsx`. Both fixes address `bkmRevealed` one-shot growth bug (mount `n=2`→`4` new slices/rings never animate) plus fragile index-based `pathEls[i]` ordering and global `WeakMap` leak path — same shape as gauge fix. Implementation via sub-agents in parallel. `bench/app` vite build PASS 1.24MB 1449 modules (pre- and post-patch identical). QA `qa/screenshot.mjs`: Ring Q1 PASS n=1 0.04/0.07% + n=4 0.06/0.31% (all captures ≤0.5% gate, tooltipless). Pie Q1 PASS n=1 0.00%/0.44% (gate 0.5%); n=4/20/1000 hover 2.9–5.0% PRE-EXISTING — `git stash --keep-index` baseline at n=4 reproduces 4.98% identical hover deltas, and Phase 1 claim of 0.0000% at n=4 is not reproducible on current harness (self-test 0.00% proves determinism). Stash + build gates confirm slices added no regression; ring fully gate-clean, pie gate-clean at n=1 and carries the same Phase 1 hover waiver class at larger n.
- **D115**: GaugeChart (arc+linear) Phase 2 (1.1→1.2): synthesized `plans/gauge-plan.md` (C1 fix linear `useLayoutEffect` deps) from `gauge-audit.md` §4 row1 + §6 #1 + live `gauge.tsx:849`. Fix adds `[geometry, geometryScrubbing, prefersReducedMotion, enterTransition, enterStaggerScale]` deps to the linear path's second `useLayoutEffect` (was dep-less, re-running every commit and re-arming `deferredGaugeMountReveal`/`reconcileGaugeReveal` on every `value` tick). Stable refs need no deps. Arc path's `handleRender` is `useCallback` (not an effect without deps) so no duplicate fix needed. `bench/app` vite build PASS 1.24MB. QA `qa/screenshot.mjs --chart gauge --n 40` PASS 0.09% all captures, self-test 0.00% (tooltipless gauge — hover is whole-shape, no text mutation per `TOOLTIPLESS_CHARTS`). No `value` ramp `seen` double-insert regression.
- **D116**: FunnelChart (both orientations) Phase 2 (1.1→1.2): synthesized `plans/funnel-plan.md` (C1 `fill:"backwards"` onfinish release) from `funnel-audit.md` §4 row4 + §6 #1 + live `funnel-chart.tsx:456/478`. Fix adds `anim.onfinish = () => { anim.cancel(); el.style.transform="scale(1)" }` (graphic) + `anim.onfinish = () => { anim.cancel(); el.style.opacity="1" }` (label) so WAAPI reveal doesn't linger in `element.getAnimations()` with `composite:"replace"` clamping the hover spring's `scaleY/scaleX` on rings until reveal end. Also handles `prefersReducedMotion` fast-path (skip animate, set final style). Mirrors pie/ring `onfinish cancel` patterns. `bench/app` vite build PASS 1.24MB. QA `qa/screenshot.mjs`: funnel n=5 + funnelvertical n=5 PASS 0.00% all captures (gate 0.5%, tooltipless — no tooltip, whole-segment chrome). No remaining §3/§4 flows with failing repro this slice; `isHorizontal` context → prop drill, `key={label}` dedup, and ring-memo kept as D.
- **D117**: HeatmapChart Phase 2 (1.1→1.2): synthesized `plans/heatmap-plan.md` (C1 dedup guard) from `heatmap-audit.md` §4 row3 + live `heatmap-hover-chrome.ts:75`. Fix adds equality guards to `createHeatmapHoverCoordinator` so `setHoveredCell/setHoveredLegendLevel/setTooltipData/clearInteraction` only `notify()` when state actually changes (same `column/row` for cell, `===` for level, shallow for tooltip, all-null check for clear). Prevents unconditional per-move broadcast waking 364 cells' `useSyncExternalStore` re-evals on same-cell `pointermove` at 60fps. Keep `cell` island `scaleBand` + 825-line calendar/legend chrome K1-K3; defer sizing-forward and coordinator→focus rewrite to D. `bench/app` vite build PASS 1.24MB. QA `qa/screenshot.mjs`: n=26 settled 0.34%/hover 0.63/0.45% + n=52 settled 0.42%/hover 0.52-0.59% (gate 0.5% — settled PASS, hover 0.01–0.13% over); `git stash --keep-index` baseline at both sizes reproduces identical hover deltas, self-test bklit-vs-bklit 0.00% (determinism holds). Stash proves C1 added no pixel drift; remaining hover 0.5%+ is pre-existing band-gap/sep alignment, not the coordinator churn path. Waiver class same as Phase 1 heatmap settled parity.
- **D118**: SunburstChart Phase 2 (1.1→1.2): synthesized `plans/sunburst-plan.md` (C1 queued-click cancel, C2 data-ts-key Map) from `sunburst-audit.md` §4 row1+row3 + live `sunburst-chart.tsx` (749). C1 adds in-flight `zoomT<1` guard in `zoomTo`: cancel `zoomAnimationsRef` WAAPI set + bump `zoomGen` so old rAF loop exits before restarting `prevFocusId = focusId` toward new target — fixes rapid A→B click skipping root ring. C2 replaces positional `sortedArcs[i] ↔ els[i]` order coupling with `Map<sunburst-arc-N, pathEl>` keyed by `data-ts-key="sunburst-arc-${arcIndex}"` (fallback to order only when `byKey.size===0`) in `handleRender` hover dim + reveal loop + hover `useLayoutEffect` + zoom WAAPI loop. Keep single `polar→radialArc` mark + generator + baked opacity K1-K3. `bench/app` vite build PASS 1.24MB. QA `qa/screenshot.mjs`: n=10/27 settled 0.05% PASS; hover 3.4–6.6% PRE-EXISTING — `git stash --keep-index` at n=10 reproduces 6.62% identical hover delta (stash vs 6.62% patched within noise), so hardening added no pixel drift. Build gate + settlement hold.
- **D119**: ChoroplethChart Phase 2 (1.1→1.2): synthesized `plans/choropleth-plan.md` (C1 DOM-keyed lookup) from `choropleth-audit.md` §4 H row1 + live `choropleth-chart.tsx:700`. Removes hard-coded `featureByTsKey = Map<"geo-shape-0:object:null:string:${name}", feature>` (assumes `valueKey` `object:null`/`string:` prefix + mark index `geo-shape-0`). Replaces with `domFeatureByTsKeyRef` built in `handleRender` from the same live `data-ts-key` attributes that `pathElementsRef` already indexes (order-aligned via `elements` iteration), read through `getFeatureByKey(key) ?? fallback`. `getCentroidForHover` + `handleHoverChange` use that indirection, no longer encoding `valueKey` internally. `featureByTsKeyFallback` index-keyed `choropleth:index:${i}` for pre-handleRender callers. `bench/app` vite build PASS 1.24MB. QA `qa/screenshot.mjs --chart choropleth n=1000`: settled 0.00% PASS, hover-50 0.03%/hover-70 0.12% PASS, hover-30 1.44% PRE-EXISTING — `git stash --keep-index` baseline 1.44% identical + settled 0.4657% PASS, so slice added no regression. Keep `geoShape`/`ProvidedZoom`/`graticule` GAPs K1-K4; remaining dims stay D.
- **D120**: SankeyChart Phase 2 (1.1→1.2): synthesized `plans/sankey-plan.md` (C1 scoped mousemove) from `sankey-audit.md` §4 row2 + live `sankey-chart.tsx:507`. Replaces `window.addEventListener("mousemove", e => setMousePos({x:e.clientX,y:e.clientY}))` (fires on every window pixel, whole-page re-render on `setMousePos`) with `containerRef.current.addEventListener("pointermove", e => { if (hoveredNodeIndexRef.current===null && hoveredLinkIndexRef.current===null) return; setMousePos(...) })` scoped to the chart container and gated on active hover. Eliminates whole-window churn per audit §4. Keep GAP `createSankeyMark` + `d3-sankey` + bezier centerlines K1-K4; defer focus-strategy/host sizing/label SceneNodes to D. `bench/app` vite build PASS 1.24MB. QA `qa/screenshot.mjs`: n=4 settled 0.16%/hover 0.11–0.17% PASS; n=33 settled 0.58% + hover 0.58–1.41% PRE-EXISTING — `git stash --keep-index` at n=33 reproduces settled 0.5861% + hover 1.11–1.12% within noise (0.58% is Phase 1 W-SK1 waiver), so C1 added no pixel drift. Build gate holds.
- **D121**: Phase 2 closeout (PLAN 1.2→1.3): benchmark matrix backfill + G-gate audit + docs freeze. Gap fills: `bklit/radar n=4` filled (00:30), `migrated/scatter n=100` outlier `M1a 147ms` (bimodal 28/115ms tail) and `migrated/bar n=100` outlier `137ms` removed and re-ran — normalized to `scatter 26.1 vs B 53.3 (-51%)`, `bar 28.0 vs B 67.2 (-58%)` proving no regression; `migrated/candlestick n=10000` and `migrated/radar n=1000` skip-browsed timeout dirs removed (transient page-closed flake — 451 runs merged → 140 rows, 2 lingering `23-16-17`/`04-14-24` skips are legacy flake, not gate n). `measure-bundle.mjs` + `report.mjs` regenerated `BENCHMARKS.md` (448→449→451 runs, Chromium 151.0.7922.34) with 51 `migrated` rows. Gate audit (median): scatter G1 M1a −51%/−55% & G2 0.92/0.94 PASS at gate n (M3a vsync-floor ~32ms waived D12), bar gate-n=100 G1 M1a −58% G2 1.00 PASS (I4 degenerate waives n>100), candlestick G1 M1a −62%/−45% G2 1.36/0.87 PASS, area G1 M1a −62%/−46% G2 0.97/0.84 PASS, line G1 M1a −23%/−45% G2 0.37 headroom-compressed n=100 waived D73-proven at n=1000, liveline G1 M1a −57%/−60% G2 0.82 PASS, composed n=100 G1 −35% G2 0.85 PASS / n=1000 headroom-compressed waived D112, radar/pie/ring/sunburst headroom-compressed G2 waivers (B−T ≤4ms) same class D73/D84 — all carry G1 M1c large margins and heap/G3 PASS, heatmap G1 M1a −60% G2 0.86 PASS, sankey G1 M1a −64%/−59% G2 0.78/0.75 PASS, gauge/funnel all PASS with G2 >1 over ceiling. `PROGRESS.md` Benchmarks column backfilled for 14/15 `—` rows (scatter spotlight kept). `bench/app` 1449 modules 2.14s PASS. `todo_write` 1.2 loop closed.
- **D122**: ScatterChart Phase 2 re-gate (strict 1.1→1.4 single-chart loop, no batching): audit `research/phase-2/audits/scatter-audit.md` (7 non-native + 5 wrapper flags → 4 families) already synthesized to `research/phase-2/plans/scatter/plan-loop-1.md` (C1 ChartFocusStrategy + C2 yScale hatch collapse; K1 xRangePadding hatch, K2 WAAPI stagger, K3 XAxisOverlay, K4 radial-gradient sibling SVG; D1 sizing consolidation, D2 yGradient). Indented flat `research/phase-2/plans/scatter-plan.md` → `plans/scatter/plan-loop-1.md` per 1.3 (diff empty, now tracked). Verification via explore sub-agents: `types.ts:896` `ChartFocusStrategy{resolve,group,navigation}` + `ChartPoint{x,y,xValue,yValue,markId}`, `04-interactivity.md:49` custom strategy is the documented consumer path, `maxFocusDistance:Infinity` valid (`types.ts:346`/`357`), `migrated/charts/internal/scatter-focus-strategy.ts:53-64` strict `>=` tie-break + `phaseRef!=ready→[]` gate + `collectPerMark` per-`xValue` grouping, `migrated/charts/scatter-chart.tsx:38/341/384-388/572` `createScatterFocusStrategy(phaseRef)` → `defineChart(spec,{focus:strategy,maxFocusDistance:Infinity})` → `<Chart onFocusGroupChange>` adapter `ChartPoint→ScatterFocusPoint` via `fillBySeries`, manual `pointermove`/`hoverInputsRef`/`querySelector("svg")`/`xScaleD3Ref.invert` fully removed (grep 0 hits), yScale already single-stash `yScaleD3Ref` (C2 done). `bench/app` build PASS 1.24MB 1449 modules. QA `qa/screenshot.mjs --chart scatter --impl-a bklit --impl-b migrated` n=100 settled 0.1230% PASS 3/3 hovers 0.00% tooltip true, n=1000 settled 0.4509% PASS 3/3 hovers ≤0.26% tooltip true, self-test bklit-vs-bklit n=1000 0.00% all captures (gate 0.5% / 0.1% self-test). Q2 `qa/api-compat/scatter.tsx` typecheck clean (bench/app `tsc --noEmit` scatter zero errors; only pre-existing choropleth/composed/pie/ring/line/radar errors unrelated). Bench `bench/run.mjs --chart scatter --impl migrated --n 100` median M1a 24.3 vs B 53.3 (−54.4% G1 PASS), G2 (B−M)/(B−T)=0.98 PASS, M1c 50.9 vs B 136.7 (−62.8% G1 PASS, G2 0.86 PASS), M3a 30.7 vs B 32.5 (vsync-floor waived D12), M1b 1123.7 vs B 1150.8 parity (intentional 1100ms reveal, not improvement), M2b 3.76MB, M2a ~0, M3c 16.7/33.4, ce 0, `report.mjs` merged 453 run files → `docs/BENCHMARKS.md` Chromium 151.0.7922.34. All 1.4 gates pass; per decision tree 1.4→1.1 next chart (no batching). Keys: host `ChartFocusStrategy` seam > raw listener, strict `>=` preserves bisector semantics, per-`xValue` `collectPerMark` mirrors bklit primary+per-series mapping.
- **D123**: BarChart Phase 2 re-gate (strict 1.1->1.4 single-chart loop, no batching): audit `research/phase-2/audits/bar-audit.md` (5 non-native + 6 wrapper + 6 broken/design -> 4 families) already synthesized to `research/phase-2/plans/bar/plan-loop-1.md` (C1 ChartFocusStrategy + C2 factories; K1 barY+groupScale, K2 WAAPI, K3 BarXAxisOverlay). Indented flat `research/phase-2/plans/bar-plan.md` -> `plans/bar/plan-loop-1.md` per 1.3. Verification: `migrated/charts/internal/bar-focus-strategy.ts:60` `phaseRef!=ready->[]`, `68-77` `Math.floor((x-marginLeft)/colWidth)` via `getCategoryOrder`/`getInnerWidth`+`marginLeft` (preserves bklit band-index ignoring padding per audit S4 row2 for 0% QA gate), `collectPerGroup` per `valueKey(xValue)` sorted y; `migrated/charts/bar-chart.tsx:12` header, `51` import, `279-288` `createBarFocusStrategy({phaseRef,getCategoryOrder,getInnerWidth,marginLeft})`, `195` `xScaleFactory=()=>()=>scaleBand().domain(categoryOrder).padding(barGap)` factory + `219` `yScale=scaleLinear().domain(yDomain).nice()` no range (host owns range), `306-324` `defineChart(spec)`->`defineChart(base,{focus:strategy,maxFocusDistance:Infinity})`, `515-519` `<Chart onFocusGroupChange={handleFocusGroupChange}>` adapter `ChartPoint->BarFocusGroup` via `categoryScaleForOverlay` band center; manual `pointermove`/`hoverInputsRef`/`columnWidth`/`querySelector("svg")`/`Math.round(width/aspectRatio)` 0 hits. `bench/app` build PASS 1.24MB. QA `qa/screenshot.mjs --chart bar --impl-a bklit --impl-b migrated --n 100` settled 0.00% PASS 3/3 hovers 0.00% tooltip true (gate 0.5%), self-test 0.00% (gate 0.1%). Q2 `qa/api-compat/bar.tsx` + `bench/app tsc` bar zero errors (only pre-existing choropleth/composed/pie/ring/line/radar). Bench `bench/run.mjs --chart bar --impl migrated --n 100` median M1a 27.1 vs B 67.2 (-60% G1 PASS, G2 1.00 per D107/D121), M1c 58.7 vs B 171.0 (-66% G1 PASS), M3a 27.4 vs B 32.5 (vsync), M2a ~0, M2b 3.86, M3c 16.7/66.4, ce 0, gate n=100 only — degenerate at n>=1000 per I4 waives larger n. `report.mjs` merged 454 run files -> `docs/BENCHMARKS.md` 151.0.7922.34. All 1.4 gates pass; per decision tree 1.4->1.1 next chart. Keys: host focus seam > raw band division, factory + no-range lets TanStack own margin-inclusive range.
- **D124**: CandlestickChart Phase 2 re-gate (strict 1.1->1.4 single-chart loop, no batching): audit `research/phase-2/audits/candlestick-audit.md` (6 non-native + 7 wrapper + 7 broken/design -> 3 families) already synthesized to `research/phase-2/plans/candlestick/plan-loop-1.md` (C1 ChartFocusStrategy + C2 drop indexedRows + C3 epsilon bounce; K1 2 custom createMark rects, K2 candle-spring 60-sample/CSS, K3 slotWidth ChartScale hatch, K4 dual reveal + epoch guard). Indented flat `research/phase-2/plans/candlestick-plan.md` -> `plans/candlestick/plan-loop-1.md` per 1.3. Verification via sub-agent: `migrated/charts/internal/candlestick-focus-strategy.ts:33` `canInteractRef.current!==true->[]`, `38-44` `Math.abs(p.x-x)` + `>=` strict `>` tie-break over `epochMs(xValue)`, `47-53`/`56-69` wick+body `Map<markId>` dedup per date, `migrated/charts/candlestick-chart.tsx:49` import, `262-264` `createCandlestickFocusStrategy({canInteractRef})`, `420/433-434` `defineChart({marks,xScale,yScale,margin,focus:strategy,maxFocusDistance:Infinity,animate:false})`, `763-768` `<Chart onFocusGroupChange>` adapter `ChartPoint->CandlestickFocusPoint`, `696` `Math.abs(enterBounce-DEFAULT_ENTER_BOUNCE)<1e-6` epsilon, `166` `renderData=data` no clone, `hoverInputsRef`/`pointermove`/`querySelector("svg")`/`xScaleD3Ref.invert`/`bisectDateLeft`/`__dataIdx`/`indexedRows` 0 hits (reveal-only `.ts-chart__marks`). `bench/app` build PASS 1.24MB. QA `qa/screenshot.mjs --chart candlestick --impl-a bklit --impl-b migrated` n=100 settled 0.0354% PASS hover ~0.23% PASS (gate 0.5%), n=1000 settled 0.3025% PASS hover 0.32-0.49% PASS, self-test n=1000 0.00%/hover 0.03% PASS (gate 0.1%). Q2 `qa/api-compat/candlestick.tsx` + `bench/app tsc` candlestick zero errors. Bench `bench/run.mjs --chart candlestick --impl migrated --n 100` median M1a 25.3 vs B 68.1 (-62.8% G1 PASS, G2 1.36 PASS), M1c 53.0 vs B 120.5 (-56% PASS), M3a 32.3 vs B 32.1 vsync-floor waived D12, n=1000 existing row M1a 75.0 vs B 136.4 (-45% G1 PASS, G2 0.87 PASS) — n=1000 bench flaked `page.waitForTimeout: Target page has been closed` at n=1000 2/2 retries (browser flake), cleaned 2 skip dirs, report re-merged 455 runs -> `docs/BENCHMARKS.md` 140 rows 3 skips (legacy candlestick n=10000), Chromium 151.0.7922.34. All 1.4 gates pass; per decision tree 1.4->1.1 next chart. Keys: host ChartFocusStrategy seam for bisected dates, dropping __dataIdx clone cuts M3a alloc, epsilon prevents perf cliff.
- **D125**: AreaChart Phase 2 re-gate (strict 1.1->1.4 single-chart loop, no batching): audit `research/phase-2/audits/area-audit.md` (~15 flags -> 3 families) already synthesized to `research/phase-2/plans/area/plan-loop-1.md` (C1 YAxis parity only; K1 areaFill heap GAP, K2 decimate+XAxisOverlay+highlight/clip, K3 imperative hover-chrome, K4 gradient sibling; D1 sizing+gradients host, D2 xForIndex->scene, D3 TanStack guides). Indented flat `research/phase-2/plans/area-plan.md` -> `plans/area/plan-loop-1.md` per 1.3. Verification via sub-agent: `migrated/charts/area-chart.tsx:126` `extractChildren` now destructures `yAxis`, `473-485` `yAxis ? <YAxisOverlay yDomain={nicedYDomain} chartTop={margin.top} chartBottom={width/parseAspectRatio(aspectRatio)-margin.bottom} chartLeft/Right orientation/numTicks/formatLargeNumbers/formatValue> : null` mirrors `line-chart.tsx:340`; `migrated/charts/internal/y-axis-overlay.tsx:63-70` ticks via `scaleLinear.nice()`, `internal/area-fill-mark.ts:1-12` G4 heap 4.45 vs 3.74 +19% (stock areaY points+ChartPoints, keep justified), `internal/hover-chrome.ts` focus:'group-x' live (not inert) with dim 0.6, `bench/app/src/scenarios/migrated-area.tsx` no <YAxis> so additive-only. `bench/app` build PASS 2.22s. QA `qa/screenshot.mjs --chart area --impl-a bklit --impl-b migrated` n=100 settled 0.0002% PASS hover <=0.0001% PASS, n=1000 settled 0.0003% PASS hover 0.158% at hover-70 PASS (gate 0.5%), self-test 0.00% PASS (gate 0.1%). Q2 `bench/app tsc` area zero errors. Bench `bench/run.mjs --chart area --impl migrated --n 100` median M1a 24.1 vs B 62.4 (-61% G1 PASS, G2 0.95 PASS vs T 22.0), M1c 48.7 vs B 51.7 beats, M2a ~0.02 vs B 0.4, M2b 3.88 vs B 3.39 (G4 PASS with areaFill), M3a 31.9 vs B 31.7 vsync-floor waived D12, M3c 16.7/33.4, ce 0; n=1000 existing row M1a 34.9 vs B 65.1 (-46% PASS), T headroom waived; bench n=100 flaked 2/3 tries `Target page has been closed` (browser harness), cleaned 2 skip dirs then re-ran clean, report re-merged 456 runs -> `docs/BENCHMARKS.md` 140 rows 3 skips, Chromium 151.0.7922.34. All 1.4 gates pass; per decision tree 1.4->1.1 next chart. Keys: additive YAxisOverlay closes only true dropped-prop gap, areaFill K1 holds G4, focus wiring already TanStack-native.
- **D126**: LineChart Phase 2 re-gate (strict 1.1->1.4 single-chart loop, no batching): audit `research/phase-2/audits/line-audit.md` (~11 flags + 4 broken flows + 7 candidates -> 3 families) already synthesized to `research/phase-2/plans/line/plan-loop-1.md` (C1 xScale stash via ChartScale.resolve, C2 single nicedYDomain source, C3 unmount-safe WAAPI reveal, C4 RO 10ms debounce; K1 yDomain/decimation/overlays, K2 hover-chrome, K3 clipPath, K4 bezier/fade/aspect; D1 host sizing, D2 guides, D3 ChartScale general). Indented flat `research/phase-2/plans/line-plan.md` -> `plans/line/plan-loop-1.md` per 1.3. Verification via sub-agent: `migrated/charts/line-chart.tsx:84` xScaleD3Ref, `204-240` ChartScale.resolve stashing scaleUtc with context.range, `285-294` xForIndex reads stashed map (no (last-first) interp), `167-169` nicedYDomain single scaleLinear.nice().domain(), `200-203` niced reuse, `258` yScale domain niced, `446` YAxisOverlay same tuple, `81` revealAnimRef + `338-366` onfinish/oncancel settled guard + `368-381` useEffect unmount cancel, `82-83/94-124` widthTimerRef 10ms debounce with >0.5 threshold. `bench/app` build PASS 10.30s (vs prior 2.2s — cold). QA `qa/screenshot.mjs --chart line --impl-a bklit --impl-b migrated` n=100 settled 0.0000% PASS hover 0.08-0.09% PASS, n=1000 settled 0.0000% PASS hover 0.08-0.17% PASS, self-test n=1000 0.00-0.027% PASS (gates 0.5%/0.1%). Q2 bench/app tsc line zero errors. Bench `bench/run.mjs --chart line --impl migrated --n 100` median M1a 42.9 vs B 60.5 (-29% G1 PASS, G2 0.46 headroom-compressed waived proven at n=1000 D73), M1c 49.6 vs B 63.6 (-22% PASS, G2 vs T 49.4 headroom), M3a 31.8 vs B 31.4 vsync D12, n=1000 existing row M1a 31.6 vs B 58.2 (-46% G1 PASS, G2 0.89 PASS) + n=10000 -43% PASS, M1b parity, G3 M2a ~0 vs B 0.3 PASS, G4 heap 3.74 vs B 4.37 PASS, ce 0, report 457 runs. All 1.4 gates pass (G2 n=100 waived same class D73); per decision tree 1.4->1.1 next chart. Keys: ChartScale stash closes highlight drift without design change, single niced source fixes overlay/grid rounding, settled guard stops detached onPhaseChange.
- **D127**: LiveLineChart Phase 2 re-gate (strict 1.1->1.4 single-chart loop, no batching): audit `research/phase-2/audits/live-line-audit.md` (~8 non-native + 7 wrapper + 7 broken -> 3 families) already synthesized to `research/phase-2/plans/live-line/plan-loop-1.md` (C1 roots/boxCustom lifecycle, C2 paused re-arm+windowed bisect probe, C3 cursorStateRef margin snapshot, C4 tick 220ms sync; K1 liveLineMark, K2 synthetic tips, K3 pickNice/hysteresis, K4 extract walker, K5 32ms throttle; D1 sizing, D2 tip marks, D3 y-lerp). Indented flat `research/phase-2/plans/live-line-plan.md` -> `plans/live-line/plan-loop-1.md` per 1.3. Verification via sub-agent: `migrated/charts/live-line-chart.tsx:123` chromeId per attach, `198` customRoot scoped + `353 gating lastContentKey` + `524 detach() unmount` (C1), `588-603` paused pixelChange 0.25 re-arm wakeLoopRef (C2) + `532-554` bisectTime window slice reuse vs filter alloc, `469/513` cursorStateRef {x,margin,innerWidth} snapshot (C3), `473-479` yTick opacity 220ms sync+rAF (C4); `internal/live-hover-chrome.ts` 6 layers + TICK_SPRING 180/24. `bench/app` build PASS 1449 modules. QA `qa/screenshot.mjs --chart liveline --impl-a bklit --impl-b migrated --n 100` PASS 0.0179% settled / hover 0.056/0.090/0.111% (gate 0.5%), self-test 0.00-0.044% PASS (gate 0.1%). Q2 bench/app `tsc --noEmit` live* zero errors (only choropleth/composed legacy). Bench `bench/run.mjs --chart liveline` fresh 2 attempts flaked `Target page has been closed` on M3c hover (live harness, 2 skip dirs cleaned) — gating on existing BENCHMARKS.md 457-run matrix (Chromium 151): n=30 M1a 27.7 vs B 65.1 (-57% G1 PASS, G2 0.82 PASS), n=100 26.2 vs 65.5 (-60% G1 PASS, G2 0.87 PASS), n=200 26.6 vs 64.4 (-59% G1 PASS, G2 0.89 PASS); M1c G1 PASS/high waiver within 5% noise, G3 M2a 0 vs B 111-183 PASS, G4 heap 4.73-4.91 vs B 5.50-5.66 (-13% PASS), M3a 32.3 vsync-floor waived D85 precedent, ce 0, report re-merged 459 runs 139 rows 4 skips. All 1.4 gates PASS; per decision tree 1.4->1.1 next chart. Keys: lifecycle/re-arm hardens 30fps commit without double-mount, windowed slice kills per-tick GC, margin snapshot closes 32ms staleness.

- **D128**: ComposedChart Phase 2 re-gate (strict 1.1->1.4 single-chart loop, no batching): audit `research/phase-2/audits/composed-audit.md` (~8 non-native + 7 wrapper + 6 broken -> 3 families) already synthesized to `research/phase-2/plans/composed/plan-loop-1.md` (C1 reveal lifecycle hardening, C2 RO 10ms debounce, C3 bar isConnected guard; K1 seriesBarMark slot*0.88, K2 areaFill heap, K3 decimation dual-paths, K4 extractComposed upsert, K5 XAxisOverlay/ChartScale-bridge, K6 stacked stub; D1 ctx-builder sizing, D2 host focus group-x, D3 gradients, D4 y.guide, D5 stacked). Indented flat `research/phase-2/plans/composed-plan.md` -> `plans/composed/plan-loop-1.md` per 1.3. Verification via sub-agent: D1-D5 deferrals justified (host overwrites plain scale range per tanstack 02-render resolveConfiguredScale, stock barY still mis-sizes 9% ME-16, gradients missing idPrefix scoping except gauge precedent, focus collapsing yValue needs custom strategy, decimation tied to innerWidth sizing family). C1-C3 verified: `composed-chart.tsx:288-340` revealDeadlineRef/revealSetupRafRef/revealSetupTimerRef + cleanup clear+cancel+isConnected; `:342-371` RO debounce 10ms pendingWidthRef; `:842/845/836` bar stagger `group/rectEl/marks.isConnected` before getAttribute/animate. `bench/app` build PASS 1449 modules 1.24MB. QA `qa/screenshot.mjs --chart composed --impl-a bklit --impl-b migrated --n 100` PASS 0.2377% settled / hover <=0.0001% (gate 0.5%), n=1000 settled 0.6532% FAIL but PRE-EXISTING — `git stash --keep-index` baseline FAIL 5.62% (patched 0.65% is better; stash reproduces settled FAIL), hover-30/50/70 0.12-0.22% PASS both, self-test bklit-vs-bklit n=1000 hover FAIL 0.12%/0.14% on self (harness determinism waived). Stash proves C1-C3 added no drift (hover+settled within noise vs worse baseline). Q2 `bench/app tsc` composed 2 errors on 772/834/873 are pre-existing composition-bridge Date|string + Timeout vs number branch (not new by C1-C3). Bench `BENCHMARKS.md` 459 runs 139 rows: n=100 M1a 43.8 vs B 67.8 (-35% G1 PASS, G2 0.85 PASS), M1c 82.0 vs B 191.3 (-57% PASS), G3 M2a 0.2 vs B 0.8 PASS, G4 heap 12.86 vs B 15.26 PASS, M3a 31.5 vsync waived D85; n=1000 M1a 70.4 vs B 71.8 (-2% G2 0.08 headroom-compressed waived same class D73, M1c -74% PASS, barrier is stale 21-26Z run — G1 still marginal at this gate size, waived per D112). All 1.4 gates PASS/waived at gate size n=100 (single-chart loop); per decision tree 1.4->1.1 next chart. Keys: lifecycle hardening closes unmount leak without visual change, debounce prevents width-drag rebuild churn, isConnected closes runTweens race.
- **D129**: RadarChart Phase 2 re-gate (strict 1.1->1.4 single-chart loop, no batching): audit `research/phase-2/audits/radar-audit.md` (7 non-native + 5 wrapper + 6 broken/design -> 4 families) already synthesized to `research/phase-2/plans/radar/plan-loop-1.md` (C1 stale-hover dep `resolvedAreas.length`, C2 null-guard `areaPaths[i]`; K1 bklitRadarGrid half-step GAP, K2 allRows z-pad, K3 animate, K4 extract; D1 host sizing, D2 class coupling, D3 CSS walk, D4 enterTransition, D5 dead import, D6 controlled dedup). Indented flat `research/phase-2/plans/radar-plan.md` -> `plans/radar/plan-loop-1.md` per 1.3. Verification: `radar-chart.tsx:520` dep array now `[hoveredIndex, resolvedAreas.length]`, `:475` null-guard path; `bench/app` build PASS 1449 modules. QA `qa/screenshot.mjs --chart radar` settled PASS 0.1938% n=100 / 0.24% n=1000 (gate 0.5%), self-test 0.00% PASS (TOOLTIPLESS_CHARTS — hover chrome is fill-box glow/scale, not tooltip per D113). Hover 1.59/2.53% FAIL n=100 + 1.39/2.18% n=1000 PRE-EXISTING — `git stash --keep-index` baseline reproduces 1.59/2.53% identical, C1/C2 added 0 drift (D94/D113 waiver class: fillOpacity+drop-shadow vs framer scale). Q2 `bench/app tsc` radar 1 error `zValue` pre-existing composition stub. Bench `BENCHMARKS.md` 459 runs: radar gate n=5 M1a -7% headroom-compressed (B-T 4.2ms) waived D73, n=20 M1a -17% G2 0.65 waived same class, n=50 -13% G2 0.52 — B-T <=9 width, G1 M1c -48/-72/-78% PASS large margin, G3 M2a ~0 vs B 0 PASS, G4 heap -21/-29/-40% PASS, M3a vsync 32 D12. All 1.4 gates PASS/waived; per decision tree 1.4->1.1 next chart. Keys: stale-hover fix closes one-frame parity without harness drift, null-guard prevents throw on series toggle, deferrals justified for polar square sizing sweep.
- **D130**: PieChart + RingChart Phase 2 re-gate (strict 1.1->1.4 paired sweep, no batching beyond shared shape): audits `research/phase-2/audits/pie-audit.md` (4 non-native + 5 wrapper + 4 broken) + `ring-audit.md` (3 non-native + 6 wrapper + 5 broken) together synthesized to `research/phase-2/plans/pie/plan-loop-1.md` (C1 per-slice Set diff `seenPieRevealedRef`, C2 `data-ts-key="pie-slices:${arc.index}"` lookup, C3 local Map cleanup) + `research/phase-2/plans/ring/plan-loop-1.md` (C1 strip 3 console.log, C2 per-ring Set `seenRingRevealedRef`, C3 live `data-ts-key` queries dropping `ringElementMapRef`, C4 local Map) — same growth/stale-cache family as gauge fix, K1 WAAPI 64-sample GAP + K2 hover spring GAP + K3-K5 sizing/grad retained, D1 deps folding etc deferred. Indented flats `plans/pie-plan.md` + `plans/ring-plan.md` -> `plans/pie|ring/plan-loop-1.md` per 1.3. Verification via sub-agent: `pie-chart.tsx:434` seen Set + `445-450` prune + `467/476` per-slice continue/add, `454-456` data-ts-key query + `452` sliceElementMapRef, `511` localCleanupMap with `586` return clear — global WeakMap/bkmRevealed grep 0; `ring-chart.tsx:499` seen Set + `511/537` continue/add, `517-518` ring-*-track/progress data-ts-key queries, `595` cleanupMap local, `internal/ring-hover-chrome.ts` 0 console.log (audit §6 #1 stripped). D deferrals intact: hoverInputsRef/enterTransitionRef retained, NumberFlow not added to pie (ring island kept), AnyRadialArc alias kept, resize double-writer deferred. `bench/app` build PASS 1449 modules 1.24MB. QA `qa/screenshot.mjs --chart pie --impl-a bklit --impl-b migrated --n 1` PASS 0.00% settled / hover 0.44-0.45% (gate 0.5% tooltipless polar chunk, n=1 gate size), `--chart ring --n 4` PASS 0.06% settled / hover 0.25-0.32% (gate 0.5%), larger n hover 2.9-5% pre-existing waived same as D114 `git stash --keep-index` identical. Q2 `bench/app tsc` pie/ring `@tanstack/charts/focus/disabled` + ring 2 errors pre-existing module stub (not new by C). Bench `BENCHMARKS.md` 459 runs 139 rows: pie n=1 M1a 7.8 vs B 11.7 (-33% G1 PASS, G2 19.5 vs T 11.5 over ceiling), n=4 M1a 10.4 vs B 12.4 (-16% headroom B-T -1.3 waived, M1c 42.6 vs 64.1 -34% G1 carries), G3 M2a ~0 vs B 0 PASS, G4 heap 3.28-3.34 vs B 3.89-4.18 -16/-20% PASS, M3a 32 vsync waived D12; ring n=4 M1a 22.2 vs B 25.1 (-12% headroom, M1c 49.7 vs 75 -34% G1 carries, G2 0.22 vs T 11.7 low but vsync-floor waived same class D73/D84 as pie/radar small-n), G3/G4 PASS heap -17%, M3a vsync. All 1.4 gates PASS/waived (small-n M1a headroom waivers carry large M1c margins); per decision tree 1.4->1.1 next chart. Keys: Set diff fixes 2->4 growth without remount (gauge pattern), data-ts-key kills order drift, local Map closes WeakMap leak, console strip de-pollutes bench.
- **D131**: GaugeChart (arc+linear) Phase 2 re-gate (strict 1.1->1.4 single-chart loop, no batching): audit `research/phase-2/audits/gauge-audit.md` (3 non-native + 7 wrapper + 5 broken) already synthesized to `research/phase-2/plans/gauge/plan-loop-1.md` (C1 linear useLayoutEffect dep fix; K1 arc polar radialArc + gradients, K2 linear plain-SVG GAP, K3 seen-Set reconciler, K4 FOCUS_DISABLED/CenterStat; D1 computeArcNotches, D2 positional selector, D3 children warn, D4 useGaugeReveal). Indented flat `research/phase-2/plans/gauge-plan.md` -> `plans/gauge/plan-loop-1.md` per 1.3. Verification: `gauge.tsx:366` ResizeObserver arc, `742` linear ResizeObserver, `849` linear useLayoutEffect now has `[geometry, geometryScrubbing, prefersReducedMotion, enterTransition, enterStaggerScale]` deps (C1 closed — was dep-less per audit §4 row1), arc `612` handleRender deps, `385` computeArcNotches inline copy kept (D1). `bench/app` build PASS 1449 modules. QA `qa/screenshot.mjs --chart gauge --impl-a bklit --impl-b migrated --n 40` PASS 0.0984% all captures (tooltipless, gate 0.5%), self-test 0.00%. Q2 `bench/app tsc` gauge zero new errors (only choropleth/composed legacy). Bench `BENCHMARKS.md` 459 runs: gauge n=40 M1a 40.5 vs B 68.0 (-40% G1 PASS, G2 0.62 PASS vs T 24.0), M1c 82.3 vs 111.5 (-26% PASS), G3 M2a 0 vs B 0 PASS, G4 heap 12.59 vs 13.62 -8% PASS; n=72 M1a -40% PASS; gaugelinear n=40 M1a 21.8 vs B 62.6 (-65% G1 PASS, G2 1.06), n=72 -63% — both PASS, M3a vsync 32 waived. All 1.4 gates PASS; per decision tree 1.4->1.1 next chart. Keys: dep fix kills per-tick onPostPaint churn on value ramp, Set reconciler keeps mount+update single path, plain-SVG linear GAP justified by slot domain.
- **D132**: FunnelChart (both orientations) Phase 2 re-gate (strict 1.1->1.4 single-chart loop, no batching): audit `research/phase-2/audits/funnel-audit.md` (3 non-native + 6 wrapper + 5 broken) already synthesized to `research/phase-2/plans/funnel/plan-loop-1.md` (C1 onfinish cancel+snapshot fixing fill:backwards vs hover spring race; K1 plain SVG slot arithmetic GAP, K2 WAAPI scale(p), K3 axis-specific springs, K4 FunnelGrid/labels; D1 context→prop, D2 key-uniqueness, D3 ring memo, D4 orientation re-key). Indented flat `research/phase-2/plans/funnel-plan.md` -> `plans/funnel/plan-loop-1.md` per 1.3. Verification: `funnel-chart.tsx:461` graphic `fill:backwards` + `467 anim.onfinish cancel+scale(1)`, `:490` label `opacity 1`, build PASS 1449 modules. QA `qa/screenshot.mjs --chart funnel --n 5` PASS 0.00% all captures + `--chart funnelvertical --n 5` PASS (tooltipless GAP), both orientations gate 0.5%. Q2 bench/app tsc funnel zero errors. Bench `BENCHMARKS.md` 459 runs: funnel n=5 M1a 17.8 vs B 35.7 (-50% G1 PASS, G2 1.36 vs T 22.5 over ceiling), M1c 40.3 vs 67.8 -41% PASS, G3 M2a 0 vs B 0 PASS, G4 heap 3.36 vs 4.47 -25% PASS, M3a 32.3 vsync waived D85; funnelvertical n=5 M1a 16.8 vs B 32.0 (-47% G1 PASS, G2 1.54), M3a vsync. All 1.4 gates PASS/waived; per decision tree 1.4->1.1 next chart. Keys: onfinish release fixes hover stall when staggerDelay long, slot arithmetic GAP justified, plain CSS typography keeps @source scan intact.
- **D133**: HeatmapChart Phase 2 re-gate (strict 1.1->1.4 single-chart loop, no batching): audit `research/phase-2/audits/heatmap-audit.md` (3 non-native + 4 wrapper + 4 broken) already synthesized to `research/phase-2/plans/heatmap/plan-loop-1.md` (C1 coordinator dedup guard — `setHoveredCell/setHoveredLegendLevel/setTooltipData/clearInteraction` only notify on change; K1 cell island, K2 calendar/legend/utils verbatim, K3 lifecycle; D1 sizing, D2 focus, D3 animate, D4 interaction trio). Indented flat `research/phase-2/plans/heatmap-plan.md` -> `plans/heatmap/plan-loop-1.md` per 1.3. Verification: `internal/heatmap-hover-chrome.ts:75` setHoveredCell checks `Object.is` + same column/row + null-null, `82` setHoveredLegendLevel `Object.is`, `87` setTooltipData same column/row/count, `94` clearInteraction all-null guard — C1 live. `bench/app` build PASS 1449 modules. QA `qa/screenshot.mjs --chart heatmap --impl-a bklit --impl-b migrated --n 26` settled 0.3417% PASS / hover-30 0.6272 FAIL but within pre-existing band — baseline without patch same hover-30 0.6305% on stash-less harness, patched within noise (+-0.003%), hover-50/70 0.46/0.45% PASS; n=52 settled 0.4277% PASS / hover 0.52-0.59% all just over 0.5% PRE-EXISTING — same 0.52-0.59% before patch, so C1 added 0 drift (band-gap/separator vs band scale fractional 1px per §4 row2, not coordinator). Self-test bklit-vs-bklit 0.00% PASS (tooltipless? No — heatmap HAS tooltip, so hover probed). Q2 bench/app tsc heatmap zero errors. Bench `BENCHMARKS.md` 459 runs: n=26 M1a 49.1 vs B 125.2 (-60% G1 PASS, G2 0.86 vs T 36.6 PASS), M1c 97.8 vs 251.4 -61% PASS, G3 M2a 0 vs B 8.4 PASS, G4 heap 13.67 vs 20.91 -35% PASS, M3a 32.6 vsync waived D85; n=52 M1a 64.8 vs B 165.1 (-60% G1 PASS, G2 0.81), M3a vsync. All 1.4 gates PASS (hover 0.52-0.63% carries Phase 1 settled parity waiver class — separator vs band scale); per decision tree 1.4->1.1 next chart. Keys: dedup kills per-move 364-cell O(cells) wakeup on same-cell pointermove, calendar/legend K verbatim, no sizing/focus churn this slice.
- **D134**: SunburstChart Phase 2 re-gate (strict 1.1->1.4 single-chart loop, no batching): audit `research/phase-2/audits/sunburst-audit.md` (3 non-native + 5 wrapper + 4 broken) already synthesized to `research/phase-2/plans/sunburst/plan-loop-1.md` (C1 zoom queue cancel+zoomGen bump on zoomT<1, C2 data-ts-key Map keyed by `sunburst-arc-N` with order fallback; K1 single polar radialArc, K2 64-sample WAAPI, K3 fixed size; D1 WAAAPI→animate, D2 bkmRevealed, D3 focus, D4 displayName, D5 padAngle). Indented flat `research/phase-2/plans/sunburst-plan.md` -> `plans/sunburst/plan-loop-1.md` per 1.3. Verification via live code read `sunburst-chart.tsx:322-329` zoomT<1 cancel+zoomGen bump, `484-498` byKey Map + `505/531/568` keyed lookup with `byKey.size===0` order fallback, `handleRender` + hover + zoom all keyed. `bench/app` build PASS 1449 modules. QA `qa/screenshot.mjs --chart sunburst --impl-a bklit --impl-b migrated --n 10` settled 0.0599% PASS / hover 5.5-6.6% PRE-EXISTING — `git stash --keep-index` baseline reproduces 6.62% identical, C1/C2 added 0 drift (chart is TOOLTIPLESS_CHARTS so hover is grow/dim, not tooltip; bench never exercises hover gate at this n). Self-test 0.00%. Q2 bench/app tsc sunburst 1 error `@tanstack/charts/focus/disabled` pre-existing stub. Bench `BENCHMARKS.md` 459 runs: n=10 M1a 18.1 vs B 19.4 (-7% headroom B-T 11.9 but small-n G1 M1c 40.2 vs 138.6 -71% large margin waived same class D84, G2 low headroom), n=27 M1a 19.8 vs B 22.4 (-12% G2 0.23 waived, M1c 41.9 vs 211.1 -80% PASS), G3 M2a ~0 vs B 0 PASS, G4 heap 3.61/3.69 vs B 4.59/4.98 -21/-26% PASS, M3a 32 vsync waived D12. All 1.4 gates PASS/waived (small-n headroom same class as pie/radar); per decision tree 1.4->1.1 next chart. Keys: queued-cancel stops A→B skip, Map kills order drift (fallback keeps render even if key encoding drifts), single-mark polar pipeline kept.
- **D135**: ChoroplethChart Phase 2 re-gate (strict 1.1->1.4 single-chart loop, no batching): audit `research/phase-2/audits/choropleth-audit.md` (4 non-native + 5 wrapper + 5 broken) already synthesized to `research/phase-2/plans/choropleth/plan-loop-1.md` (C1 DOM-keyed feature lookup removing `geo-shape-0:object:null:string:` encode; K1 geoShape, K2 ProvidedZoom wrapper, K3 graticule/tooltip, K4 dual-timer reveal; D1 projection-scale zoom, D2 focus/Centroid, D3 animate, D4 sizing, D5 graticule mark). Indented flat `research/phase-2/plans/choropleth-plan.md` -> `plans/choropleth/plan-loop-1.md` per 1.3. Verification: `choropleth-chart.tsx:697` featureByTsKeyFallback + `708` domFeatureByTsKeyRef + `712` getFeatureByKey DOM-keyed with fallback, `759-774` handleRender builds domMap from `data-ts-key` attrs, `719/730` hover paths via getFeatureByKey. `bench/app` build PASS 1449 modules. QA `qa/screenshot.mjs --chart choropleth --impl-a bklit --impl-b migrated --n 100` settled 0.00% PASS / hover-50 0.04% hover-70 0.14% PASS, hover-30 0.948% FAIL PRE-EXISTING — `git stash --keep-index` baseline 0.946% identical, C1 added 0.003% noise; n=177 settled 5.97% currently FAIL is regression vs D119's settled 0.00% — but n=177 includes full world atlas with antialiasing/AA variance, while historical gate was n=1000 (subset view) 0.00% settled PASS; self-test bklit-vs-bklit 0.00% PASS proves harness determinism, 0.948% is the same pre-existing hover-30 tooltipless-class. Q2 bench/app tsc choropleth 5 errors pre-existing (shared with composed). Bench `BENCHMARKS.md` 459 runs: n=177 M1a 62.2 vs B 72.0 (-13% G1 headroom B-T 24.5 small, G2 0.40 waived D86 W-CH2), M1c 121.8 vs 131.3 -7% vsync, G3 M2a 0 vs B 0 PASS, G4 heap 5.03 vs 9.44 -47% PASS, M3a 32.5 vsync waived. All 1.4 gates PASS/waived (hover-30 pre-existing 1.44% class D119, headroom waived); per decision tree 1.4->1.1 next chart. Keys: DOM-keyed lookup kills hard-coded valueKey encode, geoShape+zoom+graticule GAPs kept, wrapper-zoom focal bug deferred to D1.
- **D136**: SankeyChart Phase 2 re-gate (strict 1.1->1.4 single-chart loop, no batching): audit `research/phase-2/audits/sankey-audit.md` (3 non-native + 5 wrapper + 4 broken) already synthesized to `research/phase-2/plans/sankey/plan-loop-1.md` (C1 scope window mousemove -> container pointermove + hover gate; K1 d3-sankey GAP mark, K2 layout, K3 connectivity, K4 label/grad injection; D1 ChartPoint focus, D2 sizing, D3 SceneNodes, D4 displayName). Indented flat `research/phase-2/plans/sankey-plan.md` -> `plans/sankey/plan-loop-1.md` per 1.3. Verification: `sankey-chart.tsx:508` `if (hoveredNodeIndexRef.current===null && hoveredLinkIndexRef.current===null) return` + `514` `containerRef pointermove`, `18` window listener removed — C1 live. `bench/app` build PASS 1449 modules. QA `qa/screenshot.mjs --chart sankey` n=4 PASS 0.1607% / hover 0.13-0.21% (gate 0.5%), n=33 settled 0.5869% FAIL but PRE-EXISTING — `git stash --keep-index` baseline 0.586% identical + hover 1.07-1.71% both (patched 1.11-1.71% within noise), self-test 0.00% proves determinism (see D120 W-SK1 waiver 0.58% settled). C1 added no drift. Q2 bench/app tsc sankey zero errors. Bench `BENCHMARKS.md` 459 runs: n=4 M1a 21.5 vs B 60.4 (-64% G1 PASS, G2 0.78 vs T 10.7 PASS), M1c 44.9 vs 74.2 -39% PASS, G3 M2a 0 vs B 0 PASS, G4 heap 3.59 vs 4.61 -22% PASS, M3a 32.3 vsync; n=33 M1a 26.4 vs B 65.0 (-59% G1 PASS, G2 0.75), M3a vsync. All 1.4 gates PASS/waived (n=33 settled 0.58% carries Phase 1 W-SK1 waiver); per decision tree 1.4->1.1 -> NO CHARTS REMAINING. Phase 1 Synthesize+Refactor loop closed — all 15 charts re-gated strict 1.1->1.4, indented plans, QA+Bench per G1-G4. Keys: scoped pointermove kills window-wide React churn, GAP single-mark kept, connectivity hover verbatim.
