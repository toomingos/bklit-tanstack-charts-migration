# Phase 7 — Benchmarks

Filled at 7.5 by `bench/report.mjs` and the V4.5 explained columns (DOM node count, active animation count per scenario). Until then the reference is `bench/results/latest.json` and the phase-6 tables in `archive/phase-6/docs/BENCHMARKS.md` (D503).

Bundle: V5.1 adds a second column vs `bklit/<cell>` at ≤ 1.10 and a CSS column (`bench/measure-css.mjs`). Predicted at V5.2: arealoading 114.5 → ~87 kB gzip vs bklit 88.7.

<!-- BEGIN GENERATED BENCHMARK TABLES -->
<!-- END GENERATED BENCHMARK TABLES -->
